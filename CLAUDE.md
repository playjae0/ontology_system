# CLAUDE.md — 온톨로지 시스템 구현 (국면 1)

> 이 파일은 매 세션 자동 로드된다. 상세 문서는 전부 `docs/`에 있다(목록·경로는 `docs/MANIFEST.md`).
> **세션 기준: 감사 4 통과(07-31, 10회차) + raw mock·실행 검증 1차(08-01, 11회차) + 쟁점 A 판정(08-06, 12회차) + 반입 대조 9건 판정(08-07, 13회차).**

## 정본 관계 (충돌 시 이 순서)

1. **규범 킷**: `docs/00_틀_확정본.md`(**v2.4**) · `docs/00_불변식_카드.md`(**v11**) · `docs/00_용어_대장.md`(08-06판) — 최상위. 카드는 압축 검사기이며 챕터·명세가 카드와 어긋나면 그 자체가 결함 신호다.
2. **챕터**(docs/CH2·CH3A·CH3B·CH4·CH5·CH6) — 서술 정본. CH5·CH6는 **v3**.
3. **1차 입력물**: `docs/ontology_system_구현용문서.md`(v1.16) + `docs/증분0_구현.md`(**v1.3 + 아래 v1.4 정정 5항**) — **겹치면 증분 0이 이긴다.**
   - 구현문서 v1.16은 **의도적으로 구판이다.** 증분 0이 그 delta 문서이고, 같은 사실을 두 곳에 두면 반드시 어긋난다. v1.17 동기화는 코드가 돌아간 뒤.
   - 구현문서·명세를 읽을 때는 **증분 0 §6의 판독 증분을 겹쳐 읽는다.**
4. 파서 트랙: `docs/파서_명세.md`(v0.5.1) §3·§5~§9.
5. **파생 문서 3종**(가결정의 발의 원천): `docs/raw_mock_대장.md`(**v2.1**) · `docs/판정대조_ipqc_1차.md`(v1.0) · `docs/실행검증_1차.md`(**v1.0**).

> **발의와 정본의 구분**: 파생 문서는 **가결정을 발의하는 원천**이고, 그 가결정이 증분0·챕터에 채택된 순간부터 **채택본이 정본**이다. 파생 문서의 "이 문서가 원천" 선언은 발의권이지 개정권이 아니다 — 겹치면 증분0·챕터가 이긴다.

## 증분 0 v1.4 정정 5항 (13회차 판정 — 문서보다 이것이 우선한다)

1. **DECISIONS.md 초기값 = 31건**(구 "25건"은 계수 오기). D-1~D-14 = 증분0 §7 / D-15~D-21 = raw_mock_대장 §6 / D-22~D-25 = 판정대조 §8 / **D-26~D-31 = 실행검증_1차 §6**.
2. **킷은 6종**(5종 + 실행 하네스 `run_adapter.py` — D-27). 구 "5종" 서술은 낡음.
3. **§4.1은 "마이그레이션"이 아니라 "신규 작성"이다.** 레포에 `mock/parsed/`가 없다 — 구현문서 §6의 mock 전문을 기준으로, §4.1의 4항(source_locator 개명·adapter_version 추가·provenance 표기·CP01 11행)을 적용한 상태로 새로 만든다.
4. **CP01은 parsed·raw 모두 11 record가 prefix다.** raw `CP01.xlsx`의 11번째 record 자리에 coord_mismatch 행(공정구분 "스태킹" + 공정명 "노칭")을 삽입하고 확대분을 C12~C29로 민다. **parsed에만 있고 raw에 없는 행은 D-18(역산 정합)을 무효화한다.**
5. **fixture의 정의**: "미리 만든 정답"이 아니라 **"외부 세션에서 실제 LLM이 산출한 결과물의 스냅샷"**(D-26). 사람이 손으로 쓴 fixture는 리허설을 무효화한다 — `mock/fixtures/`의 adapter·schema가 그 실물이다.

## 진행 규칙 (대장 §5.6 + 증분 0 §1)

- **카드·계약과 충돌 = BLOCKERS.md 기록 + 멈춤** → 의존 없는 다음 작업으로 이동. 추측으로 메우지 않는다. **BLOCKERS 항목에는 판단 재료 3요소(①근거 원문 ②선택지 2~3 ③각 귀결)를 첨부한다.**
- **그 외 세부 = 가결정 + DECISIONS.md 기록 + 전진.** 형식: `[D-##] 지점 / 가결정 / 근거 / 확정 경로`.
- **PROGRESS.md**: 단위 완료·테스트 결과·`git diff core/` 상태·소요. 구간 종료 시 갱신 + 커밋 1회.
- **단위 완료 = 배정된 mock 시나리오(S1~S15) 전부 통과.** 품질 측정은 완료판정이 아니다. 테스트가 통과해도 판정 문구와 어긋나면 판정이 이긴다.
- **등록 산출물(스키마·어댑터)의 판정은 문면이 아니라 실행 결과로 한다**(실행검증_1차 §1). 검수 뷰(사람) 앞단에 실행 하네스(기계 관문)가 선다.
- 구현 불변 규칙은 구현문서 §0 (core에 층 어휘 0 / 명료함 > 짧음 / 전 생성물 status+provenance / CLI+파일 / USE_MOCK=1 기본).
- **저장 계층(틀 §4B-A8, 카드 B6)**: graph.json 접근은 `core/graph.py`(GraphStore)만 — 다른 모듈이 직접 열면 그 자체가 결함이다. 직렬화 orjson(표준 json 폴백). 층별 200MB 또는 build 30초 초과 = R10 판정 개시(계기판 7·8).
- **의미 축 id는 ULID다 — 발효 확정(13회차).** A0 계수 ②=0이라 마이그레이션 대상이 없다. 처음부터 ULID로 짜고 `id_seq.json`은 만들지 않는다.

## 착수 순서

1. **A0(G0) — 최선행**: `docs/구간별_프롬프트.md` 정지점 1.
2. 배열은 **증분 0 §2**: A트랙(**G1 st·n1·n2** → G2 n10 → G3 1c′·1d′·n3·n4 → G4 → G5 n5 → G6) ∥ P트랙(P1 n7 → P2 n8 → P3 n6). 결합은 mock 계약 JSON뿐.
3. **정지점은 4개: G0 / G1+G2 / G3★ / G4.** G5·G6은 연속, P트랙은 병렬(단위별 중간 보고).

## 읽기 주의 (낡은 서술 방어용 — 감사 4 + 11~13회차)

- **큐 kind는 CH3B 3.7 표(13종)가 아니라 증분 0 §6-5가 현행**: 17종 확정 + 3종 승인 = 20종. CH3A 3.4의 "택일 대기"도 낡음(D-3).
- **CH6 6.2 n9 행의 "(D-1)" 앵커는 오기** — 정본은 파서_명세 §5 ⑴-C″ / 카드 C15.
- **n10 부트스트랩의 골격 노드 status는 `seed`.**
- **record_id의 occ = 동일 join 문자열의 출현 순번.** join 순서 = 매칭 스키마 fields **선언 순서**(D-14).
- **계기판 8종 · mock 15종 · CP01 11 record가 현행.** 구판(5·6종 / 13종 / 9·10행) 서술은 낡음. **구현문서 §6.1 "records 12행"은 오기(실제 13행) · §6.2 "6행"도 오기(실제 9행).**
- **의미 축 id의 N#### 전역 시퀀스·id_seq.json 서술은 구판** — 현행은 ULID.
- **파서_명세 §9 "킷 5종"은 D-27로 6종, §5 `expects`는 D-29로 `header_labels` 필수 키 추가** — 문서 개정 전이므로 **DECISIONS 기준으로 전진한다.** `columns`는 출력 필드명 매핑이고, 양식 표류 대조는 `header_labels`(원본 헤더 문자열 배열)로 한다. 두 관심사를 한 키에 섞으면 preflight가 무력화된다.
- **UNMAPPABLE로 판정한 열은 스키마 `fields`에 넣지 않고 어댑터 출력에서도 제외한다**(CH2 2.6 규약 5 · D-30). 인입 코드는 알 수 없는 role에 `KeyError`로 죽지 말고 **`invalid_role` 결함 로그**를 남긴다.
- **`section`·`electrode_type`은 role 배정 대상이 아닌 구조 필드로 다룬다**(D-31·D-1 — 둘 다 가결정, 확정은 세션 1/4).
- **구현문서·명세의 조각 필드 `chunk_id`는 `source_locator`로 읽는다** — 정본 chunk_id·record_id·doc_hash는 에이전트가 계산(카드 C7).

## 문서가 없어 막히면

`docs/`에 없는 문서가 필요해 결정이 안 되는 지점을 만나면 **추측하지 말고 BLOCKERS.md에 기록**하고 사용자에게 요청한다. 프로젝트(claude.ai RAG)에 전량 있다.
