# CLAUDE.md — 온톨로지 시스템 (국면 2)

> 매 세션 자동 로드된다. **명세는 `docs/spec/` 11종뿐이다.**
> 현재 위치: **국면 1 완료**(태그 `phase1-complete`) · 국면 2A 진행 — 명세↔코드 갭 58건(`docs/2A_작업목록.md`) 수리와 실 모델 연결.

## 1. 명세는 정제본뿐이다 [국면 2 신설 규율]

`docs/spec/` 11종이 **유일한 정본**이다 — README · 0 기반과원칙 · 1 금지와불변 · 2 계약 ·
3 구조 · 4 쓰기절차 · 5 읽기절차 · 6 파서와구축모드 · 7 구현규격과검증 · 부록_용어 · 개정대장.

- 옛 챕터군(`CH1~CH7`·`00_틀_확정본`·`00_불변식_카드`·`00_용어_대장`·`증분0_구현`·
  `파서_명세`)과 배경 구명세는 정제본으로 통합돼 **제거됐다.** 되돌아갈 좌표는 태그
  `phase1-complete`다.
- **제거된 문서를 git history에서 꺼내 명세로 쓰지 않는다.** 정제본이 답하지 못하면
  그것이 결함이고, 아래 2로 처리한다. (열람 자체를 막는 규율이 아니다 — 근거로 인용하지
  않는다는 규율이다.)
- `DECISIONS.md`는 남는다 — 판단 이력이고 정제본 README가 명시적으로 참조한다.
- **충돌 시 순서**: 문서 1(금지와불변 — 압축 검사기) > 나머지 정제본 > 자산 파일
  (골격 seed·config·스키마·킷의 실물은 자산이 정본이다).

## 2. 명세에 없으면 추측으로 메우지 않는다 — 멈추고 신고한다

**가장 중요한 규율이다.** 구현 중 명세가 답하지 않는 지점을 만나면:

1. **그 자리를 만들지 말고 멈춘다.** "당연히 이렇게 하겠지"로 채운 순간 그 공백은
   영영 검출되지 않는다.
2. `BLOCKERS.md`에 **판단 재료 3요소 + 선택지 2~3**을 적는다 — ①근거 원문(정제본 어느
   절이 무엇까지 말하는가) ②선택지 2~3(각각 어떻게 구현되는가) ③귀결(선택에 따라 무엇이
   달라지는가).
3. 다른 작업으로 넘어간다. 막힌 항목은 미완으로 두고 보고한다.

과거에 반대로 한 결과가 국면 2를 만들었다 — 명세가 요구한 `core/llm.py`가 누락된 채
아무도 신고하지 않아 **LLM 연결이 통째로 없는 시스템이 완료판정을 통과했다.**

**이미 알려진 공백은 `docs/국면2_명세공백_인계.md`에 있다** — 신고 전에 대조한다.
그 목록에 있으면 새 공백이 아니다.

## 3. 실행으로 판정한다

- **주석·docstring·문자열을 구현으로 세지 않는다.** "훅 5곳"이 전부 주석이었고 자동
  점검이 문자열 `"HOOK"`을 세어 있는 것처럼 보고한 실사고가 있다.
- **정의만 있고 호출부가 0인 함수는 구현이 아니다.**
- 완료 보고에는 **실행 결과**를 붙인다 — "고쳤다"가 아니라 "돌려서 이렇게 나왔다".
- **검사 — 실물 대조**: 코드의 동작·경로·파일명·현재 상태를 **단정하는 문안은 발행 전에
  해당 코드·config의 전문을 연다.** 절단된 출력·요약·타 문서의 보고로 대체하지 않는다.
- **등록 산출물(스키마·어댑터)의 판정은 문면이 아니라 실행 결과로 한다.** 검수 뷰(사람)
  앞단에 실행 하네스(기계 관문)가 선다.

## 4. 진행 규칙 (명세 문서 7 §7.4 「진행 규칙」)

- **카드·계약과 충돌 = `BLOCKERS.md` 기록 + 멈춤** → 의존 없는 다음 작업으로 이동.
  추측으로 메우지 않는다. 항목에는 3요소 + 선택지 2~3을 첨부한다.
- **그 외 세부 = 가결정 + `DECISIONS.md` 기록 + 전진.**
  형식: `[D-##] 지점 / 가결정 / 근거 / 확정 경로`.
- **번호 발번처는 이 레포의 `DECISIONS.md` 하나다.** 외부 세션이 번호를 지정해 오면
  **말번 뒤로 재번호**한다. **`PROGRESS.md`·보고·커밋 메시지에 채번 전 번호를 적지
  않는다** — 미러는 보고이고 장부는 하나다(실사고).
- **명시적 실패**(config로 표현되지 않아 core가 시끄럽게 실패하는 지점)는 raise로
  드러내는 데 그치지 않고 **장부에도 병기**한다.
- **회귀 판정 기준은 클린 상태 단독 실행이다** — 연속 실행의 순서 의존 flake는 판정에
  넣지 않는다. 클린은 `run.py init --fresh`가 만든다.
- `PROGRESS.md` 내용: 단위 완료·테스트 결과·`git diff core/` 상태·소요.

## 5. 구현 불변 규칙 (문서 1 · 문서 7 §7.1)

- **core에 층 어휘 0** / 명료함 > 짧음 / 전 생성물 status+provenance / CLI+파일 /
  USE_MOCK=1 기본.
  - 등재 예외 2건: ①`electrode_type`(파서 계약 필드) ②`polarity`·`tier`(시스템 어휘).
    축의 값(`cathode`/`anode`)은 코드에 넣지 않고 seed `AXIS_LABELS`의 키에서 읽는다.
    코드가 아는 것은 **구문 마커 4종**(`::` 접두 · `@split` · `@unordered` · `@noflow`)뿐이다.
- **core 접근 경계 — 단일 진입 모듈 3종**(문서 7 §7.1): `core/graph.py`(GraphStore —
  의미 축 id 발급 소유) · `core/dictionary.py`(사전 register/lookup) ·
  `core/matcher.py`(`match(surface, candidates, category)`). **경계 밖에서 직접 열지
  않는다** — 경로 조립도 경계 안이다.
- **골격 심기의 자리는 `core/skeleton.py`다** — 파생이 loader에 섞이면 관계 이름을
  config에서 받는 통로가 갈린다.
- **게이트웨이 2파일**: `core/llm.py`(LLM 설정 접근이 이 파일로 수렴) ·
  `core/embeddings.py`. 호출부가 설정을 직접 읽지 않는다.
- **저장 쓰기는 원자적(tmp+`os.replace`) + 파일 락**이다. 층별 200MB 또는 build 30초
  초과 = R10 판정 개시(계기판 7·8).
- **의미 축 id는 ULID**다. `id_seq.json`은 만들지 않는다.
- **큐 kind는 닫힌 20종**이다 — 새 kind를 임의로 만들지 않는다. 처리 주체가 "재빌드"인
  항목에 큐를 파지 않는다.
- **파생물에서 그래프를 수정하는 경로는 없다**(Cypher·CSV·Mermaid·HTML). 고치는 것은
  I축 도구(`run.py ops`)뿐이다.
- 런타임: **Python 3.10+** · 표준 라이브러리 우선(logging 포함) · 코어 필수 외부 의존 0 ·
  선택 의존은 **지연 import로 격리**(USE_MOCK=1 경로에서 import되지 않는다).
- 실행은 프로젝트 루트에서 `python -m cli.{진입점}` 또는 `python run.py <명령>`.
  **`sys.path` 조작으로 붙이지 않는다.**

## 6. 착수 순서

1. 작업 목록은 `docs/2A_작업목록.md`(갭 58건 — 등급·규모별), 국면 계획은
   `docs/국면2_계획.md`. **요청문 없이 임의 착수하지 않는다.**
2. 착수 전 **전제 대조** — 요청문의 사실 전제를 실물로 확인하고 어긋나면 멈추고 보고한다.
3. 산출물이 어디에 떨어지고 어떻게 보는지는 `산출물_지도.md`,
   사내 반영 수준은 `구현현황.md`, 클론의 지금 상태는 `python doctor.py`.

## 7. 문서가 없어 막히면

정제본이 답하지 않아 결정이 안 되는 지점은 **추측하지 말고 `BLOCKERS.md`에 기록**하고
사용자에게 요청한다. 정제본 개정은 이 세션이 하지 않는다 — **무엇을 어떻게 고쳐야 하는지만
보고하고, 개정은 허브가 한다.**
