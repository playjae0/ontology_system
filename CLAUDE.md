# CLAUDE.md — 온톨로지 시스템 구현 (국면 1)

> 이 파일은 매 세션 자동 로드된다. 상세 문서는 전부 `docs/`에 있다(목록·경로는 `docs/MANIFEST.md`).
> **세션 기준: 대장 §4 검사 로그 18회차 (2026-08-13).** 진행 상태는 `PROGRESS.md`, 장부는 `DECISIONS.md`(현행 **55건**)·`BLOCKERS.md`(멈춤 0 · 판정필요-5만 개방 — 판정은 났고 구현이 G4 0번).
> **현재 위치: G0~G3 완료 · M2 반입 0~4단계 완료 · M2 후속(G4 전 정리) 완료. 다음은 G4이며 별도 승인 대기다.**

## 판본은 여기 적지 않는다 [18회차 신설 규칙]

숫자를 두 곳에 두면 반드시 어긋난다. 실제로 이 파일이 틀 v2.4·카드 v11·증분0 v1.3을 정본으로 선언한 채 6일을 갔고, 그 사이 실물은 v2.7·v14·v1.4가 되어 있었다.

**판본의 단일 기준점은 `docs/00_챕터_대장.md`다.**

- 각 챕터·파생 문서의 현행 판본 = 대장 **§1** (챕터 헤더의 `파일 vN` + 파생 문서 표)
- 대장 자신이 최신인지 = 대장 **§4 검사 로그의 마지막 회차 번호**
- 어떤 문서의 판본이 궁금하면 그 파일 머리의 `version:` 줄을 **대장 §1과** 대조한다

> ⚠ **판번호가 같아도 본문이 다를 수 있다.** 실례: `증분0_구현.md`가 프로젝트·레포 양쪽 `v1.4`인데 레포 사본은 §0 기준이 구판인 딴 문서였다(131줄 차이 — 18회차 검출). 대량 개정 뒤에는 판번호가 아니라 **§0·머리말의 기준 판본 줄**을 본다.

## 정본 관계 (충돌 시 이 순서)

1. **규범 킷**: `docs/00_틀_확정본.md` · `docs/00_불변식_카드.md` · `docs/00_용어_대장.md` — 최상위. 카드는 압축 검사기이며 챕터·명세가 카드와 어긋나면 그 자체가 결함 신호다.
2. **챕터**(docs/CH2·CH3A·CH3B·CH4·CH5·CH6·CH7) — 서술 정본.
3. **1차 입력물**: `docs/ontology_system_구현용문서.md` + `docs/증분0_구현.md` — **겹치면 증분 0이 이긴다.** 단 **아래 「증분 0 미반영분」이 증분 0보다 우선한다.**
   - 구현문서는 **의도적으로 구판이다.** 증분 0이 그 delta 문서이고, 같은 사실을 두 곳에 두면 반드시 어긋난다. 동기화는 코드가 돌아간 뒤.
   - 구현문서·명세를 읽을 때는 **증분 0 §6의 판독 증분을 겹쳐 읽는다.**
4. 파서 트랙: `docs/파서_명세.md` §3·§5~§9.
5. **파생 문서 3종**(가결정의 발의 원천): `docs/raw_mock_대장.md` · `docs/판정대조_ipqc_1차.md` · `docs/실행검증_1차.md`.

> **발의와 정본의 구분**: 파생 문서는 **가결정을 발의하는 원천**이고, 그 가결정이 증분0·챕터에 채택된 순간부터 **채택본이 정본**이다. 파생 문서의 "이 문서가 원천" 선언은 발의권이지 개정권이 아니다 — 겹치면 증분0·챕터가 이긴다.

## 증분 0 미반영분 — **소진 [21회차, 2026-08-19]**

**증분0_구현.md가 v1.5로 동기화되어 이 절의 delta ①~⑤는 전부 본문에 편입됐다** — 이제 증분 0을 그대로 읽으면 된다(겹쳐 읽기 종료). 현행 기준 판본: **틀 v2.10 · 카드 v18 · CH2 v2.5 · CH3B v2.4 · CH6 v3.1 · CH7 v1.7 · 파서_명세 v0.6 · 증분0 v1.5** — 판본 기준점의 정본은 `docs/00_챕터_대장.md` §1(21회차)이다. 현행 실측: **A트랙 G0~G6 + G6.5 완료 · 장부 74건 · 회귀 7종 PASS · 큐 기준선 69**. 잔여 = P트랙(P1→P2→P3)+합류. 아래 구판 delta는 이력으로 접어 둔다.

<details><summary>(이력 — 18회차 delta ①~⑤, v1.5로 소진)</summary>

**`증분0_구현.md`는 13회차(2026-08-07) 시점에 동결되어 있다.** 그 뒤 M2 세션(골격 두 축 분리 A11)과 M2 후속이 지나갔으나 증분 0에는 반영되지 않았다(문서에 `A11`·`polarity` 문자열이 0건). 아래가 그 delta이며, **증분 0의 해당 서술과 충돌하면 아래가 이긴다.** (구 "v1.4 정정 5항"은 4항이 증분 0 본문에 흡수되어 폐기했고, 남은 1항은 ①로 갈아끼웠다.)

**① CP01 prefix = 12** (D-55). 증분 0 본문의 "11행"·"C12~C29" 서술 8곳은 전부 낡았다. 현행은 **CP01 30 record · prefix 12 · 확대분 C13~C30**이다. C11 = 좌표 불일치(coord_mismatch), **C12 = A11-9 ② 실발화 행**(좌표 `cathode 탭용접` + 극성 `anode`). `verify_roundtrip`은 prefix 12·PFMEA01 13으로 PASS 50이다.

**② 골격은 A11 체계다** — 정본은 틀 §4B-A11 · 카드 J12·C17·C10 · CH3B 3.5(**v2.2**) · CH5 5.1 규약 8. 증분 0의 골격 서술은 A11 이전이다.
  - `part_of` = 구조(공장·제품·세대 무관 분해, 주소 체계) / `precedes` = **지식(대표 흐름 한 벌)**. 순서의 출처는 **seed 하나뿐**이고 문서에서 추출하지 않는다.
  - seed 실물은 `layers/process/skeleton.json`(형식 v3.3). 블록 키는 **`TREE`·`ALIASES`·`AXIS_LABELS`** — 구 `PROCESS_TREE`·`POLARITY_LABELS`는 D-53으로 개명됐다. config의 `skeleton`은 선언만 갖는다.
  - **n10 완료판정 수치**: 노드 **46** · part_of **45** · precedes **22** · polarity≠none **16** · mirrors 쌍 **8**. 증분 0의 `Process 8노드·part_of 7·precedes 6`은 구판이다. **깨끗한 재빌드로 대조한다**(옛 그래프 위에 돌리면 잔존 4건이 남아 50이 나온다).
  - `electrode_type`은 **구조 필드로 확정**(R9 종결 · D-1 → D-43 승격)이고, 해소 코드가 노드에 **`polarity` 닫힌 4값**(`cathode`/`anode`/`none`/`unbound`)을 기록한다. **코드는 canonical 문자열을 파싱하지 않는다** — 판정 근거는 필드다.
  - 좌표 태깅 닫힌 목록 = **골격 전 노드**(개념+인스턴스). `process_group` = **tier:main 조상**. 극성 모호(구 D5)는 구조적으로 소멸했다.

**③ `relation_patterns`에 골격 관계를 넣지 않는다** (D-52). `Process part_of/precedes/mirrors Process`는 패턴표에 없어야 한다 — **seed는 후보가 아니라 선언**이라 커밋 게이트를 지나지 않는 것이 설계다(틀 §4B-A3 경로 ①). 층이 어떤 관계를 갖는지의 선언은 config `relations` 배열이 하고, `relation_patterns`는 **추출용 게이트 패턴**이다. 넓히면 경로 ②·③이 골격을 개정할 수 있게 되어 A5 하향 한정·발명 금지 ③을 깬다.

**④ G4에는 0번 선행 작업이 있다**(증분 0 §2·§3의 G4 서술에 없음). 상세는 `docs/구간별_프롬프트.md` 정지점 4. 요지: F3 하향 연쇄 구현 · A11-9 ① 적용 범위를 스코프 카테고리로 한정(Unit은 F1 유지) · 판정필요-5 종결 등재 · DECISIONS D-15·D-18 prefix 문면 정리.

**⑤ DECISIONS는 55건이다** — 증분 0 §7의 "초기값 31건"은 A0 시점 수치다. 소유 분담 4계(D-1~D-31)는 유효하고 D-32 이후는 레포가 발번한다.

</details>

## 진행 규칙 (대장 §5.6 + 증분 0 §1)

- **카드·계약과 충돌 = BLOCKERS.md 기록 + 멈춤** → 의존 없는 다음 작업으로 이동. 추측으로 메우지 않는다. **BLOCKERS 항목에는 판단 재료 3요소(①근거 원문 ②선택지 2~3 ③각 귀결)를 첨부한다.**
- **그 외 세부 = 가결정 + DECISIONS.md 기록 + 전진.** 형식: `[D-##] 지점 / 가결정 / 근거 / 확정 경로`.
- **번호 발번처는 이 레포 하나다.** 외부 세션이 번호를 지정해 오면 **말번 뒤로 재번호**한다(M1 핸드오프의 D-32·33 → D-36·37이 그 실례).
- **PROGRESS.md에 결정 번호를 적지 않는다** [08-10 신설]. 미러는 **보고**이고 장부는 `DECISIONS.md` 하나다. 번호가 필요하면 **등재한 뒤 인용**한다. G3 자체 검출 3건을 미러·커밋에만 적었다가 외부 세션이 그것을 채번된 것으로 읽어 D-39~D-41 충돌이 났다.
- **PROGRESS.md 내용**: 단위 완료·테스트 결과·`git diff core/` 상태·소요. 구간 종료 시 갱신 + 커밋 1회.
- **단위 완료 = 배정된 mock 시나리오(S1~S15) 전부 통과.** 품질 측정은 완료판정이 아니다. 테스트가 통과해도 판정 문구와 어긋나면 판정이 이긴다.
- **등록 산출물(스키마·어댑터)의 판정은 문면이 아니라 실행 결과로 한다**(실행검증_1차 §1). 검수 뷰(사람) 앞단에 실행 하네스(기계 관문)가 선다.
- 구현 불변 규칙은 구현문서 §0 (**core에 층 어휘 0** / 명료함 > 짧음 / 전 생성물 status+provenance / CLI+파일 / USE_MOCK=1 기본).
  - **B1(core 무층어휘)의 등재 예외는 3건**이다(카드 v18): ①`electrode_type`(파서 계약 필드) ②`polarity`·`tier`(A11-8 시스템 어휘) ③`core/extract.py` `_PATTERNS`(**한시 — USE_MOCK 문형 폴백 한정**[게이트는 G6.5 E3이 실물화], 해소는 P트랙 n3). 축의 값(`cathode`/`anode`)은 코드에 넣지 않고 **seed `AXIS_LABELS`의 키에서 읽는다**(D-49). 코드가 아는 것은 **구문 마커 4종**(`::` 접두 · `@split` · `@unordered` · `@noflow`)뿐이다.
- **저장 계층(틀 §4B-A8, 카드 B6)**: graph.json 접근은 `core/graph.py`(GraphStore)만 — 다른 모듈이 직접 열면 그 자체가 결함이다. **경계는 "여는 코드"뿐 아니라 "저장 위치를 아는 코드"까지**이며 경로 조립은 `GraphStore.for_layer()`가 소유한다(D-35). 직렬화 orjson(표준 json 폴백). 층별 200MB 또는 build 30초 초과 = R10 판정 개시(계기판 7·8).
- **의미 축 id는 ULID다 — 발효 확정.** `id_seq.json`은 만들지 않는다.
- **큐 kind는 20종 닫힌 목록이다** — 새 kind를 임의로 만들지 않는다(D-54: `stale_skeleton` 미신설 판정이 그 실례). 처리 주체가 "재빌드"인 항목에 큐를 파지 않는다.
- **검사 6 — 실물 대조** [17회차 신설]: 코드의 동작·경로·파일명·현재 상태를 **단정하는 문안은 발행 전에 해당 코드·config의 전문을 연다.** 절단된 출력·요약·타 문서의 보고로 대체하지 않는다. 근거 사고 2건 — 미러를 장부로 오독해 말번을 단정(실물과 3 차이) / config 출력을 300자에서 잘라 보고 "패턴에 골격 관계 없음"으로 단정(실물에는 있었다).

## 착수 순서

1. **남은 정지점은 G4 하나다.** 프롬프트는 `docs/구간별_프롬프트.md` 정지점 4. **별도 승인 없이 착수하지 않는다.**
2. 배열은 **증분 0 §2**: A트랙(G1 st·n1·n2 → G2 n10 → G3 1c′·1d′·n3·n4 → **G4** → G5 n5 → G6) ∥ P트랙(P1 n7 → P2 n8 → P3 n6). 결합은 mock 계약 JSON뿐.
3. G5·G6은 G4 정산 뒤 연속. **P트랙은 이미 열려 있다**(G3 완료로 정답지 `mock/parsed/`가 섰다) — 착수 직전에 증분 0 레포 사본을 프로젝트 현행판으로 교체한다.

## 읽기 주의 (낡은 서술 방어용)

- **큐 kind는 CH3B 3.7 표(13종)가 아니라 증분 0 §6-5가 현행**: 17종 확정 + 3종 승인 = 20종. CH3A 3.4의 "택일 대기"도 낡음(D-3).
- **CH6 6.2 n9 행의 "(D-1)" 앵커는 오기** — 정본은 파서_명세 §5 ⑴-C″ / 카드 C15.
- **n10 부트스트랩의 골격 노드 status는 `seed`이고, 부트스트랩은 canonical 기준 upsert다**(D-41 — 재실행 시 ULID 재발급으로 노드가 증식했던 실측).
- **record_id의 occ = 동일 join 문자열의 출현 순번.** join 순서 = 매칭 스키마 fields **선언 순서**(D-14).
- **계기판 8종 · mock 15종 · CP01 prefix 12가 현행.** 구판(5·6종 / 13종 / 9·10·11행) 서술은 낡음. **구현문서 §6.1 "records 12행"은 오기(실제 13행) · §6.2 "6행"도 오기(실제 9행).**
- **의미 축 id의 N#### 전역 시퀀스·id_seq.json 서술은 구판** — 현행은 ULID.
- **파서_명세 §9 "킷 5종"은 D-27로 6종, §5 `expects`는 D-29로 `header_labels` 필수 키 추가, §5 좌표 닫힌 목록은 D-45로 골격 전 노드, 템플릿 v0.3은 D-46으로 "구조 필드는 6지선다 대상 아님" 명시** — 문서 개정 전이므로 **DECISIONS 기준으로 전진한다.** `columns`는 출력 필드명 매핑이고, 양식 표류 대조는 `header_labels`(원본 헤더 문자열 배열)로 한다.
- **UNMAPPABLE로 판정한 열은 스키마 `fields`에 넣지 않고 어댑터 출력에서도 제외한다**(CH2 2.6 규약 5 · D-30). 인입 코드는 알 수 없는 role에 `KeyError`로 죽지 말고 **`invalid_role` 결함 로그**를 남긴다.
- **`electrode_type`은 확정된 구조 필드**(R9 종결 — M2). **`section`은 아직 가결정**(D-31 — 구조 필드가 유력, 확정은 명세 트랙 M2-후속. 마감선은 P트랙 착수 전).
- **구현문서·명세의 조각 필드 `chunk_id`는 `source_locator`로 읽는다** — 정본 chunk_id·record_id·doc_hash는 에이전트가 계산(카드 C7).
- **alias는 정확 일치만** 매칭한다. 문자열 포함 규칙은 canonical에만(D-39). **짧은 이름 자동 alias는 층 안에서 유일할 때만 사전에 등재**하고, 모호하면 등재하지 않고 loader가 출력한다(D-51 — 조용한 임의 선택 금지).

## 문서가 없어 막히면

`docs/`에 없는 문서가 필요해 결정이 안 되는 지점을 만나면 **추측하지 말고 BLOCKERS.md에 기록**하고 사용자에게 요청한다. 프로젝트(claude.ai RAG)에 전량 있으나 **구현 세션은 프로젝트를 직접 읽지 못한다** — 문서는 사람이 레포에 반입하고, 실행·검증은 이 세션이 한다(16회차 확정).