# CLAUDE.md — 온톨로지 시스템 구현 (국면 1)

> 이 파일은 매 세션 자동 로드된다. 상세 문서는 전부 `docs/`에 있다(목록·경로는 `docs/MANIFEST.md`).
> **세션 기준: 감사 4 통과(2026-07-31, 검사 로그 10회차) + raw mock 실물 확보(08-01, 11회차) + 쟁점 A(저장 계층) 판정 반영(08-06, 12회차 — 틀 v2.4·카드 v11·증분 0 v1.3).**

## 정본 관계 (충돌 시 이 순서)

1. **규범 킷**: `docs/00_틀_확정본.md`(**v2.4**) · `docs/00_불변식_카드.md`(**v11**) · `docs/00_용어_대장.md` — 최상위. 카드는 압축 검사기이며 챕터·명세가 카드와 어긋나면 그 자체가 결함 신호다.
2. **챕터**(docs/CH2·CH3A·CH3B·CH4·CH5·CH6) — 서술 정본. 명세·구현문서의 구판 서술을 이긴다. CH5·CH6는 **v3**(08-06 개정분 — 계기판 8종·저장 계층 규약·mock 15종).
3. **1차 입력물**: `docs/ontology_system_구현용문서.md`(v1.16) + `docs/증분0_구현.md`(**v1.3**) — **겹치면 증분 0이 이긴다.**
   - 구현문서 v1.16은 **의도적으로 구판이다.** 지금 고치지 않는 이유: 증분 0이 그 delta 문서이고, 같은 사실을 두 곳에 두면 반드시 어긋난다(세션 1에서 실측된 이중 원천 사고). v1.17 동기화는 코드가 돌아간 뒤(세션 8).
   - 구현문서·명세를 읽을 때는 **증분 0 §6의 판독 증분을 겹쳐 읽는다.** 명세 §7 계열·§5.4-1의 구판 서술도 동일(정본은 CH3A 3.4·3.10·3.11).
4. 파서 트랙 정본: `docs/파서_명세.md`(v0.5.1) §3·§5~§9. 계약 세부: CH2 2.2(파서 출력 3종)·CH3A 3.11(추출 후보 JSON)·CH6 6.1(두 축 id 산식).
5. raw mock 트랙: `docs/raw_mock_대장.md`(v1.1)·`docs/판정대조_ipqc_1차.md`(v1.0) — mock/raw 실물 10종·킷 v0.1·가결정 D-15~D-25의 원천.

## 진행 규칙 (대장 §5.6 + 증분 0 §1)

- **카드·계약과 충돌 = BLOCKERS.md 기록 + 멈춤** → 의존 없는 다음 독립 작업으로 이동. 추측으로 메우지 않는다. **BLOCKERS 항목에는 판단 재료 3요소를 첨부한다: ①근거 원문(어느 문서 어느 절) ②선택지 2~3개 ③각 선택지의 귀결**(증분 0 §1-1).
- **그 외 세부 = 가결정 + DECISIONS.md 기록 + 전진.** 형식: `[D-##] 지점 / 가결정 / 근거 / 확정 경로`. **DECISIONS.md 초기값은 25건** — 증분 0 §7(D-1~D-14) + raw_mock_대장 §6(D-15~D-21) + 판정대조 §8(D-22~D-25)을 시작 시 그대로 옮겨 적는다.
- **PROGRESS.md**: 단위 완료·테스트 결과·`git diff core/` 상태·소요. 세션 종료 시 갱신(구현문서 §9.5). 구간 종료 시 커밋 1회.
- **단위 완료 = 배정된 mock 시나리오(증분 0 §8, S1~S15) 전부 통과.** 품질 측정은 완료판정이 아니다. 테스트가 통과해도 판정 문구와 어긋나면 판정이 이긴다.
- 구현 불변 규칙은 구현문서 §0 (core에 층 어휘 0 / 명료함 > 짧음 / 전 생성물 status+provenance / CLI+파일 / USE_MOCK=1 기본).
- **저장 계층(틀 §4B-A8, 카드 B6)**: graph.json 접근은 core/graph.py(GraphStore)만 — 다른 모듈이 graph.json을 직접 열면 그 자체가 결함이다. 직렬화는 orjson(미설치 시 표준 json 폴백). 층별 파일 200MB 또는 build 30초 초과 = R10 판정 개시(계기판 7·8).
- **의미 축 id는 ULID로 개정됐다(카드 N1)** — 단 **발효는 A0의 N#### 하드코딩 계수 확인 후**다. A0 전에는 코드에 새 발급 로직을 넣지 않는다.

## 착수 순서

1. **A0(G0) — 최선행**: 레포 상태 회수 + **계수 3종**(증분 0 §3 A0 정의). 상세 프롬프트는 `docs/구간별_프롬프트.md` 정지점 1.
2. 이후 배열은 **증분 0 §2**: A트랙(**G1 st·n1·n2** → G2 n10 → G3 1c′·1d′·n3·n4 → G4 → G5 n5 → G6) ∥ P트랙(P1 n7 → P2 n8 → P3 n6) 병렬 — 결합은 mock 계약 JSON뿐. 이미 완료된 기존 단위는 재작업하지 않고 개수분(′)만 적용. **st(저장 계층 정비)는 N+3 신설 — G1 선두**다.
3. **정지점(사용자 확인 대기)은 4개: G0 / G1+G2 / G3★ / G4.** G5·G6은 정지 없이 연속 진행, P트랙은 병렬. 정지점마다 PROGRESS.md를 갱신하고 멈춰 보고한다 — 사용자가 프로젝트 세션에 미러링한다.

## 읽기 주의 (감사 4 + N+3이 기록한 낡은 서술 — 방어용)

- **큐 kind는 CH3B 3.7 표(13종)가 아니라 증분 0 §6-5가 현행**: 17종 확정 + 3종 승인(direction_conflict·duplicate_doc_hold·hierarchy_unresolved) = 20종. CH3A 3.4의 "택일 대기" 표기도 낡음 — D-3으로 신설 확정.
- **CH6 6.2 n9 행의 "(D-1)" 앵커는 오기** — 정본은 파서_명세 §5 ⑴-C″ / 카드 C15.
- **n10 부트스트랩의 골격 노드 status는 `seed`로 읽는다**(사람이 파일로 심음 — 용어 대장 status 값).
- **record_id의 occ = 동일 join 문자열의 출현 순번.** join 순서 = 매칭 스키마 fields **선언 순서**(D-14).
- **계기판은 8종이 현행**(CH5 5.5 v3 — 구판 5종·6종 서술은 낡음). **mock 시나리오는 15종이 현행**(CH6 6.3 v3 — S14 역산 정합·S15 ipqc 추가. 구판 13종 서술은 낡음). **구현문서 §6.1 "records 12행"은 오기** — 실제 13행(R1~R13).
- **CP01은 11행이 현행**(증분 0 §4.1 — C10=버 높이[D-15], C11=좌표 불일치. "C10=coord_mismatch"로 적힌 구판 서술은 라벨 충돌 정정 전 것).
- **의미 축 id의 N#### 전역 시퀀스·id_seq.json 서술은 구판** — 현행은 ULID(틀 §4B-A8-4, 발효 조건부).
- **구현문서·명세의 조각 필드 `chunk_id`는 `source_locator`로 읽는다** — 정본 chunk_id·record_id·doc_hash는 에이전트가 계산(카드 C7).

## 문서가 없어 막히면

`docs/`에 없는 문서(챕터 대장·CH1·CH7 등)가 필요해 결정이 안 되는 지점을 만나면 **추측하지 말고 BLOCKERS.md에 기록**하고 사용자에게 그 파일을 요청한다. 프로젝트(claude.ai RAG)에 전량 있다.
