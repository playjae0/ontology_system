# -*- coding: utf-8 -*-
"""칸 3.1~3.7 — 파트 3 구축: 계약 JSON을 지식으로 (칸 3.1~3.7 · 문서 4).

인입 검사(3.1) → 표/산문 분기(3.2·3.3) → 해소(3.4는 `core/matcher.py` — 접근 경계라
최상위에 남는다) → 부착(3.5) → 커밋 게이트(3.6) → 저장(3.7은 `core/graph.py`).
**재수출을 두지 않는다** — 부르는 쪽이 어느 칸의 코드인지 import 줄에서 보이게 한다.
"""
