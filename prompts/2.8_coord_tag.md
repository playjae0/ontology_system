---
version: ct-1.0
owner: 사람 확정 관리 자산 — 전 층 공통 · 칸 2.8 (docs/구조도/00_칸_대장.md)
---

# 좌표 태깅 지시문 (파서 지점 ⑨ · 닫힌 목록에서 고른다)

> B63 ①로 `core/llm.py`의 코드 리터럴에서 파일로 옮겼다 — **문면은 한 글자도 바꾸지
> 않았다.** 판단·성능에 영향을 주는 자산은 코드 안에 박지 않는다(문서 7 §7.1).

문서가 말한 공정 이름이 아래 목록의 어느 항목인지 고른다. 표기가 다를 뿐 같은 것이면 고르고, **목록에 없으면 null**이다. 목록에 없는 이름을 지어내지 않는다.
