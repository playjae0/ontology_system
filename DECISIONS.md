# DECISIONS.md — 가결정 대장

> **초기값 31건 (D-1~D-31)** + D-32~D-51 = **현재 51건** — CLAUDE.md 「증분 0 v1.4 정정 5항」 1번 판정.
> 구 "25건"은 계수 오기였다(D-26~D-31 = 실행검증_1차 §6 누락).
>
> **소유 분담 4계**: D-1~D-14 = `docs/증분0_구현.md` §7 / D-15~D-21 = `docs/raw_mock_대장.md` §6 /
> D-22~D-25 = `docs/판정대조_ipqc_1차.md` §8 / D-26~D-31 = `docs/실행검증_1차.md` §6.
>
> **지위**: 전부 **가결정**이다. 카드·계약과 충돌하지 않는 세부를 정하고 전진하기 위한 것이며,
> 확정 경로가 지정된 항목은 그 세션이 최종 판정한다. 원천 문서의 문면이 정본이고 이 대장은 그 사본이다 —
> 원천이 개정되면 같은 커밋에서 여기도 고친다.
>
> **추가**: D-32 — BLOCKERS 판정필요-1 해소(사용자 판정 08-07). **가결정이 아니라 확정이다.**
> **추가**: D-33~D-37 (08-07~08-08 판정 · 전부 **확정**). D-33~D-35 = G1+G2 정산분 / **D-36·D-37 = 명세 트랙 M1(쟁점 B·C) 반영분.**
>
> **추가**: D-39~D-41 (08-08 레포 자체 검출분의 **소급 등재** — 08-10) · D-42~D-48 (**M2 §8 전달①~⑦**).
>
> **추가**: D-49 (**M2 반입 3단계의 선행 판정 B** — 마커 어휘의 구현 형태 · 명세 트랙) ·
> D-50·D-51 (**3단계 레포 자체 검출분** — mirrors 짝 키 · 짧은 이름 alias 모호).
>
> ⚠ **미러에 번호를 적지 않는다 (08-10 신설 규칙).** `PROGRESS.md`는 **보고**이고 장부는 이 파일 하나다. 번호가 필요하면 **등재한 뒤 인용**한다. D-39~D-41 누락 사고의 전달 경로가 정확히 그것이었다 — 결정을 미러·커밋에만 적어 두자 외부 세션이 그것을 채번된 것으로 읽었다.
>
> ⚠ **번호 발번처는 이 레포 하나다.** M1 핸드오프 문서가 적재 경계·문서 등급을 D-32·D-33으로 적어 보냈으나 그 번호는 이 레포에서 이미 쓰였으므로 **D-36·D-37로 재번호**했다. 외부 세션이 번호를 지정해 오면 **말번 뒤로 재번호한다.**
>
> **생성**: A0(G0), 2026-08-07. 원천 4종에서 기계적으로 옮겨 적었으며 내용을 요약·각색하지 않았다.

| # | 지점 | 가결정 | 근거 | 확정 경로 | 소유 |
|---|---|---|---|---|---|
| **D-1** | **R9/U7** electrode_type의 role | 블록에 넣지 않는다(카드 C10 안전 기본값). 조각 공통의 **구조 필드**로 유지하고 극성 결합은 entity 해소 코드가 record.electrode_type을 직접 읽는다 | role 핸들러가 아니라 해소 코드가 소비(C10). **블라인드 판정(08-01)에서 미결이 배정을 흔든다는 실측 — G3 전 확정 권고** | CH3B 3.5 / 세션 4 | 증분0_구현 §7 |
| **D-2** | **U3** 검증 순서 전체 배열 | 인입 진입: 투입 분류(파서 소유 아님 — C15·⑴-C″) / 파서: preflight→extract→normalizer→tagger→validator / 에이전트: doc_hash→id 계산→필드 검증 (§3 G1 공통) | 파서 측=문서 단위 실패, 에이전트 측=큐 착지로 역할이 겹치지 않음 | 세션 1 묶음 V | 증분0_구현 §7 |
| **D-3** | 방향 반대(이종 쌍) 큐 | kind `direction_conflict` **신설**(기존 kind 확장 아님) | coord_mismatch 선례 — 큐는 이름 붙은 화면(P-3) | 명세 v1.19 분해 시 | 증분0_구현 §7 |
| **D-4** | doc_hash 보류의 물리 형태 | kind `duplicate_doc_hold` 큐 신설 | 조용히 버리지 않는다(G5) | 명세 v1.19 | 증분0_구현 §7 |
| **D-5** | 계층 미확정 행의 큐 | kind `hierarchy_unresolved` 신설(평면 폴백 청크에 표시+큐) | 틀 Q2의 kind가 미명명이었음 | 명세 v1.19 | 증분0_구현 §7 |
| **D-6** | 관계 목록의 구현 형태 | config `relation_patterns: [{src, rel, dst, symmetric}]` + **관계 정의문 병기**(판정대조 §5.2 실측 반영) | 게이트가 (출발,관계,도착)+대칭 표시를 요구(I5·I8). 정의문 없는 관계 목록은 등록 판정을 막는다는 실측 | 명세 v1.19 | 증분0_구현 §7 |
| **D-7** | 게이트 거부 기록 | data/gate_rejects.json **로그**(큐 아님) — 사유별 건수 | 거부는 처리 대상이 아니라 커버리지 관측 신호(3.4) | — | 증분0_구현 §7 |
| **D-8** | 신규 데이터 파일 | registry.json(등록부) · doc_registry.json(doc_hash 대장) · ops_log.json(I축 연산 로그) | 각 확정 사항의 물리 착지 지정 | — | 증분0_구현 §7 |
| **D-9** | 파서 패키지 분리 | parser/ 별도 패키지, openpyxl·python-pptx 의존 허용 | "외부 패키지 0"은 에이전트 코어(USE_MOCK 경로)의 원칙 — 파서는 사내 실행 별도 프로그램이고 결합은 JSON뿐(P1) | — | 증분0_구현 §7 |
| **D-10** | USE_MOCK 확장 3종 | 추출=힌트 파일 우선+문형 규칙 폴백 / 등록 생성=fixture 반환 / 이미지 요약=고정 문자열 (§5) | mock 목적은 메커니즘 검증 한정(6.3 규약 1) — 결정성 우선 | — | 증분0_구현 §7 |
| **D-11** | **U1** 골격 공유 자산 | data/skeleton_closed_list.json 스냅샷 + skeleton_version — 킷에 동봉 | 파서·에이전트 공유 자산의 국면 1 형태. 사내 배포 경로는 [사내 확인] | 세션 1 묶음 P / 국면 2 | 증분0_구현 §7 |
| **D-12** | **U2** 복수값 전개 | normalizer 소유. 구분자 기본 닫힌 목록 [",", "/", 개행] + 어댑터 expects로 doc_type별 재정의 가능 | 전개는 자기완결성 계약(C4)의 실행 — 규칙은 관찰 상수(expects)에 | 세션 1 묶음 P | 증분0_구현 §7 |
| **D-13** | n9 배치 | G6(최후순) | 편의 기능 — 없는 동안 사람 지정만으로 운영 성립 | — | 증분0_구현 §7 |
| **D-14** | record_id의 join 순서 | 매칭 스키마 fields **선언 순서**의 필드 값들을 join | 산식(A4)이 "field_values"의 순서를 미지정 — 처리 순서 무관·스키마 종속이므로 결정적 | 명세 v1.19 | 증분0_구현 §7 |
| **D-15** | CP01의 record 수 | **11건**으로 확정(prefix). C10 = (노칭, anode, 노칭 프레스, 버 높이, 30㎛ 이하 — mirror_asymmetry) / **C11 = 좌표 불일치 행(공정구분 "스태킹" + 공정명 "노칭" → coord_mismatch)**. *(2026-08-07 정정 — 구판 "10건"은 C11 신설 전 수치. parsed에만 있고 raw에 없는 행은 D-18을 무효화하므로 C11을 raw 실물에 편입한다. 확대분은 C12~C29로 밀린다.)* | — | — | raw_mock_대장 §6 |
| **D-16** | CP·PFMEA·IPQC 헤더 행 | **3행**. 1행 제목, 2행 문서 메타 | — | — | raw_mock_대장 §6 |
| **D-17** | 복수값 전개의 검증 위치 | **CP04_unlabeled · IPQC01**. 역산 정합 대상 파일에는 심지 않는다 | — | — | raw_mock_대장 §6 |
| **D-18** | 역산 정합 어서션 | **prefix 일치**로 정의(CP01 11 · PFMEA01 13). 확대분은 뒤에 붙되 기존 판정 무영향 — **CH6 6.3 닫힌 목록 개정 완료(v3, 08-06 채택)** | — | — | raw_mock_대장 §6 |
| **D-19** | TOC 계층의 물리 표현 | 셀 들여쓰기(indent) 속성 | — | — | raw_mock_대장 §6 |
| **D-20** | 정형 등록 시나리오 | 신규 doc_type `ipqc` 등록을 시나리오로 신설 — **CH6 6.3 닫힌 목록 개정 완료(v3, 08-06 채택 → S15)** | — | — | raw_mock_대장 §6 |
| **D-21** | UNMAPPABLE 후보의 판정 근거 | "최근 불량 이력" = L4 신호 / "관련 표준문서" = L3 `reference` role 신호 | — | — | raw_mock_대장 §6 |
| **D-22** | 표본 1부 등록의 edges 취급 | 1부 등록 시 판정자가 선언한 edges를 검수 뷰에서 **특별 확인 대상으로 표시**한다. 경고 문구를 "표본 1부 · 변형 미관찰 · **선언된 관계는 근거 1건일 수 있음**"으로 확장 | 파서_명세 §7 / 세션 1 | — | 판정대조_ipqc_1차 §8 |
| **D-23** | 측정방법 열의 지위 | mock 결함이 아니라 **"판정 불가 유도(정형판)"** 재료로 유지한다. 봉인 정답표의 attribute 배정은 철회 | — | — | 판정대조_ipqc_1차 §8 |
| **D-24** | 공정번호 체계 | 골격 순서에 정렬(10·20·30·31·40·50·60). 전 raw mock에 일관 적용 | — | — | 판정대조_ipqc_1차 §8 |
| **D-25** | Unit 정의문 | 검사·계측 장비의 포섭 여부는 **국면 2 [사내 확인]**으로 이관. 국면 1에서는 Unit으로 두되 이 유보를 기록 | 국면 2 | — | 판정대조_ipqc_1차 §8 |
| **D-26** | `USE_MOCK` fixture의 정의 | fixture는 "미리 만든 정답"이 아니라 **"외부 세션에서 실제 LLM이 산출한 결과물의 스냅샷"**이다. 사람이 손으로 쓴 fixture는 리허설을 무효화한다 | 증분0 §5-2 개정 | — | 실행검증_1차 §6 |
| **D-27** | 킷 구성물 | **실행 하네스**를 킷 6번째 구성물로 신설. 검수 뷰(사람) **이전**에 기계가 거르는 관문 | 파서_명세 §9 개정 | — | 실행검증_1차 §6 |
| **D-28** | mock 표본 규모 | 정형 24행 이상 / 비정형 25행 이상 / PPT 8슬라이드 이상을 기준으로 한다. head 관찰 규칙(M5)이 시험되려면 문서가 head보다 커야 한다 | — | — | 실행검증_1차 §6 |
| **D-29** | `expects`의 표류 지문 | **`header_labels`(원본 헤더 문자열 배열)를 필수 키로 신설.** `columns`는 출력 필드명 매핑으로 용도 확정 | 파서_명세 §5 개정 | — | 실행검증_1차 §6 |
| **D-30** | UNMAPPABLE 열의 처리 | 스키마 `fields`에 넣지 않고 어댑터 출력에서도 제외한다. 템플릿에 명시 + 인입 코드에 `invalid_role` 방어 | 템플릿 v0.3 / n7 | — | 실행검증_1차 §6 |
| **D-31** | `section`의 role | **role 배정 대상이 아닌 구조 필드**로 두는 것이 유력. CH2 2.2·CH5와 함께 확정 | 세션 1 | — | 실행검증_1차 §6 |
| **D-32** | 기존 구현물 처리 | **전량 신규 작성.** 이전 레포 회수분은 **참조용 한정**이며 코드로 반입하지 않는다 | 구 구현물은 **계약 v1 산물**(chunk_id 발급·N#### 의미 축 id·커밋 게이트 없음)이라 **v2 이식 비용 > 신규 작성 비용**이고, 낡은 전제가 코드로 유입될 위험이 있다 | **사용자 판정 08-07 (확정)** | 사용자 판정 |
| **D-33** | C11 잔여 열 값 | **노칭 프레스 · "노칭 피치" · 50±0.2mm** 승인. `raw_mock_대장` v2.1 §2.1이 C11의 메커니즘(coord_mismatch)만 명세하고 나머지 열을 비워둔 자리를 채운 것이다 | 설비는 **기존 Unit 재사용**이라 새 노드가 생기지 않고, 관리항목은 신규이나 어서션이 거는 "적층 정렬도"·"금형 클리어런스"와 **비충돌** | **사용자 판정 08-08 (확정)** | 사용자 판정 |
| **D-34** | mock/parsed 생성 방식 | `tools/gen_parsed_mock.py`가 **raw prefix에서 파생**시킨다. **예외: PPT01은 대응하는 raw가 없으므로 수기** | 수기본과 raw를 이원화하면 둘이 어긋나고 그 어긋남이 곧 **D-18(역산 정합) 무효화**다. **진실은 `verify_roundtrip`의 수기 기대값 튜플(독립 정답)**이며 사슬은 **"수기 기대값 → raw 검증 → parsed 파생"의 단방향**이다 — **기대값 튜플을 raw에서 자동 생성하는 것은 금지한다**(그 순간 검증이 자기 자신을 검증하게 되어 아무것도 시험하지 못한다) | **사용자 판정 08-08 (확정)** | 사용자 판정 |
| **D-35** | GraphStore 경계 정의 | 경계는 "여는 코드"뿐 아니라 **"저장 위치를 아는 코드"까지**다. 경로 조립은 `GraphStore.for_layer()`가 소유한다 | 밖에서 경로를 조립하면 저장 방식을 바꾸는 날(R10) 고칠 곳이 다시 여러 군데가 된다 — 경계가 하나가 아니면 경계가 아니다 | 카드 B6 문구 보강 (명세 트랙, **비차단**) | 사용자 판정 |
| **D-36** | 적재 경계 — 틀 §4B-A9 | 그래프에 들어오는 것은 **문서 경유 지식뿐(T0~T2)**이다. 생산 관측 데이터(시계열·검사 실측·로트/셀·런 이력·불량 레코드 직수신)는 **적재하지 않는다** — 파이프라인에 **T3 수신 경로를 만들지 않는다**. **로트 번호는 개체가 아니라 attribute/meta**로 다룬다 | 3테스트는 R1 층 등록 게이트의 재료다(세션 4) | **사용자 판정 08-07 (확정)** | 사용자 판정 (M1 핸드오프의 D-32 재번호) |
| **D-37** | 문서 등급 — 틀 §4B-A10 | 파서 봉투에 `classification?` **선택 필드**(널 허용·자유 문자열). **현 국면에서는 아무도 채우지 않고 인입 코드는 이 값을 읽지 않는다** — 어댑터가 채우지 않아도 **계약 위반이 아니며** validator는 존재를 강제하지 않는다. **봉투 스키마에 자리만 인정하면 끝이고 복사·소비 로직을 만들지 않는다.** 등급은 문서의 성질이므로 **노드에 등급 필드를 만들지 않는다** | 권한 로직·값 어휘·집계 범위 명시는 전부 **R12 이연** | **사용자 판정 08-07 (확정)** | 사용자 판정 (M1 핸드오프의 D-33 재번호) |
| **D-38** | CH2 파일명 중복 | 신판 `Ch2_문서계약.md`(v2.1)를 **정본 경로 `docs/CH2_문서계약.md`로 이동**하고 구판 v2를 대체한다. 소문자 `Ch2` 파일은 남기지 않는다 | M1 반영분이 소문자 `Ch2`로 올라와 **교체가 아니라 중복**이 됐다. 정본 경로에 구판(v2 — `classification` 없음)이 남아 있으면 **CH2를 계약 정본으로 읽는 G3(1c′)이 구판을 읽는다.** MANIFEST 트리가 파일명의 정본이고 그것은 대문자 `CH2`다. 신판이 구판의 **완전한 상위집합**(잃은 줄 0)임을 diff로 확인했다 | 다음 문서 반입 시 파일명 확인 (비차단) | 이 레포 |
| **D-39** | alias 매칭 범위 | **alias는 정확 일치만.** 문자열 포함 규칙은 canonical에만 적용한다 | alias는 조립 전 표면형이라 포함 규칙을 걸면 `cathode 노칭 프레스`가 무극성 노드에 흡수된다 — 다른 실물이 한 노드가 되는 사고다 | **08-08 판정, 08-10 소급 등재** — PROGRESS·커밋에만 기록되어 있던 것을 장부로 옮김 | 레포 자체 검출 (G3) |
| **D-40** | 극성 정체성 | 극성이 다르면 **감점이 아니라 후보에서 제외**한다. 문자열 휴리스틱이 아니라 **정체성 규칙**으로 건다 | `cathode 노칭 프레스`와 `anode 노칭 프레스`는 표기 차이가 아니라 다른 실물이고 다른 규격을 갖는다(CH3A 3.3 경계) | **08-08 판정, 08-10 소급 등재** — PROGRESS·커밋에만 기록되어 있던 것을 장부로 옮김 · **M2 확정과 정합**(A11-8 구조 필드 + polarity 파생 필드, canonical 비파싱) — 재대조 종결은 필드명 `electrode_type`→`polarity` 정리와 함께 3단계에서 | 레포 자체 검출 (G3) |
| **D-41** | seed 멱등성 | 부트스트랩은 **canonical 기준 upsert**다 — 이미 심긴 골격은 다시 심지 않는다 | 의미 축 id가 발급(ULID)이라 다시 심으면 같은 개념에 새 id가 붙어 노드가 배로 늘고(60→72 실측) provenance·엣지가 갈라진다. 노드 유일성(P4) 위반이며 `run.py all` 2회 = 동일 그래프(§8)도 깨진다 | **08-08 판정, 08-10 소급 등재** — PROGRESS·커밋에만 기록되어 있던 것을 장부로 옮김 | 레포 자체 검출 (G3) |
| **D-42** | 골격 두 축 분리 + seed v3.2 [M2·A11] | **part_of=구조 / precedes=지식(대표 흐름).** seed 형식 v3.2 = 단일 파일 `skeleton.json`(PROCESS_TREE 순서 배열 + ALIASES 블록 + POLARITY_LABELS) — **배열 순서가 곧 대표 흐름 선언**, 비참여 3종(`::`인스턴스 자동 · `{"@unordered":<항목>}` 개별 · `"@noflow"` 부모 전체), 마커 **닫힌 7종**. 극성 인스턴스 간 순서는 구조적 미생성 | 공장·세대별 실제 라우팅은 골격에 넣지 않는다 — 골격 밖(**R13 이연**) | **M2 확정 (08-10)** · loader 반영은 3단계 | M2 §8 전달① |
| **D-43** | R9 종결 — electrode_type·polarity [M2·A11-8] | **R9 종결 = D-1 확정 승격.** `electrode_type`은 **구조 필드**(1c′ ④ 유효). 추가로 해소 코드가 노드(Unit·Property)에 **`polarity` 기록 — `cathode`/`anode`/`none`/`unbound` 닫힌 4값**. 스키마 불가침, **canonical 비파싱** | 레포 D-40(극성 정체성)과 **정합** | **M2 확정 (08-10)** · 3단계에서 필드 정리 | M2 §8 전달② |
| **D-44** | 순서 파생(traverse) [M2·A11-5] | 자기 선언 > 부모 파생 + 동일 polarity 인스턴스 하강(없으면 개념=합류) > "순서 정보 없음". **파생 답에 해상도를 표기**한다 | 착지는 **G4** — G3에는 무영향 | **M2 확정 (08-10)** · G4 착지 | M2 §8 전달③ |
| **D-45** | 좌표 닫힌 목록 [M2·A11-6·7] | **좌표 닫힌 목록 = 골격 전 노드**(D-11 스냅샷에 개념·인스턴스 전부). `process_group` = **tier:main 조상** | **D5(극성 모호) 구조가 소멸**한다 — "목록 밖 이름"만 잔존 | **M2 확정 (08-10)** | M2 §8 전달④ |
| **D-46** | 킷 템플릿 v0.3 — 구조 필드 [M2·§7] | 템플릿 v0.3에 **구조 필드 목록은 6지선다 대상이 아님**을 명시한다 | D-30과 **같은 커밋**으로 반영 | **M2 확정 (08-10)** · P2 | M2 §8 전달⑤ |
| **D-47** | canonical 접두 + alias 원천 [M2] | sub/detail canonical = **부모 경로 균일 접두**. **seed ALIASES가 사전의 원천** — n10이 `dictionary.json`에 `provenance:["seed"]`로 등재(**장부는 하나** — B4, 운영 축적분과 태그로 구분. seed 개정 시 seed 유래 엔트리만 갱신). 짧은 이름 자동 alias(개념 노드) + 인스턴스 auto alias = `{극성영문} {이름}`·`{극성한글} {이름}`(POLARITY_LABELS: cathode=**양극** · anode=**음극**) | 공통 alias는 **개념만** 소유한다 | **M2 확정 (08-10)** · loader 반영은 3단계 | M2 §8 전달⑥ |
| **D-48** | 부착 정합 2규칙 + mock 재매핑 [M2·A11-9] | 1c′에 2규칙: **①부착 골격 노드 `polarity≠none`이면 표면형 극성 결합 생략**(주소에 이미 극성이 있으므로 이중 표기 방지 — `polarity` 필드는 기록) **②record `electrode_type` ↔ 노드 `polarity`가 둘 다 확정인데 다르면 `coord_mismatch` 큐**로 표면화(조용히 한쪽을 택하지 않는다) | mock raw 재매핑: **"실링" 행만** 사이드/프리 실링으로 행별 교체(나머지 공정명은 alias로 자동 정합 — `skeleton_seed_v3.2.md` 하단 대조표). `process_no` 재부여는 재량 | **M2 확정 (08-10)** · 3·4단계 | M2 §8 전달⑦ |
| **D-49** | **마커 어휘의 구현 형태** [M2·A11-4 / 카드 B1] | loader에 `cathode`/`anode` 문자열을 **쓰지 않는다.** 코드가 아는 것은 **구문 마커 4종**(`::` 접두 · `@split` · `@unordered` · `@noflow`)뿐이고 **축의 값은 seed의 `POLARITY_LABELS` 키에서 읽는다**: `::X`는 X ∈ keys일 때만 인스턴스 마커, `@X`는 단극성(개념+인스턴스 1), `@split`은 전 키 전개. 판정문의 "마커 닫힌 7종"이 코드에서는 **"구문 4종 + 데이터에서 온 값 N개"**로 구현된다. 어휘 밖 마커는 **명시적 실패** | 극성 값은 도메인 어휘이고 코드에 박히면 B1 위반이다(예외는 파서 계약 필드 `electrode_type` 한 건뿐 — M2 §11-2). **identity_axis 일반화(안 D) 이연분의 절반이 여기서 공짜로 선다** — 축이 바뀌어도 loader는 그대로다 | **명세 트랙 판정 (08-12)** · 3단계에서 구현 완료 | 명세 트랙 (M2 후속) |
| **D-50** | mirrors 짝 찾기의 키 [M2·F3 — 레포 자체 검출] | 짝 찾기를 **canonical 되 파싱으로 하지 않는다.** 극성 결합 시점에 노드에 `base_canonical`(스코프는 붙고 극성은 안 붙은 이름)을 적어 두고 그것 + `polarity` 필드로 페어링한다. **Tier1(seed) 노드는 build의 자동 규칙에서 제외**한다 — 골격 mirrors는 loader가 잇고 Tier1 단극성은 `mirror_asymmetry` 대상이 아니다(A11-4) | 구 `strip_polarity()`는 **앞에 붙은** 접두만 뗄 수 있어 스코프가 붙은 관리항목(`노칭::anode 노칭 정밀도`)에서 조용히 실패했다 — **정상 극성 쌍 1쌍이 페어링되지 않고 `mirror_asymmetry` 큐로 샜다**(실측 큐 3건, 문서·미러의 "1건"과 불일치). F3의 "문자열 극성 토큰 파싱 폐지"가 가리키던 실물 결함이다 | **레포 자체 검출 (08-12)** · 수정 후 큐 1건(`노칭::버 높이`)으로 문서와 일치 | 레포 자체 검출 (M2 반입 3단계) |
| **D-51** | 짧은 이름 auto alias의 모호 처리 [M2 §4 alias 규칙] | 개념 노드의 **짧은 이름 자동 alias는 층 안에서 유일할 때만 사전에 등재한다.** 모호하면(같은 짧은 이름이 여러 노드) **등재하지 않고** loader 출력에 표기한다 — 접두 키(`노칭::비전검사`)가 그 자리를 대신한다 | 모호한 짧은 이름을 등재하면 소비처가 **조용히 하나를 고른다.** 그것이 구 D5("임의 선택 없이 orphan_anchor")가 막던 사고이며, **극성 모호가 구조 소멸했다고 동명 스텝의 모호까지 사라진 것은 아니다.** seed v3.2 실물에 `비전검사` 2건이 실재한다 | **가결정 (08-12)** · 확정 경로: CH4 4.4 동기화(세션 4) | 레포 자체 검출 (M2 반입 3단계) |
