# DECISIONS.md — 가결정 대장

> **초기값 31건 (D-1~D-31)** + D-32~D-82 = **현재 82건** — CLAUDE.md 「증분 0 v1.4 정정 5항」 1번 판정 기준 + D-58.
> 구 "25건"은 계수 오기였다(D-26~D-31 = 실행검증_1차 §6 누락).
>
> **소유 분담 4계**: D-1~D-14 = `docs/증분0_구현.md` §7 / D-15~D-21 = `docs/raw_mock_대장.md` §6 /
> D-22~D-25 = `docs/판정대조_ipqc_1차.md` §8 / D-26~D-31 = `docs/실행검증_1차.md` §6.
>
> **지위**: 전부 **가결정**이다. 카드·계약과 충돌하지 않는 세부를 정하고 전진하기 위한 것이며,
> 확정 경로가 지정된 항목은 그 세션이 최종 판정한다. 원천 문서의 문면이 정본이고 이 대장은 그 사본이다 —
> 원천이 개정되면 같은 커밋에서 여기도 고친다.
>
> **추가**: D-32 — BLOCKERS 판정필요-1 해소(사용자 판정 08-07). **가결정이 아니라 확정이다.**
> **추가**: D-33~D-37 (08-07~08-08 판정 · 전부 **확정**). D-33~D-35 = G1+G2 정산분 / **D-36·D-37 = 명세 트랙 M1(쟁점 B·C) 반영분.**
>
> **추가**: D-39~D-41 (08-08 레포 자체 검출분의 **소급 등재** — 08-10) · D-42~D-48 (**M2 §8 전달①~⑦**).
>
> **추가**: D-49 (**M2 반입 3단계의 선행 판정 B** — 마커 어휘의 구현 형태 · 명세 트랙) ·
> D-50·D-51 (**3단계 레포 자체 검출분** — mirrors 짝 키 · 짧은 이름 alias 모호).
>
> **추가**: D-52~D-55 (**M2 후속 — G4 전 정리**). D-52·D-53·D-54는 BLOCKERS 판정필요-3·4 종결과
> `stale_skeleton` 큐 미신설 판정이고, **D-55는 D-15·D-18의 prefix 수치를 11 → 12로 옮긴다** —
> 원천(raw_mock_대장 §6)의 문면 갱신은 명세 트랙 몫이다.
>
> **추가**: D-56·D-57 (**G4 0단계 — 판정필요-5의 구현 캐치업**). 판정은 틀 v2.7·카드 v14에서
> 이미 났고 이 둘은 그 구현이다. **D-15·D-18의 CP01 prefix 문면도 11 → 12로 정리했다**(D-55 잔여).
>
> **추가**: D-58 (**허브 20회차 — prose 분할 시점 · 사용자 발의 규범 개정**). 정본은 틀 v2.8 Q2·카드 v16 I6/M6이고
> 전달문 원천은 프로젝트 `안건/D31_판정문안_section지위.md` §16이다. **D-31은 같은 회차에 확정으로 전환**했다(행 갱신).
>
> **추가**: D-59~D-61 (**20회차 반입 후속 — 하네스 수정 · A-3 재실행 · A-4 관통**).
> D-59는 허브 ㉮(큐 비멱등) 요청의 이행이고, **D-60·D-61은 A-4 관통이 검출한 것**이다 —
> 관통을 돌리지 않았으면 둘 다 드러나지 않았다(하네스는 조각까지만 보고 인입을 부르지 않는다).
>
> **추가**: D-62~D-66 (**G5 — n5 I축 도구**). D-62·D-63·D-64는 **R3 종결 3건**(사용자 승인 ·
> 마감선은 G5 착수 전 — CH7 7.5.5 P-8), D-65는 20회차 후속 이월 판정의 근본 해소,
> **D-66은 레포 자체 검출**이다.
>
> **추가**: D-67~D-69 (**G6 — 4′ 플랫폼 + n9**). D-67은 허브 추가 지시(G5 산출물 노출),
> D-68은 계기판 1~6 산식 가결정, **D-69는 S11 전제 공백의 자체 검출**(cp 어댑터 부재)이다.
>
> ⚠ **미러에 번호를 적지 않는다 (08-10 신설 규칙).** `PROGRESS.md`는 **보고**이고 장부는 이 파일 하나다. 번호가 필요하면 **등재한 뒤 인용**한다. D-39~D-41 누락 사고의 전달 경로가 정확히 그것이었다 — 결정을 미러·커밋에만 적어 두자 외부 세션이 그것을 채번된 것으로 읽었다.
>
> ⚠ **번호 발번처는 이 레포 하나다.** M1 핸드오프 문서가 적재 경계·문서 등급을 D-32·D-33으로 적어 보냈으나 그 번호는 이 레포에서 이미 쓰였으므로 **D-36·D-37로 재번호**했다. 외부 세션이 번호를 지정해 오면 **말번 뒤로 재번호한다.**
>
> **생성**: A0(G0), 2026-08-07. 원천 4종에서 기계적으로 옮겨 적었으며 내용을 요약·각색하지 않았다.

| # | 지점 | 가결정 | 근거 | 확정 경로 | 소유 |
|---|---|---|---|---|---|
| **D-1** | **R9/U7** electrode_type의 role | 블록에 넣지 않는다(카드 C10 안전 기본값). 조각 공통의 **구조 필드**로 유지하고 극성 결합은 entity 해소 코드가 record.electrode_type을 직접 읽는다 | role 핸들러가 아니라 해소 코드가 소비(C10). **블라인드 판정(08-01)에서 미결이 배정을 흔든다는 실측 — G3 전 확정 권고** | CH3B 3.5 / 세션 4 | 증분0_구현 §7 |
| **D-2** | **U3** 검증 순서 전체 배열 | 인입 진입: 투입 분류(파서 소유 아님 — C15·⑴-C″) / 파서: preflight→extract→normalizer→tagger→validator / 에이전트: doc_hash→id 계산→필드 검증 (§3 G1 공통) | 파서 측=문서 단위 실패, 에이전트 측=큐 착지로 역할이 겹치지 않음 | **확정 — 세션 1 종결 (2026-08-19, 21회차)**: struct-map(D-58) 편입 전체 배열로 승격. 정본 CH2 2.8 v2.5 | 증분0_구현 §7 |
| **D-3** | 방향 반대(이종 쌍) 큐 | kind `direction_conflict` **신설**(기존 kind 확장 아님) | coord_mismatch 선례 — 큐는 이름 붙은 화면(P-3) | 명세 v1.19 분해 시 | 증분0_구현 §7 |
| **D-4** | doc_hash 보류의 물리 형태 | kind `duplicate_doc_hold` 큐 신설 | 조용히 버리지 않는다(G5) | 명세 v1.19 | 증분0_구현 §7 |
| **D-5** | 계층 미확정 행의 큐 | kind `hierarchy_unresolved` 신설(평면 폴백 청크에 표시+큐) | 틀 Q2의 kind가 미명명이었음 | 명세 v1.19 | 증분0_구현 §7 |
| **D-6** | 관계 목록의 구현 형태 | config `relation_patterns: [{src, rel, dst, symmetric}]` + **관계 정의문 병기**(판정대조 §5.2 실측 반영) | 게이트가 (출발,관계,도착)+대칭 표시를 요구(I5·I8). 정의문 없는 관계 목록은 등록 판정을 막는다는 실측 | 명세 v1.19 | 증분0_구현 §7 |
| **D-7** | 게이트 거부 기록 | data/gate_rejects.json **로그**(큐 아님) — 사유별 건수 | 거부는 처리 대상이 아니라 커버리지 관측 신호(3.4) | — | 증분0_구현 §7 |
| **D-8** | 신규 데이터 파일 | registry.json(등록부) · doc_registry.json(doc_hash 대장) · ops_log.json(I축 연산 로그) | 각 확정 사항의 물리 착지 지정 | — | 증분0_구현 §7 |
| **D-9** | 파서 패키지 분리 | parser/ 별도 패키지, openpyxl·python-pptx 의존 허용 | "외부 패키지 0"은 에이전트 코어(USE_MOCK 경로)의 원칙 — 파서는 사내 실행 별도 프로그램이고 결합은 JSON뿐(P1) | — | 증분0_구현 §7 |
| **D-10** | USE_MOCK 확장 3종 | 추출=힌트 파일 우선+문형 규칙 폴백 / 등록 생성=fixture 반환 / 이미지 요약=고정 문자열 (§5) | mock 목적은 메커니즘 검증 한정(6.3 규약 1) — 결정성 우선 | — | 증분0_구현 §7 |
| **D-11** | **U1** 골격 공유 자산 | data/skeleton_closed_list.json 스냅샷 + skeleton_version — 킷에 동봉 | 파서·에이전트 공유 자산의 국면 1 형태. 사내 배포 경로는 [사내 확인] | **확정 — 세션 1 종결 (2026-08-19)**: 스냅샷+skeleton_version 형태 그대로 승격(실측: 템플릿 v0.3 골격 주입·3차 PASS). 정본 CH2 2.1 규약 4 v2.5. ⚠ 파일 실물은 미생성(G6.5 §8 전제 정정) — 생성은 P1 | 증분0_구현 §7 |
| **D-12** | **U2** 복수값 전개 | normalizer 소유. 구분자 기본 닫힌 목록 [",", "/", 개행] + 어댑터 expects로 doc_type별 재정의 가능 | 전개는 자기완결성 계약(C4)의 실행 — 규칙은 관찰 상수(expects)에 | **확정 — 세션 1 종결 (2026-08-19)**: normalizer 소유·기본 닫힌 목록+expects 재정의 승격(실측: 3차 어댑터·S11/S14). 정본 CH2 2.2 계약③ v2.5 | 증분0_구현 §7 |
| **D-13** | n9 배치 | G6(최후순) | 편의 기능 — 없는 동안 사람 지정만으로 운영 성립 | — | 증분0_구현 §7 |
| **D-14** | record_id의 join 순서 | 매칭 스키마 fields **선언 순서**의 필드 값들을 join | 산식(A4)이 "field_values"의 순서를 미지정 — 처리 순서 무관·스키마 종속이므로 결정적 | 명세 v1.19 | 증분0_구현 §7 |
| **D-15** | CP01의 record 수 | **12건**으로 확정(prefix). C10 = (노칭, anode, 노칭 프레스, 버 높이, 30㎛ 이하 — mirror_asymmetry) / **C11 = 좌표 불일치 행(공정구분 "스태킹" + 공정명 "노칭" → coord_mismatch)** / **C12 = 극성 불일치 행(좌표 `cathode 탭용접` + 극성 `anode` → coord_mismatch — A11-9 ② 실발화)**. 확대분은 **C13~C30**. *(2026-08-13 정정 — 구판 "11건"은 C12 신설 전, 구판 "10건"은 C11 신설 전 수치다. 원천 `raw_mock_대장` v2.2가 이미 12로 갱신됐다. parsed에만 있고 raw에 없는 행은 D-18을 무효화하므로 두 행 모두 raw 실물에 편입한다 — 덧붙임이 아니라 삽입이어야 prefix가 성립한다.)* | — | — | raw_mock_대장 §6 |
| **D-16** | CP·PFMEA·IPQC 헤더 행 | **3행**. 1행 제목, 2행 문서 메타 | — | — | raw_mock_대장 §6 |
| **D-17** | 복수값 전개의 검증 위치 | **CP04_unlabeled · IPQC01**. 역산 정합 대상 파일에는 심지 않는다 | — | — | raw_mock_대장 §6 |
| **D-18** | 역산 정합 어서션 | **prefix 일치**로 정의(**CP01 12** · PFMEA01 13 — 2026-08-13 정정, 구판 "CP01 11"은 C12 신설 전 수치다). 확대분은 뒤에 붙되 기존 판정 무영향 — **CH6 6.3 닫힌 목록 개정 완료(v3, 08-06 채택)** | — | — | raw_mock_대장 §6 |
| **D-19** | TOC 계층의 물리 표현 | 셀 들여쓰기(indent) 속성 | — | — | raw_mock_대장 §6 |
| **D-20** | 정형 등록 시나리오 | 신규 doc_type `ipqc` 등록을 시나리오로 신설 — **CH6 6.3 닫힌 목록 개정 완료(v3, 08-06 채택 → S15)** | — | — | raw_mock_대장 §6 |
| **D-21** | UNMAPPABLE 후보의 판정 근거 | "최근 불량 이력" = L4 신호 / "관련 표준문서" = L3 `reference` role 신호 | — | — | raw_mock_대장 §6 |
| **D-22** | 표본 1부 등록의 edges 취급 | 1부 등록 시 판정자가 선언한 edges를 검수 뷰에서 **특별 확인 대상으로 표시**한다. 경고 문구를 "표본 1부 · 변형 미관찰 · **선언된 관계는 근거 1건일 수 있음**"으로 확장 | 파서_명세 §7 / 세션 1 | — | 판정대조_ipqc_1차 §8 |
| **D-23** | 측정방법 열의 지위 | mock 결함이 아니라 **"판정 불가 유도(정형판)"** 재료로 유지한다. 봉인 정답표의 attribute 배정은 철회 | — | — | 판정대조_ipqc_1차 §8 |
| **D-24** | 공정번호 체계 | 골격 순서에 정렬(10·20·30·31·40·50·60). 전 raw mock에 일관 적용 | — | — | 판정대조_ipqc_1차 §8 |
| **D-25** | Unit 정의문 | 검사·계측 장비의 포섭 여부는 **국면 2 [사내 확인]**으로 이관. 국면 1에서는 Unit으로 두되 이 유보를 기록 | 국면 2 | — | 판정대조_ipqc_1차 §8 |
| **D-26** | `USE_MOCK` fixture의 정의 | fixture는 "미리 만든 정답"이 아니라 **"외부 세션에서 실제 LLM이 산출한 결과물의 스냅샷"**이다. 사람이 손으로 쓴 fixture는 리허설을 무효화한다 | 증분0 §5-2 개정 | — | 실행검증_1차 §6 |
| **D-27** | 킷 구성물 | **실행 하네스**를 킷 6번째 구성물로 신설. 검수 뷰(사람) **이전**에 기계가 거르는 관문 | 파서_명세 §9 개정 | — | 실행검증_1차 §6 |
| **D-28** | mock 표본 규모 | 정형 24행 이상 / 비정형 25행 이상 / PPT 8슬라이드 이상을 기준으로 한다. head 관찰 규칙(M5)이 시험되려면 문서가 head보다 커야 한다 | — | — | 실행검증_1차 §6 |
| **D-29** | `expects`의 표류 지문 | **`header_labels`(원본 헤더 문자열 배열)를 필수 키로 신설.** `columns`는 출력 필드명 매핑으로 용도 확정 | 파서_명세 §5 개정 | — | 실행검증_1차 §6 |
| **D-30** | UNMAPPABLE 열의 처리 | 스키마 `fields`에 넣지 않고 어댑터 출력에서도 제외한다. 템플릿에 명시 + 인입 코드에 `invalid_role` 방어 | 템플릿 v0.3 / n7 | — | 실행검증_1차 §6 |
| **D-31** | `section`의 role | **확정(2026-08-18, 20회차)** — `section` 포함 payload 고정 키 4종(`text`·`section`·`meta`·`image_ref`) = **payload 구조 필드**(role 배정 대상 아님). prose 매칭 스키마의 `fields`는 `{}` — 층·블록 선언이 계약의 전부. 정본: 카드 C17 · CH2 2.2/2.5/2.6 | UNMAPPABLE 선택지는 계약①(모든 조각은 정확히 한 층)×D-30과 모순 — 논리적으로 불가능했다. 실결함은 템플릿의 payload_kind 비분기(v0.3에서 해소) | **확정 — 허브 20회차 (08-18)** · 판정문안 §7.2 | 실행검증_1차 §6 · 판정문안 `안건/D31_판정문안_section지위.md` |
| **D-32** | 기존 구현물 처리 | **전량 신규 작성.** 이전 레포 회수분은 **참조용 한정**이며 코드로 반입하지 않는다 | 구 구현물은 **계약 v1 산물**(chunk_id 발급·N#### 의미 축 id·커밋 게이트 없음)이라 **v2 이식 비용 > 신규 작성 비용**이고, 낡은 전제가 코드로 유입될 위험이 있다 | **사용자 판정 08-07 (확정)** | 사용자 판정 |
| **D-33** | C11 잔여 열 값 | **노칭 프레스 · "노칭 피치" · 50±0.2mm** 승인. `raw_mock_대장` v2.1 §2.1이 C11의 메커니즘(coord_mismatch)만 명세하고 나머지 열을 비워둔 자리를 채운 것이다 | 설비는 **기존 Unit 재사용**이라 새 노드가 생기지 않고, 관리항목은 신규이나 어서션이 거는 "적층 정렬도"·"금형 클리어런스"와 **비충돌** | **사용자 판정 08-08 (확정)** | 사용자 판정 |
| **D-34** | mock/parsed 생성 방식 | `tools/gen_parsed_mock.py`가 **raw prefix에서 파생**시킨다. **예외: PPT01은 대응하는 raw가 없으므로 수기** | 수기본과 raw를 이원화하면 둘이 어긋나고 그 어긋남이 곧 **D-18(역산 정합) 무효화**다. **진실은 `verify_roundtrip`의 수기 기대값 튜플(독립 정답)**이며 사슬은 **"수기 기대값 → raw 검증 → parsed 파생"의 단방향**이다 — **기대값 튜플을 raw에서 자동 생성하는 것은 금지한다**(그 순간 검증이 자기 자신을 검증하게 되어 아무것도 시험하지 못한다) | **사용자 판정 08-08 (확정)** | 사용자 판정 |
| **D-35** | GraphStore 경계 정의 | 경계는 "여는 코드"뿐 아니라 **"저장 위치를 아는 코드"까지**다. 경로 조립은 `GraphStore.for_layer()`가 소유한다 | 밖에서 경로를 조립하면 저장 방식을 바꾸는 날(R10) 고칠 곳이 다시 여러 군데가 된다 — 경계가 하나가 아니면 경계가 아니다 | 카드 B6 문구 보강 (명세 트랙, **비차단**) | 사용자 판정 |
| **D-36** | 적재 경계 — 틀 §4B-A9 | 그래프에 들어오는 것은 **문서 경유 지식뿐(T0~T2)**이다. 생산 관측 데이터(시계열·검사 실측·로트/셀·런 이력·불량 레코드 직수신)는 **적재하지 않는다** — 파이프라인에 **T3 수신 경로를 만들지 않는다**. **로트 번호는 개체가 아니라 attribute/meta**로 다룬다 | 3테스트는 R1 층 등록 게이트의 재료다(세션 4) | **사용자 판정 08-07 (확정)** | 사용자 판정 (M1 핸드오프의 D-32 재번호) |
| **D-37** | 문서 등급 — 틀 §4B-A10 | 파서 봉투에 `classification?` **선택 필드**(널 허용·자유 문자열). **현 국면에서는 아무도 채우지 않고 인입 코드는 이 값을 읽지 않는다** — 어댑터가 채우지 않아도 **계약 위반이 아니며** validator는 존재를 강제하지 않는다. **봉투 스키마에 자리만 인정하면 끝이고 복사·소비 로직을 만들지 않는다.** 등급은 문서의 성질이므로 **노드에 등급 필드를 만들지 않는다** | 권한 로직·값 어휘·집계 범위 명시는 전부 **R12 이연** | **사용자 판정 08-07 (확정)** | 사용자 판정 (M1 핸드오프의 D-33 재번호) |
| **D-38** | CH2 파일명 중복 | 신판 `Ch2_문서계약.md`(v2.1)를 **정본 경로 `docs/CH2_문서계약.md`로 이동**하고 구판 v2를 대체한다. 소문자 `Ch2` 파일은 남기지 않는다 | M1 반영분이 소문자 `Ch2`로 올라와 **교체가 아니라 중복**이 됐다. 정본 경로에 구판(v2 — `classification` 없음)이 남아 있으면 **CH2를 계약 정본으로 읽는 G3(1c′)이 구판을 읽는다.** MANIFEST 트리가 파일명의 정본이고 그것은 대문자 `CH2`다. 신판이 구판의 **완전한 상위집합**(잃은 줄 0)임을 diff로 확인했다 | 다음 문서 반입 시 파일명 확인 (비차단) | 이 레포 |
| **D-39** | alias 매칭 범위 | **alias는 정확 일치만.** 문자열 포함 규칙은 canonical에만 적용한다 | alias는 조립 전 표면형이라 포함 규칙을 걸면 `cathode 노칭 프레스`가 무극성 노드에 흡수된다 — 다른 실물이 한 노드가 되는 사고다 | **08-08 판정, 08-10 소급 등재** — PROGRESS·커밋에만 기록되어 있던 것을 장부로 옮김 | 레포 자체 검출 (G3) |
| **D-40** | 극성 정체성 | 극성이 다르면 **감점이 아니라 후보에서 제외**한다. 문자열 휴리스틱이 아니라 **정체성 규칙**으로 건다 | `cathode 노칭 프레스`와 `anode 노칭 프레스`는 표기 차이가 아니라 다른 실물이고 다른 규격을 갖는다(CH3A 3.3 경계) | **08-08 판정, 08-10 소급 등재** — PROGRESS·커밋에만 기록되어 있던 것을 장부로 옮김 · **M2 확정과 정합**(A11-8 구조 필드 + polarity 파생 필드, canonical 비파싱) — 재대조 종결은 필드명 `electrode_type`→`polarity` 정리와 함께 3단계에서 | 레포 자체 검출 (G3) |
| **D-41** | seed 멱등성 | 부트스트랩은 **canonical 기준 upsert**다 — 이미 심긴 골격은 다시 심지 않는다 | 의미 축 id가 발급(ULID)이라 다시 심으면 같은 개념에 새 id가 붙어 노드가 배로 늘고(60→72 실측) provenance·엣지가 갈라진다. 노드 유일성(P4) 위반이며 `run.py all` 2회 = 동일 그래프(§8)도 깨진다 | **08-08 판정, 08-10 소급 등재** — PROGRESS·커밋에만 기록되어 있던 것을 장부로 옮김 | 레포 자체 검출 (G3) |
| **D-42** | 골격 두 축 분리 + seed v3.2 [M2·A11] | **part_of=구조 / precedes=지식(대표 흐름).** seed 형식 v3.2 = 단일 파일 `skeleton.json`(PROCESS_TREE 순서 배열 + ALIASES 블록 + POLARITY_LABELS) — **배열 순서가 곧 대표 흐름 선언**, 비참여 3종(`::`인스턴스 자동 · `{"@unordered":<항목>}` 개별 · `"@noflow"` 부모 전체), 마커 **닫힌 7종**. 극성 인스턴스 간 순서는 구조적 미생성 | 공장·세대별 실제 라우팅은 골격에 넣지 않는다 — 골격 밖(**R13 이연**) | **M2 확정 (08-10)** · loader 반영은 3단계 | M2 §8 전달① |
| **D-43** | R9 종결 — electrode_type·polarity [M2·A11-8] | **R9 종결 = D-1 확정 승격.** `electrode_type`은 **구조 필드**(1c′ ④ 유효). 추가로 해소 코드가 노드(Unit·Property)에 **`polarity` 기록 — `cathode`/`anode`/`none`/`unbound` 닫힌 4값**. 스키마 불가침, **canonical 비파싱** | 레포 D-40(극성 정체성)과 **정합** | **M2 확정 (08-10)** · 3단계에서 필드 정리 | M2 §8 전달② |
| **D-44** | 순서 파생(traverse) [M2·A11-5] | 자기 선언 > 부모 파생 + 동일 polarity 인스턴스 하강(없으면 개념=합류) > "순서 정보 없음". **파생 답에 해상도를 표기**한다 | 착지는 **G4** — G3에는 무영향 | **M2 확정 (08-10)** · G4 착지 | M2 §8 전달③ |
| **D-45** | 좌표 닫힌 목록 [M2·A11-6·7] | **좌표 닫힌 목록 = 골격 전 노드**(D-11 스냅샷에 개념·인스턴스 전부). `process_group` = **tier:main 조상** | **D5(극성 모호) 구조가 소멸**한다 — "목록 밖 이름"만 잔존 | **M2 확정 (08-10)** | M2 §8 전달④ |
| **D-46** | 킷 템플릿 v0.3 — 구조 필드 [M2·§7] | 템플릿 v0.3에 **구조 필드 목록은 6지선다 대상이 아님**을 명시한다 | D-30과 **같은 커밋**으로 반영 | **M2 확정 (08-10)** · P2 | M2 §8 전달⑤ |
| **D-47** | canonical 접두 + alias 원천 [M2] | sub/detail canonical = **부모 경로 균일 접두**. **seed ALIASES가 사전의 원천** — n10이 `dictionary.json`에 `provenance:["seed"]`로 등재(**장부는 하나** — B4, 운영 축적분과 태그로 구분. seed 개정 시 seed 유래 엔트리만 갱신). 짧은 이름 자동 alias(개념 노드) + 인스턴스 auto alias = `{극성영문} {이름}`·`{극성한글} {이름}`(POLARITY_LABELS: cathode=**양극** · anode=**음극**) | 공통 alias는 **개념만** 소유한다 | **M2 확정 (08-10)** · loader 반영은 3단계 | M2 §8 전달⑥ |
| **D-48** | 부착 정합 2규칙 + mock 재매핑 [M2·A11-9] | 1c′에 2규칙: **①부착 골격 노드 `polarity≠none`이면 표면형 극성 결합 생략**(주소에 이미 극성이 있으므로 이중 표기 방지 — `polarity` 필드는 기록) **②record `electrode_type` ↔ 노드 `polarity`가 둘 다 확정인데 다르면 `coord_mismatch` 큐**로 표면화(조용히 한쪽을 택하지 않는다) | mock raw 재매핑: **"실링" 행만** 사이드/프리 실링으로 행별 교체(나머지 공정명은 alias로 자동 정합 — `skeleton_seed_v3.2.md` 하단 대조표). `process_no` 재부여는 재량 | **M2 확정 (08-10)** · 3·4단계 | M2 §8 전달⑦ |
| **D-49** | **마커 어휘의 구현 형태** [M2·A11-4 / 카드 B1] | loader에 `cathode`/`anode` 문자열을 **쓰지 않는다.** 코드가 아는 것은 **구문 마커 4종**(`::` 접두 · `@split` · `@unordered` · `@noflow`)뿐이고 **축의 값은 seed의 `POLARITY_LABELS` 키에서 읽는다**: `::X`는 X ∈ keys일 때만 인스턴스 마커, `@X`는 단극성(개념+인스턴스 1), `@split`은 전 키 전개. 판정문의 "마커 닫힌 7종"이 코드에서는 **"구문 4종 + 데이터에서 온 값 N개"**로 구현된다. 어휘 밖 마커는 **명시적 실패** | 극성 값은 도메인 어휘이고 코드에 박히면 B1 위반이다(예외는 파서 계약 필드 `electrode_type` 한 건뿐 — M2 §11-2). **identity_axis 일반화(안 D) 이연분의 절반이 여기서 공짜로 선다** — 축이 바뀌어도 loader는 그대로다 | **명세 트랙 판정 (08-12)** · 3단계에서 구현 완료 | 명세 트랙 (M2 후속) |
| **D-50** | mirrors 짝 찾기의 키 [M2·F3 — 레포 자체 검출] | 짝 찾기를 **canonical 되 파싱으로 하지 않는다.** 극성 결합 시점에 노드에 `base_canonical`(스코프는 붙고 극성은 안 붙은 이름)을 적어 두고 그것 + `polarity` 필드로 페어링한다. **Tier1(seed) 노드는 build의 자동 규칙에서 제외**한다 — 골격 mirrors는 loader가 잇고 Tier1 단극성은 `mirror_asymmetry` 대상이 아니다(A11-4) | 구 `strip_polarity()`는 **앞에 붙은** 접두만 뗄 수 있어 스코프가 붙은 관리항목(`노칭::anode 노칭 정밀도`)에서 조용히 실패했다 — **정상 극성 쌍 1쌍이 페어링되지 않고 `mirror_asymmetry` 큐로 샜다**(실측 큐 3건, 문서·미러의 "1건"과 불일치). F3의 "문자열 극성 토큰 파싱 폐지"가 가리키던 실물 결함이다 | **레포 자체 검출 (08-12)** · 수정 후 큐 1건(`노칭::버 높이`)으로 문서와 일치 — **시점 주석(G4 정산 08-18)**: 이 "1건"은 C12 삽입 전 측정값이다. 현행 유니크 4건(탭용접::cathode::용접 가압력 포함) 전부 참 — 시점 차이지 결함이 아니다 | 레포 자체 검출 (M2 반입 3단계) |
| **D-51** | 짧은 이름 auto alias의 모호 처리 [M2 §4 alias 규칙] | 개념 노드의 **짧은 이름 자동 alias는 층 안에서 유일할 때만 사전에 등재한다.** 모호하면(같은 짧은 이름이 여러 노드) **등재하지 않고** loader 출력에 표기한다 — 접두 키(`노칭::비전검사`)가 그 자리를 대신한다 | 모호한 짧은 이름을 등재하면 소비처가 **조용히 하나를 고른다.** 그것이 구 D5("임의 선택 없이 orphan_anchor")가 막던 사고이며, **극성 모호가 구조 소멸했다고 동명 스텝의 모호까지 사라진 것은 아니다.** seed v3.2 실물에 `비전검사` 2건이 실재한다 | **가결정 (08-12)** · 확정 경로: CH4 4.4 동기화(세션 4) | 레포 자체 검출 (M2 반입 3단계) |
| **D-52** | 게이트 경로 ①의 지위 [틀 §4B-A3 · CH3A 3.4] | **seed는 후보가 아니라 선언이다.** 골격 엣지가 커밋 게이트를 지나지 않는 것은 우회가 아니라 설계이며, `relation_patterns`에 **골격 관계(`Process part_of/precedes/mirrors Process`)를 넣지 않는다.** 층이 어떤 관계를 갖는지의 선언은 config `relations` 배열이 하고, `relation_patterns`는 별개의 **추출용 게이트 패턴**이다. gate.py의 "넷 모두 같은 관문" 서술과 죽은 `PATH_SEED` 상수를 정정·제거했다 | 패턴표를 골격까지 덮도록 넓히면 **경로 ③(추출)이 골격을 개정**할 수 있게 된다 — A5 하향 한정·발명 금지 ③ 위반. 반입 시 표에 `Process precedes Process`가 **이미 있어** 경로 ②(스키마 edges 선언)가 골격 흐름을 쓸 수 있었다(A11-2 "순서의 출처는 seed 하나뿐"과 충돌) — 소비처 0곳·킷 템플릿 v0.2의 "골격이 이미 보유하므로 스키마에서 선언하지 않는다"와 정합하여 제거했다 | **명세 트랙 판정 (08-13)** · BLOCKERS 판정필요-3 종결 · 회귀 잠금 4항 | 명세 트랙 (M2 후속) |
| **D-53** | seed 형식의 블록 이름 [카드 B1] | `PROCESS_TREE` → **`TREE`** · `POLARITY_LABELS` → **`AXIS_LABELS`**로 **개명**한다(상수 격리 아님). 노드 필드 `polarity`와 config `polarity` 블록은 **그대로 둔다** — A11-8이 정한 시스템 어휘이자 B1의 명시된 예외이며 해소는 identity_axis 일반화로 이연 | B1은 최상위 불변식이고, 형식의 블록 이름이 특정 층·특정 축을 달고 있으면 그것을 읽는 loader에 층 어휘가 샌다. 상수 격리는 **위반을 유지한 채 숨기는 것**이다. seed 실물 1개·의존 코드 1곳인 지금이 가장 싸다 | **명세 트랙 판정 (08-13)** · BLOCKERS 판정필요-4 종결 · 개명 후 기대값 5종 불변 확인 | 명세 트랙 (M2 후속) |
| **D-54** | `stale_skeleton` 큐 — **신설하지 않는다** | 큐 20종은 **닫힌 목록**이고 골격 잔존은 운영 상시 유입이 아니라 **일회성 마이그레이션 산물**이다. 개발 국면의 정본 경로는 **깨끗한 재빌드**, 운영 국면의 정본 경로는 **I1 개명**(id 불변 · 옛 canonical은 alias 강등)이므로 잔존은 구조적으로 발생하지 않는다. loader의 ⚠ 경고 + `defects.log`는 **개발 안전망**으로 유지 | 큐는 이름 붙은 화면(P-3)이고 처리 주체가 있는 것만 올린다 — 처리 주체가 "재빌드"인 항목에 큐를 파면 닫힌 목록이 사유 없이 늘어난다 | **명세 트랙 판정 (08-13)** · 종결 | 명세 트랙 (M2 후속) |
| **D-55** | A11-9 ② 실발화 mock [M2·A11-9 · D-18] | `CP01.xlsx`에 좌표 `cathode 탭용접`(→ `탭용접::cathode`) + 극성 `anode` 행을 **record 12 자리에 삽입**하고 **CP01 prefix를 11 → 12로 확장**한다. C1~C11 불변, 확대분은 C13~C30으로 밀린다. 기대 결과 = `coord_mismatch` 큐 1건(표면화만 — 자동 정정 아님) | 규칙 ②는 반입 시점에 배선만 되고 **한 번도 발화하지 않았다** — 좌표가 인스턴스로 해소되면서 record 극성이 반대인 행이 mock에 없었기 때문이다. 발화시키려면 행이 **prefix 안에** 있어야 하므로(확대분은 인입되지 않는다) prefix 확장이 따라온다 — C11 삽입(10→11, D-15)과 같은 사유·같은 방식이다. 사슬은 단방향 유지(수기 기대값 튜플 → raw 검증 → parsed 파생) | **08-13** · **D-15·D-18의 "11건" 수치를 12로 갱신 — 명세 트랙 확인 요망** | 레포 (M2 후속 4항) |
| **D-56** | F3 하향 연쇄의 짝 키 [틀 v2.7 · 카드 F3 · CH3B v2.2 3.5 규약 6] | mirrors 짝 키를 canonical 전문이 아니라 **세 요소**로 잡는다 — **①부모**(축 인스턴스이고 그 인스턴스에 mirrors 엣지가 있으면 개념 노드로 **동일시**) **②주소 접두를 제외한 자기 이름부** **③polarity 반대**. 노드에 `mirror_scope`·`mirror_name`을 조립 시점에 적어 두고(canonical 비파싱 — A11-8) `link_mirrors`가 그 둘로 묶는다. 구 `base_canonical` 단일 키는 폐기 | 구판은 `탭용접::cathode::용접 강도`와 `탭용접::anode::용접 강도`가 **둘 다 있어도** 키가 달라 영영 페어링되지 않고 전부 편측 큐로 샜다(판정필요-5 ①). **짝 없는 인스턴스는 동일시하지 않는다** — mirrors 엣지가 없으면 그 자리는 분화 쌍이 아니고, 올려 잡으면 무관한 자식들이 한 키로 뭉친다 | **틀 v2.7 판정의 구현 (08-13)** · BLOCKERS 판정필요-5 ① 종결 · 회귀는 개수가 아니라 **구조**로 잠금(오검출 0) | 레포 (G4 0단계) |
| **D-57** | A11-9 ① 생략의 적용 범위 [틀 v2.7 · 카드 F1 v14 · CH3B v2.2 3.5 규약 3] | 표면형 극성 결합 **생략은 스코프 카테고리(현행 Property)에 한정**한다. 스코프가 없는 카테고리(**Unit**)는 극성이 주소에 실리지 않으므로 표면형 표기가 이중이 아니다 — **F1 극성 결합을 유지**한다. `canonical_scope.bind_categories`는 건드리지 않는다(Unit 스코프 확대 = 선택지 가는 미채택) | 무한정 생략을 Unit에 적용하면 **polarity 필드만 다르고 canonical이 같은 노드 2개가 공존**한다(P4 취지 위반 — C12 실측: `초음파 융착기`(none)와 `초음파 융착기`(cathode)). 생략의 근거가 "극성이 이미 주소에 있다"인데 Unit에는 그 주소가 없다 | **틀 v2.7 판정의 구현 (08-13)** · BLOCKERS 판정필요-5 ② 종결 · canonical 중복 0건 확인 | 레포 (G4 0단계) |
| **D-58** | prose 분할의 시점 [틀 v2.8 Q2 개정 · 사용자 발의] | 구조 가변 prose는 분할 규칙을 **등록 상수로 고정하지 않는다** — 같은 doc_type 안에서 구조가 다른 것이 기본값이다(사용자 실측: RFQ 판본별 heading pattern 상이). 운영 인입마다 **구조 지도 패스**(LLM이 지도=데이터 산출, 코어 소유·C12 동형) → 지도 타당성 검사(결정적 — 실패는 평면 폴백 + `hierarchy_unresolved`/`parse_failure`, 큐 신설 없음) → 결정적 분할기(지도 적용). 지도는 보존. 어댑터 순수성(C11)·doc_type당 1어댑터·검수 1회(M9)·운영 무인 불변. table·분할 자명 prose는 현행 유지 | 틀 Q2 근거 전제("같은 양식")가 prose에서 불성립 · 파서_명세 ⑴-C″("비정형은 구조 신호 변별력 없음")과의 내부 긴장 · prose 인입은 이미 문서당 LLM 사용(추출 n3·이미지 요약) — 비용 급 불변 · 고정 상수도 R17(헤딩 개정 → id 이동)을 피하지 못함 | **확정 — 허브 20회차 (08-18)** · 정본 틀 v2.8 Q2·카드 v16 I6-④/M6 · 파서_명세 세션 1P·구현 P1/P2 후속. 템플릿 v0.3은 D-31 최소형(prose `fields:{}`)으로 선행 | 허브 (판정문안 제2부 §16) |
| **D-59** | 큐 적재의 멱등성 [CH3B 3.5 규약 6 · 3.7 · 3.8 규약 6] | **싣는 쪽과 내리는 쪽을 가른다.** ①`store.enqueue`가 **(kind, doc_id, payload) 동일 항목을 두 번 싣지 않는다**(규약 6 "큐는 쌍 키로 중복 제거") ②재계산하는 쪽이 **자기 소관 범위를 먼저 걷어내고** 현재 스냅샷을 다시 싣는다 — `link_mirrors`가 `store.drop("mirror_asymmetry", 이 층 노드)`를 부른다(규약 6 "대칭이 회복되면 큐에서 제거"). 새 kind 0 · 새 데이터 파일 0 | 허브 진단은 `link_mirrors` 전역 재스캔이었으나 **실측은 더 넓다** — `coord_mismatch`·`orphan_anchor`·`spec_conflict`·`direction_*`이 전부 증식한다(`run.py all` 1회 → 2회에 2→4·1→2). 원인은 `enqueue`가 무조건 append하는 것이라 **적재 지점 하나**를 고치는 것이 옳다. **문서 단위 일괄 회수(`queue.remove_doc`)는 채택하지 않았다** — 그러면 재검출되지 않는 **상시 조건**(`auto_node` 51건 = 미검토 노드 작업목록)까지 재인입이 지워 버린다(실측 61 → 11). 계약의 "옛 큐 회수"는 조건이 해소된 항목을 내리라는 뜻이지 화면 전체를 비우라는 뜻이 아니다 | **레포 자체 판단 (08-18)** · 허브 ㉮ 요청의 이행 · 실측 1회 61 → 2회 62 → 3회 62(안정) | 레포 (20회차 반입 후속) |
| **D-60** | 조각 `context`의 형태 위반 방어 [CH2 2.2 · D-30 계보] | `context`는 **임의 딕셔너리**가 계약이다(CH2 2.2). 스칼라로 오면 인입 코드는 **예외로 죽지 않고** `missing_field` 큐로 표면화한 뒤 그 필드만 버리고 전진한다. 새 kind를 만들지 않는다 — `missing_field`가 "필수 값 부재·**계약 위반**"을 이미 덮는다(CH3B 3.7 · 닫힌 20종) | 3차 산출 어댑터(`mock/fixtures/adapters/ipqc.py`)가 `"context": "M1"`을 낸다 — 정답은 `{"model": "M1"}`. A-4 관통에서 `ValueError: dictionary update sequence element #0`으로 **인입이 통째로 죽었다**. D-30이 "알 수 없는 role에 KeyError로 죽지 말라"고 정한 것과 같은 계열이며, 한 필드의 형태 오류로 나머지 33행의 정상 지식까지 잃을 이유가 없다. **fixture는 손대지 않았다**(D-26 — 그 위반 자체가 관측 데이터다) | **레포 자체 검출 (08-18)** · A-4 관통 실측 33건 표면화 | 레포 (20회차 반입 후속) |
| **D-61** | 스키마 `edges` 끝점의 `@` 해소 대칭 | `@` 접두는 **필드가 아니라 그 레코드의 공정좌표**를 가리키는 표기이며 `from`·`to` **양쪽에서 같게 해소한다.** 부착 대상 좌표는 `process_ref` 하나이고(③ — `process_group`은 조상 대조만), 그 밖의 `@` 표기는 해소하지 않되 **결함 로그로 드러낸다** | 구판은 `to`에서만 `@process_ref`를 해소하고 `from`에서는 `resolved.get("@process_ref")` → `None`으로 **조용히 버렸다.** 하네스의 참조 무결성 검사는 양쪽을 같게 보므로(`t[1:] if t.startswith("@")`) 스키마는 정당한 선언이었고, 큐도 로그도 없이 사라졌다 — A-4 관통 실측: ipqc의 `@process_ref has_property 검사항목`이 **0건** 적재(수정 후 28건) | **레포 자체 검출 (08-18)** · A-4 관통 기대 확인 ①의 전제 | 레포 (20회차 반입 후속) |
| **D-62** | 병합(I2) 생존자 우선순위 [R3 종결 ⑴] | **3단 규칙**. ①사람 override(항상 최상위) → ②status 등급(`seed` > `registered` > `auto` — 병합에 seed가 관여하면 생존자는 무조건 seed) → ③정렬상 앞선 id(ULID = 생성순, 결정적). **seed끼리의 병합은 쓰기 거부**한다 — 골격은 loader 소유이고 골격 변경의 정본 경로는 I1 개명 또는 seed 개정이지 병합이 아니다 | auto가 seed를 흡수하면 **골격이 데이터 조작으로 훼손된다.** 증분0의 "정렬상 앞선 id" 단일 규칙은 이 경우를 막지 못한다 — L7이 말한 "기준이 하나뿐인 이유는 정확성이 아니라 결정성"은 유효하되, 결정성을 지키면서 골격을 보호하는 등급을 그 위에 얹는다 | **사용자 승인 — 허브 R3 종결 (08-18)** · CH7 7.5.5 P-8 마감선 | 허브 (R3-⑴) |
| **D-63** | 분리(I3) UI와 배분표 [R3 종결 ⑵] | 국면 1은 **CLI + 배분표 파일**이 전부다(신규 UI 없음). 배분표 = JSON — `{"targets": [{"canonical", "aliases", "provenance", "edges"}…]}`. **배분표 없이는 거부**(자동 분리 경로 없음). **지정되지 않은 잔여가 있으면 실행 거부 + 잔여 목록 출력.** 새 노드의 canonical로 승격된 표기는 배분된 것으로 센다 — 잃은 것이 아니라 이름이 됐다. 화면형 UI는 검수 뷰(S12)와 함께 국면 2 이연 | 분리가 어려운 이유는 "어느 것이 어느 쪽인가"이고 그 판단은 코드가 대신할 수 없다(L5·I3 자동 불가). 잔여를 조용히 한쪽에 몰아 넣으면 사람이 그것을 영영 모른다 — **조용한 유실 금지**(G5 원칙) | **사용자 승인 — 허브 R3 종결 (08-18)** | 허브 (R3-⑵) |
| **D-64** | obsolete 질의 노출 [R3 종결 ⑶] | **기본 비노출·전이 답변.** 질의가 obsolete 노드에 닿으면 `replaced_by`를 따라 대체 노드로 답하고 **"(대체됨: 구명칭 → 신명칭)"**을 표기한다. `replaced_by` 없는 obsolete는 일반 결과에서 제외하되 **정확 이름으로 직접 지명한 질의에는 "폐기됨"을 명시**해 답한다. 툼스톤·provenance·이력은 보존. 표기 문안은 **config `fact_templates`가 소유**한다(J3 유지) | 침묵 소실 금지 — D-30 계보. 폐기된 것을 조용히 없는 것처럼 답하면 사람은 자기 질문이 틀렸다고 오해한다. 전이 추적의 순환·깊이 방어는 읽기 측 2겹의 절반이다(L8) | **사용자 승인 — 허브 R3 종결 (08-18)** | 허브 (R3-⑶) |
| **D-65** | 전역 재평가의 실행 시점 [20회차 후속 이월 판정] | mirrors 재평가(drop + 재적재)를 **문서 빌드에서 빌드 말미 패스로 이설**한다(`pipeline.finalize`). 노드에 짝 키를 적는 자동 규칙(`mirror_scope`·`mirror_name`)은 층 빌드에 그대로 두고 **재평가·큐 갱신만** 옮긴다. 말미 패스가 다는 큐 항목의 `doc_id`는 **`None`** — 이 조건은 한 문서의 것이 아니라 그래프 전체의 상태다 | 문서 빌드 중에 돌면 **뒤에 인입되는 문서가 만드는 극성 노드를 1회차에 못 본다**(실측 1회 3건 → 2회 4건). doc_id를 특정 문서에 귀속시키면 그 문서를 다시 넣을 때만 갱신되는 같은 문제로 되돌아간다 | **허브 판정 (08-18)** · 완료판정 충족: 깨끗한 상태 `run.py all` **1회 만에 큐 62** · 2회 불변 · auto_node 51 보존 · 그래프 동일 | 허브 (이월분) |
| **D-66** | I축 연산의 대상 자격과 로그 [카드 L5·L8 · D-7 계보 — 레포 자체 검출] | ①**툼스톤은 연산 대상이 아니다** — `merged_into`를 가진 id에 대한 개명·병합·분리·폐기를 쓰기 시점에 거부한다. canonical로 노드를 찾는 코드는 전부 생존 판정(`is_live`)을 거친다. ②**행위자 없는 I축 연산은 거부**한다 — `ops_log`의 5요소(연산·행위자·시점·대상·사유) 중 하나가 비면 로그가 로그가 아니다. `--actor` 필수 | **툼스톤이 `canonical`을 그대로 지니기 때문**이다(옛 이름 표시용). 조회가 그것을 거르지 않으면 옛 이름이 산 노드를 가린다 — 실측: 병합 툼스톤을 폐기 처리해 한 노드가 `merged_into`와 `obsolete`를 **동시에** 갖는 모순 상태가 됐고, 그 canonical이 살아 있는 동명 노드와 중복으로 잡혔다 | **레포 자체 검출 (08-18)** · S4 I-5에 회귀 잠금. **[G6.5 정정] 생존 판정 지점은 5곳이 아니라 6곳이다** — `matcher.resolve`의 ②후보 검색이 빠져 있었고(툼스톤에 MATCH 0.95 실측), G6.5 묶음 ③에서 채웠다 | 레포 (G5) |
| **D-67** | G5 산출물의 플랫폼 노출 [4′ 정의의 적용 — 허브 추가 지시] | 4′ 노출 목록에 **`ops_log` 열람**(I축 연산 이력 — 5요소 표시)과 **툼스톤 계수**(층별 `merged_into`·`obsolete` 건수)를 더한다. 구현은 `cli/platform.py ops` — 읽기 전용 관측이며 그래프·큐에 쓰지 않는다 | 4′의 정의가 "신규 산출물 노출"인데 증분0 문면은 G5 이전(13회차)에 동결되어 I축 산출물이 목록에 없다 — **정의의 적용이지 확장이 아니다.** 툼스톤은 그래프 안에 남는 산출물이라 계수가 없으면 수정 이력의 규모를 화면에서 볼 수 없다 | **허브 지시 — G6 요청문 §1-5 (08-19)** · 등재 후 구현 | 허브 (G6) |
| **D-68** | 계기판 1~6의 국면 1 산식 [CH5 5.5 · 명세 §10 — 측정 방법 상세는 "최후" 유보였다] | ①**recall류는 mock 스모크 12문항**: 1 recall = 링킹 기대 문항(expected_path ≠ general) 중 링킹 성공률 / 5 미스율·4 잘림률 = 12문항 분모의 실측 비율. ②**mock 인입 실측**: 2 plateau = 문서별 신규 개체율(첫 출처 문서 기준 — provenance 첫 locator, seed 제외) / 3 보류율 = 큐 건수 ÷ 그래프·청크에 발자국을 남긴 조각 수 / 6 허브 차수 = 층별 상위 차수(J9). ③**측정 무오염** — 계기판 실행 중에는 운영 로그 적재(link_miss)를 끈다: 측정이 재료 로그를 오염시키면 다음 측정이 자기 흔적을 센다. ④계기판은 **별도 호출**로만 계산한다 — build·query 경로에 심지 않는다(8번이 자기 자신을 오염시키지 않게) | 골든셋(120문항)은 국면 2 실물 문항의 장치라 지금 recall의 데이터 기준이 없다 — G6 요청문이 "recall류는 스모크 12문항, 나머지는 mock 인입 실측"으로 기준을 줬고 구체 산식은 레포가 채웠다. 보류율 분모의 한계(어디에도 발자국을 남기지 않은 조각은 계수 밖)는 산식에 명시해 둔다 — mock 계기판은 메커니즘 검증이지 품질 측정이 아니다(§16.3) | **가결정 — 레포 (08-19)** · 확정 경로: 국면 2 골든셋 구성 시 1·4·5의 분모를 골든셋으로 교체 | 레포 (G6) |
| **D-69** | n9 어댑터 소재지와 참조 cp 어댑터 [S11의 전제 공백 — 자체 검출] | ①n9 스캔의 어댑터 소재지: **등록부(P3 n6)가 서기 전에는 CLI 인자 + 기본 소재지 2처**(`mock/adapters/` · `mock/fixtures/adapters/`)가 그 자리를 대신하고, 등록부가 서면 **registry의 expects가 정본**이 된다. ②**참조 cp 어댑터를 `mock/adapters/cp.py`로 신설** — fixtures 밖이다(fixtures는 LLM 실산출 스냅샷이라 손대지 않는다, D-26). extract는 verify_roundtrip 참조 구현과 같은 결과(CP01 30 · CP04 12 record)를 내고, 구조는 3차 실산출 ipqc.py의 형태를 따른다. ③후보 판정 = **header_labels 완전 일치**(누락 0·잉여 0) — 부분 일치는 내역만 제시한다. P2 참조 어댑터 예시가 서면 그쪽이 정본 | **S11이 요구하는 cp 어댑터가 레포에 없었다** — 어댑터 실물은 ipqc·toc_report 2종(fixtures)뿐이고 doc_type 등록부는 0건이다(P1·P2·P3 미착수, A-4는 "파일 배치까지"). "확정 후 정상 파싱"이 성립하려면 cp의 expects·extract 실물이 필요하다. CP04는 "헤더가 cp와 완전 일치"하도록 설계된 표본(raw_mock_대장 §2.4)이라 완전 일치 판정이 그 설계 의도 그대로다 | **가결정 — 레포 자체 검출 (08-19)** · 확정 경로: P2(참조 어댑터)·P3(등록부) 완성 시 대체 | 레포 (G6) |
| **D-70** | 재인입 회수 3분류의 실장 [CH3B 3.8 H2 — 미완 공사 마감 · G6.5 묶음 ①] | ①**회수**: 그 문서의 청크·describes와 노드·엣지·값의 provenance 항목을 걷어낸다. 문서 발자국 = **봉투의 source_locator ∪ 기존 청크의 source_locator ∪ doc_id 접두** — 새 인덱스를 만들지 않는다. ②**보존**: 살아있는 노드의 사전·alias, 그리고 **`STANDING_KINDS`(`auto_node`·`uncertain_match`)** — 조건 판정이 아니라 상시 작업목록이라 재인입이 지우지 않는다. ③**재평가**: 그 문서발 나머지 큐를 내리고 이번 인입이 현재 스냅샷을 다시 싣는다. **근거 0 판정(`evidence_lost`)은 회수 시점이 아니라 `pipeline.finalize()` 빌드 말미**이며 `doc_id=None`이다 — 회수 직후는 재적재 전이라 "근거 0"이 참이 아니다. 내용이 바뀐 재인입은 추출 체크포인트를 `invalidate()`한다(3.11 규약 7). `linked`는 describes 존재의 **재계산**이지 1회성 플래그가 아니다 | 회수가 없으면 개정판 재인입 후 삭제된 행의 청크가 잔존해 현행에 없는 내용이 계속 근거로 서빙된다(실측 12행 → 6행 개정판인데 청크 12건 잔존). `evidence_lost`는 닫힌 20종에 등재돼 있으나 발화 지점이 0곳이었다. **회수 시점 판정은 실측으로 무너졌다** — 1회 62 → 2회 167(evidence_lost 105건 거짓 적재). D-65와 같은 자리·같은 이유로 빌드 말미로 옮겨 1·2회 62로 수렴했다. `STANDING_KINDS` 예외가 없으면 auto_node 51이 재인입 한 번에 증발한다(20회차 실측 61 → 11의 재발) | **레포 (08-19)** · 허브 G6.5 §1 지시의 이행 · A1·A2·A3·A4·C4 5건 종결 | 레포 (G6.5) |
| **D-71** | 인입 검증 ③단과 닫힌 계약의 강제 [CH2 2.8 · 카드 C14·D6·I3·B3 — G6.5 묶음 ②] | ①**필드 검증**(`_check_fields`): 스키마 밖 필드는 **값을 동봉해** `unknown_field`, `optional` 미선언 필드의 부재는 `missing_field`. 큐로 갈 뿐 문서를 죽이지 않는다. ②**payload_kind 닫힌 2값**과 ③**미등록 doc_type**은 폴백이 아니라 **문서 단위 미인입 + `parse_failure`**(C14) — 그래프·청크·체크포인트에 아무것도 쓰지 않는다. 구축 모드 배선은 P3 몫이고 여기서는 명시적 실패까지다. ④**발명 카테고리**: 어느 층도 선언하지 않은 카테고리는 `invalid_category` + **노드 미생성**(I3). ⑤**리스트 값 방어**(`_scalar`): entity·attribute·anchor 값이 스칼라가 아니면 `missing_field` + 그 필드만 버리고 전진(D-60 `context` 방어와 동형 — D6). ⑥**`invalid_role` enqueue 순삭** — D-30대로 결함 로그만 남긴다 | 여섯 지점 모두 **정의는 있고 배선이 없었다**: `unknown_field`·`missing_field`·`invalid_category`·`parse_failure`는 닫힌 20종에 등재됐으나 생산자가 0곳이었고, 이를 위해 정의된 `STRUCTURAL` 상수는 죽은 코드였다. `invalid_role`은 반대로 **21번째 kind**로 새어 플랫폼 열람이 스스로 "닫힌 20종 위반"을 찍을 수 있었다. `or "process"` 폴백은 층 배정을 코드가 추측하는 것이라 그 자체가 층 어휘다(B1). **새 큐 기준선 62 → 63** — 늘어난 1건은 PFMEA01의 `비고`("재발 2건")로, mock이 `unknown_field` 재료로 심어 둔 열이 이제야 발화한 것이다 | **레포 (08-19)** · 허브 G6.5 §2 지시의 이행 · B1~B6 6건 종결 | 레포 (G6.5) |
| **D-72** | 병합·값의 무손실 계약 수리 [CH3B 3.6 규약 5·6 · 3.7 I2 · 카드 L7 — G6.5 묶음 ③] | ①**attr 병합은 인입 규칙 재사용**: `setdefault` 한 줄을 `Builder.put_attribute` 호출로 교체한다 — 같은 이름은 context 그룹별로 합치고 같은 그룹의 다른 값은 `spec_conflict`다. 자체 병합 로직을 새로 쓰지 않는다. ②**엣지 이설은 `graph.add_edge` 경유**: 치환 대신 제거 후 재적재라 `(src,rel,dst)` 유일성·provenance 합집합·툼스톤 존중이 공짜로 성립하고, **자기참조가 되는 엣지는 이설하지 않고 결함 로그**만 남긴다(병합의 부산물이지 참 관계가 아니다). ③**비맥락형은 빈 context 그룹 하나**(3.6 규약 5) — 분기가 아니라 특수 사례이므로 deep-equal·`spec_conflict`·기존 값 보존이 맥락형과 동형으로 적용된다. ④**후보 검색의 생존 판정**: `matcher.resolve` ②단에 `is_live` 1줄. ⑤**분리 리다이렉트는 옛 id만 걷는다** — 배분표가 각 표기를 이미 자기 타깃에 등재했다 | ①구판은 이름이 같으면 흡수측 값을 통째로 버려 값·provenance가 무기록 소실했다 — "정보 손실 0"의 정면 위반. ②실측: 서로 causes를 갖던 쌍의 병합이 자기 루프를 만들어 질의가 **"용접 강도 부족는 용접 강도 부족의 원인이 될 수 있다"**를 사실로 출력했고, 같은 대상을 가리키던 쌍은 물리 중복 2건으로 provenance가 갈라졌다. ③구판은 deep-equal 없이 덮어써서 교차 출처의 다른 주장이 provenance째 사라졌다("충돌은 정보다"). ④**D-66의 5지점이 부족했다** — 병합 툼스톤이 0.95로 MATCH돼 재인입 데이터가 죽은 노드에 착지한다. ⑤병합용 리다이렉트를 분리에 재사용해 한 표기가 두 산출물을 동시에 가리켰다(사전 오염 실측) | **레포 (08-19)** · 허브 G6.5 §3 지시의 이행 · C1~C6 6건 종결 · D-66 문면 정정 동반 | 레포 (G6.5) |
| **D-73** | 걸침층 배선 — `target_layer` 소비와 외부 그래프 저장 [명세 §15.7 규칙 A · CH2 2.4 · 카드 B6 — G6.5 묶음 ④] | ①**`Builder.for_layer(layer)`** 신설 — 걸침 층별 하위 빌더를 **층당 하나로 캐시**하고 사전·문서 버퍼를 공유한다(장부는 하나다 — B4). `_graph_for`·entity·attribute가 전부 이 캐시를 탄다. ②entity 분기가 스키마의 **`target_layer`를 읽어** 그 층에 해소한다. ③`run_document`가 `builder.graphs()`로 **걸침 층 그래프까지 저장**한다. ④`gate.commit_edge`가 `src_graph`를 **`dst_graph`와 같게** 받고, 끝점 미해소는 무음 반환이 아니라 `unresolved_endpoint`로 `gate_rejects` 로그에 착지한다(큐 아님 — D-7) | ①캐시가 없으면 `open_graph`가 레코드마다 새 인스턴스를 물어 와 앞 레코드가 쓴 것이 뒤 레코드에 안 보이고 어느 것도 저장되지 않는다(외부 층 attribute 증발 실측). ②스키마가 `target_layer: process`를 선언하는데 코드가 읽지 않아 **PFMEA의 관리항목 11종이 품질층에 복제**됐고 CP↔PFMEA 걸침 병합이 조용히 깨졌다(수리 후 품질층 Property 0 · `노칭::노칭 정밀도`가 두 문서 provenance를 공유). ④`to`만 받던 구판은 `from`이 걸침일 때 판정 전에 사라졌다 — D-61 수리의 남은 절반이다. **새 큐 기준선 63 → 71**: `mirror_asymmetry` 4 → 11(관리항목이 공정층으로 들어와 mirrors 재평가 범위에 편입 — 전건 참 양성, 오검출 0 회귀로 확인) · `auto_node` 51 → 52 | **레포 (08-19)** · 허브 G6.5 §4 지시의 이행 · D1~D3 3건 종결 · **판정필요-6 발의 동반**(같은 대상이 좌표 해상도에 따라 두 canonical로 갈린다 — 이 수리가 드러냈고 (나)로 전진) | 레포 (G6.5) |
| **D-74** | 소수리 5건 — 무기록 소멸·mock 경계·시점 [G6.5 묶음 ⑤] | ①**E1 무기록 소멸 기록**: 추출 후보의 관계 끝점 미해소와 attach **자식** 미해소를 `defects.log`에 남긴다(대상 쪽만 `orphan_attach`를 울리던 비대칭 해소 — 추출 후보의 미해소는 큐 급이 아니라 로그 급이다, D-30 계보). ②**E3 `USE_MOCK` 경계**: 문형 폴백이 `USE_MOCK=0`에서 `NotImplementedError`로 명시 실패한다 — 한시 예외 3호의 문면("USE_MOCK 한정")이 코드에 없었다. ③**E2 추출 어휘 한정**: `_vocab`이 사전·현재 그래프 대신 **골격(`status="seed"`) 스냅샷만** 읽는다. ④**E4 시점**: `ops_log`·툼스톤·`obsoleted_at`이 실제 시각(`_now()`)이다. ⑤**E5**: 관통 경로가 `finalize()`를 부른다 | ③이 핵심이다 — 구판은 추출이 "지금까지 무엇이 인입됐나"에 의존해 **인입 순서에 따라 노드가 소실**했고(봉인 실측 정순 66 · 역순 65 · 큐·로그 0) 체크포인트가 그 우연을 동결했다. 추출 계약 규약 1이 노드 id 참조를 금지한 이유가 정확히 이 메커니즘이며 표면형 어휘라는 뒷문으로 같은 의존이 성립해 있었다. **수리 후 정순·역순 76/76·24/24 · 소실 0**(남은 차이는 두 표기 중 어느 쪽이 canonical이 되는가뿐이고 양쪽 다 alias로 보존된다 — 매칭의 정상 동작이다). ④는 로그 5요소의 "시점"이 위조값이던 것을 고친다. **⚠ 지시문의 "D-11 스냅샷 파일이 이미 존재"는 사실과 다르다** — `data/skeleton_closed_list.json`은 레포에 없다. 골격 닫힌 목록 = 골격 전 노드(A11-6·D-45)이므로 파일을 새로 만들지 않고 그래프의 seed 노드에서 읽었다 | **레포 (08-19)** · 허브 G6.5 §5 지시의 이행 · E1~E5 5건 종결 | 레포 (G6.5) |
| **D-75** | 하강 부착 — 개념 좌표 + 축값 확정의 부착 대상 [틀 v2.10 §4B-A11-9 ⓪ · 카드 v18 F1 · CH3B v2.4 3.5 규약 3-⓪ — 판정필요-6 종결] | 부착 대상 좌표가 **개념 노드**인데 record의 `electrode_type`이 확정이면, 그 개념의 **동일 극성 인스턴스가 존재할 때** 그리로 하강해 부착한다(`탭용접`+cathode → `탭용접::cathode`). 하강 후에는 ①(표면형 결합 생략)이 그대로 적용된다. **인스턴스가 없으면 하강하지 않고 F1 결합 유지** — 부착 코드가 골격 인스턴스를 임의로 만들지 않는다(Tier1). 구현은 `Builder.descend_anchor` 1곳이고 **A11-5 순서 파생의 하강(`query._descend`)을 그대로 재사용**한다 — 새 로직 0줄 | 같은 실물이 좌표 해상도에 따라 두 canonical로 갈리던 규칙 접점 결함의 봉쇄다(P4 위반). 실측: `PFMEA01-R7`(좌표 `탭용접` 개념 + cathode)이 F1 결합으로 `탭용접::cathode 용접 가압력`, `PPT01-C004`(좌표 `cathode 탭용접` 인스턴스)가 ① 생략으로 `탭용접::cathode::용접 가압력` — 두 규칙이 각각 문면대로 돌았는데 이름이 갈렸다. **종결 어서션 실측: 깨끗한 재빌드 후 `탭용접::cathode::용접 가압력` 단일 노드.** 하강 없는 경로도 함께 잠갔다 — `패키징::전해액 주액::cathode 주액량`은 그 세부공정에 극성 인스턴스가 없어 F1 결합을 유지한다 | **사용자 판정 (08-19)** · 틀 v2.10 반입분의 이행 · BLOCKERS 판정필요-6 종결 | 허브 (P1 §0) |
| **D-76** | 파서 참조 어댑터의 소재지와 지위 [D-69 확장 — P1] | 사람이 쓴 **참조 어댑터**는 `mock/adapters/`에 둔다(`cp.py`·**신설 `pfmea.py`**). `mock/fixtures/adapters/`는 외부 LLM 실산출 스냅샷 전용이라 손대지 않는다(D-26). 참조 어댑터의 `expects`는 관찰 상수이고 `extract` 본체는 doc_type 간에 같다 — 어댑터 계약(§5 "선언 1개 + 함수 1개")이 의도한 모양이다. P2의 참조 어댑터 예시가 서면 그쪽이 정본 | **S14를 실물 파서로 돌리려면 pfmea의 expects·extract가 있어야 하는데 레포에 없었다** — 어댑터 실물은 ipqc·toc_report(fixtures)와 cp(D-69)뿐이었고, `verify_roundtrip.py`의 참조 구현은 ADAPTER 선언이 없는 함수 두 개다. 실측 결과 prefix CP01 12 · PFMEA01 13이 **파서 산출로** 일치했다(실행 시점 값·locator 표기 제외). 부수 검출 2건도 여기서 잡혔다: ①빈 행 가드가 "일부만 빈 행"까지 삼켜 CP03_bad의 결측 행이 조용히 사라졌다(C14 위반 — 전 열이 빈 행만 빈 행이다로 조임) ②`cp.py`가 `context`를 스칼라로 냈다(계약은 임의 딕셔너리 — D-60과 같은 형태. `context_key` 관찰 상수 신설) | **확정 — P2 종결 (2026-08-19)**: `cp.py`가 킷 ④ few-shot 3종에 채택되어 참조 어댑터의 지위·소재지가 실물로 섰다(`kit/참조어댑터/`). 잔여였던 P3 등록부는 **소재지 판정과 무관**하다 — 등록부는 어느 어댑터를 활성화하느냐이지 참조 예시가 어디 사느냐가 아니다 | 레포 (P1→P2) |
| **D-77** | validator의 좌표 검사는 **보고이지 판정이 아니다** [파서_명세 §8 · §4 — P1 자체 검출] | 좌표가 골격 닫힌 목록 밖이어도 **문서를 죽이지 않는다.** validator의 "좌표 존재"는 필드가 있는가이고, 목록 대조는 `coord_report`가 **보고**로 낸다. 목록 밖 이름의 판정은 **인입 소관**(`orphan_anchor` 큐)이다 | 파서가 좌표를 판정하면 ①파서가 골격을 해석하게 되고(태거 계약 위반 — "검증은 인입 소관") ②`orphan_anchor` kind가 영영 도달 불가능해진다. 실측: 초판 validator가 목록 대조를 문서 단위 실패로 걸어 **PFMEA01 전체가 미인입**됐다 — 원인은 `레이저노칭` 한 행이고, 그 행은 mock이 orphan_anchor 재료로 심어 둔 것이다(test_g3이 어서션한다). 좌표 태깅의 계약은 "닫힌 목록에서 선택 **또는 null**"이지 "목록 안이어야 함"이 아니다 | **레포 자체 검출 (08-19)** · test_p1에 회귀 잠금 | 레포 (P1) |
| **D-78** | 문형 규칙의 config 이동 — **B1 예외 3호 해소** [카드 v17 B1 ③의 명시된 해소 경로 · P1] | USE_MOCK 문형 폴백의 규칙 표를 코드에서 **층 config로 옮긴다** — `layers/{층}/config.json`의 `extract_patterns: [{pattern, rel}]`. 코드가 아는 것은 **정규식이 구문이라는 것**뿐이고 관계 이름은 데이터가 말한다. 규칙이 없는 층은 빈 배열이다(공정층 — 문형 추출 대상 아님) | 카드 B1 ③이 예외를 **한시**로 등재하며 해소 경로를 "문형 규칙의 config 이동, P트랙 n3 정비 시 재론"으로 못박았고, 허브 P1 요청문 §3이 "n7이 parser 정비의 첫 자리라 재론 조건에 부합"이라며 판단을 열었다. `causes`·`affects`는 품질층의 관계 이름이라 core에 있으면 그 자체가 B1 위반이다 — **예외는 세어질 때만 예외다**. 실측: 이동 후 core 전체에서 층 관계 이름이 사라졌고(주석·독스트링만 잔존) 회귀 8종 불변. 한정 조건의 코드 게이트(USE_MOCK=0 → 명시 실패)는 G6.5 E3이 이미 세워 뒀으므로 이 이동으로 예외 3호는 **양쪽 다** 닫힌다 | **레포 (08-19)** · 허브 P1 §3이 연 선택지의 실행 · 카드 B1 ③ 삭제 대상(문서 개정은 명세 트랙) | 레포 (P1) |
| **D-79** | 검수 뷰 데이터 스키마 — 산출자와 렌더러의 경계 계약 [파서_명세 §7 규약 3 · 카드 M11 · P-2 — P2] | 뷰 데이터 형식을 **파일로 확정**한다: `kit/검수뷰_데이터스키마.json`. 3구획(`parse_result`·`role_table`·`adapter_summary`) · 구획 1은 3층 필수(`summary`·`anomalies`·`normal`). **렌더러는 계산하지 않는다** — 채움율·이상 신호 판정은 전부 산출자(P3 n6)가 채워 넣고 렌더러는 있는 것을 그린다. 렌더러는 `core`·`parser`를 import하지 않고 JSON만 읽는다 | §7 규약 3이 "렌더러만 교체되고 데이터 계약은 불변"을 요구하는데, 계약이 **문서에만 있고 파일로 없으면** P3가 산출할 때와 렌더러가 읽을 때가 갈린다. 렌더러가 한 줄이라도 계산하면 그 순간 계약이 렌더러에 종속되고, 나중에 플랫폼 화면이 같은 JSON을 읽어도 다른 화면이 나온다. **3구획 구조는 payload_kind와 무관하게 같고 다른 것은 구획 1의 렌더뿐**이다(table=레코드 표 / prose=계층 트리) — 화면의 목적이 "모든 행이 맞는가"가 아니라 "규칙이 맞는가"이기 때문이다 | **레포 (08-19)** · 허브 P2 §1-4 지시의 이행 · test_p2에 잠금 | 레포 (P2) |
| **D-80** | 하네스 `use_blocks` 로더 — 구조 필드 목록의 소유 [실행검증_1차 §4.4 처방 이행 — P2] | `run_adapter.py`의 하드코딩 `STRUCT_FIELDS`를 **삭제**하고 스키마의 `use_blocks`를 `schemas/blocks.json`에서 전개해 합친다. 합친 필드는 `unknown_field` 계산에서 빠지고 **role 루프 드라이런에는 들어간다**. `fields` 판정(D-31 — prose는 `{}`)은 **스키마 선언분**으로 한다 — 블록은 스키마가 쓴 것이 아니라 참조한 것이라 prose의 정답을 흔들면 안 된다. 같은 이름이면 스키마 선언이 블록을 이긴다 | §4.4가 "스키마는 옳고 하네스가 미완"이라 판정하고 처방을 n7과 함께로 미뤄 뒀다. 하드코딩 목록은 `blocks.json`이 소유한 선언을 코드가 베껴 든 것이라, 블록 파일이 바뀌어도 하네스가 모른다. **실측: ipqc의 role 루프 `anchor=0` → `anchor=66`** — 좌표를 블록에 위임한 스키마의 anchor 경로가 처음으로 드라이런됐다. **봉인 로그 대비 43판정 전건 보존 · 추가는 `use_blocks 전개` 1종 · FAIL 0** | **레포 (08-19)** · 허브 P2 §1-6 지시의 이행 | 레포 (P2) |
| **D-81** | doc_type 등록부의 물리 형태와 단일 조회 [카드 M2 · D-8 · 증분0 §3 P3 아웃풋 — P3] | **`data/doc_types.json`** 신설 + `core/registry.py`가 유일한 조회처다. 출처는 둘, 조회는 하나: **내장(builtin)** = 레포가 싣고 나온 `schemas/{doc_type}.json`(층의 J10과 같은 결) · **등록(registered)** = n6이 확정해 등재한 것. 같은 이름이면 등록분이 이긴다. 세 소비자 — **M2 조회**(`ingest.load_schema`) · **preflight**(`cli/scan.py` 지문 스캔) · **플랫폼 노출**(`platform doctypes` — D-67 계보) — 가 전부 이 모듈을 부른다. **층 등록부(`registry.json`)와는 다른 장부다**(목적이 다르면 장부도 다르다 — D-8이 이미 목적별로 나눠 뒀다). 이름 중복은 내장과의 충돌까지 거부한다 | M2 조회 결과가 운영/구축 모드를 가르므로 **묻는 곳이 셋이어도 답하는 곳은 하나**여야 한다. 구판은 셋이 각자 파일 시스템을 뒤졌다 — `load_schema`는 `schemas/` 파일 실재를, 지문 스캔은 mock 디렉터리를, 플랫폼은 층 등록부를 봤고, **n6이 등록한 doc_type을 인입이 모르는** 상태였다. 부수 정리: `schemas/ipqc.json`은 20회차 A-4가 "파일 배치까지"로 둔 임시분(fixture와 바이트 동일·코드 참조 0)이라 **걷어내고 n6 정식 등록으로 대체**했다 — 그것이 S15의 전제다 | **레포 (08-19)** · 허브 P3 §1-3 지시의 이행 | 레포 (P3) |
| **D-82** | UNMAPPABLE 열의 배정표 복원 [D-30의 부작용 처방 — P3 자체 검출] | UNMAPPABLE 열은 **어댑터가 스스로 밝힌 미매핑분**에서 파생한다: `expects.header_labels`(원본 헤더 전량 — D-29)에서 `expects.columns`가 가리키지 않는 열. 스키마가 `unmappable`을 명시하면 그쪽이 이긴다(명시는 판정이고 차집합은 복원이다). 복원분은 배정표에 `UNMAPPABLE`로 뜨고 이상 신호에 **질문 형태**로 오른다(§7 규약 5) | D-30이 "UNMAPPABLE 열은 스키마 `fields`에 넣지 않고 어댑터 출력에서도 제외"라 정한 결과, **그 열은 어디에도 이름이 남지 않아 검수 화면에서 증발했다** — 사람이 판정해야 할 것이 판정 화면에 없다. 6지선다의 여섯째 경로가 화면에서 사라지는 셈이다. 실측: 복원 전 ipqc 배정표에 UNMAPPABLE 0건·이상 신호 0건 → 복원 후 `최근 불량 이력` 1건이 질문으로 착지. **fixture는 손대지 않았다**(D-26 — 파생은 어댑터의 자기 선언에서 한다) | **레포 자체 검출 (08-19)** · test_p3에 회귀 잠금 | 레포 (P3) |
| **D-83** | 지시문 템플릿 파일의 소재지 [문서 7 §7.6-B-5 · §7.1 관리 자산의 원칙 · 문서 4 §4.10-4 — 2A-1] | 프롬프트 파일은 **레포 루트 `prompts/{이름}.md`**에 두고 머리말의 `version:` 줄이 `prompt_version`의 정본이다. `core/extract.py`의 `prompt_version(name)`이 그것을 읽고, **파일 없음·version 줄 없음은 명시적 실패**다. 층 어휘는 넣지 않고 `{{categories}}`·`{{relations}}`·`{{attach_candidates}}`·`{{chunk}}` 조립 자리로 둔다(문서 1 B9) | §7.6-B-5가 "템플릿은 파일이 정본이고 코드가 그것을 읽는다"를 요구하고 §7.1이 "프롬프트는 프롬프트 파일(사람 확정 관리 자산·prompt_version)"이라 하는데, **그 파일의 경로를 명세가 정하지 않는다** — §7.1 코드 배치 트리에 `prompts/` 항목이 없다. 기록에서 빼는 선택은 M12·§4.10-4가 체크포인트 필수 필드로 규정하므로 불가하다. 구판은 `PROMPT_VERSION = "e-1.0"` 상수여서 **실체 없는 버전이 재현성 기록에 남았다** — 템플릿이 아예 없어도 있는 것처럼 기록된다. `schemas/`와 같은 결(루트의 자산 디렉터리)로 정했다 | **레포 가결정 (2026-08-24)** · 확정 경로 = **명세 §7.1 트리에 `prompts/` 추가**(허브 개정 몫 — 이 세션은 명세를 고치지 않는다) | 레포 (2A) |
| **D-84** | mock 산출물의 데이터 표시 필드 [문서 7 §7.6-B-4 · §7.5 「실호출로만 검증되는 항목의 표시」 — 2A-1] | 이미지 요약 조각의 `meta`에 **`image_summary_source`(`"mock"` \| `"live"`)**를 함께 싣는다. 기존 `image_summary=true`는 "이 텍스트는 원문이 아니라 요약이다"만 말하고 **어느 갈래가 만들었는지는 말하지 않는다.** 수기 계약 검체(`mock/parsed/PPT01.json`)도 같은 계약으로 맞췄다 | §7.6-B-4가 "mock 산출물이 그래프·청크에 그대로 실려서는 안 된다"고 하고 §7.5가 표시를 **주석이 아니라 데이터 필드**로 요구하는데, 무엇을 필드 이름으로 쓸지는 정하지 않는다. `image_summary`만으로는 소비부(질의·답변)가 오염을 걸러낼 근거가 없다 — 실측: `run.py all` 후 청크 저장소에 `"MOCK 요약: sealing_unit_diagram.png"`가 실려 있고 표시가 그것이 mock임을 말하지 않았다. 크래시가 아니라 조용한 오염이라 더 위험하다 | **레포 가결정 (2026-08-24)** · 확정 경로 = 명세 §7.1 대체 표의 이미지 요약 행에 이 필드를 병기(허브 개정 몫) | 레포 (2A) |
| **D-85** | 인프라 모듈 2종의 신설 — `core/log.py` · `core/init.py` [문서 7 §7.1 런타임 전제·§7.8 로그 규격 · §7.6-4 — 2A-2] | 로깅 설정은 **`core/log.py`**가, 클린 상태의 정의는 **`core/init.py`**가 소유한다. 둘 다 §7.1 코드 배치 트리에 이름이 없는 신설 모듈이다. 로그 3종(MOCK 경고·큐 적재·명시적 실패)의 함수를 log가 갖고, `setup()`은 **진입점만** 부른다(라이브러리가 `basicConfig`를 부르면 호스트의 설정을 덮어쓴다). init의 `EMPTY`가 §7.2 말미의 빈 상태 형태를 그대로 갖고, 층 그래프는 **GraphStore 경유로만** 만든다(경로도 이름도 모른다) | 명세가 요구하는 것(표준 logging 레벨 INFO · `run.py init [--fresh]`)에는 **살 자리가 필요한데 트리가 그것을 지정하지 않는다.** 대안은 기존 모듈에 얹는 것인데, 로깅을 `store.py`에 넣으면 저장 계층이 로깅을 소유하게 되고 init을 `run.py`에 인라인하면 테스트 10종이 그것을 부를 수 없다(실제로 각자 `rmtree`를 반복하고 있었고 그래서 클린이 "파일 없음"이었다). 초기 구현이 `init.py`에 저장 파일명을 문자로 담아 **회귀 st가 즉시 잡았다** — 경계가 신설 모듈에도 그대로 걸린다는 실증이다 | **레포 가결정 (2026-08-24)** · 확정 경로 = 명세 §7.1 트리에 두 모듈 등재 여부 판단(허브 개정 몫) | 레포 (2A) |
| **D-86** | 판정 관문의 반환 계약과 후보 조립의 분리 [문서 7 §7.1 core 경계 3종 · 문서 4 §4.3-6 — 2A-3] | `matcher`를 셋으로 가른다: **`candidates(...)`**(후보 조립 — 안전망 넷을 감점이 아니라 **제외**로 걸고 후보에 `parent`·`scoped`·`polarity`를 싣는다) · **`match(surface, candidates, category)`**(판정만 — 반환은 `{type, matched_id, confidence}`) · `resolve(...)`(둘의 2단 편의 형태, 기존 호출부 무변경). 재사용 3지점 중 **attach_to 해소**(`pipeline._dict_hit` — 무조건 첫 히트 제거)와 **I2 병합 후보**(`ops.merge_targets` 신설)를 배선했고, **orphan 재시도는 배선할 코드가 아직 없다**(P-B 몫) | §7.1이 "개체 판정은 이 함수 하나로 수렴한다"를 요구하는데 구판은 이름(`resolve`)도 인자(`candidates`를 받지 않음)도 반환(튜플)도 달라, 재사용 지점마다 별도 판정 코드가 생겼다. 실측: `_dict_hit`이 `hits[0]`을 무조건 골라 **카테고리 불일치 안전망·극성 제외·툼스톤 제외가 전부 적용되지 않은 선택이 엣지 끝점**이 됐다. 사전은 전 층 단일이라 같은 표면형에 여러 노드가 달릴 수 있다. 다중 히트에서 확신이 하나로 모이지 않으면 **조용히 첫 히트를 고르지 않고** 결함 로그 + 미해소(→ `orphan_attach`)로 둔다 | **레포 가결정 (2026-08-24)** · 확정 경로 = 실호출 갈래의 프롬프트 주입 통로가 서면 재대조(별개 갭 항목) | 레포 (2A) |
| **D-87** | 핸들러 `ctx`의 여섯 + 레코드 유도값 캐시 [문서 2 §2.7 — 2B] | `ctx`는 명세가 못박은 **여섯**(`graphs`·`dic`·`buffer`·`queue`·`record`·`schema`)을 그대로 갖고, 레코드마다 다시 계산되는 유도값(해소된 좌표 `ref`·부모 canonical·`electrode_type`·anchor 극성·`resolved`·`external`·보류 그릇)은 **일곱째 `state`에 캐시**한다. `state`는 계약의 확장이 아니라 여섯에서 매번 재계산할 수 있는 것의 저장이다 | §2.7이 "`ctx`는 여섯으로 구성한다"고 적고 그 열거를 **닫힌 목록으로 읽히게** 서술한다(버퍼를 빼지 말라는 경고가 그 근거다). 그런데 핸들러가 실제로 필요한 좌표·극성·부모는 여섯 중 `record`+`graphs`+`dic`에서 유도 가능하되 **레코드마다 재계산하면 anchor 해소가 필드 수만큼 반복**된다. 여섯을 그대로 두고 캐시를 곁에 두는 것이 문면을 지키면서 재계산을 피하는 유일한 형태다. 참고: 정제본 7 §7.2는 저장 레코드에 대해 "위 열거는 **최소 집합**이며 구현 내부 파생 필드를 금지하지 않는다"를 명시하는데, §2.7의 `ctx` 열거에는 그 문장이 없다 — **비대칭이다** | **레포 가결정 (2026-08-24)** · 확정 경로 = §2.7에 §7.2와 같은 「최소 집합」 문장을 넣을지 판단(허브 개정 몫 — 이 세션은 명세를 고치지 않는다) | 레포 (2B) |
| **D-88** | orphan 재시도의 판정 강도는 지점마다 다르다 [문서 4 §4.7-5 · §4.4-3 — 2B] | **좌표(anchor) 재시도는 사전 조회 + Tier1 한정**으로, **attach 재시도는 판정 파이프라인 재사용**으로 한다. 전자에 유사도 판정을 태우지 않는다 | §4.7-5는 "보존 표면형을 판정 파이프라인(4.3)에 다시 태워"라고 한 갈래로 적지만, 좌표와 부착은 성질이 다르다 — anchor는 **Tier1 한정·신규 생성 금지**(§2.4-①)이고 attach는 **auto 설비에도 붙는다**(§4.4-3이 명시적으로 Tier1 한정을 배제한다). 실측: 좌표에 판정을 태웠더니 USE_MOCK 포함 규칙이 `레이저노칭`을 `노칭`에 해소해 `버 발생 occurs_in 노칭`을 커밋했다(큐 69 → 68, mock의 orphan 증거 소멸). **재시도가 존재하는 이유는 그래프가 자랐기 때문이지 매칭이 느슨해졌기 때문이 아니다** — 첫 인입이 못 찾은 것을 재시도가 더 느슨한 규칙으로 찾으면 그것은 재시도가 아니라 다른 판정이다 | **레포 가결정 (2026-08-24)** · **판정필요-10과 한 몸이다** — 그 판정이 (나)로 나면 이 결정도 뒤집힌다 | 레포 (2B) |
| **D-89** | `pending_attrs`의 착지는 요청문이 아니라 명세를 따랐다 [문서 2 §2.4-③ — 2B] | 부착 대상 미해소로 보류된 attribute는 **`orphan_anchor`/`orphan_attach` 항목의 `pending_attrs`**에 실린다. `attach_conflict` 큐를 쓰지 않는다 | P-B 요청문 §2-3c는 "`attach_conflict` 큐로 보류"라 적었으나, 그것이 인용한 §2.4-③은 **`pending_attrs`를 orphan 계열 두 kind에 싣고 "이 처분을 위해 새 큐 kind를 만들지 않는다 — 착지할 kind가 이미 둘 다 있다"**를 명시한다. `attach_conflict`는 §4.4-7(b)의 **교차 출처 충돌**(다른 문서가 같은 개체에 다른 부착을 주장) 자리라 성질이 다르다 — 보류는 충돌이 아니다. 요청문과 명세가 갈리면 **명세가 이긴다**(§0-1) | **레포 (2026-08-24)** · 요청문 문면 정정 필요를 보고했다 | 레포 (2B) |
| **D-90** | 확정 산출의 이행 순서와 등재 취소의 회수 범위 [문서 6 §6.5 — 2B 자체 검출] | **등재가 먼저, 승격이 나중이다.** `registry.register`가 성립한 뒤에만 어댑터·스키마를 정본 자리로 복사한다. 대칭으로 `unregister`는 **등재 항목이 가리키는 승격분을 함께 걷는다**(레포가 싣고 나온 내장은 등재 항목이 없으므로 대상이 아니다) | 반대 순서로 짰더니 실측 결함이 났다 — 등재가 「이름 중복」으로 거부됐는데 승격 파일은 이미 `schemas/{doc_type}.json`에 남았고, 그 파일이 **내장 출처**가 되어 그 이름의 재등록이 영영 막혔다. 회수 쪽도 같은 축이다: 등재만 지우고 파일을 남기면 조회에는 계속 잡히면서 등록부에는 없는 **반쪽 상태**가 된다. §6.5는 이행을 요구하지만 **실패 시의 순서·역연산은 말하지 않는다** — 구현 재량으로 소화했다 | **레포 자체 검출 (2026-08-24)** · test_p3에 회귀로 잠금 | 레포 (2B) |
| **D-91** | 픽스처 소재의 단일 지점과 「없을 때의 거동」 [문서 7 §7.5 mock 경로 규약 — 2C §2-4] | `core/fixtures.py`가 픽스처 경로를 **혼자 안다**. 본체(core·parser·cli)는 mock 경로를 상수로 갖지 않고, 각 참조점은 **없을 때 무엇을 하는지**를 정의한다 — queries.json 부재 = 계기판 분모 0(비율은 `None`) · adapters 부재 = 그 디렉터리 건너뜀 · parsed 부재 = 말하고 끝냄 · hints/maps 부재 = 폴백. 뿌리는 `ONTO_FIXTURES`로 갈아 끼운다 | mock을 물리적으로 지우면 `gauges`·`scan`이 죽고 회귀가 붕괴한다(실측). 원인은 본체가 mock 경로를 **USE_MOCK 분기 밖 무조건 상수**로 안다는 것이었고 분기 안 참조는 0건이었다. 명세는 **자산의 자리**(§7.5 경로 규약)를 정하지만 **그 자산이 없을 때의 거동**은 정하지 않는다 — 사내에서는 없는 것이 정상인데 그 상태가 미규정이었다. 부수 검출: 계기판 4·5의 분모가 0일 때 `ZeroDivisionError`였고, **0.0을 찍으면 "측정했더니 0"이 되어 측정 부재와 구분되지 않는다** | **레포 가결정 (2026-08-25)** · 확정 경로 = §7.5에 「자산 부재 시의 거동」 한 줄 추가 여부(허브 개정 몫) | 레포 (2C) |
| **D-92** | 이관 연산의 엣지 재배선 범위 [문서 4 §4.7·§4.9 — 2C] | 이관이 옮기는 것은 **소속을 주장하는 엣지 2갈래**다 — ①옛 부모(같은 카테고리) 직접 부착 ②**옛 부모 하위 설비**를 통한 부착. ②는 새 부모 아래 대응 설비가 없을 수 있으므로 **좌표 부착만** 세우고 **설비를 만들지 않는다**(P2). 옛 부모 판정은 **canonical 스코프 접두**로 한다 | 명세는 이관을 "스코프 변경 연쇄"로 부르고 **미리보기 대상**으로 등재하지만 **무엇을 재배선하는지는 말하지 않는다**. ①만 옮기면 정상 경로(설비를 통해 붙는 경우)가 통째로 남아 **이름만 바뀌고 소속은 안 바뀐다** — 실측으로 그랬다(edges_moved 0). 옛 부모를 이 노드의 엣지에서 찾는 구현도 같은 이유로 실패한다 | **레포 가결정 (2026-08-25)** · 확정 경로 = §4.9에 이관의 재배선 범위 명문화(허브 개정 몫) | 레포 (2C) |
| **D-93** | 판정 정확도는 계기판 8종 **밖**에서 잰다 [갭 spec-s7-11-14 · 문서 5 §5.5 — 2C] | `platform accuracy`를 별도 창구로 둔다. 재는 것은 ①골든셋/스모크의 `expected_path` 일치 ②**수정 큐로 가는 건수의 추이**(`data/accuracy_log.json`). **계기판 8종 표는 건드리지 않는다.** mock 세트로 돌 때는 화면이 **"메커니즘 점검"이라고 밝힌다** | 갭 목록이 "계기판 8종 표는 건드리지 않고 별도 측정"을 지시했고, 그 이유가 명세에 있다 — 8종은 닫힌 목록이고(§5.5) 항목을 끼우면 그 지위가 흔들린다. **mock 표시가 필요한 이유**: §7.5-1이 "가짜 데이터의 점수는 가짜 확신"이라 못박았는데, 화면이 그 구분을 안 하면 12/12가 품질 점수로 읽힌다 | **레포 가결정 (2026-08-25)** · 확정 경로 = 골든셋이 서면 재대조(2B) | 레포 (2C) |
| **D-94** | 구조 지도 보존 파일의 내부 형태와 «언제 쓰는가» [문서 6 §6.3 — 2A P-D] | 보존 파일은 **문서 하나에 하나**(`extract/struct_maps/{doc_id}.json`)이고 그 안에서 **프레임 키로 나눈다**(`maps[{시트\|슬라이드 키}]`). 이미지 요약(`image_summaries`)과 **같은 파일·같은 `source_hash`**를 공유한다. 쓰기는 **이번 인입에서 새로 산출된 것이 있을 때만** 한다 | §6.3은 보존 자리와 재사용 판정만 정하고 **파일의 내부 형태를 말하지 않는다.** 한 문서가 여러 프레임을 갖는데(어댑터의 지도 훅은 프레임 키마다 1회 불린다) 지도 1개를 통째로 저장하면 프레임이 섞여 재사용이 엉뚱한 프레임의 지도를 물어온다. 매번 쓰면 재사용 갈래에서도 파일이 갱신돼 «재사용했나»가 파일로 판정되지 않는다 | **레포 가결정 (2026-08-26)** · 확정 경로 = §6.3에 보존 파일 내부 형태 한 줄 추가 여부(허브 개정 몫) | 레포 (2A P-D) |
| **D-95** | 진입점 가드의 자리와 「외부 CDN 없음」의 판정 방법 [문서 7 §7.8 — 2B 자체 검출] | ①`if __name__ == "__main__":`은 **파일 끝**이다 — 그 아래 정의된 명령은 디스패치 표를 만들 때 존재하지 않는다. ②출력 경로 표기는 `_short()`로 감싼다 — 레포 밖이면 절대 경로 그대로. ③「CDN 없음」은 문자열 `cdn`이 아니라 **바깥을 부르는 행위 8종**(`http://`·`https://`·`//cdn`·`@import`·`fetch(`·`xmlhttprequest`·`<script src`·`<link `)으로 잰다 | 셋 다 실측 결함이다. ①`python -m cli.export html`이 `NameError: cmd_html`로 죽었다 — **import 경로는 파일을 끝까지 읽으므로 회귀가 못 잡았고**, 회귀가 부르는 갈래와 사람이 부르는 갈래가 갈렸다. ②`relative_to`가 레포 밖·상대 경로에 ValueError를 던져 **파일은 만들어졌는데 화면이 크래시했다**(cypher·csv도 같은 자리). ③노드 id가 ULID(Crockford base32)라 26자 안에 「CDN」이 우연히 들어간다 — 실측 20회 중 1회(`01M10DM4QSCDN9YQP6PC35RADS`). 그 어서션이 무작위로 붉어져 **526 기준선 자체가 흔들리고 있었다** | **레포 자체 검출·수리 (2026-08-27)** · 확정 경로 없음(명세 판단 불요 — 구현 결함) | 레포 (2B) |
