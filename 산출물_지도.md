# 산출물 지도 — 무엇이 어디에 떨어지고, 어떻게 보나

> 파이프라인이 만드는 파일 전부와 **각각을 보는 명령**을 한 자리에 모았다.
> 요지 셋: **①진실은 `data/`의 JSON이다 ②전부 텍스트로 볼 수 있다 ③Cypher는 파생물이다.**

---

## 0. 먼저 — 자주 묻는 것 넷

**Q. 입력해야 하는 것이 seed인가?**
seed는 **필수 입력 하나**이고, 전부는 아니다. 넣어야 할 것은 셋이다.

| 넣는 것 | 어디에 | 누가 | 없으면 |
|---|---|---|---|
| **골격 seed** | `layers/<층>/skeleton.json` | 사람 (사내 공정 체계) | 좌표가 없어 문서가 붙을 자리가 없다 |
| **문서 유형 등록** | `run.py register` 3단 | 사람 승인 + (LLM 초안) | 그 양식의 문서를 못 읽는다 |
| **문서 실물** | 아무 경로 (창작 표본은 `tests/fixtures/raw/`) | — | 넣을 지식이 없다 |

`layers/<층>/config.json`(카테고리·관계 선언)도 층을 새로 만들 때만 손댄다.

**Q. 결과가 Cypher로 나오나?**
**아니다.** 결과는 `data/`의 **JSON**이다. Cypher는 `run.py export cypher`로 뽑는
**파생물**이고(`docs/spec/7_구현규격과검증.md` §7.7 · P5), 질의는 Cypher를 거치지
않고 JSON을 직접 읽는다.
Neo4j는 **사람이 눈으로 보려고** 올리는 것이지 파이프라인의 일부가 아니다.

**Q. 시각화 안 하고 볼 수 있나?**
전부 볼 수 있다. `run.py show` **9종**이 그 자리다 — 골격 트리·노드 상세·문서
역추적·청크 원문·엣지·매칭 스키마·메타데이터 계약·**로그 4종**·**추출 체크포인트**.

**Q. 그림으로 보고 싶으면?**
`run.py export html` — **단일 HTML 파일**이고 브라우저로 연다. 외부 CDN을 쓰지
않으므로 **사내망·오프라인에서 그대로 열린다.** 필터 5축(층·카테고리·상태·tier·
극성)이고 **걸침 엣지는 붉은 점선**으로 남는다(층간 연결이 이 시스템의 존재 이유다).

**Q. 중간 결과물은 어디서 보나?**
§2의 표가 단계별로 정리돼 있다. 파싱 → 인입 → 추출 → 구축의 각 단계가 파일을 남기고,
그 파일이 다음 단계의 입력이다(P1 — 결합은 파일 계약뿐).

---

## 1. 파일 지도 — 전량

`data/`·`extract/`·`review/`·`export/`는 **실행 산출물**이라 git에 추적하지 않는다.
`run.py all` 한 번으로 재생성된다.

### `data/` — 진실

| 파일 | 무엇 | 보는 법 |
|---|---|---|
| `<층>/graph.json` | **그래프 본체** — 노드·엣지 | `show tree` · `show node` · `show edges` · `platform graph` |
| `chunks.json` | **청크 저장소** — 원문 전량 + `describes`(청크↔노드 연결) | `show chunk` |
| `dictionary.json` | **동의어 사전** — 표면형 → 노드 id. 영속 지식(P4) | `show node`의 「별칭」 |
| `review_queue.json` | **수정 큐** — 사람이 판정할 것 (닫힌 20종) | `platform queue [kind]` |
| `doc_registry.json` | 문서 대장 — doc_id → doc_hash·revision·원본 경로 | `show doc` |
| `doc_types.json` | **doc_type 등록부** — n6이 확정한 것 | `platform doctypes` |
| `registry.json` | 층 등록부 | `platform registry` |
| `skeleton_closed_list.json` | **골격 닫힌 목록 스냅샷** — 파서와 공유하는 좌표 목록 | `show tree` |
| `ops_log.json` | I축 연산 이력 — 누가 언제 무엇을 고쳤나 | `platform ops` |
| `gate_rejects.json` | 게이트 거부 로그 — 큐가 아니라 관측 신호 | `show log gate` |
| `defects.log` | 결함 로그 — 조용히 버리지 않기 위한 자리 | `show log defects` |
| `link_miss.log` | 질의 링킹 미스 — **계기판 5**의 재료 | `show log link_miss` |
| `chunk_truncated.log` | 청크 잘림 — **계기판 4**의 재료 | `show log truncated` |
| `accuracy_log.json` | 판정 정확도 이력 — 추이가 신호다 | `platform accuracy` |
| `.*.lock` (숨김) | 원자적 쓰기의 파일 락 — 진실이 아니다. 지워도 된다 | — |

두 `.log`는 **해당 사건이 있어야 생긴다** — 없으면 그런 일이 없었다는 뜻이다.

### `extract/` — 추출 체크포인트

| 파일 | 무엇 | 보는 법 |
|---|---|---|
| `<doc_id>.json` | 그 문서에서 뽑은 **개체·관계 후보**(계약 B). **파일 존재 = 추출 완료**(P-1) | **`show extract <doc_id>`** · `platform extract`(상태만) |
| `struct_maps/<doc_id>.json` | 구조 지도 보존분 — 같은 지도 → 같은 분할 → 같은 chunk_id | 직접 열람 |

### `review/<doc_type>/` — 등록 세션 산출

| 파일 | 무엇 |
|---|---|
| `input_package.json` | 생성 세션에 넘긴 입력 — 사람 4 + 시스템 5 |
| `view.json` | **검수 뷰 데이터** — 3구획(파싱 결과·role 배정표·어댑터 요약) |
| `view.html` | 위를 렌더한 정적 HTML — **브라우저로 열어 승인** |
| `approval.json` | 승인 기록 — 승인자·시점·수정 지시 이력 |
| `state.json` | 진행 상태 (내부용) |

### `export/` — 파생물 (필요할 때만)

| `graph.cypher` · `nodes.csv` · `edges.csv` | `run.py export` 산출. **되돌려 읽지 않는다** |
|---|---|

---

## 2. 단계별 중간 결과물 — 문서 한 건이 그래프가 되기까지

```
원본 문서(.xlsx)
   │  parser/reader        포맷별 원시 추출          ← 중간물 없음(메모리)
   │  parser/preflight     지문 ↔ expects 대조        → 불일치면 adapter_mismatch 큐
   │  adapter.extract      원시 → 정규 조각           ← 중간물 없음
   │  parser/normalizer    상동·병합·복수값·nested    ← 중간물 없음
   │  parser/tagger        좌표 태깅 + 봉투 구성
   ▼
계약 JSON  ─────────────────────────────────────── ★ 파서와 에이전트의 경계
   │                                                  run.py parse run … out.json
   │  ingest               doc_hash → 근거 축 id      → data/chunks.json · doc_registry.json
   │  extract (prose만)    개체·관계 후보             → extract/<doc_id>.json  ★
   │  build                판정 → 노드·값             → data/<층>/graph.json · dictionary.json
   │  gate                 관계 커밋 판정             → graph.json 엣지 / 큐 / gate_rejects
   │  finalize             전역 재평가(mirrors 등)     → 큐 갱신
   ▼
그래프 (data/<층>/graph.json)
```

★ = 파일로 남는 중간물. 나머지는 메모리에서 흐른다.

**계약 JSON을 따로 보고 싶으면** 파서만 돌린다 — 인입 없이 산출만 확인:

```bash
python run.py parse run tests/fixtures/adapters/cp.py CP01 \
    tests/fixtures/raw/CP01.xlsx        # 출력 생략 시 parsed/CP01.json
```

---

## 3. 열람 명령 — `run.py show`

| 명령 | 무엇을 답하나 |
|---|---|
| `show tree [층]` | **골격이 맞게 섰나** — 계층·대표 흐름·별칭·극성. seed 교체 후 첫 확인 |
| `show node <이름>` | **이 개체의 전부** — 값(맥락별)·별칭·출처·연결 엣지·근거 청크 |
| `show doc [doc_id]` | **이 문서가 무엇을 만들었나** — 청크·노드·엣지·큐 (역추적) |
| `show chunk <doc\|id>` | **원문** — 질의가 「문서 근거」로 내놓는 그 문장 |
| `show edges [층] [관계]` | 엣지 목록 — 출처와 상태까지 |
| `show schema <doc_type>` | **매칭 스키마** — 필드 → role 배정표 · 선언 엣지 |
| `show meta` | **메타데이터 계약 3층**을 실물로 (봉투·조각 공통·payload) |
| `show log [이름] [N]` | **로그 4종** — 결함·게이트 거부·링킹 미스·청크 잘림 |
| `show extract [doc_id]` | **추출 체크포인트의 내용** — 파서가 잘못 냈나 구축이 잘못 읽었나 |

### 예 — 답의 뒷면을 보는 흐름

```bash
python run.py query "노칭 정밀도 규격이 뭐야?"   # ① 답을 받고
python run.py show node "노칭 정밀도"            # ② 그 값의 출처·연결을 보고
python run.py show chunk CP01                    # ③ 원문 문장을 확인한다
python run.py show doc CP01                      # ④ 그 문서가 만든 것 전부
```

`platform` 쪽은 **집계·상태**를 본다 — `graph`(층별 계수·걸침) · `queue`(닫힌 20종) ·
`doctypes` · `registry` · `extract` · `ops` · `gauges` · **`dashboard`**(Q7류 집계 +
수정 도구 세트 + 등록 워크플로우) · **`accuracy`**(판정 정확도 — 계기판과 별도).

---

## 4. 시각화 — 필요할 때만

```bash
python run.py export html        # **그래프 뷰어** — 브라우저로 연다 (CDN 없음)
python run.py export cypher      # export/graph.cypher → Neo4j
python run.py export csv         # nodes.csv · edges.csv → Gephi · 엑셀
python run.py export mermaid         # 골격 대표 흐름 (보고서용)
python run.py export mermaid cross   # **걸침 관계** — 층 구분 표기
```

**`export html`이 첫 선택지다** — 파일 하나면 열리고 서버도 설치도 네트워크도
필요 없다. 필터로 걸러 봐도 **걸침 엣지는 화면에 남는다**(문서 7 §7.8).

**빈 출력을 성공으로 내지 않는다** — `export mermaid quality`처럼 그릴 것이 없으면
왜 비었는지를 말하고 **실패로 끝낸다**(exit 1).

**Cypher 매핑**: 노드 라벨 = `category`(+`:Node`) · 키 = `id`(ULID) · 엣지 타입 = `rel`.
attribute는 **속성으로 펴지 않고** `attrs_json`에 통째 싣는다 — 맥락과 출처가 딸린
구조라 평탄화하면 그 둘이 사라진다. 툼스톤과 사람이 지운 엣지는 내보내지 않는다.

**되돌려 읽는 경로는 없다.** Neo4j에서 고친 것은 돌아오지 않는다 — 고치는 것은
`run.py ops`(개명·병합·분리·폐기)다.

---

## 5. 메타데이터 계약 3층 — 파서와 에이전트의 경계

`run.py show meta`가 실물로 보여준다. 정본은 `docs/spec/2_계약.md` §2.2.

| 층 | 필드 | 누가 만드나 |
|---|---|---|
| **문서 봉투** | `doc_id` · `doc_type` · `payload_kind` · `source_path` · `revision` · `parsed_at` · `parser_version` · `adapter_version` · `context?` | **파서** |
| **조각 공통** | `source_locator` · `doc_type` · `process_group` · `process_ref` · `process_no?` · `electrode_type` · `context?` | **파서** |
| **payload** | table → `records[]` / prose → `chunks[]`(`text`·`section`·`meta`·`image_ref`) | **파서** |
| *(정본 id)* | `chunk_id` · `record_id` · `doc_hash` | **에이전트** — 파서는 만들지 않는다 |

**파서는 위치를, 에이전트는 정체성을** 소유한다. 파서가 정본 id를 부여하면 계약 위반이고
하네스가 잡는다.

---

## 6. 매칭 스키마 — 문서의 열이 그래프의 무엇이 되는가

`run.py show schema <doc_type>` · 실물은 `schemas/<doc_type>.json` 또는 등록부가 가리키는 경로.

| role | 뜻 | 그래프에 무엇을 쓰나 |
|---|---|---|
| `anchor` | 좌표 — 골격에서 **찾기만** 한다 | 아무것도 안 만든다(조회) |
| `entity` | 개체 | **노드** (신규 또는 기존에 병합) |
| `attribute` | 값 | 노드의 `attrs` — 맥락별 병렬 저장 |
| `content` | 자유 서술 | **청크** + `describes` 연결 |
| `meta` | 관리 정보 | 출처 장부에만 (그래프에 안 들어감) |
| `UNMAPPABLE` | **5종 어디에도 안 맞음** | 아무것도 안 만들고 **사람에게 질문** |

구조 필드(`electrode_type` · `context` · payload 고정 키 4종)는 **6지선다 대상이 아니다** —
미해결이 아니라 정상·완결이고 시스템 코드가 직접 소비한다.

공용 블록(`schemas/blocks.json`)이 주는 필드(`source_locator`·`process_group`·
`process_ref`·`process_no`)는 스키마에 다시 안 써도 된다 — `use_blocks`가 끌어온다.

---

## 7. 사내 첫 실행 — 이 순서로 보면 된다

```bash
python doctor.py                              # ① 온전한가 · 다음은 무엇인가
python run.py init --fresh                    # ①' 클린 상태 — 이것이 클린의 정의다
# ② layers/process/skeleton.json 을 사내 공정으로 교체
python run.py bootstrap
python run.py show tree                       # ③ 골격이 맞게 섰나 ★ 여기서 눈으로 확인
python run.py register generate <이름> process <표본1> <표본2>
python run.py register review <이름>          # ④ review/<이름>/view.html 을 브라우저로
python run.py register confirm <이름> --by "<승인자>"
python run.py parse run <어댑터> <doc_id> <문서> out.json   # ⑤ 계약 JSON 확인
python run.py ingest out.json                 # ⑥ 인입
# 위 두 줄을 한 번에 — 선택(지문 스캔 유일 일치 또는 --doc-type) → 파싱 → 인입 (B46)
python run.py ingest-file <문서> [--doc-type X] [--dry-run]
python run.py ingest-dir <경로> [--doc-type X] [--dry-run]   # 문서 단위 독립 · 끝에 성공/실패/미선택 목록
python run.py show doc <doc_id>               # ⑦ 무엇이 만들어졌나
python run.py query "..."                     # ⑧ 답이 서나
python run.py platform queue                  # ⑨ 사람이 판정할 것
python run.py gauges                          # ⑩ 첫 측정
```

③과 ⑦이 가장 중요하다 — **골격이 틀리면 뒤가 전부 어긋나고**, ⑦을 안 보면 무엇이
들어갔는지 모른 채 질의만 하게 된다.

---

**함께 보기**: `README.md`(시작) · `doctor.py`(지금 상태) · `구현현황.md`(반영 수준과 갱신 트리거)
