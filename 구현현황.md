# 구현 현황 — 지금 어디까지 되어 있고, 무엇을 언제 업데이트하나

> **살아있는 문서다.** 사내 반입·기능 추가가 있을 때마다 갱신한다.
> 여기 적힌 수치는 전부 실측이며, 다시 재려면 `python doctor.py` 한 줄이다.
>
> 이 문서가 답하는 것 둘: **①지금 무엇이 되나 ②나중에 무엇을 손대야 하나.**
> "어떻게 시작하나"는 `README.md`, "이 클론의 지금 상태"는 `doctor.py`가 답한다.

| | |
|---|---|
| 기준 판 | **국면 2A 진행** — 국면 1 완료본(2026-08-20 승인) + 2A P-A(08-24) |
| 명세 | **`docs/spec/` 정제본 11종이 유일한 정본** — 옛 챕터군은 제거됐다(태그 `phase1-complete`가 좌표) |
| 회귀 | **461 PASS · FAIL 0** (11종, 클린 단독 실행) |
| 장부 | 가결정 86건 · 멈춤 0건 · **판정 필요 3건**(BLOCKERS 7·8·9) |
| 사내 반입 | **아직 없음** — 이 문서의 「반입 이력」이 비어 있다 |

---

## 1. 한눈에 — 기능별 반영 수준

| 기능 | 상태 | 지금 되는 것 | 남은 것 |
|---|---|---|---|
| **문서 읽기(파서)** | 🟢 동작 | xlsx · pptx | **docx · pdf** (→ §2) |
| **문서 유형 등록** | 🟢 동작 | 구축 모드 3단(생성→검수→확정) · 검수 뷰 HTML | 사내 문서로 첫 등록 (→ §3) |
| **인입** | 🟢 동작 | 계약 JSON → 그래프 · 재인입 회수 · 중복 차단 | — |
| **개체 판정** | 🟡 규칙 | 사전 조회 + 문자열 규칙 · **실호출 분기 서 있음** | 게이트웨이 연결 (→ §5) |
| **관계 추출** | 🟡 규칙 | 힌트 파일 + 문형 정규식 · **실호출 분기 서 있음** | 게이트웨이 연결 + 프롬프트 조립부 (→ §5) |
| **커밋 게이트** | 🟢 동작 | 패턴 대조 · 방향 미검증 큐 · 거부 로그 | — |
| **질의** | 🟢 동작 | 4단(링킹→확장→수집→답변) · 이원 근거 · 걸침 1홉 · **답변 생성 분기** | 게이트웨이 연결 · 그래프 사실 관련성 필터(P-B) |
| **수정 도구(I축)** | 🟢 동작 | 개명·병합·분리·폐기 · 파급 미리보기 · 연산 로그 | — |
| **플랫폼 창구** | 🟢 동작 | subprocess build/query · 그래프·큐·등록부 열람 | 화면(UI)은 사내 몫 |
| **계기판** | 🟡 mock | 8종 전부 계산됨 — **수치는 창작 데이터 기준** | 실데이터 첫 측정 (→ §6) |
| **층** | 🟢 2개 | 공정정보층(내장) · 품질지식층(등록 절차로 검증) | **신규 층 등록(R1)은 국면 2** |
| **골격** | 🔴 mock | 창작 공정 46노드 | **사내 공정으로 교체** ← 첫 작업 |

🟢 그대로 쓴다 · 🟡 돌지만 정밀도가 규칙 수준 · 🔴 사내 것으로 갈아야 한다

---

## 2. 문서 읽기(파서) — 포맷 확장이 여기다

### 지금 읽는 것

| 포맷 | 상태 | 무엇을 뽑나 |
|---|---|---|
| `.xlsx` · `.xlsm` | 🟢 | 셀 값 · **병합 범위**(전개는 normalizer) · 들여쓰기 · 굵기 · 이미지 위치 |
| `.pptx` | 🟢 | 슬라이드별 텍스트 프레임 · 발표자 노트 |
| `.docx` | 🔴 미지원 | — |
| `.pdf` | 🔴 미지원 | — |

미지원 포맷을 넣으면 `ValueError: 지원하지 않는 포맷`으로 **즉시·명시적으로** 실패한다.
조용히 빈 결과를 내지 않는다.

### 포맷을 늘리려면

**`parser/reader.py` 한 파일만 고친다.** 포맷 의존은 reader에만 격리돼 있어서(파서_명세 §3
규약 1) 새 포맷은 함수 하나 추가 + `read()`에 분기 한 줄이다.

```python
def read_docx(path):
    ...   # 반환 계약: {"format": "docx", "path": ..., <포맷별 구조>}

def read(path):
    if path.lower().endswith(".docx"):
        return read_docx(path)
    ...
```

**주의 3가지**
- 반환 구조는 **어댑터가 소비하는 계약**이다. 새 포맷의 구조를 정하면 그 포맷용 어댑터도
  그 구조를 전제로 쓰인다 — 나중에 바꾸면 어댑터가 전부 깨진다.
- reader는 **공용 코드**라 수정 시 버전 기록 + 통보가 계약이다(파서_명세 §9 규약 5).
- pdf는 텍스트 레이어 유무가 갈린다. 스캔 pdf는 OCR이 필요하고, 그것은 reader가 아니라
  **앞단 전처리**로 두는 것이 맞다(reader에 OCR을 넣으면 포맷 격리가 깨진다).

### 의존 패키지

| 패키지 | 쓰는 곳 | 없으면 |
|---|---|---|
| `openpyxl` | xlsx 읽기 | 파서만 멈춘다. 코어·질의는 돈다 |
| `python-pptx` | pptx 읽기 | 〃 |
| `orjson` (선택) | 그래프 직렬화 가속 | 표준 json으로 폴백 — 문제 없음 |

**`core/`는 표준 라이브러리만 쓴다** — 폐쇄망에서 설치 없이 돈다. 외부 패키지는 파서 전용이다.

---

## 3. 문서 유형(doc_type) — 지금 등록된 것

| doc_type | 지위 | 층 | payload | 무엇 |
|---|---|---|---|---|
| `cp` | 내장 | process | table | 관리계획서 |
| `pfmea` | 내장 | quality | table | 공정 FMEA |
| `ppt_process` | 내장 | process | prose | 공정 설명 슬라이드 |
| `ppt_quality` | 내장 | quality | prose | 품질 설명 슬라이드 |
| *(사내 문서)* | — | — | — | **아직 0건** |

`ipqc`(검사 성적서) · `toc_report`(목차형 보고서)는 등록 절차 검증에 쓴 창작 표본이고
**상시 등록 상태가 아니다** — 회귀가 돌 때 등록하고 지운다.

### 새 문서 유형을 늘리려면 — 코드를 안 짠다

```bash
python run.py register generate <이름> <층> <표본1> <표본2> --hint "관찰 힌트"
python run.py register review   <이름>            # 검수 뷰 HTML이 나온다
python run.py register confirm  <이름> --by "<승인자>"
```

표본 2부를 권장한다(1부면 "변형 미관찰" 경고가 뜬다). 검수는 **결과 뷰 승인 1회**이고
코드를 읽지 않는다 — 기계 관문(하네스)이 먼저 거른다.

**어댑터는 LLM이 만든다.** 지금은 그 자리가 fixture로 대체돼 있어(§5), 사내에서는 생성
세션을 붙이거나 어댑터를 손으로 써서 `mock/adapters/`와 같은 형식으로 두면 된다.
양식은 `kit/어댑터_스켈레톤.py`(빈칸 채우기) · 예시는 `kit/참조어댑터/` 3종.

---

## 4. 층 — 지금 2개

| 층 | 지위 | 카테고리 | 관계 |
|---|---|---|---|
| `process` 공정정보층 | 내장 | Process · Unit · Property | part_of · precedes · has_property · mirrors |
| `quality` 품질지식층 | 등록 | Failure · FailureEffect | causes · affects · occurs_in · controlled_by |

**층 추가는 config 파일 작성뿐이고 코드는 안 바뀐다** — 품질층이 그 증명이다(J3).
다만 **층 등록 도구(R1)는 국면 2 게이트**라 지금은 `layers/<이름>/config.json`을 손으로 쓴다.

---

## 5. LLM 지점 8종 — **골조는 섰고 설정만 비어 있다**

2A에서 게이트웨이 이음매를 세웠다. 여태 "훅 5곳"이라 불린 것은 **전부 주석**이었고
자동 점검이 문자열 `"HOOK"`을 세어 있는 것처럼 보고했다 — 이제 세는 것은 **실제
분기의 존재**다(`python doctor.py` ④절이 매번 실측한다).

| # | 지점 | mock 갈래 | 실호출 갈래 |
|---|---|---|---|
| ① | 비정형 추출 | 힌트 파일 + 문형 정규식 | `core/extract.py::_candidates_for` |
| ② | 개체 동일성 판정 | 공백 제거 후 동일/포함 → 0.95 | `core/matcher.py::_judge_live` |
| ③ | 임베딩 | sha256 → 64차 정규화 벡터 | `core/embeddings.py::embed` |
| ④ | 이미지 요약 | 고정 문자열 + `image_summary_source=mock` | `core/llm.py::summarize_image` (주입) |
| ⑤ | 구축 모드 생성 | fixture 반환 | `cli/register.py::_draft_live` |
| ⑥ | 질의 링킹 2·3단 | **돌지 않는다** (가짜 벡터의 유사도는 가짜 확신) | `core/query.py::_link_deep` |
| ⑦ | 구조 지도 | 지도 파일 + 번호 휴리스틱 | `parser/struct_map.py` (`ask` 주입) |
| ⑧ | 답변 생성 | 두 채널 정형 나열 (문장 생성 없음) | `cli/query.py::generate` |

**두 갈래가 같은 반환 계약을 지킨다** — 소비부는 어느 쪽인지 모른다. 그래서 mock
회귀가 실 연결에도 유효하다.

### 연결하려면 — 환경변수 4종

```bash
export USE_MOCK=0
export LLM_GATEWAY_URL=https://<사내 게이트웨이>/v1
export LLM_API_KEY=<토큰>
export CHAT_MODEL=<모델명>
export EMBED_MODEL=<임베딩 모델명>
```

**설정 접근은 `core/llm.py` 하나로 수렴한다** — 호출부는 환경변수를 직접 읽지 않고,
회귀가 그 수렴을 잠근다. 프로토콜은 OpenAI 호환 `/chat/completions`·`/embeddings`를
기본 가정으로 두었다. 사내 게이트웨이가 다르면 **`core/llm.py::_post` 하나만 고친다** —
그 국지성이 수렴점의 값이다.

**미설정 상태의 `USE_MOCK=0`은 조용히 mock으로 떨어지지 않고 명시적으로 실패한다.**
8지점 각각을 회귀가 실행으로 확인한다(`tests/test_2a_gateway.py`).

**USE_MOCK=1에서는 없어도 전 파이프라인이 돈다** — 추출·판정의 정밀도만 규칙 수준이다.

## 6. 관측 — 큐와 계기판

### 수정 큐 — 닫힌 20종, mock에서 8종 발화

| 발화 중 (8) | `auto_node` · `orphan_anchor` · `unknown_field` · `spec_conflict` · `mirror_asymmetry` · `direction_unverifiable` · `coord_mismatch` · `direction_conflict` |
|---|---|
| **미발화 — 회귀가 잠금 (8)** | `orphan_attach` · `missing_field` · `invalid_category` · `evidence_lost` · `parse_failure` · `adapter_mismatch` · `duplicate_doc_hold` · `hierarchy_unresolved` |
| **미발화 — 생산자 있음·회귀 없음 (1)** | `uncertain_match` (`core/build.py` 판정 3분기의 불확실 가지) |
| **미발화 — 생산자 없음 (3)** | `orphan_chunk_link` · `attach_conflict` · `structural_proposal` |

읽는 법이 셋으로 갈린다.

- **회귀가 잠근 8종**은 mock에 조건이 없을 뿐 코드가 있고 합성 입력으로 발화가 확인됐다.
  실데이터를 넣으면 자연히 뜬다.
- **`uncertain_match`**는 생산자가 있으나(판정이 임계 아래일 때) mock 표기가 전부 명확해
  조건이 오지 않았고 회귀도 걸어 두지 않았다. **실데이터에서 가장 먼저 뜰 kind다** —
  사내 표기 흔들림이 곧 이 큐다.
- **생산자 없는 3종**은 닫힌 목록에 이름만 있고 코드가 없다. 조건이 실제로 관찰되면 그때
  배선한다(P7 — 근거 없는 선반영 금지). 지금 없는 것이 결함은 아니지만 **"20종이 다 돈다"고
  읽으면 안 된다.**

**kind는 늘리지 않는다**(닫힌 20종). 새 사유가 필요해 보이면 기존 kind에 담기거나
`defects.log`로 가는 것이 설계다.

### 계기판 8종 — 지금 수치는 **품질 측정이 아니다**

창작 데이터 기준이라 숫자 자체에 의미가 없다. **실데이터 첫 측정이 진짜 기준선**이고,
그 수치가 이후 도입 판정(P7)의 근거가 된다.

| # | 지표 | 무엇을 판정하나 |
|---|---|---|
| 1 | 링킹 recall | 품질 기준선 |
| 2 | plateau | 층 구축 완료 시점 |
| 3 | 판정 보류율 | 판정 에이전트화 도입 |
| 4 | 청크 잘림률 | 랭킹 고도화 도입 |
| 5 | 링킹 미스율 | 하이브리드 서치 도입 |
| 6 | 허브 노드 차수 | 층 설계 점검 |
| 7 | graph 저장 크기 | **200MB → 저장 전환 판정 개시** |
| 8 | build 소요 시간 | **30초 → 동상** |

`python run.py gauges`

---

## 7. 업데이트가 필요해지는 시점 — 트리거별

**지금 하지 않는다.** 각 항목은 트리거가 와야 착수한다(근거 없는 선반영 금지 — P7).

| 트리거 | 무엇을 한다 | 근거 |
|---|---|---|
| 사내 공정 체계 확정 | **골격 seed 교체** → `bootstrap` | 첫 작업 |
| 첫 사내 문서 유형 | 구축 모드로 등록 | §3 |
| docx·pdf 문서가 대상에 들어옴 | `parser/reader.py`에 포맷 추가 | §2 |
| 사내 LLM 게이트웨이 개통 | **환경변수 4종 설정 + `USE_MOCK=0`** — 코드 수정 0(프로토콜이 다르면 `core/llm.py::_post` 하나) | §5 |
| 실데이터 인입 완료 | 계기판 첫 측정 → 기준선 확정 | §6 |
| 사내 문서 유형 첫 확정(등록) | **확정 어댑터·스키마의 소재지 판정** — 지금은 `review/`(미추적 디렉터리)라 유실 위험 | BLOCKERS 판정필요-8 |
| 실 모델로 질의 시작 | **링킹 2·3단을 매 질의 돌릴지 판정** — 질의당 임베딩+LLM 1회가 고정 비용이다 | BLOCKERS 판정필요-7 |
| 링킹 미스율이 높게 나옴 | 하이브리드 서치 검토 (R6) | 계기판 5 |
| 청크 잘림률이 높게 나옴 | 랭킹 고도화 검토 | 계기판 4 |
| graph 200MB 또는 build 30초 | 저장 계층 전환 판정 (R10) | 계기판 7·8 |
| 세 번째 층이 필요해짐 | 층 등록 도구 (R1) | 국면 2 게이트 |
| 극성 외 두 번째 분화 축 관찰 | `identity_axis` 일반화 | Rule of Three |
| 시계열·설비 데이터 연동 요구 | T3 참조 구조 (R11) | 적재 경계 밖 |
| 권한 분리 요구 | 접근 권한 일괄 (R12) | 지금은 좌석만 |

미결 전체 목록은 `docs/spec/1_금지와불변.md`·`docs/spec/개정대장.md`,
국면 2의 갭 목록은 **`docs/2A_작업목록.md`(58건)**, 계획은 `docs/국면2_계획.md`.
명세가 정하지 않은 것은 `docs/국면2_명세공백_인계.md` + `BLOCKERS.md` 판정필요-7·8·9.

---

## 8. 사내 반입 이력

반입할 때마다 한 줄씩 추가한다. **어느 판이 사내에 있는지**를 이 표로 안다.

| 반입일 | 판(커밋) | 무엇이 들어갔나 | 사내 확인 |
|---|---|---|---|
| *(아직 없음)* | | | |

기록할 것: 커밋 해시 · `doctor.py` 결과(회귀 합계) · 그때 교체한 골격 seed 판 ·
등록한 doc_type · 발견한 문제.

---

## 9. 알려진 한계 — 미리 알고 있어야 할 것

| 한계 | 실질 |
|---|---|
| **골격이 창작이다** | 사내 공정과 무관하다. 교체 전에는 좌표가 안 맞아 `orphan_anchor`가 대량 발생한다 — 정상이다 |
| **판정이 문자열 규칙이다** | 표기가 많이 다른 동의어("노칭기" ↔ "노칭 프레스")는 못 붙인다. 사전에 alias를 넣거나 LLM을 붙인다 |
| **추출이 통제 문형만 잡는다** | mock 텍스트는 그 규칙에 맞춰 창작됐다. 실문서 문장은 대부분 안 잡힌다 — 힌트 파일 또는 LLM이 답이다 |
| **시험 격리가 없다** | 회귀 스위트가 `data/`를 공유한다. **판정은 클린 상태 단독 실행**이 기준이고 `doctor.py`가 그렇게 돌린다 |
| **s15 정답표 갈림 1건** | 참조 성격 열을 LLM이 `attribute`로 배정했다(정답은 `UNMAPPABLE`). 템플릿 v0.4가 처방했고 검증은 다음 생성 실험 |
| **UI가 없다** | 국면 1은 CLI + 정적 HTML까지다. 화면은 사내 플랫폼 몫이고 결합은 subprocess + JSON이다 |

---

## 갱신 규칙

- **수치를 손으로 쓰지 않는다.** `python doctor.py`가 재고, 그 결과를 옮긴다.
- **§8 반입 이력은 반입할 때마다** 한 줄. 비어 있으면 사내에 아직 안 들어간 것이다.
- **§7 트리거 표는 줄어드는 표다.** 착수한 항목은 지우고 §1 상태를 올린다.
- 이 문서는 **현황**이고 결정이 아니다 — 무엇이 참인지는 `docs/`의 틀·카드가, 무엇이
  정해졌는지는 `DECISIONS.md`가 갖는다.
