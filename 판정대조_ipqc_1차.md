---
doc: 판정대조_ipqc_1차
version: v1.0 (2026-08-01 — 블라인드 role 배정 1차 실측)
status: 실측 완료. 프롬프트 템플릿 개정 지시 7건 · 원천 결함 3건 · mock 결함 2건 도출
relation: raw_mock_대장 §1의 파일 9·10(IPQC01·IPQC02)을 재료로 한 실측 기록이다. 파서_명세 §9 규약 1("외부에서 리허설 완료 상태로 전달")의 첫 실행이며, 킷 구성물 1(생성 프롬프트 템플릿)의 개정 근거를 소유한다.
---

# 블라인드 role 배정 판정 대조 — ipqc 1차

> **답하려는 질문**: 처음 보는 정형 양식에 대해 LLM이 role 배정표를 실제로 산출할 수 있는가, 특히 **6지선다의 6번째(UNMAPPABLE)**를 고르는가.
> **왜 지금까지 몰랐나**: 가결정 D-10이 등록 생성 LLM 호출을 fixture 반환으로 대체한다. 국면 1 리허설은 배선만 보고 판단은 보지 않는다. 그리고 S1(구축 모드 전 과정)이 비정형만 다뤄서 role 배정표가 의미를 갖는 정형 등록이 리허설 대상에 없었다.

---

## §1 실험 설계

| 항목 | 내용 |
|---|---|
| 재료 | `IPQC01.xlsx`(12행 16열) · `IPQC02.xlsx`(8행 16열) — 신규 정형 doc_type `ipqc`, 층 process |
| 지시문 | `kit/생성프롬프트_템플릿_v0.1.md` — role 6종 정의 · process 층 카테고리 정의문 · 관계 목록 · 골격 닫힌 목록 · 공통 조각 필드 · 출력 규격 4구획 |
| 힌트 | "1~2행은 제목과 문서 정보이고, 표는 그 아래부터 시작한다" |
| 판정자 | 이 세션의 맥락을 **전혀 모르는** 서브에이전트 2개. 프로젝트 문서 조회 금지, 지정 파일 외 열람 금지 |
| 조건 A | 표본 **2부**(IPQC01 + IPQC02) |
| 조건 B | 표본 **1부**(IPQC01만) |
| 정답표 | 파일을 만든 쪽이 사전에 봉인 작성(`_sealed/정답표_ipqc_봉인.md`). 판정자에게 미제시 |

**"정답"은 설계 의도이지 진리가 아니다.** 갈린 지점은 판정자의 오류가 아니라 **프롬프트 템플릿이 그 근거를 싣지 않았다**는 신호로 읽는다.

---

## §2 대조표 — 16열

| 열 | 의도 | 조건 A (2부) | 조건 B (1부) | 판정 |
|---|---|---|---|---|
| 대공정 | `anchor` | `anchor` | `anchor` | ○ |
| 공정No | `meta` | `attribute` | `attribute` | **✗ 둘 다** |
| 공정명 | `anchor` | `anchor` | `anchor` | ○ |
| 극성 | *(구조 필드 — role 대상 아님)* | `attribute` | `attribute` | **△ 둘 다** |
| 검사설비 | `entity`(Unit) | `entity`(Unit) + 정의문 이의 | `entity`(Unit) | ○ (단 §5-1) |
| 검사항목 | `entity`(Property) | `entity`(Property) | `entity`(Property) | ○ |
| 규격 | `attribute` spec contextual | 동일 | 동일 | ○ |
| 측정방법 | `attribute` | **`UNMAPPABLE`** | `attribute`(잠정)+이의 | **✗ — mock 결함(§6-1)** |
| 판정기준 | `attribute` | `attribute` | `attribute` | ○ |
| 부적합 조치 | **`content`** | `attribute` | `attribute` | **✗ 둘 다** |
| 적용모델 | *(구조 필드 — context)* | `attribute` contextual | `attribute` context | **△ 둘 다** |
| 검사자 | `meta` | `meta` | `meta` | ○ |
| 검사일시 | `meta` | `meta` | `meta` | ○ |
| 성적서번호 | `meta` | `meta` | `meta` | ○ |
| **최근 불량 이력** | **`UNMAPPABLE`** | **`UNMAPPABLE`** | **`UNMAPPABLE`** | **○ 둘 다** |
| **관련 표준문서** | **`UNMAPPABLE`** | **`UNMAPPABLE`** | **`UNMAPPABLE`** | **○ 둘 다** |

**일치 10/16.** 구조 필드 2건(프롬프트 누락)과 mock 결함 1건을 제외하면 **10/13**.

### 핵심 결과 — UNMAPPABLE 경로가 작동한다

**두 판정 모두 두 열을 정확히 `UNMAPPABLE`로 골랐고, 근거도 정확했다.**

- 최근 불량 이력 → "시점 의존 집계 스냅샷. 공정·항목의 항구 속성도 아니고 기록의 신상정보도 아니다. attribute로 넣으면 stale 데이터가 그래프에 박힌다."
- 관련 표준문서 → "`WI-노칭-003`은 노드 자격이 있으나 process 층 카테고리 3종에 문서가 없고 기록할 `target_layer`를 모른다. `entity` + `target_layer=<문서층>`이 정답일 가능성이 높으나 어떤 층이 존재하는지 제시받지 못해 판정 불가."

두 번째 근거는 CH2 2.7의 **L3 판정 신호**를 판정자가 스스로 도달한 것이다 — "5종에 안 맞으니 role을 하나 더 만들어야 한다"까지 갔다. 억지로 `meta`에 밀어 넣지 않았다는 것이 프롬프트의 "맞지 않으면 UNMAPPABLE" 지시가 실제로 먹힌다는 실증이다.

---

## §3 갈린 4건 — 전부 프롬프트 결함이다

| # | 열 | 원인 | 근거 |
|---|---|---|---|
| 1 | 공정No | 프롬프트에 **"사내 공정번호는 비표준·가변이라 조회 키가 아니다"**가 없다 | CH2 2.2 경계가 정한 내용인데 템플릿에 빠졌다. 판정자는 "공정에 붙은 식별값"으로 보고 attribute를 골랐다 — 그 자체로는 합리적이다 |
| 2 | 부적합 조치 | `content`의 판별 기준이 **"자유 서술 텍스트"**뿐이라, 짧은 정형 문구는 attribute로 보인다 | 두 판정 모두 "짧은 정형 조치 구절 — 노드 자격 없음"으로 attribute. 그런데 기존 `cp.json`은 같은 성격의 "대응계획"을 `content`로 정해 놨다. **원천과 프롬프트의 content 정의가 어긋난다** |
| 3 | 극성 | 프롬프트가 **"공통 조각 필드는 role 배정 대상이 아니다"**를 말하지 않는다 | 템플릿은 공통 조각 필드를 나열만 하고 지위를 안 밝혔다. CH2 2.5 규약 3이 정한 내용 |
| 4 | 적용모델 | 동상 | 동상. `context`는 맥락 상속 처리의 **입력**이지 role 핸들러를 타지 않는다 |

3·4는 같은 뿌리다. 그리고 4번은 **가결정 D-1(electrode_type의 role 미결)이 실제로 판정을 흔든다**는 첫 실측이다 — 원천 3건(정의서 §5.1 attribute / 명세 §6.2 정체성 관여 / D-1 구조 필드)이 불일치인 채로 두면 등록 세션마다 다르게 배정된다.

---

## §4 표본 2부 vs 1부 — ⑵-① 규약의 첫 실측

파서_명세 §6 규약 1은 "2부 이상 권장, 1부 허용 + 경고"이고, 1부의 위험을 "그 문서의 특이점이 규칙이 된다"로 서술했다. **이번에 그 이득이 구체적으로 관측되었다.**

**2부에서만 나온 관찰 4건**

1. **대공정 채움 전략이 표본마다 다르다.** IPQC01은 A열을 통째 병합했고, IPQC02는 행마다 "조립"을 반복 기재하다 마지막 2행만 병합했다. 2부 판정의 결론: *"어댑터는 병합 캐리와 행별 반복을 동시에 지원해야 한다. 병합만 가정하면 IPQC02가, 반복만 가정하면 IPQC01이 깨진다."* — 1부는 이 결론에 도달할 수 없다.
2. **극성이 공정 고정 속성이 아님을 확정.** 같은 "노칭"이 01=both, 02=cathode. 2부는 이걸 근거로 극성의 부착 대상을 정했고, 1부는 근거 없이 정했다.
3. **복수값 분할이 맞다는 확증.** IPQC01의 `"실링 폭, 실링 강도"`를 IPQC02가 `"실링 강도"` 단일 행으로 쪼개 적었다 → 2부는 "F는 쉼표 분할 열이 맞다"로 확정, 1부는 *"관용적 단일 항목명일 수 있어 판정 불가"*로 남겼다.
4. **분할 후 규격 복원 불가를 발견.** IPQC01의 그 행은 규격이 비어 있어, 쪼개도 "실링 폭"의 규격을 복원할 수 없다. 2부만 이 함의까지 갔다.

**1부가 더 나았던 지점 1건 — 그리고 그것이 오히려 위험이었다**

1부는 `mirrors` 엣지를 스키마에 선언했다(근거 1쌍, optional=true). 2부는 선언하지 않고 *"이 관계는 서로 다른 행에 있는 두 anchor 사이의 관계라 `{from, to}`가 같은 행 필드를 가리키는 현 edges 형식으로는 표현할 수 없다"*고 보류했다.

**2부가 옳다.** `mirrors`는 스키마 선언 대상이 아니라 **build 저장 직전의 자동 규칙**(config.mirrors, 구현문서 §5)이다. 1부는 근거 1쌍만으로 선언까지 갔고, 이것이 정확히 "그 문서의 특이점이 규칙이 되는" 사례다.

> **판정**: 표본 2부 권장은 유지하되, **경고 문구를 강화할 근거가 생겼다.** 현행 "표본 1부 · 변형 미관찰"에 더해 **1부 등록 시 판정자가 선언한 edges는 검수에서 특별 확인 대상**으로 표시하는 것을 검토한다(가결정 D-22).

---

## §5 두 판정이 공통으로 제기한 원천 결함 3건

### 5.1 Unit 카테고리 정의문이 검사 장비를 포섭하지 못한다 [실제 결함]

현행 정의문: *"공정을 **수행하는** 물리 장비의 기종/표준 명칭"*

2부 판정: *"`노칭 검사기`는 노칭을 수행하지 않고 계측만 한다. 정의를 넓게 읽어 Unit으로 배정했으나, **여기서 Unit이 아니라고 결정되면 검사설비 열 전체가 UNMAPPABLE이 된다.**"*

사내에 검사성적서·계측기록 계열 문서를 넣으려면 **Unit 정의문 개정이 선행**되어야 한다. 이것은 config 값 수정(L1)이지만, 대상 카테고리가 바뀌면 기존 노드 재분류가 따라오므로 가볍지 않다. **국면 2의 [사내 확인] 항목으로 등재한다.**

### 5.2 관계 닫힌 목록에 정의문이 없다 [실제 결함]

`categories`에는 정의문이 있는데 `relations`는 이름만 나열한다(`part_of`·`precedes`·`has_property`·`mirrors`). 그 결과 두 판정 모두 "설비와 공정 사이 관계"에서 막혔다.

- 2부: *"'수행한다/사용한다'에 해당하는 관계가 없어 `part_of`로 **근사**했다. 부적절하면 삭제해야 한다."*
- 1부: *"판정 불가 → 엣지 미선언."*

그런데 기존 `cp.json`은 실제로 `{설비, part_of, @process_ref}`를 쓴다. **정답이 있는데 그 근거가 어디에도 적혀 있지 않아 판정자가 도달하지 못한 것이다.** 층 config에 관계 정의문을 신설해야 한다 — 가결정 D-6의 `relation_patterns`와 같은 커밋에서 처리한다.

### 5.3 `mirrors`의 지위가 프롬프트에 없다

§4에서 서술. `mirrors`는 자동 규칙 소관이고 스키마 선언 대상이 아니라는 사실이 템플릿에 없다.

---

## §6 mock 결함 2건과 처리

### 6.1 측정방법 열에 두 종류가 섞였다 — **유지 결정**

두 판정 모두 지적: 계측기 기종명(`현미경`·`인장 시험기`·`열전대`)과 행위 서술(`육안 검사`), 그리고 혼합(`게이지 및 인장 시험`)이 한 열에 있다. 2부는 이걸 근거로 `UNMAPPABLE`을 골랐다.

**이것은 의도하지 않은 오염이었으나 결과적으로 유효한 재료이므로 유지한다.** 근거: 현실 문서에 흔한 형태이고, 두 판정 모두 이를 검수 화면 질문으로 올렸다 — 즉 **"판정 불가" 유도 메커니즘이 정형에서도 작동한다는 실증**이다. TOC01의 T6(들여쓰기 혼재)이 비정형에서 하는 역할을 정형에서 이 열이 한다.

단 CH6 6.3 규약 2("모든 항목은 짊어진 메커니즘이 있다")를 만족하려면 **명시적 배정이 필요하므로**, 봉인 정답표의 `attribute`를 철회하고 **"판정 불가 유도 (정형판)"**으로 재배정한다.

### 6.2 공정번호가 골격 순서와 어긋났다 — **수정 완료**

두 판정 모두 지적: 행 순서와 공정No 순서가 불일치(패키징 OP-70이 실링 OP-60보다 앞). 골격 순서는 `노칭 → 스태킹 → cathode 탭용접 → anode 탭용접 → 패키징 → 전해액주입 → 실링`이다.

번호를 골격 순서에 정렬했다: **노칭 10 · 스태킹 20 · cathode 탭용접 30 · anode 탭용접 31 · 패키징 40 · 전해액주입 50 · 실링 60.** IPQC01·IPQC02의 행 순서도 골격 순서로 재정렬했고, `PFMEA01.xlsx`·`CP04_unlabeled.xlsx`의 패키징 번호도 함께 고쳤다(`process_no`는 parsed 계약의 검증 대상이 아니므로 역산 정합에 영향 없음).

---

## §7 프롬프트 템플릿 개정 지시 (킷 구성물 1 → v0.2)

| # | 추가할 것 | 근거 |
|---|---|---|
| 1 | **공통 조각 필드는 role 배정 대상이 아니다**를 명시 — `process_group`·`process_ref`만 anchor이고, `electrode_type`·`context`는 시스템이 직접 읽는 구조 필드 | §3-3·3-4 |
| 2 | **`process_no`는 조회 키가 아니라 `meta`** — 사내 번호 체계가 비표준·가변이라 키로 쓸 수 없다는 근거까지 | §3-1 |
| 3 | **`content`의 판별 기준 보강** — "자유 서술"에 더해 *"필드 단위 규칙 처리가 불가능하고, 값이 아니라 문장으로 읽히며, 청크로 보존해 노드에 연결할 것"*. 짧아도 조치·설명·대응 계열이면 content | §3-2 |
| 4 | **관계 정의문 제시** — 관계 이름만 주지 말고 각각의 뜻과 방향(`Unit part_of Process` = 설비가 공정에 소속)을 명시 | §5.2 |
| 5 | **`mirrors`는 시스템 자동 규칙이므로 스키마에 선언하지 않는다**를 명시 | §4·§5.3 |
| 6 | **존재하는 층 목록을 제시** — "관련 표준문서" 판정에서 판정자가 `target_layer`를 몰라 막혔다. 걸침 필드 판단의 전제다 | §2 |
| 7 | **표본이 1부일 때의 지시 추가** — "변형을 관찰하지 못했으므로 edges 선언은 보수적으로, 근거가 1건뿐인 관계는 선언하지 말고 구획 4에 올려라" | §4 |

1~5는 전부 **원천에 이미 있는 내용을 템플릿이 안 실은 것**이다. 판정 정확도를 올리는 방법이 "더 좋은 모델"이 아니라 **"이미 확정된 규약을 프롬프트에 싣기"**라는 것이 이번 실측의 실용적 결론이다.

---

## §8 신규 가결정

| # | 지점 | 가결정 | 확정 경로 |
|---|---|---|---|
| **D-22** | 표본 1부 등록의 edges 취급 | 1부 등록 시 판정자가 선언한 edges를 검수 뷰에서 **특별 확인 대상으로 표시**한다. 경고 문구를 "표본 1부 · 변형 미관찰 · **선언된 관계는 근거 1건일 수 있음**"으로 확장 | 파서_명세 §7 / 세션 1 |
| **D-23** | 측정방법 열의 지위 | mock 결함이 아니라 **"판정 불가 유도(정형판)"** 재료로 유지한다. 봉인 정답표의 attribute 배정은 철회 | — |
| **D-24** | 공정번호 체계 | 골격 순서에 정렬(10·20·30·31·40·50·60). 전 raw mock에 일관 적용 | — |
| **D-25** | Unit 정의문 | 검사·계측 장비의 포섭 여부는 **국면 2 [사내 확인]**으로 이관. 국면 1에서는 Unit으로 두되 이 유보를 기록 | 국면 2 |

---

## §9 이 실측이 남긴 것

1. **UNMAPPABLE 경로는 작동한다** — 2/2, 두 조건 모두. 6지선다의 6번째가 장식이 아님이 확인되었다.
2. **판정이 갈린 4건은 전부 프롬프트가 근거를 안 실은 탓이다.** 모델 능력 문제가 아니다. 개정 지시 7건이 그 목록이다.
3. **표본 2부의 이득이 구체적으로 관측되었다** — 병합/반복 이중 전략, 극성 가변성, 복수값 확증. 그리고 1부의 위험(근거 1건으로 관계 선언)도 실제로 나타났다.
4. **판정자가 층 config의 결함을 두 건 찾아냈다**(Unit 정의문, 관계 정의문 부재). 등록 세션이 config 품질의 검사 장치이기도 하다는 것 — 예상하지 못한 이득이다.
5. **다음 회차**: 템플릿 v0.2로 같은 표본을 재판정해 갈린 4건이 수렴하는지 본다. 수렴하면 개정 지시가 유효했다는 뜻이고, 안 하면 원천 자체가 모호한 것이다.
