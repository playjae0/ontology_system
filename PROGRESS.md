# PROGRESS.md — 국면 1 구현 진행

> ⚠ **이 파일에 결정 번호를 적지 않는다 (08-10 신설 규칙).** 미러는 **보고**이고
> 장부는 `DECISIONS.md` 하나다. 번호가 필요하면 **등재한 뒤 인용**한다.
> 근거: G3 자체 검출 3건을 미러·커밋에만 적었더니 외부 세션이 그것을 이미 채번된 것으로
> 읽어 D-39~D-41 채번 충돌이 났다(08-10 소급 등재로 해소).

## A0 (G0) — 레포 상태 회수 + 계수 3종 · 2026-08-07 · **완료**

### 환경 확인 (구현문서 §9.1)

| 항목 | 값 | 판정 |
|---|---|---|
| OS | Linux 6.18.5 x86_64 · glibc 2.39 (Ubuntu 계열) | OK |
| Python | **3.11.15** | OK (3.10+ 요구 충족) |
| venv | `.venv/` 생성함 | OK |
| 설치 패키지 | openpyxl 3.1.5 · python-pptx 1.0.2 (+lxml 6.1.1 · pillow 12.3.0) | OK |

파서 의존(openpyxl·python-pptx)은 D-9에 따라 `parser/` 패키지에만 허용된다. 에이전트 코어(USE_MOCK 경로)의 "외부 패키지 0" 원칙과 충돌하지 않는다.

---

### 1. 상태 회수 — 증분 0 §2.2 단위 대장

**이 레포에 `core/`도 `data/`도 없다. 소스 코드가 0바이트다.** 따라서 기존 단위는 **전부 미완료**가 답이다(G0 프롬프트 항목 1). "G0 확인" 표기는 하나도 남지 않았다.

| 구간 | 단위 | 성격 | 상태(A0 확정) | 근거 |
|---|---|---|---|---|
| G0 | A0 레포 회수 | 신규(절차) | **완료** | 이 문서 |
| G1 | st 저장 계층 정비 | 신규(틀 A8) | **미완료** | `core/` 없음 |
| G1 | n1 근거 축 id | 신규 | **미완료** | 〃 |
| G1 | n2 doc_hash | 신규 | **미완료** | 〃 |
| G2 | n10 부트스트랩 | 신규(1b 흡수) | **미완료** (구 표기 "1b 완료 추정" 해소) | 〃 |
| G3 | 1c′·1d′ 인입 개수 | 개수 | **미완료** (구 표기 "1c까지 완료 실측" 해소 — 아래 판정 필요 ①) | 〃 |
| G3 | n3 추출 분리 | 신규 | **미완료** | 〃 |
| G3 | n4 커밋 게이트 | 신규 | **미완료** | 〃 |
| G4 | 2′ 질의 회귀 | 회귀 | **미완료** | 〃 |
| G4 | 3′ 품질층 + 3.5′ 재인입 | 개수+회귀 | **미완료** | 〃 |
| G5 | n5 I축 도구 | 신규(단위 5 흡수) | **미완료** | 〃 |
| G6 | 4′ 플랫폼 | 개수(+계기판 이관) | **미완료** | 〃 |
| G6 | n9 지문 스캔 | 신규(편의) | **미완료** | 〃 |
| P1 | n7 파서 코어 | 신규(외부 전달물) | **미완료** — 단 선행 자산 반입됨(미검증) | `parser/reader.py` |
| P2 | n8 생성 킷 | 신규(외부 전달물) | **미완료** — 단 구성물 3/6 반입됨 | `kit/` |
| P3 | n6 등록 파이프라인 | 신규 | **미완료** | `core/` 없음 |

---

### 2. 계수 ① — graph.json 직접 접근

**0지점.**

`core/`·`data/` 자체가 없고, 레포 전체 `.py` 6개 어디에도 `graph.json` 문자열이 없다(전수 검색).

**귀결**: G1의 `st`는 "기존 직접 접근 지점을 GraphStore 경유로 **교체**"가 아니라 **"처음부터 GraphStore로 작성"**이 된다. 교체 대상 목록이 비었으므로 st의 완료판정("graph.py 밖 직접 접근 0")은 착수 시점부터 자명하게 성립하며, 실질 작업은 GraphStore 신규 구현 + orjson 전환 + 계기판 7·8 출력이다.

### 3. 계수 ② — N#### 하드코딩

**0지점.** `N####` 형식도 `id_seq` 문자열도 전무하다.

**귀결**: **ULID 발효 확정**(CLAUDE.md 진행 규칙 — 13회차 사용자 판정). 마이그레이션 대상이 0건이므로 처음부터 ULID로 작성하고 `id_seq.json`은 만들지 않는다.

### 4. 계수 ③ — raw mock 반입

**반입 완료 · verify_roundtrip PASS(50항목 전량, FAIL 0).**

| 항목 | 결과 |
|---|---|
| `mock/raw/` 실물 | **10종** (CP01·PFMEA01·TOC01·TOC02·CP02_drift·CP03_bad·CP04_unlabeled·PPT_basic·IPQC01·IPQC02) |
| `tests/verify_roundtrip.py` | 실행 성공 — 전체 결과 **PASS** (역산 정합 성립 · 확대 목표 충족) |
| `kit/` | 템플릿 v0.1·v0.2 + 봉인 정답표 + `run_adapter.py` = **6종 중 반입 4** |
| 집계 | **PASS 50 · FAIL 0** |

재현: `./.venv/bin/python tests/verify_roundtrip.py` (결정적 — 로그 파일은 레포에 두지 않는다. MANIFEST 트리에 실행 로그 자리가 없고, 구 `out/`은 13회차 판정으로 `mock/fixtures/`에 흡수되었다).

> **이 PASS는 `prefix 10` 기준이다 — 진짜 판정이 아니다.**
> 출력이 `CP01.xlsx — 역산 정합 prefix 10건 + 확대분`, `record 28건`으로 나온다.
> D-15·정정 5항 #4가 요구하는 **prefix 11**은 raw `CP01.xlsx`의 11번째 record 자리에 coord_mismatch 행(공정구분 "스태킹" + 공정명 "노칭")을 **삽입**하고 확대분을 C12~C29로 민 뒤에야 성립한다(현재 28 record → 삽입 후 29 record, raw_mock_대장 v2.1 §2.1).
> **삽입은 G1의 선행 작업**이며, 그때 재실행한 `verify_roundtrip`(prefix 11)이 진짜 판정이다.

### 5. 반입 자산 (계수 ②·③에 포함하지 않음)

`core/` 코드가 아니므로 계수 대상이 아니다 — **"n7 선행 자산 반입됨(미검증)"**으로 기록한다.

| 경로 | 정체 | 상태 |
|---|---|---|
| `parser/reader.py` | 파서 공용 코어 reader (포맷별 원시 추출, 파서_명세 §3) | 반입됨 · **미검증** |
| `kit/run_adapter.py` | 실행 하네스 — 킷 6번째 구성물(D-27) | 반입됨 · **미검증** |
| `tests/xlsx_common.py` | raw mock 생성 유틸 | 반입됨 · 참조처 없음 |
| `mock/fixtures/` | 외부 세션에서 **실제 LLM이 산출한** adapter 2·schema 2·로그 3 (D-26) | 반입됨 |
| `mock/parsed/` | **비어 있음 — 신규 작성 대상**(§4.1, 정정 5항 #3) | 착수 전 |

문법 검사(`py_compile`)는 6개 `.py` 전부 통과. 절대경로 하드코딩 2건은 0단계 b에서 제거했다.

---

### 6. 초기 장부

- `DECISIONS.md` — **31건 생성**(D-1~D-31, 결번 없음). 소유 분담 4계 = 증분0 §7 14건 + raw_mock §6 7건 + 판정대조 §8 4건 + 실행검증_1차 §6 6건 = 31.
- `BLOCKERS.md` — 생성. **멈춤 사유 0건**, 판정 필요 2건 기록.

---

### 7. 판정 필요 (임의 판정하지 않음)

**① 이전 레포의 1a~1c 구현물이 이 레포에 없다.**
`CH6 6.5`는 *"착수 단위 1a~1d 중 최소 1c까지 실행·검수됨(재인입 중복 노드 실측 · run.py all 2회 61→147)"*이라는 실측 흔적을 기록한다. 그 코드는 **이 레포에 존재하지 않는다.** A0는 프롬프트 지시대로 "실물 기준 전부 미완료"로 확정했으나, 그 구현물이 (가) 다른 위치에 남아 회수 가능한지 (나) 유실되어 재작성 대상인지는 레포만으로 판정할 수 없다. **(나)라면 G1~G3의 실질 분량이 "개수(改修)"가 아니라 "신규 작성"으로 늘어난다.**

**② `mock/raw/CP01.xlsx`의 C11 삽입 시점.**
정정 5항 #4는 삽입을 지시하지만 실행 구간을 "G1 선행 작업"으로 둔다. A0에서는 손대지 않았다(prefix 10 기준 기록만 남김). G1 착수 시 첫 작업으로 처리한다 — 확인 요청.

---

### 8. 다음

정지점 2 — **G1 + G2**. 착수 전 선행 작업 2건:
1. `mock/raw/CP01.xlsx`에 C11 삽입 → `verify_roundtrip` 재실행(prefix 11)이 진짜 판정.
2. `mock/parsed/` 신규 작성(§4.1 — 구현문서 §6 mock 전문 기준, 4항 적용 상태로).

---

## G1 + G2 — 저장 계층 · 주소 체계 · 부트스트랩 · 2026-08-07 · **완료**

정지점 2. 완료판정 전 항목 PASS (`tests/test_g1_g2.py` — 31항목).

### 선행 작업

| # | 내용 | 결과 |
|---|---|---|
| ② | `mock/raw/CP01.xlsx`에 C11(coord_mismatch) **삽입** | 28 → **29 record**. C1~C10 불변, 확대분 C12~C29로 밀림 |
| ① | `mock/parsed/` **신규 작성**(마이그레이션 아님 — 정정 5항 #3) | CP01 11 · PFMEA01 13 · PPT01 8청크 |
| ③ | §4.2 신규 mock | PPT03(occ 충돌) · CP01B(doc_hash) |

`verify_roundtrip` **prefix 11 PASS** — 50항목 전량, FAIL 0. **이것이 D-18의 진짜 판정이다**(A0의 prefix 10 결과를 대체).

`mock/parsed/`는 손으로 쓰지 않고 `tools/gen_parsed_mock.py`가 raw의 prefix에서 파생시킨다. 손으로 쓰면 raw와 어긋날 수 있고 그 어긋남이 곧 D-18의 무효화이기 때문이다.

### st — 저장 계층 (틀 §4B-A8 · 카드 B6)

- `core/graph.py`의 **GraphStore가 층 그래프의 유일 경계**. 파일 이름까지 이 클래스가 소유한다(`GraphStore.for_layer()`) — 밖에서 경로를 조립하면 저장 방식을 바꾸는 날(R10) 고칠 곳이 다시 여러 군데가 된다.
- **직접 접근 0지점** — 테스트가 레포 전체 `.py`를 훑어 실증한다. A0 계수 ①이 0지점이었으므로 "교체"가 아니라 **처음부터 GraphStore로 작성**했다(D-32와 같은 취지).
- 직렬화 **orjson**(표준 json 폴백 — "외부 패키지 0" 원칙을 폴백으로 보존).
- **계기판 7** graph 0.005MB / **계기판 8** build 0.0s. 알람선(200MB·30초) 미초과.

### n1 — 근거 축 id · n2 — doc_hash · n10 — 부트스트랩

| 판정 | 결과 |
|---|---|
| **S6** occ 충돌 | PPT03 M2·M3이 동일 (section, text) → chunk_id 갈림(occ 0·1). 결함 로그 0건 |
| **멱등성 ①** 2회 인입 | id 집합·청크 저장 동일 |
| **멱등성 ②** 조각 순서 셔플 3회 | id 집합·청크 저장 동일 |
| **S5** doc_hash 차단 | CP01B 보류 · `duplicate_doc_hold` 큐 1건 · 청크 변화 0 · 같은 doc_id 재인입은 통과 |
| **1b 승계** | Process **8**노드 · part_of **7** · precedes **6** · status `seed` |
| registry | **builtin 층 1개**(process)만 존재 |
| **ULID** | 의미 축 id 전부 ULID · `id_seq.json` **없음**(발효 확정) |
| content 청크 | `{record_id}-{필드명}` 37건 (D8) |

### 다음

정지점 3 — **G3**(계약 v2 정합 · 추출 분리 · 게이트). ★가장 중요한 구간.

---

## G3 — 계약 v2 정합 · 추출 분리 · 게이트 · 2026-08-08 · **완료**

정지점 3(★). 완료판정 전 항목 PASS (`tests/test_g3.py` — 40항목). G1+G2·verify_roundtrip 회귀도 PASS.

### 1c′·1d′ — 변경점 닫힌 목록 5항 전부 반영

| 판정 | 결과 |
|---|---|
| C4 spec_conflict | **1건** (같은 context, 다른 값) |
| C7 병렬 항목 | context M1·M2 **2건 병렬** — 충돌 아님 |
| C8·C9 극성 결합 | `cathode 노칭 프레스` · `anode 노칭 프레스` **2노드** |
| mirrors | **2건** 자동 연결 |
| C10 mirror_asymmetry | 큐 1건 (`노칭::anode 버 높이`) |
| C5 극성 모호 anchor | `탭용접` → orphan_anchor |
| **C11 coord_mismatch** | **1건** — 파서→인입 사슬의 실증 |
| P5 linked=false | 보존 |
| P6 auto + 큐 | `주액기` |

`process_group`은 **부착하지 않고 골격 조상 대조만** 한다. 골격 밖 값은 coord_mismatch가 아니라 orphan_anchor로 갈리는 것도 확인했다.

### n3 — 추출 분리

`extract/{doc_id}.json`이 3.11 스키마 그대로다 — 표면형만·노드 id 없음·confidence 없음·span 없음. 재현성 3입력(`mock-1.0` / `e-1.0` / `quality-1`) 기록. **구축 2회 재실행 시 추출 미재호출** 확인. S13 orphan_attach **0건**, attach가 다른 청크의 개체로 붙어 has_property 생성.

### n4 — 커밋 게이트

| 분기 | 결과 |
|---|---|
| K1 동종 쌍 causes(경로 ③) | **direction_unverifiable** 큐, 커밋 0, 근거 청크 동봉 |
| K2 이종 쌍 affects | **커밋** |
| K3 방향만 반대 | **direction_conflict** 큐, 자동 반전 없음 |
| K4 관계 없는 서술 | 후보 **0** (과추출 없음) |

**회귀 — 경로 ②는 무비용 통과**: PFMEA causes **13** · affects **9** · occurs_in **8** · controlled_by **11** 전부 커밋. `gate_rejects.json`은 큐가 아니라 로그(사유별 건수)다.

### 이번 구간에서 고친 결함 3건 (장부 등재는 08-10 소급 — DECISIONS 참조)

1. **alias 과매칭** — alias에 문자열 포함 규칙을 걸면 `cathode 노칭 프레스`가 무극성 노드에 흡수된다. alias는 **정확 일치만**, 포함 규칙은 canonical에만 적용하도록 갈랐다.
2. **극성 정체성** — 위와 같은 뿌리. 극성이 다르면 후보에서 제외하는 것을 문자열 휴리스틱이 아니라 **정체성 규칙**으로 걸었다.
3. **부트스트랩 비멱등** — 골격을 다시 심어 ULID가 새로 발급되면서 2회 실행 시 노드가 60→72로 늘었다. 이미 심긴 골격은 다시 심지 않도록 고쳤다. **`run.py all` 2회 = 동일 그래프(60노드·71엣지)** 확인.

### 보고 — 품질층 config 생성 시점

G3 프롬프트가 "quality config의 prompts.extract 훅 활성"을 지시하고 §8 총괄표가 S2·S3을 QPPT01(층 quality)에 배정하므로, `layers/quality/config.json`을 이 구간에서 작성했다. 증분0 G2 n10의 *"품질지식층은 여기서 내장하지 않는다(G4의 3′이 신규 층 등록 절차로 추가)"* 서술과 겹치는 지점이다.

**내장하지는 않았다** — `registration: "registered"`로 두어 registry의 **builtin 층은 여전히 process 1개**다. G4 3′의 「수동 config 경로로 층을 등록한다」는 절차 검증은 그대로 남아 있고, config는 값 파일이라 되돌리기 비용이 0이다. **판정이 다르면 알려 달라.**

### 다음

정지점 4 — **G4**(질의 회귀 · 품질층 층 등록 절차 · 재인입 회귀). `git diff core/`(J3)가 여기서 판정된다.


---

## M2 반입 — 0~2단계 · 2026-08-10 · **완료** (3~4단계는 별도 세션)

**0단계** — `origin/main` 머지. 충돌 1건(`docs/CH2_문서계약.md`)은 main의 v2.2를 채택했고, v2.1에서 빠진 줄은 전수 확인 결과 전부 M2가 대체한 것들이다(좌표 닫힌 목록·극성 모호 구조 소멸·electrode_type 미결 종결·2.5 미결). 소문자 `Ch2` 중복은 되살아나지 않았고, `Ch7_운영.md`는 `CH7_운영.md`로 통일했다. seed 파일은 `skeleton_seed_v3.2.md`로 정상.

**1단계** — 판본 전건 일치: 틀 v2.6 · 카드 v13(J12·C17 신설, C10 확정) · CH2 v2.2 · CH3B v2.1 · CH5 v3.1 · 용어대장 08-10.

**2단계** — DECISIONS 38 → **48건**(D-1~D-48 결번 없음).
- **소급 등재 3건** — 08-08 레포 자체 검출분. 미러·커밋에만 있던 것을 장부로 옮겼다.
- **M2 §8 전달①~⑦ 7건** 반입.
- **챕터_대장 D-40 오기 4곳을 D-46으로 정정**. 명세 트랙이 §8 초안 채번을 대장에 인용해 놓고 재채번 때 개별 참조를 안 고친 것이 원인이다.
- 재대조 대상(극성 정체성)의 실물은 M2와 정합함을 확인했다 — `core/matcher.py`가 canonical 문자열을 파싱하지 않고 노드의 극성 필드를 비교해 후보를 제외한다. **종결 표기는 필드명 정리(3단계)와 함께 한다.**

**다음** — 3단계(seed 교체 → loader v3.2 전면 개정 → n10 재실행 5개 기대값 → G3 회귀 골격 기대값 갱신 → 1c′ M2 반영)와 4단계(mock raw 실링 재매핑). **별도 세션 착수.** G4는 그 뒤 별도 승인.

---

## M2 반입 — 3·4단계 · 2026-08-12 · **완료** (G4는 별도 승인)

기준점 `fd7020a`. 선행 판정 A(골격의 소재)·B(마커 어휘)·C(멱등성 함정) 전건 반영.

### 1. seed 반입 — 판정 A대로 파일을 갈랐다

`layers/process/skeleton.json` 신설(seed 문서의 JSON 블록 그대로 — 트리·ALIASES·POLARITY_LABELS).
`config.json`의 `skeleton`은 **선언만** 남겼다: `type: "tree-v3.2"` · `category` · `source` · `relations`.
골격의 **모양·카테고리·관계·소재**를 config가 값으로 선언하는 구조는 유지된다 — loader는 `type` 값으로
갈릴 뿐 층 이름도, 축의 값도 모른다.

### 2. loader(n10) v3.2 — 파생·검증·출력

파생 규칙은 판정문 §4 표대로 구현했다(tier / parent / polarity / precedes / canonical / alias).
**판정 B 반영**: 코드가 아는 것은 **구문 마커 4종**(`::` 접두·`@split`·`@unordered`·`@noflow`)뿐이고
축의 값은 seed `POLARITY_LABELS`의 **키에서 읽는다**. `cathode`/`anode` 문자열은 `core/`에 없다.

**명시적 실패 6종**: main·sub 이름 충돌 / 마커 어휘 밖 / `@unordered`가 인스턴스에 붙음 /
ALIASES 키가 트리에 없거나 모호 / 축값을 공정 이름으로 씀(리스트 표기 금지) / 값 타입 오류.

로드 시 **파생 대표 흐름을 출력**한다(순서 오선언의 유일한 안전망 — M9 계보). 출력이 seed 문서의
기대 출력과 문자 단위로 일치한다.

### 3. n10 재실행 — 기대값 5개 전건 일치

**깨끗한 그래프에서 전체 재빌드**(판정 C)로 대조했다.

| 항목 | 기대 | 실측 |
|---|---|---|
| 노드 | 46 | **46** |
| part_of | 45 | **45** |
| precedes | 22 | **22** |
| polarity ≠ none | 16 | **16** |
| mirrors 쌍 | 8 | **8** (엣지 16) |

파생 tier도 함께 확인: main 1 · sub 8(개념 4 + 인스턴스 4) · detail 37.
**극성 인스턴스 간 precedes 0건** — 비참여가 구조적으로 성립한다.

**판정 C 확인**: 옛 그래프 위에 그냥 돌리면 **50노드**가 나온다(프롬프트 추정 54와 다름 —
`조립·노칭·스태킹·패키징`은 canonical이 v3.2에서도 같아 멱등 upsert가 **재사용**하고, 체계가 바뀐
`cathode 탭용접·anode 탭용접·전해액주입·실링` 4건만 잔존한다). 잔존 경로는 **감지된다** — loader가
`⚠ 옛 골격 노드 4건 잔존`을 출력하고 `defects.log`에 남긴다. 조용히 지우지 않는다(P4 id 불변).

### 4. G3 회귀 — 3종 전건 PASS

| 스크립트 | 결과 |
|---|---|
| `tests/test_g1_g2.py` | **PASS 42 / FAIL 0** (n10 기대값을 구 8·7·6 → 46 체계로 갱신 + 파생·alias·흐름 출력 판정 추가) |
| `tests/test_g3.py` | **PASS 48 / FAIL 0** |
| `tests/verify_roundtrip.py` | **PASS 50 / FAIL 0** (역산 정합 prefix 11·13 유지) |

`run.py all` **2회 = 동일 그래프** 재확인(process 64노드 103엣지). 결함 로그 0건.

**G3 기대값 중 판정에 따라 뒤집힌 것 1건**: 구 *"C5 극성 모호 anchor('탭용접') → orphan_anchor"*는
A11-6(D5 **구조 소멸**)으로 성립하지 않는다. "탭용접"은 이제 개념 노드가 극성 무관 alias를 단독
소유하므로 모호하지 않고 **저해상도 부착**된다. 어서션을 뒤집어 다시 걸었다 — 극성 모호
orphan_anchor **0건** · orphan_anchor는 "목록 밖 이름"(`레이저노칭`·`셀 부풀음`)만 남는다.

### 5. 1c′ M2 반영

- 노드(Unit·Property)에 **`polarity` 닫힌 4값** 기록(`cathode`/`anode`/`none`/`unbound`).
  `electrode_type`은 **노드에 기록하지 않는다** — record의 구조 필드로만 읽는다.
- `core/matcher.py`의 후보 제외를 `electrode_type` → **`polarity`**로 정리했다.
- **A11-9 ①** 부착 골격 노드 polarity ≠ none이면 표면형 극성 결합을 생략하고 polarity는
  부착 노드에서 **상속**한다. 실발화 확인: `탭용접::cathode::용접 가압력`(polarity=cathode) —
  구 규칙이었다면 `…::cathode::cathode 용접 가압력`이 됐을 자리다.
- **A11-9 ②** record `electrode_type` ↔ 노드 `polarity`가 둘 다 확정인데 다르면 `coord_mismatch`.
  구현·정형/비정형 양 경로에 배선했으나 **현 mock에서는 발화하지 않는다**(아래 보고 3).
- **D-40 재대조 종결.** 판정 근거가 canonical 문자열이 아니라 **필드**임을 실물로 확인했다 —
  나아가 구 `strip_polarity()`를 **폐지**해 레포에서 canonical 극성 파싱 지점이 **0**이 됐다.

### 6. mock 재매핑 (4단계)

bare "실링"만 교체했다. 행별 판단 기준은 **관리항목/고장원인**이다 —
**실링 시간 계열 → 프리 실링**(주액 뒤 가실링 · 실패가 셀 부풀음으로 간다),
**온도·압력·폭·강도 → 사이드 실링**(파우치 외곽 봉지 · 밀봉 불량·이물 유입).
raw 7파일 23행 교체(CP01 4 · CP02_drift 1 · CP03_bad 2 · CP04_unlabeled 2 · PFMEA01 4 ·
IPQC01 6 · IPQC02 4). 한 병합 안에서 값이 갈리는 자리는 병합을 풀고 **값이 같은 연속 구간만
다시 병합**했다(실물 시트의 모양 유지 — record 수·자기완결 판정 불변).

`mock/parsed/`의 prose 2건(PPT01-C007 · QPPT01-K4)도 좌표만 교체했다. **본문은 그대로다** —
좌표 태깅은 닫힌 목록에서 고르는 것이지 표면형 일치가 아니다(A11-6). 나머지 공정명은 무수정이며
alias(`전해액주입`)·auto alias(`cathode 탭용접`)·짧은 이름(`사이드 실링`)으로 자동 정합했다.
`process_no`는 meta라 재부여하지 않았다.

### 7. 보고 — 새로 검출된 명세·구현 결함

1. **mirrors 짝 찾기가 스코프 붙은 canonical에서 조용히 실패했다.** 구 `strip_polarity()`는 **앞에
   붙은** 접두만 뗄 수 있어 `노칭::cathode 노칭 정밀도` ↔ `노칭::anode 노칭 정밀도`를 영영 짝짓지
   못했고, **정상 쌍이 `mirror_asymmetry` 큐로 샜다**(실측 3건 — 문서·미러의 "1건"과 불일치).
   F3의 "문자열 극성 토큰 파싱 폐지"가 가리키던 실물이다. 필드 기반으로 고쳤고 큐가 1건으로 맞았다.
2. **짧은 이름 auto alias의 모호.** seed에 `비전검사`가 2노드에 걸려 있어, 등재하면 소비처가 조용히
   하나를 고른다. 유일할 때만 등재하고 모호분은 loader가 출력하도록 했다.
3. **A11-9 ②가 현 mock에서 한 번도 발화하지 않는다.** 좌표가 인스턴스로 해소되면서 record 극성이
   **반대**인 행이 mock에 없다(있는 것은 일치하는 행뿐). ①은 실발화한다. 규칙 ②의 실증에는
   mock 1행(예: 공정명 `cathode 탭용접` + 극성 `anode`) 추가가 필요하다 — **명세 트랙 판단 요청.**
4. **① seed 경로가 커밋 게이트를 우회한다** + config `relation_patterns`에 골격 자신의 관계
   (`Process part_of Process`·`Process mirrors Process`)가 **없다.** 두 결함이 서로를 가려 왔다.
   → `BLOCKERS.md` 판정필요-3(비차단, 현행 유지로 전진).
5. **seed 형식의 트리 블록 키가 `PROCESS_TREE`**라 loader에 층 이름이 상수로 들어간다(B1 저촉).
   → `BLOCKERS.md` 판정필요-4(비차단, 상수 1곳에 격리).
6. **옛 골격 잔존을 담을 큐 kind가 없다.** 큐 20종이 확정 목록이라 신설하지 않고 `defects.log` +
   loader 출력으로 표면화했다. `stale_skeleton` kind 신설 여부 — **명세 트랙 판단 요청.**

### 다음

정지점 4 — **G4**(질의 회귀 · 품질층 층 등록 절차 · 재인입 회귀). **별도 승인 대기 · 착수 금지.**

---

## M2 후속 — G4 전 정리 · 2026-08-13 · **완료** (G4는 별도 승인)

대원칙: **core를 늘리지 않는다.** 새 큐·새 분기·새 특수 처리 **0건**. `core/`에서 바뀐 실행 코드는
**상수 4줄(개명) + 죽은 상수·분기 제거 −2줄**이 전부이고 나머지 증분은 독스트링·주석이다.

### 0. 선행 재확인 — 명세 트랙 서술이 실물과 일치한다

엣지 생성 지점 전수 확인: ① seed = `bootstrap.py` 3곳이 `g.add_edge()` 직접 호출(**게이트 비경유**) /
② 스키마 = `pipeline.py` / ③ 추출 = `pipeline.py` / ④ 자동 = `build.py` — 셋만 `commit_edge`를 지난다.
`PATH_SEED` 상수는 선언만 있고 **호출부 0곳**이었다. 서술과 실물이 어긋나지 않으므로 그대로 착수했다.

### 1. seed 경로 ① — 결함이 아니라 설계로 종결

seed는 후보가 아니라 **선언**이다. gate.py 독스트링을 "넷 모두 같은 관문" → "**셋이 지난다 · ①은 비경유가 설계**"로
정정하고, 죽은 `PATH_SEED` 상수와 그것을 읽던 분기를 **제거**했다(남겨 두면 다음 사람이 태워도 되는 신호로 읽는다).
config에 주석 1줄. 회귀 4항으로 잠갔다 — 패턴표에 골격 관계 부재 / **경로 ②③④ 어느 쪽도 커밋 불가**(게이트 실판정) /
loader가 gate를 부르지 않음 / 골격 엣지 status=seed·거부 로그 0건.

> **지시 전제와 달랐던 점 1건**: `relation_patterns`에 **`Process precedes Process`가 이미 있었다.**
> 경로 ③은 `direction_unverifiable`로 막히지만 **경로 ②(스키마 edges 선언)는 무비용 통과**라, 정형 문서가
> 골격 흐름을 개정할 수 있는 상태였다(A11-2 "순서의 출처는 seed 하나뿐"과 충돌). 소비처 0곳이고 킷 템플릿 v0.2가
> 이미 *"골격이 이미 보유하므로 스키마에서 선언하지 않는다"*라고 못박고 있어 **제거**했다. "넣지 마라"의 취지를
> 그대로 집행한 것이며, 되돌리려면 config 한 블록이다.

### 2. seed 키 개명 — 격리가 아니라 개명

`PROCESS_TREE` → **`TREE`**, `POLARITY_LABELS` → **`AXIS_LABELS`**. seed 실물 · loader 상수 · seed 문서의
JSON 블록을 같은 커밋에서 맞췄다. 노드 필드 `polarity`와 config `polarity` 블록은 **그대로** — A11-8의 시스템
어휘이자 B1의 명시된 예외다. **개명 후 깨끗한 재빌드로 기대값 5종 불변 확인.**
판정문·대장의 형식 서술 갱신은 명세 트랙 몫으로 남긴다.

### 3. `stale_skeleton` 큐 — 미신설로 종결

큐 20종은 닫힌 목록이고 골격 잔존은 **일회성 마이그레이션 산물**이다. 개발 국면 정본 경로 = 깨끗한 재빌드,
운영 국면 정본 경로 = **I1 개명**(id 불변 · 옛 canonical은 alias 강등)이므로 구조적으로 발생하지 않는다.
loader의 ⚠ 경고 + `defects.log`는 개발 안전망으로 유지.

### 4. A11-9 ② 실발화 — core 0줄, 데이터 1행

`CP01.xlsx`에 좌표 `cathode 탭용접`(→ `탭용접::cathode`) + 극성 `anode` 행을 **record 12 자리에 삽입**했다.
C1~C11 불변, 확대분 C13~C30. **코드는 한 줄도 바뀌지 않았고 규칙이 그대로 발화했다:**

```
coord_mismatch | record의 극성 'anode'과 좌표 '탭용접::cathode'의 극성 'cathode'이 다르다
```

표면화만 하고 자동 정정하지 않는다. 사슬은 단방향 유지 — **수기 기대값 튜플**(EXP_CP 12번째, 손으로 씀) →
raw 검증 → parsed 파생. 기대값을 raw에서 만들지 않았다.

> **딸려 온 비용 1건**: 확대분에 넣으면 인입되지 않아 규칙이 발화하지 않는다 — 행이 **prefix 안**에 있어야 하므로
> **CP01 prefix가 11 → 12로 확장**된다. C11 삽입(10→11)과 같은 사유·같은 방식이지만, 원천의 "11건" 수치를
> 옮기는 일이다. **명세 트랙 확인 요망.**

### 5. 실링 재매핑 후속 확인

**봉인 정답표는 IPQC 16열의 role 배정 의도만 담는 열 단위 문서다** — 행 데이터가 없어 재매핑의 영향을 받지 않는다.
수기 기대값(EXP_CP·EXP_FM·SKEL·ORDER)은 3단계에서 이미 갱신했고 이번 회귀에서 재확인했다. **추가 조치 없음.**

### 6. 회귀 — 3종 전건 PASS

| 스크립트 | 결과 |
|---|---|
| `tests/test_g1_g2.py` | **PASS 46 / FAIL 0** (seed 경로 잠금 4항 추가 · CP01 12 record) |
| `tests/test_g3.py` | **PASS 50 / FAIL 0** (coord_mismatch 2건 · A11-9 ② 실발화 판정 추가) |
| `tests/verify_roundtrip.py` | **PASS 50 / FAIL 0** (prefix 12 · CP01 30 record) |

기대값 5종(깨끗한 재빌드): **46 / 45 / 22 / 16 / 8** — 개명·행 추가 후에도 불변.
`run.py all` 2회 = 동일 그래프(process 66노드 105엣지).

### 7. 보고 — 새로 검출된 결함

**C12 한 행이 인접 결함 2건을 함께 드러냈다.** 둘 다 core에 새 분기가 있어야 고쳐지므로 ★ 조항대로
**고치지 않고** 현행을 회귀로 잠갔다(`test_g3.py`가 조용한 변화를 잡는다).

1. **F3의 "하향 연쇄"가 미구현이다.** A11-9 ①로 부착된 노드는 극성이 이름이 아니라 **주소**에 있는데 짝 찾기
   키(`base_canonical`)에는 그 주소가 그대로 들어간다. `탭용접::cathode::용접 강도`의 짝은
   `탭용접::anode::…`이라 **둘 다 있어도 절대 페어링되지 않고**, 그런 노드가 전부 편측으로 큐에 오른다.
   실측 오검출 2건.
2. **A11-9 ①의 전제가 Unit에서 성립하지 않는다.** "극성이 이미 주소에 있다"는 **스코프가 붙는 카테고리에서만**
   참인데 `canonical_scope.bind_categories`는 Property뿐이다. 결과로 `초음파 융착기`(polarity=none)와
   `초음파 융착기`(polarity=cathode)가 **같은 canonical로 공존**한다 — D-40이 후보에서 제외하고 A11-9 ①이
   이름을 안 바꾸기 때문이다. 이름으로 구별되지 않는 두 노드는 P4의 취지에 어긋난다.

→ 선택지·귀결은 `BLOCKERS.md` **판정필요-5**(비차단).

### 다음

정지점 4 — **G4**. **별도 승인 대기 · 착수 금지.**

---

## G4 0단계 — 판정필요-5 종결 + 장부 정리 · 2026-08-13 · **완료**

착수 전 문서 확인 3건 전건 통과: CH3B **v2.2** · `skeleton_seed_v3.2.md` 없음(정본 `skeleton_seed.md`) ·
seed 실물(`TREE`·`AXIS_LABELS`)과 문서 JSON 블록 **완전 일치**.

### ① F3 하향 연쇄

짝 키를 canonical 전문에서 **세 요소**로 바꿨다 — 부모(축 인스턴스이고 mirrors 엣지가 있으면 **개념 노드로
동일시**) · 주소 접두를 제외한 자기 이름부 · polarity 반대. 조립 시점에 `mirror_scope`·`mirror_name`을
적어 두므로 canonical을 되 파싱하지 않는다. **짝 없는 인스턴스는 동일시하지 않는다** — mirrors 엣지가
없으면 그 자리는 분화 쌍이 아니고, 올려 잡으면 무관한 자식들이 한 키로 뭉친다.

실측: `탭용접::cathode` → 키 부모 `탭용접`으로 정규화됨. 합성 쌍을 넣어 규칙 자체를 실증했다 —
`탭용접::cathode::용접 강도` ↔ `탭용접::anode::용접 강도` **페어링 성립**, 그때 큐는 **1건**(`노칭::버 높이`).

### ② A11-9 ① 생략의 적용 범위

스코프 카테고리(현행 Property) **한정**. Unit은 F1 극성 결합 유지. `canonical_scope.bind_categories`는
건드리지 않았다. 실측: `초음파 융착기`(none) / `anode 초음파 융착기`(anode)로 갈려 **canonical 중복 0건**.

### ③④ 장부

BLOCKERS 판정필요-5 **종결** — 이로써 판정 필요 **0건**. DECISIONS 55 → **57건**,
D-15·D-18의 CP01 prefix 문면을 11 → 12로 정리(원천 `raw_mock_대장` v2.2는 이미 12).

### 회귀 (0단계 끝)

| 항목 | 결과 |
|---|---|
| `test_g1_g2` / `test_g3` / `verify_roundtrip` | **46 / 50 / 50 PASS · FAIL 0** |
| 골격 기대값 5종 | **46 · 45 · 22 · 16 · 8 — 불변** |
| `run.py all` 2회 | 동일 그래프 |

### ⚠ 보고 — 정지점 4의 예측과 어긋난 수치 1건

*"mirror_asymmetry가 3건에서 1건으로 내려온다"*는 성립하지 않았다. **큐는 3건 그대로이되 내용이 바뀌었고,
남은 3건은 전부 참이다.**

| 큐 항목 | 수정 전 | 수정 후 |
|---|---|---|
| `노칭::버 높이` | 참 (정상 신호) | 참 — 유지 |
| `초음파 융착기` | **오검출** — canonical 중복 노드가 만든 것 | 참 — `anode 초음파 융착기`에 cathode 짝이 없다 |
| `탭용접::용접 강도` | **오검출** — 짝 키가 달라 페어링 불가 | 참 — `탭용접::anode::용접 강도`가 존재하지 않는다 |

두 오검출 **메커니즘**은 사라졌다(중복 노드 0건 · 키 정규화 성립). 남은 것은 **C12가 한쪽 극성만 선언한
행이기 때문**이다 — C12는 좌표가 cathode인데 극성이 anode인 의도적 모순 행이고, 그것이 만드는 노드는
구조적으로 편측일 수밖에 없다. 정지점 4의 "1건" 예측은 짝이 이미 있다는 전제에서 나온 것으로 보인다.

**판단 요청**: 큐를 1건으로 내리려면 anode 쪽 대응 행이 필요한데, 그것은 CP01 prefix를 12 → 13으로 다시
올린다. `raw_mock_대장` v2.2가 방금 12로 동기화됐으므로 **레포가 원천을 다시 앞지르게 된다** — 데이터
추가는 하지 않았다. 대안은 현행 유지(남은 3건을 참으로 인정)다.

---

## G4 본편 — 2′ 질의 회귀 · 3′ 품질층 · 3.5′ 재인입 회귀 · 2026-08-13 · **완료**

### 2′ — 질의 4단 + 이원 근거 채널

**증분 0은 2′를 "회귀" 단위로 분류하지만 이 레포에는 `core/query.py`도 `cli/query.py`도
`mock/queries.json`도 없었다** — 회귀가 아니라 **신규 작성**이다(D-32 계보). 셋 다 작성했다.

- `core/query.py` — 범용 4단(링킹·확장·수집·답변). 확장 규칙·문장 틀·질문 유형 표기가
  전부 층 config 값이고 코드는 스펙을 인자로 받아 돈다.
- `cli/query.py` — 단일 진입점 라우터(전역 링킹 → 층별 호출 → 브리지 1홉 → 합성).
  `python run.py query "<질문>"`으로도 부른다.
- `mock/queries.json` — 스모크 12문항 + 순서 파생 4문항.

| 판정 | 결과 |
|---|---|
| 12문항 expected_path | **전건 일치** |
| flow 특례(5번) | 골격 대표 흐름 **22줄 통째** · 축 인스턴스 없음(개념 레벨) |
| 2홉 도달 | 스태킹 →(part_of 하향)→ 스태커 →(has_property)→ 적층 정렬도 |
| 링킹 미스 로그 | `data/link_miss.log` 기록 확인 |
| 읽기 전용(P6) | 질문 표기가 사전에 들어가지 않음 |
| precedes·mirrors | 기본 확장에서 제외(config 선언) |

**순서 파생 3분기와 해상도 표기** — 규약 8 문면 그대로 나온다.

```
① 자기 선언   노칭 다음 공정은 스태킹이다                       (해상도 표기 없음)
② 부모 파생   (탭용접::pre용접 기준) 탭용접::pre용접::cathode 다음 공정은
                                     탭용접::pre vision::cathode이다
② 합류 자동   (탭용접::laser용접 기준) 탭용접::laser용접::cathode 다음 공정은
                                     탭용접::bead press이다
③ 정보 없음   조립의 다음 공정은 골격에 선언되어 있지 않다 (순서 정보 없음)
```

### 3′ — 품질층 = 신규 층 등록 절차의 첫 검증 대상 (J10)

**수동 config 경로로 수행했다. R1 도구를 쓰지 않았고, 등록을 위해 실행한 코드도 없다.**
필요했던 입력 항목은 아래가 전부이며 **전부 config 파일 한 장**이다 — 세션 4(R1)의 재료다.

| 입력 항목 | 값의 출처 |
|---|---|
| `layer` · `registration` | 사람이 정한다(`registered` — 내장이 아님) |
| `categories` + **정의문** | 사람(층 소유자). 추출·판정 프롬프트에 그대로 실린다 |
| `skeleton`(type·category·data) | 닫힌 Tier1 목록이면 여기 직접, 트리면 `source` 파일 |
| `relations` 배열 | 층이 갖는 관계의 선언 — registry가 이것을 읽는다 |
| `relation_patterns` + **정의문** | 게이트 패턴(추출 경로 전용). 정의문이 없으면 등록 판정이 막힌다 |
| `query_traverse` · `cross_layer_traverse` | 확장 규칙 — 관계별 방향·재귀 |
| `fact_templates` | 답변 문장 틀(엣지·attr) |
| `query_intents` | 질문 유형 표기(USE_MOCK 분류기) |
| `prompts` | 추출·판정 지시문 훅 |

등록 후에도 **registry의 builtin 층은 공정층 1개**다.

| 판정 | 결과 |
|---|---|
| causes 사슬 | 이물 유입 → 절연 파괴 → 내부 단락 **존재** |
| cross 9번 | occurs_in 역방향 — 노칭의 Failure 3종 |
| cross 10번 | affects 역방향 직접 결과 + 수집 노드 간 causes 문장화 |
| 걸침 엣지 문장화 | 출발 층(품질층) 템플릿 사용 |
| **Q1~8 회귀 무오염** | 브리지 on/off에서 **경로·직접 근거 동일** |

> 회귀 무오염의 판정 기준을 밝혀 둔다. 명세 §8-6은 *"1홉은 확장 깊이만 제한하지 딸림 자체를
> 막지 못한다"*고 명문화하므로, 판정은 "Failure가 안 딸려온다"가 아니라 **"직접 근거가 밀려나지
> 않는다"**로 잡았다 — 브리지 유래는 tier2로만 들어오고 잘림은 바깥부터다.

### 3.5′ — 재인입 회귀

전 문서 재인입 후 **노드 66 → 66**(중복 0) · provenance 잃은 항목 0 · 질의 응답 동일.
`run.py all` 2회 = 동일 그래프.

### 회귀

| 스크립트 | 결과 |
|---|---|
| `test_g1_g2` / `test_g3` / **`test_g4`** / `verify_roundtrip` | **46 / 54 / 25 / 50 PASS · FAIL 0** |
| 골격 기대값 5종 | **46 · 45 · 22 · 16 · 8 — 불변** |

### J3 — `git diff core/` (이 구간의 가장 중요한 구조 검증)

**소급 확인: J3는 이미 그때 깨져 있었다.** `layers/quality/config.json`이 처음 들어온 커밋은
`4b3f6eb`(G3, 08-07)이고 **그 커밋은 core/를 7파일 924줄 변경했다.** 기준점을 옮겨 맞추지 않고
그대로 적는다. 다만 그 변경은 같은 커밋에 든 G3 자신의 단위(1c′·1d′·n3·n4) **신규 작성분**이며
품질층 전용 코드는 아니다 — 아래가 그 구분의 실측이다.

**실질 판정(품질층이 config만으로 도는가): 성립.** `core/`에 품질층 어휘 0건 —
`quality`·`FailureEffect`·`occurs_in`·`controlled_by`·`pfmea` 전부 **0**. 남은 언급은 독스트링
예시뿐이다(`graph.py`·`ids.py`의 카테고리 열거, `gate.py`의 사례 문장).

**이번 구간분**: 0단계 커밋(`5f67897`) 이후 core/ 변경은 **2′ 신규 작성분뿐**이다 —
`core/query.py`(신규) + `core/store.py`(줄 로그 헬퍼 1개·상수 1줄). **3′·3.5′로 인한 core 변경은
0줄**이며, 그것이 J3가 실제로 묻는 것이다. 정지점 4의 완료판정 문면(*"git diff core/가 비어
있음"*)은 2′가 회귀가 아니라 신규였기 때문에 **문자 그대로는 성립하지 않는다.**

### 보고 — 새로 검출된 결함 1건 (J3 관찰 데이터)

**`core/extract.py`의 문형 규칙이 품질층 관계 이름을 코드로 알고 있다.** `_PATTERNS` 상수가
`causes`·`affects`를 하드코딩한다 — config `prompts.extract` 훅은 있으나 **문형 규칙 자체가
config로 표현되지 않았다.** 이것이 정확히 구현문서 §7이 말한 *"config 표현 부족 → core 패턴
추가"*의 실측 지점이다. G3에서 들어온 것이고 이번 구간이 만든 것은 아니다.
고치면 core가 층 어휘에서 완전히 자유로워지지만 추출 거동이 바뀌므로 **손대지 않았다.**

### 다음

**국면 1 잔여: G5(n5 I축 4연산) · G6(4′ 플랫폼 + 계기판 8종 · n9 지문 스캔) · P트랙(n7 파서 코어 ·
n8 킷 6종 · n6 등록 파이프라인) — 셋 다 미착수이며 G5·G6은 별도 지시 대기다.**

---

## 20회차 반입 후속 — 하네스 수정 · A-3 재실행 · A-4 관통 · 큐 멱등화 · 2026-08-18 · **완료**

### 1. 하네스 1줄 수정 (payload_kind 분기)

`kit/run_adapter.py` ④단의 `fields 선언 존재`를 payload_kind로 갈랐다 —
**prose는 `fields`가 비어 있어야 PASS**(D-31 확정 · 카드 C17 · CH2 2.5), table은 현행 유지.

> **요청문 전제와 다른 점 1건**: *"payload_kind는 매칭 스키마 JSON에 있다"*고 했으나
> **3차 산출 스키마 2종(ipqc·toc_report) 모두 그 키가 없다.** 레포 정본 `schemas/`에서도
> prose 2종만 갖고 table 3종은 없다 — 즉 스키마의 payload_kind는 선택 선언이다.
> 그래서 **스키마 우선 · 어댑터 폴백**으로 읽는다(`payload_kind_of`). 어느 쪽도 선언하지
> 않으면 분기 불가이므로 명시적 실패다. 하드코딩 분기는 없고 doc_type 이름도 보지 않는다.
> fixture는 손대지 않았다(D-26).

### 2. A-3 공식 하네스 재실행 — **전항 PASS**

| doc_type | 결과 | 로그 |
|---|---|---|
| ipqc (table) | **PASS 43 / FAIL 0** | `mock/fixtures/log/harness_ipqc_공식.txt` |
| toc_report (prose) | **PASS 43 / FAIL 0** | `mock/fixtures/log/harness_toc_공식.txt` |

3차 대비 판정 변동을 전수 대조했다 — **정확히 4줄만 움직였고 그 외 동일**하다.
toc 2건 `FAIL → PASS`(대상) · ipqc 2건은 라벨 개명뿐(판정 불변).

### 3. A-4 관통 1회 — ipqc (table)

재현 가능하도록 `tools/passthrough.py`(범용 — 어댑터·스키마·문서를 인자로 받는다)를 두고
`schemas/ipqc.json`에 3차 스키마를 배치했다. **정식 등록 절차(n6·S15)는 P3 소관이고
여기서는 파일 배치까지다.**

| 기대 확인 | 결과 |
|---|---|
| ① 산출이 스키마 선언과 일치 | `검사설비 part_of @process_ref` **11건** · `@process_ref has_property 검사항목` **28건** — 선언 2종 전부 적재. 신규 노드 38(Unit 10 · Property 28) |
| ② 큐 유입이 닫힌 목록 안 | `missing_field` 33 · `auto_node` 38 · `spec_conflict` 2 — **전부 20종 안. 새 kind 0** |
| ③ 회귀 4종 불변 | `test_g1_g2` 46 · `test_g3` **57**(+3 잠금) · `test_g4` **27**(+2 잠금) · `verify_roundtrip` 50 — FAIL 0 |

**관통이 결함 2건을 잡았다. 둘 다 하네스는 통과시킨 것들이다** — 하네스는 조각까지만 보고
인입 코드를 부르지 않기 때문이다.

- **어댑터가 `context`를 스칼라로 낸다**(`"M1"`, 계약은 임의 딕셔너리 — CH2 2.2).
  인입이 `ValueError`로 **통째 죽었다.** 이제 `missing_field`로 표면화하고 그 필드만
  버린 채 전진한다(D-30 계보). 33행 전부 해당 — fixture는 손대지 않았다.
- **엣지 끝점 `@`가 `to`에서만 해소됐다.** `from`에 오면 `resolved.get("@process_ref")`
  → `None`으로 **큐도 로그도 없이 사라진다.** ipqc의 `has_property` 선언이 0건 적재되고
  있었다(수정 후 28건). 하네스의 참조 무결성 검사는 양쪽을 같게 보므로 스키마는 정당했다.

### 4. ㉮ 큐 적재 비멱등 — 처리

**허브 진단보다 범위가 넓었다.** `link_mirrors`만이 아니라 `coord_mismatch`·`orphan_anchor`·
`spec_conflict`·`direction_*`이 **전부** 증식한다(1회 → 2회에 2→4 · 1→2). 원인은
`enqueue`의 무조건 append이므로 **적재 지점 하나**를 고쳤다.

**싣는 쪽과 내리는 쪽을 갈랐다.**
- `enqueue` — 동일 `(kind, doc_id, payload)`를 두 번 싣지 않는다(규약 6 "쌍 키로 중복 제거")
- `store.drop` — 재계산하는 쪽이 자기 소관을 먼저 걷어낸다. `link_mirrors`가 호출한다
  (규약 6 "대칭이 회복되면 큐에서 제거" = self-heal)

| `run.py all` | 1회 | 2회 | 3회 |
|---|---|---|---|
| 전체 큐 | 61 | **62** | **62** |
| mirror_asymmetry | 3 | 4 | 4 |
| auto_node | 51 | **51** | **51** |

> **문서 단위 일괄 회수(`queue.remove_doc`)는 채택하지 않았다.** 먼저 그것으로 구현해
> 재보니 **재검출되지 않는 상시 조건까지 지워졌다** — `auto_node` 51건(미검토 노드
> 작업목록)이 재인입 한 번에 0이 됐다(61 → 11). 계약의 "옛 큐 회수"는 조건이 해소된
> 항목을 내리라는 뜻이지 화면 전체를 비우라는 뜻이 아니다.
>
> **잔여 1건(비차단)**: 1회(61) ≠ 2회(62)다. `link_mirrors`가 문서 빌드 중에 돌아
> **뒤에 인입되는 문서가 만드는 극성 노드를 1회차에는 못 본다**(PPT01의
> `탭용접::cathode::용접 가압력`). 2회차에 수렴하고 이후 불변이며 그래프는 1회차부터
> 동일하다. 근본 해소는 mirrors 재평가를 **전 문서 인입 후 1회**로 옮기는 것인데
> 그것은 run 오케스트레이션 변경이라 손대지 않았다 — 판단 요청.

### 회귀

| 스크립트 | 결과 |
|---|---|
| `test_g1_g2` / `test_g3` / `test_g4` / `verify_roundtrip` | **46 / 57 / 27 / 50 PASS · FAIL 0** |
| 골격 기대값 5종 | **46 · 45 · 22 · 16 · 8 — 불변** |
| `run.py all` 2회 | 동일 그래프 |

새로 건 잠금 5항 — 계약 위반에 죽지 않음 · `missing_field` 표면화 · `@` 끝점 대칭 ·
재인입 큐 무증식 · `auto_node` 보존.

---

## G5 — n5 I축 도구 4연산 + 전역 재평가 이설 · 2026-08-19 · **완료**

기준점 `92b5b06`(20회차 후속). 선행 판정 3건(R3 종결)을 반입해 구현하고, 이월 판정
1건(큐 표면화 지연)을 근본 해소한 뒤 본편을 썼다. **계기판 최소는 하지 않았다 — G6 몫.**

### 0. 선행 판정 반입 — R3 종결 3건

| 판정 | 구현 지점 | 실물 |
|---|---|---|
| ⑴ 병합 생존자 **3단** | `core/ops.py::_survivor` | ①override → ②status 등급(`seed` 3 > `registered` 2 > `auto` 1) → ③정렬상 앞선 id. **seed끼리 병합은 쓰기 거부** |
| ⑵ 분리 UI = CLI + 배분표 | `core/ops.py::split` · `cli/ops.py` | 배분표(JSON) 없이는 거부 · **잔여가 있으면 실행 거부 + 잔여 목록 출력** |
| ⑶ obsolete 기본 비노출·전이 | `core/query.py::transit` · `cli/query.py::answer` | `replaced_by` 추적 후 `(대체됨: 구 → 신)` 표기 · 대체 없는 폐기는 **직접 지명 시에만** "폐기됨" 명시 |

문안 3종은 전부 **층 config `fact_templates`가 소유**한다(`node:replaced`·`node:obsolete`).
core에 문자열을 넣지 않았다 — J3 유지.

> **⑵의 잔여 계수에서 한 번 틀렸다.** 새 노드의 `canonical`로 승격된 표기(원본의 옛 alias)를
> 잔여로 세어 정상 배분표를 거부했다. **배분된 것이지 잃은 것이 아니다** — 이름이 됐을 뿐이다.
> 계수에서 대상의 `canonical`과 `aliases`를 함께 뺀다.

### 1. 이월 판정 — 전역 재평가를 빌드 말미로 이설

mirrors 재평가(drop + 재적재)를 문서 빌드에서 **빌드 말미 패스**(`pipeline.finalize`)로
옮겼다. 짝 키를 노드에 적는 자동 규칙은 층 빌드에 그대로 뒀다 — **재평가·큐 갱신만** 옮겼다.
말미 패스가 다는 큐 항목의 `doc_id`는 `None`이다(한 문서의 조건이 아니라 그래프의 상태).

| 깨끗한 상태 `run.py all` | 1회 | 2회 |
|---|---|---|
| 전체 큐 | **62** | **62** |
| `auto_node` | **51** | **51** |
| `mirror_asymmetry` | 4 | 4 |
| graph.json 해시 | — | **동일** |

**완료판정 충족** — 1회 만에 62 · 2회째 불변 · auto_node 51 보존 · 그래프 동일.
(직전 구간의 잔여 1건 "1회 61 ≠ 2회 62"가 여기서 닫혔다.)

### 2. 본편 — n5 I축 4연산

새 파일 3개, 기존 core는 읽기 측만 늘었다.

| 파일 | 줄 | 몫 |
|---|---|---|
| `core/ops.py` | 450 | 4연산 · 파급 미리보기 · 순환 방어 · `ops_log` |
| `cli/ops.py` | 96 | 진입점 — `--actor` 필수 · 미리보기 후 `--yes` 관문 |
| `tests/test_g5.py` | 240 | S4 I-1~I-6 + 단위 5 |

- **I1 개명** — id 불변, 옛 canonical은 alias로 자동 강등, **스코프 자식 canonical 연쇄**
  (`스태킹::적층 정렬도` → 개명된 부모를 따라간다).
- **I2 병합** — canonical 후보를 **빈도·출처 등급과 함께 제시**하고 확정은 사람이 한다.
  provenance 전량 이관 · 흡수 표기는 alias로 잔존 · 흡수 id는 `merged_into` 툼스톤 ·
  사전 리다이렉트로 옛 표기 조회가 생존자를 가리킨다.
- **I3 분리** — 배분표대로 재구성, 원본은 툼스톤.
- **I4 폐기** — 삭제가 아니다. status만 바뀌고 `replaced_by`·사유·시점을 남긴다.
- **순환 방어 2겹** — 쓰기 시점 거부(체인 위의 id·`replaced_by` 순환) + 읽기 시점
  방문집합·깊이 제한(`MAX_CHAIN = 16`). 읽기는 **조용히 멈추지 않고** 표기한다.
- **`data/ops_log.json`** — 큐가 아니라 로그다. 5요소(연산·행위자·시점·대상·사유).
  **큐 kind는 늘지 않았다**(닫힌 20종 유지).

**단위 5(엣지 삭제 enforcement)를 여기서 흡수했다** — 사람의 삭제는 툼스톤으로 남고
재인입이 되살리지 못한다.

### 3. 완료판정 — S4 전항 + 회귀

| 항목 | 결과 |
|---|---|
| S4 I-1 병합 | **8/8 PASS** |
| S4 I-2 개명 | **5/5 PASS** |
| S4 I-3 분리 | **4/4 PASS** |
| S4 I-4 폐기 | **5/5 PASS** |
| S4 I-5 순환 방어 | **5/5 PASS** |
| S4 I-6 연산 로그 | **5/5 PASS** |
| 단위 5 + 재인입 왕복 | **4/4 PASS** |
| **`tests/test_g5.py` 합계** | **36 PASS · FAIL 0** |

| 회귀 | 결과 |
|---|---|
| `test_g1_g2` / `test_g3` / `test_g4` / `verify_roundtrip` | **46 / 57 / 27 / 50 PASS · FAIL 0** |
| 골격 기대값 5종 | **46 · 45 · 22 · 16 · 8 — 불변** |
| 재인입 회귀(3.5′) | 노드 중복 0 · provenance 복원 · 큐 무증식 · 질의 판정 동일 |

### 보고 — 새로 검출된 결함 1건 (수정·잠금 완료)

**툼스톤이 산 노드를 가린다.** 툼스톤은 옛 이름 표시를 위해 `canonical`을 그대로 지니는데,
canonical로 노드를 찾는 코드가 그것을 거르지 않았다. 실측 3건이 **한 원인**에서 나왔다 —
병합 툼스톤을 폐기 처리해 한 노드가 `merged_into`와 `obsolete`를 **동시에** 갖는 모순
상태가 됐고, 그 canonical이 살아 있는 동명 노드와 중복으로 잡혔다.

생존 판정(`is_live`)을 개명 중복검사·병합·분리·폐기·스코프 자식 수집에 일괄로 넣었고,
S4 I-5에 회귀로 잠갔다. **차단 아님 — 이 구간에서 닫혔다.**

### 다음

국면 1 잔여: **G6**(4′ 플랫폼 + 계기판 최소 이관) · **P트랙**(P1 n7 → P2 n8 → P3 n6).

---

## G6 — 4′ 플랫폼 연동 + 계기판 8종 · n9 지문 스캔 · 2026-08-19 · **완료**

기준점 = main 최신(G5 머지분 포함). **A트랙(G0~G6)이 이것으로 닫혔다** — 국면 1 잔여는
P트랙(P1 n7 → P2 n8 → P3 n6)뿐이다.

### 0. 착수 전 보고 — 전제 공백 1건 (가결정으로 채우고 전진)

**S11이 요구하는 cp 어댑터가 레포에 없었다.** 어댑터 실물은 ipqc·toc_report 2종
(fixtures — LLM 실산출 스냅샷)뿐이고 doc_type 등록부는 0건이다(P1·P2·P3 미착수,
A-4는 "파일 배치까지"). "확정 후 정상 파싱"이 성립하려면 cp의 expects·extract 실물이
필요하다. **참조 cp 어댑터를 `mock/adapters/cp.py`로 신설**했다 — fixtures 밖이라
반입 스냅샷은 그대로다. extract는 verify_roundtrip 참조 구현과 같은 결과를 낸다
(실측 CP01 **30** · CP04 **12** record — 대장 수치 그대로). P2 참조 어댑터·P3 등록부가
서면 그쪽이 정본이다.

### 1. 본편 A — 4′ 플랫폼 연동 (`cli/platform.py` 신설)

플랫폼↔파이프라인 결합은 **subprocess 파일 계약뿐**이다(명세 §16.1). build·query는
subprocess로 부르고, 표시는 data/의 JSON(진실)을 읽는다.

| 명령 | 노출 | 실측 |
|---|---|---|
| `build` / `query` | subprocess 호출 (기존 단위 4) | 양쪽 returncode 0 · 응답 정상 |
| `graph` | 2층 + cross-layer 표시 (기존 단위 4) | process 66 · quality 35 · **걸침 엣지 9건** 문장화 |
| `queue` | 큐 열람 — **닫힌 20종 전부**(0건 kind 포함) | 62건 · 목록 밖 kind 0 · G3~G5 발화 7종이 화면에 뜸 |
| `extract` | 추출 상태 (파일 존재 = 추출 완료 — P-1) | prose 4건 완료 · table 2건 "경로 아님" |
| `registry` | 층 등록부 조회 | builtin 1(process) + registered 1(quality) |
| `ops` | **ops_log 열람 + 툼스톤 계수** [허브 추가 지시 — 등재 후 구현] | 5요소 표시 · 층별 merged_into/obsolete 계수 |
| `gauges` | 계기판 8종 | 아래 표 |

`run.py`는 `platform`·`scan`을 위임으로 추가했고, `gauges`는 7·8 2종 출력에서
**8종 전체 위임**으로 올렸다. core 수정 0 — `git diff core/` 빈 것 유지(J3).

### 2. 계기판 8종 (CH5 5.5) — 실측값

국면 1 데이터 기준: recall류는 mock 스모크 12문항, 나머지는 mock 인입 실측.
**별도 호출로만 계산**하고(8번이 자기를 오염시키지 않게), 측정 중 운영 로그 적재를
끈다. **실행 전후 data/ 해시 동일** — 관측이지 쓰기가 아니다.

| # | 지표 | 실측 | 비고 |
|---|---|---|---|
| 1 | 링킹 recall | **1.0** (10/10) | 링킹 기대 문항 = expected_path ≠ general |
| 2 | plateau | 1.0 → 1.0 → 0.111 → 1.0 → **0.0 → 0.0** | 문서별 신규 개체율 — 말미 수렴이 보인다 |
| 3 | 판정 보류율 | **0.667** (큐 62 ÷ 조각 93) | 분모 = 그래프·청크에 발자국 남긴 조각 |
| 4 | 청크 잘림률 | **0.25** (3/12문항) | 상한 8 초과 잘림 |
| 5 | 링킹 미스율 | **0.083** (1/12) | 무근거 문항(Q12)이 정확히 잡힘 |
| 6 | 허브 차수 | process: 탭용접(17)·노칭(11)·스태킹(9) / quality: 최대 4 | J9 폭증 조기 관측 |
| 7 | graph 저장 크기 | process 0.065MB · quality 0.022MB | 알람선 200MB — 여유 |
| 8 | build 소요 | 0.003s · 0.001s | 알람선 30초 — 여유 |

### 3. 본편 B — n9 지문 스캔 (`cli/scan.py` 신설) · S11

헤더 지문 ↔ 어댑터 `expects.header_labels` **일괄 대조**(preflight와 같은 연산 —
결정적, LLM 아님). 후보 판정은 완전 일치(누락 0·잉여 0), 부분 일치는 내역만 제시.
**유일 일치여도 자동 라우팅하지 않는다**(P7) — 확정은 `--confirm <doc_type>` 사람 입력.

| S11 판정 | 결과 |
|---|---|
| CP04_unlabeled 투입 → 후보 제안 | **후보 ['cp'] 유일** · 일치 내역 10/10 · ipqc 5/16 불일치 내역 함께 제시 · prose(toc)는 대상 아님 표기 |
| **확정 입력 전 파싱 미실행** | extract 호출 0회 어서션 PASS (유일 일치에도 자동 라우팅 없음) |
| 확정 후 정상 파싱 | **10행 → 12 record** — 병합 전개(공정구분)·상동 해소(설비 〃)·복수값 분리 2건 실증 |
| 부정 케이스 | CP02_drift → 후보 없음, **표류 1열 정확 검출**(누락 [관리항목] · 잉여 [관리 항목명]) · 불일치 확정은 거부 |
| 무오염 | 스캔·확정 전후 data/ 해시 동일 (그래프·큐 쓰기 0) |

### 4. 완료판정 — `tests/test_g6.py` **30 PASS · FAIL 0**

기존 단위 4 판정 5항 + 신규 노출 7항 + 계기판 8항 + S11 10항.

### 5. 회귀·불변

| 항목 | 결과 |
|---|---|
| `test_g1_g2` / `test_g3` / `test_g4` / `test_g5` / `verify_roundtrip` | **46 / 57 / 27 / 36 / 50 PASS · FAIL 0** |
| 깨끗한 상태 `run.py all` 1·2회 | **큐 62 · 62 · auto_node 51 보존 · 그래프 해시 동일** |
| 골격 기대값 5종 (깨끗한 부트스트랩) | **46 · 45 · 22 · 16 · 8 — 불변** |
| 새 큐 kind | **0** (닫힌 20종 유지) |
| core/ 수정 | **0줄** — 신설은 cli 2·mock 어댑터 1·테스트 1, run.py는 위임 배선만 |

### 하지 않은 것 (지시대로)

검수 뷰 렌더 S12(P3+G3) · BM-25 대조군 채점(국면 2 골든셋 장치 — 배선 안 함) ·
자동 라우팅 승격(P7 — 제안까지) · `core/extract.py` `_PATTERNS`(허브 지시 전 수정 금지) ·
템플릿·fixture 불변 · prose 구조 지도(P1).

### 다음

**A트랙 G0~G6 전 구간 완료.** 국면 1 잔여 = **P트랙**(P1 n7 → P2 n8 → P3 n6).
착수 직전 준비 1건: 레포 `docs/증분0_구현.md` 사본을 프로젝트 현행판으로 교체.

---

## G6.5 — 계약 미배선 24건 수리 · 2026-08-19 · **완료**

울트라 점검 확정 24건. **전건이 기존 계약 문면의 미배선**이고 신규 규범은 0이다.
수리는 세 종류뿐 — 빠진 배선 잇기 / 자체 구현을 기존 장치로 교체 / 미완 공사 마감.
각 수리 전에 봉인 파일의 repro를 돌려 결함을 재현하고, 수리 후 같은 입력으로 확인했다.

### 묶음별 결과

| 묶음 | 항목 | core 증감 | repro 전 → 후 |
|---|---|---|---|
| ① 재인입 계약 | A1·A2·A3·A4·C4 | ingest +89 · pipeline +24/−2 | 10항 FAIL → **10 PASS** |
| ② 인입 검증 | B1~B6 | pipeline +69/−7 · build +10 | 10항 FAIL → **10 PASS** |
| ③ 병합·값 | C1·C2·C3·C5·C6 | ops +47/−8 · build +5/−3 · matcher +3 | 8항 FAIL → **8 PASS** |
| ④ 걸침층 | D1·D2·D3 | build +28/−2 · pipeline +23/−12 · gate +13/−5 | 5항 FAIL → **5 PASS** |
| ⑤ 소수리 | E1~E5 | pipeline +25/−8 · ops +11/−4 · extract +10/−1 | 5항 FAIL → **5 PASS** |
| **합계** | **24건** | **+359 / −53** | **38항 전건 PASS** |

순증이 큰 곳은 ①(`withdraw` — 없던 절을 새로 씀)과 ②(검증 ③단 자체가 없었다)다.
③·④는 자체 구현을 기존 장치로 갈아끼운 자리라 로직이 줄었다 — 병합의 엣지 치환 4줄이
`add_edge` 1줄로, attribute `setdefault` 1줄이 인입 규칙 재사용으로, 비맥락형 분기가
맥락형에 합류로 사라졌다.

### 계약이 실제로 돌기 시작한 지점

- **회수 3분류** — 12행 → 6행 개정판 재인입이 청크 6건만 남기고, 근거 0 판정은
  빌드 말미로 옮겼다(회수 직후에 울리면 거짓 105건이 쌓인다 — 실측).
- **닫힌 20종의 첫 발화** — `evidence_lost`·`unknown_field`·`missing_field`·
  `invalid_category`·`parse_failure`가 등재만 돼 있고 생산자가 0곳이었다.
  반대로 `invalid_role`은 21번째 kind로 새고 있어 순삭했다.
- **걸침 병합** — 스키마가 선언한 `target_layer`를 코드가 읽지 않아 관리항목 11종이
  품질층에 복제돼 있었다. 이제 CP↔PFMEA가 한 노드를 공유한다(품질층 Property 0).
- **자기참조 소멸** — 질의가 "용접 강도 부족는 용접 강도 부족의 원인이 될 수 있다"를
  사실로 출력하던 것이 병합 이설의 부산물이었다.
- **순서 무관** — 추출이 그래프 상태를 어휘로 읽어 인입 순서에 따라 노드가 소실했다
  (정순 66 · 역순 65). 골격 스냅샷으로 한정해 **76/76 · 24/24 · 소실 0**.

### 새 기준선 (큐 총수는 바뀌는 것이 정상 — 지시문 §6)

| | 수리 전 | 수리 후 |
|---|---|---|
| 깨끗한 `run.py all` 1회 큐 | 62 | **69** |
| 2회 | 62 | **69** (불변) |
| `auto_node` | 51 | **50** |
| `mirror_asymmetry` | 4 | **11** |
| `unknown_field` | 0 | **1** |
| 공정층 / 품질층 노드 | 66 / 35 | **76 / 24** |
| graph.json 해시 1·2회 | 동일 | **동일** |

`mirror_asymmetry` 4 → 11은 걸침 수리로 관리항목이 공정층에 들어와 mirrors 재평가
범위에 편입된 결과이고, **전건 참 양성**이다(오검출 0 회귀가 잠근다).
`unknown_field` 1건은 PFMEA01의 `비고`("재발 2건") — mock이 그 재료로 심어 둔 열이다.

### 회귀

| 스크립트 | 결과 |
|---|---|
| `test_g6_5` (신설) | **38 PASS · FAIL 0** |
| `test_g1_g2` / `test_g3` / `test_g4` / `test_g5` / `test_g6` / `verify_roundtrip` | **46 / 57 / 27 / 36 / 30 / 50 PASS · FAIL 0** |
| 새 큐 kind | **0** (닫힌 20종 밖 0 · 오히려 1종 제거) |
| core 층 어휘 신규 | **0** (`_PATTERNS` 한시 예외 3호는 손대지 않았다) |
| 계기판·플랫폼 | 무오염 유지 (실행 전후 `data/` 해시 동일) |

기존 회귀 중 **두 어서션의 문면을 실물에 맞췄다**(둘 다 test_g3, 계수는 57 불변):
A11-9 ① 항은 프록시 문자열 대신 자기 문면("이중 표기 0")을 검사하고, 증인을 정형
유래 노드로 바꿨다 — 구 증인은 prose 유래라 E2 수리로 corpus에서 빠졌다.

### 보고 — 신규 검출 1건 (BLOCKERS 판정필요-6 · 멈춤 아님)

**같은 대상이 좌표 해상도에 따라 두 canonical로 갈린다.** 걸침 수리가 드러냈다 —
개념 좌표 + 축값 확정이면 F1 결합(`탭용접::cathode 용접 가압력`), 인스턴스 좌표면
A11-9 ① 생략(`탭용접::cathode::용접 가압력`). 두 규칙이 각각 문면대로 돌았는데 이름이
갈렸으므로 **결함은 코드가 아니라 규칙의 접점**에 있다. 선택지 3개와 귀결을 첨부했고,
새 규범을 세우지 않는다는 원칙에 따라 (나)로 전진했다.

### 지시문 전제 정정 1건

§5 E2의 "골격 닫힌 목록 스냅샷(D-11 — **이미 존재**)"은 사실과 다르다.
`data/skeleton_closed_list.json`은 레포에 없다(D-11은 가결정이고 파일은 미생성).
골격 닫힌 목록 = 골격 전 노드이므로(A11-6 · D-45) 파일을 새로 만들지 않고
그래프의 `status="seed"` 노드에서 읽어 같은 결과를 얻었다.

### 다음

국면 1 잔여 = **P트랙**(P1 n7 → P2 n8 → P3 n6). A트랙은 G6.5로 정합을 회복했다.

---

## 21회차 반입 + P1 — n7 파서 공용 코어 · 2026-08-19 · **완료**

착수 전 전제 확인에서 **21회차 반입이 zip 상태로만 올라와 있고 풀리지 않은 것**을
발견했다(증분0 v1.4 · 파서_명세 v0.5.1 · 「미반영분」 절 활성). 반입 README 지시대로
기계 반입을 먼저 하고 P1에 들어갔다 — 정본 9종 교체 · CLAUDE.md delta 소진 ·
D-2/11/12 확정 표기 · 봉인 파일 이동. 문서 반입은 코드에 무영향(회귀 7종 불변).

### §0 하강 부착 — 판정필요-6 종결 (단독 커밋)

좌표가 **개념**인데 record의 축값이 확정이면 **동일 극성 인스턴스가 존재할 때** 그리로
하강해 부착한다. 하강 후에는 기존 ①(표면형 결합 생략)이 적용되고, 인스턴스가 없으면
하강하지 않는다(임의 생성 금지 — 골격은 Tier1).

구현은 `Builder.descend_anchor` **1곳**이고 A11-5 순서 파생의 하강을 그대로 재사용한다
— 새 로직 0줄. 정형·비정형 양쪽 부착점에서 부른다.

| 종결 어서션 | 결과 |
|---|---|
| 실측 1쌍이 깨끗한 재빌드 후 단일 노드 | `탭용접::cathode 용접 가압력` ↔ `탭용접::cathode::용접 가압력` → **`탭용접::cathode::용접 가압력` 단일** |
| 하강 없는 경로도 유지 | `패키징::전해액 주액::cathode 주액량` — 그 세부공정에 극성 인스턴스가 없어 F1 결합 |
| 클린 1·2회 | 큐 69 · 그래프 해시 동일 · 노드 76/24 **불변**(canonical만 이동) |

**G3 기대값 갱신 1항** — 사유: CP01-C8·C9는 좌표가 개념(`노칭`)이고 극성이 확정이라
이제 `노칭::cathode`·`노칭::anode`로 하강한다. 그래서 극성 쌍의 canonical이 F1 결합형
(`노칭::cathode 노칭 정밀도`)에서 스코프형(`노칭::cathode::노칭 정밀도`)으로 이동했다.
판정 대상인 **'페어링 성립'은 그대로**다. 종결 어서션 3항 신설(57 → 60).

### 본편 — 코어 6종 + struct-map + 기본 어댑터 + 하네스

| 구성물 | 줄 | 몫 |
|---|---|---|
| `parser/reader.py` | 106 | **신작 0** — 08-01 참조 구현 채택 확정, 편입·대조 마감만 |
| `parser/preflight.py` | 70 | 지문 ↔ `header_labels` 결정적 대조 · 차이 내역 동봉 |
| `parser/struct_map.py` | 147 | **신설** — 지도 산출 → 타당성 검사 → 결정적 분할기 |
| `parser/normalizer.py` | 163 | 4기능 · 전 기능 "풀 것이 있을 때만"(멱등) |
| `parser/tagger.py` | 127 | 좌표 태깅 · `process_group` tier:main 파생 · 이미지 요약 |
| `parser/validator.py` | 88 | 3층·좌표·자기완결·payload_kind self-check |
| `parser/adapters/basic_ppt.py` | 104 | PPT 임계 조건화 4경로 |
| `parser/pipeline.py` | 104 | 코어를 순서대로 엮는 진입점 |
| `cli/parse.py` | 147 | run · head · **build**(구축 모드 3단 배선) |
| `mock/adapters/pfmea.py` | 127 | 참조 어댑터(S14 재료) |
| `tests/test_p1.py` | 281 | 완료판정 |
| core 수정 | +60/−8 | 스냅샷 생성 배선 · 하강 부착 · 어휘 참조 교체 |

### 완료판정 실측

| 항목 | 결과 |
|---|---|
| **S7** preflight 불일치 | CP02_drift → extract 미실행 · `adapter_mismatch` · 누락 `['관리항목']` 잉여 `['관리 항목명']` |
| **S8** 계약 위반 행 | CP03_bad → **문서 통째** 미인입 · `parse_failure` · 사유에 `row 15`·결측 필드 |
| **S9** 기본 어댑터(확장) | 임계 이하 **9청크** / shape 분할 **6** / 지도 분할 **9** / 훅 없으면 폴백+표시 |
| **S14** 역산 정합 | **CP01 prefix 12 · PFMEA01 prefix 13 일치** — 실물 파서 산출로 |
| **지도 경로 동치** | TOC01 9=9 · TOC02 8=8 — 청크·section·text 전부 동일 |
| **지도 실패 경로** | 레벨 비단조 결정적 검출 → 평면 폴백 + 표시, 조용한 오파싱 0 |
| **스냅샷** | `data/skeleton_closed_list.json` 46노드 · `skeleton_version` 정합 · 재빌드 재생성 |
| `tests/test_p1.py` | **46 PASS · FAIL 0** |

**PPT 확대분**: `PPT_basic.pptx` 말미 +2장 — B 경로(693자·6프레임) · C 경로(736자·1프레임).
**지도 mock**: 정상 1 · 깨진 것 1(레벨 비단조). 지위는 fixture가 아니라 mock 힌트다.

### 후속 — B1 예외 3호 해소

카드가 적어 둔 해소 경로("문형 규칙의 config 이동")를 이번에 실행했다. 규칙 표가
층 config로 가고 코드는 정규식이 구문이라는 것만 안다. **core에서 층 관계 이름이
사라졌다.** 한정 조건의 코드 게이트는 직전 구간이 이미 세웠으므로 예외 3호는 양쪽 다
닫힌다(카드 문면 삭제는 명세 트랙 몫).

### 회귀

| 스크립트 | 결과 |
|---|---|
| `test_p1`(신설) / `test_g1_g2` / `test_g3` / `test_g4` / `test_g5` / `test_g6` / `test_g6_5` / `verify_roundtrip` | **46 / 46 / 60 / 27 / 36 / 30 / 38 / 50 PASS · FAIL 0** |
| 클린 `run.py all` 1·2회 | **큐 69 · 그래프 해시 동일** — 파서는 인입 경로와 독립이다 |
| 새 큐 kind | **0** (닫힌 20종 · `parse_failure`·`adapter_mismatch`·`hierarchy_unresolved`만 씀) |

### 보고 — 지시문 전제 정정 1건 · 자체 검출 2건

**전제 정정**: `mock/raw/PPT02.pptx`는 실재하지 않는다. raw PPT는 `PPT_basic.pptx`
하나이고 PPT02는 parsed 계약 JSON(`source_path: "(mock)"`)이다. 확대 2장은 실질
대상인 `PPT_basic.pptx` 말미에 붙였다 — D-18 확대 계약(뒤에 붙임·번호 불변)은 그대로다.

**자체 검출 2건** (둘 다 수리·잠금 완료):
- **빈 행 가드가 "일부만 빈 행"을 삼켰다.** 한 칸씩 밀린 행(CP03_bad 15행)이 여백으로
  오인돼 조용히 사라졌다 — C14 위반이다. 전 열이 빈 행만 빈 행으로 조였다.
- **validator의 좌표 목록 대조가 문서를 죽였다.** `레이저노칭` 한 행 때문에 PFMEA01
  전체가 미인입됐는데, 그 행은 mock이 `orphan_anchor` 재료로 심어 둔 것이다.
  좌표 판정은 **인입 소관**이고 파서가 하면 그 큐 kind가 도달 불가능해진다 — 보고로 내렸다.

### 다음

국면 1 잔여 = **P2**(n8 킷 6종) → **P3**(n6 등록 파이프라인 · S1·S10·S12·S15).

---

## P2 — n8 어댑터 생성 킷 6종 · 2026-08-19 · **완료**

킷은 **외부 전달물**이다(사내 반입 폴더 `kit/`). ①⑥은 실물이 있었고 이번 범위는
**②③④⑤ 신설 + ①의 v0.4 + ⑥ 보강**이었다.

### 킷 6종 실물

| # | 구성물 | 파일 | 줄 |
|---|---|---|---|
| ① | 생성 프롬프트 템플릿 | `생성프롬프트_템플릿_v0.4.md` (v0.3 보존) | 225 (+16) |
| ② | 어댑터 스켈레톤 | `어댑터_스켈레톤.py` | 97 |
| ③ | 표적 출력 정의 주입 | `표적출력_정의.md` | 63 |
| ④ | 참조 어댑터 예시 | `참조어댑터/` (3쌍 + README) | 39 |
| ⑤ | 검수 뷰 렌더러 | `render_review.py` + `검수뷰_데이터스키마.json` | 226 + 126 |
| ⑥ | 실행 하네스 | `run_adapter.py` (+45/−8) | — |

### ⑥ 하네스 보강 — `use_blocks` 로더

하드코딩 `STRUCT_FIELDS`를 삭제하고 `schemas/blocks.json`을 전개해 합친다.
하드코딩 목록은 블록 파일이 소유한 선언을 코드가 베낀 것이라, 파일이 바뀌어도
하네스가 모른다. `fields` 판정은 **스키마 선언분**으로 유지했다 — 블록은 참조지
선언이 아니라 prose의 `{}` 정답을 흔들면 안 된다.

| | 봉인 로그 | 재실행 |
|---|---|---|
| ipqc | 43 PASS · 0 FAIL | **45 PASS · 0 FAIL** (사라진 판정 0 · 추가 1종) |
| toc | 43 PASS · 0 FAIL | **45 PASS · 0 FAIL** (동상) |
| ipqc role 루프 `anchor` | **0** | **66** |

`anchor=0`은 실행검증 §4.4가 "스키마는 옳고 하네스가 미완"이라 판정한 결함이었다.
좌표를 공용 블록에 위임한 스키마의 anchor 경로가 이제 드라이런된다.

### ② 스켈레톤 — 안내를 실측으로 썼다

빈칸 상태로 하네스에 넣었을 때의 거동을 동봉했다. **초판에 "필수 키 4종이 FAIL"이라
적었다가 실행해 보고 정정했다** — 그 검사는 키 존재만 보고 값은 안 보므로 PASS다.
정상 결과는 **FAIL 4건**이고 그 넷이 전부 "아직 안 채웠다"의 신호다. 이 기준선이
있어야 사람이 "안 채운 것"과 "잘못 고친 것"을 가른다.

### ④ few-shot 3종과 선택 근거

| doc_type | kind | 출처 | 왜 |
|---|---|---|---|
| `ipqc` | table | fixture(LLM 실산출) | **"이렇게 내면 통과한다"의 실증** — 공식 하네스 전항 PASS 로그가 봉인돼 있다. 16열로 복수값·상동·UNMAPPABLE 제외를 한 번에 |
| `toc_report` | prose | fixture(LLM 실산출) | **prose 필수 1종** — 분할 신호 상수·section 경로·`fields {}` 스키마의 실례 |
| `cp` | table | 참조(사람) | **가장 단순한 정석** — 앞의 둘이 복잡한 경우를 덮으므로 셋째는 기본형이 낫다. `context`를 딕셔너리로 내는 실례라 v0.4 처방 ⓐ와 짝이 맞는다 |

table 2 · prose 1로 조건 충족. **떨어진 후보는 `pfmea`** — 같은 table 정석인데 열이
더 많아(13열) "단순한 기본형" 역할은 `cp`가 낫다고 봤다. few-shot은 많을수록 좋은
것이 아니라 각각이 다른 것을 보여줘야 한다.

fixtures는 **바이트 그대로 복사**했고 원본은 손대지 않았다 — 대조 차이 0.

### ⑤ 렌더러 — 데이터와 표현의 분리

뷰 데이터 스키마를 **파일로 확정**했다(`kit/검수뷰_데이터스키마.json`). 렌더러는
JSON만 읽고 `core`·`parser`를 import하지 않으며 **아무것도 계산하지 않는다** —
채움율도 이상 신호 판정도 산출자(P3)의 몫이다. 렌더러가 한 줄이라도 계산하면
"렌더러만 교체되고 데이터 계약은 불변"이 깨진다.

| M11 규격 | 실증 |
|---|---|
| 3구획 (구판 구획 4[층 초안] 없음) | table·prose 양쪽에서 전부 렌더 |
| ① 요약 통계 | 조각 수·채움율 |
| ② 이상 신호 **전량** | 접힘보다 앞에 위치 · 실패/판정 불가/경고 3종 · "판정 불가"는 질문 형태 |
| ③ 정상 발췌 + 전량 접힘 | `<details>전량 보기 (N건)</details>` |
| 구획 3 코드 전문 접힘 | 〃 |
| 재생성 루프 | 이력 표 + **"상한은 없다"** 명시 |
| payload_kind별 렌더 | table=레코드 표 / prose=계층 트리 — **3구획 구조는 같다** |

### ① 템플릿 v0.4 — 후보 2건만

**ⓐ `context` 형태 지시**(규약 9 신설) — 임의 딕셔너리다. `"M1"`이 아니라
`{"model": "M1"}`. 어기면 인입이 그 필드를 버리고 맥락 정보가 통째로 사라진다.
**ⓑ UNMAPPABLE 회피 경고** — 억지 배정의 비용이 훨씬 크다(잘못 배정된 열은 조용히
그래프에 들어가지만 UNMAPPABLE은 화면에 뜬다). 집계·이력·비고 계열을 콕 집었다.

v0.3 파일은 보존했고 **fixture는 재생성하지 않았다** — 봉인 산출물은 v0.3 산출이다.
v0.4의 4차 블라인드 검증은 P2 범위 밖이다.

### 회귀

| 스크립트 | 결과 |
|---|---|
| `test_p2`(신설) | **45 PASS · FAIL 0** |
| `test_p1` / `test_g1_g2` / `test_g3` / `test_g4` / `test_g5` / `test_g6` / `test_g6_5` / `verify_roundtrip` | **46 / 46 / 60 / 27 / 36 / 30 / 38 / 50 PASS · FAIL 0** |
| 클린 `run.py all` 1·2회 | **큐 69 · 그래프 해시 동일** — 킷은 인입 경로 무영향 |

### 다음

국면 1 잔여 = **P3**(n6 등록 파이프라인 · S1·S10·S12·S15). 렌더러를 n6에 잇는 것도
P3다 — P2는 렌더러 + 스키마까지다.

---

## P3 — n6 구축 모드 등록 파이프라인 · 2026-08-19 · **완료**

**국면 1의 마지막 코드 단위다.** doc_type 등록의 3단(생성 → 검수 → 확정)을 코드로 세웠다.

### doc_type 등록부 — 묻는 곳은 셋, 답하는 곳은 하나

`core/registry.py`(121줄) + `data/doc_types.json`. 출처는 둘이고 조회는 하나다:

| 출처 | 정체 |
|---|---|
| **내장(builtin)** | 레포가 싣고 나온 `schemas/{doc_type}.json` — 층의 J10과 같은 결이다 |
| **등록(registered)** | n6이 확정해 등재한 것. 같은 이름이면 이쪽이 이긴다 |

세 소비자가 전부 이 모듈을 부른다 — **M2 조회**(`ingest.load_schema`) · **preflight**
(`cli/scan.py` 지문 스캔) · **플랫폼 노출**(`platform doctypes`). 구판은 셋이 각자
파일 시스템을 뒤졌고, 그래서 **n6이 등록한 doc_type을 인입이 모르는** 상태였다.

**부수 정리**: `schemas/ipqc.json`을 걷어냈다. 20회차 A-4가 "파일 배치까지"로 둔
임시분이고 fixture와 바이트 동일·코드 참조 0이었다 — 그것이 남아 있으면 ipqc가
내장으로 잡혀 S15("신규 정형 doc_type 등록")가 이름 중복에 걸린다.

### n6 3단 — `cli/register.py` (428줄)

| 단 | 하는 일 | 경계 |
|---|---|---|
| ① 생성 | 입력 패키지 **사람 4 + 시스템 5** → 초안(USE_MOCK: fixture) · 분할 자명 계열이면 기본 어댑터 제안 | 이름 중복 거부 · **존재하는 층만**(R1은 국면 2) |
| ② 검수 | 기계 관문(`kit/run_adapter.py` **호출**) → 뷰 데이터(D-79 스키마) → HTML(`kit/render_review.py` **호출**) → 재생성 루프 | 렌더러·하네스 **재작성 0** |
| ③ 확정 | 승인 1회 → 등록부 등재 + `approval.json` | 기계 관문 통과가 전제 · **승인자 없이 등재 없음** |

### 완료판정 실측

| 시나리오 | 결과 |
|---|---|
| **S1** 전 과정 | TOC01+TOC02 생성 → 검수(하네스 45 PASS) → 확정(승인 등재) → **등록된 어댑터로 운영 파싱 9조각** |
| **S10** 표본 1부 | TOC01 단독 → D-22 확장 문구가 **이상 신호로** 뜬다. 선언 edges 동봉(prose는 0건이라 table 1부로 보강 — `part_of`·`has_property`) |
| **S12** 검수 뷰·재생성 | 뷰 데이터가 D-79 스키마 준수 · HTML은 kit 렌더러 산출 · 지시 이력 1건 · **"상한은 없다"** 화면 표기 |
| **S15** 정형 등록 | ipqc 2부 · 조각 33+20 · 6지선다 안 · 구조 필드 배정표 부재 · **UNMAPPABLE 질문 형태 착지** · 정답표 대조 아래 |
| `tests/test_p3.py` | **45 PASS · FAIL 0** |

### 봉인 정답표 대조 — 16열 중 **갈림 1건**

15열 일치(구조 필드 2열은 "배정표 부재"가 정답이므로 그것도 일치). 갈린 한 열:

| 열 | 정답표 | 실산출 |
|---|---|---|
| P · 관련 표준문서 | `UNMAPPABLE` (참조 성격 → L3 `reference` role 신호) | `attribute` (attach_to=검사항목) |

**판정: 정답표가 옳다.** 그 값은 다른 문서를 가리키는 **참조**이지 검사항목의 값이
아니다 — 닫힌 5종에 `reference`가 없으니 정직한 답은 UNMAPPABLE이고 그것이 L3 신호다.
다만 `attribute` 배정이 파괴적이지는 않다(값·provenance는 보존되고 사람이 나중에
승격할 수 있다). **봉인 산출은 v0.3 산출**이고, 이 경향을 정확히 겨냥한 처방이
직전 구간의 템플릿 v0.4 ⓑ(UNMAPPABLE 회피 경고)다 — 검증은 4차 블라인드 몫이다.

정답표가 어서션으로 못박은 "**O·P 두 열에 UNMAPPABLE이 나오는가**"는 **1/2**다.

### 자체 검출 1건 — UNMAPPABLE이 검수 화면에서 증발했다

D-30이 "UNMAPPABLE 열은 스키마 `fields`에 넣지 말고 어댑터 출력에서도 제외"라고
정한 결과, **그 열은 어디에도 이름이 남지 않아 배정표에 뜨지 않았다** — 사람이
판정해야 할 것이 판정 화면에 없다. 6지선다의 여섯째 경로가 화면에서 사라지는 셈이다.

복원 경로는 어댑터의 자기 선언 둘의 차집합이다(`header_labels` ↔ `columns`).
실측: 복원 전 UNMAPPABLE 0건·이상 신호 0건 → 복원 후 `최근 불량 이력` 1건이
질문으로 착지. **fixture는 손대지 않았다.**

### 회귀

| 스크립트 | 결과 |
|---|---|
| `test_p3`(신설) | **45 PASS · FAIL 0** |
| `test_p1` / `test_p2` / `test_g1_g2` / `test_g3` / `test_g4` / `test_g5` / `test_g6` / `test_g6_5` / `verify_roundtrip` | **46 / 45 / 46 / 60 / 27 / 36 / 30 / 38 / 50 PASS · FAIL 0** |
| 클린 `run.py all` 1·2회 | **큐 69 · 그래프 해시 동일** — 등록 파이프라인은 기존 인입 산출 무영향 |

### 다음

**A트랙(G0~G6.5) · P트랙(P1·P2·P3) 전 단위 완료.** 국면 1 잔여는 합류 시나리오 정산뿐이다.

---

# 국면 2A · P-A — 문서 교체 + 런타임 경계 + core 경계 3종 + 게이트웨이 골조 (2026-08-24)

## 요약

명세를 **정제본 11종으로 단일화**하고, `docs/2A_작업목록.md`의 갭 58건 중 이 요청문이
지목한 항목을 수리했다. 커밋 5회 · 회귀 기준선 **423 → 461**(증가분 전량 신규 회귀).

| 단위 | 결과 |
|---|---|
| 2-0 문서 교체 | `docs/` 옛 챕터군 17종 + 배경 구명세 3종 제거 → `docs/spec/` 11종 · `점검_문면.py` 위반 0 통과 |
| 2-1 오염 차단 | mock 이미지 요약이 `image_summary_source`로 데이터 표시 · `prompt_version` 파일 로더 |
| 2-2 런타임 경계 7종 | `init [--fresh]` · 원자적 쓰기+flock · 지연 import · `requirements.txt` · logging · `sys.path` 0 · 3.10+ |
| 2-3 core 경계 3종 | `core/dictionary.py` · matcher 재계약 · `core/skeleton.py` + 저장 레코드 정합 |
| 2-4 게이트웨이 골조 | `core/llm.py` · `core/embeddings.py` + LLM 지점 8종 분기 |

## 전제 대조표 (§1 9행)

| # | 전제 | 결과 |
|---|---|---|
| 1 | HEAD `aac08a5` · 태그 동일 커밋 | **어긋남** — 로컬 브랜치가 `7f5112a`였고 로컬 태그는 `241ae82`를 가리켰다. `main`은 `aac08a5`, 원격 태그도 `aac08a5`가 맞다(사람이 웹 UI에서 달았다). `git fetch --tags -f`로 동기화하고 `main`에서 새 브랜치를 뗐다 |
| 2 | 옛 챕터군 17 + 구명세 3 + MANIFEST | 확인 (`docs/` 23항목) |
| 3 | 루트 md 6종 | 확인 (`구현현황.md`·`산출물_지도.md` 포함 7종. 첫 대조에서 2건이 없다고 봤으나 **내 grep이 git의 비ASCII 인용 형식에 빗나간 것**이었다) |
| 4 | `core/`에 4파일 없음 | 확인 (15파일) |
| 5 | `run.py init` 없음 | 확인 (`KeyError: 'init'`) |
| 6 | `reader.py` 최상단 openpyxl import | 확인 (31~32행) |
| 7 | `logging` 사용 0건 | 확인 |
| 8 | 회귀 423/423 | **어긋남(간헐)** — 첫 실행 422/423(`test_g6_5` 1 FAIL). 같은 스위트를 단독 3회·doctor 3회 재실행에서 전부 38/0 · 423/423. **착수 전 flake**이며 §0-4대로 판정에 넣지 않았다. 재현되지 않아 원인 미확인 |
| 9 | `requirements.txt`·`pyproject.toml` 없음 | 확인 |

## 실행으로 확인한 것 (완료판정 §3)

| # | 조건 | 실측 |
|---|---|---|
| 2 | 회귀 기준선 유지 | **461/461 PASS** (클린 단독). 423 → 461의 증가분은 **신규 회귀 38건뿐**이다 — `test_g1_g2` +12(46→58) · `test_2a_gateway` 신설 26 |
| 3 | `init --fresh` 후 2회 → 동일 그래프 | 그래프 **바이트 동일** · 큐 69항목 **집합 동일** · 거부 로그 2→2. 배열 순서만 다르다(재인입이 비상시 큐를 회수 후 재적재) — doctor가 매번 이 대조를 한다 |
| 4 | 포맷 패키지 미설치 완주 | openpyxl·pptx를 import 불가로 막고 `run.py all`+`query` 완주. doctor `--env`의 상시 항목 |
| 5 | USE_MOCK=0 미설정 → 명시적 실패 | 8지점 각각 — ①②③⑤⑥⑧ `NotConfigured` · ④⑦ `RuntimeError` · **조용한 통과 0건** |
| 6 | `show node` 100노드 전수 | 크래시 **50 → 0** |
| 7 | core 경계 3종 · 게이트웨이 2파일 실물 | `core/dictionary.py` 141 · `core/skeleton.py` 368 · `core/llm.py` 178 · `core/embeddings.py` 72 |
| 8 | `점검_문면.py` | 위반 0 통과 (문서 10종 · 부정형 문면 466개) |

## 자체 검출 — 내가 만든 결함 1건

`core/init.py`의 첫 구현이 `review/`를 클린 대상에 넣어 **승인 기록이 소실됐다.**
확정 후 `review/ipqc/approval.json` 존재 → `init --fresh` 후 0건을 실행으로 확인했다.
§7.8이 그 파일을 "사람 판단 기록이라 재생성되지 않는다"로 규정하므로 `WIPE`에서
빼고 회귀로 잠갔다. 클린 범위 자체는 명세가 정하지 않아 **판정필요-9**로 올렸다.

## 회귀가 잡은 것 3건 (수리 중)

| 회귀 | 잡은 것 |
|---|---|
| `test_g1_g2` 저장 계층 경계 | `core/init.py`가 저장 파일명을 문자로 알고 있었다(3지점) — 신설 모듈에도 경계가 그대로 걸린다 |
| 같은 회귀 | `core/dictionary.py` docstring이 같은 문자를 담았다 |
| `test_p3` 2건 | `sys.path` 제거로 `cli/*.py` **직접 실행**이 깨졌다 — 실행 규약이 `python -m cli.{진입점}`이므로 테스트를 그쪽으로 갱신 |

`core/skeleton.py` 이동 중에는 `_pad`·`SeedError`가 bootstrap에 남아 **회귀가
102/426까지 떨어졌다**. 미정의 이름을 AST로 전수 검출해 함께 옮겼다.

## 장부

- `DECISIONS.md` **D-83 ~ D-86** 4건 (프롬프트 파일 소재지 · mock 표시 필드 ·
  인프라 모듈 2종 · 판정 관문 계약)
- `BLOCKERS.md` **판정필요-7·8·9** 3건 (링킹 2·3단 조기 종료 · 확정 어댑터 소재지 ·
  클린 범위) — 셋 다 `국면2_명세공백_인계.md`와 중복 없음을 대조했다

## 미완

- **orphan 재시도의 판정 재사용** — 배선할 코드가 없다(재시도 배치 자체가 미구현).
  §5가 재부착 갈아끼움을 P-B로 두었으므로 그 자리에서 함께 배선한다.
- **추출·판정 프롬프트의 조립부** — 이번에 세운 것은 이음매이고, 정의문·부착 후보
  목록 주입은 별개 갭 항목(작업목록의 `spec-A-104`·`spec-A-95`)이다.

---

# 국면 2A · P-B — 실 연결 배선과 쓰기 경로 (2026-08-24)

## 요약

명세를 개정판(91건 개정)으로 교체하고, LLM **9지점**의 실호출 갈래와 쓰기 경로
갭을 배선했다. 커밋 6회 · 회귀 기준선 **461 → 492**(증가분 전량 신규 회귀).

| 단위 | 결과 |
|---|---|
| §0-1 문서 교체 | `docs/spec/` 11종 개정판 · 회귀스위트 3종(문면·자산·문서간) 전부 통과 |
| §2-1 자산·코드 8건 | a·c·h 수리 · b(seed_format 개명) · d 확인 · e·f는 §2-3와 한 자리 · g(prose 지문) 신설 |
| §2-2 실호출 7지점 | 배선 완료 + **⑨좌표 태깅이 9번째 지점임을 발견해 함께 배선** |
| §2-3 쓰기 경로 8건 | a~h 전량 |
| §2-4 개정 대조 | **어긋난 지점 12건 검출·수리** (아래) |

## 전제 대조표 (§1 9행)

9행 전부 확인. 6번(USE_MOCK=0 8지점 명시적 실패)은 `test_2a_gateway` 26 PASS가
실행으로 잠그고 있었고, 8번 기준선은 461/461이었다.

## 실행으로 확인한 것

| # | 조건 | 실측 |
|---|---|---|
| §3-2 | 자산·코드 8건 | `level` 삭제 · `seed_format` 개명(registry·스냅샷 반영) · Q5 통일 · `STATUS_RANK` confirmed · prose 지문(TOC 통과·CP01 거부) |
| §3-3 | mock 회귀 유지 | **492/492**(461 → 492, 증가분은 신규 회귀 31건뿐) |
| §3-4 | 실 연결 9지점 | 가짜 게이트웨이로 관통 — completions 7 · embeddings 1 · ⑦은 주입 함수. **반환 계약 9/9 일치** · 실 연결 인입 관통 · 닫힌 20종 밖 kind 0 · status/provenance 위반 0 |
| §3-5 | 좌표 태깅이 모델을 안 부름 | `llm.chat`·`require`·`_post` 감싸 계측 — **0건**. 목록 밖은 값을 고치지 않는다 |
| §3-6 | 규칙 B 전후 `orphan_attach` | mock 표본 **0 → 0**(발화 조건 없음). 실증 케이스: 미해소 attach 1건 투입 → 저해상도 부착 1엣지 + orphan_attach 1건 · 노드 증가 0 |
| §3-7 | orphan 재시도 | 대상 없는 attach → `노칭 has_property`(저해상도) → 대상 생성 문서 인입 → **재시도가 provenance를 회수하고 `롤투롤 언와인더`로 갈아끼움** · orphan_attach 0 |
| §3-8 | 점검 3종 | 문면 위반 0 · 자산 13건 판정대상 · 문서간 0건 |

## §2-4 대조 — 개정된 자리와 어긋난 지점 12건

**전부 수리했다.** 지목 항목 밖에서 나온 것에 ★.

| # | 자리 | 어긋난 것 |
|---|---|---|
| 1 | §7.2 청크 레코드 | `parsed_at` 부재 |
| 2 | §7.2 큐 항목 | `resolution` 자리 부재 → 회수가 kind 화이트리스트로 대신했다(H6가 실물에서 깨져 있었다) |
| 3★ | §7.6-4 클린 범위 | `parsed/` 누락 + `doc_types.json`을 지우고 있었다 |
| 4★ | §7.1 진입점 계약 | `run.py build` 없음 · `cli/extract.py` 없음 |
| 5★ | §7.8 단계 경계 | `parsed/{doc_id}.json` 자리 없음 |
| 6★ | §7.6-B-1 | 환경변수 **기본 명칭이 명세에 있다** — 내가 지은 `ONTO_LLM_*`을 `LLM_GATEWAY_URL`·`LLM_API_KEY`·`CHAT_MODEL`·`EMBED_MODEL`로 교체 |
| 7★ | §7.6-B-2 | **LLM 지점이 9종**이다 — ⑨좌표 태깅 누락 |
| 8★ | §4.7 생존 판정 | `is_live`가 `merged_into`만 봤다 — `obsolete`도 제외해야 한다(I4 부활 차단의 강제 지점) |
| 9★ | §2.7 리스트 값 방어 | attribute까지 방어하고 있었다 — 대상은 entity·anchor뿐 |
| 10★ | §7.1·§7.8 로그 | `link_miss.log`에 잘림이 섞여 있었다 → `chunk_truncated.log` 분리 |
| 11★ | §2.2 조각 공통 | 기본 어댑터 산출에 조각 공통 5키 중 4개가 없었다 · `STRUCTURAL`에 `doc_type` 누락 · 수기 검체 7종도 어긋나 있었다 |
| 12★ | §5.1-6 근거 정렬 | tier 안에 정렬 키가 없어 **잘림 대상이 비결정적**이었다 |

**대조해서 이미 맞았던 것**: `query_traverse` 3단 중첩 · `cross_layer_traverse`
별개 키 · `relation_patterns[].symmetric` · 삼항 패턴 단위 · `skeleton.source`
유무 두 갈래 · record_id join 범위(`use_blocks` 제외) · 레코드 `occ` 정의 ·
클린 2회 판정 단위 · 노드 `polarity`/`tier` 최상위 · alias 항목 형태 · 엣지 5키.

## 요청문과 명세가 갈린 자리 2건 — **명세를 따랐다**(§0-1)

1. **§2-3c** 요청문 "`attach_conflict` 큐로 보류" ↔ 명세 §2.4-③ "`pending_attrs`를
   `orphan_anchor`/`orphan_attach`에 싣고 **새 큐 kind를 만들지 않는다**".
   `attach_conflict`는 §4.4-7(b)의 **교차 출처 충돌** 자리라 성질이 다르다.
2. **§2-2 말미** 요청문 "좌표 태깅은 LLM 지점이 아니다" ↔ 명세 §7.6-B-2 "**9종**…
   ⑨좌표 태깅". 모순은 아니다 — 명세의 근거 문장이 그 구분을 한다: **목록에 있다는
   것과 mock에서 모델을 부른다는 것은 다른 말**이고, 요청문의 운영 지시(mock에서
   모델 금지)는 §7.1 대체 표와 정확히 일치한다. 목록에도 넣고 §3-5도 증명했다.

## 자체 검출 1건

확정 이행을 **승격 → 등재** 순서로 짰더니, 등재가 「이름 중복」으로 거부됐을 때
승격 파일만 `schemas/{doc_type}.json`에 남아 **내장 출처**가 되고 그 이름의
재등록이 영영 막혔다(실측). **등재 → 승격**으로 뒤집고, `unregister`가 자기
승격분을 함께 걷게 했다.

## 장부

- `DECISIONS.md` **D-87 ~ D-90** (ctx 여섯+캐시 · 재시도 판정 강도 · pending_attrs
  착지 · 승격 순서와 회수 범위)
- `BLOCKERS.md` **판정필요-7·8·9 종결**(개정이 전부 답했다) · **판정필요-10·11·12
  신규**(anchor 후보검색 3문면 충돌 · attach 판정 카테고리 · 규칙 B 정형 발화 조건)

## 미완

- **감사 지적 중 미수리 4건** — ①`--allow-duplicate` 보류 해제 2택(§2.7) ②비정형
  Pass 1/2 분리(§4.2) ③cross 브리지의 홈층 적용(§5.1-4 — 명세가 정하지 않은
  자리로 보인다) ④킷의 골격 목록 렌더 원천(§6.7). 감사의 적대적 검증이 아직
  전건을 훑지 못해 **내가 명세 실물로 재확인한 것만** 반영했다.
- §5가 P-C로 미룬 것(HTML 뷰어·관측 구멍·mock 격리·운영 도구 나머지)은 손대지 않았다.

---

# 국면 2A · P-C — 가시성·이식 준비, 2A 마감 (2026-08-25)

## 요약

커밋 5회 · 회귀 **492 → 520**(증가분 전량 신규 회귀 28건). **BLOCKERS 판정 필요 0건.**

| 단위 | 결과 |
|---|---|
| §0-1 문서 교체 | [개정] 6건 반영판 · 점검 3종 통과 |
| §2-0 P-B 미완 4건 | **전건 구현** — 명세가 답하고 있던 자리였다 |
| §2-1 B10·B11·B12 | 반영 (B11은 `prompts.extract` 자산 문면까지) |
| §2-2 HTML 뷰어 | 실물 · **브라우저 실행 증명** |
| §2-3 관측 구멍 4 | `show log` 4종 · `show extract` 내용 · 지도 정정 |
| §2-4 mock 격리 | `tests/fixtures/` 이동 · **기준선 불변** |
| §2-5 운영 도구 8건 | 이관·미리보기 생략·대시보드·정확도·cross mermaid·인접 제시·role 실험·자재 열 |

## 완료판정 10조건

| # | 실측 |
|---|---|
| 1 | 전제 6행 확인 · §2-0 4건 구현 |
| 2 | **520/520**(492 → 520 · 증가분은 신규 회귀뿐). **mock 격리로 기준선이 바뀌지 않았다** |
| 3 | B10·B11·B12 반영 · `prompts/extract.md` 문면 개정 |
| 4 | `export html` 실물 46KB → **Chromium으로 실제로 열었다**: JS 에러 0 · 캔버스 59,091px · 필터 5축 · category 하나를 끄자 **노드 100→54 · 엣지 150→49 · 걸침 23→12** |
| 5 | `export mermaid quality` → **exit 1** + 사유·대안 안내 |
| 6 | `show log`(4종)·`show extract`(내용) 동작 · `산출물_지도.md` 정정 |
| 7 | 픽스처를 통째로 들어낸 상태에서 **bootstrap·all·gauges·scan 전부 정상** |
| 8 | `extract_hints` 이동 전후 — **노드·엣지·청크·describes·큐 전건 동일** |
| 9 | 점검 3종 통과 |
| 10 | USE_MOCK=0 미설정 → 9지점 각각 명시적 실패 (P-B 판정 유지) |

## §2-4 mock 격리 — 요청문 분류를 하나 뒤집었다

「그냥 지운다」로 분류된 `mock/fixtures/log/`·`mock/review_views/`는 **회귀가 실제로
읽는다** — test_p2가 `harness_ipqc_공식.txt`를, 검수 뷰 대조가 `ipqc_table.json`을
연다. 지웠더니 회귀가 깨졌고, **함께 옮겼다.** 실행이 분류를 정정한 사례다.

`extract_hints` 부재의 결과 변화(`주액기`→`주액`)는 **일어나지 않았다** — 그 실측은
힌트를 **지웠을 때**의 거동이고, 여기서는 옮기고 경로를 배선했다.

## 자체 검출 3건

1. **`run.py`가 반환값을 종료 코드로 쓰지 않았다** — 실패한 명령이 exit 0으로 끝나
   플랫폼·스크립트가 "성공"으로 읽는다. `export mermaid quality`에 실패 판정을
   붙여도 여전히 0이었다.
2. **계기판 4·5의 분모가 0이면 `ZeroDivisionError`** — 픽스처를 들어내자 드러났다.
   비율을 `None`으로 바꿨다: **0.0은 "측정했더니 0"이라 측정 부재와 구분되지 않는다.**
3. **이관의 엣지 재배선이 0이었다** — 옛 부모를 이 노드의 엣지에서 찾으면 정상
   경로(설비를 통해 붙는 경우)를 놓친다. canonical 스코프 접두로 판정하게 고쳤다.

## 장부

- `DECISIONS.md` **D-91 ~ D-93** (픽스처 부재 거동 · 이관 재배선 범위 · 정확도 측정 자리)
- `BLOCKERS.md` **판정필요-10·11·12 종결** → **판정 필요 0건.** 12건 전부 명세로 닫혔다.

## 미완

- §5가 2B로 미룬 것(사내 게이트웨이 실물 · 실 골격 · 실물 문서 파일럿 · 골든셋).
- `structural_proposal` 큐의 생산자는 여전히 0이다 — §4.9의 ①LLM 감지·②제안은
  Tier1 구조 개정 경로이고, 이번 요청의 범위 밖이다.

## 부록 — 2B 감사 워크플로 결과 반영 (2026-08-25)

P-B에서 띄운 명세↔코드 대조 감사가 P-C 종료 후 완료됐다(85 에이전트 · 2시간 15분 ·
지적 79건 → **확인 37 · 반박 42**). 지적은 **P-B 시점 코드** 기준이라 현재 상태와
대조했다.

### 이미 닫혀 있던 것 15건

`parsed_at`(P-B) · `doc_types.json` 클린 예외(P-C) · 청크 정렬(P-C) · prose 2-pass
(P-C) · `--allow-duplicate`(P-C) · 구조 지도 보존(P-B) · 킷 렌더 원천(P-C) 등.
**감사가 독립적으로 같은 결론에 닿았다** — P-B·P-C의 §2-4 대조와 교차 검증된 셈이다.

### 이번에 수리한 것 11건

| 지적 | 무엇이었나 |
|---|---|
| §5.1-5 **프론티어 전파** | `recursive: false`로 도달한 노드가 프론티어에서 빠져 **그 노드에서 뻗는 다른 관계가 한 번도 적용되지 않았다.** 프론티어에 «어느 관계로 왔는지»를 함께 실어 고쳤다. 실측: 노칭 확장의 Property가 **0 → 10건** |
| §7.2 **빈 상태** | `registry.json`·`doc_registry.json`이 `EMPTY`에 없었다 |
| §7.2 **툼스톤** | 병합 툼스톤에 `target` 키가 없어 리다이렉트 포인터가 하나뿐이었다 |
| §7.6-4 **큐 `created`** | 상수 `"2026-01-05T00:00:00"`였다 — 명세가 멱등 판정에서 `created`를 빼라고 이미 못박았으므로 시각을 위조할 이유가 없다 |
| §3.1 **`match_threshold`** | 코드 상수만 있고 층 config를 읽지 않았다. `threshold(cfg)` 통로 신설 |
| §4.7-4 등급 어휘 | `_survivor` docstring에 `registered`가 남아 있었다 |
| §4.2 **버퍼** | `resolve_anchor` 성공분이 문서 버퍼에 안 실려, `attach_to`가 골격 표기를 가리킬 때 사전으로 샜다 |
| §7.4 **명시적 실패** | `SeedError` 앞에 `explicit_fail` 병기가 없었다 |
| §6.4-5 **PPT 임계** | `600`·`5`가 `register.py`에 복제돼 있었다 — 어댑터 `expects`가 소유한다 |
| §6.4-6 **UNMAPPABLE 복원** | `labels[i]`↔`i+1`번째 열이라는 **위치 가정**이 있었다. 헤더에 빈 칸이 하나만 있어도 그 뒤가 밀려 엉뚱한 열이 뜬다. 실물 헤더에서 열 문자를 다시 읽고, 못 읽으면 **그 사실을 로그로 남긴다** |
| §5.5 **로그 무력화 범위** | 계기판 측정이 `append_line` **전체**를 껐다 — `defects.log`까지 죽어 측정 중 결함이 조용히 사라졌다. 재료 로그 2종만 끈다 |

### 미수리 11건 — 보고만 한다

- **§5.1-4 브리지의 홈층 적용**(지적 4): 감사 스스로 *"§5.1-4가 정하지 않았다"*고
  적었다 — **명세 공백이지 위반이 아니다.** 2C 개정이 "브리지로 건너간 노드에는
  도착층 `query_traverse`를 적용하지 않는다"를 넣었으나, **홈층이 선언층인 경우**는
  여전히 미규정이다.
- **§4.4 좌표 미해소 시 걸침 entity 미생성**(6·19): 좌표가 미해소면 걸침 entity를
  아예 만들지 않고 표면형만 큐에 보존하라는 요구. 현재는 만들고 `_scoped=False`로
  둔다(§4.5-6이 병합에서 배제). **둘 다 명세 문장을 갖고 있어** 어느 쪽인지 판정이
  필요하다.
- 나머지 9건(§7.2 chunk_id 충돌 판정 · §5.2-1 fact_templates 기본 문안 ·
  §5.3 답변 관련성 필터 · §6.4-9 분할 자명 prose 지문 · §6.3 `doc_hash` 소유 ·
  §6.6-3 검수 뷰 산출 단일화 · §7.1 진입점 docstring 4곳 · §7.5 표본 규모 하한 ·
  §7.6-B-5 프롬프트 4종 파일화)은 **범위가 P-C 밖이거나 자산 재제작이 필요하다.**

회귀 520 → **526/526 PASS**(증가분은 신규 회귀 6건뿐).

---

## 2A P-D — 조용한 잔가지 6건 (2026-08-26)

허브가 판정한 6건을 닫았다. **전건 실행으로 판정했다** — 아래 수치는 전부 실측이다.

### ① B14 — 좌표 없는 노드를 미리 만들지 않는다 (문서 4 §4.4)

가르는 기준을 **좌표가 canonical에 들어가는가**로 잡고, 판정을 층 config의
`canonical_scope.bind_categories`에 물어본다(`_scoped_category` — 코드가 카테고리
이름을 알지 않는다, B1). 미해소 anchor 아래의 스코프 카테고리 entity는 만들지 않고
**재료를 `orphan_anchor` 큐 항목의 `dropped_entities`에 싣는다.** 재시도(`_retry_anchor`)가
좌표를 해소하면 그 재료로 노드를 **새로 세운다.**

실측 — 클린 전량 인입 후:

| | |
|---|---|
| 좌표 없는 노드 | **0건** / 전체 75건 |
| 드롭 기록 | `orphan_anchor`(`레이저노칭`) 항목에 `빔 출력`(Property·process) 1건 |
| 그때 `빔 출력` 노드 | **0건** (만들지 않았다) |
| 별칭 등재 후 `retry_orphans` | `orphan_anchor: 1` 처리 → **`노칭::빔 출력` 1건 생성**, 전체 75 → 76 |

### ② 문서 5 §5.1-4 — 홈층 특례 없음

`bridge`에서 홈층 건너뛰기를 제거했다. §5.1-4의 "홈층이 아닌 전 층의 config에서
읽고"는 **타층을 빠뜨리지 말라는 요구**이지 홈층을 빼라는 뜻이 아니다.

실측 — quality 층 Failure 20건에서 출발(quality가 `occurs_in`·`controlled_by`의 선언층):

| | 도달 노드 | 걸침 엣지 |
|---|---|---|
| 옛 동작(홈층 제외) | 0 | 0 |
| 현행(특례 없음) | **20** | **22** |

특례가 삼키던 몫이 그대로다 — `전극 치수 불량 -occurs_in-> 노칭::cathode`,
`금형 마모 -controlled_by-> 노칭::cathode::금형 클리어런스` 등. 같은 질문이 **어느
층에서 출발하느냐에 따라 다른 답**을 내고 있었다.

부수 수리: 홈층을 켜자 `facts()`가 타층 노드 id를 물고 죽었다(`NoneType`) — 층 경계
가드 2곳을 넣었다.

### ③ 문서 5 §5.3 Q5 — 답변 관련성 필터

**필터의 주체는 답변 LLM이다.** `ANSWER_SCHEMA`에 `used_facts`(필수)를 넣고 사실을
**인덱스를 붙여** 넘긴다. 되돌아온 인덱스만 남기고 `facts_before_filter`에 원 건수를
적는다 — 필터가 실제로 좁혔는지를 호출부·계기판이 셀 수 있어야 한다.

실측(실호출 갈래 스텁): 사실 10건 → LLM이 `[0,3,7]` 반환 → **10 → 3건**,
`facts_before_filter=10`. `used_facts`가 비면 **전량 보존**(조용한 소실 금지).

### ④ 문서 7 §7.2 — chunk_id 충돌 판정

구판 조건 `prev.get("doc_id") != doc_id`는 **참이 될 수 없었다**(cid가 항상
`{doc_id}:…` 꼴). 판정을 「이번 인입에서 이미 쓴 cid인가」로 바꿨다 — 재인입은 그
문서 몫을 회수한 뒤 다시 쓰는 정상 경로라 저장소의 옛 청크는 충돌이 아니다.

실측: 재인입 전량 → 충돌 결함 **0건**(정상). 같은 인입에서 24청크가 같은 id를 받도록
강제 → 충돌 결함 **23건**(첫 쓰기는 충돌이 아니다) + 앞선 청크의 위치·원문 동봉.

### ⑤ 문서 6 §6.3 — 지도 재사용 판정은 `source_hash`로

파싱 시점에는 `doc_hash`가 **아직 없다**(에이전트 소유 — C7·§2.7-①). 원본 파일
**바이트 해시**를 `source_hash`로 계산해 `struct_maps/{doc_id}.json`에 함께 싣는다.

보존 파일의 내부 형태는 명세에 없어 가결정했다(**D-94**): 문서 하나에 파일 하나,
그 안에서 **프레임 키로** 나눈다(`maps[key]`) — 어댑터의 지도 훅은 프레임마다 불린다.
이미지 요약(`image_summaries`)이 같은 파일·같은 해시를 공유한다. **새로 산출된 것이
있을 때만 쓴다.**

실측 — `parse()` 전 경로(ppt_basic · 24조각):

| | |
|---|---|
| ① 1차 | `apply` 1회 · 보존파일 생성 · `maps` 1프레임 · `source_hash` 878c41dc… |
| ② 같은 파일 재인입 | `apply` **추가 0회**(재사용) · 조각 24개 **동일** |
| ③ 파일 1바이트 변경 | `apply` **추가 1회**(무효화) · 보존 해시 59c010d4… 로 갱신 |

USE_MOCK 갈래는 보존하지 않는다 — 지도가 고정 파일이라 보존이 아무것도 더하지
않는다(§6.3이 그 사실을 적어 두었다). 보존이 필요한 것은 매 인입 새로 산출되는
실호출 갈래다.

### ⑥ 문서 7 §7.6-B-5 — 프롬프트 4종 파일화

`prompts/judge.md`·`image_summary.md`·`link.md`·`answer.md` 신설(기존 `extract.md`와
같은 형식 — 머리말 `version:`이 `prompt_version`의 정본). `core/llm.prompt(name)` /
`prompt_version(name)`으로 읽는다.

호출부 실재 확인(정의만으로는 구현이 아니다):

| 프롬프트 | 크기 | 버전 | 호출부 |
|---|---|---|---|
| judge | 716자 | j-1.0 | `core/matcher.py:197` |
| image_summary | 370자 | i-1.0 | `core/llm.py:214` |
| link | 434자 | l-1.0 | `core/query.py:80` |
| answer | 816자 | a-1.0 | `cli/query.py:198` |

### 회귀·검사

- **회귀 526/526 PASS** (클린 단독 실행 — `run.py init --fresh` 뒤 `doctor.py`). 변동 없음.
- 검사 3종(`REFINED_DIR=docs/spec`): 문면 **위반 0**(문서 10종·부정형 563개) ·
  문서 간 **0건 통과**(조항 242·수치 주장 29) · 자산 대조 **13건 판정 대상**
  (보존분 `자산대조_결과.json`과 동수 — 문서 정합은 P-D 범위 밖).

### 명세 개정 필요 (고치지 않고 보고한다)

1. **문서 6 §6.3** — *"doc_hash가 같으면 보존분을 그대로 재사용하고 … doc_hash가
   바뀌면 무효화"*의 `doc_hash` 2회를 **`source_hash`**로. 파싱 시점에 `doc_hash`는
   존재하지 않는다(C7 — 에이전트가 인입 진입에서 계산). 허브 판정을 코드에 반영했고
   **문면만 남았다.**
2. **문서 6 §6.3 (부수)** — 보존 파일의 **내부 형태**가 미규정이다(D-94). 한 문서가
   여러 프레임을 갖는데 파일은 문서당 하나다.

---

## doctor.py — 실패 원인을 화면이 말하게 한다 (2026-08-26)

**사고**: 사내 PC에서 ②가 8건 [필요]로 떴는데 **화면이 원인을 말하지 않았다.** 사내망은
출력을 밖으로 가져올 수 없어 진단이 그 자리에서 끝나야 하는데, 코드가 원인을 숨겼다.

### 1-1. 실패한 스위트의 진짜 원인 (`doctor.py::_why`, 새 함수)

구판이 숨긴 두 자리:

```python
if r.returncode and not p:                                  # ← p>0이면 아예 안 낸다
    print(f"{(r.stderr or '').strip().splitlines()[-1:]}")  # ← 마지막 1줄뿐
```

- **`[FAIL]` 줄** 최대 5줄(넘으면 `… 외 N건`).
- **returncode ≠ 0 또는 `p != expect`면 stderr 마지막 15줄** — `p`가 0인지와 무관하게.
- stderr가 비고 `p < expect`면 **stdout 마지막 10줄**(조용한 조기 종료).
- 실패마다 **`재현: python tests/<name>.py`**.

세 갈래를 인위 실패로 실증했다(전부 원복):

| 상황 | 구판 화면 | 현행 화면 |
|---|---|---|
| import 시점 크래시(p=0) | stderr 마지막 1줄 | `SyntaxError: from __future__ imports must occur at the beginning of the file` + 재현 명령 |
| **스위트 중간 크래시(p=5)** | **아무것도 안 냄** | traceback 4줄 — `RuntimeError` 발생 파일·줄까지 |
| 조용한 조기 종료(rc=0·stderr 빔) | 아무것도 안 냄 | stdout 끝 6줄 — 어디까지 갔는지 |

### 1-2. 환경 차이를 스스로 지목한다 (`doctor.py::_env_diff`, 새 함수)

②의 합계가 실패일 때만 6행 표를 낸다 — **성공하면 화면을 늘리지 않는다.**
표준 출력 인코딩 · 파일시스템 인코딩 · Python 버전 · 줄바꿈(CRLF) · 작업 디렉터리 ·
`data/`·`extract/` 잔재. 한글 열 정렬은 `east_asian_width`로 맞춘다.

잔재 행의 재료는 `_clean()`이 돌려준다 — `core/init.fresh()`가 `ignore_errors=True`로
지우므로 **권한 실패가 조용하다.** 이제 returncode와 남은 경로를 함께 본다.

가장 유력한 후보(cp949)와 잔재 갈래를 각각 강제해 실측:

```
  [ 주의 ] 표준 출력 인코딩     cp949
         utf-8이 아니면 한글 출력에서 스위트가 죽는다 — PYTHONIOENCODING=utf-8 로 다시 돌려 본다
  [ 필요 ] data/·extract/ 잔재  extract
         클린이 조용히 실패했다(권한?) — 순서 의존이 살아 있어 단독 실행 판정이 성립하지 않는다
```

### 1-3. §④의 마크를 갈랐다 — `[다음]` 신설

`transition()`의 [1]·[1′]·[2]·[4]가 §①②③과 같은 `[주의]`라 **결함으로 읽혔다**(실사고).
`NEXT = " 다음 "`을 신설하고 절 머리에 한 줄을 넣었다 — *「아래는 결함이 아니라 사내에서
남은 작업이다. `[필요]`가 뜨면 그때가 고장이다.」* `_fail`은 여전히 `[필요]`만 센다.

### 1-4. `EMBED_MODEL` 안내 정정

임베딩은 **호출부 0건의 이연 항목**이라 지금 설정할 것이 아니다. 게이트웨이 3종만
안내한다 — `README.md` · `구현현황.md` · `docs/가이드/2B_작업가이드.md` ·
`docs/국면1_마감문안.md`. `core/embeddings.py`의 `require(...)`는 그대로 뒀다.

### 판정

- 정상 상태 `doctor.py` **exit 0** · 회귀 **526/526 PASS** · `tests/` 변경 0건(원복 확인).
- 문면 검사 위반 0 · 문서 간 0건 통과.
