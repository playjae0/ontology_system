---
chapter: 3B
title: 인입 데이터 규칙과 수명주기 — 운영 모드 (쓰기의 규칙)
axis: 축1 (쓰기)
question: 노드의 이름과 값은 어떤 규칙으로 정해지고, 커밋된 그래프는 어떻게 고쳐지고 개정을 따라가는가?
owns: [§5.2(극성·canonical), §6.2(context 상속), §9, §9.1, §5.5-3, 틀 §4B-A2·A4(재인입 측)·A5]
sections: [3.5, 3.6, 3.7, 3.8, 3.9]
version: v2 (2026-07-29 — 전체 정리 패스 배치 6: A5 하향 한정 명문화(3.9), 폐기 문서 라벨(D5) 참조 정정, 절 태그 부여. 틀 v2.3 기준)
status: 서술완료
tags: {3.5: 확정(R9 electrode_type role은 미결 — 이 절과 함께 확정 예정), 3.6: 확정, 3.7: 확정(I축 잔여는 미결 R3), 3.8: 확정, 3.9: 확정}
---
 
# CH3B · 인입 데이터 규칙과 수명주기 — 운영 모드
 
> CH3 분할의 후편. **CH3A가 "무엇이 어떤 순서로 일어나는가"라면, CH3B는 "그때 이름·값이 어떤 규칙을 따르고, 커밋된 것이 어떻게 살아가는가"다.** 절 번호(3.x)는 분할 전과 동일.
> 서술 규격: 00_챕터_대장 §3. 용어는 00_용어_대장 등재어만 무설명 사용.
 
---
 
## 3.5 이름 규칙 — 극성과 canonical 스코프
 
### 무엇인가 (구조)
 
"같은 것은 한 노드로, 다른 것은 섞이지 않게"(P4·A5)를 이름 규칙으로 지키는 장치들. 노드의 대표 이름(canonical)은 표면형 그대로가 아니라 규칙으로 조립된다.
 
### 규약
 
1. **극성 결합은 조건 3개를 전부 만족할 때만**: ①카테고리가 Unit·Property ②그 행의 극성이 cathode/anode로 확정 ③그 문서가 속한 층의 config가 polarity를 선언. 하나라도 빠지면 극성 무관 노드다. Failure는 결합하지 않는다. both/무표기는 극성 무관이며, 나중에 값 충돌(spec_conflict)이 나면 그것이 극성 분리가 필요하다는 신호다.
2. **공정 흐름은 단일 스트림이다** — 공정 트리는 극성으로 갈라지지 않는다(잎사귀 수준의 극성 공정만 seed에 존재: cathode/anode 탭용접). 극성은 주로 설비·인자 레벨의 문제다.
3. **mirrors는 표시만 한다**: 같은 부모 아래 (극성 제거 canonical 동일 + 극성 반대)인 노드 쌍에 자동으로 mirrors 엣지를 건다. 값을 공유하지 않고, 한쪽에만 있는 자식이 있으면 mirror_asymmetry 큐로 노출한다(한쪽 문서만 갱신되는 사고의 안전망). 매 빌드마다 재평가(self-heal)하고, 큐는 (극성 제거 canonical, 부모) 쌍 키로 중복 제거하며, 대칭이 회복되면 큐에서 제거한다. LLM 불요.
4. **Property의 canonical은 `{세부공정}::{표면형}`이다** — 부모는 세부공정이지 설비가 아니다. 이유: 관리항목은 세부공정 단위로 관리되고, CP와 PFMEA가 같은 키로 병합되려면 좌표 기준이 같아야 한다. 극성이 결합되면 `노칭::cathode 노칭 정밀도` 형태(극성은 표면형 쪽에 붙는다).
5. **부모 미해소 노드는 스코프 노드와 병합 금지** — 좌표를 모른 채 생성된 Property를 스코프 붙은 노드와 유사도로 합치면 오병합이다. 후보에서 제외한다.
6. **이중 접두 방어**: 표면형이 이미 극성으로 시작하면("cathode 노칭 프레스"가 표면형으로 들어옴) 다시 결합하지 않는다.
7. **anchor의 극성 모호**: 극성 제거 표면형이 양 극성 골격 노드를 함께 가리키면("탭용접") 임의 선택하지 않고 orphan_anchor 큐(후보 동반). 쓰기는 좁게, 읽기(질의 링킹)는 양쪽.
### 경계와 오해
 
- **극성 결합은 표기 편의가 아니라 노드 정체성 규칙이다.** cathode 노칭 프레스와 anode 노칭 프레스는 다른 실물이고 다른 값(규격)을 갖는다 — 하나로 합치면 값이 충돌하고, 표면형("노칭 프레스")은 두 노드가 alias로 공유한다.
- **파서는 극성을 이름에 붙이지 않는다**(CH2.2 계약 ③). 결합은 조건을 확인한 에이전트만 한다 — 파서가 붙이면 이중 접두가 생긴다.
- **canonical_scope의 대상 변경은 L1(스키마 한 줄)이지만 지뢰다.** 스코프 기준을 세부공정에서 설비로 바꾸면 CP와 PFMEA의 병합 키가 갈라진다 — 걸침 문서의 병합 규칙을 먼저 마감하지 않고 바꾸면 안 된다.
### 원천
명세 §5.2(극성 v1.5·v1.7, canonical G1·G4, 이중 접두 F11), §5.3(mirrors v1.11). 카드 F1~F5, D5.
 
---
 
## 3.6 값 규칙 — attribute와 맥락
 
### 규약
 
1. **값은 노드가 아니라 노드의 필드다.** 규격 ±0.1mm는 더 말할 것이 없는 값이므로 attribute로 저장된다(판별 테스트: CH2.4 ③).
2. **구조체는 통째로 저장·비교한다.** `spec: {min, center, max, unit, ...}`를 흩뿌리지 않는다 — 사람이 스펙을 세트로 파악하는 단위와 일치하고, 충돌이 실체대로 1건으로 집계된다.
3. **모든 값 항목에 provenance가 붙는다** — 단순형 `{value, provenance}`, 맥락형 `{context, value, provenance}`. 노드·엣지·alias와 대칭(불변식 G2).
4. **맥락형**: 값이 모델/사이트/라인마다 다르면 `[{context:{model:"M1"}, value:…}, {context:{model:"M2"}, value:…}]`로 병렬 저장한다. context는 봉투→레코드 상속(CH2.2). 맥락 축은 미리 발명하지 않는다 — 필요할 때 스키마 한 줄(L1).
5. **충돌 판정은 같은 context 그룹 안에서만**: deep-equal로 비교해 다르면 spec_conflict 큐(덮어쓰지 않음), 완전 동일이면 무시, **context가 다르면 충돌이 아니라 병렬 항목**이다. 단순형은 빈 context 그룹 하나로 취급한다.
6. **같은 문서의 개정값은 충돌이 아니라 교체다** — 재인입이 그 문서 몫을 회수한 뒤 새 값을 쓰므로(3.8), spec_conflict는 교차 출처에서만 발생한다.
7. **부착 대상은 entity뿐 아니라 anchor로 해소된 골격 노드도 된다** — severity가 FailureEffect(골격)에 붙는 것이 그 예다.
### 경계와 오해
 
- **"충돌"은 나쁜 상태가 아니라 정보다.** 두 문서가 같은 항목에 다른 값을 주장한다는 사실 자체가 품질 신호이고, 시스템은 그것을 자동 판정하지 않고 보존한다.
- **is_cc 같은 구조체 내부 플래그는 현재 저장 전용이다** — 질의 가능한 노드로의 승격은 감사 질의 수요가 확인될 때 검토(질의 이연 목록).
### 원천
명세 §6.2(context, v1.6), §15.5(severity 부착). 정의서 §3.3(v1.4~v1.6). 카드 F6, G2.
 
---
 
## 3.7 자동 커밋과 수정 큐
 
### 무엇인가 (구조)
 
승인 게이트는 없다 — 인입은 멈추지 않고 흐르며, 모든 자동 생성물에 표시(status)와 출처(provenance)를 남기고, 사람이 나중에 자기 리듬으로 고친다. 큐는 차단 대기열이 아니라 **작업 목록**이다.
 
### 규약
 
1. **모든 자동 생성물(노드·엣지·alias·값 항목)에 status와 provenance를 예외 없이 기록한다.** 이것이 사후 수정을 가능하게 하는 재료다.
2. **아무것도 조용히 버리지 않는다.** 처리 못 한 것은 전부 종류가 붙은 큐 항목이 된다 — 실패는 예외가 아니라 등급이다.
3. **큐 13종**:
| kind | 뜻 | kind | 뜻 |
|---|---|---|---|
| auto_node | 자동 생성 노드 — 검토 대상 | spec_conflict | 같은 항목·같은 맥락에 다른 값 |
| uncertain_match | 판정 불확실 — 신규로 만들고 표시 | evidence_lost | 재인입 후 근거가 0이 된 것 |
| orphan_anchor | 공정좌표가 골격에 없음 | mirror_asymmetry | 극성 쌍의 자식 구성 불일치 |
| orphan_chunk_link | 청크 속 언급 미해소 | unknown_field | 스키마에 없는 필드(파서가 앞서간 신호) |
| orphan_attach | 부착 대상 이름 미해소 | missing_field | 필수 값 부재·계약 위반 |
| attach_conflict | 문서끼리 부착 주장 충돌 | invalid_category | 카테고리 오류 |
| structural_proposal | Tier1 구조 개정 제안(3.9) | | |
 
- 게이트 도입(3.4)과 함께 direction_conflict(방향 반대 후보)와 거부 기록이 추가된다 — 신설 kind로 갈지 기존 kind 확장으로 갈지는 명세 개정(v1.19)에서 확정.
4. **수정 도구 = 인스턴스 변경 I축 4연산 (틀 v2.2 A2 — 스키마 축 L1~L4와 직교)**
| 등급 | 연산 | 핵심 규칙 | 자동화 |
|---|---|---|---|
| **I1** | 개명 | canonical 변경, **id 불변**(P4). **옛 canonical은 자동으로 alias로 강등** — 문서에 옛 이름이 계속 나오므로 사라지면 재매칭이 깨진다. 스코프 카테고리는 부모 개명 시 자식 canonical 연쇄 변경. 골격·config 대상이면 파일도 함께(3.9) | 도구 실행, 파급 자동 |
| **I2** | 병합 | **id 생존자 = 작은 id.** 기준이 하나뿐인 이유는 정확성이 아니라 **결정성**이다 — 어느 쪽을 죽여도 툼스톤이 리다이렉트하고 내용은 전부 합쳐지므로, 필요한 것은 "처리 순서·병렬과 무관하게 같은 답"이다(사람 지정 override 가능). 결과 노드 status는 둘 중 높은 쪽. **canonical 선택은 분리**(아래).<br>**정보 손실 없음**: 흡수 노드의 **provenance가 전부 생존자로 이관**되므로 어느 문서에서 온 지식인지 그대로 조회된다. canonical·alias도 각자의 provenance와 함께 alias로 이관, 엣지 이설(중복은 provenance 합집합), attribute는 context 그룹별 합침(같은 그룹 값 충돌 시 spec_conflict).<br>**흡수 id에는 `{status: merged_into, target, at}` 툼스톤만** — 내용은 생존자에 있으므로 중복 저장이 아니고, 용도는 옛 id 참조의 리다이렉트 하나다. 질의는 전이 추적 | 도구 실행, 사람 지시 |
| **I3** | 분리 | **자동 불가.** 어느 alias·provenance·엣지가 어느 쪽인지 사람이 지정해야 한다. **판정 3분기의 "불확실 → 신규 생성"이 분리를 예방하는 장치다** — 병합은 쉽고 분리는 어렵다 | 사람 지정 필수 |
| **I4** | 폐기 | **삭제하지 않는다.** `status: obsolete` + `replaced_by: <id>\|null` + 사유 + 시점. 이유: ①옛 문서·답변이 참조 ②재인입 부활 차단. 질의는 기본 제외하되 replaced_by를 전이 추적 | 사람 판정 |
 
**병합 시 canonical 선택 — 자동으로 정하지 않는다.** 병합은 사람이 지시하는 연산이므로 그 자리에서 고르게 한다. 후보는 두 노드의 canonical + 모든 alias 전부이고, 각 후보에 **출현 빈도와 출처 등급**(Tier1/confirmed 문서에서 온 표기인지)을 표시한다. **LLM이 대표 이름으로 적합한 것 하나를 추천**하고(오타·약어·비정형 표기 배제) **사람이 확정**한다. 빈도를 자동 규칙으로 쓰지 않는 이유는 복사·붙여넣기로 **오타가 반복 등장**할 수 있어 빈도가 정확성을 보장하지 않기 때문이다. 선택되지 않은 표기는 전부 alias로 남으므로 정보 손실이 없다.
 
**체인 순환 방어 (I2·I4 공통).** `merged_into`·`replaced_by` 체인에 순환이 생기면 질의 추적이 무한히 돈다. 두 겹으로 막는다 — **쓰기 시점**: 도구가 연산 전에 대상이 이미 체인상에 있는지 검사하고 순환이면 **거부**한다. **읽기 시점**: 추적에 방문 집합과 최대 깊이 제한을 두고, 한계 초과 시 **결함으로 로그**한다(조용히 멈추지 않는다).
 
**재인입은 노드를 제거하지 않는다 — 제거는 I4다.** 재인입은 그 문서 몫의 provenance를 회수하고 근거가 0이 된 노드를 `evidence_lost`로 **표시만** 한다. 노드는 그대로 남는다. 즉 재인입은 **제거 후보를 찾아주는 것**이고 제거 자체는 도구의 일이다. 그리고 **시스템이 자동으로 기존 문서를 다시 읽는 경로는 없다** — 재인입은 사람이 그 문서를 다시 넣을 때만 일어난다.
 
**evidence_lost와 obsolete는 다르다.** evidence_lost는 근거가 0이 된 **자동 감지·검토 대기**이고, obsolete는 사람의 **종결 판정**이다. 문서 개정으로 사라지는 경로는 `재인입 → provenance 0 → evidence_lost 큐 → 사람이 obsolete 또는 보존`이다 — **근거가 사라졌다는 것이 곧 지식이 틀렸다는 뜻은 아니므로 자동 폐기하지 않는다.**
 
**전 연산 공통**: 실행 전 파급 미리보기(영향 노드·엣지 수, 이름 연쇄 변경 목록) + 실행 후 연산 로그(행위자·시점·대상·사유).
5. **orphan 재시도 배치**: orphan 계열(anchor·chunk_link·attach)은 그래프가 자라면 해소될 수 있으므로 주기적으로 판정 파이프라인에 재시도한다. 확신되면 자동 해소, 불확실만 사람에게.
 
### 경계와 오해
 
- **큐를 비우는 것이 목표가 아니다.** 처리하지 않아도 시스템은 돌고, 처리하면 그래프가 좋아진다. 큐 종류별 건수는 파이프라인 건강 지표다 — 조용한 실패가 없어야 커버리지를 과대평가하지 않는다.
- **승인 게이트 폐기와 커밋 게이트(3.4)는 다른 것이다.** 전자는 "사람이 매건 승인해야 진행"의 폐기(사람 게이트 없음), 후자는 "패턴 선언과의 자동 대조"(코드 게이트)다. 이름이 비슷하지만 주체가 다르다.
### 원천
명세 §9(§9.1 제외). 구현문서 §2.3(큐 enum). 카드 G1~G6.
 
---
 
## 3.8 재인입 — 문서 개정이 상시 상태다
 
### 규약
 
1. **재인입 단위는 doc_id다.** 같은 doc_id의 개정본이 오면 그 문서 몫만 걷어내고 다시 넣는다.
2. **회수 3분류**:
   - ① **회수**: 그 문서에서 온 청크·describes·엣지/값의 provenance 항목. provenance가 0이 된 auto 노드·엣지는 삭제가 아니라 **evidence_lost 큐**로.
   - ② **보존 (회수 금지)**: 살아있는 노드의 사전 엔트리(canonical·alias). 노드가 살아 있으면 식별 수단도 살아야 재매칭된다 — 걷으면 사전 미스로 **중복 노드가 생긴다**(실측으로 확인된 결함: 재인입 두 번에 61→147 노드).
   - ③ **재평가**: 그 문서가 유발한 큐 항목 — 해소됐으면 제거, 아니면 갱신(self-heal).
3. **사람이 지운 엣지는 툼스톤(deleted_by_user)으로 영속한다** — 재인입이 되살리지 못한다. 사람의 삭제 판단은 문서에서 재생성되지 않는 지식이다.
4. **빌드는 직렬 실행이 계약이다** — id 발급·저장이 원자적이지 않으므로 동시 빌드는 id 충돌·저장 유실을 낳는다. 호출부(플랫폼)가 한 번에 하나를 보장한다.
5. **빌드 말미 sweep**: evidence_lost·auto_node 검토 목록을 정리해 큐 상태를 최신으로 유지한다.
6. **검증 기준(멱등성)**: 같은 문서를 두 번 넣으면 그래프가 같아야 한다 — 노드 수 불변, 사전 재매칭, 옛 큐 회수.
7. **멱등성의 두 축 (틀 v2.2 A4)**: 근거 축(문서·청크·레코드)은 **id를 내용에서 계산**하므로 재적재·순서·병렬·재시작에 무관하게 같은 id가 나온다. 의미 축(Unit·Property·Failure 등)은 **발급 id + 사전 보존**이 담당한다(위 ②). 두 축이 각각 지켜져야 멱등성이 성립한다 — 산식은 CH6.1.
### 경계와 오해
 
- **재인입은 예외 처리가 아니라 주 경로다.** 표준서·CP·PFMEA는 개정으로 사는 문서들이다. "한 번 넣으면 끝"을 전제한 설계는 이 도메인에서 성립하지 않는다.
- **evidence_lost는 삭제 유예다.** 근거가 사라진 지식을 자동 삭제하면 문서에 없는(그러나 참인) 지식이 소리 없이 죽는다 — 사람이 보고 지운다.
### 원천
명세 §5.5-3 (v1.10·v1.11·v1.13 실측 반영). §16.1(직렬 계약 F14). 카드 H1~H5.
 
---
 
## 3.9 Tier1 구조 개정 경로 — 골격도 진화한다
 
### 무엇인가 (구조)
 
골격·config(Tier1)의 개정은 세 단계로 일어난다:
 
```
① LLM 감지 (적극) → ② 제안 (자동 반영 없음) → ③ 사람 반영 (파일 경유)
```
 
- ① 문서를 읽어 골격과 대조 — 순서 서술로 삽입 위치까지 특정("노칭과 스태킹 사이에 검사 공정"), 계층 불일치·미선언 관계 후보·대칭 누락을 감지한다.
- ② 제안 + 근거 청크 + **파급 미리보기**(영향 노드·엣지 수, 이름 연쇄 변경 목록)를 structural_proposal 큐에 넣는다.
- ③ 사람이 승인하면 **seed·config 파일의 diff로** 반영하고 재빌드한다.
### 규약
 
1. **골격의 원천은 끝까지 사람의 파일이다.** 그래프에 직접 넣으면 다음 재빌드에 덮인다 — Tier1과 Tier2의 유일한 구조적 차이는 위험도가 아니라 원천의 위치(파일 vs 문서)이고, 그래서 검토 방식은 같되 반영 경로만 다르다.
2. **LLM이 자유롭게 감지해도 그래프가 오염되지 않는 이유는 제안까지만 하기 때문이다.** 문서 수백 건의 골격 빈틈을 사람이 다 찾을 수는 없다 — 여기가 LLM이 제 몫을 하는 지점이다.
3. 반영의 3단계 진화(현재는 1단계): 제안+diff 생성 → 플랫폼이 적용까지 → 파급 정리까지 자동.
4. **자동 생성의 방향은 하향뿐이다 (틀 v2.2 A5).** 자동 생성 노드는 기존 노드의 **하위 또는 동급으로만** 붙는다 — 기존 노드의 상위에 새 노드가 자동 삽입되는 일은 없다. 예: 문서에 "노칭은 전극가공 대공정에 속한다"고 되어 있는데 골격에 `전극가공`이 없으면, 그것을 노칭의 상위로 자동 생성하지 않고 `orphan_anchor` 큐 → `structural_proposal` 제안으로 넘긴다. 계층 상단의 변경은 골격 개정이며 사람이 파일로 수행한다. **그래프가 조용히 재편되는 경로는 없다** — 부착 변경은 재부착 3분류(CH3A 3.4 규약 7)가, 구조 변경은 이 절의 제안 경로가 받는다.
### 경계와 오해
 
- **층 등록 시 config 초안(틀 §2 구축 모드 산출물)과 이 절은 다른 시점이다.** 이 절은 운영 중 발견된 개정 제안이고, 층 등록은 구축 모드의 최초 생성이다. 둘 다 "LLM 초안·제안 + 사람 확정"이라는 같은 원리를 따른다.
### 원천
명세 §9.1 (v1.18). 틀 §4B-A5. 카드 A2(P2), I3.
 
---
 
---
 
## 이 챕터(3B)가 남긴 것
 
| 항목 | 상태 | 다음 |
|---|---|---|
| 인스턴스 변경 I축 (I1~I4) | **확정 (틀 v2.2 A2)** | 명세 §9 확장 + §6.3 직교 축으로 v1.19 반영 |
| I축 잔여 미결 | 생존자 3순위 필요성 · 분리 UI 형태 · obsolete의 질의 노출 정책 | 실측·감사 요구 확인 후 |
| 근거 축 결정적 id (N1~N4) | **확정 (틀 v2.2 A4)** | 소유는 CH6.1, 이 챕터는 재인입 멱등성의 근거로 참조 |
| is_cc 등 구조체 내 플래그의 질의 승격 | 이연 | 감사 질의 수요 확인 시 |
| 허브 노드 차수 계기판 (J9) | 신규 확정 | CH5.5 계기판 목록에 추가 |
| 플랫폼 훅 | P-3 (큐 kind별 조회·처리·재시도 API) | CH6에서 통합 |