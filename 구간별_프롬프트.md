# 구간별 착수 프롬프트 (Claude Code — VS Code · Ubuntu) — 2026-08-06 개정판

> 사용법: 위에서부터 순서대로 하나씩 붙여넣는다. **정지점은 4개다(G0 / G1+G2 / G3★ / G4)** — 각 정지점이 끝나면 Claude Code가 멈추고 보고하며, 당신은 PROGRESS.md 내용을 프로젝트 세션(claude.ai RAG)에 가져와 미러링한 뒤 다음 것을 붙여넣는다. G5·G6은 G4 확인 후 정지 없이 연속 진행하고, P트랙은 G1 이후 아무 때나 병렬로 돌린다.
> 프롬프트가 짧은 이유: 규범·정본 관계·진행 규칙·읽기 주의는 전부 `CLAUDE.md`에 있고 매 세션 자동 로드된다. 같은 사실을 프롬프트에 다시 쓰면 이중 원천이 된다.
> **공통 규칙(모든 구간)**: 문서로 결정되지 않는 지점은 추측하지 말고 BLOCKERS.md에 **판단 재료 3요소(근거 원문·선택지 2~3·귀결)**와 함께 기록하고 묻는다 / 세부 판단은 DECISIONS.md에 가결정으로 기록하고 전진한다 / 구간 종료 시 PROGRESS.md 갱신 + 커밋 1회.

---

## 정지점 1 — G0 (A0: 레포 상태 회수 + 계수 3종) 【개정판 — 이것을 쓴다】

```
이 레포의 CLAUDE.md와 docs/MANIFEST.md를 먼저 읽어라. 규범·정본 관계·진행 규칙·읽기 주의는 전부 거기에 있다.

작업 전 준비 2가지:
- git이 초기화되어 있지 않으면 `git init`을 하고, A0 종료 시 기준선 커밋 1회를 남겨라 —
  G4의 완료판정이 `git diff core/`이므로 기준선이 필요하다.
- 환경 확인 1줄: OS(Ubuntu)·Python 버전(3.10+)·venv 여부를 PROGRESS.md 머리에 기록하라.
  3.10 미만이면 멈추고 보고한다(구현문서 §9.1).

지금 할 일: 증분 0 단위 A0(G0) 하나. docs/증분0_구현.md §3의 A0 정의(v1.3)대로 수행한다.

[상태 회수]
1. 레포의 PROGRESS.md·BLOCKERS.md와 실제 코드·data/ 상태를 확인해 기존 단위 1a~5의 완료 여부를 확정한다.
   문서와 실물이 어긋나면 실물이 진실이다 — 어긋난 지점을 기록하라.
2. 증분 0 §2.2 단위 대장의 "상태" 칸을 채운 표를 PROGRESS.md에 기록한다("G0 확인"이 하나도 남지 않아야 완료).
   완료/미완료가 애매한 항목은 임의 판정하지 말고 "판정 필요 — 사유"로 따로 모아라.
3. data/에 기존 저장분이 있으면 n1 마이그레이션 대상(구 id 체계의 chunks·describes) 목록을 산출한다.

[계수 3종 — 증분 0 §3 A0, N+3 신설]
4. graph.json 직접 접근 계수: core/graph.py 밖에서 graph.json을 열거나 쓰는 지점을 전수 검색해
   파일·함수 단위 목록으로 기록한다. 이 목록이 G1 st(저장 계층 정비)의 작업 목록이 된다.
5. N#### 하드코딩 계수: 의미 축 id의 N#### 형식(발급·파싱·정렬·비교)을 전제한 코드 지점을 전수 검색해
   기록한다. 이 계수가 ULID 발효 판정의 근거다(카드 N1 — 판정은 사용자가 한다. 네가 발효를 결정하지 않는다).
6. raw mock 반입 확인: mock/raw/ 실물 10종 + verify_roundtrip.py + kit/이 레포에 있는지 확인하고,
   있으면 verify_roundtrip를 실행해 PASS 여부를 기록한다. 없거나 FAIL이면 BLOCKERS에 올리되
   나머지 A0 작업은 계속한다(반입은 S14·P트랙의 전제이지 G1의 전제가 아니다).

[초기 장부]
7. DECISIONS.md를 만들고 가결정 25건을 그대로 옮겨 적는다 —
   증분 0 §7(D-1~D-14) + docs/raw_mock_대장.md §6(D-15~D-21) + docs/판정대조_ipqc_1차.md §8(D-22~D-25).

A0가 끝나면 멈추고 보고하라. 다음 구간 착수는 내가 확인한 뒤 지시한다.
```

**당신이 볼 것**: 상태 표에 "G0 확인" 없음 · "판정 필요" 항목 목록 · **계수 3종의 실제 숫자**(직접 접근 몇 지점 / N#### 몇 지점 / verify PASS 여부) · DECISIONS.md 25건 · git 기준선 커밋.
**여기(프로젝트 세션)로 가져올 것**: PROGRESS.md 내용 전체 + 판정 필요 항목 + 계수 3종. **ULID 발효 판정을 여기서 한다.**

---

## 정지점 2 — G1 + G2 (저장 계층 정비 · 주소 체계 · 부트스트랩)

```
G0 결과를 전제로 다음 구간을 진행한다. 증분 0 §3의 해당 단위 정의(3요소·완료판정)를 그대로 따른다.

G1: st 저장 계층 정비(A0 계수 4의 직접 접근 지점을 전부 GraphStore 경유로 교체 · orjson+폴백 ·
    계기판 7(graph 저장 크기)·8(build 소요) 출력 — 틀 §4B-A8, 카드 B6)
    → n1 근거 축 id 전환 → n2 doc_hash 중복 차단
G2: n10 부트스트랩 seed·config (기존 1b 흡수)

ULID 발효는 사용자 판정 결과를 따른다: [발효 지시가 있으면 n1과 함께 적용 / 없으면 N#### 유지].

선행 작업: 증분 0 §4.1의 기존 mock 마이그레이션(source_locator 개명·adapter_version 추가·
provenance 표기·CP01 = 11행 확정 — C10 버 높이·C11 좌표 불일치)을 먼저 적용한다. n1이 이것을 전제한다.
또한 §4.2의 신규 mock 중 이 구간에 필요한 것(⑤ PPT03, ⑥ CP01B)을 작성한다.

완료판정: st(graph.py 밖 직접 접근 0 — grep으로 실증 + 계기판 7·8 출력)
+ S6(occ 충돌) + 멱등성 검사 2종(2회 인입 동일 · 조각 순서 셔플 동일) + S5(doc_hash 차단)
+ 기존 1b 판정 승계(Process 8노드·part_of 7·precedes 6) + registry에 builtin 층 1개만 존재.

구간이 끝나면 멈추고 보고하라. 각 판정의 실제 수치·통과 여부를 함께 제시할 것.
```

**당신이 볼 것**: **직접 접근 0이 grep 실증인지** · 멱등성 2종이 실제로 통과하는지(여기가 뒤 전부의 전제다) · duplicate_doc_hold 큐 1건 · 골격 노드 status가 `seed`인지 · 계기판 7·8 숫자.

---

## 정지점 3 — G3 (계약 v2 정합 · 추출 분리 · 게이트) ★가장 중요

```
G3 구간을 진행한다. 증분 0 §3의 단위 정의를 그대로 따른다.

1c′·1d′ 인입 개수 (계약 v2 정합 — 변경점 닫힌 목록 5항)
n3 추출 단계 분리 + 체크포인트 (CH3A 3.11이 계약 정본)
n4 커밋 게이트 + direction_unverifiable

필요 mock: §4.2의 ③ QPPT01, ④ PPT02. §5의 USE_MOCK 확장(추출 힌트·문형 규칙)도 이 구간에서 구현한다.
config 증분: 각 층 config에 relation_patterns 신설(D-6), quality config의 prompts.extract 훅 활성.

완료판정: 기존 1c·1d 판정 전 항목의 계약 v2 재통과 + coord_mismatch 1건
+ S2(추출) + S13(attach 청크 밖 해소) + 구축 2회 재실행 시 추출 미재호출
+ S3(게이트: K1→direction_unverifiable, K2→커밋, K3→direction_conflict, K4→후보 0)
+ 회귀: PFMEA의 causes·affects·occurs_in·controlled_by가 게이트 도입 후에도 전부 커밋.

이 구간은 설계 결정이 처음으로 코드가 되는 지점이다. 계약과 조금이라도 어긋나는 판단이 필요하면
가결정으로 넘기지 말고 BLOCKERS.md에 올리고 멈춰라. 구간이 끝나면 멈추고 보고하라.
```

**당신이 볼 것**: extract/{doc_id}.json이 3.11 스키마 그대로인지(표면형만·confidence/span 없음) · 게이트 4분기가 K1~K4에서 각각 다르게 동작하는지 · gate_rejects.json이 큐가 아니라 로그인지 · **경로 ②(정형 edges)가 무비용 통과하는지**.
**참고**: D-1(electrode_type) 최종 확정은 G3 착수 전 권장(카드 C10) — 미확정이면 가결정 D-1대로 진행.

---

## 정지점 4 — G4 (회귀와 층 등록 절차 검증) 【마지막 정지점】

```
G4 구간을 진행한다. 증분 0 §3의 단위 정의를 그대로 따른다.

2′ 질의 회귀 · 3′ 품질층 · 3.5′ 재인입 회귀

3′의 지위 변경에 주의: 품질층은 내장이 아니라 신규 층 등록 절차의 첫 검증 대상이다(J10).
R1 도구 없이 수동 config 작성 경로로 수행하고, 그 자체가 절차 검증이다.
수동 경로로 수행했음을 PROGRESS.md에 명기하라.

완료판정: 기존 단위 2·3·3.5 판정 전 항목(2홉 도달·causes 사슬·orphan·Q1~8 회귀 무오염·
재인입 중복 0) + `git diff core/`가 비어 있음(J3)을 실제 명령으로 확인해 결과를 기록.

구간이 끝나면 멈추고 보고하라.
```

**당신이 볼 것**: **`git diff core/` 출력이 실제로 비었는지** — 이 프로젝트에서 가장 중요한 구조 검증이다. 안 비었으면 어느 결정점이 config로 표현되지 않았는지가 3층 대비 관찰 데이터다(그 자체가 성과이니 기록만 하고 실패로 취급하지 말 것).
**G4 통과 후**: G5·G6은 정지 없이 연속 진행을 지시한다(아래 프롬프트). 세션 4(R1·층 등록 입력 형식) 논의도 이 시점 이후가 트리거다.

---

## 연속 구간 — G5 + G6 (정지점 아님 — G4 확인 후 한 번에)

```
G5·G6 구간을 연속으로 진행한다. 증분 0 §3의 단위 정의를 그대로 따른다. 구간 사이에 멈추지 않는다 —
단 BLOCKERS 사유가 나오면 규칙대로 멈춘다.

G5: n5 I축 도구 4연산 (기존 단위 5 흡수 — 계기판은 제외, 4′로 이관됨)
G6: 4′ 플랫폼 연동 (계기판 8종 포함 — CH5 5.5 v3) · n9 지문 스캔

필요 mock: §4.3 I축 조작 시나리오(I-1~I-6), §4.2의 ⑧ CP04_unlabeled(mock/raw 실물 사용).

완료판정: S4(I축 4연산 + 순환 거부, I-1~I-6 전부) + 기존 단위 5 판정(계기판 제외)
+ 기존 단위 4 판정 + 신규 큐 kind 20종이 큐 열람에 뜸 + 계기판 8종 출력 + S11(지문 스캔).

끝나면 종합 보고하라.
```

**당신이 볼 것**: 파급 미리보기가 실행 **전에** 나오는지 · I-5 순환이 쓰기 시점에 거부되는지 · n9가 유일 일치에도 자동 라우팅하지 않는지 · **계기판이 8종 전부**인지(7·8번 포함).

---

## 병렬 트랙 — P트랙 (파서 코어 · 킷 · 등록 파이프라인)

> A트랙과 병렬 — G1 이후 아무 때나 시작해도 된다. P3(n6)은 P1(n7) 다음이고, S1·S10·S12는 P3 완료 + G3 완료 후 실행. **S14(역산 정합)는 n7 완료판정에 포함, S15(ipqc 등록)는 n6·n8 소관이다.**

```
P트랙을 진행한다. 증분 0 §3의 P1·P2·P3 단위 정의를 그대로 따르고,
파서 내부 규격은 docs/파서_명세.md §3·§5~§9가 정본이다.

P1: n7 파서 공용 코어 6종 + 생성 하네스 (parser/ 별도 패키지, openpyxl·python-pptx 허용 — D-9)
    완료판정에 S14 포함: mock/raw/의 CP01.xlsx·PFMEA01.xlsx를 파서로 파싱한 결과가
    기존 parsed mock JSON과 일치해야 한다(역산 정합 — D-18. verify_roundtrip.py가 심판).
    단 CP01.xlsx에 C11(좌표 불일치) 행이 없으면 raw_mock_대장 기준으로 추가부터 하라(증분 0 §4.1).
P2: n8 어댑터 생성 킷 5종 (kit/ — 프롬프트 템플릿 v0.1이 반입되어 있다. 판정대조_ipqc_1차의
    개정 지시 7건을 반영한 v0.2로 개정하며 작성한다)
P3: n6 구축 모드 등록 파이프라인 (생성 → 검수 → 확정)
    S15 포함: 신규 정형 doc_type ipqc(IPQC01.xlsx — 16열 = 유효 11 + meta 3 + UNMAPPABLE 2)를
    등록 전 과정으로 통과시킨다. role 배정표 6지선다와 UNMAPPABLE 판정이 여기서만 검증된다(D-20).

필요 mock: mock/raw/ 실물(TOC01·TOC02·CP02_drift·CP03_bad·PPT_basic·IPQC01·IPQC02) + §4.2 잔여.
USE_MOCK 등록 생성 fixture(§5-2)도 함께.

완료판정: S7(preflight 불일치)·S8(계약 위반 행)·S9(기본 어댑터)·S14
+ S1(구축 모드 전 과정: 입력 패키지→생성→검수 뷰→재생성 지시 1회→승인→등록→운영 인입)
+ S10(표본 1부 경고)·S12(검수 뷰 렌더 + 재생성 이력)·S15.

세 단위 각각이 끝날 때마다 중간 보고하고, P3까지 끝나면 멈추고 종합 보고하라.
```

**당신이 볼 것**: **S14가 손으로 만든 JSON이 아니라 raw 실물 파싱으로 통과**하는지 · 검수 뷰가 코드가 아니라 결과 뷰인지(3구획·3층 표시) · 이상 신호가 전량 표시되는지 · S15에서 UNMAPPABLE 2열이 정말 UNMAPPABLE로 판정되는지 · 어댑터가 순수 함수인지(LLM 호출이 코어에 있는지).

---

## 전체 완료 판정 (증분 0 §8)

시나리오 **15종(S1~S15)** + 기존 단위 판정 전부 + `git diff core/` 빈 것(J3) + `run.py all` 2회 동일 그래프.

이 넷이 다 서면 국면 1 종료다. 다음은 국면 2(사내 이식) — 첫 작업은 코딩이 아니라 role 배정 실험(CH2 2.6).
