# 국면 2A 작업 목록 — 명세↔코드 갭 58건

> **출처**: 정제본→코드 정합 감사(2026-08-24). 복원 140건을 레포 `aac08a5`와 실물 대조한 결과 — 코드가지킴 69 · **코드가어김 58** · 코드대상아님 13.
> **판정 규율**: evidence(파일:줄) 없는 판정은 무효로 처리했고, 주석·docstring·호출부 0인 함수는 구현으로 세지 않았다.
> 등급은 명세 쪽 중요도(높음=없으면 구현이 실제로 달라진다), 규모는 코드 쪽 수리 크기다.

**분포** — 규모: 설계 3 · 모듈 22 · 함수1개 30 · 1줄 3 / 등급: 높음 26 · 보통 29 · 낮음 3


## 규모: 설계 (3건)

| 등급 | 항목 | 고쳐야 할 것 | 현재 코드 |
|---|---|---|---|
| 높음 | `impl-B-84` | 답변 단계를 두고 [그래프 사실] 채널을 질문 관련성으로 거르게 한다(현재 답변 LLM 단계 자체가 없어 채널 분리의 절반이 비어 있다) | [그래프 사실] 채널에 **관련성 필터가 전혀 없다**. cli/query.py:105-108 `res["facts"] += Q.cross_facts(crossed, graphs, configs)` / `for lay, ids in collected.items(): r |
| 높음 | `spec-12-16-94` | 실사용 7~8키를 일람에 등재하거나 config에서 제거한다 — config-only 확증의 대상 범위를 다시 정하는 결정이 선행해야 한다. | 일람 밖 키가 실재하고 코드가 그것을 읽는다. layers/process/config.json 일람 밖 7종: `layer · mirrors · match_threshold · skeleton_version · config_version · registration ·  |
| 보통 | `spec-s7-11-59` | 골격 중간 삽입(엣지 재배선) 연산을 신설하고, canonical 파생을 부모 경로 종속에서 떼어 하위 canonical 불변을 성립시킨다. | 중간 삽입(골격 엣지 재배선) 연산도 제안 경로도 없다 — core/ops.py:1-4가 소유한 연산은 I1 개명·I2 병합·I3 분리·I4 폐기 + 엣지 삭제 5종뿐이고 `structural_proposal` enqueue는 0건이다. 실재하는 유일한 골격 개정 경로 |

## 규모: 모듈 (22건)

| 등급 | 항목 | 고쳐야 할 것 | 현재 코드 |
|---|---|---|---|
| 높음 | `spec-s7-11-36` | cli/query.py의 facts 합성 뒤에 질문 관련성 필터 단계(답변 LLM 또는 명시적 훅)를 신설한다. | core/query.py:192-220 `facts()`는 수집 노드 집합 안의 **모든** 엣지(201-206행)와 모든 attr 항목(207-219행)을 fact_templates로 문장화해 그대로 반환한다 — 질문 관련성 판정이 어디에도 없다. cli/query. |
| 높음 | `spec-A-15` | `core/dictionary.py`를 신설해 register/lookup 단일 관문으로 만들고, build·ops·bootstrap·query·matcher의 직접 read/write 전부를 그 모듈 경유로 돌린다(등재 시 provenance 필수 강제를 그 자리에 둔다) | `core/dictionary.py`가 존재하지 않는다(core/ 파일 16종: bootstrap·build·extract·gate·graph·ids·ingest·matcher·naming·ops·pipeline·query·registry·store). 사전 접근이 관 |
| 높음 | `spec-s7-11-26` | orphan_anchor payload에 그 행이 만든 Failure(및 미부착 개체) node_id를 실어 재시도 손잡이로 만들고, 그 큐를 소비하는 orphan 재시도 배치를 신설한다 | ①occurs_in 미생성은 지킨다 — core/gate.py:104-109 `s=...get(src) if src else None … if not s or not d: log_reject(..., UNRESOLVED_ENDPOINT, ...); return`. 실측 |
| 높음 | `spec-A-16` | matcher를 `match(surface, candidates, category)` 계약으로 정리하고(후보 검색을 호출부/별도 함수로 분리), attach_to 해소와 I2 병합 후보를 그 함수로 배선한다 | 모듈은 있으나 **계약도 단일 진입점도 성립하지 않는다**. core/matcher.py:59 `def resolve(surface, category, layer, graph, dictionary, *, scoped=True, polarity=None)` — 이름이 ` |
| 높음 | `spec-A-16` | attach_to 해소·orphan 재시도·병합 후보 판정을 matcher.resolve로 수렴시키고, pipeline._dict_hit의 무조건 첫 히트 선택을 제거한다. | 판정 함수는 core/matcher.py:59 `resolve(surface, category, layer, graph, dictionary, *, scoped, polarity)` 하나이고 호출부는 core/build.py:277 단 1곳이다(grep 결과 test  |
| 높음 | `spec-s7-11-55` | 이관(스코프 변경 연쇄)을 4.9 정의대로 별개 연산으로 신설해 preview 대상에 넣고, bootstrap(골격 개정)에 실행 전 파급 미리보기를 붙인다. | 미리보기 함수 core/ops.py:132-152 `preview()`는 rename(:145 `_scope_children`)과 merge(:146-149)만 실질 계산한다. **이관(소속 변경) 연산 자체가 없다** — cli/ops.py:44 `p.add_argu |
| 높음 | `spec-A-103` | 자재 열을 content/meta로 배정하는 스키마 규칙과 열 등장 빈도 관찰 항목(계기판 또는 관찰 로그)을 신설한다. | 레포 전체 `자재` grep 0건 — 스키마(schemas/cp.json fields 5종: 설비·관리항목·규격·측정방법·대응계획)에도, mock/raw 실물 헤더에도 없다(CP01~CP04 헤더 = 공정구분·공정번호·공정명·극성·설비·관리항목·규격·측정방법·대응계획· |
| 높음 | `impl-B-35` | 규칙 B 폴백 부착을 table·prose 양쪽에 실장하고, orphan_anchor payload에 그 행이 만든 노드 id(Failure·Property)를 동봉하며, orphan 재시도 배치를 신설 | 세 불릿 중 첫째만 지켜진다. ①occurs_in 드롭은 성립 — core/pipeline.py:161-169가 미해소 끝점도 게이트에 넘기고 core/gate.py:104-109 `if not s or not d: log_reject(..., UNRESOLVED_EN |
| 높음 | `spec-A-104` | 추출 프롬프트 조립부 신설 — 지시문 템플릿 + 층 어휘 정의문 + 해소된 process_ref의 세부공정·하위 part_of 설비 canonical 목록을 삽입하고, 좌표 null이면 세부공정 목록만 넣는 분기 | 프롬프트 조립부가 레포에 없다. 추출 단계 전부인 core/extract.py에서 PROMPT_VERSION="e-1.0"(36)은 체크포인트에 적히는 문자열일 뿐이고(134), 후보는 core/extract.py:104-106 `_load_hints`(mock/ext |
| 높음 | `spec-A-98` | role 5종 공통 시그니처 handle(value, spec, ctx)와 ctx 묶음을 도입하고, 반환 3형태(resolved_id\|value\|None)로 edges 후처리의 '미해소' 판별을 단일화 | `handle(value, spec, ctx)` 형태의 핸들러가 존재하지 않는다(레포 전체에 `def handle` 0건). role 처리는 core/pipeline.py:113-144의 인라인 if/elif 체인이고(125 anchor · 130 entity · 14 |
| 높음 | `spec-A-95` | resolve를 (mention, candidates[{canonical, aliases, parent, scope, category}]) 입력 · {type, matched_id, confidence} 반환으로 재계약하고, 후보 조립부를 build.py에서 분리하며, config `prompts.judge`를 실제로 주입하는 통로와 US | 판정 호출은 core/matcher.py:59-61 `def resolve(surface, category, layer, graph, dictionary, *, scoped=True, polarity=None)` 하나이고, 명세의 입출력 계약과 세 곳이 다르다. ①입력 |
| 높음 | `spec-s7-11-26` | resolve_anchor의 enqueue를 즉시 적재에서 레코드 처리 말미로 미루거나 큐 항목 핸들을 돌려받아, entity 해소가 끝난 뒤 생략된 엣지의 출발 node_id(들)를 payload에 적어 넣는다. core/build.py:104-121과 core/pipeline.py:96-169(정형)·249-285(비정형) 양쪽 레코드 | orphan_anchor 큐 항목의 payload에 출발 노드가 없다. core/build.py:118-120 `store.enqueue("orphan_anchor", f"골격에 없는 좌표 — '{surface}'", self.doc_id, {"surface": sur |
| 보통 | `impl-B-101` | cli/* 8개 파일의 sys.path.insert를 제거하고 진입점 docstring·doctor 안내를 `python -m cli.{진입점}`으로 통일한다. | `__init__.py`는 core/(6줄)·cli/(빈 파일)·parser/에 있고 import도 절대경로다. 그러나 **sys.path 조작이 전 CLI에 박혀 있다** — cli/query.py:18-19, cli/ops.py:21-22, cli/platform. |
| 보통 | `spec-A-164` | 노드 canonical + 카테고리 정의문을 로드 시 1회 임베딩해 후보 검색에 쓰는 단계를 신설한다(또는 그 자리를 명시적 훅으로 봉인). | ②후보 검색에 임베딩이 없다 — core/matcher.py:83-104가 `for nid, n in graph.nodes.items()` 선형 순회로 후보를 훑고 점수는 core/matcher.py:37-51 `_similarity(a, b)`(공백 제거 후 `x = |
| 보통 | `spec-A-160` | 확장 결과를 답변으로 넘기기 전에 관련성 필터(주체 = 답변 LLM)를 둔다 — spec-s7-11-36과 같은 수리 자리. | 양방향 확장은 있다 — core/graph.py:170-176 `if d in ("out","both") … if d in ("in","both") …`, cross-layer도 core/query.py:126-133 `bridge()`가 `d = spec[e["rel |
| 보통 | `spec-s7-11-85` | 플랫폼 노출 목록에 Q7류 집계 대시보드 화면을 신설하고, 스키마 등록 워크플로우와 수정 도구 세트를 플랫폼 창구로 등재한다. | ③계기판만 요건을 채운다 — cli/platform.py:207-307 `gauges()` + 310-332 `cmd_gauges()`가 별도 명령이고(347행 라우팅, run.py:83-86 `gauges`), 파이프라인에 심기지도 않았다(core/pipeline.p |
| 보통 | `spec-A-031` | spec-A-15과 한 건으로 처리 — core/dictionary.py 신설 후 위 등재 4경로·조회 4경로를 전부 그 모듈 경유로 돌린다 | spec-A-15와 동일 규범이며 결과도 같다 — `core/dictionary.py` 부재, `register`/`lookup` 단일 관문 없음. 등재는 core/build.py:64-72 `_register()`가(여기만 provenance 필수를 지킨다), 다른  |
| 보통 | `impl-B-102` | 표준 logging(레벨 INFO)을 도입하고 MOCK 경고·큐 적재·명시적 실패 3종을 로거 경유로 남긴다 — 현재 print/예외/파일직접append로 흩어진 착지점을 그쪽으로 모은다 | **표준 로깅 모듈이 레포 전체에서 한 번도 쓰이지 않는다** — `grep -rn "logging\|getLogger\|basicConfig" --include=*.py .` 0건. 레벨 INFO 설정도 없다. 3종의 현재 착지점은 전부 로깅 밖이다: ①MOCK 경고 — |
| 보통 | `spec-A-106` | 추출 산출 계약에 attach_to 3단 우선순위(후보 목록 표기 → 본문 이름 → null)를 구현하고 후보 목록 공급을 붙이며, prompts.extract 자산에 문면을 싣는다. | attach_to를 **산출**하는 유일한 코드 경로가 우선순위를 갖지 않는다 — core/extract.py:69-101 `_mock_candidates`는 entities/relations만 만들고 :100-101 `return {"chunk_id": chunk_i |
| 보통 | `spec-12-16-70` | spec-12-16-69의 갈아끼움 기구가 선 뒤에, 그것을 확인하는 단위 판정을 붙인다 — 저해상도 부착 1건을 만든 뒤 골격을 자라게 해 재시도하고, (a) 고해상도 엣지가 서고 (b) 저해상도 엣지의 해당 provenance가 회수돼 같은 지식이 두 벌로 남지 않으며 (c) 재인입 2회에 결과가 같음을 대조한다. | orphan 재시도 자체가 없어 이 판정이 걸릴 대상이 없다. ①재시도 배치 코드 0곳 — 레포 전역에서 '재시도'·'retry'·'재부착'·'reattach'·'갈아끼' 문자열이 .py에 하나도 없다(유일 hit는 tests/verify_roundtrip.py:8의  |
| 보통 | `spec-12-16-69` | 같은 출처(같은 source_locator 계열)의 부착 해상도가 올라갔을 때를 인지하는 판정을 인입 경로에 세우고, 고해상도 엣지 커밋 + 저해상도 엣지의 해당 provenance 회수 + 근거 0이 된 엣지의 착지(core/pipeline.py:196-214 `_evidence_lost`와 연결)를 한 흐름으로 잇는다. core/bui | 보류 해제 조건은 충족됐는데 갈아끼움 기구가 아예 없다. ①2홉 도달성 검증은 존재하고 통과한다 — tests/test_g4.py:75-78 `show("2홉 도달 — part_of 하향으로 닿은 설비의 has_property 인자까지 수집", any("스태커의 관리인 |
| 보통 | `impl-B-03` | `core/skeleton.py`를 만들어 `_TreeParser`·`_plant_tree`·`_link_seed_mirrors`와 flat 골격 분기(bootstrap.py:502-508)를 옮기고, bootstrap.py는 config 로딩·적재·등록부 갱신만 남긴다. 호출부는 bootstrap.py:497-508 한 곳이라 이동 자체 | `core/skeleton.py`가 존재하지 않는다 — core/ 파일은 __init__.py·bootstrap.py·build.py·extract.py·gate.py·graph.py·ids.py·ingest.py·matcher.py·naming.py·ops.py·pi |

## 규모: 함수1개 (30건)

| 등급 | 항목 | 고쳐야 할 것 | 현재 코드 |
|---|---|---|---|
| 높음 | `spec-12-16-78` | store.write()·GraphStore.save()를 공용 원자적 쓰기(같은 디렉터리 tmp 파일 + os.replace)로 바꾸고 쓰기 구간에 파일 락(fcntl.flock)을 건다. | core/store.py:56-58 `def write(name, obj): DATA.mkdir(parents=True, exist_ok=True); path(name).write_bytes(_dumps(obj))` — 대상 파일을 직접 덮어쓴다. core/graph. |
| 높음 | `impl-B-99` | run.py에 `init [--fresh]` 진입점을 신설해 data/ 하위를 7.2 빈 상태 형태로 생성·재생성한다. | run.py:124-137 진입점 dispatch dict에 init이 없다 — bootstrap·ingest·all·query·ops·gauges·platform·scan·parse·register·show·export 12종뿐. 실행 확인: `python3 run. |
| 높음 | `spec-A-115` | pipeline.py:288-300에 attach_to null 분기를 두어 큐를 달지 않고 폴백만 하게 하고, orphan_attach payload를 (자식 node_id, norm(attach_to))로 바꾼다. | ①은 지킴 — core/pipeline.py:288-300의 attach는 `resolve_anchor`(=Tier1 골격 한정)를 타지 않고 290행 `target = b.buffer.get(_n(a["attach_to"])) or _dict_hit(b, a["att |
| 높음 | `impl-B-37` | pipeline.py:146-149(및 content의 155-158)에서 tgt 미해소 시 값·부착대상·provenance를 동봉해 attach_conflict 큐로 보류한다. | core/pipeline.py:146-149 `for f, spec in attrs: tgt = resolved.get(spec.get("attach_to_field")); if tgt is None: continue` — 부착 대상 미해소 시 큐도 결함 로그도 없이  |
| 높음 | `spec-s7-11-51` | cmd_queue의 kind별 항목 출력을 payload.node_id의 엣지 참조 수 내림차순으로 정렬한다. | [표행 1]은 있다 — 엣지 삭제 도구 core/ops.py:479-496 `delete_edge()`가 `e["status"] = STATUS_DELETED`(core/graph.py:54 `STATUS_DELETED = "deleted_by_user"`)로 툼스톤을 |
| 높음 | `spec-s7-11-51` | 큐 열람에 정렬 규칙을 넣는다 — auto_node/uncertain_match 항목을 그 node_id의 엣지 차수(참조 수) 내림차순으로 앞세운다(cli/platform.py queue_view 또는 store 측 정렬 함수) | 뒷부분(엣지 삭제 도구)은 지킨다 — core/ops.py:479-496 `def delete_edge(layer, src, rel, dst, actor, reason="")` + cli/ops.py:44 `choices=["rename","merge","split", |
| 높음 | `impl-B-92` | 답변 생성 지점에 주입형 mock/실호출 분기를 세우고 mock 측을 현행 render 나열로 고정한다. | ⑧답변 생성 지점(cli/query.py:35-121 `answer()` + :147-161 `render()`)에 USE_MOCK 분기도 주입 훅도 없다 — 이 파일 전체에 USE_MOCK·HOOK 문자열 0. 레포에서 **실행되는** mock/실호출 분기는 core |
| 높음 | `impl-B-97` | openpyxl import를 read_xlsx 안으로 내려 read_pptx와 동형으로 만들고, doctor의 REQUIRED→OPTIONAL 재분류 + 'USE_MOCK=1 전 경로가 포맷 패키지 없이 완주하는가'를 실측 항목으로 추가 | parser/reader.py:31-32가 모듈 최상단에서 `from openpyxl import load_workbook` / `from openpyxl.utils import get_column_letter` — 같은 파일의 read_pptx(74)는 `from p |
| 높음 | `spec-A-38` | run.py에 `init [--fresh]` 진입점을 만들어 data/ 하위 빈 상태 생성·재생성을 한 곳에 정의하고, doctor.py와 10개 테스트의 rmtree 3줄 반복을 그 진입점 호출로 바꾼다. 부수적으로 트리가 선언한 build/test/status 이름도 현행 all/(없음)/(없음)과 어긋난다. | run.py:123-136 디스패치 dict의 키는 bootstrap/ingest/all/query/ops/gauges/platform/scan/parse/register/show/export뿐 — `init`도 `--fresh`도 없다. 실행 확인: `python3  |
| 높음 | `impl-B-58` | mock 항목에 실호출 전용 표시 필드(예: `verified_by: "live"`)를 넣고, 채점부(tests/test_g4.py:63-66의 expected_path 대조, cli/platform.py:218-231 계기판 1)가 그 필드를 읽어 mock 실패를 정상으로 처리하게 배선한다. matcher의 0.95 포함 규칙은 그 표시 | mock 자산 어디에도 '실호출로만 검증' 표시 데이터 필드가 없다. mock/queries.json:4-15의 항목 필드는 id·type·q·expected_path 4개뿐이고, mock/parsed/*.json 전 키를 덤프해도 records/chunks 필드에 그 |
| 높음 | `spec-A-201` | 등록 세션 진입 전에 도는 role 배정 시험 명령(문서 열 이름 전량 → role 5종/L3/L4 배정 시도, 실행만 하고 등록부는 건드리지 않음)을 만들고, doctor.py transition()의 [1]·[2] 사이에 그 단계를 넣어 실험 산출 없이는 register generate로 가지 않게 한다. | 사내 작업 순서를 실행 가능하게 선언하는 자리는 doctor.py:201-244 `transition()`이고, 거기에 role 배정 실험이 없다. doctor.py:209-215이 [1]로 지목하는 사내 첫 작업은 `layers/process/skeleton.json |
| 보통 | `impl-B-81` | pipeline.py:296-300에 규칙 B 폴백을 넣는다 — 미해소·null attach_to면 그 행/청크의 공정좌표(ref)에 `_pair_relation`으로 부착한다. | 기준선은 잠겨 있다 — tests/test_g3.py:79-80 `show("C4 spec_conflict 1건 (같은 context 그룹, 다른 값)", len(q("spec_conflict")) == 1, …)`이고, 클린 재빌드 실측에서도 spec_conflict |
| 보통 | `spec-12-16-71` | tests/test_g3.py S13에 부정 경로 어서션을 추가한다 — 미해소 attach_to 투입 후 노드·엣지 증가 0 + orphan_attach 1건. | 런타임 동작은 규범대로다 — core/pipeline.py:296-300 `if target is None: store.enqueue("orphan_attach", f"부착 대상 미해소 — '{a['attach_to']}'", env["doc_id"], {…}); co |
| 보통 | `spec-s7-11-56` | 파급 계수를 보고 ≤1이면 미리보기·--yes를 생략하는 분기를 cli/ops.py에 세우고 차단형 승인 게이트를 걷는다; 미리보기 대상에 이관·골격 개정을 넣는다. | 미리보기가 **차단형 승인 게이트로** 구현돼 있다 — cli/ops.py:52 `p.add_argument("--yes", action="store_true", help="미리보기 확인 후 실행")`이고 60·68·75·82행이 `if a.yes:`일 때만 실제 연산 |
| 보통 | `spec-s7-11-56` | preview()의 nodes·edges 계수로 파급 ≤1을 판정해 미리보기와 --yes 차단을 건너뛰는 공통 분기를 cli/ops.py에 둔다. | 검사기 문면만 읽은 구현이 실제로 차단형 게이트가 됐다 — core/ops.py:14 docstring "**파급이 1건을 넘으면 실행 전에 보여준다**(G6). 미리보기 없이 도는 연산은 없다"가 조건 없는 강제로 번역돼, core/ops.py:187·327·466이 |
| 보통 | `impl-B-23` | core/pipeline.py의 attach 처리에 규칙 B를 넣는다 — 부착 대상이 없거나 미해소면 그 행/청크의 `ref`(process_ref 해소 노드)에 저해상도 부착하고, attach_to가 null이면 큐를 달지 않고 폴백만 한다 | 두 규칙 중 auto 생성만 있고 **부착 폴백(규칙 B)이 없다**. auto 생성은 실재 — core/build.py:295-301 `nid = self.g.add_node(canonical, category, "auto", provenance=[prov], **e |
| 보통 | `spec-12-16-80` | cli/export.py cmd_mermaid에 cross_layer_traverse 관계(occurs_in·controlled_by)를 층 구분 표기와 함께 추가로 그리는 갈래를 넣는다 — 골격만 그리더라도 브리지는 남긴다 | 시각화 3형태 중 mermaid만 cross-layer를 못 그린다. cypher는 지킨다 — cli/export.py:90-97 `live = {i for g in _world().values() for i, n in g.nodes.items() if is_live( |
| 보통 | `spec-A-116` | 판정 함수의 입출력을 계약대로 고정한다 — 입력을 mention+후보 목록으로, 출력을 `{"type","matched_id","confidence"}` dict로 바꾸고 층 config의 정의문·비대칭 기준을 프롬프트로 주입하는 자리를 만든다 | 입력·출력 계약이 둘 다 다르다. 입력: core/matcher.py:59 `def resolve(surface, category, layer, graph, dictionary, *, scoped=True, polarity=None)` — `mention + 후보들`이 |
| 보통 | `spec-A-080` | spec_conflict 적재 시 대상 노드의 polarity를 payload에 싣고, 무극성(none) 노드의 spec_conflict를 큐 열람에서 'I3 분리 후보'로 구분해 표면화한다 | spec_conflict를 극성 분리 신호로 읽는 코드가 없다. 생산부는 극성을 payload에 담지도 않는다 — core/build.py:330-335 `store.enqueue("spec_conflict", f"{n['canonical']}의 {name}: 같은 맥 |
| 보통 | `spec-s7-11-62` | 소속 변경(이관) 연산을 삽입과 분리된 별개 op으로 신설한다 — 엣지 재배선 + canonical 연쇄(_scope_children 재사용) + preview 지원 + actor/로그, 건별 승인(--yes) 경유 | 이동(=이관) 연산이 존재하지 않는다. I축 도구는 4연산+엣지삭제로 닫혀 있다 — core/ops.py:171 `rename`, :304 `merge`, :371 `split`, :451 `obsolete`, :479 `delete_edge`; cli/ops.py:4 |
| 보통 | `spec-s7-11-14` | 판정 정확도(틀린 매칭이 조용히 통과한 비율)를 재는 장치를 만든다 — 골든셋 대조 기반 측정치와 '수정 큐로 가는 건수' 추이. 계기판 8종 표는 건드리지 않고 별도 측정으로 둔다 | 도입 트리거 두 지표 중 **판정 정확도를 재는 장치가 없다**. 보류율만 있다 — cli/platform.py:257-267 `# 3 판정 보류율 — 큐 유입 ÷ 발자국을 남긴 조각 수` / `hold_rate = round(len(q) / pieces, 3)`, 반 |
| 보통 | `spec-A-78` | spec_conflict payload에 대상 노드 polarity를 싣고, 무극성 노드 건은 극성 분리(I3) 촉발 신호로 표시한다. | spec_conflict 적재부 core/build.py:330-336 `store.enqueue("spec_conflict", f"{n['canonical']}의 {name}: 같은 맥락에 다른 값", self.doc_id, {"node_id":…, "attr":…, |
| 보통 | `spec-A-153` | 근거 없음 경로에서 사전·그래프의 인접 등록 개체를 함께 제시해 재질문을 유도한다(링킹 미스율 오염 방지). | 근거 없음 경로에 등록 개체 안내가 없다 — cli/query.py:72-78 `if not hits or intent == "general": … res["note"] = ("사내 문서에서 근거를 찾지 못했다. " + GENERAL if not hits else …) |
| 보통 | `role-136` | doctor.py 전환 안내(transition)의 첫 걸음을 role 배정 실험으로 바꾸고, register generate에 실험 산출물 확인 절차를 넣는다. | 코드가 사내 첫 작업을 다르게 단정한다 — doctor.py:210-214 `line(WARN, f"[1] 골격 seed가 아직 창작 mock이다 …", "**사내 첫 작업이 이것이다.** layers/process/skeleton.json을 사내 공정 체계로\n    |
| 보통 | `role-023` | resolve_anchor의 미해소 경로에서 같은 표면형·카테고리의 auto 노드를 조회해 orphan_anchor payload에 후보 node_id를 동봉 | core/build.py:88-121 resolve_anchor는 사전 히트를 카테고리로만 거른 뒤(107-108 `hits = [nid for nid in self.dict.get(norm(surface), []) if (g.get(nid) or {}).get("ca |
| 보통 | `impl-B-19` | 근거 없음 분기에서 등록 개체(사전·그래프의 인접 canonical) 후보를 뽑아 응답에 담고 render에 안내 줄 추가 | 표시와 로그는 있으나 등록 개체 안내가 없다. 있는 것: cli/query.py:26 `GENERAL = "[일반지식 — 사내 검증 필요]"`, :74 `Q.log_miss(question)` → core/query.py:95-97 `store.append_line(s |
| 보통 | `impl-B-104` | 코어(저장) 단위 판정 하나를 추가한다 — 임시 data_dir에 GraphStore를 열어 노드 2개를 add하고 save한 뒤 새 인스턴스로 load해 (a) 두 id가 서로 다르고 다른 층 그래프의 id와도 겹치지 않으며 (b) nodes/edges 덤프가 저장 전과 완전 동일함을 대조한다. GraphStore.for_layer(la | 노드 2개 add → 저장 → 로드 → 복원 동일을 확인하는 판정이 없다. GraphStore의 왕복 경로는 core/graph.py:87-100 `load()` / `save()`로 존재하지만, tests/ 전체에서 save 후 재로드해 내용을 대조하는 어서션이 0건 |
| 보통 | `spec-s7-11-18` | role 핸들러 분기에 관찰 지점을 만들어 ①빈 셀 부착(attach_to_field 대상 미해소)을 이미 선언된 `attach_conflict` 큐로 올리고, ④entity 값의 산문 혼입 판정을 하나 세운다. ②③은 파서 자기완결 계약이 1차 방어선이므로 추가 작업 없음. | 갭 4종 중 2종만 관찰되고 2종은 관찰자가 0곳이다. 관찰되는 쪽: ②상동/병합 셀 — parser/normalizer.py:31 `expand_merged` · :68 `resolve_ditto`가 처리하고 :154-163 `normalize()`가 `{"ditto |
| 낮음 | `impl-B-100` | impl-B-99의 init --fresh가 graph.json={"nodes":{},"edges":[]} · dictionary.json={} · chunks.json={"chunks":{},"describes":[]} · review_queue.json=[] 네 파일을 실제로 생성하게 한다. | 네 형태를 **파일로 만드는 코드가 없다** — init 진입점이 없고(run.py:124-137), .gitignore:8-13이 data/를 "실행 산출물 … run.py all 한 번으로 재생성"으로 두어 현행 클린이 `rm -rf data`(파일 없음)다. 명시 |
| 낮음 | `impl-B-50` | verify_roundtrip이 mock/parsed/{doc_id}.json을 읽어 대조하도록 바꾼다(원본 헤더명 ↔ 계약 필드명 매핑 포함) — 인라인 EXP_CP·EXP_FM 제거 | 자리 자체는 있다 — mock/parsed/에 CP01·CP01B·PFMEA01·PPT01~03·QPPT01 7개의 수기 계약 JSON이 있고 run.py:64 `sorted((ROOT / "mock" / "parsed").glob("*.json"))`가 이것을 인입한 |

## 규모: 1줄 (3건)

| 등급 | 항목 | 고쳐야 할 것 | 현재 코드 |
|---|---|---|---|
| 높음 | `spec-A-49` | core/bootstrap.py:404의 `attrs={"process_no": None}`를 제거하거나 값 항목 리스트 형태로 바꾼다(process_no는 role=meta라 attrs에 있을 자리가 아니다 — schemas/blocks.json:9) | 골격은 맞다 — core/graph.py:99 `_dumps({"nodes": self.nodes, "edges": self.edges})`(nodes=dict, edges=list), 노드 레코드 core/graph.py:117-122 `{id, canonical,  |
| 보통 | `role-104` | layers/process/config.json:6 Property 정의문 끝에 '측정방법·주기·샘플도 제외(관리항목에 붙는 attribute)'를 이어 붙인다. 이 정의문은 아직 코드가 값으로 읽지 않지만(prompts 주입이 미배선 — spec-A-95 참조) 등록 세션 role 배정과 추출 프롬프트의 근거 자산이므로 문면이 정본이어야 | Property 정의문을 담은 코드 자산이 이 문구를 갖고 있지 않다 — layers/process/config.json:6 `"Property": "공정/설비에서 관리·측정·통제되는 항목의 이름(예: 노칭 정밀도). 관리항목/특성/CTQ 포함. 규격값·측정값·판정결과 |
| 낮음 | `impl-B-96` | doctor.py:70과 README.md:52의 버전 바닥을 3.10+로 올린다(코드가 이미 3.10 전용 sys.stdlib_module_names를 쓴다) — 표준 라이브러리 기준선과 '외부 의존 0'의 적용 범위(core/ 한정)도 함께 명시 | 런타임 바닥이 3.10+가 아니라 3.9로 선언·집행돼 있고, 정작 코드는 3.10 전용 API를 쓴다. 선언: README.md:52 `\| Python \| **3.9+** (개발·검증은 3.11) \|`, 집행: doctor.py:70-73 `line(OK if v > |
