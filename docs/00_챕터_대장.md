# 챕터 대장 (정본)

> 지위: **챕터 체계의 단일 원천.** 챕터 번호·제목·하위 절·소유 명세 §·상태를 여기서만 정한다.
> 작성: 2026-07-25 · 개정: 2026-07-27 (**세션 1(CH2 검토) 반영**) · **2026-07-29 (전체 정리 패스 배치 1~7)** · **2026-07-30 (세션 N 종결 — [보완필요] ⑴~⑻ 전량 종결, 판정 26건 + §5.6 진행 원칙 신설)** · **2026-07-30 (세션 N+1 — 증분 0 작성·대조 검토 통과·블록 판정)** · **2026-07-31 (세션 N+2 — 감사 4 수행·통과)** · **2026-08-01 (raw mock 트랙 + 실행 검증 1차 — 사후 등재 08-06/08-07, 검사 로그 11회차)** · **2026-08-06 (세션 N+3 — 쟁점 A 판정·문서 개정 커밋, 12회차)** · **2026-08-07 (세션 N+4 — 구현 레포 정비 중 검출된 문서 간 상충 9건 판정, 13회차)** · **2026-08-07 (M1 세션 — 쟁점 B·C 판정, 14회차)** · **2026-08-10 (M2 세션 — 골격 두 축 분리 A11 + R9 종결, 15회차)** · **2026-08-10 (M2 반입 — 채번 사고 검출·D-42~48 반입, 16회차)** · **2026-08-13 (M2 후속 — 판정필요 3·4·5 처리·검사 6 신설, 17회차)** · **2026-08-13 (정합 수리 — 14-B 복원·문서 6종 동기화, 18회차)**
> 기준: 명세 v1.18 / 정의서 v1.8 / 구현문서 v1.16 / **틀 v2.7 / 카드 v14**
> 상위 프레임: 3축 + **두 모드**(운영/구축 — 카드 M그룹). 구축 모드는 CH2(문서)와 CH4.7(층)에, 운영 모드는 CH3(쓰기)·CH5(읽기)에 산다.
>
> **원천 구조 (확정 7-25)**
> - **챕터 MD = 작성 원천.** 완전 서술(오해의 여지 0)이 여기 산다. 확정 전에는 한 파일에 4각도(구조·규약·기능·로직)를 다 담는다 — 문서체계_대전제 §7의 실행.
> - **PPT = 파생 뷰.** 탐색·상태 확인·논의 좌표용. 챕터에서 재생성하며, 챕터와 어긋나면 챕터가 이긴다.
> - **JS = 렌더러.** 레이아웃만 담고 내용은 담지 않는다.
> - **명세·정의서·구현문서 = 분해 대상.** 챕터가 안정되면 각 챕터의 규약→명세, 기능→정의서, 로직→구현문서로 옮기고 챕터엔 구조설명+참조만 남긴다(대전제 §6·§7).
> - **발의와 정본의 구분 [명문화 2026-08-07]**: 파생 문서(raw_mock_대장·판정대조·실행검증)는 **가결정을 발의하는 원천**이고, 그 가결정이 증분0·챕터에 채택·반영되는 순간부터 **채택본이 정본**이다. 파생 문서의 "이 문서가 원천" 선언은 **발의권이지 개정권이 아니다.** 겹치면 증분0·챕터가 이긴다(D-15의 10건↔11건 상충이 이 규칙으로 판정되었다 — 13회차).
>
> **지금 시점의 우선순위**: 감사 4 통과(10회차) → raw mock 실물·킷·실행 검증 확보(11회차) → 쟁점 A 판정(12회차) → **구현 레포 정비 중 검출된 문서 간 상충 9건 판정(13회차)**. **다음 단계 = 구현 착수(별도 Claude Code 세션 — 첫 행동 = 증분 0 단위 A0·G0). 구현의 1차 입력물 = 구현문서 v1.16 + 증분 0 v1.3(겹치면 증분 0이 이긴다). DECISIONS.md 초기값은 31건(D-1~D-31). ULID는 A0 계수 ②=0 확인으로 발효 확정(13회차). M2(15회차) 전달분은 D-42~D-48로 반입 완료(16회차 — 소급 등재 D-39~D-41 포함, 레포 48건). 후속 D-49~D-55(17회차).**
> **단, 서술완료 챕터라도 틀이 개정되면 그 즉시 `동기화 필요`가 된다** — 세션 1에서 CH2가 틀 v1 시점에 멈춰 있었던 것이 실례다(§4 검사 로그 5회차). 틀을 고치는 커밋은 영향받는 챕터를 같이 고치거나, 최소한 상태를 내린다.

---

## 0. 전체 flow 조감 — 챕터가 어디에 사는가

```
[구축 모드]  미등록 입력(새 doc_type / 새 층)
             → LLM 표본 관찰(정형 head ~10줄 / 비정형 10~30줄·패턴 2~3회) → 어댑터 코드(+분할 규칙)·매칭 스키마·(층이면) config 초안
             → 실행 하네스(기계 관문 — D-27) → 사람 검수(결과 뷰 확인 · 승인 1회) → 등록   …… CH2 (층: CH4.7, 파서 내부: 파서_명세)

[운영 모드]  문서 투입 → 등록 조회(결정적)
             → 인입 진입 검증: doc_hash 대조 → 근거 축 id 계산 → 필드 검증    …… CH2 2.8
             → 파싱: 어댑터 실행 → 계약 JSON (record/청크 + 공정좌표)         …… CH2(계약) · CH3A · 파서_명세
             → 추출(비정형만): 청크별 LLM → 개체·관계 후보+부착 이름 [체크포인트 — 계약은 CH3A 3.11] …… CH3A
             → 구축: 2-pass 해소(판정) → 부착·관계 생성(게이트) → 자동 커밋+큐  …… CH3A
                └ 이름·값의 규칙(극성·스코프·맥락)                            …… CH3B
             → 그래프(JSON=진실, 접근은 GraphStore 경유 — 틀 §4B-A8) → 파생물(임베딩·시각화) …… CH3B · CH6
             → 수명주기: 재인입 · 큐 처리 · 구조 개정 제안 · I축 4연산          …… CH3B
[질의]       링킹 → 확장 → 수집 → 답변(이원 근거)                             …… CH5
[층]         여섯 칸 · config-only · 층간 연결(A안 + 허브 규칙)               …… CH4
[기반]       목적 · 원칙 P1~P7 · 용어                                        …… CH1
[구현·운영]  코드 배치 · 근거 축 id 산식 · mock · 플랫폼 훅 / 로드맵 · 문서 체계 …… CH6 · CH7
```

## 1. 챕터 구조 (상위 7 · 하위 41 + 파생 9)

상위 챕터는 **3축 프레임**을 따르고(기반 → 축1 → 축2 → 축3 → 구현 → 운영), 하위 절이 명세의 절과 대응한다. **CH3은 검토 단위 조정을 위해 두 파일(CH3A 절차 / CH3B 규칙)로 분할되었다 — 절 번호는 불변.**

### CH1 · 기반 — 무엇을 왜 만드는가 · **파일 v1 · 서술완료 (배치 4)**
| 절 | 제목 | 소유 명세 § | 상태 |
|---|---|---|---|
| 1.1 | 목적과 범위 | §1 | **서술완료** |
| 1.2 | PoC 범위와 완료 정의 | §1.1 | **서술완료** |
| 1.3 | 불변 원칙 P1~P7 | §2 | **서술완료** |
| 1.4 | 용어 참조 | (용어_대장 = 정본) | **서술완료** (참조 절) |

### CH2 · 문서 계약과 등록 — 구축 모드 [축1 전반] · **파일 v2.2 (15회차: 2.2 좌표 목록·tier 파생, 2.5 electrode_type 확정, 2.6 구조 필드) · 상태 `검토중` (잔여 묶음은 §5.3 — U7은 M2로 종결)**
| 절 | 제목 | 소유 명세 § | 상태 |
|---|---|---|---|
| 2.1 | 파서의 위치와 경계 | §6.4, §12(경계 부분) | **서술완료** |
| 2.2 | 파서 출력 계약 3종 | §12, §5.4-3, §6.2(3층 원천), 틀 §4B-A1·A4·A7·A11 | **서술완료** — `section`의 role은 미결(D-31 — **M2-후속 이월, 마감선: P트랙 착수 전**) |
| 2.3 | 파서·등록의 LLM 활용 수준 | (파서_명세 골자 — 틀 §3·§4-Q1·Q2) | **서술완료** |
| 2.4 | role 어휘 — 5종 + edges | §6.1, 정의서 §1·2·3·4 | **서술완료** |
| 2.5 | **매칭 스키마 — 필드 인벤토리와 role 번역표를 겸하는 한 파일** | §6.5(겸용 확정), 정의서 §4·§5.1~5.5, 구현문서 §2.4, 틀 §4-Q5·§4B-A11-8 | **검토중** (묶음 P·V 잔여 — **U7(electrode_type)은 M2로 종결**: 구조 필드 확정) |
| 2.6 | 구축 모드 — 등록 워크플로우와 산출물 | §6.5, §6.7, 정의서 §8, 틀 §2 | **서술완료** — 등록 입력 패키지·검수 뷰 **확정(세션 N ⑵⑺)**. 규약 5(UNMAPPABLE 필드는 스키마에 넣지 않음)는 **템플릿 미탑재가 실측됨** → D-30. **구조 필드는 6지선다 대상 아님(M2 — D-46)** |
| 2.7 | 변경 등급 L1~L4 | §6.3, 정의서 §7 | **서술완료** |
| 2.8 | 핸들러 루프와 인입 검증 | §6.6, §6.5(인입 검증), 정의서 §6, 틀 §4B-A4·A6·A7 | **서술완료** — 알 수 없는 role 방어(`invalid_role`)는 D-30 |

### CH3A · 인입 절차와 LLM 역할 — 운영 모드 · **파일 v3 (세션 N ⑶: 3.11 확정 + 검사 1~3 전수 대조 수행)**
| 절 | 제목 | 소유 명세 § | 상태 |
|---|---|---|---|
| 3.1 | 두 체제와 세 단계 (파싱→추출→구축) | §5.5(개요), §5.4-1, 틀 §4-Q1·Q2·A6 | **서술완료** |
| 3.2 | 2-pass — 문서 단위 처리 | §5.5-1 | **서술완료** |
| 3.3 | 개체 판정과 동의어 사전 | §5.4-2, §5.5-2, §8-R2 | **서술완료** |
| 3.4 | 관계의 생성 — 4경로와 커밋 게이트 | §5.3, §5.4(부착), §5.5-4, 틀 §3·§4B-A3 | **서술완료** [명세 §7-2 개정 대기] — direction_conflict kind는 **신설로 확정(D-3)**, 챕터 내 "택일 대기" 표기는 낡음 |
| 3.10 | LLM 역할 — 전역 지도 (확정판) | §7 (개정 대기), 틀 §3 | **서술완료** |
| **3.11** | **추출 후보 JSON — 체크포인트 계약** | **(명세 대응 절 없음 — v1.19 신설 대상, 이 절이 유일 원천)** | **확정 (세션 N ⑶, 07-30)** |

### CH3B · 인입 데이터 규칙과 수명주기 — 운영 모드 · **파일 v2.2 (18회차: 3.5 규약 3-① 틀 v2.7 동기화 — 생략은 스코프 카테고리 한정·Unit은 F1 유지. v2.1 = M2 3.5 전면 개정)**
| 절 | 제목 | 소유 명세 § | 상태 |
|---|---|---|---|
| 3.5 | 이름 규칙 — 극성과 canonical 스코프 | §5.2(극성·canonical 부분), 틀 §4B-A11 | **서술완료 — R9 종결(M2, A11-8: electrode_type=구조 필드+polarity 파생 필드·부착 정합 A11-9·3패턴·mirrors polarity 비교)** |
| 3.6 | 값 규칙 — attribute와 맥락 | §6.2(context 상속), 정의서 §3.3 | **서술완료** |
| 3.7 | 자동 커밋과 수정 큐 (**+ I축 4연산 소유**) | §9(9.1 제외), 틀 §4B-A2 | **서술완료** — 큐 표 "13종"은 낡음(현행 17종 확정+3종 승인 — 정본 보충은 증분 0 §6-5) |
| 3.8 | 재인입 | §5.5-3, 틀 §4B-A4(멱등성 두 축) | **서술완료** |
| 3.9 | Tier1 구조 개정 경로 (**+ A5 하향 한정**) | §9.1, 틀 §4B-A5 | **서술완료** |

### CH4 · 정보층 — 층의 정의와 확장 [축2] · **파일 v1 · 동기화 필요 (틀 v2.5 §4B-A9[T0~T3·3테스트] + v2.6 §4B-A11[4.4 골격 작성 규칙 v3.2·seed 형식] — 세션 4에서 4.7과 함께 동기화. 배치 1 = 서술완료)**
| 절 | 제목 | 소유 명세 § | 상태 |
|---|---|---|---|
| 4.1 | 층의 구성 — 여섯 칸 | §4 | **서술완료** |
| 4.2 | 범용성 전략 — A/B/C와 Rule of Three | §3.1~3.4 | **서술완료** |
| 4.3 | config-only 구조와 명시적 실패 | §3.6 | **서술완료** |
| 4.4 | 공정정보층 | §5.1, §5.2(골격 부분), §5.3, §5.4(카테고리 정의문) | **서술완료** — **A11 동기화 필요(골격 seed v3.2·3패턴·대표 흐름)** |
| 4.5 | 품질지식층 | §15 | **서술완료** — **⑵-③′로 지위 변경: 기본 내장이 아니라 신규 층 등록 절차의 첫 검증 대상(J10)** |
| 4.6 | 층간 연결 (J7 A안·J8 경유·J9 층별 요약 반영) | §8, §8-R3 | **서술완료** — 층간 엣지의 저장 소속(가리키는 층의 graph.json — J5 단방향 정합)은 틀 §4B-A8-1이 명문화 |
| 4.7 | 신규 층 진입 절차 (구축 모드의 층 버전) | (§3·§4·§8 종합), 틀 §2 | **서술완료 — [보완필요④]는 세션 4로 이관 확정(⑷-A).** 세션 N 선확정분: 층 선행 완결·부트스트랩·층 검수 뷰 소유 이관 |

### CH5 · 질의 — RAG로 작동하기 [축3] · **파일 v3.1 (M2: 5.1 순서 파생 규약 8)**
| 절 | 제목 | 소유 명세 § | 상태 |
|---|---|---|---|
| 5.1 | 질의 4단 파이프라인 | §5.6.1~5.6.3, §8-R1, 틀 §4B-A11-5 | **서술완료** — **규약 8 신설(순서 파생 — M2)** |
| 5.2 | 이원 근거 채널과 답변 3단 | §5.6.4, §8-R4 | **서술완료** |
| 5.3 | 질문 유형 대응표 | §5.6.5 | **서술완료** |
| 5.4 | 이연 항목과 벡터 대비 | §5.6.6(확장) | **서술완료** — 임베딩 대상 선택은 미결 R6. 저장 계층 전환(R10) 행 + **라우팅 표현(R13) 행 추가(M2)** |
| 5.5 | 계기판과 골든셋 | §10 | **확정 — 계기판 8종(N+3) + BM-25 상시 대조군 + 가결정(골든셋 구성 120)** |

### CH6 · 구현 — 코드 배치와 진입 · **파일 v3 (세션 N+3: 규약 6 저장 계층·ULID·mock 15종, 08-06)**
| 절 | 제목 | 소유 명세 § | 상태 |
|---|---|---|---|
| 6.1 | 코드 배치와 두 축 id | §3.5, §8-R3(확장) | **서술완료** — 규약 2 의미 축 ULID + 규약 6(저장 계층 — GraphStore 경계·orjson·알람선). **ULID 발효 확정(13회차 — A0 계수 ②=0)** |
| 6.2 | 파이프라인 형상 — 3단계와 두 모드 | §16.1, 틀 Q1 | **서술완료** — 단위 10종+st. n9 행의 "(D-1)" 앵커는 오기(정본 ⑴-C″/C15) |
| 6.3 | mock 체계 | §16.3 | **서술완료** — **15종 닫힌 목록(N+3: S14 역산 정합·S15 ipqc 등록 — D-18·D-20 채택)** |
| 6.4 | 파생물 — 시각화와 외부 적재 | §11, §16.2 | **서술완료** |
| 6.5 | 현재 구현 상태 | (레포 PROGRESS — 회수 필요) | **미결** — 구현 재개 첫 행동 = A0. **레포 정비 완료(08-07): docs/ 18종·mock/raw 10종·kit/·tests/ 배치. 코드는 0바이트 — 계수 ①②=0. G0~G3 완료(08-08) · M2 반입 0~4단계 완료(08-12) · M2 후속(G4 전 정리) 완료(08-13, DECISIONS 55) — **G4 별도 승인 대기**** |

### CH7 · 운영 — 로드맵·문서·결정 · **파일 v1.4 (18회차: 7.3 미러 위치 claude/ 정본화·7.4 갱신. v1.3 = 14-B 참조/ 신설)**
| 절 | 제목 | 소유 명세 § | 상태 |
|---|---|---|---|
| 7.1 | 로드맵 — 4국면과 현재 위치 | §14 | **서술완료** |
| 7.2 | 미결 항목 총괄 (R1~R9 + §13 대조) | §13, 틀 §5 | **서술완료** — R1은 세션 4 이관. **R10~R13 신설은 틀 §5가 정본(R9는 M2 종결)** |
| 7.3 | 문서 체계 — 정본 관계와 파일 대장 | §16.4, 문서체계_대전제 | **서술완료** — **발의/정본 구분은 이 대장 머리말이 정본(13회차 신설)** |
| 7.4 | 결정 현황판 | (구 쟁점레지스터) | **현황판** — 세션 N 판정 26건 등재 완료. **가결정 31건(D-1~D-31)의 소재는 §1 파생 문서 표가 지시한다** |

### 파생 문서
| 파일 | 버전 | 상태 |
|---|---|---|
| `chapters/파서_명세.md` | **v0.5.1 (감사 4 정정, 07-31)** | **§1~§7·§9(경계) 확정** — 잔여는 §9 킷의 실물 작성(단위 n8). **개정 대기 3건: §9 킷 5종→6종(D-27) · §5 `expects`에 `header_labels` 신설(D-29) · §5 좌표 닫힌 목록=골격 전 노드+킷 템플릿 v0.3 구조 필드 지시(M2 — D-45·D-46)** — 가결정·확정이므로 코드는 DECISIONS 기준으로 전진, 문서 개정은 세션 1P |
| `chapters/증분0_구현.md` | **v1.4 (프로젝트판 현행 — 08-13 §0 정정)** | **구현 착수의 1차 입력물.** 통합 배열(G0~G6 + G1 st)·단위 3요소·mock 15종 총괄·판독 증분·가결정 체계. **v1.4 동기화 5항 반영 완료.** 레포 사본은 구 본문 — 교체 반입 필요(§7 ③) |
| `chapters/raw_mock_대장.md` | **v2.1 (08-07 — D-15 정정 10→11, §7 상태 갱신. v2.0 = 전량 2~3배 확대 + prefix 정의)** | raw 실물 10종·normalizer 배치·역산 정합 계약 + **가결정 D-15~D-21의 발의 원천** |
| `chapters/판정대조_ipqc_1차.md` | **v1.0 (08-01)** | 블라인드 role 배정 실측(UNMAPPABLE 2/2·일치 10/16) → 킷 개정 지시 7건 + **가결정 D-22~D-25** |
| `chapters/실행검증_1차.md` | **v1.0 (08-01 — 사후 등재 08-07)** | **성공 판정의 정정을 소유하는 문서**(LLM 출력의 문면이 아니라 **실행 결과**로 판정한다). LLM이 짠 adapter·schema를 실제로 돌린 기록 — 정형(ipqc)·비정형(toc_report) 2건, 실행 하네스 4단 검사. 원천 결함 2건(`expects.columns` 키 미정의 → preflight 무력화 / `section` role 미결)·프롬프트 결함 1건(UNMAPPABLE 처리 미지시 → 런타임 크래시)·하네스 한계 1건 도출 + **가결정 D-26~D-31** |
| `안건/M2_판정문안_초안.md` | **r8 (08-10 — 승인·반영 완료)** | **M2 판정 기록(소진)** — A11 전문·카드 v13 문안·seed 작성 규칙 v3.2·형식 판정 경위·**§11 통용성 검토**(세션 4의 CH4 4.4 동기화가 §4를 소비)·**§8 DECISIONS 전달 문안 D-42~D-48(허브 채번 확인 후 반입 — M1 지정 번호 D-32·33과 레포 자체 채번 충돌 검출)** |
| `안건/skeleton_seed_v2.md` | **v3.3 — 키 개명 TREE·AXIS_LABELS, D-53** | **조립 골격 seed 실물(n10 운반물)** — 단일 파일(PROCESS_TREE 순서 배열+ALIASES 블록+POLARITY_LABELS), loader 기대값(노드 46·part_of 45·precedes 22·polarity 16·mirrors 쌍 8)·파생 흐름 기대 출력·mock 재매핑 표("실링" 행만 수정) |
| `claude/구현트랙_PROGRESS_미러.md` | **정본 (08-10 갱신)** | **구현 트랙 정산 미러** — G0·G1+G2·G3 정산 + M2 반입 진행판 + 3단계 선행 판정 3건. **이 파일에 결정 번호를 적지 않는다**(16회차 규칙 — 장부는 레포 `DECISIONS.md` 하나). 구 `chapters/구현트랙_PROGRESS_미러.md`(08-07판)는 **폐기** |
| `archive/세션기록_raw_mock_제작.md` | v2.0 (08-01) | **종결(archive)** — raw mock 제작 세션의 경위 기록. 정본은 raw_mock_대장·판정대조·실행검증 3종(자기 frontmatter가 양보 선언). 14-B에서 등재했으나 소실 → 18회차 재등재 |

**상태 값**: `서술완료` / `골격` / `논의중` / `미결` / `초안`.

---

## 2. 명세 § → 챕터 역방향 분할표 (누락 0 · 중복 0)

| 명세 § | 챕터 | 명세 § | 챕터 |
|---|---|---|---|
| §1 | 1.1 | §6.1 | 2.4 |
| §1.1 | 1.2 | §6.2 (3층 구조) | 2.2 |
| §2 | 1.3 | §6.2 (context 상속) | 3.6 |
| §3.1~3.4 | 4.2 | §6.3 | 2.7 |
| §3.5 | 6.1 | §6.4 | 2.1 |
| §3.6 | 4.3 | §6.5 (연결·겸용) | 2.5, 2.6 |
| §4 | 4.1 | §6.5 (인입 검증) | 2.8 |
| §5.1 | 4.4 | §6.6 | 2.8 |
| §5.2 (골격) | 4.4 | §6.7 | 2.6 |
| §5.2 (극성·canonical) | 3.5 | §7 | 3.10 |
| §5.3 | 4.4 | §8-1~8-8 | 4.6 |
| §5.4 (카테고리 정의문) | 4.4 | §8-R1 | 5.1 |
| §5.4-1 (추출·부착 규약) | 3.1, 3.4 | §8-R2 | 3.3 |
| §5.4-2 (판정 프롬프트) | 3.3 | §8-R3 | 6.1 |
| §5.4-3 (좌표 태깅) | 2.2 | §8-R4 | 5.2 |
| §5.5-1 | 3.2 | §9 | 3.7 |
| §5.5-2 | 3.3 | §9.1 | 3.9 |
| §5.5-3 | 3.8 | §10 | 5.5 |
| §5.5-4 | 3.4 | §11 | **6.4** |
| §5.6.1~3 | 5.1 | §12 | 2.2 |
| §5.6.4 | 5.2 | §13 | 7.2 |
| §5.6.5 | 5.3 | §14 | 7.1 |
| §5.6.6 | 5.4 | §15 | 4.5 |
| — | — | §16.1 | 6.2 |
| — | — | §16.2 | **6.4** |
| — | — | §16.3 | **6.3** |
| — | — | §16.4 | 7.3 |

**정의서**: §1·§2 → 2.4 / §3.1·§3.2·§3.4·§3.5 → 2.4 / §3.3 → 2.4(구조)·3.6(충돌) / **§4 → 2.4(개념)·2.5(문법)** / **§5.1~§5.5 → 2.5** / §6 → 2.8 / §7 → 2.7 / §8 → 2.6.
> **원천 줄과 이 표는 같아야 한다.** 챕터의 "원천" 줄이 이 표와 다른 배정을 주장하면 이 표가 이긴다.

**구현문서**: §0·§1·§3 → 6.1 / §2.1·§2.2 → 2.2·6.1 / **§2.4 → 2.5** / §4·§5 → 4.3·4.4·4.5 / §6 → 6.3 / §7·§9 → 6.2·6.5 / §8 → 6.3.

**틀 §4B**: A1 → 2.2·5.4 / A2 → 3.7 / A3 → 3.4 / A4 → 6.1(산식)·2.2(소유)·2.8(계산 지점)·3.8 / A5 → 3.9 / A6 → 2.8·3.1 / A7 → 2.2·2.8·6.1 / **A8 → 6.1(규약 6)·4.6(층간 엣지 소속)·5.5(계기판 7·8)** / A9 → 4.1·4.7(세션 4 동기화) / A10 → 2.2 / **A11 → 3.5(극성·polarity·부착 정합)·2.2(좌표 목록·tier 파생)·5.1(순서 파생)·4.4(골격 작성 규칙 — 세션 4 동기화)**.

**파서_명세**: §5·§6·§7은 **파서_명세가 유일 원천**이다. 카드 C11~C15·M10·M11·J10이 검사기. **CH3A 3.11의 검사기는 M12, CH5 5.5는 K8, 저장 계층(틀 §4B-A8)은 B6·N1.**

**증분 0**: 구현 단위의 통합 배열·3요소·mock 행별 주석·가결정 대장의 **유일 원천**이다. 챕터·카드와 충돌하면 챕터·카드가 이긴다.

**raw_mock_대장 · 판정대조_ipqc_1차 · 실행검증_1차**: mock/raw 실물·생성 킷·블라인드 판정·실행 검증의 **발의 원천**. 가결정 D-15~D-31이 여기 산다(증분 0 §7의 D-1~D-14와 합쳐 **DECISIONS.md 초기값 31건**). **발의권은 개정권이 아니다** — 머리말의 발의/정본 구분을 따른다.

---

## 3. 챕터 파일 서술 규격 (필수 준수)

### 3.1 파일과 이름
- 파일 1개 = 상위 챕터 1개. 이름 `CH{n}_{제목}.md`.
- 파일 머리에 **머리표**(3.2)를 둔다. 절 제목은 `## {n}.{m} 제목` 형식.

### 3.2 머리표 (기계 판독용)
```
---
chapter: 2
title: 문서 계약과 등록 — 구축 모드
axis: 축1 (쓰기)
question: 시스템은 문서로부터 무엇을 어떤 약속으로 받으며, 처리 방식이 없는 새 입력은 어떻게 처리 가능해지는가?
owns: [§6.4, §12, §5.4-3, §6.1, §6.2(구조), §6.3, §6.5, §6.6, §6.7, 틀 §2·§3·§4·§4B]
version: v2 (2026-07-27 — 세션 1 검토 반영. 틀 v2.3 동기화)
status: 서술완료
sections: [2.1, …]
tags: {2.1: 확정, …}
---
```
> **`version:` 필드는 필수다.** 개정할 때마다 갱신하고 근거가 된 틀 버전을 함께 적는다.

### 3.3 절의 내부 구조 (4각도 + 오해 방지)
1. **무엇인가 (구조)** 2. **규약**(조건 전부 명시) 3. **기능·계약** 4. **로직** 5. **경계와 오해**(생략 금지) 6. **원천**.

### 3.4 서술 원칙 — 오해의 여지 0
1. 조건을 흘리지 않는다. 2. "등·류·같은"으로 목록을 닫지 않는다. 3. 부정형을 반드시 쓴다. 4. 수치·임계·상한에 출처와 근거를 붙인다. 5. 예시 1개를 붙인다. 6. 미확정은 미확정이라 쓰고, **확정된 것을 미결로 남기지 않는다.**

### 3.5 슬라이드 규칙
슬라이드는 요약하지 않는다 — ①주장 ②조건의 존재와 개수 ③원천 앵커. **단독으로 읽어 틀린 문장이 되는 서술은 올리지 않는다.**

### 3.6 갱신 규칙
- 챕터를 고치면 **같은 커밋에서** 이 대장의 상태를 갱신한다.
- **틀을 고치면 영향받는 `서술완료` 챕터를 같은 커밋에서 고치거나 상태를 내린다.**
- 용어를 새로 쓰면 `용어_대장.md`에 등재한다.
- **파생 문서를 새로 만들면 §1 파생 문서 표에 그 커밋에서 등재한다** — 미등재 파생 문서는 다음 세션이 존재를 모른다(11회차의 `실행검증_1차` 누락이 실례. 13회차에서 정정).
- **정본을 고칠 때는 고치기 직전에 그 정본의 현행판을 다시 읽는다(재독 규칙 — 18회차 신설).** 세션 시작 시점의 사본·요약 위에 재구성하면 그 사이의 개정이 소실된다 — 14-B 기록 소실(M2 세션이 구판 사본 위에 대장을 재구성)이 실례다.

---

## 4. 정합 검사

| # | 검사 | 방법 | 시점 |
|---|---|---|---|
| 검사 1 | **불변식 대조** | 카드 그룹 전 항목 1:1 대조 + 그룹 간 교차 대조 | 챕터 서술완료 시마다 |
| 검사 2 | **커버리지 대조** | §2 분할표의 각 원천 절이 실제 서술되었는지 지워나가기 | 챕터 서술완료 시마다 |
| 검사 3 | **용어 검사** | 등장 용어의 용어_대장 등재 여부 | 챕터 서술완료 시마다 |
| 검사 3.5 | **버전 대조** | 머리표 `version:` ↔ 현행 틀 버전 | 챕터 검토 착수 시 **가장 먼저** |
| 검사 4 | **전체 정합 감사 패스** | 별도 세션에서 수치·조건 개수·부정형의 챕터 간 일치 전수 감사. **범위 한정(§5.6)** | 증분 0 직후 1회 + 명세 분해 직전 1회 |
| **검사 5** | **반입 대조 (신설 13회차)** | **구현 레포에 문서를 반입한 뒤, 레포 안 문서 간 상충을 전수 대조한다.** 프로젝트 안에서는 파일이 갈라져 있어 드러나지 않던 계수·판본·경로 불일치가 한 레포에 모이면 드러난다 | 레포 반입 시 1회 + 대량 문서 개정 후 |
| **검사 6** | **실물 대조 [17회차 신설]** | 코드의 동작·경로·파일명·현재 상태를 지시·단정하는 문안은 발행 전에 해당 코드·config의 **전문**을 연다(절단된 출력·요약·타 문서의 보고로 대체 금지). 검사 5(반입 대조)가 문서→문서 방향이라면 이것은 문서→코드 방향이다. 근거 사고 2건: ①명세 트랙이 미러를 장부로 오독해 DECISIONS 말번을 D-41로 단정(실물 D-38 — 16회차) ②config 출력을 300자에서 절단해 보고 "relation_patterns에 골격 관계 없음"으로 단정(실물에 precedes 실재 — 17회차). 공통 원인 = 안 열었거나 끝까지 안 봄 | 코드·config 상태를 단정하는 문안 발행 전 |

### 검사 로그

| 회차 | 일자 | 범위 | 결과 | 조치 |
|---|---|---|---|---|
| 1 | 2026-07-27 | CH2(개정)·CH3 ↔ 카드 전 그룹 / 분할표 / 용어 | 검사 1 불일치 0. 검사 3 신규 용어 3건 등재 | 카드·용어 대장 동일 커밋 갱신 |
| 2 | 2026-07-27 | 틀 v2→v2.1 + CH3 분할 | 카드 M6·M9 정합 확인. 절 번호 불변으로 상호참조 무손상 | 대장 §0·§5 신설 |
| 3 | 2026-07-27 | 틀 v2.1→v2.2 (A1~A4) | A2↔A4·A4↔쟁점2·A3↔I5 충돌 없음. 신규 용어 4건 | 카드 v4, CH3A 3.4, CH3B 3.7 |
| 4 | 2026-07-27 | **틀 v2.2 초안의 자체 결함 검토** | **결함 5건 발견·수정**(occ / 순환 방어 / 생존자·canonical 분리 / 재파싱 없음 / direction_unverifiable 재인입). 신규 A5·A6 | 틀 v2.2 확정판, 카드 N3·N6·N8·N9·L5·L7~L9 |
| **5** | **2026-07-27** | **세션 1 — CH2 ↔ 명세·정의서·핸드오프·카드 전수** | 검사 3.5(신설): CH2가 틀 v1 시점에 정지 — 확정 6건을 미결로 서술. 검사 1: 카드 내부 모순 1건(I6↔M6). 검사 2: 원천 충돌 2건 + 정의서 §5.1~5.5 미착지, "5지선다"는 오류(6지선다 정본). 검사 3: 미등재 8건. 신규 미결 R9 | CH2 v2 전면 개정 · 카드 v5 · 용어 11건 · 틀 v2.3 · 대장(검사 3.5 신설) |
| 5-B | 2026-07-27 | **5회차 결과물 4종의 자체 검증** | **개정이 만든 결함 12건**(상 1·중 7·하 4). [상] anchor 정의문이 거짓이 됨 | anchor를 "해소"로 재정의 · 틀 A7-4 신설 · `coord_mismatch` 신설 |
| **6** | **2026-07-29** | **전체 정리 패스 (배치 1~7)** | 발견·정정 4건(§7 잔존 서술·폐기 문서 참조·A5 커버리지 구멍·§11/§16 배정) | CH1·CH4~CH7 신설 · CH3A v2·CH3B v2 · 파서_명세 v0.1 |
| **7** | **2026-07-29** | **파서 전달물 경계 결정** | 틀 §1과 충돌 아님. ⑻이 ⑴⑵⑺에 종속 | 파서_명세 v0.2(§9 신설) |
| **8** | **2026-07-30** | **세션 N — [보완필요] ⑴~⑻ 전량 확정** | **판정 26건.** 초안 결함 2건 검출·정정(locate 역산 모호성 / prompt_version 뭉뚱그림). CH3A 검사 1~3 불일치 0 | 파서_명세 v0.5 · CH3A v3 · CH5 v2 · CH6 v2 · 카드 v10 · **§5.6 진행 원칙 신설** |
| **9** | **2026-07-30** | **세션 N+1 — 증분 0 작성·대조 검토·블록 판정** | 원천 결함 2건 발견(구현문서 "12행" 오기 / 대장 "큐 15종" 오기). 사용자 검토 통과(수정 1건) + 블록 판정 3건 | 증분0 v1.1 · 대장 갱신 |
| **10** | **2026-07-31** | **세션 N+2 — 감사 4 (범위 한정 §5.6-③)** | **차단급 3건 즉시 해소**(파서_명세 §2 표본 범위 / coord_mismatch fixture가 orphan_anchor 경로 / K3 문형이 causes 산출). **비차단급 4건 기록.** 그 외 불일치 0 — 통과 | 파서_명세 v0.5.1 · 증분0 v1.2 |
| **11** | **2026-08-01** *(사후 등재 08-06, **08-07 보정**)* | **raw mock 트랙 + 실행 검증 1차 (별도 세션)** — raw 실물 10종 확대 제작 · `verify_roundtrip` · 킷 프롬프트 v0.1→v0.2 · **블라인드 role 판정** · **LLM 산출 adapter/schema의 실행 검증**(정형 ipqc·비정형 toc_report 2건, 하네스 4단) | verify PASS(51항목). 블라인드 1차: UNMAPPABLE 2/2·일치 10/16 → 개정 지시 7건. **v0.2 실행 검증: 1차에서 갈린 4건 중 3건 수렴 — 프롬프트 개정이 판정을 수렴시킨다는 것이 실측됨.** 원천 결함 2건(`expects.columns` 키 미정의 → preflight 무력화 / `section` role 미결) · 프롬프트 결함 1건(UNMAPPABLE 처리 미지시 → `KeyError` 크래시) · 하네스 한계 1건(`use_blocks` 미로더). 가결정 **D-15~D-31 (17건)** 신설. **성공 판정의 정의가 정정됨: LLM 출력의 문면이 아니라 실행 결과로 판정한다.** *(08-06 등재 시 `실행검증_1차.md`를 누락해 가결정 계수를 25건으로 오기 — 13회차에서 31건으로 정정)* | `raw_mock_대장` v2.0 · `판정대조_ipqc_1차` v1.0 · **`실행검증_1차` v1.0** — §1 파생 문서 표 등재 |
| **12** | **2026-08-06** | **세션 N+3 — 쟁점 A(저장 계층) 판정 + D-18·D-20 채택** | A안(GraphStore 경계·orjson·알람선 200MB/30초) · C안(의미 축 ULID — 발효 조건부) · D안(층별 graph.json 유지, 층간 엣지는 가리키는 층 소속) 채택. mock 13종→**15종**. **정합 정정 1건: "C10행" 라벨 충돌**(coord_mismatch vs 버 높이) → CP01 11행·C10 버 높이·C11 좌표 불일치로 확정 | 틀 v2.4 · 카드 v11 · CH6 v3 · CH5 v3 · 증분0 v1.3 · 용어 대장 · 대장 |
| **13** | **2026-08-07** | **세션 N+4 — 검사 5(반입 대조, 신설).** 구현 레포 정비 후 docs/ 18종 내부의 계수·판본·경로 상충 전수 대조 | **상충 9건 검출·전건 판정.** [차단 2] ①DECISIONS 초기값 25 vs 31 → **31건 확정**(`실행검증_1차` 반입 필수) ②raw_mock 요구 판본 v1.1 vs 실물 v2.0 → **v2.0 현행**. [수치 3] ③CP01 record 10 vs 11 → **prefix 11 확정** + **raw 실물에 C11 삽입 지시**(parsed에만 있고 raw에 없는 행은 D-18을 무효화한다) ④킷 5종 vs 6종 → **6종**(D-27) ⑤템플릿 v0.1 지시 vs 실물 v0.2 → **v0.3 개정으로 전진**(D-29·D-30). [배치 4] ⑥`verify_roundtrip` → **`tests/`** ⑦zip 잉여 자산 배치 확정(reader→`parser/` · run_adapter→`kit/` · xlsx_common→`tests/` · LLM 산출물→`mock/fixtures/`, D-26) ⑧raw_mock §7의 D-18·D-20 "판정 대기" → **반영 완료로 정정** ⑨`mock/parsed/` 부재 → **"마이그레이션"이 아니라 신규 작성**. **메타 판정 1건: 발의(origination) vs 정본(authority) 구분을 머리말에 명문화** — 파생 문서의 "원천" 선언은 발의권이며 채택본이 정본이다. **부수 확정: ULID 발효**(A0 계수 ②=0) | `raw_mock_대장` v2.1 · 대장(§1 파생 문서에 `실행검증_1차` 등재 · 11회차 보정 · **검사 5 신설** · §5.4 정정 5항 추가 · 이 회차) · 레포 전달물 3종(CLAUDE.md·MANIFEST·구간별_프롬프트) 갱신. **증분0 v1.4 동기화 5항은 §5.4에 등재 — 레포 사본과 프로젝트 사본을 같은 내용으로 고친다** |
| **14** | **2026-08-07** | **M1 세션 — 적재 전 결정 잔여: 쟁점 B·C 판정 (안건서 복원 포함)** | **쟁점 B 수정 채택**: T0~T3 정의 + 부정형(생산 관측 데이터 미적재·그래프 진입은 문서 경유만) + 현 구축 범위=T0~T2 규정. 원안의 Event Frame T1 예외·로트 T1은 **기각(예외 0)** — 로트 번호는 개체가 아니라 attribute/meta. 3테스트는 운영 규칙이 아니라 **R1 층 설계 게이트로 편입**(세션 4 운반). 승격 절차 불요(필요 시 R1 문제). **쟁점 C 수정 채택**: 등급=문서의 성질(DocRevision 실체화 비채택 — R8 비선결) + 봉투 `classification?` 자리 1칸만(미채움), 권한 일체는 **R12 일괄 이연**. 정정 1건: "필드 없으면 전량 재인입"은 문서 단위에선 과장(doc_id 소급 가능) — 실제 리스크는 섹션 단위 등급뿐. R11(T3 연동)·R12 등록, B-5·C-5 발송은 각 트리거에 묶음. **부수: 안건서가 삭제되어 세션 내 판독 원문에서 복원**(→ 안건/ 폴더 신설, CH7.3 등재 — 구 claude/ 대체) | 틀 v2.5(§4B-A9·A10) · 카드 v12(J11·C16) · CH2 v2.1(2.2 봉투) · CH7 v1.2(7.3 안건/) · 용어 대장(T0~T3·SeriesRef·classification) · 안건서 복원본(판정표 기입) · 대장(이 회차). **CH4는 상태만 하향** — 4.1(계층 서술)·4.7(3테스트 게이트)은 세션 4에서 틀 A9와 함께 동기화 |
| **14-B** | **2026-08-07** *(18회차 복원 — M2 세션이 세션 시작 전 사본 위에 대장을 재구성하며 이 회차 기록이 소실됐다)* | **Cowork 세션 — 문서 체계 정비 집행** | M1 반영 6종의 이중 정본 해소(chat 산출 루트 사본 → chapters/ 동기화 후 삭제 — 검증 2중) · claude/ 폐지 집행: 안건/(여는프롬프트·논의안건서)·archive/(핸드오프 M1·세션기록·반영검토후보·메인스트림)·**참조/ 신설**(벤치마크 3부작 — 판정 종결분이 아니라 현행 정본의 인용 근거물이라 archive와 구분) · 경로 참조 4곳 정정 | CH7 v1.3(7.3 참조/ 신설·7.4 dangling 정정) · 증분0(§0 배제 명단·기준 판본 v2.5/v12) · 미러는 이후 16회차가 claude/로 재정본화(chapters/판 폐기) |
| **15** | **2026-08-10** | **M2 세션 — 골격 두 축 분리(A11 신설) + R9 종결** | **판정: 틀 §4B-A11 신설(A11-1~9)** — ①part_of=구조 / precedes="대표 흐름" 지식(두 축 분리 — ISA-95/88·BOP 계열 정합. seed 배열 선언+비참여 옵트아웃 3종, 나열 자동 파생 폐지) ②**부정형: 공장·세대별 라우팅은 골격에 넣지 않는다**(R13 등록 — 트리거: 공장별 흐름 질의 실수요) ③**극성 표현 3패턴+단극성**(케이스 ①/② 폐지 — 분화 단위는 공정이 아니라 스텝. 명세 §5.2 "단일 스트림"은 조립 관찰의 과일반화로 판명 — 전극~노칭은 극성 라인 완전 분리·병렬) ④순서 파생 규칙(극성 인스턴스 순서는 선언 없이 파생·공유 스텝 합류 자동·해상도 표기) ⑤좌표 닫힌 목록=골격 전 노드(**D5 극성 모호 구조 소멸**) ⑥tier(main/sub/detail) 깊이 파생·process_group=tier:main 조상 ⑦**R9 종결 = D-1 확정 승격**: electrode_type은 구조 필드(원천 다수의견 attribute를 문법적 불가능—한 행 두 entity·attach_to_field 하나—으로 기각) + **polarity 파생 필드**(닫힌 4값) + canonical 비파싱 원칙 ⑧부착 정합(이중 결합 방지·polarity 대조→coord_mismatch) ⑨구조 필드 판정 3조건(C17 — 6지선다 대상 아님). **seed 형식 v3.2 확정**(단일 파일·순서 배열=대표 흐름·마커 닫힌 7종·ALIASES=사전의 원천[장부는 dictionary.json 하나, provenance 태그 구분]) + **조립 seed 실물 확정**(노드 46 — 라미 삭제·전해액주입/실링=패키징 내부·노칭 인스턴스·cathode=양극/anode=음극). **통용성 검토(판정문 §11): 규칙 본체는 축 무관 — 특수 잔재는 축의 이름·값뿐, 확장 조건 충족.** **검출 2건**: M1 핸드오프 지정 번호(D-32·33)와 레포 자체 채번 충돌(전달 시 재채번+허브 확인) · mock raw "실링" 행 재매핑 필요(나머지는 alias 자동 정합 — D-42). **D-31(section)은 M2-후속 이월**(마감선: P트랙 착수 전) | 틀 v2.6(§4B-A11·R9 종결·R13) · 카드 v13(J12·C17 신설, C3·C10·D5·F1·F2·F3 개정, B1·F4 주석) · CH2 v2.2(2.2·2.5·2.6) · CH3B v2.1(3.5 전면 개정) · CH5 v3.1(5.1 규약 8) · 용어 대장(6건 등재·5건 정정) · 대장(이 회차 + 파생 문서 2종 등재) · 판정 기록 `안건/M2_판정문안_초안`(r8·소진) · seed 실물 `안건/skeleton_seed_v2`(v3.2 확정) · **DECISIONS 전달 문안 D-42~D-48(판정문 §8 — 허브 채번 확인 후 반입)**. **CH4는 동기화 목록에 A11 추가**(세션 4에서 A9와 함께) |
| **16** | **2026-08-10** | **M2 반입 — 코드 트랙 전달 및 채번 사고** | **검출 3건 · 판정 4건.** ①**채번 오독 사고**: 구현 트랙 PROGRESS 미러가 G3 자체 검출 3건을 D-39·D-40·D-41로 적었으나 레포 `DECISIONS.md` 실물 말번은 **D-38**이었다(등재된 것은 대소문자 중복 1건뿐). 명세 트랙이 **미러를 장부로 오독**해 판정문 §8에 "레포 말번 = D-41"로 단정하고 전달분을 D-42~D-48로 채번해 발행 → 반입 직전 허브가 실물 대조로 검출. **판정: 소급 등재**(3건은 08-08의 레포 내부 결정이므로 시간순으로 그 자리가 맞고, 번호는 외부 지정이 아니라 레포가 지금 부여하는 것 — 발번처 단일 규칙 유지). 전달①~⑦ = **D-42~D-48** 확정 ②**신규 규칙: PROGRESS 미러에 결정 번호를 적지 않는다** — 미러는 보고이고 장부는 `DECISIONS.md` 하나다. 번호가 필요하면 등재 후 인용한다. 미러가 번호를 선점하면 발번처가 둘이 된다 ③**대장 오기 4곳 정정**: 15회차에 넣은 D-39·D-40 참조가 판정문 §8 **초안** 채번(D-36~D-42)을 가리키고 있었고, 재채번 시 범위 문자열만 치환되어 개별 참조가 따라가지 않았다 → D-45·D-46으로 정정(명세 트랙 결함, 허브가 검출) ④**문서 전달 경로 확정**: **구현 세션은 claude.ai 프로젝트를 읽지 못한다** — MANIFEST의 "프로젝트에서 직접 내려받는다"는 사람 기준 서술이며 구현 세션 기준으로는 성립하지 않는다. 문서는 사람이 레포에 반입하고 실행·검증은 구현 세션이 한다 ⑤**3단계 선행 판정 3건**(명세 트랙이 레포 실물 대조): **A** 골격은 `config.json`의 `skeleton` 블록 안에 있고 별도 `skeleton.json`은 없다 → 트리 데이터를 파일로 분리하고 config엔 선언만 남긴다 **B** `bootstrap.py`에 `cathode`/`anode`를 넣으면 **B1 위반** — 코드가 아는 것은 구문 마커 4종(`::`·`@split`·`@unordered`·`@noflow`)뿐이고 축의 값은 seed의 `POLARITY_LABELS` 키에서 읽는다(identity_axis 일반화의 절반이 여기서 선다) **C** `plant()`가 canonical로 노드를 재사용하는데 v3.2는 canonical 체계 자체가 바뀌므로 기존 그래프 위에 돌리면 옛 8노드가 남아 54가 된다 → **깨끗한 재빌드에서 대조** | 레포 `DECISIONS.md` 38→**48건**(D-39~D-41 소급 등재 + D-42~D-48 반입, 결번 없음) · 대장(이 회차 + D-46 정정 4곳) · 판정문 §8 채번 주석 재작성 · **미러 정본화**(`claude/구현트랙_PROGRESS_미러.md` — 구 `chapters/` 판 폐기) · M2 반입 0~2단계 완료(main `fd7020a`), **3~4단계 별도 세션** |
| **17** | **2026-08-13** | **M2 후속(G4-전) — 판정필요 3·4·5 처리 + 검사 6 신설** | ①**판정필요-3 종결**: seed=선언(A3 경로 ①), 게이트 비경유가 정상 — 단 허브가 명세 전제 오류 검출: relation_patterns에 `Process precedes Process` **실재**(명세 트랙이 config를 300자 절단 관찰 — 검사 6의 근거 사고). 경로 ②(정형 edges)로 골격 흐름 개정 가능하던 구멍 = A11-2 충돌 → 제거 + 게이트 실판정 잠금(D-52) ②**판정필요-4 종결**: seed 키 개명 `TREE`·`AXIS_LABELS`(B1 — 상수 격리 기각, D-53). 노드 필드 polarity·tier는 B1 예외 2호로 명시(카드 v14) ③**stale_skeleton 미신설**(D-54): 잔존은 일회성 마이그레이션 산물 — 개발=깨끗한 재빌드/운영=I1 개명이 정본 경로 ④**A11-9 ② 실발화**: mock C12 행(coord_mismatch — core 0줄, prefix 11→12, D-55·raw_mock_대장 v2.2) ⑤**신규 결함 2건(판정필요-5, 허브 C12 한 행이 검출)**: F3 하향 연쇄 미구현(짝 키가 canonical 전문 — 스펙 유지·구현 캐치업 지시) · **A11-9 ①이 Unit에서 깨짐**(스코프 없는 카테고리에 생략 적용 → 동명 2노드) → **틀 v2.7 개정: 생략은 스코프 카테고리 한정, Unit은 F1 유지**(사용자 판정) ⑥**검사 6 "실물 대조" 신설** ⑦A11-6에 auto alias 유일 조건(D-51 — "비전검사" 모호 실측) | 틀 **v2.7**(A11-9 ① 한정·A11-6 유일 조건) · 카드 **v14**(F1·F3·B1 예외 2호·게이트 주석·재빌드/I1) · 용어(AXIS_LABELS·마커 구현 D-49) · seed **v3.3**(키 개명) · raw_mock_대장 **v2.2**(prefix 12·C12) · 대장(검사 6 + 이 회차). 레포 DECISIONS 51→**55** · 판정필요-5 해소는 G4 0번 |
| **18** | **2026-08-13** | **정합 수리 (Cowork) — 레포 실물 대조 + 문서 6종 동기화** | 레포 main 실물 대조: DECISIONS 55건·BLOCKERS 멈춤 0(판정필요-5만 개방 — 판정은 틀 v2.7로 기완료, **구현이 G4 0번**)·M2 반입 0~4단계+후속 완료 확인. 검출·수리: ①CH3B 3.5가 틀 v2.6 문면(A11-9 ① 무한정)으로 잔존 — §3.6 위반(17회차 커밋이 챕터 미동기화) ②대장 머리말 기준·이력 낡음 ③**14-B 기록 소실**(구판 사본 위 재구성 사고 — 재독 규칙 신설로 대응) ④CH7 행 v1.2 오기(실물 v1.3)·미러 위치 충돌(CH7 7.3 chapters/ ↔ 16회차 claude/ — **나중 판정 우선, claude/ 확정**) ⑤미러 2정지점 미갱신 ⑥여는프롬프트 M1 잔여 낡음(실제 D-36·37 반입 완료) ⑦날짜 오기 2곳(틀 v2.7·17회차 "08-10" → 08-13) | CH3B **v2.2**(레포 재반입 대기 — G4 전) · CH7 **v1.4**(7.3 미러=claude/ 명문화·7.4 갱신) · 미러 갱신(3·4단계+후속 정산) · 대장(기준·이력·14-B·17회차 일자·§1 정정·§3.6 재독 규칙) · 여는프롬프트(M1·M2 종결) · 증분0 §0(claude/ 배제 명단) · 틀 v2.7 일자 정정 · **§7 ③ 현황 동기화**(낡은 행 3건 정정 + 누락 4행 추가 — G4 0번·레포 재반입 4종·MANIFEST 개정·파일명 정합) · §5.4에 M2 후속 3줄(D-52·53·55) · §1 파생 표 증분0 판본 |

---

## 5. 검토·상세화 계획 (세션 단위)

### 5.1 세션 목록

| 세션 | 대상 | 성격 | 상태 |
|---|---|---|---|
| 1 | CH2 | **동기화** | **진행 중** — 잔여는 §5.3. **U7은 M2로 종결. 신규 유입: D-31(`section`의 role) — M2-후속으로 이월** |
| **1P** | **파서_명세.md** | **파생** | §5·§6·§7 확정(세션 N). **개정 대기 3건: §9 킷 6종(D-27) · §5 `header_labels` 신설(D-29) · §5 좌표 목록·템플릿 v0.3 구조 필드(M2)** |
| 2 | CH3A | **동기화** | **완료 (세션 N ⑶)** |
| 3 | CH3B | **동기화** | **부분 수행** — **R9는 M2로 종결(3.5 반영 완료)**. 3.7 큐 표 미반영 |
| 4 | CH4 4.7 + R1 + [보완필요④] | **설계** | **이관 확정(⑷-A).** 트리거: 코드 트랙 G4 완료 후. **A9(3테스트)·A11(골격 작성 규칙 v3.2) 동기화 포함** |
| 5 | ~~CH5 서술~~ | 서술 | **완료** — 계기판 8종은 N+3, 순서 파생은 M2 |
| 6 | ~~CH6+CH7 서술~~ | 서술 | **완료** — 심화 재검토는 감사 4에 흡수 |
| 7 | ~~CH1 서술~~ + 정합 감사 4 | 서술+감사 | **완료 (N+2)** |
| **N** | **[보완필요] ⑴~⑻** | **설계** | **완료 (07-30)** — 판정 26건 |
| **N+1** | **증분 0 작성** | 파생 | **완료 (07-30)** |
| **N+2** | 감사 4 | 감사 | **완료 (07-31)** |
| **raw / 실행검증** | **raw mock 실물 + 킷 v0.1·v0.2 + 블라인드 판정 + 실행 검증** | 파생 | **완료 (08-01 — 11회차).** 잔여: **템플릿 v0.3(D-29·D-30·D-46) + 3차 판정** — P트랙과 병행 |
| **N+3** | **적재 전 결정 3건 (쟁점 A·B·C)** | 설계 | **전건 완료** — A(08-06, 12회차) · **B·C(08-07, M1 세션 — 14회차)** |
| **N+4** | **반입 대조 (검사 5)** | 감사 | **완료 (08-07 — 13회차, 상충 9건 판정)** |
| **M2** | **골격 두 축 분리(A11) + R9 종결** | 설계 | **완료 (08-10 — 15회차).** seed v3.2 실물 확정 포함. **후속(M2-후속): D-31(section) — 마감선 P트랙 착수 전** |
| 8 | 명세 v1.19 분해·클린업 | **분해** | **최후 — 코드가 돌아간 뒤** |

### 5.2 성격별 규칙

| 성격 | 착수 절차 | 완료 판정 | 초안 선작성 |
|---|---|---|---|
| **동기화** | 검사 3.5 → 원천 전수 대조 → 카드 교차 대조 | 누락 0 + 카드 정합 + 표기 일치 | **위험** |
| **설계** | 미결의 선택지와 판정 기준을 먼저 만든다 | 확정으로 바뀌고 틀·카드 반영 | **불가**(단 [보완필요] 초안은 예외) |
| **서술** | 명세가 정본이므로 대조가 아니라 **인용** | §3.3·§3.4 규격 충족 | **안전** |
| **파생·분해** | 상속 전제를 먼저 고정 | 원천에 복제 없이 §참조로만 연결 | 안전 |
| **감사** | 범위를 먼저 한정(§5.6-③) | 차단급 0 + 비차단급 기록 | — |

### 5.3 세션 1 잔여 진행판 (CH2)

| 묶음 | 담는 것 | 판정 | 상태 |
|---|---|---|---|
| **P · 파서 계약** | U1 공정좌표 체계 · U2 복수값 전개 | 2건 | 진행 중 — 가결정 D-11·D-12 |
| **V · 검증과 경계** | U3 인입 검증 순서 · U5 "오프라인" 표현 | 2건 | 대기 — D-2. preflight(C15)와 인입 검증 3단의 순서 정합을 함께 볼 것. **U7(electrode_type)은 M2로 종결(A11-8)** |
| **L · 설계 시점의 LLM과 사람** | U4 판단 기준 3개 | 1건 | U6은 세션 N ⑺로 닫힘 |
| **S · 구조 필드의 지위 (신설 13회차)** | **D-31 `section`의 role** | 1건 | `electrode_type`과 같은 성격 — 계약 고정 필드인데 role 미정. **M2-후속으로 이월(마감선: P트랙 착수 전 — section 안정성 요구가 파서 계약·템플릿 문안이므로). CH2 2.2 + CH5(하이브리드 서치)와 함께 판정. 판정 틀(구조 필드 3조건 — C17)은 M2가 마련함** |

**묶음 운영 규칙**: ①상태는 대화가 아니라 이 표에 있다 ②묶음마다 원천을 다시 읽는다 ③판정은 그 자리에서 반영한다 ④묶음 경계에서 대화를 끊어도 된다.

### 5.4 세션 8로 넘길 원천 정정 목록 (누적)

- 정의서 §8.2 "5지선다" → **6지선다**
- `chunk_id` → **`source_locator`** 개명 (정의서·구현문서·파서_인수인계 전역)
- 구현문서 §2.4 `common_core`를 `{source_locator: meta}`로 정정
- 명세 §6.2 조각 공통에 `payload_kind`·`adapter_version` 추가
- 명세 §7-7을 틀 §3으로 교체 / anchor 서술을 "해소"로 정정
- 구현문서 §2.3 큐 kind에 `coord_mismatch`·`parse_failure`·`adapter_mismatch` 추가
- 명세 §7의 "운영 시점 청크 분할 보조" 전수 제거 / §13→CH7.2 / §14에 증분 0 / 추출 후보 JSON 절 신설
- "파서는 사내 구현" → "사내 작성은 어댑터뿐(코어·킷은 외부 전달물)"
- 명세 §14·§15에 부트스트랩 / §6.7에 등록 입력 패키지·층 선행 완결
- 명세 §6.7·정의서 §8에 검수 뷰 확정판(3구획·3층·상한 없음·JSON/HTML)
- 구현문서 §6.1 "records **12행**" → **13행** / 큐 총수 "17종 확정 + 승인 3종 = 20종"
- **(감사 4)** CH3B 3.7 큐 표·"택일 대기" 동기화 / CH6 6.2 n9 "(D-1)" 앵커 정정 / occ 정의문 정밀화
- **(N+3)** 의미 축 N#### → **ULID**, `id_seq.json` 폐지 / graph.json 직접 접근 서술 → **GraphStore 경유** / 계기판 6종 → **8종** / mock 13종 → **15종**
- **(11회차 — raw mock)** 구현문서 §6.2 "records 6행" → **9행**(실제 표 C1~C9) / CP01 서술 → **11 record**(C10 버 높이·C11 좌표 불일치)
- **(13회차 — 반입 대조) 증분0 v1.4 동기화 5항** ①§7 헤더: 가결정 대장의 소유 분담 명시 + **DECISIONS 초기값 31건**(D-1~D-14 이 절 / D-15~D-21 raw_mock §6 / D-22~D-25 판정대조 §8 / **D-26~D-31 실행검증_1차 §6**) ②§3 P2: **킷 6종**(5종 + 실행 하네스 `run_adapter.py` — D-27) ③§4.1 머리: "기존 mock 마이그레이션" → **"mock/parsed 신규 작성"**(레포에 기존 parsed 저장분이 없다 — 구현문서 §6 mock 전문을 기준으로 §4.1의 4항을 적용한 상태로 새로 만든다) ④§4.1-4: CP01 11행은 **raw CP01.xlsx의 11번째 record와 동일**해야 한다(prefix 11 — raw에 C11 삽입, 확대분 C12~C29) ⑤§5-2: **fixture의 정의 = 외부 세션에서 실제 LLM이 산출한 결과물의 스냅샷**(D-26 — 사람이 손으로 쓴 fixture는 리허설을 무효화한다)
- **(13회차)** 파서_명세 §9 킷 **6종**(D-27) · §5 `expects`에 **`header_labels` 필수 키 신설**(D-29 — `columns`는 출력 필드명 매핑으로 용도 확정. 두 관심사가 한 키에 섞여 preflight가 무력화되었다) · CH2 2.6 규약 5(UNMAPPABLE은 스키마에 넣지 않음)를 **템플릿 v0.3에 명시**(D-30) + 인입 코드에 `invalid_role` 방어
- **(M2 — 15회차)** 명세 §5.2: "흐름은 단일 스트림" = 조립 관찰의 과일반화 → A11-2·4로 교체 / 케이스 ①/② 이분법 폐지 → 극성 3패턴(A11-4) / "2단으로 시작" → 3단 기본·가변(A11-1) / "PoC 첫 골격 = 노드 7개" → seed v3.2 기준(46) 재기재 / 극성 결합에 polarity 필드 기록·이중 결합 방지(A11-9) 추가
- **(M2)** 명세 §5.3: precedes "seed 나열 순서 자동" → "seed 배열 선언 + 비참여 옵트아웃, 대표 흐름 지위"(문서 추출 금지 유지) / mirrors 규칙 ② → polarity 필드 비교+하향 연쇄(+Tier1 단극성 제외)
- **(M2)** 명세 §5.4-3·CH2 2.2: 좌표 닫힌 목록 "세부공정" → "골격 전 노드" / process_group "골격 트리 부모 파생" → "tier:main 조상 파생"
- **(M2)** 명세 §6.2 / 정의서 §5.1 / 구현문서 §2.4: electrode_type 서술 교체(구조 필드 + polarity 파생 필드 — A11-8) / 구현문서 §5 skeleton 형식 → v3.2(순서 배열 + ALIASES 블록)
- **(M2)** 골격 유일화 구분자 `-`(예: "스태킹-탭정렬") → `::` 통일
- **(M2)** 구현문서 §6.4 mock 스모크 12문항 중 flow 관련 문항 점검·정정(개념 레벨 흐름 기준) + **mock raw "실링" 행 재매핑**(D-42 — 허브)
- **(M2 후속 — 17회차)** seed 형식의 블록 이름 `PROCESS_TREE` → **`TREE`** · `POLARITY_LABELS` → **`AXIS_LABELS`**(D-53 — B1). **M2 판정문 §1-12·§4·§8의 형식 서술과 이 대장 §1 파생 표의 표기를 함께 고친다** — 레포가 "판정문·대장의 형식 서술 갱신은 명세 트랙 몫"으로 남겼다
- **(M2 후속)** `raw_mock_대장` §6 D-15·D-18의 CP01 prefix **11 → 12**(D-55 — A11-9 ② 실발화 행 C12 삽입). raw_mock v2.2는 반영 완료, **레포 DECISIONS 해당 행 문면 정리는 G4 0번**
- **(M2 후속)** 명세·킷 템플릿에 **"골격 관계(`Process part_of/precedes/mirrors Process`)는 `relation_patterns`에 넣지 않는다"** 명문화(D-52 — 경로 ②가 골격 흐름을 개정할 수 있던 구멍을 봉합). 층의 관계 선언은 config `relations` 배열이 하고 `relation_patterns`는 추출용 게이트 패턴이다

### 5.5 순서 근거

- 전체 정리 패스(배치 1~7)로 서술을 선완료했다 — CH4~7이 틀 v2.2·v2.3과 한 번도 대조되지 않은 상태로는 [보완필요]와 구현 범위를 확정할 수 없었다.
- **파서 명세를 1P로 당긴 근거**: ①묶음 P·L의 결정이 전부 파서 계약 문제다 ②LLM이 어댑터를 생성하는 방식이라 계약의 빈틈이 곧바로 임의 구현으로 메워진다 ③파서 구현은 사내 최대 조각이다 ④CH2가 닫히는 순간이 상속 전제가 확정되는 순간이다.
- **세션 종료 규칙**: 그 세션의 결정은 같은 커밋에서 카드·틀·대장에 반영하고 끝낸다. **파생 문서를 만들었으면 §1 표 등재까지가 종료다** — 11회차의 `실행검증_1차` 누락이 이 규칙 위반의 실례이고, 그 누락이 6일 뒤 구현 레포에서 "DECISIONS 25건 vs 31건" 상충으로 되돌아왔다(13회차).

### 5.6 진행 원칙 — 전량 구현 + 충돌 등급 분류 [사용자 확정 · 2026-07-30]

1. **해야 할 것은 다 하고 넘어간다 — 범위는 전 기능이되 깊이는 무한하지 않다.**
2. **명세 문서 다듬기는 차단물이 아니다.**
3. **충돌은 등급으로 가른다**: 차단급(계약·불변식 모순) = 즉시 해소 / 비차단급 = 기록하고 전진. 감사도 범위 한정.
4. **"정말 돌아가는가"의 최종 판정은 문서 검토가 아니라 실물 투입이다.** **[11회차 보강] 등록 산출물의 판정도 같다 — LLM이 짠 스키마·어댑터는 문면이 아니라 실행 결과로 판정한다**(`실행검증_1차` §1).
5. **3요소(인풋·조절점·아웃풋)를 단위 정의에 내장한다.**
6. **구현 중 막힘**: 카드·계약과 충돌 = BLOCKERS(멈춤, **판단 재료 3요소 첨부**) / 그 외 세부 = 가결정 + DECISIONS(전진).

---

## 6. 세션 운영 프로토콜

### 6.1 여는 문장

> `chapters/00_틀_확정본.md`, `chapters/00_불변식_카드.md`, `chapters/00_용어_대장.md` 기준으로 진행한다.
> 세션 N — `chapters/CH#_____.md` 검토(또는 상세화).
> **대조 원천**: 대장 §1의 해당 챕터 소유 절 + `role_어휘_정의서.md`·`ontology_system_구현용문서.md`의 해당 부분.
> 대장 §5 계획, §6 운영 프로토콜을 따른다.

**착수 시 첫 동작은 검사 3.5(버전 대조)다.** 이전 대화의 내용을 요약해 옮기지 않는다 — 요약이 곧 왜곡이고, 현재 진실은 언제나 프로젝트 문서에 있다.

### 6.2 파일 명명 규칙

| 접두 | 대상 | 예 |
|---|---|---|
| `00_` | 기준 문서 | `00_틀_확정본` · `00_불변식_카드` · `00_챕터_대장` · `00_용어_대장` |
| `CH#_` | 챕터 | `CH2_문서계약` · `CH3A_인입절차` |
| `archive/` | 보관 | `archive/제작기록/...` |

- **버전은 파일명이 아니라 문서 머리(`version:`)에 넣는다.**
- 절 번호는 분할·병합·재정의되어도 유지한다.
- **구현 레포로 반입할 때는 소문자 `.md`·평면 `docs/`로 정규화한다**(Ubuntu는 대소문자를 구분한다 — 13회차).

### 6.3 outputs와 프로젝트

- **프로젝트 = 원본(현재 진실). outputs·구현 레포 사본 = 그 시점의 사본.**
- **레포 사본을 고쳤으면 같은 커밋 정신으로 프로젝트 원본도 고친다** — 두 곳이 갈라지면 다음 세션이 어느 쪽을 믿을지 알 수 없다(13회차).

### 6.4 세션 진행 방식

1. **검토는 절 단위로.** 판정을 요청할 때는 판단 재료(실물 서술·선택지별 귀결)를 함께 제시한다.
2. **결정이 나면 그 자리에서 반영한다.**
3. **세션 종료 시**: 대장 §5 상태 갱신 + §4 검사 로그 1줄 + §5.4 원천 정정 목록 추가 + **파생 문서 §1 등재**.
4. **문맥이 틀어졌다고 느끼면** `00_틀_확정본` §1~§4로 돌아간다.

---

## 7. 구현 전환 분류표

> 상세 현황판은 **CH7.4가 정본**이고 이 표는 대장 수준의 집계다. **구현 범위는 전 기능이다 — 축소 없음.**

### ① 돌릴 것 — 확정 서술 완료, 구현 착수 가능

| 영역 | 원천 절 |
|---|---|
| 인입 진입 검증 3단 + 산식 | CH2 2.8, CH6 6.1 |
| 파서 출력 계약 3종 + source_locator 체제 | CH2 2.2 |
| role 5종+edges, 핸들러 루프, 매칭 스키마 문법 | CH2 2.4·2.5·2.8 |
| 3단계 파이프라인·2-pass·판정 3분기·커밋 게이트 | CH3A 3.1~3.4 |
| 추출 후보 JSON 체크포인트 계약 | CH3A 3.11 |
| 극성·canonical·맥락·큐 20종·I축 4연산·재인입 | CH3B 전체, 증분 0 §6-5 |
| **골격 두 축 분리·seed 형식 v3.2·polarity 파생 필드·순서 파생·부착 정합** | **틀 §4B-A11, 카드 J12·C10·C17, CH3B 3.5, 안건/skeleton_seed_v2(v3.2), D-42~D-48** |
| 층 config-only·여섯 칸·층간 연결 | CH4 4.1~4.6 |
| **저장 계층 — GraphStore 경계·층별 graph.json·ULID(발효 확정)·알람선(R10)** | 틀 §4B-A8, 카드 B6·N1, CH6 6.1 규약 6 |
| 질의 4단·이원 채널·**계기판 8종**·골든셋·BM-25 | CH5 전체 |
| 코드 배치·mock 원칙·**단위 10종+st·mock 15종** | CH6 전체(6.5 제외) |
| **파서 공용 코어 6종 + 생성 하네스** | 파서_명세 §3·§9 |
| 어댑터 인터페이스·등록 입력 패키지·검수 뷰·부트스트랩 | 파서_명세 §5·§6·§7 |
| **어댑터 생성 킷 6종**(5 + 실행 하네스 — D-27). 템플릿은 v0.2 실측 완료 → **v0.3** | 파서_명세 §9, 실행검증_1차 |
| **raw mock 실물 10종 + verify_roundtrip + 봉인 정답표 + LLM 산출물 스냅샷** | raw_mock_대장 v2.1, 실행검증_1차 |
| **구현 단위 통합 배열·3요소·mock 행별 주석·가결정 31건** | **증분0_구현 v1.3 (+ §5.4의 v1.4 동기화 5항)** |

### ② 보완할 것 — **세션 N(07-30)으로 전량 종결**

| # | 항목 | 종결 상태 |
|---|---|---|
| ⑴ | 어댑터 인터페이스 | **확정** — 파서_명세 §5, 카드 C11~C15. *(D-29 `header_labels` 보강 대기)* |
| ⑵ | 등록 세션 입력 패키지 | **확정** — 파서_명세 §6, 카드 M10·J10 |
| ⑶ | 추출 후보 JSON 계약 | **확정** — CH3A 3.11, 카드 M12 |
| ⑷ | 신규 층 등록 입력 형식 | **세션 4 이관(⑷-A)** |
| ⑸ | 신규 구현 단위·mock 확장 | **확정** — 단위 10종+st · 시나리오 **15종** |
| ⑹ | 골든셋·BM-25 | **확정(BM-25) + 가결정(구성 120)** |
| ⑺ | 검수 뷰 규격 | **확정** — 파서_명세 §7, 카드 M11. *(실행 하네스가 그 앞단에 신설됨 — D-27)* |
| ⑻ | 어댑터 생성 킷 | **구현 단위 n8로 전환.** 템플릿 v0.2까지 실측 완료 |

### ③ 추가 작성할 것

| 항목 | 시점 |
|---|---|
| ~~증분 0 / 감사 4 / 쟁점 A / 반입 대조 / 쟁점 B·C / 골격 A11·R9~~ | **완료** (v1.3 · 10회차 · 12회차 · 13회차 · 14회차 · **15회차**) |
| **증분0 v1.4 동기화 5항** | **프로젝트판 반영 완료.** 레포 사본은 판번호만 v1.4이고 본문은 구판이다(§0 기준 틀 v2.4/카드 v11 · 킷 5종 v0.1 · raw_mock v1.1 · 실행검증_1차 미등재 — 131줄 차이). **판번호가 같아 MANIFEST 판본 대조를 통과한다** — 교체 반입 필요(18회차 검출) |
| **템플릿 v0.3 + 3차 판정** | D-29·D-30·**D-46(구조 필드)** 반영 — P2와 병행 |
| CH2 잔여 묶음(P·V·L) 판정 | U1·U2·U3은 가결정 처리 완료. ~~U7~~ **M2 종결** |
| **D-31(`section`) 확정 — M2-후속** | **마감선: P트랙 착수 전** (판정 틀 C17은 준비됨) |
| ~~seed v3.2 → n10 재실행 + G3 기대값 정합 + mock "실링" 행 교체~~ | **완료 (08-12 — M2 반입 3·4단계).** loader v3.2 전면 개정 · n10 기대값 5종 전건 일치(노드 46 · part_of 45 · precedes 22 · polarity≠none 16 · mirrors 쌍 8, 깨끗한 재빌드) · 1c′ M2 반영(polarity 4값·A11-9 ①②·strip_polarity 폐지) · 실링 raw 7파일 23행 + parsed prose 2건 교체. PR #5 머지 08-13 |
| 구현 레포 상태 회수 → CH6 6.5 확정 | **G0~G3 완료(08-08) · M2 반입 0~4단계 완료(08-12) · M2 후속(G4 전 정리) 완료(08-13, DECISIONS 55·BLOCKERS 멈춤 0).** 미러는 `claude/구현트랙_PROGRESS_미러.md`(16회차 정본화). **G4는 별도 승인 대기 — 착수 금지 상태** |
| 명세 v1.19 분해·클린업 | **최후 — 코드가 돌아간 뒤** |
| **G4 0번 — 판정필요-5 구현** | **판정은 이미 끝났고 구현만 남았다.** ①F3 하향 연쇄(짝 키 `base_canonical`에서 부모 축값 제거 — 부모가 mirror 쌍일 때) ②A11-9 ① Unit 한정(틀 v2.7·CH3B v2.2) ③판정필요-5 종결 등재 ④레포 DECISIONS D-15·D-18 행의 prefix 문면 11→12 정리(D-55 지시 잔여). **G4 착수 첫 작업** |
| **레포 문서 재반입 4종** | `증분0_구현`(구 본문) · `00_챕터_대장`(15→18회차) · **`CH3B_인입규칙` v2.2(G4 전 필수 — 극성 결합 구현이 읽는 절)** · `CH7_운영`(v1.2→v1.4) + 틀 날짜 2곳. **사람이 반입한다** — 구현 세션은 claude.ai 프로젝트를 읽지 못한다(16회차 확정) |
| **MANIFEST 개정** | 요구 판본표가 구판(틀 v2.4 · 카드 v11 · 증분0 v1.3 · raw_mock v2.1)이라 "넣은 뒤 확인"을 그대로 수행하면 **최신본을 구판으로 되돌리라는 지시**가 된다 — 검사 5를 무력화한다. 반입 목록도 실물과 어긋남: "넣지 않는 것"에 있는 `00_챕터_대장`·`CH7_운영`이 docs/에 실재(G3에서 편입 판정했으나 MANIFEST 미갱신)하고, `M2_판정문안`·`skeleton_seed`는 표에 없는 반입물이다 |
| **파일명 정합 2건** | ①레포 `M2_판정문헌_초안.md` — **"문안"의 오타**. 틀 §4B-A11 배경·카드 v13 근거·seed 상태줄이 전부 `안건/M2_판정문안_초안.md`로 참조하므로 레포에서 추적이 끊긴다(D-38 대소문자 사고와 같은 계열) ②seed 파일명 — 레포 `skeleton_seed.md` ↔ 프로젝트 `안건/skeleton_seed_v2.md`(내용은 양쪽 v3.3 동일). **양쪽 문서 내부 참조가 아직 "skeleton_seed_v2.md (v3.2)"** 라 재반입으로는 안 고쳐진다 |