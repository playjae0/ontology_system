# 감사 2차 — 명세↔코드 갭 (HEAD a40ff6d (B54 08e864e 포함), 2026-09-09)

- 대상: 레포 `/home/claude/repo` HEAD a40ff6d (B54 08e864e 포함). 명세 정본 `docs/spec/` 11종. 판정 기준 CLAUDE.md §3(주석·문자열은 구현이 아니다 · 호출부 0은 구현이 아니다 · 단정은 코드 전문을 열고).
- 입력: (A) high 27건의 반박 검증 결과(confirmed/partial/refuted) · (B) med/low 227건 **미검증** 원자료. 본 보고서는 (B)를 검증하지 않았다 — 미검증 표시 그대로 나열한다.
- 읽기 전용 감사 — git 조작·data/·parsed/·extract/·review/ 쓰기·tests 실행 없음. "실행 확인"이라 적힌 것은 메모리 내 python -c 또는 스텁 재현이다.

## 한눈에

감사 22절 · 검출 254건 (high 27 · med 110 · low 117). 입력 자료에 절(seg) 이름이 나타난 것은 21개다.

### high 검증 결과

| Verdict | Count |
|---|---|
| confirmed | 26 |
| partial | 1 |
| refuted | 0 |
| **합계** | **27** |

검증자 재판정 심각도(입력 verdict.severity): high 유지 17 · med로 하향 10 (H3·H11·H12·H14·H16·H18·H19·H21·H23·H26). 고칠 쪽(fix_owner): code 19 · both 8 · spec/doc 단독 0.

같은 사안의 다절 중복(high 안에서): H2·H3·H22·H25 = 파서 측 실패의 큐 미착지(doc2-contractA ×2 · doc7-layout · doc7-outputs) / H9·H10 = matcher 사전 갈래 is_live 누락(doc4-write · doc4-queue-ops). 삭제하지 않고 모두 실었다.

### 절별 검출

| Seg | Found | high | med | low |
|---|---|---|---|---|
| doc1-invariants | 9 | 1 | 3 | 5 |
| doc2-roles-schema | 13 | 1 | 5 | 7 |
| doc2-contractA | 9 | 3 | 3 | 3 |
| doc3-config | 10 | 0 | 3 | 7 |
| doc3-skeleton | 10 | 0 | 5 | 5 |
| doc4-write | 17 | 5 | 6 | 6 |
| doc4-queue-ops | 15 | 3 | 5 | 7 |
| doc4-extract | 9 | 1 | 3 | 5 |
| doc5-query | 10 | 1 | 5 | 4 |
| doc5-gauges | 9 | 2 | 3 | 4 |
| doc6-parser-core | 13 | 1 | 7 | 5 |
| doc6-adapters | 10 | 1 | 4 | 5 |
| doc6-ingest-routing | 11 | 1 | 4 | 6 |
| doc6-register | 13 | 1 | 7 | 5 |
| doc6-review-view | 14 | 1 | 5 | 8 |
| doc6-kit | 16 | 0 | 9 | 7 |
| doc7-layout | 13 | 1 | 7 | 5 |
| doc7-ids-records | 7 | 0 | 4 | 3 |
| doc7-llm | 17 | 1 | 7 | 9 |
| doc7-outputs | 15 | 2 | 8 | 5 |
| guide-vs-code | 14 | 1 | 7 | 6 |
| **합계** | **254** | **27** | **110** | **117** |

## 위계 2 — high 확정·부분

각 건: 제목 / 종류 / 명세 인용(절) / 코드 좌표 / 무엇이 어긋났나(partial이면 corrected_claim) / 고칠 쪽 / 확인 방법 / 검증자 근거 한 줄. 「재판정」은 검증자가 매긴 severity.

### H1. process_ref가 null/부재인 행 — missing_field 없이 Property entity·보류 attribute·생략 엣지가 큐 0건으로 사라진다
- **판정**: confirmed · 재판정 high · seg doc2-roles-schema
- **종류**: spec_vs_code
- **명세 인용**: 문서 2 §2.7-③ · §2.4-① · §2.5 규약 3 — 「스키마에 있는데 없음 → optional 검사, 미선언이면 missing_field 큐 / 미해소는 레코드 전체를 보류하지 않고 해당 엣지만 생략한다 — 좌표만 못 붙은 행의 entity·attribute·content까지 함께 사라지면 큐에는 … 조용히 버린 사실이 큐 화면에 드러나지 않는다」
- **코드 좌표**: core/pipeline.py:24-25,55-60 (STRUCTURAL에 process_ref 포함 → missing_field 검사 제외) · core/build.py:123-124 (surface 비면 큐 없이 None) · core/pipeline.py:280-284 (scoped entity 드롭) · core/pipeline.py:459-467 (pending 적재) · core/pipeline.py:192-202,388-389 (_land_deferred는 defer 항목이 있을 때만 dropped/pending/dropped_entities를 싣는다)
- **무엇이 어긋났나**: 블록 process_coord의 process_ref는 optional 없는 anchor 필드인데 코드는 STRUCTURAL로 취급해 부재·null에 missing_field를 올리지 않는다. 그 상태에서 h_entity는 canonical_scope 카테고리(Property) entity를 dropped_entities로만 보내고, _row_attributes는 그 entity에 붙는 attribute를 r.pending으로만 보내며, _row_edges는 @process_ref 엣지를 r.dropped로만 보낸다. 그런데 _land_deferred는 r.defer(orphan_anchor 항목)가 있을 때만 이 셋을 payload에 싣고, process_ref가 null이면 resolve_anchor가 큐 항목을 만들지 않으므로 defer가 비어 셋 다 어디에도 기록되지 않는다. 계약 ②는 '해당 없으면 null'을 정상 출력으로 허용하므로 실물에서 도달 가능한 경로다.
- **검증자 보정**: `fields`는 스키마 JSON의 fields뿐이고 use_blocks를 전개하는 코드가 core/에 없어 블록 필드는 missing_field 루프에 아예 오르지 않는다(설령 올라도 STRUCTURAL로 건너뜀). null 좌표의 연쇄 드롭 재료를 보유할 큐 kind는 명세(§2.4-③은 orphan_anchor·orphan_attach만)에도 없고 docs/국면2_명세공백_인계.md:60-61에 공백으로 등재돼 있다 — missing_field 미발생·STRUCTURAL 오분류는 코드 결함, 착지 kind는 명세가 정해야 한다.
- **고칠 쪽**: both
- **확인 방법**: core/pipeline.py 24-25·55-60·280-284·388-389·192-203과 core/build.py 123-124를 읽는다. 실행 확인은 클린 data에서 `process_ref: null` + Property entity 행 하나짜리 table 봉투를 run_document로 넣고 review_queue.json 항목 0건·그래프 노드 0건인지 본다(쓰기 명령이라 감사에서는 돌리지 않았다).
- **검증자 근거**: build.py:123 `if not surface: return None, None`(defer 미적재) → pipeline.py:280 dropped_entities → pipeline.py:388 `_land_deferred(r.defer=[], …)`는 `for item in defer:` 안에서만 enqueue; schemas/blocks.json:13-16 process_ref는 role anchor·optional 없음; gate.py:114-119는 끝점 None이면 log_reject만.

### H2. 파서 측 실패(parse_failure·adapter_mismatch)가 review_queue에 착지하지 않는다 — 코드 docstring이 명세와 반대 원칙을 선언
- **판정**: confirmed · 재판정 high · seg doc2-contractA (중복: H3 · H22 · H25)
- **종류**: spec_vs_code
- **명세 인용**: 문서 2 §2.7 「인입 검증의 전체 배열」 (+ 문서 7 §7.1 core/store.py 행) — 「파서 공용 코어가 전 층 공유 단일 큐(review_queue)에 parse_failure·adapter_mismatch·hierarchy_unresolved 항목을 직접 착지시킨다(같은 data/ 위에서 실행되므로 에이전트가 대신 적재하는 단계가 없다)」
- **코드 좌표**: parser/pipeline.py:8 (docstring 「파서는 큐 파일을 직접 쓰지 않는다(D-9)」) · parser/pipeline.py:169,184,238 (res.fail만) · cli/ingest.py:141-145 (print 후 return) · cli/parse.py:115-118 (print만) · core/pipeline.py:767 (유일한 enqueue('parse_failure') — 에이전트 측 payload_kind·미등록 doc_type 전용)
- **무엇이 어긋났나**: preflight 불일치(adapter_mismatch)·계약 self-check 실패·extract 예외(parse_failure)는 ParseResult.failures에만 남고 어느 호출부도 store.enqueue하지 않는다. 명세는 파서 공용 코어가 review_queue에 직접 착지시킨다고 하고 문서 7 §7.1은 store.py를 「파서 공용 코어와 에이전트 두 프로세스가 함께 쓰는 유일한 공유 상태」로 규정하는데, 코드는 D-9를 근거로 정반대 원칙을 선언하고 있다. 결과: ingest-dir로 100부 투입 중 파싱 실패한 문서는 stdout 한 줄과 행 status=FAIL로만 남고 큐·data/ 어디에도 없다 — parsed/{doc_id}.json도 res.ok일 때만 쓰이므로(cli/parse.py:80) 「조용한 소실 금지」가 파일 단위에서 깨진다.
- **검증자 보정**: hierarchy_unresolved도 같은 공백(파서는 meta 플래그+res.failures에만, core는 그 플래그를 읽지 않음) — 파서 측 3종 전부 미착지. 「대신 싣는다」고 적힌 에이전트 측(cli/ingest.py)도 싣지 않으므로 A1과 §7.1의 착지 주체 긴장과 무관하게 결함은 코드다.
- **고칠 쪽**: code
- **확인 방법**: `grep -rn "enqueue(\"parse_failure\"\|enqueue(\"adapter_mismatch\"" --include=*.py .` → core/pipeline.py:767 1건. parser/pipeline.py 1~13행과 cli/ingest.py 139~145행을 연다. 양식 표류 문서를 ingest-file로 넣으면 review_queue.json에 adapter_mismatch 항목이 생기지 않는다(실행 금지로 돌리지 않음).
- **검증자 근거**: docs/spec/2_계약.md:238·7_구현규격과검증.md:60·6_파서와구축모드.md:92,94·1_금지와불변.md:85-86(C14·C15)·개정대장.md:70이 일관되게 큐 착지를 요구; parser/pipeline.py는 core를 import하지 않고(grep 0), cli/ingest.py:140-145는 enqueue 없이 return, adapter_mismatch·hierarchy_unresolved enqueue는 레포 전체 0건.

### H3. hierarchy_unresolved 큐 kind가 코드 어디서도 적재되지 않는다 — meta 플래그와 ParseResult.failures에만 존재
- **판정**: confirmed · 재판정 med · seg doc2-contractA (중복: H2 · H22 · H25)
- **종류**: code_missing
- **명세 인용**: 문서 2 §2.7 「인입 검증의 전체 배열」 · 문서 6 §6.3 (44행) · 문서 4 §4.7-3 닫힌 20종 표 — 「parse_failure·adapter_mismatch·hierarchy_unresolved 항목을 직접 착지시킨다 / 실패 시: 평면 폴백 + 기존 큐(hierarchy_unresolved·parse_failure — 신설 없음)」
- **코드 좌표**: parser/pipeline.py:226-231 (res.fail('hierarchy_unresolved') — 큐 미적재) · parser/adapters/basic_ppt.py:252,256 · parser/struct_map.py:330 (meta 플래그만) · core/ingest.py·core/pipeline.py (hierarchy_unresolved 문자열 0회)
- **무엇이 어긋났나**: 구조 미확정 문서는 res.ok=True로 인입까지 정상 진행되고(D-5 대로 문서는 들어간다), 그 사실은 청크 meta.hierarchy_unresolved=True와 ParseResult.failures에만 남는다. 명세가 「기존 큐로 뜬다」고 한 hierarchy_unresolved 항목은 어느 프로세스도 store.enqueue하지 않아 닫힌 20종 중 도달 불가능한 kind다. 사람이 큐 화면에서 「평면 폴백된 문서」를 볼 길이 없다.
- **고칠 쪽**: code
- **확인 방법**: `grep -rn "hierarchy_unresolved" --include=*.py core cli parser | grep enqueue` → 0건. parser/pipeline.py 225~243행: res.fail 뒤에도 res.ok=True로 반환됨을 확인.
- **검증자 근거**: parser/pipeline.py:225-231 res.fail 후 243행 ok=True 반환; cli/ingest.py:140-145는 `if not res.ok:`일 때만 failures를 읽음; enqueue 호출부 4파일(core/build·gate·ingest·pipeline) 어디에도 hierarchy_unresolved 없음(core/llm.py:811은 프롬프트 문자열); 명세 2_계약.md:238·6_파서와구축모드.md:44·4_쓰기절차.md:152·1_금지와불변.md:237 확인.

### H4. provenance 항목에 doc_id 접두가 없어 재인입 회수가 같은 locator를 가진 타 문서의 근거까지 걷어낸다
- **판정**: confirmed · 재판정 high · seg doc2-contractA
- **종류**: spec_vs_code
- **명세 인용**: 부록_용어 「provenance」 행 · 문서 2 §2.2 계약 ①-부속 source_locator 행 · 문서 4 §4.8-2 ① — 「항목 형식은 source_locator 기반 표기(doc_id 접두 포함) / 형식은 파서 자유, 요구는 같은 문서 안에서 유일할 것 하나 / 회수 대상인 문서 발자국은 … 봉투의 source_locator ∪ 기존 청크의 source_locator ∪ doc_id 접두」
- **코드 좌표**: core/pipeline.py:38-39 (_prov = rec.get('source_locator') — 접두 없음) · core/pipeline.py:643 (prose도 동일) · core/ingest.py:112-124 doc_locators, 165-172 mine/strip (locator 문자열 전역 대조) · kit/참조어댑터/cp.py:105 ('%s!R%d' — 시트명!행) · parser/adapters/basic_ppt.py:30-31 ('슬라이드 {index}') · parser/adapters/basic_pdf.py:30 ('P{index}')
- **무엇이 어긋났나**: 노드·엣지·값·alias의 provenance는 봉투 doc_id 없이 source_locator 원문 그대로 저장된다. 실 어댑터의 locator는 「Sheet1!R12」「슬라이드 3」「S1-TB1」「P3」처럼 문서 간에 동일한 값이므로(§2.2가 문서 내 유일만 요구), withdraw()의 mine(p)=`p in locs`는 CP01 재인입 시 CP02가 같은 행 번호로 기여한 provenance까지 걷어낸다 — 걷힌 노드는 근거 0으로 빌드 말미 evidence_lost 큐에 거짓 적재되고, 그 문서의 근거 이력이 소리 없이 사라진다. 픽스처는 우연히 doc_id 접두 locator(CP01-C1·PFMEA01-R1)를 써서 테스트가 이 경로를 통과하지 못했다. 실물 data/chunks.json의 TOC01 청크 locator는 「보고서!A5」다. 부록은 doc_id 접두를 요구하고 §2.2는 파서에 접두를 요구하지 않으므로, 접두를 붙이는 주체는 에이전트여야 하는데 그 코드가 없다.
- **검증자 보정**: 「주체는 에이전트여야 한다」는 명세가 말하지 않은 추론 — 부록(접두 포함)과 문서 2·문서 6 §6.4-5(접두 없는 locator)가 충돌하고 접두 부여 주체를 어느 문서도 지정하지 않으며, 문서 4 §4.8-2 ①의 회수 산식도 raw locator 대조를 전제한다. 명세가 주체(에이전트 인입 시점 `{doc_id}:{locator}` 등)를 확정하고 코드가 _prov·build_prose·withdraw.mine을 맞춰야 한다. cli/platform.py:261-262 계기판도 `doc_id-` 접두를 전제(H15와 연동).
- **고칠 쪽**: both
- **확인 방법**: core/pipeline.py 38행·643행과 core/ingest.py 112~190행을 연다. kit/참조어댑터/cp.py:105, parser/adapters/basic_ppt.py:30을 열어 locator에 doc_id가 없음을 본다. 사고 재현(다른 클론): 같은 어댑터로 두 xlsx를 인입한 뒤 한쪽을 재인입하고 다른 쪽 노드의 provenance 개수 변화를 본다.
- **검증자 근거**: _prov(38-39)·_Row.prov(357)·465·471·398·build.py:323-337·gate.commit_edge가 접두 없이 적재, tagger.envelope(parser/tagger.py:253-259)도 접두 없음; core/ingest.py:120-124·165-190 전역 문자열 대조 후 pipeline.py:578-587 _evidence_lost 적재; DECISIONS D-122 ①이 B53 shape 청크 locator에 "doc_id 접두 없음"을 명시.

### H5. 비정형(prose) 경로는 B14(좌표 없는 노드 미생성)를 구현하지 않는다 — 좌표 미해소 청크에서 Property·Unit을 스코프 없이 생성하고 orphan_anchor 항목은 재료(dropped_entities)를 보유하지 않는다
- **판정**: confirmed · 재판정 high · seg doc4-write
- **종류**: spec_vs_code
- **명세 인용**: 문서 4 §4.4 「좌표(anchor) 미해소의 연쇄」 (개정대장 §L B14) — 「좌표 없는 노드를 미리 만들어 두지 않는다 — 걸침 노드도, 같은 층 Property도 마찬가지다. … 스코프를 못 붙이는 노드(Property·걸침)는 만들지 않고」
- **코드 좌표**: core/pipeline.py:644-655 (build_prose Pass 1), core/pipeline.py:280-284 (h_entity — 정형만 검사), core/build.py:308-320
- **무엇이 어긋났나**: 정형 경로는 h_entity(pipeline.py:280)에서 ref None + 스코프 카테고리면 노드를 만들지 않고 dropped_entities에 싣지만, build_prose Pass 1(pipeline.py:651-655)은 ref가 None이어도 무조건 b.resolve_entity를 호출한다. scope_canonical이 (surface, False)를 돌려주고 노드가 스코프 없이 생성된다(부모 미해소 노드). 게다가 prose의 resolve_anchor는 defer=None(644행)이라 orphan_anchor가 즉시 적재되고 dropped_entities·pending 재료가 없어 재시도 배치가 되살릴 재료도 없다.
- **검증자 보정**: tests/test_g3.py:318-325 S13 어서션이 그 옛 동작(부모 미해소 노드 존재)을 통과 조건으로 고정하고 있어 함께 뒤집어야 한다. core/retry.py:229는 `pl.get("dropped_entities")`로만 노드를 세우므로 prose orphan_anchor 항목은 회수 불가.
- **고칠 쪽**: code
- **확인 방법**: `grep -n "_scoped_category\|dropped_entities" core/pipeline.py` → 정형(h_entity·_Row·_land_deferred)에만 등장하고 build_prose(622~725행)에는 없음. tests/test_g3.py:322-329 어서션 문안 대조.
- **검증자 근거**: 4_쓰기절차.md:94-98의 주어가 "행/청크의 공정좌표"로 비정형을 포함하고 예외 없음; build_prose 644행 defer 없는 resolve_anchor(→ build.py:143-144 즉시 enqueue, payload에 재료 없음), 651-655 무조건 resolve_entity → naming.py:86-90 (surface, False) → build.py:336 `_scoped` 없는 add_node; _scoped_category·dropped_entities·_land_deferred 호출부는 정형에만(grep).

### H6. orphan_anchor 재시도가 dropped_edges를 좌표(anchor) 층 config로 게이트에 넘겨 걸침 문서(PFMEA)의 occurs_in·controlled_by가 undeclared_relation으로 거부된 뒤 큐 항목이 self-heal로 사라진다
- **판정**: confirmed · 재판정 high · seg doc4-write
- **종류**: spec_vs_code
- **명세 인용**: 문서 4 §4.4 「좌표 미해소의 연쇄」 셋째 불릿 · §4.7-5 — 「재시도가 좌표를 해소하면 이것들로 걸침 층 노드와 controlled_by를 다시 세운다 … 이것이 없으면 골격이 나중에 자라도 그 Failure는 영영 공정에 붙지 않는다」
- **코드 좌표**: core/retry.py:220 (cfg = cfgs[rlay]), core/retry.py:257 (gate.commit_edge(sg, src, rel, dst, cfg, …)), 비교: core/pipeline.py:512-514 (정상 빌드는 문서 층 cfg)
- **무엇이 어긋났나**: 정상 빌드는 문서 층(quality) config의 relation_patterns로 판정하지만, 재시도는 해소된 anchor가 사는 층(process) config를 쓴다. process config에는 occurs_in·controlled_by가 없어 judge가 UNDECLARED를 돌려주고 log_reject만 남긴 채 return한다. 이어 store.drop으로 항목이 큐에서 내려가므로 그 Failure는 영구히 공정에 붙지 않는다 — 명세가 큐 손잡이로 막으려던 결과 그대로다.
- **검증자 보정**: retry.py:266은 거부 여부와 무관하게 store.drop(n은 commit_edge 반환값을 보지 않음). 부수: :229-241에서 먼저 세운 걸침 Property 노드는 controlled_by 없이 엣지 0 고아로 남는다. tests/에 dropped_edges 재커밋 테스트 0건.
- **고칠 쪽**: code
- **확인 방법**: `USE_MOCK=1 python -c "from core import gate; from core.bootstrap import load_config; print(gate.judge('Failure','occurs_in','Process',load_config('process'),gate.PATH_SCHEMA))"` 와 core/retry.py:220·257 읽기. 실물은 PFMEA01 좌표 미해소 행 → 골격 alias 등재 → finalize 후 gate_rejects.json의 undeclared_relation 건수와 quality graph의 occurs_in 유무 대조(쓰기 필요 — 미실행).
- **검증자 근거**: 실행 — process cfg relation_patterns = [Unit part_of Process, Unit/Process has_property Property, Unit/Property mirrors]에 occurs_in·controlled_by 없음, judge('Failure','occurs_in','Process',process_cfg) → undeclared_relation, quality cfg면 commit; retry.py:209-220은 pl["category"](Process)로 층을 잡고 doc_id로 문서 층을 되찾는 코드 없음; gate.py:71-77·142 확인.

### H7. 재시도가 pending_attrs를 attach_to_field 대상이 아니라 좌표 노드(process_ref)에 저장한다 — B14로 드롭된 관리항목의 규격·측정방법이 세부공정 Process 노드에 붙는다
- **판정**: confirmed · 재판정 high · seg doc4-write
- **종류**: spec_vs_code
- **명세 인용**: 문서 2 §2.4-③ · 문서 4 §4.4-4·§4.7-5 — 「orphan 재시도 배치가 그 좌표를 해소하면 그때 보유한 값을 해소된 노드에 저장한다 … 없는 값을 저해상도로 만들어 붙이면 문서에 없던 사실이 그래프에 생긴다」
- **코드 좌표**: core/retry.py:260-265 (b.put_attribute(ref, …); a['attach_to_field'] 미사용), core/pipeline.py:456-467 (_row_attributes가 attach_to_field를 payload에 싣는 곳)
- **무엇이 어긋났나**: cp 행에서 process_ref가 미해소면 관리항목(Property, 스코프 카테고리)은 B14로 드롭되고 규격(attach_to_field=관리항목, 값 있음)은 pending_attrs로 orphan_anchor(process_ref) 항목에 실린다. 재시도가 좌표를 해소하면 관리항목은 revived로 새로 세우지만 pending_attrs는 무조건 ref(=Process 노드)에 put_attribute 한다 — 규격이 세부공정에 attribute로 붙는다(문서에 없던 사실). 또한 contextual 여부를 스키마의 contextual 플래그가 아니라 context 존재 여부(bool(a.get('context')))로 정하므로 봉투 context가 있으면 단순형이 맥락형으로 저장돼 정상 경로와 그룹 키가 갈린다.
- **검증자 보정**: §2.4-③의 "해소된 노드"라는 문구는 좌표 노드로도 읽힐 여지가 있어 문면 정밀화가 함께 필요(개정대장 단절-21 "인입 규칙대로 저장"이 부착 대상=attach_to_field를 뒷받침). cp 어댑터(kit/참조어댑터/cp.py:42-46,101-102)가 context={"model": …}를 실어 r.ctx가 비지 않는다. 검증 테스트 0건 — 회귀 테스트 추가 필요.
- **고칠 쪽**: both
- **확인 방법**: core/retry.py:228-265 읽기: `grep -n revived core/retry.py`로 revived가 pending_attrs 블록에서 쓰이는지 본다. 실물: CP01의 한 행 process_ref를 골격 밖 값으로 바꿔 인입 → alias 등재 → finalize → process graph에서 그 Process 노드 attrs에 규격이 있는지(쓰기 필요 — 미실행).
- **검증자 근거**: revived 참조는 retry.py 239·240·248·252(dropped_edges 루프)뿐, 260-265는 `b.put_attribute(ref, …)`에 attach_to_field 미판독(core 전체 소비처는 pipeline.py 457·479·540); 정상 경로 pipeline.py:471-472는 bool(spec.get("contextual"))이나 retry.py:263은 bool(a.get("context")); build.py:357-361 그룹 키 상이.

### H8. category_pair_map 조회가 항상 (부착대상, 자식) 순서라 명세가 든 attach_to 폴백 `Unit,Process→part_of`에 닿지 않는다 — 비정형 Unit은 공정에 part_of로 붙지 못한다
- **판정**: confirmed · 재판정 high · seg doc4-write
- **종류**: spec_vs_code
- **명세 인용**: 문서 4 §4.4 (relation_patterns 문단) · §4.4-3 — 「앞의 것은 정형 edges와 attach_to 폴백(category_pair_map `Unit,Process→part_of`)이 … 그 삼항으로 커밋하기 때문이다」
- **코드 좌표**: core/pipeline.py:138-139 (_fallback_attach: pair_relation(cfg, ref.category, child.category)), core/pipeline.py:717-718 (prose attach: pair_relation(cfg, target.category, child.category)), core/gate.py:61-67, layers/process/config.json category_pair_map
- **무엇이 어긋났나**: config 키는 결과 엣지의 (src,dst)이고 Unit part_of Process는 src=자식(Unit)·dst=대상(Process)이다. 그런데 코드는 두 경로 모두 (대상, 자식)=('Process','Unit')로만 조회해 None을 받는다 — 규칙 B 폴백은 결함 로그 '카테고리쌍 매핑 없음', prose attach는 무기록 생략. 결과적으로 산문에서 발견된 설비(auto Unit)는 어떤 공정에도 part_of가 걸리지 않아 §4.4-3이 말한 'auto 설비에도 부착' 이후의 part_of 하향 2홉(공정→설비→관리항목)이 산문 유래 설비에서 성립하지 않는다.
- **검증자 보정**: 호출부는 3곳(core/retry.py:156 orphan 재시도 포함). 조회만 뒤집으면 commit_edge가 Process part_of Unit 방향이 되어 게이트 reverse 인덱스(gate.py:55-56, 87-88)가 direction_conflict로 거부하므로 끝점 방향까지 표의 키 순서에 맞춰야 한다. 검출 evidence 일부 부정확: PPT02 `주액기`는 attach가 비어 폴백 경로를 타지 않고, test_g3.py:451-453 어서션은 스키마 edges의 part_of를 재는 것 — 현 fixtures에 드러내는 케이스 없음. pipeline.py:719-721은 else 없음(§4.7-2 위반 겸).
- **고칠 쪽**: code
- **확인 방법**: `USE_MOCK=1 python -c "from core import gate; from core.bootstrap import load_config; c=load_config('process'); print(gate.pair_relation(c,'Process','Unit'), gate.pair_relation(c,'Unit','Process'))"` 및 pipeline.py:138·717 인자 순서 읽기. 실물: PPT02 인입 후 process graph에서 `패키징::전해액 주액::주액기`의 part_of 유무와 data/defects.log의 '규칙 B 폴백 — 카테고리쌍 매핑 없음 (Process → Unit)' 확인(쓰기 필요).
- **검증자 근거**: 실행 — pair_relation(c,'Process','Unit') → None, ('Unit','Process') → 'part_of'; 인메모리 시뮬레이션 `_fallback_attach(child=Unit, ref=Process)` → False + 결함 로그; layers/process/config.json:44-48·87-92와 3_구조.md:63 키 순서 (src,dst) 확인; 4_쓰기절차.md:77·79·86, 1_금지와불변.md:236.

### H9. matcher.candidates의 사전 조회 갈래가 is_live를 거치지 않고 obsolete·replaced_by 전이도 없다 — 폐기(I4) 노드가 사전 정확 히트로 1.0 매칭되어 재인입 부활 차단이 새어 나간다
- **판정**: confirmed · 재판정 high · seg doc4-write (중복: H10)
- **종류**: spec_vs_code
- **명세 인용**: 문서 4 §4.7-4 「툼스톤은 연산 대상이 아니다」 불릿 (생존 판정 두 갈래) — 「매칭 후보 판정(is_live)은 merged_into 툼스톤과 status: obsolete를 둘 다 제외한다 — 사전 조회·후보 검색·anchor 조회 전부에 적용되며 … obsolete에 replaced_by가 있으면 그 표면형은 대체 노드로 전이해 해소하고, 없으면 … uncertain_match로 표시한다」
- **코드 좌표**: core/matcher.py:113-119 (① 사전 조회 — is_live 없음), core/matcher.py:150-153 (exact면 즉시 MATCH 1.0), core/ops.py:574-598 (obsolete가 사전 엔트리를 걷지 않음)
- **무엇이 어긋났나**: ops.obsolete는 status만 바꾸고 dictionary 엔트리를 남긴다(§4.8-2②에 따라 보존이 맞다). 그런데 candidates의 사전 갈래는 category·layer·polarity만 보고 is_live를 검사하지 않아 obsolete 노드가 exact 후보로 들어가고, match()는 exact를 보자마자 MATCH 1.0을 돌려준다. resolve_entity가 그 폐기 노드에 provenance·alias를 다시 붙인다. replaced_by 전이와 uncertain_match 표시는 matcher·build 어디에도 없다(grep replaced_by core/matcher.py core/build.py → 0건).
- **검증자 보정**: 사전 갈래는 merged_into 툼스톤도 거르지 않는다(실운영에서는 I2가 redirect하므로 드묾). anchor 조회(build.py:126-127)·질의 링킹(pipeline.py:748)은 is_live를 적용하므로 누락은 이 갈래 하나로 특정된다.
- **고칠 쪽**: code
- **확인 방법**: USE_MOCK=1 python -c 로 matcher.candidates에 status obsolete 노드 + Dictionary({'표면형':['A']})를 넣고 match 결과 확인. `grep -n "replaced_by" core/matcher.py core/build.py core/pipeline.py`.
- **검증자 근거**: 재현(가짜 graph/dictionary) — status='obsolete', replaced_by='B'인 A → candidates()=[('A',exact=True),('B',False)] → match()={'match','A',1.0}; matcher.py:114-119 is_live 호출 없음(89행 docstring은 ②에만 해당), 150-153 즉시 MATCH; ops.py:574-598은 redirect/drop 없음(split은 566행에서 redirect); 4_쓰기절차.md:177·189, 개정대장.md:616-619.

### H10. 사전 히트 경로가 is_live를 거치지 않아 폐기(obsolete) 노드가 재인입에서 1.0 MATCH로 부활한다
- **판정**: confirmed · 재판정 high · seg doc4-queue-ops (중복: H9)
- **종류**: spec_vs_code
- **명세 인용**: 4_쓰기절차.md §4.7-4 (툼스톤은 연산 대상이 아니다 단락) — 「매칭 후보 판정(`is_live`)은 `merged_into` 툼스톤과 `status: obsolete`를 둘 다 제외한다 — 사전 조회·후보 검색·anchor 조회 전부에 적용되며, 이것이 I4 이유 ②(재인입 부활 차단)의 강제 지점이다」
- **코드 좌표**: core/matcher.py:118-123 (candidates ① 사전 조회) · core/ops.py:574-598 (obsolete가 사전 엔트리를 건드리지 않음) · core/build.py:321-326
- **무엇이 어긋났나**: candidates()의 ① 사전 조회 갈래는 `n and category==… and layer==… and polarity==want`만 보고 is_live를 검사하지 않는다(② 후보 검색 갈래만 is_live). obsolete()는 status만 바꾸고 사전 엔트리를 남기므로, 폐기 노드의 canonical/alias가 문서에 다시 나오면 exact=True 후보로 조립되어 match()가 confidence 1.0 MATCH를 돌려주고 build.resolve_entity가 그 폐기 노드에 provenance를 덧붙인다 — 사람의 폐기 판정이 재인입에서 조용히 뒤집힌다.
- **고칠 쪽**: code
- **확인 방법**: `sed -n 115,135p core/matcher.py`로 ① 갈래에 is_live 부재 확인. USE_MOCK=1 python -c 로 status='obsolete' 노드를 lookup이 돌려주는 스텁을 만들어 candidates→match 결과가 MATCH 1.0인지 재현.
- **검증자 근거**: matcher.py:117-122에 is_live 없음(123행 ②만), 158-160 exact→1.0; ops.py:574-598 dictionary 미접촉, dictionary.py:80-86 lookup 무필터; build.py:321-326 provenance append; 스텁 재현 candidates('용접기','Unit','process',G,D) → [{'id':'n1','exact':True}], match → 1.0; core/status.py:22-38 docstring은 구현 아님(CLAUDE.md §3).

### H11. I축 연산 대상 판정(_target)이 is_live를 써서 폐기 노드의 개명·재폐기가 거부되고, 그마저 KeyError로 죽는다
- **판정**: confirmed · 재판정 med · seg doc4-queue-ops
- **종류**: spec_vs_code
- **명세 인용**: 4_쓰기절차.md §4.7-4 (생존 판정은 두 갈래다) — 「연산 대상 판정은 `merged_into`만 거부한다 — 폐기 노드는 개명·재폐기의 대상이 되기 때문이다」
- **코드 좌표**: core/ops.py:52-63 (_target) · core/status.py:22-24 (is_live)
- **무엇이 어긋났나**: _target은 `if not is_live(n)`로 거부하는데 is_live는 obsolete도 제외하므로 명세가 허용한 「폐기 노드 개명·재폐기」가 막힌다. 거부 메시지가 `n[STATUS_MERGED]`를 읽어 obsolete 노드(merged_into 키 없음)에서는 OpRefused가 아니라 KeyError가 난다 — cli/ops.py는 OpRefused만 잡으므로 스택트레이스로 종료한다. status.py 독스트링은 '연산 대상 판정은 _target이 갖는다 — 폐기 노드도 연산 대상일 수 있으므로 다른 갈래'라 적었으나 코드는 같은 갈래를 쓴다.
- **고칠 쪽**: code
- **확인 방법**: `python -c "from core import ops; class G:\n def __init__(s,n): s.n=n\n def get(s,i): return s.n.get(i)\nops._target(G({'x':{'id':'x','status':'obsolete'}}),'x')"` 실행 → KeyError.
- **검증자 근거**: 재현 KeyError('merged_into'); _target 호출부 ops.py:158-159(transfer)·278(rename)·438(merge)·512(split)·583(obsolete) 전부 경유; cli/ops.py:121은 OpRefused만 잡음; status.py:31-32 독스트링은 구현 아님; 4_쓰기절차.md:177 원문 확인.

### H12. 상시 kind 종결 경로(auto→confirmed 검토 도구)가 없다 — confirmed의 생산자 0, resolve_item 호출부 0
- **판정**: confirmed · 재판정 med · seg doc4-queue-ops
- **종류**: code_missing
- **명세 인용**: 4_쓰기절차.md §4.7-3 「상시 kind의 종결 경로」(개정대장 [개정] 단절-8) — 「검토 도구가 그 노드의 `status`를 `auto` → `confirmed`로 올리고 항목을 큐에서 내린다(드롭 키는 payload의 node_id). 이것이 `confirmed`를 만드는 유일한 연산이며」
- **코드 좌표**: core/store.py:188-210 (resolve_item — 정의만, 호출부 0) · cli/ops.py:55-56 (op choices에 검토/확정 없음) · core/ops.py:45 (STATUS_RANK의 confirmed)
- **무엇이 어긋났나**: 코드 어디에도 노드 status를 'confirmed'로 쓰는 문장이 없다(grep confirmed core cli run.py → 비교·주석·STATUS_RANK뿐). store.resolve_item은 resolution만 기록하고 status를 올리지도 항목을 내리지도 않으며, 호출부는 cli/platform.py:498의 안내 문자열 하나뿐이다. 결과: auto_node·uncertain_match가 영구 잔류하고, I2 3단 규칙의 2단(confirmed)과 anchor 자격(seed·confirmed)이 생산자 없는 값 위에 선다 — 명세가 이 조항의 근거로 적은 바로 그 상태.
- **검증자 보정**: 「호출부 0」은 tests/test_g1_g2.py:205 테스트 호출 1건을 빠뜨렸으나 운영 코드 호출부 0은 그대로. auto_node·uncertain_match에 대한 store.drop 호출도 0건이고 core/ingest.py:26,153 STANDING_KINDS가 회수에서 두 kind를 무조건 보존 — 「회수로 노드가 사라진 항목도 같은 자리에서 내린다」 후반부도 미구현.
- **고칠 쪽**: code
- **확인 방법**: `grep -rn "= \"confirmed\"\|'confirmed'" core cli run.py ; grep -rn "resolve_item(" core cli run.py tests`
- **검증자 근거**: status 대입 전수 grep에 STATUS_DELETED/MERGED/OBSOLETE/seed 외 없음(core/ops.py:224,232,480,559,592,615 · core/graph.py:133,156); store.py:188-210 docstring 스스로 "항목을 내리지 않는다"; drop 호출부는 build.py:439·ingest.py:90·pipeline.py:576뿐; cli/ops.py:55-56 choices에 검토 연산 없음; 4_쓰기절차.md:160·개정대장.md:606-609·부록_용어.md:88.

### H13. 규약 9 「추출 실패의 처분은 청크 단위」가 코드에 없다 — 한 청크 실패가 문서 전체 추출을 예외로 중단시키고 failed 표시·결함 로그가 0건
- **판정**: confirmed · 재판정 high · seg doc4-extract
- **종류**: code_missing
- **명세 인용**: docs/spec/4_쓰기절차.md §4.10 규약 9 — 「재시도가 소진된 청크는 체크포인트에 `{"chunk_id": …, "failed": "사유"}`로 남겨 「애매해서 무후보」와 구분하고, 구축은 그 청크만 건너뛰고 전진한다. … 처분 급은 큐가 아니라 결함 로그(`data/defects.log`)」
- **코드 좌표**: core/extract.py:189-201 (_candidates_for) · core/extract.py:258-292 (extract) · core/llm.py:388-407 (chat)
- **무엇이 어긋났나**: 실호출 갈래 `_candidates_for`는 `llm.chat(...)`를 try 없이 부르고, `llm.chat`은 재시도 소진 시 `raise RuntimeError(reason)`(core/llm.py:405-407), 4xx는 즉시 `raise`(391)한다. 상위 `extract()`(258-292)의 청크 루프에도 예외 처리가 없어 예외가 cli.extract/run_document까지 올라간다 — 결과: ①부분 실패 청크를 `{"chunk_id", "failed"}`로 남기는 코드 0건 ②구축이 「그 청크만 건너뛰고 전진」할 재료가 생기지 않음 ③`store.append_defect` 호출이 추출 단계에 0건이라 결함 로그도 남지 않음. 「전 청크 실패면 체크포인트를 쓰지 않는다」는 우연히 성립하지만, 부분 실패도 파일을 안 쓰므로 청크 하나의 게이트웨이 오류가 문서 전체를 미추출 상태로 되돌린다.
- **검증자 보정**: 예외는 cli/ingest.py:161의 문서 단위 except에서 잡히므로 배치 전체가 멈추진 않지만, 처분 단위가 「문서」이지 「청크」가 아니며 결정적 4xx 청크가 하나라도 있으면 그 문서는 영구 미추출·성공 청크 호출 비용은 매 재실행마다 소실. build_prose(pipeline.py:640-675)에 "failed" 키 판독 분기도 없음. docs/2A_작업목록.json에도 항목 없음.
- **고칠 쪽**: code
- **확인 방법**: `grep -n 'failed\|append_defect\|try' core/extract.py` → 0건. `python -c "import inspect,core.extract as e; print(inspect.getsource(e._candidates_for)); print(inspect.getsource(e.extract))"`로 예외 처리 부재 확인. core/llm.py:381-407 chat의 예외 경로를 읽는다.
- **검증자 근거**: core/extract.py 전문 grep 'failed|append_defect|try|except' → 0건; llm.py:391 즉시 raise·405-407 RuntimeError(log.explicit_fail은 로거 경고일 뿐); append_defect 호출처는 bootstrap/ingest/retry/ops/status/pipeline뿐; core/pipeline.py:805 run_document가 extract를 try 없이 호출; 4_쓰기절차.md:245·138, 개정대장.md:641-644.

### H14. part_of 상향 1홉 뒤 하향 재귀가 다시 걸려 공정 질의가 골격 트리 전체로 확장된다
- **판정**: confirmed · 재판정 med · seg doc5-query
- **종류**: spec_vs_code
- **명세 인용**: 문서 5 §5.1-4 · §5.1-5 — 「공정층: part_of 하향 재귀·상향 1홉 … `recursive: false`는 "같은 관계를 연달아 재추적하지 않음"일 뿐」
- **코드 좌표**: core/graph.py:188-202 (neighbors) · layers/process/config.json query_traverse.part_of
- **무엇이 어긋났나**: neighbors()의 비재귀 가드는 `not rec and via == e["rel"]`로 규칙별 recursive 플래그만 본다. up(비재귀)으로 부모에 닿은 노드에 down(재귀) 규칙이 다시 걸려 부모의 하위 트리 전부(형제와 그 자손)가 확장에 들어간다. 실데이터에서 '노칭'(부모=조립=루트) 확장이 47노드 중 46노드, facts 83건(그래프 엣지 전량)이 된다. 명세가 말한 '상향 1홉'의 결과(부모 1개)와 다르며, 명세는 같은 관계의 규칙 둘이 프론티어에서 어떻게 상호작용하는지(규칙 단위 가드인지 관계 단위 가드인지)를 적지 않아 공백 성분도 있다. 확장이 층 전체가 되면 tier2 청크가 무작위에 가까운 축에서 상한 8에 잘리고 mock 갈래에서는 [그래프 사실]이 전량 출력된다.
- **검증자 보정**: 코드 정정은 프론티어에 도착 규칙의 recursive 여부를 함께 싣고 비재귀 규칙으로 도달한 노드에는 그 관계의 모든 규칙을 걸지 않는 것; 명세는 그 상호작용을 명문화해야 한다. tests/test_g1_g2.py:313의 2홉 검사는 Property 수 ≥3만 보므로 과확장을 잡지 못한다. 데이터 유실·불변식 위반은 아니어서 med.
- **고칠 쪽**: both
- **확인 방법**: python3 -c로 core.graph.GraphStore.neighbors를 data/process/graph.json 노드·엣지에 바인딩해 노칭 id 하나로 호출하고 len()을 본다(읽기 전용). 또는 core/graph.py:190-193의 가드가 규칙의 rec 플래그만 보는지 읽는다.
- **검증자 근거**: 실데이터(47노드·83엣지) 노칭 01M21ZNT450MXX696RDMQGKMHB → neighbors 46개, down+has_property만 7개; 스텁 트리 P1→root,P2→root,P2a→P2,P2b→P2a에서 neighbors([P1])={P1,P2,P2a,P2b,root}; graph.py:192 가드·202 프론티어에 관계명만 적재; 5_읽기절차.md:19-20.

### H15. 계기판 2 plateau가 provenance→문서 대응을 `doc_id + "-"` 접두로 추정 — 실파서 locator에서는 전 문서 rate=None
- **판정**: **partial** · 재판정 high · seg doc5-gauges
- **종류**: spec_gap (검출 분류) → 검증자는 「명세 공백이 아니라 명세 자기모순 + 코드 갭」으로 정정
- **명세 인용**: 문서 5 §5.5 산식 2 (+문서 2 §2 source_locator 행) — 「plateau 산식: 문서별 신규 개체율 — provenance 첫 locator 기준, seed 제외 / source_locator … 형식은 파서 자유, 요구는 같은 문서 안에서 유일할 것 하나」
- **코드 좌표**: cli/platform.py:261-262 (doc_of), core/build.py:336 (provenance=[prov]), core/pipeline.py:39·643
- **무엇이 어긋났나 (corrected_claim)**: 계기판 2 plateau(cli/platform.py:261-278)는 provenance 항목이 `{doc_id}-` 접두를 갖는다는 부록_용어 provenance 행("source_locator 기반 표기(doc_id 접두 포함)", 예 CP01-C8)을 전제로 문서를 역산하지만, 인입 코드(core/pipeline.py:38-39·643, core/build.py:336)는 파서의 raw source_locator를 접두 없이 provenance에 넣는다. 픽스처 locator(`CP01-C1`)만 우연히 접두 꼴이라 통과하고, 실파서 locator(`보고서!A5`·`슬라이드 3#2`·`P4`·`S8-T1`)에서는 전 문서 mentioned=new=0 → rate=None(현재 data/의 TOC01로 재현). 명세는 공백이 아니라 자기모순이다 — 부록은 접두 포함, 문서 4 §4.8-1(세 집합 합집합)과 문서 6 [정정] 42(raw locator가 안정 키)는 raw locator를 provenance로 전제하며, 접두를 붙이는 주체(에이전트 인입 시점)를 어디도 명시하지 않는다. 부수: 계기판 3 pieces(platform.py:283-289)도 locator를 문서 구분 없이 합쳐 두 문서의 같은 locator가 1조각으로 센다.
- **고칠 쪽**: both (H4와 같은 뿌리 — 함께 수리)
- **확인 방법**: cli/platform.py:261-278 읽기 → core/build.py:336에서 provenance에 doc_id가 없음을 확인 → parser/adapters/basic_ppt.py:30-31 (`슬라이드 {index}#{frame}`)·basic_pdf.py:29-30 (`P{index}`) locator 형식 대조 → python3 -c로 doc_of를 옮겨 '보고서!A5'→None 재현.
- **검증자 근거**: 사실 부분 확인(doc_of 접두 역산, _prov·643·build.py:324/336/369/386 접두 없음, data/doc_registry.json TOC01 청크 locator '보고서!A5' → None); 부록_용어.md:23 "doc_id 접두 포함" vs 4_쓰기절차.md:188 합집합 산식(core/ingest.py:112-124·166 구현)·6_파서와구축모드.md:90 [정정] 42 — 원인 귀속만 정정.

### H16. 계기판 8(build 소요)의 별도 호출값은 build가 아니라 save() 시간 — 실제 build 측정치는 버려진다
- **판정**: confirmed · 재판정 med · seg doc5-gauges
- **종류**: spec_vs_code
- **명세 인용**: 문서 5 §5.5 표 8행 · 산식 규율 5 — 「8 | build 소요 시간 | 쓰기 비용의 직접 관측 | 알람선 30초 → 동상 / 계기판을 build·query 경로에 심지 않는다 — 별도 호출로만 계산한다」
- **코드 좌표**: cli/platform.py:306-311, core/graph.py:207-225, core/pipeline.py:794·811, run.py:84·77-78
- **무엇이 어긋났나**: run.py gauges(platform.gauges)의 8번은 `open_graph(lay); g.build_begin(); g.build_end()`로 재는데 build_end의 secs는 build_begin 이후 경과 = save() 직렬화 시간뿐이라 실제 인입·빌드 시간과 무관하다(30초 알람이 사실상 발동 불가). 실제 build 시간은 pipeline.run_document가 build_end()로 재서 metrics로 돌려주지만 run.py cmd_ingest가 `r, m, extracted = …`에서 m을 버리고, finalize()도 build_end() 반환을 버린다. 한편 pipeline·bootstrap이 build 경로 안에서 계기판 7·8을 계산하고 run.py:77-78이 bootstrap 중 '계기판 7·8'을 출력하는 것은 규율 5와 문면 충돌 — 8번 지표의 정의와 규율 5가 어떻게 양립하는지(빌드가 남긴 측정치를 별도 호출이 읽는 자리) 명세가 침묵한다.
- **검증자 보정**: "실제 build 측정치가 전부 버려진다"는 과장 — bootstrap의 metrics는 run.py:74-78이 콘솔에 출력한다(다만 저장되지 않아 gauges가 읽지 못함). metrics는 GraphStore 인스턴스 필드(graph.py:84·214)에만 있고 파일·registry에 지속 저장되지 않는다.
- **고칠 쪽**: both
- **확인 방법**: cli/platform.py:306-311과 core/graph.py:207-225를 나란히 읽는다; `grep -n "metrics\|build_end" run.py core/pipeline.py`로 반환값 소비처가 없음을 확인; `python run.py gauges` 출력의 '8 build 시간'이 매번 0.0x초인지(graph.json을 재기록하므로 주의).
- **검증자 근거**: graph.py:212-213 `size = self.save(); secs = now - _build_started`, platform.py:309-311 build_begin 직후 build_end(주석 "재측정 경로(내용 동일 재기록)"); run.py:84 m 미사용, pipeline.py:565 finalize 반환 버림; 5_읽기절차.md:100·110, 7_구현규격과검증.md:46.

### H17. propose()가 프레임별 보존 파일을 따로 써서 source_hash 무효화를 우회한다 — 원본이 바뀌어도 옛 지도가 재사용된다
- **판정**: confirmed · 재판정 high · seg doc6-parser-core
- **종류**: spec_vs_code
- **명세 인용**: 문서 6 §6.3 보존 자리와 재사용 — 「같은 doc_id의 재인입은 source_hash가 같으면 보존분을 그대로 재사용하고 … source_hash가 바뀌면 추출 체크포인트와 함께 무효화·재생성한다 … 보존 파일의 내부 형태: 문서당 파일 하나이고 그 안에서 프레임 키로 가른다」
- **코드 좌표**: parser/struct_map.py:123-136 (propose: load_kept(doc_id, src_hash=None) → keep(doc_id, m, None)) · parser/struct_map.py:70 (source_hash 없는 파일은 무조건 통과) · parser/pipeline.py:68 (apply(f"{doc_id}:{key}", …) — src_hash 미전달)
- **무엇이 어긋났나**: 지도 보존이 두 층으로 갈린다. pipeline은 문서 단위 파일 `extract/struct_maps/{doc_id}.json`에 source_hash와 함께 싣지만, apply()→propose()는 프레임 키를 붙인 `{doc_id}:{프레임}.json`을 source_hash 없이 따로 쓰고, 재인입 때 load_kept(…, src_hash=None)으로 그 파일을 해시 대조 없이 먼저 읽는다. 원본 파일이 바뀌어 pipeline이 문서 단위 보존분을 버려도(kept_maps={}), 훅이 propose()로 내려가 옛 프레임 파일을 물고 `source: live`로 돌려주므로 모델이 다시 불리지 않고 옛 지도가 새 source_hash의 문서 단위 파일에 다시 실린다. 「문서당 파일 하나」 형태도 깨진다.
- **검증자 보정**: 행 수가 달라진 경우 validate(:182-186) 커버리지 불일치로 재인입마다 「영영 평면」. invalidate(doc_id)(:90)는 콜론 프레임 파일을 지우지 않음. mock 경로(ask=None)는 보존하지 않으므로 실호출 경로에서만 드러난다(실물 extract/struct_maps에는 PPTB_T01.json만 존재).
- **고칠 쪽**: code
- **확인 방법**: `python -c "import pathlib; from parser import struct_map as sm, pipeline; sm.KEEP_DIR=pathlib.Path('/tmp/x'); L=[(1,'1. 장'),(2,'a'),(3,'2. 장'),(4,'b')]; calls=[]; ask=lambda d,l: (calls.append(d) or {'rows':[{'row':n,'heading':n in(1,3),'level':1 if n in(1,3) else 0} for n,_ in l]}); h=pipeline._map_hook('DOC',{}, {}, [], ask=ask); h('슬라이드 3',L,lambda a,b:'L'); h2=pipeline._map_hook('DOC',{}, {}, [], ask=ask); h2('슬라이드 3',L,lambda a,b:'L'); print(sorted(p.name for p in sm.KEEP_DIR.iterdir()), calls)"` → 파일 'DOC:슬라이드 3.json' 하나, ask 1회만.
- **검증자 근거**: 스크래치 KEEP_DIR에서 source_hash H1→H2로 pipeline.parse(basic_ppt, 'PPTX', tests/fixtures/raw/PPT_basic.pptx, map_structure=ask) 2회 — run2 ask 호출 수 그대로 1회, PPTX.json이 H2로 재기록되되 maps['슬라이드 11']은 run1 옛 지도, 'PPTX:슬라이드 11.json'에 source_hash 키 없음, 지도_출처='live'; struct_map.py:344→123→70·136·83·90 및 apply(:334)에 src_hash 인자 부재.

### H18. PDF 쪽 렌더가 ④에 절대 붙지 않는다 — tagger는 meta.slide로 찾고 basic_pdf는 meta.page로 낸다
- **판정**: confirmed · 재판정 med · seg doc6-adapters
- **종류**: spec_vs_code
- **명세 인용**: 문서 6 §6.4-5 기본 PDF 어댑터(B53) — 「페이지 = prose 청크(shape_kind=page) — 텍스트는 PyMuPDF가 뽑고, 페이지 렌더를 ④ 맥락으로 붙인다(그림·표가 텍스트로 안 잡히는 페이지의 유일한 근거)」
- **코드 좌표**: parser/tagger.py:211 · parser/adapters/basic_pdf.py:77-85 · parser/pipeline.py:111-112,206
- **무엇이 어긋났나**: pipeline._page_map은 PDF에서 {쪽번호: PNG}를 정상 산출하지만(키 1..N), tagger.complete_images는 `pages.get(meta.slide)`로 찾는다. basic_pdf의 조각 meta에는 `page`만 있고 `slide`가 없어 조회가 항상 None → summarize에 page=None이 가고 slide_render가 항상 'none'으로 기록된다. 렌더가 있는데도 없다고 데이터에 남으므로 「조용히 다르게 돌지 않는다」도 거짓이 된다. 기존 회귀(test_p1 B53 c)는 PDF의 slide_render=page를 검사하지 않아 통과했다.
- **검증자 보정**: 개별 그림 바이트와 맥락 텍스트는 PDF에서도 ④에 도달하므로 손실은 쪽 전체 렌더에 한정 — med. 수정처는 tagger.py:211(`meta.slide` 없으면 `meta.page` 폴백) 또는 basic_pdf.py:83(`slide` 키 병기) + test_p1 [B53 c]에 pipeline.parse 경유 PDF slide_render=page 검사 추가.
- **고칠 쪽**: code
- **확인 방법**: `USE_MOCK=1 python -W ignore -c "from parser import reader,pipeline,tagger; from parser.adapters import basic_pdf; p='tests/fixtures/raw/PDF_basic.pdf'; raw=reader.read(p); pg=pipeline._page_map(p,raw); got=[]; s=lambda ref,image=None,mime=None,context='',page=None: got.append(page is not None) or 'x'; out=tagger.complete_images(basic_pdf.extract(raw), s, kept={}, images=raw['_images'], pages=pg); print(list(pg), got, [c['meta']['slide_render'] for c in out if c.get('image_ref')])"` → [1,2,3] [False] ['none']
- **검증자 근거**: 실측 그대로 `[1, 2, 3] [False] ['none']`; tagger.py:211 조회 키 `meta.slide` 단일·172 `_mark`는 page 없으면 'none'; basic_pdf.py:83-85 meta 키에 slide 없음(basic_ppt.py:216은 있음); tests/test_p1.py:668-700은 slide_render 미검사(585-586·639-640은 PPTX 대상); 6_파서와구축모드.md:91.

### H19. D-110 경로 상이 경고가 파일명을 비교해 의도한 경우(같은 이름·다른 폴더)에 절대 발화하지 않는다
- **판정**: confirmed · 재판정 med · seg doc6-ingest-routing
- **종류**: spec_vs_code
- **명세 인용**: 문서 6 §6.4 투입 분류(B47 · D-110) — 「다른 폴더의 같은 이름이 같은 문서로 취급되므로, 대장에 기록된 경로와 다르면 화면에 경고한다 — 조용히 덮지 않는다」
- **코드 좌표**: cli/ingest.py:131-134
- **무엇이 어긋났나**: 경고 조건이 `Path(prev["source_path"]).name != Path(doc).name`이라 파일명을 비교한다. doc_id가 파일명 stem에서 오므로 같은 doc_id로 재인입되는 문서는 이름이 (거의) 항상 같고, 명세가 경고하라는 「같은 이름·다른 폴더」에서는 두 이름이 동일해 경고가 나지 않는다. 발화하는 경우는 확장자만 다르거나(`X.xlsx`↔`X.xlsm`) 공백 정규화로 stem이 겹치는 경우뿐이다. 결과: 다른 문서가 같은 이름으로 오면 조용히 개정(회수→덮어쓰기)된다.
- **검증자 보정**: 대장 source_path는 전체 경로로 저장된다(core/ingest.py:202 ← parser/pipeline.py:233 ← cli/ingest.py:140 str(doc)) — 비교 대상을 정규화한 source_path(또는 parent)로 바꿔야 한다. tests/test_g6.py:289-291 동명·타폴더 인입에 경고 어서션 추가, 2B_작업가이드.md:188 함정 행에 경고 발화 병기.
- **고칠 쪽**: code
- **확인 방법**: `python -c "from pathlib import Path; print(Path('/a/CP01.xlsx').name != Path('/b/CP01.xlsx').name)"` → False. 또는 cli/ingest.py:132를 열어 비교 대상이 `.name`임을 확인.
- **검증자 근거**: cli/ingest.py:132 `.name` 비교, :48 doc_id = Path(path).stem; 같은 파일 docstring(43-46)은 source_path 비교를 말하나 구현 아님; 6_파서와구축모드.md:101·개정대장.md:1438; test_g6.py:281·289-291 경고 어서션 없음; 2B_작업가이드.md:188은 경고 언급 없음.

### H20. 재생성 지시(자동 하네스 문면·--instruct·실패 뒤 문답 답)가 실호출 생성 LLM에 전혀 전달되지 않는다
- **판정**: confirmed · 재판정 high · seg doc6-register
- **종류**: spec_vs_code
- **명세 인용**: 문서 6 §6.5 「하네스는 생성 안에서 돌고…」 표 + 「재생성은 어디서 일어나든 관문을 다시 지난다」([정정] 40) + §6.6-2 — 「자동 재생성 — 실패 문면을 지시로 그대로 실어 보낸다. 사람의 통역을 거치지 않는다 / 재생성 루프 — 수정 지시 → 재생성 → 재파싱 → 재검수」
- **코드 좌표**: cli/register.py:363-419 (_draft_live) · :836-862 (machine_gate 재생성) · :1315-1326 (cmd_review --instruct) · cli/prompt.py:160-248 (_render_template)
- **무엇이 어긋났나**: 실호출 갈래 `_draft_live(doc_type, revision)`는 system=템플릿 렌더(pkg만 입력) + user=input_package.json 원문만 보낸다. machine_gate·cmd_review가 만든 `instruction`은 `st["instructions"]`(state.json)에만 기록되고, `_render_template`도 템플릿(v1.0)도 그것을 읽는 자리가 없다(템플릿 치환 자리 7개에 지시 자리 없음, 코드 전체에서 `instruction` 소비처는 뷰·등록부·승인 기록뿐). 따라서 실호출에서 revision 1·2…는 첫 초안과 같은 입력으로 다시 부르는 것이고, 화면의 「재생성 지시 — 보낸 문면 그대로」는 보내지 않은 것을 보냈다고 말한다. mock 갈래는 `_rev{n}` fixture를 이름으로 골라 루프 배선만 검증하므로 회귀가 잡지 못한다.
- **검증자 보정**: 실패 뒤 문답 `_interview(pkg or {}, context=ask)`(:839)는 `on_round` 없이 호출되어 라운드 이력이 input_package.json에 저장되지 않는다(생성 전 문답 :628-637만 _persist). draft() 시그니처(:190-217)에 지시를 실을 인자 자체가 없다. tests/test_p3.py:1193·1248은 by 라벨·관문 결과만 센다.
- **고칠 쪽**: code
- **확인 방법**: `grep -n instruction cli/prompt.py core/llm.py kit/생성프롬프트_템플릿_v1.0.md` → 0건. `sed -n 363,419p cli/register.py`에서 st·instructions 참조가 없음을 확인. 실호출로 `register generate` FAIL→자동 재생성 시 ONTO_DUMP_PROMPT=1로 prompt_rendered.md를 떠 보면 실패 문면이 없다.
- **검증자 근거**: register.py:383-387 msgs=[{system},{user: raw_pkg}] + revision은 :393-395 `_rev{n}` 접미사에만; prompt.py 치환 자리 = 자유 텍스트 + 7개(참조_어댑터·층_이름·존재하는_층_목록·카테고리_정의문·관계_패턴_표·공용_블록_목록·골격_닫힌_목록); instruction 소비처는 render_review.py:241·registry.py:114·register.py 뷰/승인뿐; llm.py:355-375 chat은 messages 외 상태 미판독; 6_파서와구축모드.md:144·151·179.

### H21. 리허설 파싱의 doc_id가 운영의 것이 아니다 — 구조 지도·이미지 요약 보존분이 CP01 키로 남아 운영 인입이 재사용 못 한다
- **판정**: confirmed · 재판정 med · seg doc6-review-view
- **종류**: spec_vs_code
- **명세 인용**: 문서 6 §6.6 ②구획(B51-2) · §6.3 보존 규칙 — 「리허설의 doc_id는 운영의 것(파일명 파생, D-110)이다 — 체크포인트 키가 같아야 재사용이 성립한다」
- **코드 좌표**: cli/register.py:1367-1368 (pipeline.parse(mod, f"{doc_type.upper()}{i:02d}", …)) · :1258 (env["doc_id"] = doc_id_of(s) — 추출 봉투만 덮어씀) · parser/pipeline.py:163,217 (struct_map.load_kept/keep(doc_id…)) · parser/struct_map.py:31-32 (keep_path = extract/struct_maps/{doc_id}.json)
- **무엇이 어긋났나**: 검수의 리허설 파싱은 doc_id를 `{DOC_TYPE}{i:02d}`(예: CP01)로 돌리고, 운영 doc_id(파일명 stem)는 추출 리허설 직전에 봉투의 doc_id 키만 바꿔 끼운다. 그래서 파싱 중 ⑦구조 지도·④이미지 요약이 실호출로 만든 보존분은 `extract/struct_maps/CP01.json`에 남고, 확정 뒤 `ingest-file`(doc_id=파일명 stem)은 load_kept에서 miss → 같은 LLM을 다시 부른다. pipeline.py:158-159 주석대로 「매 인입 새로 부르면 text가 흔들려 chunk_id가 전량 이동」하므로, 운영 doc_id로 저장된 추출 체크포인트의 candidates(chunk_id 키)가 새 chunk_id와 어긋나 재사용도 실질 실패할 수 있다. 뷰의 where/doc_id도 CP01로 찍힌다. 개정대장 B51 회차 마감은 「구현이 운영 doc_id로 간 것이 맞다」고 적었으나 실물은 추출 봉투 한 곳만 그렇다.
- **검증자 보정**: 「재사용 실질 실패」는 어긋난 채 쓰이는 게 아니라 core/ingest.py:248-252가 doc_hash 변경 시 extract_mod.invalidate(doc_id)로 통째 폐기 → 재추출(같은 결과). 고칠 자리는 register.py:1368의 리허설 파싱 doc_id를 doc_id_of(s)로, :1258 덮어쓰기 제거(cli/parse.py:176 cmd_build 같은 꼴 동반 검토). 영향은 ⑦/④가 실제로 도는 live prose에 한정.
- **고칠 쪽**: code
- **확인 방법**: cli/register.py 1361-1372와 1254-1262를 읽어 doc_id 두 갈래를 확인; parser/pipeline.py 158-166·209-217과 parser/struct_map.py 31-32를 읽어 보존 키가 parse의 doc_id임을 확인. (USE_MOCK 리허설 뒤 extract/struct_maps/에 `CP01.json` 꼴 파일이 생기는지 — review 실행 필요, 미실행)
- **검증자 근거**: register.py:1361-1372 `_run()`의 CP01 꼴 doc_id·:1258 봉투 키만 교체; parser/pipeline.py:162-163·177·199·216-217이 parse에 들어온 doc_id를 키로 사용; cli/ 전체 grep에 struct_maps·keep_path·invalidate 호출 0; cli/ingest.py:140 → cli/parse.py:78 stem 파싱; register.py:1062,1081,1114 뷰 doc_id=CP01; 6_파서와구축모드.md:174·60, 개정대장:1531.

### H22. 파서 측 실패 3종(parse_failure·adapter_mismatch·hierarchy_unresolved)이 공유 큐에 착지하지 않는다 — 화면 출력만
- **판정**: confirmed · 재판정 high · seg doc7-layout (중복: H2 · H3 · H25)
- **종류**: code_missing
- **명세 인용**: 문서 7 §7.1 core 접근 경계 표 core/store.py 행 · 문서 2 §2.7(238행) — 「파서 측이 parse_failure·adapter_mismatch·hierarchy_unresolved를 직접 착지시킨다 / 파서 공용 코어가 전 층 공유 단일 큐(review_queue)에 … 직접 착지시킨다(에이전트가 대신 적재하는 단계가 없다)」
- **코드 좌표**: parser/pipeline.py:169,184,229 (res.fail만) · cli/parse.py:115-118 (print만) · cli/ingest.py:140-146 (print만) · core/pipeline.py:767 (에이전트 측 C14 거부 전용)
- **무엇이 어긋났나**: parser/ 패키지에 review_queue·enqueue 참조가 0건이고, `parse run`·`ingest-file`이 받은 ParseResult.failures는 stdout에 찍고 끝난다. 큐 kind `hierarchy_unresolved`·`adapter_mismatch`를 enqueue하는 코드는 레포 전체에 없다(cli/platform.py:45-47의 kind 목록에는 있음). 파서 유래 실패가 큐(사람의 작업목록)에 남지 않아 G5(조용히 버리지 않는다)와 §2.7의 전달 계약이 코드에 없다.
- **검증자 보정**: parser/pipeline.py 도크스트링(D-9: 에이전트가 싣는다)과 명세 §2.7(파서 코어가 직접 싣는다)이 착지 주체를 서로 다르게 적고 있어, 구현 시 A1과의 정합(파서가 core/store.py를 import할지, cli 측 run_parse 호출부에서 실을지)을 함께 확정해야 한다.
- **고칠 쪽**: code
- **확인 방법**: `grep -rn "enqueue\|review_queue" parser/` ; `grep -rn "hierarchy_unresolved\|adapter_mismatch" --include=*.py core cli parser | grep enqueue` ; cli/parse.py:110-125 와 cli/ingest.py:140-146 을 열어 failures 처리 확인
- **검증자 근거**: 레포 전체 enqueue 호출부 16곳(core/ingest.py:98, core/gate.py:129, core/build.py 7곳, core/pipeline.py 7곳) 열거 — 두 kind 0건, parse_failure는 pipeline.py:767 _reject(774-783 진입 거부)뿐; parser/pipeline.py:227-231 주석 "문서는 들어가고 큐가 뜬다"는 구현 아님; 2_계약.md:238·7_구현규격과검증.md:60·개정대장.md:70.

### H23. llm-check가 HTTP 401/403을 「②도달 실패」로 보고한다 — ③인증 단계가 도달 불가(죽은 except)
- **판정**: confirmed · 재판정 med · seg doc7-llm
- **종류**: spec_vs_code
- **명세 인용**: 문서 7 §7.6-B-0 (B19) — 「게이트웨이 왕복 1회로 ①설정 ②도달 ③인증 ④응답 형태 ⑤구조화 출력을 단계로 끊어 가른다 — 어디까지 갔는지가 곧 원인이다」
- **코드 좌표**: core/llm.py:340-352 (_post가 HTTPError를 잡아 GatewayError로 바꿔 던짐) · core/llm.py:519-542 (probe의 except urllib.error.HTTPError 분기)
- **무엇이 어긋났나**: _post()는 urllib HTTPError를 잡아 GatewayError(RuntimeError 계열)로 다시 던지므로, probe()의 `except urllib.error.HTTPError` 분기(523행)는 절대 실행되지 않는다. 401/403/404/400 전부가 `except Exception`(536행)으로 떨어져 「② 도달 False · fatal · 프록시 환경변수·타임아웃 확인」으로 보고되고, ③인증 단계는 화면에 나오지 않는다. 사내에서 키가 틀렸을 때 운영자는 프록시를 의심하게 된다.
- **검증자 보정**: detail 문자열에 "HTTP 401 — 본문"이 남아 코드를 아는 운영자는 읽어낼 수 있다 — med. 수정은 probe():523의 except 타입을 GatewayError로 바꾸고 e.code→e.status, e.reason 대체(GatewayError에는 reason 없음).
- **고칠 쪽**: code
- **확인 방법**: python3 -c 로 urlopen을 HTTPError(401) 몽키패치 후 llm.probe() 출력의 id 목록을 본다 — ③이 없으면 재현. 또는 core/llm.py:340-352와 :523을 나란히 읽는다.
- **검증자 근거**: llm.py:313 `class GatewayError(RuntimeError)`(issubclass(…, HTTPError) → False), :337-352 `raise GatewayError(e.code, body, url) from e`; 재현(USE_MOCK=0, 401·403 치환) 모두 stage id가 ①,②에서 종료; cli/llmcheck.py:47-61 "② 도달에서 막혔다" 출력; 7_구현규격과검증.md:225.

### H24. doctor의 「클린 2회 동일 그래프」(완료판정 4) 계측이 죽은 코드다 — _idempotent()가 비교 전에 return한다
- **판정**: confirmed · 재판정 high · seg doc7-outputs
- **종류**: code_missing
- **명세 인용**: 문서 7 §7.6-4 — 「클린 2회 동일 그래프 — 깨끗한 상태에서 전체 빌드를 2회 실행해 큐·그래프가 동일 … 그래프는 바이트 동일, 큐는 집합 동일로 판정한다」
- **코드 좌표**: doctor.py:213-234 (return at :220), doctor.py:360
- **무엇이 어긋났나**: _idempotent()는 :213-220에서 init --fresh만 돌리고 `return r.returncode, residue`로 끝난다. 그 아래 :221-234(run.py all 2회 → snap() 대조 → line("클린 2회 동일 그래프 (완료판정 4)"))는 도달 불가이며, 호출부 run_suites(:360)는 반환값도 버린다. 결과적으로 doctor는 완료판정 4를 한 번도 재지 않으면서 docstring은 「이 판정을 센다」고 말한다. 부수: 살아났더라도 snap()이 data/**/graph.json을 read_bytes로 직접 열어 §7.1 B6(graph.json 직접 열기 금지)에 걸린다. tests/test_g4.py 3.5′는 같은 프로세스 안 재인입 비교이지 클린 2회 바이트 동일이 아니다.
- **검증자 보정**: line()이 실행되지 않으므로 _fail도 늘지 않아 멱등성이 깨져도 doctor는 「온전하다」로 종료. 줄 213-220은 _clean()(:86-93)과 문자 그대로 동일한 블록의 잘못된 복제. 수리: 그 블록을 `rc, residue = _clean()` 호출로 바꾸고 클린 실패 시 NG, 반환값을 호출부에서 받음. B6 부수 지적은 수리 시 GraphStore 경유 바이트 해시 제공 vs 점검 도구 예외 명기를 결정해야 하는 partial 사항.
- **고칠 쪽**: code
- **확인 방법**: `python3 -c "import ast;t=ast.parse(open('doctor.py').read());f=[n for n in t.body if getattr(n,'name','')=='_idempotent'][0];print([(s.lineno,type(s).__name__) for s in f.body])"` → (220,'Return') 뒤에 문장이 이어진다. 또는 `python doctor.py --quick` 출력에 「클린 2회 동일 그래프」 줄이 없음(쓰기 명령 — 미실행).
- **검증자 근거**: ast 실측 본문 순서 [(185 docstring),(199 snap),(213 Assign),(218 Assign),(220 Return),(221…),(223…),(224…),(226…),(228…229),(233 line(...))]; doctor.py:360 반환값 미수신; 대체 계측 부재(test_g4.py:162-193, test_g1_g2.py:429-453, test_g3.py:336·507은 클린 2회 바이트 대조 아님); 7_구현규격과검증.md:202·200, README.md:38, 1_금지와불변.md:49 B6.

### H25. 파서 측 문서 단위 실패(parse_failure·adapter_mismatch)가 review_queue에 착지하지 않는다 — 파서도 에이전트 CLI도 enqueue하지 않는다
- **판정**: confirmed · 재판정 high · seg doc7-outputs (중복: H2 · H3 · H22)
- **종류**: spec_vs_code
- **명세 인용**: 문서 7 §7.1 core/store.py 행 · 문서 2 §2.7 · 문서 6 §6.4-6·7 — 「수정 큐는 파서 공용 코어와 에이전트 두 프로세스가 함께 쓰는 유일한 공유 상태다(문서 2 §2.7 — 파서 측이 parse_failure·adapter_mismatch·hierarchy_unresolved를 직접 착지시킨다)」
- **코드 좌표**: parser/pipeline.py:6-8, cli/parse.py:113-123, cli/ingest.py:141-145, core/pipeline.py:761-767
- **무엇이 어긋났나**: parser/pipeline.py 머리말은 「파서는 큐 파일을 직접 쓰지 않는다 … 에이전트 측 인입은 그 결과를 받아 큐에 싣는다(D-9)」라 하지만, 그 결과(ParseResult.failures)를 받는 에이전트 측 cli/parse.py는 print만 하고 exit 1, cli/ingest.py는 row에 status=FAIL을 담아 print만 한다. 레포 전체에서 enqueue("adapter_mismatch")·enqueue("hierarchy_unresolved") 호출은 0건이고 enqueue("parse_failure")는 core/pipeline._reject뿐이다. 따라서 S7(양식 표류 → adapter_mismatch 큐·차이 내역)·S8(계약 위반 행 → parse_failure 큐)이 명세가 말한 착지 지점(큐)에 도달하지 않고 화면 출력으로만 남는다.
- **검증자 보정**: cli/register.py:1079-1081은 검수 뷰 anomalies에 담을 뿐(운영 큐 아님). hierarchy_unresolved는 res.ok=True로 봉투가 넘어가지만 run_document가 meta.hierarchy_unresolved를 읽지 않는다.
- **고칠 쪽**: code
- **확인 방법**: `grep -rn 'adapter_mismatch' --include=*.py core cli parser`로 enqueue 호출이 없음을 확인하고, cli/parse.py:113-123과 cli/ingest.py:141-145를 열어 failures 처리가 print뿐임을 본다.
- **검증자 근거**: `grep -rn 'enqueue(' --include=*.py .` 리터럴·변수 kind 전부(core/ingest.py:98 duplicate_doc_hold, core/gate.py:129 direction_*, core/pipeline.py:709 orphan_attach 포함) 확인 — 두 kind 0건, parse_failure는 pipeline.py:767 1건; cli/parse.py:66-85 run_parse는 ok일 때만 parsed/ 파일 씀; tests/test_p1.py:137-144 S7은 res.failures만 봄; 명세 2_계약.md:238·7:60·6:92,94·1:85-86·4:151-152.

### H26. F1(문서 1)은 Unit을 「스코프 없는 카테고리」라 하는데 문서 4 §4.5-3·config·코드는 Unit을 스코프 대상으로 둔다 (B26 미러 미반영)
- **판정**: confirmed · 재판정 med · seg doc1-invariants
- **종류**: spec_vs_spec
- **명세 인용**: 문서 1 F1 (1_금지와불변.md:162) ↔ 문서 4 §4.5-3 (4_쓰기절차.md:112) · 개정대장 §Q B26 — 「F1: 단 스코프 카테고리(현행 Property) 한정. 스코프 없는 카테고리(Unit)는 극성 결합을 유지한다 / 문서4 §4.5-3: canonical_scope.bind_categories — B26 이후 Property·Unit」
- **코드 좌표**: layers/process/config.json:31-35 (bind_categories: [Property, Unit]) · core/build.py:307-312 (scoped_category → anchor_polarity 유지·생략 적용) · core/naming.py:76 scope_canonical
- **무엇이 어긋났나**: B26(08-28)이 문서 4만 고치고 문서 1 F1·F4·부록 「canonical 스코프」 항목을 그대로 두어, 최상위 검사기 문면대로면 현행 코드(Unit에 스코프 접두 `노칭::커터` + 부착 노드 극성 확정 시 Unit도 표면형 결합 생략)는 F1 위반이다. core/build.py:293-296 docstring도 D-57 구판을 말해 코드 문서와 실제 동작이 어긋난다. 미러쌍.json에 F1↔§4.5 쌍이 없어 점검_미러도 못 잡는다.
- **검증자 보정**: F1의 작동 규칙 자체(「스코프 카테고리 한정」)는 config를 참조하는 형태라 실제 동작은 B26 의도와 일치 — 코드 동작 변경은 불필요. 고칠 것은 문면(문서 1 F1 괄호 예시·F4·부록_용어:66·build.py:293-296 docstring·tests/test_g3.py:126 show 문구)과 미러쌍 등재.
- **고칠 쪽**: both (코드 몫은 docstring·테스트 문구)
- **확인 방법**: `grep -n 'bind_categories' -A3 layers/process/config.json; sed -n 162p docs/spec/1_금지와불변.md; sed -n 112p docs/spec/4_쓰기절차.md;` `python -c "from core.bootstrap import load_config; from core.naming import scope_canonical; cfg=load_config('process'); print(scope_canonical('커터','Unit','노칭',cfg))"` → ('노칭::커터', True)
- **검증자 근거**: 1_금지와불변.md:162·165, 4_쓰기절차.md:112·114, 개정대장.md:1200(B26 표제 「문서 4 §4.5」만), 부록_용어.md:66, config.json:32-35, build.py:307-312·293-296, 실행 ('노칭::커터', True), 미러쌍.json 4쌍(I14·C15·C19·M9)뿐, test_g3.py:126-135.

### H27. 가이드 §7의 구판 등록분 재생성 경로(generate --resume · review --instruct)가 confirm에서 「이름 중복」으로 막힌다 — CLI에 재등록·변형 등록 경로가 없다
- **판정**: confirmed · 재판정 high · seg guide-vs-code
- **종류**: doc_stale (+ 코드 측 §6.5 규격 4 물리 배선 미구현)
- **명세 인용**: docs/가이드/2B_작업가이드.md §7 「뺀 열을 또 묻는다」·「adapter_mismatch」 행 · docs/spec/6_파서와구축모드.md §6.5 「doc_type의 구조 — 스키마 1 + 어댑터 N」 — 「구판 등록분 — `generate <doc_type> --resume`으로 스키마만 다시 / 문서 양식이 바뀌었다 — `review --instruct`로 어댑터 재생성」
- **코드 좌표**: core/registry.py:106-109 · cli/register.py:1493-1499 · cli/register.py:528-529 · core/registry.py:120(unregister — 호출부 tests/ 2곳뿐)
- **무엇이 어긋났나**: 가이드 §7은 이미 확정된 doc_type을 `generate --resume` 또는 `review --instruct`로 재생성해 다시 쓰라고 하지만, 그 뒤의 `confirm`은 `registry.register()`가 등록부에 같은 이름이 있으면 ValueError('doc_type 이름 중복')를 던져 등재하지 못한다. 등재를 지우는 `unregister`는 CLI 호출부가 0(테스트만)이고 `register revise`/변형 등록 서브커맨드도 없다. 또 `review/`는 추적하지 않는 작업 폴더라 state.json이 없으면 `review`는 「생성 단계가 먼저다」로 멈추고, `generate`(--resume 없이)는 528행에서 이름 중복으로 거부한다 — 구판 등록분을 고칠 수 있는 CLI 경로가 실제로는 없다.
- **검증자 보정**: `--resume` 분기(:494)는 528행 중복 검사보다 앞이라 통과하지만 review/<doc_type>/input_package.json(:495)이 있어야 하고, 종착 confirm(:1494 try 없음)에서 반드시 막힌다. 명세 6 §6.5(:157-164) 규격 4 「물리 배선은 구현이 정하고 보고한다」의 배선(변형 등록·adapter_version 갱신)이 구현에 없다 — 가이드(doc_stale)와 코드 양쪽.
- **고칠 쪽**: both
- **확인 방법**: `grep -n '이름 중복' core/registry.py cli/register.py` 와 `grep -rn 'unregister(' --include=*.py .` 로 호출부를 센다; cli/register.py:1536-1609 main 분기 목록을 읽는다. 등록된 doc_type에 review --instruct → confirm을 돌리면 ValueError(쓰기 명령 — 미실행).
- **검증자 근거**: core/registry.py:106-109 무조건 ValueError; cli/register.py:1471-1499 cmd_confirm이 :1494 register()를 try 없이 호출; unregister 호출부 = core/registry.py:120 정의 + tests/test_p3.py:58,959; main 분기 roles/generate/review/confirm/list 5개(:1536-1609); .gitignore가 review/ 추적 제외; 2B_작업가이드.md:271-272.

## high 반박된 것

- 없음 — 입력 (A)의 `refuted` 목록이 비어 있다(0건). 27건 중 26건 confirmed, 1건 partial(H15 — 결함은 확인, 원인 귀속 「명세 공백」을 「명세 자기모순 + 코드 갭」으로 정정).

## 미검증 med/low 227건

절별로 묶어 전부 나열한다(med 110 · low 117). 항목 앞 `#n`은 입력 (B) 배열의 0-기준 인덱스다. 형식: `#n [severity · kind] 제목 — spec_ref — code_ref (중복: …)`. 「중복」은 같은 사안이 다른 절(또는 위 high)에서도 검출된 경우이며, 어느 것도 삭제하지 않았다. 이 227건은 **검증되지 않았다** — 반박 검증 전에는 사실로 취급하지 말 것.

### doc1-invariants — 8건

- **#206** [med · spec_vs_spec] 이미지 placeholder 조각의 `context`가 문자열이다 — C32·문서 2 §2.2(임의 딕셔너리)와 문서 6 §6.4-3(맥락 텍스트)이 서로 다르고 코드는 문서 6을 따른다 — spec_ref: 문서 1 C32 (1_금지와불변.md:109) · 문서 2 §2.2 (2_계약.md:49) ↔ 문서 6 §6.4-3 (6_파서와구축모드.md:87) — code_ref: parser/adapters/basic_ppt.py:104-112·241 ("context": context — str) · parser/adapters/basic_pdf.py:70·95 · parser/tagger.py:228 (context=r.get("context") or "") · parser/validator.py:51-53 (봉투 context만 dict 검사, 조각은 미검사)
- **#207** [med · spec_vs_code] 좌표 태깅(⑨) 실호출 지시문이 prompts/ 파일이 아니라 core/llm.py에 인라인이다 — §7.6-B-5가 `coord_tag` 템플릿을 명시하고 B11·B1도 걸린다 — spec_ref: 문서 7 §7.6-B-5 (7_구현규격과검증.md:236) · §7.1 관리 자산의 원칙 (:28) · 문서 1 B11·B1 — code_ref: core/llm.py:724-730 pick_coord — system 메시지 "문서가 말한 공정 이름이 아래 목록의 어느 항목인지 고른다…" 인라인; prompts/ 디렉터리에 coord_tag.md 없음(answer·extract·image_summary·interview·judge·link·struct_map 7종뿐) **(중복: doc7-llm #178)**
- **#208** [med · code_missing] structural_proposal 큐의 생산자가 코드에 0이다 — 문서 4 §4.9 ①LLM 감지→②제안 경로 미구현 — spec_ref: 문서 4 §4.9 (4_쓰기절차.md:200-206) · 문서 1 N9 (1_금지와불변.md:390) · G7 닫힌 20종 표(4_쓰기절차.md:149) — code_ref: core/·cli/·parser/ 전체 — "structural_proposal" 문자열은 cli/platform.py:44 QUEUE_KINDS 열람 상수에만 존재; enqueue 호출 0 **(중복: doc4-queue-ops #53)**
- **#209** [low · spec_vs_code] `python -m cli.extract`(LLM 지점 ① 추출)에 모드 1줄 표시와 mock 관문이 없다 — spec_ref: 문서 7 §7.6-B-1 (7_구현규격과검증.md:232) · 문서 1 B12·B48 계보 — code_ref: cli/extract.py:29-91 (mode_line·require_live_or_allow 호출 없음) · run.py:206-208 (관문 대상 build/ingest/query만) · run.py:95-112 cmd_all(구축을 관문 없이 실행, 모드 줄만 표시) **(중복: doc7-outputs #196 · guide-vs-code #217)**
- **#210** [low · spec_vs_code] kit/run_adapter.py가 sys.path.insert로 레포 루트를 붙인다 — 「sys.path 조작으로 붙이지 않는다」와 어긋나며 회귀는 cli/만 센다 — spec_ref: 문서 7 §7.1 패키지화 (7_구현규격과검증.md:48) · CLAUDE.md §5 — code_ref: kit/run_adapter.py:28 sys.path.insert(0, …parent.parent) · cli/register.py:724 subprocess.run([sys.executable, KIT/'run_adapter.py', …])로 스크립트 실행 · tests/test_g1_g2.py:86 (cli/ 8종만 검사)
- **#211** [low · spec_gap] 닫힌 20종 kind가 코드로 강제되지 않는다 — enqueue는 임의 문자열을 받고 목록 상수는 단일 진입 모듈이 아니라 cli/platform.py에 산다 — spec_ref: 문서 1 G7 (1_금지와불변.md:201) · 문서 4 §4.7-3 (4_쓰기절차.md:139) · §4.7 큐 접근 관문 (:158) — code_ref: core/store.py:213-236 enqueue(kind, …) — kind 검증 없음 · cli/platform.py:38-48 QUEUE_KINDS(열람 화면 상수) · tests/ 에 「enqueue kind ⊂ 20종」 어서션 없음
- **#212** [low · doc_stale] §7.1 패키지화 문면(layers/에 __init__.py · import는 절대경로)이 실물과 다르다 — spec_ref: 문서 7 §7.1 (7_구현규격과검증.md:48) · 문서 1 B2 — code_ref: layers/ (process·quality 디렉터리만, __init__.py 없음) · core/·parser/ 23파일에 상대 import 79건(예: core/build.py:53 from .bootstrap import …, parser/pipeline.py:18 from . import normalizer …) **(중복: doc7-layout #165 일부)**
- **#213** [low · doc_stale] 점검_미러의 M9 봉인이 본문 §6.5 하네스 문단의 첫 문장까지만 덮는다 — 표·후속 3문단(자동 1회·by 병기·[정정] 40 재관문)은 미봉인 — spec_ref: 문서 1 M9 (1_금지와불변.md:309) ↔ 문서 6 §6.5 (6_파서와구축모드.md:140-151) · 개정대장 §AG 「미러쌍 2건 등재」 — code_ref: docs/회귀스위트/점검_미러.py:42 (_extract가 "\n| "에서 문단을 끊음) · docs/회귀스위트/미러쌍.json M9 항목(body anchor 「**하네스는 생성 안에서 돌고…**」)

### doc2-roles-schema — 12건

- **#0** [med · spec_vs_code] process_group anchor 미스가 조용히 통과한다 — orphan_anchor도 coord_mismatch도 없다 — spec_ref: 문서 2 §2.4-① · §2.2 계약 ②-1·3·4 — code_ref: core/pipeline.py:402 (process_group은 check_coord로만) · core/build.py:170-180 (`if not group_surface or not ref_id: return True` · dict 미스 시 `return True  # 골격 밖 → orphan_anchor 경로가 이미 처리`)
- **#1** [med · spec_vs_code] process_ref의 리스트 값이 missing_field가 아니라 orphan_anchor로 간다 — 영구 재시도 항목 — spec_ref: 문서 2 §2.4 「리스트 값의 role별 처리」 · §2.7 「리스트 값 방어」 — code_ref: core/pipeline.py:398 (_row_anchor가 rec.get("process_ref")를 _scalar 없이 resolve_anchor에 직접 넘김) · core/pipeline.py:430 (_scalar 방어는 _row_roles의 fields 순회에만) · core/build.py:126,162 (norm(list)→"['a', 'b']" 문자열 키로 조회 후 '골격에 없는 좌표'로 orphan_anchor)
- **#2** [med · spec_vs_code] anchor 필드에 target_category가 없으면 KeyError로 빌드가 죽는다 — 명세는 생략 시 공정 골격 — spec_ref: 문서 2 §2.4-① · §2.5 규약 2 — code_ref: core/pipeline.py:248 (`spec["target_category"]` 첨자 접근) · kit/run_adapter.py:244-300 (하네스는 anchor의 target_category를 검사하지 않음) · kit/생성프롬프트_템플릿_v1.0.md:112 ("target_category(공정 골격이 아닌 다른 골격을 볼 때만)")
- **#3** [med · spec_vs_code] orphan_anchor 항목이 어느 anchor 필드의 것인지 구분하지 않아 재시도가 pending_attrs를 엉뚱한 노드에 저장하고, 비좌표 anchor의 생략 엣지는 복구 없이 항목만 내려간다 — spec_ref: 문서 2 §2.4-③ · §2.4-① — code_ref: core/pipeline.py:192-202 (_land_deferred: 행의 모든 defer 항목에 같은 dropped_edges·pending_attrs를 복제) · core/pipeline.py:462-466 (pending에 attach_to_field를 싣지만) · core/retry.py:260-265 (`b.put_attribute(ref, a["attr_name"], ...)` — attach_to_field 무시, 해소된 anchor에 저장) · core/retry.py:243-258 (`@process_ref`만 ref로 치환, 필드명 끝점은 revived에서만 찾음) · core/retry.py:266 (엣지 복구 0건이어도 store.drop) **(중복: high doc4-write(pending_attrs 재시도) 일부)**
- **#4** [med · spec_vs_code] core가 use_blocks를 읽지 않는다 — 블록 필드를 STRUCTURAL로 하드코딩하고 겹침 규칙이 없다(하네스와 core의 동작이 갈린다) — spec_ref: 문서 2 §2.5 규약 1·3·4 · §2.7 — code_ref: core/pipeline.py:24-25 (STRUCTURAL = {..., "process_group", "process_ref", "process_no", "source_locator", ...}) · core/pipeline.py:33-35 (blocks.json에서 process_ref.target_category만 상수로 읽음) · core/pipeline.py:398-402 (rec.get("process_ref")·rec.get("process_group") 직독) · kit/run_adapter.py:49-73 (하네스는 use_blocks를 전개하고 '스키마 선언이 블록을 이긴다'를 구현) **(중복: high doc2-roles-schema(process_ref null) 일부)**
- **#5** [low · spec_vs_spec] 문서 6 §6.5는 좌표 블록 필드·source_locator를 '구조 필드(C17)'라 부르고, 문서 2 §2.5 규약 3·C17은 블록 role 필드라 한다 — spec_ref: 문서 2 §2.5 규약 3 ↔ 문서 6 §6.5(132행, [정정] 40) ↔ 문서 1 C17 — code_ref: core/pipeline.py:24-25 (문서 6 쪽 독해대로 STRUCTURAL에 포함) · kit/run_adapter.py:43-44,255 (블록 필드를 '선언된 필드'로 role 루프에 넣되 struct 집합에도 합침) **(중복: doc6-review-view #138)**
- **#6** [low · doc_stale] §2.5 규약 6·규약 1이 B49의 스키마 `unmappable` 키를 반영하지 못했다 — '스키마에 넣지 않고' — spec_ref: 문서 2 §2.5 규약 1·규약 6 ↔ 문서 1 C19 · 문서 6 §6.5 (B49·[정정] 40) — code_ref: cli/register.py:229,268 (_ROLES에 unmappable · GENERATE_SCHEMA required [field, kind, reason]) · schemas/ppt_process.json (헤더에 명세 밖 `payload_kind` 키) · cli/register.py:1582 (schema.payload_kind를 읽어 prose 판정)
- **#7** [low · spec_gap] pending_attrs 형식에 contextual이 없다 — 재시도가 context 유무로 맥락형을 추정해 정상 경로와 다른 그룹에 저장한다 — spec_ref: 문서 2 §2.4-③ · §2.5 규약 2 — code_ref: core/pipeline.py:462-466 (`"context": r.ctx or None` — contextual 여부와 무관하게 봉투·레코드 context를 실음, contextual 플래그 미보존) · core/retry.py:260-264 (`bool(a.get("context"))`를 contextual로 사용) · core/build.py:357-361 (정상 경로는 contextual=False면 context={}로 강제) **(중복: high doc4-write(pending_attrs contextual) 일부)**
- **#8** [low · spec_gap] entity의 attach_to_field — 명세가 속성으로 열거하지만 정형 경로에서 해소된 대상에 붙이는 코드가 없다(폴백 빈 셀 검사에만 쓰인다) — spec_ref: 문서 2 §2.4-② · §2.5 규약 2 · 문서 4 §4.4-4·5 — code_ref: core/pipeline.py:255-295 (h_entity는 spec.attach_to_field를 읽지 않음) · core/pipeline.py:540-544 (_row_fallback: attach_to_field는 빈 셀이면 폴백 생략하는 B12 검사에만 사용, 값이 있고 해소됐어도 대상이 아니라 좌표에 붙임) · grep orphan_attach core/pipeline.py → build_prose(709)에만 **(중복: high doc4-write(attach_to_field 미소비) 일부)**
- **#9** [low · spec_vs_code] L3 '핸들러 함수 1개 — 기존 핸들러 무수정'과 달리 role 닫힌 목록이 4곳에 복제돼 있고 _row_roles에 role별 분기가 있다 — spec_ref: 문서 2 §2.6 L3 · §2.4 경계 — code_ref: core/pipeline.py:340-341 (HANDLERS) · core/pipeline.py:430,439-442 (role 문자열 분기: entity/anchor 스칼라 방어, attribute/content 후처리 큐잉) · core/build.py:27 (ROLE_HANDLERS — 호출부 0) · kit/run_adapter.py:31 (ROLES) · cli/register.py:229 (_ROLES)
- **#10** [low · spec_gap] schema_version이 인입 산출물 어디에도 기록되지 않는다 — '병기'의 자리가 명세에 없다 — spec_ref: 문서 2 §2.5 규약 1 — code_ref: core/ingest.py:257,291 (adapter_version만 청크에 복사) · core/ingest.py:201-204 (register_doc 항목에 schema_version 없음) · core/registry.py:106-112 (register()는 adapter_version만 저장, schema_version은 _builtin에서만 읽음)
- **#11** [low · spec_gap] table content의 부착 대상 미해소 처분이 명세에 없다 — 코드는 결함 로그만 남긴다 — spec_ref: 문서 2 §2.4-④ — code_ref: core/pipeline.py:475-490 (_row_contents: 대상 미해소 시 store.append_defect만, 큐·재시도 손잡이 없음)

### doc2-contractA — 6건

- **#12** [med · spec_vs_code] ㉡ --allow-duplicate 예외가 새 doc_id 쪽에만 등재되어, 원본 문서의 정상 재인입이 이후 duplicate_doc_hold로 보류된다 — spec_ref: 문서 2 §2.7 ① 진입 검증 — code_ref: core/ingest.py:80-83 (예외 조회는 자기 doc_id 항목만) · core/ingest.py:86-89 (예외를 reg[doc_id]에만 등재)
- **#13** [med · spec_vs_code] duplicate_doc_hold의 사람 판단이 항목에 기록되지 않는다 — ㉠은 기록 경로 자체가 없고(resolve_item 호출부 0), ㉡은 항목을 삭제한다 — spec_ref: 문서 2 §2.7 ① 진입 검증 · 문서 4 §4.4-7 · 문서 7 §7.2 resolution — code_ref: core/ingest.py:90-91 (store.drop — ㉡ 항목 삭제) · core/ingest.py:72 (㉠ = 「아무것도 하지 않는다」) · core/store.py:165-187 resolve_item (비테스트 호출부 0 — cli/platform.py:499는 안내 문자열) **(중복: doc7-ids-records #172)**
- **#14** [med · spec_vs_code] parsed_at이 실행 시점이 아니라 상수 '2026-01-05T00:00:00'으로 고정된다 — 호출부가 값을 넘기지 않는다 — spec_ref: 문서 7 §7.5 S14 비교 제외 필드 · 문서 5 §5.1-6 · 문서 7 §7.2 · 문서 2 §2.7 ①(시점 제시) — code_ref: parser/pipeline.py:123 (기본값 parsed_at='2026-01-05T00:00:00') · parser/tagger.py:244 · cli/parse.py:78 · cli/register.py:1367 (둘 다 parsed_at 미전달) · core/ingest.py:200,258 (그 값이 first_ingested_at·청크 parsed_at으로 복사)
- **#15** [low · spec_vs_code] duplicate_doc_hold 항목에 명세가 요구한 「크기·시점」이 실리지 않는다 — spec_ref: 문서 2 §2.7 ① 진입 검증 — code_ref: core/ingest.py:98-106 (payload: doc_id·existing_doc_id·doc_hash·source_path·existing_source_path·revision)
- **#16** [low · spec_gap] doc_hash 산식이 명세에 없고, 코드는 revision·doc_type·meta(모드·환경 의존 값)를 해시에 넣는다 — spec_ref: 문서 2 §2.2 계약 ①-부속 doc_hash 행 · 문서 7 §7.2 doc_hash 불릿 — code_ref: core/ids.py:89-97 (parts = doc_type, revision, 조각의 source_locator 제외 전 키 — meta 포함) · core/ingest.py:250-252 (hash 변화 시 extract 체크포인트 invalidate)
- **#17** [low · spec_vs_code] chunk_id 충돌 시 결함 로그를 남기되 앞 청크를 덮어쓴다 — 「조용히 덮어쓰지 않는다」의 독해가 갈린다 — spec_ref: 문서 2 §2.7 ② 근거 축 id 계산 · 문서 7 §7.2 — code_ref: core/ingest.py:276-285 (충돌이면 append_defect 후 그대로 chunks['chunks'][cid] = {...} 대입) **(중복: doc7-ids-records #176)**

### doc3-config — 10건

- **#18** [med · code_missing] config 문법 키 19종 닫힘의 강제 지점이 코드에 없다 — 일람 밖 키는 조용히 무시된다 — spec_ref: 문서 3 §3.1 층 config 키 일람 (+ §3.3-3 명시적 실패) — code_ref: core/bootstrap.py:58-59 (load_config) · core/skeleton.py:352-377 (plant — type만 검증)
- **#19** [med · doc_stale] config작성_가이드가 「부팅 실패 = 명시적 실패가 어느 키인지 말해 준다」고 하나 키 검증 코드가 없다 — spec_ref: docs/가이드/config작성_가이드.md §7 (문서 3 §3.1 키 일람·§3.3-3을 근거로 인용) — code_ref: docs/가이드/config작성_가이드.md:137-138, 188 · core/bootstrap.py:58-59
- **#20** [med · spec_vs_code] §3.4 Property 정의문 「원문」이 실물 config와 다르다 — 측정방법·주기·샘플 제외 문구가 주입되지 않는다 — spec_ref: 문서 3 §3.4 카테고리 3종 (닫힌 목록 · 정의문 원문) — Property — code_ref: layers/process/config.json:6 · core/extract.py:190-197 (categories를 추출 프롬프트에 주입)
- **#21** [low · spec_vs_code] seed 마커·ALIASES 모호의 「명시적 실패」가 raise만 하고 로그(explicit_fail)를 남기지 않는다 — spec_ref: 문서 3 §3.9-⑹ (3.3 명시적 실패 인용) · 문서 7 §7.6 로그 규격 — code_ref: core/skeleton.py:133, 143, 165, 173, 311-315 (SeedError raise · log 없음) vs core/skeleton.py:374-377 (plant — explicit_fail 후 raise)
- **#22** [low · spec_vs_spec] 골격 노드 canonical 생성 규칙이 명세에 없고, 부록은 「sub/detail 균일 접두(문서 3)」라 하나 코드는 detail만 접두한다 — spec_ref: 부록_용어 「canonical 스코프」 항목 ↔ 문서 3 §3.9-⑴⑺ (해당 규칙 부재) — code_ref: core/skeleton.py:174-175 (_concept: tier != detail이면 name 그대로) · core/skeleton.py:189-197 (_check_name_collision main·sub만) **(중복: doc3-skeleton #30)**
- **#23** [low · spec_vs_spec] §3.6-2 근거의 「Tier2 anchor 스키마를 로더가 거부」는 코드·문서 2와 다르다 — 거부는 인입 시 행 단위 큐다 — spec_ref: 문서 3 §3.6 일반 규칙 2 근거절 ↔ 문서 2 §2.4-① 「Tier1 한정」 — code_ref: core/build.py:129-161 (resolve_anchor Tier1 한정 → hold/orphan_anchor) · core/ingest.py:31-39 (load_schema — 등록부 조회만) · kit/run_adapter.py:244-300 (check_schema — target_category 대조 없음)
- **#24** [low · spec_vs_code] 극성 게이팅 ③(층 config polarity 미선언) 미충족 시 polarity가 unbound가 아니라 none으로 기록된다 — spec_ref: 문서 3 §3.9-⑻ polarity 닫힌 4값 — code_ref: core/naming.py:61-68 (derive_polarity)
- **#25** [low · spec_gap] 인라인 data 골격(품질층)은 §3.7 확정 절차 대상에서 코드가 제외했고, 확정 산출 파일이 §3.3-1 층 폴더 목록과 어긋난다 — spec_ref: 문서 3 §3.7 규약 3 확정 절차의 물리 규격 ↔ §3.3-1 · §3.5 — code_ref: cli/skeleton.py:105-110 (source 없는 층은 확정 거부) · cli/skeleton.py:141-150 (layers/{층}/skeleton.prev.json·confirmations.json 기록)
- **#26** [low · spec_gap] skeleton 선언의 하위 문법(category 필수·flat의 tier)이 명세에 없다 — spec_ref: 문서 3 §3.1 ② · §3.3-3 · §3.9-⑵⑺ — code_ref: core/skeleton.py:227 (skel["category"] 필수) · core/skeleton.py:365-368 (flat: tier=main·polarity=none 고정) · core/bootstrap.py:190, 145
- **#27** [low · doc_stale] 품질층 config의 prompts.extract_enabled·「훅 활성」과 prompts.extract는 어떤 코드도 읽지 않는다 — spec_ref: 문서 3 §3.1 ④ (config.categories·prompts) · 문서 4 §4.10 프롬프트 자산의 위치 — code_ref: layers/quality/config.json:61-65 · core/matcher.py:198-199 (prompts.judge만 읽음) · core/extract.py:186-197 (prompts/extract.md 파일 + categories·relations 조립) · core/pipeline.py:798-806 (prose면 무조건 추출)

### doc3-skeleton — 10건

- **#28** [med · spec_vs_code] skeleton-confirm의 뷰가 명세가 요구하는 「mirrors 쌍·모호 이름」을 렌더하지 않는다 — spec_ref: 3_구조.md §3.7 규약 3 (B28 확정 절차 물리 규격) — code_ref: cli/skeleton.py:64, cli/skeleton.py:118-120 (대조: core/bootstrap.py:213-222)
- **#29** [med · doc_stale] 골격작성_가이드의 「확정 절차」가 skeleton-confirm을 거치지 않고 확정 전에 bootstrap을 돌린다 — spec_ref: 3_구조.md §3.7 규약 3 (B25·B28) — code_ref: docs/가이드/골격작성_가이드.md:95-105 (대조: cli/skeleton.py:4, run.py:189-192, run.py:235)
- **#30** [med · spec_vs_code] 골격 sub 노드 canonical에 부모 접두가 없다 — 문서 1 F4·부록은 「sub/detail 균일 접두」라 하고 문서 3 §3.9는 canonical 산식 자체가 없다 — spec_ref: 1_금지와불변.md F4 · 부록_용어.md「canonical 스코프」 · 3_구조.md §3.9 (산식 부재) — code_ref: core/skeleton.py:174-175, core/skeleton.py:189-197 **(중복: doc3-config #22)**
- **#31** [med · spec_gap] 축값의 원천이 둘(seed AXIS_LABELS 키 vs config polarity.values)인데 일치 검사가 없고 명세도 정본을 지정하지 않는다 — spec_ref: 3_구조.md §3.9 ⑻ · 1_금지와불변.md B1 · 부록_용어.md「골격 seed 마커」 — code_ref: core/skeleton.py:220 (labels=seed AXIS_LABELS) · core/skeleton.py:132·162·264 · core/naming.py:61-66·73 (values=cfg.polarity.values) · core/build.py:434
- **#32** [med · code_missing] resolve_entity에 골격 카테고리 가드가 없어 실LLM 경로에서 Process·FailureEffect auto 노드가 생성될 수 있다 — spec_ref: 3_구조.md §3.5 FailureEffect · §3.6 일반 규칙 4 · §3.4 · 부록_용어.md「골격」 — code_ref: core/build.py:298-344 (resolve_entity) · core/pipeline.py:651-655 · core/extract.py:104-109
- **#33** [low · spec_gap] 사람 ALIASES의 같은 표면형이 여러 노드에 걸릴 때의 규칙이 없고, 태거는 첫 노드를 조용히 고른다 — spec_ref: 3_구조.md §3.9 ⑹ — code_ref: layers/process/skeleton.json:24·30·44 · core/skeleton.py:317-318 · parser/tagger.py:40-46 · core/build.py:157-159
- **#34** [low · spec_gap] _plant가 status를 보지 않고 canonical+category로 기존 노드를 재사용해, auto 노드가 골격 자리에 앉으면 seed 지위를 얻지 못한다 — spec_ref: 3_구조.md §3.9 ⑽ · §3.4「골격 개정은 … 사람이 파일로만 한다」 — code_ref: core/skeleton.py:332-336 · core/bootstrap.py:129-131·102-104
- **#35** [low · spec_gap] loader가 골격 구분자를 config canonical_scope.sep에서 읽는데 명세는 `::`를 코드가 아는 구문 마커로 고정하고 sep 키를 두지 않는다 — spec_ref: 3_구조.md §3.9 ⑵·⑻ · §3.1 config 키 일람 · 1_금지와불변.md B1 — code_ref: core/skeleton.py:221 · core/skeleton.py:130·175·182
- **#36** [low · spec_vs_spec] §3.3-1 층 폴더 구성 목록이 §3.7 B28이 추가한 confirmations.json·skeleton.prev.json을 빠뜨린다 — spec_ref: 3_구조.md §3.3 규약 1 ↔ §3.7 규약 3 (B28) — code_ref: cli/skeleton.py:35-36·148·153 **(중복: doc3-config #25 일부)**
- **#37** [low · doc_stale] 골격작성_가이드가 「마커는 넷뿐」이라 하여 단극성 마커(@cathode/@anode)를 누락한다 — spec_ref: 3_구조.md §3.9 ⑷ · 부록_용어.md「골격 seed 마커」(닫힌 7종) — code_ref: docs/가이드/골격작성_가이드.md:37·70·81 (대조: core/skeleton.py:162-163)

### doc4-write — 12건

- **#38** [med · spec_vs_code] 재시도가 되살리는 entity(dropped_entities)에 그 행의 electrode_type이 전달되지 않아 극성 결합·하강 부착·polarity 파생 필드가 빠진 노드가 선다 — spec_ref: 문서 4 §4.5-2·§4.5-3 ⓪ (극성 결합 3조건 · 하강 부착) · §4.7-3 orphan_anchor payload — code_ref: core/retry.py:234-236 (resolve_entity(surface, category, prov, parent_canonical=parent) — electrode_type·anchor_polarity 없음), core/pipeline.py:281-283 (dropped_entities에 electrode_type 미보유)
- **#39** [med · spec_vs_code] 비정형 attach: 대상은 해소됐는데 카테고리쌍 매핑이 없으면 큐도 결함 로그도 없이 조용히 생략된다 — spec_ref: 문서 4 §4.7-2 · §4.4-5 — code_ref: core/pipeline.py:717-721 (rel = pair_relation(...); if rel: commit_edge(...)) — else 분기 없음; 비교 core/pipeline.py:140-145 (_fallback_attach는 append_defect) **(중복: high doc4-write(category_pair_map, prose attach 무기록 생략) 일부)**
- **#40** [med · spec_vs_code] §4.5-6 병합 배제가 한 방향만 구현됐다 — 스코프 붙은 표면형을 해소할 때 부모 미해소 노드가 후보에 남아 포함 규칙(0.95)으로 오병합된다 — spec_ref: 문서 4 §4.5-6 — code_ref: core/matcher.py:127-128 (`if not scoped and n.get("_scoped"): continue`), core/build.py:328-329 (_scoped는 스코프 성립 시에만 기록)
- **#41** [med · spec_vs_code] retry_orphans가 시작 시 연 Dictionary를 말미에 save 하여 재시도 중 Builder가 등재한 사전 엔트리를 덮어쓴다 — spec_ref: 문서 4 §4.8-2 ② 보존 · §4.3-4 · §4.7-5 — code_ref: core/retry.py:50 (dic = Dictionary.open()), core/retry.py:69 (dic.save()), core/retry.py:234-236 (Builder → build.py:37 별도 Dictionary.open() → b2.flush()가 새 등재를 저장)
- **#42** [med · spec_gap] 비정형 청크의 좌표가 null(태깅 실패·미기재)일 때 스코프 카테고리 entity의 처분이 명세에 없다 — orphan_anchor 항목도 없어 드롭하면 무기록, 만들면 B14 위반 — spec_ref: 문서 4 §4.4 「좌표 미해소의 연쇄」 · §4.10 프롬프트 자산 문단(「청크의 좌표가 null이면」) · §4.7-2 — code_ref: core/build.py:123-124 (surface 비면 (None,None) 반환 — 큐 없음), core/pipeline.py:644-655 **(중복: high doc4-write(prose B14)·high doc2-roles-schema(process_ref null) 명세 공백 성분)**
- **#43** [med · spec_gap] 정형에서 entity의 '부착 여부' 판정 기준이 명세와 다르다 — edges 선언에 끝점으로 등장하기만 하면(커밋 여부·상대 끝점 공란 무관) 규칙 B 폴백에서 제외된다 — spec_ref: 문서 4 §4.4-4 (B12) · 문서 2 §2.4-② — code_ref: core/pipeline.py:525-535 (_row_fallback touched 집합), core/pipeline.py:502-514 (미해소 끝점도 dropped·게이트로)
- **#44** [low · spec_vs_spec] 문서 3 §3.5는 커밋 게이트 관할을 「경로 ③ 한정」이라 쓰고, 문서 4 §4.4는 「③ 한정으로 좁히지 않는다」고 쓴다 — spec_ref: 문서 3 §3.5 품질지식층 (category_pair_map 불릿) ↔ 문서 4 §4.4 — code_ref: core/gate.py:47-49, 103-143 (②③④ 전부 commit_edge 경유 — 문서 4 쪽 구현), core/gate.py:24 docstring은 문서 3 쪽 문안
- **#45** [low · spec_vs_code] 게이트의 「관계 자체가 미선언」 판정이 config `relations`가 아니라 relation_patterns의 rel 집합으로 계산돼, 선언은 됐으나 패턴이 없는 관계가 invalid_pattern이 아닌 undeclared_relation으로 집계된다 — spec_ref: 문서 4 §4.4 커밋 게이트 4분기 · 「층의 관계 이름 선언은 config relations 배열이 하고」 — code_ref: core/gate.py:57 (declared = {p['rel'] for p in pats}), core/gate.py:76-77
- **#46** [low · spec_vs_code] attach_to 해소(_dict_hit)와 orphan 재시도(_pick_cat/_pick_any)는 matcher.match를 cfg 없이 불러 실호출 갈래에서 층 어휘(정의문·비대칭 기준)가 주입되지 않고 임계도 config 값을 쓰지 않는다 — spec_ref: 문서 4 §4.3-6 · §4.3 (재사용 세 지점) — code_ref: core/pipeline.py:756 (matcher.match(surface, cands, category)), core/retry.py:79·103 (동일), core/matcher.py:181-199 (_judge_live가 cfg.prompts.judge를 조립)
- **#47** [low · spec_vs_code] 빌드 말미 sweep 순서가 mirrors 재평가 → evidence_lost → orphan 재시도라, 재시도가 세운 노드·엣지의 mirror_asymmetry·evidence_lost 판정이 한 빌드 지연된다 — spec_ref: 문서 4 §4.7-5 · §4.8-5 — code_ref: core/pipeline.py:559-566 (finalize: link_mirrors → _evidence_lost → build_end, 그 뒤 retry_orphans)
- **#48** [low · spec_vs_code] 정형 경로의 2-pass는 문서 단위가 아니라 레코드 단위다 — Pass 1(문서 전체 해소) 뒤 Pass 2(부착)라는 §4.2 문면과 다르다 — spec_ref: 문서 4 §4.2 — code_ref: core/pipeline.py:379-389 (build_table — 레코드마다 anchor→roles→attributes→contents→edges→fallback→적재를 완결)
- **#49** [low · spec_vs_code] 비정형 attach_to 해소가 문서 버퍼를 카테고리 무관하게 먼저 조회해, 추출이 낸 category와 다른 카테고리의 동명 노드가 부착 대상이 될 수 있다 — spec_ref: 문서 4 §4.2 · §4.10-8 (B11) — code_ref: core/pipeline.py:689 (target = b.buffer.get(_n(name))), core/pipeline.py:690-693 (사전+카테고리 판정은 버퍼 미스일 때만)

### doc4-queue-ops — 12건

- **#50** [med · code_missing] obsolete의 replaced_by 전이 해소와 「대체 없는 폐기 표기 → uncertain_match 표시」가 쓰기 측에 없다 — spec_ref: 4_쓰기절차.md §4.7-4 (툼스톤은 연산 대상이 아니다 단락 말미) — code_ref: core/matcher.py:80-133 (candidates) · core/build.py:283-344 (resolve_entity) · core/pipeline.py:733-760 (_dict_hit) **(중복: high doc4-write·doc4-queue-ops(matcher is_live: replaced_by 전이·uncertain_match) 일부)**
- **#51** [med · code_missing] 빌드 말미 sweep에 「auto_node 검토 목록 정리」가 없다 — 병합·분리로 툼스톤이 된 노드의 auto_node/uncertain_match 항목이 영구 잔류 — spec_ref: 4_쓰기절차.md §4.8-5 · §4.7-3 종결 경로 — code_ref: core/pipeline.py:547-566 (finalize) · core/ops.py:478-483, 559-562 (툼스톤 생성 시 큐 미정리) **(중복: high doc4-queue-ops(confirmed 종결 경로) 일부)**
- **#52** [med · spec_vs_code] 재인입 시 추출 체크포인트 무효화가 doc_hash 변경 시에만 일어나 §4.10-5의 「소급 개선 = doc_id 재인입」 경로가 막힌다 — spec_ref: 4_쓰기절차.md §4.8-8 · §4.10-5 — code_ref: core/ingest.py:247-252 · core/pipeline.py:773-813 (run_document 체크포인트 재사용) · cli/extract.py:55-57 **(중복: doc4-extract #62)**
- **#53** [med · code_missing] §4.9 골격 개정 경로 ①LLM 감지→②structural_proposal 큐 적재의 생산자가 없고, 문서 7의 LLM 지점 9종 목록에도 그 지점이 없다 — spec_ref: 4_쓰기절차.md §4.9 ①·② (↔ 7_구현규격과검증.md §7.6-B-2 LLM 지점 9종) — code_ref: core/ (structural_proposal enqueue 0건) · cli/platform.py:43 (QUEUE_KINDS 목록에만 존재) · core/bootstrap.py (골격 개정 미리보기 없음) **(중복: doc1-invariants #208)**
- **#54** [med · spec_vs_code] I1 개명이 seed(골격) 노드를 거부도 하지 않고 파일도 갱신하지 않아, 다음 부트스트랩이 옛 이름을 새 seed 노드로 다시 심는다 — spec_ref: 4_쓰기절차.md §4.7-4 I1 개명 · §4.9-1 — code_ref: core/ops.py:267-312 (rename — seed 검사 없음; transfer는 164-167에서 seed 거부) · core/skeleton.py:332-336 (_plant: canonical+category로 기존 매칭, 없으면 새 seed add_node)
- **#55** [low · spec_vs_code] canonical로 노드를 찾는 코드 중 is_live를 거치지 않는 지점 2곳(skeleton._plant · build._mirror_scope) — spec_ref: 4_쓰기절차.md §4.7-4 — code_ref: core/skeleton.py:332-335 · core/build.py:404
- **#56** [low · code_missing] 큐 우선순위 정렬(참조 많은 auto 노드 우선)이 없다 — 2A 목록 spec-s7-11-51과 동일, 아직 미해소 — spec_ref: 4_쓰기절차.md §4.7-3 「큐 우선순위 정렬」 · 7_구현규격과검증.md §7.8 표 review_queue.json 행 — code_ref: cli/platform.py:121-146 (queue_view·cmd_queue — 적재순 출력) · core/store.py:213-236 **(중복: doc7-outputs #197)**
- **#57** [low · spec_gap] I2 병합에서 사람 override로 auto/confirmed 생존자가 seed를 흡수하면 코드가 생존자 status를 seed로 승격한다 — 명세는 침묵 — spec_ref: 4_쓰기절차.md §4.7-4 I2 병합 — code_ref: core/ops.py:449 (_survivor override 최상위) · core/ops.py:472-473 (keep status = max(keep, gone))
- **#58** [low · spec_gap] I3 분리 배분표에 attribute 배분 항목이 없어 코드가 원본 attrs 전체를 모든 타깃에 복제한다 — 명세 침묵 — spec_ref: 4_쓰기절차.md §4.7-4 I3 분리 — code_ref: core/ops.py:536-544 (add_node attrs=dict(node.get("attrs")), status=node["status"])
- **#59** [low · spec_vs_code] transfer(이관) 코어 함수가 행위자 부재를 거부하지 않는다 — CLI만 --actor를 강제 — spec_ref: 4_쓰기절차.md §4.7-4 전 연산 공통 — code_ref: core/ops.py:131-264 (transfer — `if not actor: raise` 없음; rename 274, merge 435, split 507, obsolete 580, delete_edge 608에는 있음)
- **#60** [low · spec_vs_code] I2 병합에서 같은 표면형 alias가 양쪽에 있으면 흡수측 alias의 provenance가 버려진다 — spec_ref: 4_쓰기절차.md §4.7-4 I2 병합 — code_ref: core/ops.py:459-464
- **#61** [low · doc_stale] 구조도 03_구축.md가 「같은 doc_id·같은 hash → duplicate_doc_hold」라 적었으나 코드·명세는 「같은 hash가 다른 doc_id일 때」만 보류한다 — spec_ref: 2_계약.md §2.7-① (참조: 4_쓰기절차.md §4.8-1) — code_ref: core/ingest.py:61-108 (check_doc_hash: `other != doc_id`일 때만 hold) · docs/구조도/03_구축.md:17, 84

### doc4-extract — 8건

- **#62** [med · spec_vs_code] §4.10-5 「소급 개선은 그 문서만 doc_id 재인입」이 성립하지 않는다 — 재인입 시 체크포인트 무효화가 doc_hash 변경 조건부라 내용이 같은 재인입은 옛 prompt_version의 체크포인트를 그대로 재사용 — spec_ref: docs/spec/4_쓰기절차.md §4.10-5 · §4.8-8 — code_ref: core/ingest.py:248-252 · core/extract.py:261-262 · cli/extract.py:53-58 **(중복: doc4-queue-ops #52)**
- **#63** [med · doc_stale] prompts/extract.md 조립 표가 부착 후보 목록에 「그 하위 설비」를 넣는다고 적어 명세(모순-12 정정 후)·코드·자기 본문(②)과 어긋난다 — spec_ref: docs/spec/4_쓰기절차.md §4.10 「프롬프트 자산의 위치」 · 개정대장 [정정] 모순-12 — code_ref: prompts/extract.md:68 · core/extract.py:137-173
- **#64** [med · spec_vs_code] mock 힌트 파일(extract_hints)이 USE_MOCK 분기 밖에서 무조건 우선된다 — 실호출 모드에서도 doc_id가 맞으면 LLM을 건너뛰고 픽스처 후보가 체크포인트가 된다 — spec_ref: docs/spec/7_구현규격과검증.md §7.1 USE_MOCK 대체 표 「추출」행 · §7.6-B-3 — code_ref: core/extract.py:253-255 (_load_hints) · core/extract.py:264-275 (extract) **(중복: doc7-llm #184)**
- **#65** [low · doc_stale] prompts/extract.md의 `{{categories}}`·`{{relations}}`·`{{attach_candidates}}`·`{{chunk}}` 자리는 「호출부가 채운다」고 적혀 있으나 코드는 치환하지 않고 템플릿 원문 + 별도 JSON user 메시지로 보낸다 — spec_ref: docs/spec/4_쓰기절차.md §4.10 「프롬프트 자산의 위치」 — code_ref: prompts/extract.md:14·43·52·62-69 · core/extract.py:188-197 **(중복: doc7-llm #177)**
- **#66** [low · spec_vs_code] 체크포인트 `extracted_at`에 봉투의 `parsed_at`을 복사한다 — --force 재추출·프롬프트 개정 재생성에서도 시각이 파싱 시각으로 고정 — spec_ref: docs/spec/4_쓰기절차.md §4.10 계약 JSON 예시 — code_ref: core/extract.py:286
- **#67** [low · spec_vs_code] config_version 부재 시 skeleton_version을 조용히 대신 기록한다 — prompt_version(명시적 실패)과 처리가 갈리고 「골격 판번호 ≠ 층 어휘 판번호」가 섞인다 — spec_ref: docs/spec/4_쓰기절차.md §4.10-4 · 문서 7 §7.6-B-4/5 — code_ref: core/extract.py:284
- **#68** [low · spec_vs_spec] 체크포인트 무효화 트리거가 세 문면에서 갈린다 — §4.8-8(재인입이면 무조건) · §6.3(source_hash 변경 시) · 코드(doc_hash 변경 시) — spec_ref: docs/spec/4_쓰기절차.md §4.8-8 vs docs/spec/6_파서와구축모드.md §6.3 — code_ref: core/ingest.py:251-252 · parser/struct_map.py:59-72
- **#69** [low · spec_vs_code] §6.3 보존분 재사용 판정에서 source_hash가 없는 보존 파일은 어떤 바이트에도 재사용된다(휴면) — spec_ref: docs/spec/6_파서와구축모드.md §6.3 — code_ref: parser/struct_map.py:70 **(중복: high doc6-parser-core(propose 프레임 파일, struct_map.py:70) 일부)**

### doc5-query — 9건

- **#70** [med · spec_vs_code] 링킹이 두 층에 걸리면 브리지가 2홉으로 번지고 cross 사실이 중복 문장화된다 — spec_ref: 문서 5 §5.1-4 (B13) — code_ref: cli/query.py:121-127 · core/query.py:245-249
- **#71** [med · spec_vs_code] 브리지 도달 노드가 노드의 층이 아니라 엣지 보관 층 버킷에 담겨 도착 노드의 값·엣지 문장화가 한 방향에서만 빠진다 — spec_ref: 문서 5 §5.2-1 · §5.1-4 — code_ref: core/query.py:248 (`found.setdefault(layer, set()).add(other)`) · cli/query.py:125-131 · core/query.py:355-361
- **#72** [med · spec_vs_code] fact_templates 없는 키의 기본 문안을 core·cli가 들고 있다 — spec_ref: 문서 5 §5.2-1 — code_ref: core/query.py:194,197 · core/query.py:433 · cli/query.py:160,162,165
- **#73** [med · spec_vs_code] flow 특례가 골격 트리 없이 precedes 체인만 공급한다 — spec_ref: 문서 5 §5.2-4 — code_ref: core/query.py:426-438 (flow_chain) · cli/query.py:104-107
- **#74** [med · spec_vs_code] 순서 파생·flow 체인이 deleted_by_user 툼스톤 엣지를 거른다는 코드가 없다 — spec_ref: 문서 4 §4.8-3 · 문서 1 H4 · 문서 5 §5.1-8 — code_ref: core/query.py:401,405 (next_of) · 419 (_descend) · 436-438 (flow_chain)
- **#75** [low · spec_vs_spec] §5.1-4 「홈층이 아닌 전 층」 문면이 개정대장 판정·코드(홈층 포함)와 반대로 읽힌다 — spec_ref: 문서 5 §5.1-4 vs 개정대장 §L 실측표 「§5.1-4 홈층 특례 없음」 — code_ref: core/query.py:226-235 (bridge — 홈층 포함 전 층 순회)
- **#76** [low · spec_gap] cross_layer_traverse의 direction 'out/in' 의미가 query_traverse와 반대로 구현돼 있다 — spec_ref: 문서 5 §5.1-4 — code_ref: core/query.py:240-245 vs core/graph.py:194-198
- **#77** [low · doc_stale] --json 묶음에 명세 열거 밖 키(suggest·facts_before_filter)가 실린다 — spec_ref: 문서 5 §5.2-6 ([정정] 41) — code_ref: cli/query.py:92 (`res["suggest"]`) · cli/query.py:223 (`res["facts_before_filter"]`)
- **#78** [low · code_missing] 「넓은 질의는 층별 요약으로 받는다」 경계가 코드에 없다 — spec_ref: 문서 5 §5.1 경계 — code_ref: cli/query.py:35-144 (answer) · layers/*/config.json query_intents

### doc5-gauges — 7건

- **#79** [med · spec_vs_code] 플랫폼 계기판 호출이 층별 graph.json을 재기록한다 — 관측 경로의 쓰기 — spec_ref: 문서 5 §5.5 「플랫폼 노출(관측)」 · 규율 5 — code_ref: cli/platform.py:306-311 → core/graph.py:212 (build_end → save) **(중복: high doc5-gauges(계기판 8 build_begin/build_end→save) 일부)**
- **#80** [med · spec_vs_code] `platform accuracy`가 판정 정확도를 큐 유입 추이 로그로 상시 측정한다 — §5.4의 '표본 채점 1회·9번째 상시 지표 금지'와 다르고 kind도 다르다 — spec_ref: 문서 5 §5.4 두 칸의 근거 문단 — code_ref: cli/platform.py:362-426 (cmd_accuracy), 특히 407·414-425
- **#81** [med · spec_vs_code] 측정 뮤트가 chunk_truncated까지 끈다 — 문서 5는 link_miss만 끄라 하고 문서 7은 그 이유로 두 로그를 분리했다 — spec_ref: 문서 5 §5.5 산식 규율 4 · 문서 7 §7.1 파일표 defects/link_miss/chunk_truncated 행 — code_ref: core/store.py:157 (mute = {LINK_MISS, CHUNK_TRUNCATED})
- **#82** [low · spec_gap] 계기판 4·5의 '재료'로 명명된 link_miss.log·chunk_truncated.log를 어떤 계기판도 읽지 않는다 — spec_ref: 문서 5 §5.1-6 · §5.2-3 · 문서 7 §7.1 파일표 — code_ref: cli/platform.py:251-254·323-326 (세트 결과로만 계산), core/query.py:203·332 (적재만)
- **#83** [low · doc_stale] 구조도 「계기판·골든셋」이 '받는 것: link_miss.log·chunk_truncated.log'라 적었으나 계기판은 두 로그를 읽지 않는다 — spec_ref: docs/구조도/09_부품_해설.md 「👤 계기판·골든셋」 (부품카드.md:304 동일) — code_ref: cli/platform.py:211-333, cli/golden.py:133-147
- **#84** [low · spec_vs_code] 계기판 1 recall의 분모가 '12문항'이 아니라 Q8을 뺀 10문항이고, 골든셋 모드에서도 채점기의 expected_linked 기반 recall과 다른 값을 낸다 — spec_ref: 문서 5 §5.5 산식 규율 1 · 개정대장 §AK B54 — code_ref: cli/platform.py:251-252·314, cli/golden.py:160-165·181-184
- **#85** [low · spec_vs_code] platform ops의 5요소 표시에서 '대상'이 목록이 아니라 건수로만 나온다 — spec_ref: 문서 5 §5.5 플랫폼 노출 · 문서 7 §7.1 요구 규격 ③ · 문서 4 §4.7 전 연산 공통 — code_ref: cli/platform.py:197-199

### doc6-parser-core — 12건

- **#86** [med · spec_gap] 휴리스틱(mock) 지도가 보존·재사용된다 — mock으로 한 번 돌린 문서는 실호출로 바꿔도 모델이 불리지 않는다 — spec_ref: 문서 6 §6.3 보존 자리와 재사용 · 문서 7 §7.6-B-1(B48) · docs/구조도/02_파싱과_추출.md:123 · docs/가이드/2B_작업가이드.md:273 — code_ref: parser/pipeline.py:73-74 (made[key]=out — unavailable만 거른다) · parser/struct_map.py:137-145 (propose는 휴리스틱을 보존하지 않는다고 적었으나 훅이 보존한다) · parser/pipeline.py:78-79 주석 「휴리스틱 지도는 보존하지 않으므로」
- **#87** [med · spec_vs_code] PDF의 쪽 렌더가 ④에 닿지 않는다 — tagger가 meta.slide로 찾는데 basic_pdf는 meta.page를 낸다 — spec_ref: 문서 6 §6.4-5 기본 PDF 어댑터(B53) · §6.1 — code_ref: parser/tagger.py:211 (page = pages.get(meta.get("slide"))) · parser/adapters/basic_pdf.py:77,83 (meta에 "page": idx만, "slide" 없음) · parser/pipeline.py:110-112 (_page_map은 PDF에 쪽→PNG를 정상 반환) **(중복: high doc6-adapters(PDF 쪽 렌더))**
- **#88** [med · spec_vs_code] 보존분 재사용 경로에서는 고른 레벨·레벨별 분포가 검수 뷰 데이터에서 빠진다 — spec_ref: 문서 6 §6.3 (B45) · §6.6-3 — code_ref: parser/pipeline.py:63-66 (hit이면 seen.append 전에 return) · parser/pipeline.py:249-253 (map_picks 비면 adapter_level_picks → PPT는 heading_pattern 없어 []) · cli/register.py:1113-1116 · kit/render_review.py:273
- **#89** [med · spec_vs_code] 검수 리허설의 파싱 doc_id가 운영 doc_id와 달라 지도·이미지 요약 보존분이 운영 인입에서 재사용되지 않는다 — spec_ref: 문서 6 §6.6 ②구획(B51-2) · §6.3 — code_ref: cli/register.py:1367-1368 (pipeline.parse(mod, f"{doc_type.upper()}{i:02d}", …)) · cli/register.py:1258 (추출 봉투만 env['doc_id']=doc_id_of(s)로 바꿈) · cli/parse.py:176 (cmd_build도 같은 꼴) · parser/pipeline.py:162-163,217 (보존 키 = 파싱 doc_id) **(중복: high doc6-review-view(리허설 doc_id))**
- **#90** [med · spec_vs_code] reader.head()가 `_images` 바이트를 그대로 싣는다 — 그림이 있는 PPTX/PDF는 `parse head`와 등록 입력 패키지 작성이 TypeError로 죽는다 — spec_ref: 문서 6 §6.5 입력 패키지(시스템 — reader 원시 추출) · §6.2-3 · §6.2-4 — code_ref: parser/reader.py:564-565 (head: {**raw, 'slides': …} — _images 제거 없음, pages 미절단) · cli/parse.py:129 (json.dumps(reader.head(...))) · cli/parse.py:165 · cli/register.py:576-582 (reader_head → input_package.json)
- **#91** [med · spec_vs_code] 킷 어댑터 스켈레톤의 extract 서명에 struct_map_fn이 없다 — 생성된 구조 가변 prose 어댑터는 지도 패스 진입 ①(계열)에 도달할 수 없다 — spec_ref: 문서 6 §6.4 인터페이스 · §6.3 지도 패스의 진입 · §6.4-8 — code_ref: kit/어댑터_스켈레톤.py:81 (def extract(raw)) · kit/어댑터_스켈레톤.py:127-130 (prose 뼈대가 pass) · parser/pipeline.py:176-180 (TypeError면 extract(raw)로 재호출 — 훅 없이 진행) **(중복: doc6-kit #144)**
- **#92** [low · code_missing] 지도 패스 입력에 어댑터 expects 힌트(프라이어)가 전달되지 않는다 — spec_ref: 문서 6 §6.3 흐름도 · §6.4-8 — code_ref: parser/pipeline.py:68 (apply(doc_key, lines, locator, ask=ask)) · parser/struct_map.py:128 (ask(doc_id, lines)) · core/llm.py:777 (map_structure(doc_id, lines)) **(중복: doc7-llm #190)**
- **#93** [low · spec_vs_spec] §6.3·부록·§7.5는 USE_MOCK에서 mock/struct_maps 고정 파일을 쓴다고 하고, §7.1 ⑦행(B48)은 운영 코드가 fixture를 찾지 않는다고 한다 — spec_ref: 문서 6 §6.3 마지막 문장 · 부록_용어.md:44 「구조 지도」 · 문서 7 §7.5 mock 자산 경로 규약 ↔ 문서 7 §7.1 대체 표 ⑦행 — code_ref: parser/struct_map.py:105-145 (fixture 조회 없음, 휴리스틱만) · core/fixtures.py:16 · mock/ 디렉터리에 struct_maps 없음 **(중복: doc6-kit #157 · doc7-layout #161 · doc7-outputs #198)**
- **#94** [low · spec_vs_code] 지도에 제목이 없고, 레벨별 분포 산식이 실제 분할 크기와 다르다(하위 헤딩 행·서두 본문 제외) — spec_ref: 문서 6 §6.3 (지도 출력 형식 · B43 레벨 선택 근거) — code_ref: core/llm.py:828-837 (headings의 title을 버리고 rows에 heading·level만) · parser/struct_map.py:209-215 (level_stats: 모든 헤딩 행을 body에서 제외, 첫 헤딩 앞 본문 미계수) · parser/struct_map.py:274-283 (split: 하위 헤딩 행은 본문에 포함, 서두도 청크로 냄)
- **#95** [med · spec_vs_code] _resplit이 텍스트→행 역매핑을 써서 같은 문장이 두 번 나오면 잘못 자르고 locator가 거꾸로 된다 — spec_ref: 문서 6 §6.3 (한 단계 재분할) · §6.4-5 source_locator — code_ref: parser/struct_map.py:295-296 (text_of=dict(lines); row_of={v:k for k,v in text_of.items()}) · :303-319
- **#96** [low · spec_gap] 타당성 검사에 실패한 지도(헤딩 0건·비단조·커버리지)는 unavailable과 달리 보존·재사용되어 재인입이 영영 평면이다 — spec_ref: 문서 6 §6.3 · 개정대장 [정정] 39 근거(pipeline.py:70 인용) — code_ref: parser/struct_map.py:136 (keep은 validate 전에 실행) · parser/pipeline.py:73 (unavailable만 제외) · parser/struct_map.py:344-349
- **#97** [low · spec_vs_code] 보존 파일의 maps 값이 지도가 아니라 [chunks, smap, reasons] 3짝이다 — 청크 본문까지 struct_maps에 실린다 — spec_ref: 문서 6 §6.3 보존 파일의 내부 형태 · docs/구조도/02_파싱과_추출.md:123 — code_ref: parser/pipeline.py:67-74 (made[key]=out, out=(chunks, smap, reasons)) · parser/pipeline.py:64-66 (재사용은 3짝을 그대로 돌려줌 — split()을 다시 돌리지 않음)

### doc6-adapters — 9건

- **#98** [med · spec_vs_code] 목차 슬라이드가 section_path에 기여하지 않는다 — _is_toc는 정의만 있고 호출부 0 — spec_ref: 문서 6 §6.4-5 기본 PPT 어댑터의 판독 범위(B53) — code_ref: parser/adapters/basic_ppt.py:65-67 (_is_toc 정의) · 70-92 (_sections) · 95-101 (_path)
- **#99** [med · spec_vs_code] CSV 정형 문서는 preflight에서 항상 adapter_mismatch — header_labels가 format=='xlsx'만 읽는다 — spec_ref: 문서 6 §6.4-6 지문 추출 규약 — code_ref: parser/preflight.py:23 · parser/reader.py:521-526 · parser/pipeline.py:167-171
- **#100** [med · spec_gap] 분할 자명 prose의 preflight는 문서를 보지 않는다 — 상수 존재만 확인해 포맷 오배정이 adapter_mismatch가 아니라 parse_failure로 떨어진다 — spec_ref: 문서 6 §6.4-6 · §6.4-9 — code_ref: parser/preflight.py:98-101 (_prose 둘째 갈래) · parser/adapters/basic_ppt.py:126 · parser/adapters/basic_pdf.py:64
- **#101** [med · spec_vs_code] basic_pdf가 텍스트 0·래스터 그림 0인 쪽을 조각 없이 버린다 — 벡터 도면 쪽이 표시 없이 사라진다 — spec_ref: 문서 6 §6.4-5 기본 PDF 어댑터(B53) — code_ref: parser/adapters/basic_pdf.py:68-97 · parser/reader.py:406-422
- **#102** [low · doc_stale] kit/참조어댑터/README.md가 「바이트 그대로 복사본 · cmp 차이 0」이라 적는데 실물은 규약 10 전환본이고 지목한 원본 경로가 존재하지 않는다 — spec_ref: 문서 6 §6.7 킷 #4(B27) — code_ref: kit/참조어댑터/README.md:24-32 · kit/참조어댑터/ipqc.py:1-3 · cp.py:2-4 · toc_report.py:1-3 **(중복: doc6-kit #152)**
- **#103** [low · spec_vs_spec] 개정대장 §AJ B53의 표 locator 예시 `S8-T1`이 문서 6 §6.4-5의 `S8-TB1`과 다르다 — 코드에서 T는 제목, TB가 표다 — spec_ref: 개정대장 §AJ [개정] B53 「근거는 shape까지」 · 문서 6 §6.4-5 — code_ref: parser/reader.py:123-124 (_KIND_TAG: title→T, table→TB)
- **#104** [low · spec_vs_code] 기본 어댑터의 판단·관찰 상수 일부가 expects 밖 모듈 상수·리터럴로 산다 (CONTEXT_CHARS=1500 · 구획/목차 판정 키워드) — spec_ref: 문서 6 §6.4-4 · §6.4-5 — code_ref: parser/adapters/basic_ppt.py:47-49 (_SECTION_LAYOUT·_TOC_TITLE·CONTEXT_CHARS) · basic_ppt.py:112 · parser/adapters/basic_pdf.py:70 (text[:1500])
- **#105** [low · spec_gap] 노트(⑦)는 meta.notes에만 실리고 소비부가 0 — 노트가 청크 text·④ 맥락·추출 입력 어디로 가는지 명세가 침묵한다 — spec_ref: 문서 6 §6.4-5 기본 PPT 어댑터의 판독 범위(B53) ⑦ — code_ref: parser/reader.py:370-372 · parser/adapters/basic_ppt.py:104-112,219-220,249-250 · core/extract.py:205-213
- **#106** [low · spec_vs_code] 지문 스캔(cli/scan._header_actual)이 preflight.header_labels를 재사용하지 않고 같은 연산을 복제했다 — spec_ref: 문서 6 §6.4-6 · §6.4 투입 분류 — code_ref: cli/scan.py:70-80 · parser/preflight.py:21-31 (cli/register.py:138-140은 preflight 것을 import) **(중복: doc6-ingest-routing #112)**

### doc6-ingest-routing — 10건

- **#107** [med · spec_vs_code] 손상 문서·깨진 어댑터가 select() 단계에서 예외를 던져 ingest-dir 전체가 중단된다 — 문서 단위 독립 위반 — spec_ref: 문서 6 §6.4 일괄 투입(B46) — code_ref: cli/ingest.py:123 (select 호출) vs cli/ingest.py:139-164 (try/except 범위)
- **#108** [med · spec_vs_code] 같은 doc_type 어댑터가 등록부와 기본 소재지 양쪽에 있으면 후보가 중복돼 자동 라우팅이 막히고, --doc-type 지정 시 비등록 실물이 등록분을 이긴다 — spec_ref: 문서 6 §6.5 등록부 「같은 이름이면 등록분이 이긴다」 · §6.4 투입 분류 「등록 전체 어댑터의 expects와 일괄 대조」·조건 ③ — code_ref: cli/scan.py:47-67 (adapters — 경로로만 dedup) · cli/scan.py:126 (candidates) · cli/ingest.py:69 (dict comprehension last-wins)
- **#109** [med · spec_vs_code] `parse build`가 register와 같은 review/{doc_type}/ 파일(approval.json·view.json·input_package.json)을 등록부 확인 없이 덮어쓰는 제2의 구축 모드 진입점으로 남아 있다 — spec_ref: 문서 7 §7.1 진입점 계약 「같은 기능을 다른 이름으로 제공하지 않는다」 · 문서 6 §6.5 「review/{doc_type}/에 남는 것은 입력 패키지·뷰 데이터·정적 HTML·승인 기록」 · §6.6-4 승인 기록 · §6.6 ②(B51-2 리허설 doc_id) — code_ref: cli/parse.py:133-206 (cmd_build) · run.py:35 · cli/register.py:86-87,1513 **(중복: doc7-llm #183)**
- **#110** [med · code_missing] doc_type 변형 어댑터 N(B37/B38) — 변형 등록(register revise)·등록부 다중 어댑터·투입 시 지문에 의한 변형 선택이 코드에 없다 — spec_ref: 문서 6 §6.5 「doc_type의 구조 — 스키마 1 + 어댑터 N」 규격 2·3 (B37·B38) — §6.4 투입 분류와 직결 — code_ref: core/registry.py:110-115 (항목당 adapter 1개) · cli/ingest.py:69-76 (doc_type→어댑터 1:1 dict) · cli/register.py (revise/변형 0건) **(중복: doc6-register #117 · doc6-kit #150 · guide-vs-code #214)**
- **#111** [low · spec_vs_code] 정형 CSV/TSV는 지문 스캔 대상에서 빠지고, 미선택 사유가 「대조할 정형 어댑터가 없다」로 오도한다 (개정대장 §AE 미결 기록) — spec_ref: 문서 6 §6.4 투입 분류 · 개정대장 §AE 「미결 — CSV 지문 스캔」 — code_ref: cli/scan.py:72,106,115 · cli/ingest.py:103-107
- **#112** [low · spec_vs_code] 지문 추출 연산이 preflight와 scan에 두 벌 구현돼 있다 (명세는 「같은 연산의 재사용」) — spec_ref: 문서 6 §6.4 투입 분류 · §6.4-6 지문 추출 규약 — code_ref: cli/scan.py:70-80 (_header_actual) vs parser/preflight.py:19-29 (header_labels) **(중복: doc6-adapters #106)**
- **#113** [low · spec_gap] 「완전 일치」의 순서·중복 의미가 명세에 없고 코드는 집합 비교(순서·중복 무시)를 택했다 — spec_ref: 문서 6 §6.4 투입 분류 · §6.4-6 — code_ref: cli/scan.py:96-101 · parser/preflight.py:118-121
- **#114** [low · doc_stale] scan.py의 사용자 출력·docstring이 B46 이전 문면(「유일 일치여도 자동 라우팅하지 않는다 — P7」)과 §7.1이 금지한 `python cli/scan.py` 표기를 낸다 — spec_ref: 문서 6 §6.4 투입 분류(B46·B47) · 문서 7 §7.1 「진입점 표기를 … 파일 경로 호출로 적지 않는다」 — code_ref: cli/scan.py:7,16,110,166 · cli/parse.py:7-11 **(중복: guide-vs-code #224 일부)**
- **#115** [low · spec_vs_code] scan --confirm 경로가 preflight·파이프라인을 거치지 않고 extract만 직접 돌린 뒤 「정상 파싱」이라 찍으며 산출을 남기지 않는다 — spec_ref: 문서 6 §6.4 투입 분류(사람 1클릭 확정 → 파싱) · §6.4-6 preflight — code_ref: cli/scan.py:130-145,185-189
- **#116** [low · spec_gap] ingest-dir의 하위 폴더 비순회는 가결정(D-110 ④)만 있고 §6.4 문면은 침묵한다 — spec_ref: 문서 6 §6.4 일괄 투입(B46·B47) — code_ref: cli/ingest.py:167-175

### doc6-register — 12건

- **#117** [med · code_missing] 구축 모드 재진입(어댑터 개정·변형 어댑터 등록·차분 생성)이 코드에 없다 — 등록된 doc_type은 generate가 거부하고 confirm은 중복으로 raise — spec_ref: 문서 6 §6.5 「doc_type의 구조 — 스키마 1 + 어댑터 N」 규격 1~4(B37·B38) · §6.3 「상수를 고치는 경로는 어댑터 개정(구축 모드 재진입)」 · §6.4-6 — code_ref: cli/register.py:528-529 (registry.lookup 중복 거부) · core/registry.py:107-111 (register 중복 raise) · cli/register.py:1536-1609 (main — revise/variant 명령 없음) **(중복: high guide-vs-code(가이드 §7 재등록 경로) 일부 · doc6-ingest-routing #110 · doc6-kit #150 · guide-vs-code #214)**
- **#118** [med · spec_vs_code] 기계 관문 실패 뒤 문답의 라운드 전문이 입력 패키지(관찰 힌트 그릇)에 실리지 않는다 — 재현 조건 유실 — spec_ref: 문서 6 §6.5 「생성 전 문답 …」(B21·B36) · 「하네스는 생성 안에서…」 표(B50) — code_ref: cli/register.py:839-842 (machine_gate: `_interview(pkg or {}, context=ask)` — on_round 없음) vs :629-636 (cmd_generate의 _persist) **(중복: high doc6-register(재생성 지시 미전달) 일부)**
- **#119** [med · spec_vs_code] --interview와 --no-fewshot을 함께 주면 no_fewshot 플래그가 덮어써져 전시물이 주입되고 끈 기록도 사라진다 — spec_ref: 문서 6 §6.5 「전송 프롬프트의 크기…」(B30) ② — code_ref: cli/register.py:567-568 (hint={'text','no_fewshot':True}) → :629-630 (_persist: `pkg['human']['hint'] = {'text': hint, 'interview': rounds}`) · cli/prompt.py:226-229 (`_hint.get('no_fewshot')`)
- **#120** [med · spec_vs_code] 기계 관문 FAIL 상태에서도 `register review`가 리허설·뷰 데이터·HTML을 만든다 — 미통과 산출이 검수 화면에 오른다 — spec_ref: 문서 6 §6.5 「하네스는 생성 안에서 돌고…」·「해소되지 않으면 산출하지 않는다」(B50) — code_ref: cli/register.py:1343-1352 (ok=machine_gate=='PASS' 표시만) → :1361-1416 (리허설 파싱·build_view·render 계속) · 대비 :1336-1341 (--instruct 갈래만 차단) **(중복: doc6-review-view #129 · doc6-kit #143)**
- **#121** [med · code_missing] 열 프로파일의 「좌표 열 기준 변동성」이 생산 경로에서 한 번도 계산되지 않는다(coord_col 전달 호출부 0) — spec_ref: 문서 6 §6.5 「열 프로파일 — 관찰은 표본이 아니라 전 행에서 센다」(B39) — code_ref: cli/register.py:577 (`profile.profile(sh)` — coord_col 미전달) · parser/profile.py:74-96·99-110 (_coord_variation, coord_col=None이면 None)
- **#122** [med · spec_vs_spec] 개정대장 B40·B41이 「문서 6 §6.5 개정」이라 적었으나 문서 6 §6.5에 그 문면(대표값 행 번호 병기·헤더 확정 시 재계산·예산 대조·감축 순서)이 없다 — spec_ref: 개정대장 §Z B40·B41(1341-1349행) vs 문서 6 §6.5 「전송 프롬프트의 크기」(128행)·「열 프로파일」(130행) — code_ref: cli/prompt.py:291-306 (예산 대조·감축 순서 구현) · parser/profile.py:124-131 (행 번호 병기 구현)
- **#123** [low · code_missing] B40 ②「헤더가 힌트·문답으로 확정되면 프로파일 재계산」의 경로가 없다 — header_row를 주는 생산 호출부 0 — spec_ref: 개정대장 §Z B40 ②(문서 6 §6.5 귀속 — 위 검출처럼 문서 6 본문에는 부재) — code_ref: cli/register.py:575-579 (`profile.profile(sh)`, 헤더행_제외 False 고정) · parser/profile.py:99·138-140
- **#124** [med · doc_stale] 2B 작업가이드가 adapter_mismatch·「뺀 열을 또 묻는다」의 처방으로 `review --instruct`·`generate --resume`을 안내하지만 등록된 doc_type에서는 confirm이 중복 raise로 막히는 막다른 길이다 — spec_ref: 문서 6 §6.5 「doc_type의 구조 — 스키마 1 + 어댑터 N」(B38) · §6.4-6 preflight — code_ref: docs/가이드/2B_작업가이드.md:271-272 · cli/register.py:1315-1341 (review --instruct) · :493-526 (--resume, registry 검사 없음) · core/registry.py:107-111 **(중복: high guide-vs-code(가이드 §7 재등록 경로))**
- **#125** [low · spec_vs_code] prose 추출 리허설의 동의 프롬프트가 기본 「켬」([Y/n])이다 — 좌표 보조의 기본 끔([y/N])과 다르고 명세의 「기본은 끔」과 어긋난다 — spec_ref: 문서 6 §6.5 「검수 리허설의 범위와 비용 관문」(B22) · §6.6 ②(B51 「비용 관문은 좌표 보조와 동형」) — code_ref: cli/register.py:1394-1396 (`input('켤까? [Y/n]') not in ('n','no')`) vs :1210-1213 (좌표 보조 `[y/N]`) **(중복: doc6-review-view #135 · guide-vs-code #225)**
- **#126** [low · spec_vs_code] prose 등록의 리허설 파싱 기본이 전량(--rows all)이다 — 명세는 「기본이 부분, 전량은 명시 선택」 — spec_ref: 문서 6 §6.5 「검수 리허설의 범위와 비용 관문」(B22) — code_ref: cli/register.py:1577-1583 (`raw_rows = opt('--rows', 'all' if _prose else str(REHEARSAL_ROWS))`) **(중복: doc6-review-view #141 일부)**
- **#127** [low · spec_vs_code] 문답이 「빈 입력 2연속」을 종료로 읽는다 — 종료는 사람의 명시 입력만이 정한다는 문면과 다르다 — spec_ref: 문서 6 §6.5 「생성 전 문답 — 선택 단계, 종료는 사람」(B21) — code_ref: cli/interview.py:227-230
- **#128** [low · doc_stale] 생성 템플릿 v1.0의 관찰 범위 규칙이 「정형 약 10줄」이라 명세(B34 10~20줄)·reader(OBSERVE_ROWS=20)와 어긋난다 — spec_ref: 문서 6 §6.5 절차 문단(B34) · 입력 패키지 표 「관찰 범위 규칙은 … 프롬프트 템플릿에 고정」 — code_ref: kit/생성프롬프트_템플릿_v1.0.md:62 · parser/reader.py:553 (OBSERVE_ROWS = 20) **(중복: doc6-kit #154)**

### doc6-review-view — 13건

- **#129** [med · spec_vs_code] 기계 관문 FAIL 상태여도 `review`가 뷰 데이터·HTML을 만든다 — 미통과 산출이 검수 화면에 오른다 — spec_ref: 문서 6 §6.5 「해소되지 않으면 산출하지 않는다」 · 문서 1 M9 — code_ref: cli/register.py:1348-1351 (ok = machine_gate == PASS 를 출력만 함) · :1374-1416 (파싱→build_view→view.json/view.html 무조건 생성) **(중복: doc6-register #120 · doc6-kit #143)**
- **#130** [med · spec_vs_code] `review`를 한 번도 돌리지 않아도 `confirm`이 통과한다 — 검수 없는 산출물이 등록부에 든다 — spec_ref: 문서 6 §6.5 규격 2 · §6.6 전제 · 문서 1 M4 — code_ref: cli/register.py:1479-1486 (cmd_confirm 전제 검사) · :788-790 (_finish_generate가 machine_gate=PASS를 state에 기록)
- **#131** [med · spec_vs_code] UNMAPPABLE 복원의 실물 헤더 경로가 도달 불가 — 항상 위치 가정으로 떨어지고 매 호출 결함 로그를 남긴다 — spec_ref: 문서 6 §6.6 「UNMAPPABLE 열의 복원 산식」 · §6.4-6 지문 추출 규약 — code_ref: cli/register.py:1003-1007 (_label_columns: SAMPLE 속성 또는 expects.sample_path 필요) · :949-956 (_unmapped_labels 폴백 + store.append_defect) · 호출부 :767, :895, :1083, :1420
- **#132** [med · code_missing] 배정표의 「근거 한 줄」이 실질 부재 — 상수 문자열 「생성 세션의 배정 근거」가 전 필드에 찍힌다 — spec_ref: 문서 6 §6.6 ②배정표 — code_ref: cli/register.py:888-890 (reason = spec.get("정의문") or 상수) · :413-418 (attribute_ranking[].reason은 state에만 저장) · :903-905 (rank만 소비)
- **#133** [med · spec_vs_code] B39 ③ 「갈린 열·경계선 부근을 먼저 보인다」의 표지(attention·rank·machine_suggest)가 렌더되지 않고 뷰 데이터 계약에도 없다 — spec_ref: 문서 6 §6.5 role 배정 3단 깔때기 ③ · §6.6-3 뷰 데이터 스키마 정본 — code_ref: cli/register.py:910-926 (attention·machine_suggest·rank 부여 + 정렬) · kit/render_review.py:214-225 (_roles: 필드·role·카테고리·부착·근거 5열만) · kit/검수뷰_데이터스키마.json:167-183 (role_table items에 해당 키 없음) **(중복: doc6-kit #149)**
- **#134** [low · spec_vs_code] 재생성 이력의 주체(by)가 뷰 데이터 계약에 없고 화면에 그려지지 않는다 — 사람 지시와 자동 지시가 검수 화면에서 구분되지 않는다 — spec_ref: 문서 6 §6.5 「자동으로 보낸 지시도 이력에 남긴다」 · §6.6-2·-4 — code_ref: kit/검수뷰_데이터스키마.json:18-27 (regenerations items: n·instruction·at) · kit/render_review.py:238-246 (_regen: #·수정 지시·시점 3열) · cli/register.py:852-854,1317-1319 (by 기록) **(중복: doc6-kit #151)**
- **#135** [low · spec_vs_code] 추출 리허설 동의 관문의 기본값이 「켬」([Y/n]) — 리허설 중 실호출 보조의 「기본은 끔」과 어긋나고 가이드 자신도 「기본은 끔」이라 적는다 — spec_ref: 문서 6 §6.5 「검수 리허설의 범위와 비용 관문」 · 개정대장 §AH B51 「비용 관문은 좌표 보조와 동형」 — code_ref: cli/register.py:1394-1396 (input("켤까? [Y/n]") … not in ("n","no") → Enter=켬) vs :1210-1213 (좌표 보조 [y/N] Enter=끔) · docs/가이드/2B_작업가이드.md:148,153 **(중복: doc6-register #125 · guide-vs-code #225)**
- **#136** [low · spec_vs_code] view.json이 「잎은 한 줄」 표기(_pretty_json)를 타지 않고 표준 indent=2로 쓰인다 — spec_ref: 문서 6 §6.6-6 (B23) — code_ref: cli/register.py:1414-1415 (json.dumps(view, …, indent=2)) vs :310-345 (_pretty_json — 스키마에만 사용 :359)
- **#137** [low · doc_stale] 킷 자산과 register 머리말이 제거된 구명세(「파서_명세 §7 · 카드 M11」)를 정본으로 지목한다 — spec_ref: CLAUDE.md §1 「명세는 정제본뿐」 · 문서 6 §6.6-3 「뷰 데이터 스키마의 실물은 킷 자산이 정본」 — code_ref: kit/검수뷰_데이터스키마.json:4 ("_정본": "파서_명세 §7 · 카드 M11. 어긋나면 그쪽이 이긴다.") · kit/render_review.py:2,357-358 (footer 「규격 정본 = 파서_명세 §7 · 카드 M11」) · cli/register.py:2 (「파서_명세 §6·§7 · 틀 §2」) **(중복: doc6-kit #155)**
- **#138** [low · spec_vs_spec] [스펙끼리] 문서 6 §6.5 [정정] 40 문면은 source_locator·좌표 블록 필드를 「구조 필드」로 열거하나 문서 2 §2.4 규약 3·C17은 그것들을 role 배정 블록 필드로 둔다 — 배정표 노출 여부의 근거가 갈린다 — spec_ref: 문서 6 §6.5 「모든 열은 판정을 갖는다」([정정] 40 괄호) vs 문서 2 §2.4 규약 3 · 문서 1 C17 — code_ref: cli/register.py:882-891 (role_table이 load_blocks 전개분을 배정표에 「공용 블록」 표기로 올림) · schemas/blocks.json (common_core·process_coord에 role 선언) **(중복: doc2-roles-schema #5)**
- **#139** [low · spec_vs_spec] [스펙끼리] §6.6 복원 산식 「명시하면 그쪽이 이긴다」와 §6.5 B49 「어느 쪽에도 없는 열은 결함」이 명시 스키마에서 차집합을 계속 돌리는지에 대해 다르게 읽힌다 — spec_ref: 문서 6 §6.6 「UNMAPPABLE 열의 복원 산식」 vs §6.5 「모든 열은 판정을 갖는다」(B49) — code_ref: cli/register.py:976-1000 (명시 스키마에서도 차집합을 돌려 named 밖을 orphan=failure로) · :1090-1094
- **#140** [low · spec_vs_code] 표본이 여럿일 때 부분 리허설 표시가 첫 표본 하나만 남는다 — spec_ref: 문서 6 §6.5 「검수 리허설의 범위와 비용 관문」 — code_ref: cli/register.py:1052-1065 (for r in results: … anomalies.append(…); break) · summary.rehearsal 단수(:1107)
- **#141** [low · doc_stale] 구조도 01_등록_흐름이 리허설 파싱 기본을 「앞 200행」으로만 적는다 — prose 기본은 전량 — spec_ref: 문서 6 §6.5 「리허설 파싱은 기본이 부분이다 … (기본값은 구현 소유)」 — code_ref: cli/register.py:1577-1583 (prose면 --rows 기본 "all", table 200) vs docs/구조도/01_등록_flow.md:140 「리허설 파싱(기본 앞 200행 — 뷰에 필수 표시)」 **(중복: doc6-register #126 일부)**

### doc6-kit — 16건

- **#142** [med · spec_vs_code] 하네스 ②preflight가 코어 preflight와 다른 연산이다 — 코어가 거부할 어댑터를 관문이 통과시킨다 — spec_ref: 문서 6 §6.7 킷 #6 · §6.4-6 · §6.4 투입 분류 — code_ref: kit/run_adapter.py:166-194 (preflight) vs parser/preflight.py:118-137 (check)
- **#143** [med · spec_vs_code] 기계 관문 FAIL 상태에서도 `register review`가 검수 뷰를 산출한다 — spec_ref: 문서 6 §6.5 「해소되지 않으면 산출하지 않는다」 · 문서 1 M9 — code_ref: cli/register.py:1343-1351, 1412-1416 (cmd_review) **(중복: doc6-register #120 · doc6-review-view #129)**
- **#144** [med · spec_vs_code] 어댑터 스켈레톤·템플릿의 extract 서명에 struct_map_fn 주입 자리가 없다 — spec_ref: 문서 6 §6.4 어댑터 인터페이스 · §6.7 킷 #2 — code_ref: kit/어댑터_스켈레톤.py:81 · kit/생성프롬프트_템플릿_v1.0.md:218,261 · parser/pipeline.py:176-180 **(중복: doc6-parser-core #91)**
- **#145** [med · spec_vs_code] few-shot 전시물 선택이 payload_kind가 아니라 파일 확장자다 — xlsx 목차형 prose·pdf에 table 전시물이 실린다 — spec_ref: 문서 6 §6.7 킷 #4 — code_ref: cli/prompt.py:132-142 (_reference_adapter)
- **#146** [med · spec_vs_code] 「전시물의 산출 동등은 봉인 대조로 증명한다」에 해당하는 어서션이 없고, toc_report 전시물은 스냅샷과 산출이 다르다 — spec_ref: 문서 6 §6.7 킷 #4 — code_ref: kit/참조어댑터/toc_report.py:27-32,90-97 · tests/test_p2.py:98-141 · tests/test_p3.py:876-895
- **#147** [med · code_missing] 킷 #3 표적 출력 정의는 어느 템플릿 판에도 주입 자리가 없다 — 호출부 0 — spec_ref: 문서 6 §6.7 킷 #3 — code_ref: kit/표적출력_정의.md:3-8 · cli/prompt.py:238-245 (_render_template 치환 7종) · kit/생성프롬프트_템플릿_v1.0.md(전문)
- **#148** [med · doc_stale] 템플릿 v1.0의 reader 출력 계약(pptx)이 B53 이전 형태다 — shapes가 문자열 배열, pdf 없음 — spec_ref: 문서 6 §6.4-5 기본 PPT 어댑터 판독 범위(B53) · §6.7 킷 #1 — code_ref: kit/생성프롬프트_템플릿_v1.0.md:233-235 vs parser/reader.py:24-56
- **#149** [med · spec_vs_code] 검수 뷰가 B39 ③「갈린 열·경계선 부근」 표시를 그리지 않는다 — 산출자는 채우고 스키마·렌더러는 모른다 — spec_ref: 문서 6 §6.5 role 배정 3단 깔때기 ③ · §6.6-3 — code_ref: cli/register.py:903-926 (role_table attention/machine_suggest/rank) · kit/render_review.py:214-225 (_roles) · kit/검수뷰_데이터스키마.json:167-183 **(중복: doc6-review-view #133)**
- **#150** [med · code_missing] doc_type = 스키마 1 + 어댑터 N(변형 등록·차분 생성·지문 선택) 경로가 코드에 없다 — spec_ref: 문서 6 §6.5 「doc_type의 구조 — 스키마 1 + 어댑터 N」(B37·B38) — code_ref: cli/register.py:528-529 (registry.lookup 중복 거부) · cli/register.py:1536-1609 (main: roles/generate/review/confirm/list뿐) **(중복: doc6-ingest-routing #110 · doc6-register #117 · guide-vs-code #214)**
- **#151** [low · spec_vs_code] 재생성 이력의 주체(by)가 검수 뷰 계약·렌더에 없다 — spec_ref: 문서 6 §6.5 「자동으로 보낸 지시도 이력에 남긴다」(B50) — code_ref: kit/검수뷰_데이터스키마.json:18-27 · kit/render_review.py:238-246 (_regen) **(중복: doc6-review-view #134)**
- **#152** [low · doc_stale] kit/참조어댑터/README.md가 B27·B29 이전 문면이다 — 「바이트 동일 복사본」·mock/ 경로·3종 순차 주입 — spec_ref: 문서 6 §6.7 킷 #4 (B27·B29) — code_ref: kit/참조어댑터/README.md:24-33,35-39 vs kit/참조어댑터/ipqc.py:1-3 · cli/prompt.py:132-142 **(중복: doc6-adapters #102)**
- **#153** [low · doc_stale] 스켈레톤 동봉 안내의 「빈칸 정상 결과 = FAIL 4건」이 하네스 실물(5건)과 다르다 — spec_ref: 문서 6 §6.7 킷 #2 · §6.5 하네스 실패 갈래(B50 「조각 0건」) — code_ref: kit/어댑터_스켈레톤.py:18-30 vs kit/run_adapter.py:208-214 · tests/test_p2.py:71-85
- **#154** [low · spec_vs_code] 관찰 범위 규칙: 템플릿 「정형 약 10줄」·명세 「10~20줄」·코드 20행 고정, 목차형 확장 규칙은 구현이 없다 — spec_ref: 문서 6 §6.5 절차(표본 크기 · B34) · 입력 패키지 표 「관찰 범위 규칙은 시스템 소유 — 프롬프트 템플릿에 고정」 — code_ref: kit/생성프롬프트_템플릿_v1.0.md:61-62 · parser/reader.py:553-575 (OBSERVE_ROWS=20, head) **(중복: doc6-register #128)**
- **#155** [low · doc_stale] 킷 자산들이 제거된 옛 문서(파서_명세 §7/§9·CH2·카드 M11)를 정본으로 가리킨다 — spec_ref: 문서 6 §6.6-3·§6.7 킷 #3 · CLAUDE.md §1(명세는 정제본뿐) — code_ref: kit/표적출력_정의.md:3 · kit/검수뷰_데이터스키마.json:4 · kit/render_review.py:357-358 · kit/생성프롬프트_템플릿_v1.0.md:3 · kit/run_adapter.py:98,146 · tests/test_p2.py:92-93 **(중복: doc6-review-view #137)**
- **#156** [low · spec_gap] 규약 10 재구현 판정이 함수 이름 목록(SELFMADE 8종)에 의존한다 — 명세가 판정 방식에 침묵 — spec_ref: 문서 6 §6.7 킷 #6 (B31) — code_ref: kit/run_adapter.py:38-39,101-108,123-129
- **#157** [low · spec_vs_code] 문서 7 §7.5 mock 자산 경로 규약(`mock/…`)과 실물(`tests/fixtures/…`)이 다르다 — spec_ref: 문서 7 §7.5-4 · §7.5 「mock 자산의 자리 — 경로 규약」 — code_ref: core/fixtures.py:33-40 · tests/fixtures/{adapters,fixtures,parsed,queries.json} **(중복: doc6-parser-core #93 · doc7-layout #161 · doc7-outputs #198)**

### doc7-layout — 12건

- **#158** [med · spec_vs_code] cli/register.py가 모듈 최상단에서 kit.run_adapter→openpyxl을 import — USE_MOCK=1 등록 경로가 패키지 미설치 환경에서 ImportError로 죽는다 — spec_ref: 문서 7 §7.1 「USE_MOCK=1(기본)에서 외부 의존 없이 전체가 동작」 불릿 — code_ref: cli/register.py:56 (`from kit.run_adapter import load_blocks`) · kit/run_adapter.py:24 (`from openpyxl.utils import range_boundaries, get_column_letter` 최상단) · run.py:167 (`from cli.register import main`)
- **#159** [med · code_missing] run.py에 명세가 열거한 서브커맨드 `test`·`status`가 없다 — spec_ref: 문서 7 §7.1 코드 배치 트리(22행) 및 진입점 계약 문단(49행) — code_ref: run.py:209-235 (디스패치 표) **(중복: guide-vs-code #226)**
- **#160** [med · spec_vs_code] run.py가 같은 기능을 `build`와 `ingest` 두 이름으로 제공한다 — spec_ref: 문서 7 §7.1 진입점 계약 문단(49행) — code_ref: run.py:214-217 ("build"·"ingest" 두 키가 동일 cmd_ingest 호출) · run.py:18-19·206 (관문도 두 이름에 건다)
- **#161** [med · spec_vs_code] mock 자산 경로 규약(`mock/…`)이 코드(`tests/fixtures/…`)와 다르다 — 명세 낡음 — spec_ref: 문서 7 §7.1 코드 배치 트리(19-20행) · §7.1 대체 표 추출 행(39행) · §7.5 「mock 자산의 자리 — 경로 규약」(191행) — code_ref: core/fixtures.py:33-41 (ROOT_DIR = tests/fixtures, PARSED·QUERIES·ADAPTERS·EXTRACT_HINTS 전부 그 아래) · 레포 루트에 mock/ 디렉터리 없음 **(중복: doc6-parser-core #93 · doc6-kit #157 · doc7-outputs #198)**
- **#162** [med · spec_vs_code] record_id의 필드 구분자 `\x1f`가 norm()의 `\s+` 치환에 삼켜져 필드 경계가 사라진다 — 다른 행이 같은 해시 입력이 된다 — spec_ref: 문서 7 §7.2 산식(72행) 및 구분자 불릿(79행) — code_ref: core/ids.py:36 (`_WS = re.compile(r"\s+")`) · core/ids.py:78-79 (`norm(joined)`) · core/ids.py:112 (OccCounter.next도 norm) · core/ingest.py:302-303 **(중복: doc7-ids-records #170)**
- **#163** [med · spec_vs_code] 수정 큐의 파일 락이 write 구간만 감싸 read-modify-write 유실을 막지 못한다 — spec_ref: 문서 7 §7.1 core 접근 경계 표 core/store.py 행(60행) — code_ref: core/store.py:213-236 (enqueue: read→append→write) · core/store.py:79-126 (atomic_write_bytes — 락 획득이 함수 안, tmp 쓰기~replace만 보호) · core/store.py:173-185 (drop), 188-210 (resolve_item) 동형 **(중복: doc7-ids-records #171)**
- **#164** [med · spec_vs_code] doctor.py가 graph.json·review_queue.json을 직접 연다 — 경계 검사 패턴을 문자열 분할로 회피 — spec_ref: 문서 7 §7.1 「저장 계층 — GraphStore 경계」 불릿(46행) · core/store.py 행(60행) — code_ref: doctor.py:203-204 (`rel.endswith("graph" + ".json")` 후 `f.read_bytes()`) · doctor.py:205 (`json.loads((ROOT/"data"/"review_queue.json").read_text(...))`) **(중복: high doc7-outputs(doctor _idempotent) 부수 지적(B6) 일부)**
- **#165** [low · code_missing] `layers/__init__.py`가 없다 — spec_ref: 문서 7 §7.1 「패키지화」 불릿(48행) — code_ref: layers/ (process/·quality/만 존재) · core/__init__.py·cli/__init__.py는 존재 **(중복: doc1-invariants #212 일부)**
- **#166** [low · spec_vs_code] chunk_id 산식에서 section을 norm()해 해시 입력에 넣는다 — 명세 산식은 section 원문 — spec_ref: 문서 7 §7.2 산식(71행) — code_ref: core/ids.py:69 (`norm(text) + NUL + norm(section) + NUL + str(occ)`) **(중복: doc7-ids-records #175)**
- **#167** [low · spec_vs_code] 테스트가 dictionary.json을 store.write로 직접 써 provenance 관문을 우회한다 — spec_ref: 문서 7 §7.1 core 접근 경계 표 core/dictionary.py 행(57행) — code_ref: tests/test_g5.py:81-84, 181-183 (`store.read(store.DICTIONARY)` → setdefault().append() → `store.write(store.DICTIONARY, d)`) · 관문 검사 tests/test_g1_g2.py:126-134는 core/·cli/만 훑는다
- **#168** [low · doc_stale] §7.1 코드 배치 트리에 실물 디렉터리 adapters/·prompts/가 없다 — spec_ref: 문서 7 §7.1 코드 배치 트리(10-26행) — code_ref: core/registry.py:124,136-140 (adapters/{doc_type}.py 정본 자리) · core/llm.py:230-244 (prompts/{name}.md 읽기) · 레포 루트 adapters/·prompts/ 존재
- **#169** [low · doc_stale] CLI 사용법 문자열이 금지된 파일 경로 호출형(`python cli/ops.py …`)으로 적혀 있다 — spec_ref: 문서 7 §7.1 진입점 계약 문단(49행) — code_ref: cli/ops.py:4-8 · cli/parse.py:7-11 · cli/scan.py:16 · cli/platform.py:19 (모듈 docstring 사용법) **(중복: guide-vs-code #224)**

### doc7-ids-records — 7건

- **#170** [med · spec_vs_spec] record_id 산식의 `\x1f` 구분자가 norm()에 지워져 필드 경계가 뭉개진다 — 명세 산식과 「구분자 필수」가 서로 어긋난다 — spec_ref: 문서 7 §7.2 산식 블록 + 「구분자(`\x00`·`\x1f`) 필수」 불릿 — code_ref: core/ids.py:72-79 (record_id: `norm(joined)`), core/ids.py:36·60 (norm = re `\s+`), core/ingest.py:302-303 (occ 키도 norm(joined)) **(중복: doc7-layout #162)**
- **#171** [med · spec_vs_code] 수정 큐·공유 대장의 read-modify-write가 락 밖에서 돈다 — 락이 쓰기 순간만 감싼다 — spec_ref: 문서 7 §7.1 core 접근 경계 표 `core/store.py` 행 — code_ref: core/store.py:79-126 (atomic_write_bytes — flock은 이 함수 안에서만), core/store.py:129-131 (read: 락 없음), core/store.py:213-236 (enqueue: read→검사→append→write), core/store.py:173-185 (drop), core/store.py:188-210 (resolve_item), core/ingest.py:79-89·197-213 (doc_registry read→수정→write) **(중복: doc7-layout #163)**
- **#172** [med · spec_vs_code] duplicate_doc_hold ㉡ 해제가 판단을 항목에 기록하지 않고 항목을 지운다 — spec_ref: 문서 2 §2.7-① 진입 검증 · 문서 7 §7.2 큐 항목 `resolution` 불릿 — code_ref: core/ingest.py:84-94 (allow_duplicate → store.drop('duplicate_doc_hold', …)), core/ingest.py:80-81·208-209 (판단은 doc_registry.duplicate_ok에만 남음) **(중복: doc2-contractA #13)**
- **#173** [med · code_missing] 큐 항목에 `resolution`을 쓰는 운영 경로가 없다 — resolve_item은 테스트에서만 호출된다 — spec_ref: 문서 7 §7.2 저장 레코드 스키마 큐 항목 + §7.3-3① CLI+파일 — code_ref: core/store.py:188-210 (resolve_item 정의), cli/platform.py:498 (안내 문자열 `core.store.resolve_item(...)`만), core/ingest.py:151-154 (보존 판정은 있음)
- **#174** [low · spec_vs_code] 비정형 문서의 인입 검증 3단이 추출 단계와 구축 단계에서 두 번 돈다 — spec_ref: 문서 7 §7.3-1 — code_ref: cli/extract.py:62 (ingest(env)), core/pipeline.py:788 (run_document이 payload_kind와 무관하게 ingest(env) 재호출), core/ingest.py:247-252 (두 번째 호출은 doc_id가 대장에 있어 withdraw 경로로 들어감) **(중복: high doc4-queue-ops(resolve_item 호출부 0) 일부)**
- **#175** [low · spec_vs_code] chunk_id 산식이 section에도 norm()을 건다 — 명세 산식은 section을 정규화하지 않는다 — spec_ref: 문서 7 §7.2 산식 블록 + 「section 표기도 산식의 일부」 불릿 — code_ref: core/ids.py:68-69 (`norm(section)`), core/ids.py:111-115 (OccCounter도 키 전부 norm) **(중복: doc7-layout #166)**
- **#176** [low · spec_gap] chunk_id 충돌 시 로그 후 덮어쓴다 — 어느 청크가 남는지를 명세가 정하지 않는다 — spec_ref: 문서 7 §7.2 「절단 12자」 불릿 · 문서 2 §2.7-② — code_ref: core/ingest.py:262-294 (put_chunk: defects.log 적재 후 `chunks["chunks"][cid] = {...}`로 뒤 청크가 앞 청크를 대체) **(중복: doc2-contractA #17)**

### doc7-llm — 16건

- **#177** [med · spec_vs_code] extract.md의 {{categories}}·{{relations}}·{{attach_candidates}}·{{chunk}}를 코드가 치환하지 않는다 — 설명문에도 같은 표기가 쓰인다 — spec_ref: 문서 7 §7.6-B-5 ([정정] 39) — code_ref: prompts/extract.md:14,43,52,66-69 · core/extract.py:188-196 (_candidates_for — 템플릿을 그대로 system에 싣고 categories 등은 user JSON으로 보냄) **(중복: doc4-extract #65)**
- **#178** [med · spec_vs_code] ⑨좌표 태깅의 지시문이 코드에 박혀 있다 — 명세가 말하는 coord_tag 템플릿 파일이 없다 — spec_ref: 문서 7 §7.6-B-5 — code_ref: core/llm.py:718-731 (pick_coord — system 문안 인라인) · prompts/ 디렉터리(coord_tag.md 부재) **(중복: doc1-invariants #207)**
- **#179** [med · code_missing] strict 스키마 회귀가 core.extract(EXTRACT_SCHEMA)·cli.interview(INTERVIEW_SCHEMA)를 훑지 않는다 — spec_ref: 문서 7 §7.6-B-1 (B44) — code_ref: tests/test_p3.py:738 (모듈 목록 ("cli.register","cli.query","core.query","core.llm","core.matcher")) · core/extract.py EXTRACT_SCHEMA · cli/interview.py INTERVIEW_SCHEMA
- **#180** [med · spec_vs_code] LLM을 부르는 화면 명령 4종(build/ingest · ingest-file/dir · parse run · cli.extract)이 머리에 모드 1줄을 표시하지 않는다 — 가이드는 「뜬다」고 말한다 — spec_ref: 문서 7 §7.6-B-1 (B42) · docs/가이드/2B_작업가이드.md:60 — code_ref: run.py:81-92 (cmd_ingest — mode_line 호출 없음) · run.py:206-216 · cli/ingest.py:200-224 · cli/parse.py:88-123 (cmd_run) · cli/extract.py:79-88 **(중복: guide-vs-code #216 · doc7-outputs #196 일부)**
- **#181** [low · spec_vs_code] 세션 사용량 합계를 표시하는 화면 명령이 register뿐이다 — query·build·ingest·parse는 실호출 후 합계를 내지 않는다 — spec_ref: 문서 7 §7.8 로그 규격 (B24) — code_ref: cli/register.py:647,706 (usage_total 호출 2곳) · cli/query.py:264-281 · run.py:81-92 · cli/ingest.py:200-224 **(중복: doc7-outputs #195)**
- **#182** [med · spec_gap] `python -m cli.extract`가 mock 관문 밖이고 추출 체크포인트에 mock/live 표시가 없다 — mock 후보가 실호출 build에 그대로 재사용된다 — spec_ref: 문서 7 §7.6-B-1 (B48 관문 목록) · §7.6-B-4 — code_ref: cli/extract.py:79-88 (main — require_live_or_allow 호출 없음) · core/extract.py:258-262 (has_checkpoint면 그대로 반환) · core/extract.py:278-287 (체크포인트 필드에 갈래 표시 없음) **(중복: doc7-outputs #196 일부 · doc1-invariants #209 일부)**
- **#183** [med · spec_vs_code] `parse build`(P1 잔재)가 mock 관문 밖에서 돌며 review/{doc_type}/approval.json을 P1 형식으로 덮어쓴다 — spec_ref: 문서 7 §7.6-B-1 (B48 관문) · §7.8 (approval.json — 사람 판단 기록) — code_ref: cli/parse.py:133-206 (cmd_build) · cli/parse.py:213-215 (관문은 run만) · cli/register.py:1513 (같은 경로에 승인 기록) **(중복: doc6-ingest-routing #109)**
- **#184** [med · spec_vs_code] ①추출의 mock 힌트 파일(extract_hints/{doc_id}.json)이 실호출 모드에서도 LLM보다 먼저 적용된다 — spec_ref: 문서 7 §7.1 USE_MOCK 대체 표 (추출 행) · §7.6-B-3 · B48 「운영 코드가 fixture 파일을 찾지 않는다」 — code_ref: core/extract.py:264-275 (_load_hints → hints 우선, use_mock 무관) · core/extract.py:179-183 (분기는 _candidates_for 안에만) **(중복: doc4-extract #64)**
- **#185** [low · spec_vs_code] ⑦·⑨의 대체 갈래가 MOCK 로그를 남기지 않는다(파서 로그는 ④만) — spec_ref: 문서 7 §7.8 로그 규격 — code_ref: parser/pipeline.py:201 (_mock_log 호출 — ④만) · parser/struct_map.py:141-147 (휴리스틱 갈래, 로그 없음) · parser/tagger.py:141-144 (pick None이면 정확 일치만, 로그 없음) **(중복: doc7-outputs #194)**
- **#186** [low · spec_gap] GatewayError는 5xx도 재시도하지 않는다 — 명세는 4xx 재시도 금지만 말하고 5xx는 침묵; 그 4xx 조항도 문서 7 본문에 없다 — spec_ref: 문서 7 §7.6-B-1 「모델 호출 + JSON 파싱 + 재시도」 · 개정대장 §AC 「함께 수용: 4xx 재시도 금지」 — code_ref: core/llm.py:388-391 (except GatewayError: raise) · core/llm.py:340-352 (_post — 모든 HTTPError → GatewayError)
- **#187** [low · spec_gap] USE_MOCK 값의 판독이 "1" 문자열 동일성뿐이라 llm.json의 `"USE_MOCK": true`가 실호출로 판독된다 — spec_ref: 문서 7 §7.6-B-1 (B42) — code_ref: core/llm.py:85-91 (use_mock — `return "1" if v is None else str(v) == "1"`)
- **#188** [low · spec_gap] 명세가 이름을 정한 설정 4종 밖에 코드가 5종을 더 만들었다(EMBED_MODEL·LLM_TIMEOUT·LLM_RETRY·LLM_CONTEXT_TOKENS·ONTO_CONFIG) — B41의 「설정에 채우면」의 키 이름이 명세에 없다 — spec_ref: 문서 7 §7.6-B-1 · 개정대장 §Z B41 — code_ref: core/llm.py:122-123 (ONTO_CONFIG) · core/llm.py:205-207 (EMBED_MODEL·LLM_TIMEOUT·LLM_RETRY) · core/llm.py:440-441 (LLM_CONTEXT_TOKENS)
- **#189** [low · spec_gap] ⑥질의 링킹 LLM 폴백이 전 층 생존 노드 전량을 후보로 전송한다 — 크기 예산 대조가 ⑤⑦에만 있고 ①⑥⑧에는 없다 — spec_ref: 문서 6 §6.5 (B30·B41) · 문서 7 §7.6-B-2 ⑥ — code_ref: core/query.py:73-84 (_link_llm — pool = 전 층 live 노드 전량) · core/llm.py:432-447 (context_limit) · cli/prompt.py:291-306 (⑤만 대조) · core/llm.py:789-796 (⑦만 대조)
- **#190** [low · spec_vs_code] ⑦구조 지도 실호출 입력에 어댑터 expects 힌트가 들어가지 않는다 (§6.3 입력 정의와 다름 — 담당 절 밖) — spec_ref: 문서 6 §6.3 구조 지도 패스 — code_ref: core/llm.py:777-824 (map_structure(doc_id, lines) — 힌트 인자 없음) · parser/pipeline.py:178 (ask=map_structure 주입) · parser/struct_map.py:105-128 (propose(doc_id, lines, ask)) **(중복: doc6-parser-core #92)**
- **#191** [low · spec_vs_code] llm-check ⑦(이미지 입력)이 「그 지점을 켤 때만」이 아니라 매 실행 무조건 왕복한다 — spec_ref: 문서 7 §7.6-B-0 ([정정] 42) — code_ref: core/llm.py:576-590 (⑥은 EMBED_MODEL 유무로 조건) · core/llm.py:591-612 (⑦은 무조건)
- **#192** [low · doc_stale] 구조도 05의 prompts/ 목록에 link.md가 빠져 있다 — spec_ref: docs/구조도/05_조절_지도.md:102 (문서 7 §7.6-B-5의 파일 정본 목록에 대한 구조도) — code_ref: prompts/link.md (존재 · core/query.py:80에서 읽음)

### doc7-outputs — 13건

- **#193** [med · spec_vs_code] `python -m cli.*` 진입점은 log.setup()을 부르지 않아 MOCK 경고·큐 적재 INFO 로그가 통째로 사라진다 — spec_ref: 문서 7 §7.8 로그 규격 · §7.1 진입점 계약 — code_ref: core/log.py:29-50 (setup), cli/parse.py:218, cli/scan.py, cli/platform.py, cli/query.py:284-285, cli/ops.py:129, cli/register.py, cli/viewer.py:134 (setup 호출 0) **(중복: guide-vs-code #218)**
- **#194** [med · spec_vs_code] LLM 지점 ⑦구조 지도·⑨좌표 태깅의 mock(대체) 갈래는 MOCK 경고를 로그로 남기지 않는다 — spec_ref: 문서 7 §7.8 로그 규격 · §7.6-B-2 · §7.1 USE_MOCK 대체 표 — code_ref: parser/pipeline.py:200-202 (④만 _mock_log), parser/struct_map.py:137-145, parser/tagger.py:139-149, core/llm.py:734-742·847-856 (팩토리가 None만 반환) **(중복: doc7-llm #185)**
- **#195** [med · spec_vs_code] LLM 사용량 세션 합계를 표시하는 화면 명령이 `register generate`뿐이다 — query·build·parse·extract·ingest-file은 표시하지 않는다 — spec_ref: 문서 7 §7.8 로그 규격 — code_ref: core/llm.py:276-282 (usage_total), cli/register.py:647·706 (유일한 호출부), cli/query.py:264-282, cli/extract.py:80-91, cli/parse.py:209-217, run.py:cmd_ingest **(중복: doc7-llm #181)**
- **#196** [med · spec_vs_code] 계약 진입점 `python -m cli.extract`는 모드 1줄 표시도 mock 관문도 없다 — spec_ref: 문서 7 §7.6-B-1 · §7.1 진입점 계약 — code_ref: cli/extract.py:79-91, core/extract.py:182-183 **(중복: doc1-invariants #209 · guide-vs-code #217 · doc7-llm #182 일부)**
- **#197** [med · code_missing] 수정 큐 열람에 「참조가 많은 auto 노드를 앞에 두는 우선순위 정렬」이 없다 (2A 항목 spec-s7-11-51 — HEAD에서도 미수리) — spec_ref: 문서 7 §7.8 data/ 표 review_queue.json 행 · 「수정 도구 세트의 등재」 · 문서 4 §4.7-3 — code_ref: cli/platform.py:119-144 (queue_view·cmd_queue), core/store.py:213-236 (enqueue append) **(중복: doc4-queue-ops #56)**
- **#198** [med · spec_vs_spec] mock 자산 경로 규약이 세 갈래로 갈린다 — 문서 7은 `mock/…`, 문서 5는 `tests/fixtures/`, 코드는 `tests/fixtures/`(ONTO_FIXTURES) — spec_ref: 문서 7 §7.5 「mock 자산의 자리 — 경로 규약」 · §7.1 코드 배치 · §7.8 단계 경계 산출물 ↔ 문서 5 §5.5-4(B54-2) — code_ref: core/fixtures.py:31-40, tests/fixtures/{parsed,adapters,fixtures,extract_hints,struct_maps,queries.json} **(중복: doc6-parser-core #93 · doc6-kit #157 · doc7-layout #161)**
- **#199** [med · spec_vs_code] 등록 리허설 창작 표본이 §7.5 최소 규모에 못 미친다 — IPQC02 23행(<24) · TOC02 25행(<31) — spec_ref: 문서 7 §7.5 「창작 표본의 최소 규모」(개정대장 §… 25→31행 개정) — code_ref: tests/fixtures/raw/IPQC02.xlsx, tests/fixtures/raw/TOC02.xlsx, tests/test_p3.py:82-84·179-181
- **#200** [med · spec_gap] doctor.py는 매 실행(--env 포함) init --fresh로 data/·parsed/·extract/를 지운다 — 「아무것도 고치지 않는다」는 점검기가 백업 대상인 진실을 무경고 삭제한다 — spec_ref: 문서 7 §7.8 (data/ 백업 대상) · §7.6-4 (클린의 용도) · §7.5-7 — code_ref: doctor.py:77-93 (_clean), :140 (check_env), :336·369 (run_suites), :381 (show_state), :15 docstring
- **#201** [low · spec_gap] mock 관문 면제 목록(닫힌 6종)에 없는 viewer가 관문 없이 질의(answer)를 돌린다 — 면제 여부를 명세가 정하지 않았다 — spec_ref: 문서 7 §7.6-B-1 관문 · §7.8 시각화 3형태(B52) — code_ref: cli/viewer.py:13-14·84-86·111, run.py:220-221
- **#202** [low · doc_stale] doctor의 선택 의존 목록·미설치 실측·USE_MOCK=0 문안이 B53 이후 명세와 어긋난다(PyMuPDF 누락, 훅 문안 낡음) — spec_ref: 문서 7 §7.6-B-6 · §7.1 (선택 의존 지연 import 검증 가능 조건) — code_ref: doctor.py:54-56 (OPTIONAL), :132-138 (noformat guard가 openpyxl·pptx만 차단), :168-171 **(중복: guide-vs-code #222 일부)**
- **#203** [low · spec_vs_code] 뷰어에서 층 필터를 끄면 그 층으로 가는 cross-layer 엣지가 함께 사라진다 — 「걸러 그리더라도 cross 엣지는 표시」의 해석 여지 — spec_ref: 문서 7 §7.8 시각화 3형태 — code_ref: cli/export.py:385-388 (layout: edges = DATA.edges.filter(e => byId.has(e.src) && byId.has(e.dst))), :428-436
- **#204** [low · code_missing] 등록 재생성 루프 검증용 `_rev1` 대안본 fixture가 레포에 없다 — 재생성 지시가 같은 fixture를 되돌려준다 — spec_ref: 문서 7 §7.1 USE_MOCK 대체 표 「등록 생성」 행 · §7.5 S12 — code_ref: cli/register.py:190-218 (draft: `{doc_type}_rev{revision}` 탐색 후 기본으로 폴백), tests/fixtures/fixtures/{adapters,schemas}/
- **#205** [low · doc_stale] `show meta` 화면이 제거된 옛 챕터(docs/CH2_문서계약.md)를 정본으로 안내한다 — spec_ref: CLAUDE.md §1 · 문서 7 §7.8 요구 규격 ①(텍스트 창구) — code_ref: cli/show.py:326·332·355

### guide-vs-code — 13건

- **#214** [med · code_missing] 명세 6 §6.5 「스키마 1 + 어댑터 N(양식 변형)」 규격 4항(차분 생성·변형 선택·경로 규약)이 코드에 없다 — spec_ref: docs/spec/6_파서와구축모드.md §6.5 「doc_type의 구조 — 스키마 1 + 어댑터 N(양식 변형별)」 1~4 · 개정대장 §W B37·§X B38 — code_ref: cli/register.py:1536-1609(main) · cli/register.py:556-614(입력 패키지 — 기존 확정본을 싣는 키 없음) · core/registry.py:93-117(단수 adapter 경로만) **(중복: doc6-ingest-routing #110 · doc6-register #117 · doc6-kit #150)**
- **#215** [med · doc_stale] 가이드 §4.1 「기계 관문 PASS 뒤 view.html 경로가 찍힌다」 — generate는 view.html을 만들지도 찍지도 않는다(review가 만든다) — spec_ref: docs/가이드/2B_작업가이드.md §4.1 확인 — code_ref: cli/register.py:790-798(_finish_generate) · cli/register.py:1414-1431(cmd_review — view.html 생성·경로 출력)
- **#216** [med · spec_vs_code] 명세 7 §7.6-B-1 「LLM을 부르는 화면 명령은 머리에 모드 1줄」 — ingest-file/ingest-dir·parse run·build·cli.extract는 실호출 모드에서 모드 줄을 내지 않는다 — spec_ref: docs/spec/7_구현규격과검증.md §7.6-B-1(232행) · 2B_작업가이드 §1 확인 — code_ref: cli/ingest.py:200-225(main) · cli/parse.py:88-123(cmd_run) · run.py:81-92(cmd_ingest=build) · cli/extract.py:79-86 · cli/_gate.py:39(mock일 때만 출력) **(중복: doc7-llm #180)**
- **#217** [med · spec_vs_code] `python -m cli.query`는 mock 관문을 지나지 않고, `cli.extract`는 관문·모드 줄 모두 없다 — 가이드 「run.py <명령>과 -m cli.<모듈>은 같은 것」과 §1 운영 명령 관문 표가 어긋난다 — spec_ref: docs/spec/7_구현규격과검증.md §7.6-B-1 · 2B_작업가이드 20행·§1 「mock 모드입니다」 행 — code_ref: run.py:206-208(build·ingest·query만 run.py 쪽에서 관문) · cli/query.py:264-282(main — require_live_or_allow 호출 없음) · cli/extract.py:79-86(main — 관문 없음) **(중복: doc7-outputs #196 · doc1-invariants #209)**
- **#218** [med · doc_stale] `python -m cli.<모듈>` 진입은 log.setup()을 하지 않아 INFO 로그(MOCK 경고·큐 적재·LLM 사용량)가 버려진다 — 가이드가 §4 전체를 `-m cli.register`로 안내한다 — spec_ref: 2B_작업가이드 20행 「같은 것이다」·§5.4 「LLM 호출 수·토큰 | 실행 화면 머리·끝 + INFO 로그」 · docs/spec/7_구현규격과검증.md §7.8 로그 규격(276행) — code_ref: core/log.py:29-50(setup) · run.py:202(log.setup — run.py 경로만) · cli/register.py:1612-1613 · cli/ingest.py:228-229 · cli/query.py:284-285 (__main__에 setup 없음) · core/log.py:64·68·79(INFO 레벨) **(중복: doc7-outputs #193)**
- **#219** [med · doc_stale] doctor.py `_idempotent()`가 조기 return으로 죽은 코드다 — 「클린 2회 동일 그래프 (완료판정 4)」 판정 줄이 화면에 절대 나오지 않는다 — spec_ref: docs/spec/7_구현규격과검증.md §7.6 완료판정 4(202행) · 2B_작업가이드 §0 「python doctor.py # 환경 → 회귀 → 상태 → 사내 전환 판정」 — code_ref: doctor.py:213-220(조기 return) · doctor.py:221-234(도달 불가) · doctor.py:360(호출부 — 반환 무시) **(중복: high doc7-outputs(doctor _idempotent))**
- **#220** [med · doc_stale] 가이드 §5.1 `--doc-type` 생략 규칙이 비정형(pptx·pdf)의 지정 필수와 「둘 이상·0건은 묻지 않고 미선택으로 끝난다」를 빠뜨렸다 — spec_ref: 2B_작업가이드 §5.1 Rule 표 「--doc-type 생략」 행 · docs/spec/1_금지와불변.md C15 ② — code_ref: cli/ingest.py:77-79(PROSE_EXT → status none) · cli/ingest.py:98-107(ambiguous/none) · cli/ingest.py:126-129·225(미선택 출력 후 rc 1)
- **#221** [low · doc_stale] doctor 화면·config 가이드가 존재하지 않는(또는 경로 없는) 파일을 가리킨다 — docs/skeleton_seed.md · 점검_자산.py — spec_ref: 2B_작업가이드 §0(doctor 화면이 다음 걸음을 낸다) · docs/가이드/config작성_가이드.md 138행 — code_ref: doctor.py:423 · docs/가이드/config작성_가이드.md:138
- **#222** [low · doc_stale] doctor 기준선·포맷·선택 의존 문면이 낡았다 — 가이드 §0 「824」 vs SUITES 합 903, 「회귀 10종」 vs 11종, 포맷 목록에 .pdf 없음, PyMuPDF·soffice 미점검 — spec_ref: 2B_작업가이드 §0 확인 · requirements.txt 12행 · docs/spec/7 §7.6-B-6 — code_ref: doctor.py:35-47(SUITES 11종, 합 903) · doctor.py:326(「회귀 10종」) · doctor.py:120-122(포맷 목록에 .pdf 없음) · doctor.py:54-56(OPTIONAL에 PyMuPDF 없음, soffice 검사 없음) **(중복: doc7-outputs #202 일부)**
- **#223** [low · doc_stale] 도움말 문면이 실물과 다르다 — run.py/cli.show 도움말에 show log·extract·bm25 누락, `python run.py --help`는 KeyError, 인자 없는 `python run.py`는 `all`(빌드) 실행 — spec_ref: 2B_작업가이드 §5.3·§5.4(show log·extract·bm25 안내) · run.py 머리 docstring 44행 — code_ref: run.py:44 · cli/show.py:4-10(7종) vs cli/show.py:550-552(10종) · run.py:203(`cmd = … else "all"`) · run.py:209-235(dict 인덱싱 — 미지 명령·--help는 KeyError)
- **#224** [low · doc_stale] cli 도움말이 명세 7 §7.1이 금지한 파일 경로 호출 표기(`python cli/register.py …`)를 쓰고, cli/scan.py 도움말은 B47 이전 「유일 일치여도 자동 라우팅하지 않는다」를 말한다 — spec_ref: docs/spec/7_구현규격과검증.md §7.1(49행) · docs/spec/1_금지와불변.md C15 ②(B47) — code_ref: cli/register.py:21-35 · cli/ops.py:4-8 · cli/parse.py:7-11 · cli/platform.py:19 · cli/scan.py:7·16-17 vs cli/ingest.py:83-97(유일 일치 자동 chosen) **(중복: doc7-layout #169 · doc6-ingest-routing #114 일부)**
- **#225** [low · doc_stale] 가이드 §4.2 「review가 묻는 것 둘 — 기본은 끔」 vs 추출 리허설 프롬프트는 Enter가 켬([Y/n]); 명세는 추출 리허설 기본값에 침묵 — spec_ref: 2B_작업가이드 §4.2 「review가 묻는 것 둘」 · docs/spec/6_파서와구축모드.md §6.5 「검수 리허설의 범위와 비용 관문」·§6.6 3구획 ② — code_ref: cli/register.py:1393-1401(추출 리허설 [Y/n] — 빈 입력=켬, 비대화형=끔) · cli/register.py:1209-1213(좌표 보조 [y/N] — 빈 입력=끔) **(중복: doc6-register #125 · doc6-review-view #135)**
- **#226** [low · spec_vs_code] 명세 7 §7.1이 run.py 진입점을 (init/build/query/test/status/all)로 열거하지만 run.py에 test·status 서브커맨드가 없다 — spec_ref: docs/spec/7_구현규격과검증.md §7.1 진입점 계약(49행) — code_ref: run.py:209-235(서브커맨드 dict) **(중복: doc7-layout #159)**

## 허브 판정용 요약

### (1) 코드 수리 후보(B55) — high 확정 중 fix_owner code/both

| # | 한 줄 | fix_owner | 재판정 |
|---|---|---|---|
| H1 | process_ref null/부재 — 블록 필드 use_blocks 미전개·STRUCTURAL 오분류로 missing_field 미발생, defer 비어 dropped_entities·pending·dropped 엣지 무기록 소실 (core/pipeline.py:24-25,55-60,192-202,280-284,388 · core/build.py:123) | both | high |
| H2·H3·H22·H25 | 파서 측 실패 3종(parse_failure·adapter_mismatch·hierarchy_unresolved)을 review_queue에 enqueue하는 코드 0건 — 착지 주체(파서 core import vs cli 호출부)를 A1과 정합해 확정 후 구현 (parser/pipeline.py:169,184,229 · cli/ingest.py:141-145 · cli/parse.py:115-118) | code | high/med |
| H4 | provenance에 doc_id 접두 없음 + withdraw.mine 전역 locator 대조 → 타 문서 근거 회수·거짓 evidence_lost (core/pipeline.py:38-39,643 · core/ingest.py:112-190) — 명세가 접두 주체 확정 후 _prov·build_prose·withdraw 수정 | both | high |
| H5 | build_prose가 B14 미구현 — 좌표 미해소 청크의 Property를 스코프 없이 생성, orphan_anchor에 재료 없음 (core/pipeline.py:644-655) + tests/test_g3.py:318-325 어서션 뒤집기 | code | high |
| H6 | orphan 재시도가 anchor 층 cfg로 dropped_edges 게이트 → occurs_in·controlled_by undeclared_relation 거부 후 store.drop (core/retry.py:220,257,266) — 문서 층 config 복원 + 거부 시 항목 유지 | code | high |
| H7 | 재시도 pending_attrs가 attach_to_field 무시하고 좌표 노드에 put_attribute, contextual을 context 유무로 추정 (core/retry.py:260-265) — pending에 contextual 플래그, attach_to_field→revived 저장, 회귀 테스트 | both | high |
| H8 | category_pair_map 조회 순서 (대상,자식) 고정 — `Unit,Process→part_of` 도달 불가, prose attach 무기록 생략 (core/pipeline.py:138,717-721 · core/retry.py:156) — 조회·commit_edge 끝점 방향 모두 표 키 순서로 | code | high |
| H9·H10 | matcher.candidates 사전 갈래 is_live 누락 → obsolete 노드 1.0 MATCH 부활; replaced_by 전이·uncertain_match 미구현 (core/matcher.py:114-123,150-160) | code | high |
| H11 | ops._target이 is_live로 obsolete까지 거부 + `n[STATUS_MERGED]` KeyError (core/ops.py:52-63) — merged_into만 거부하도록 | code | med |
| H12 | auto→confirmed 검토 연산·payload.node_id 드롭·회수 후 항목 정리 부재 (core/store.py:188-210 · cli/ops.py:55-56) | code | med |
| H13 | 추출 규약 9 미구현 — 청크 예외 미포착·{"chunk_id","failed"} 0건·append_defect 0건·구축 건너뛰기 0건 (core/extract.py:189-201,258-292) | code | high |
| H14 | graph.neighbors 비재귀 가드가 규칙 단위 — up 1홉 뒤 down 재귀 재개로 층 전체 확장 (core/graph.py:188-202) — 프론티어에 도착 규칙 recursive 여부 동반 | both | med |
| H15 | 계기판 2 plateau doc_of `doc_id-` 접두 전제 vs raw locator provenance → 실파서 전 문서 rate=None; 계기판 3 pieces 문서 구분 없음 (cli/platform.py:261-289) — H4와 같은 수리 | both | high |
| H16 | gauges 계기판 8이 save() 시간 측정, 실제 빌드 metrics 미저장·미소비 (cli/platform.py:306-311 · run.py:84 · core/pipeline.py:565) — 측정치 저장 자리 확정 후 별도 호출이 읽도록 | both | med |
| H17 | struct_map.propose가 `{doc_id}:{프레임}.json`을 source_hash 없이 별도 보존·해시 대조 없이 재사용, invalidate 미정리 (parser/struct_map.py:70,123-136 · parser/pipeline.py:68) | code | high |
| H18 | tagger.complete_images가 meta.slide만 조회 → PDF 쪽 렌더 ④ 미전달, slide_render='none' (parser/tagger.py:211 · parser/adapters/basic_pdf.py:83) + test_p1 [B53 c] 검사 추가 | code | med |
| H19 | D-110 경로 상이 경고가 `.name` 비교 — 동명·타폴더에서 미발화 (cli/ingest.py:132) — source_path 비교 + test_g6 어서션 | code | med |
| H20 | 재생성 지시(하네스 실패 문면·--instruct·문답 답)가 _draft_live 메시지에 미전달, draft() 시그니처·템플릿에 지시 자리 없음 (cli/register.py:363-419,836-862,1315-1326 · cli/prompt.py:160-248) | code | high |
| H21 | 리허설 파싱 doc_id `{DOC_TYPE}{i:02d}` → struct_maps 보존분 CP01 키, 운영 인입 재사용 miss (cli/register.py:1368,1258 · cli/parse.py:176 동반) | code | med |
| H23 | llm.probe except HTTPError 죽은 코드 — GatewayError로 바꿔 401/403을 ③인증으로 (core/llm.py:340-352,523,536) | code | med |
| H24 | doctor._idempotent 조기 return — 완료판정 4 계측 0회; _clean() 호출로 교체, 반환값 수신, snap()의 graph.json 직접 열기(B6) 처리 결정 (doctor.py:213-234,360) | code | high |
| H26 | (코드 몫) core/build.py:293-296 docstring·tests/test_g3.py:126 show 문구를 B26 모델로 갱신 — 동작 변경 불필요 | both | med |
| H27 | 등록된 doc_type의 재등록·변형 등록·adapter_version 갱신 경로 부재 — registry.register 이름 중복 ValueError(cli/register.py:1494 미처리), unregister CLI 호출부 0, revise 서브커맨드 없음 (core/registry.py:106-109,120) | both | high |

### (2) 명세·문서 문면 후보 — fix_owner spec/doc

fix_owner가 spec/doc **단독**인 high는 0건이다. 아래는 both 8건의 명세·문서 측 몫(입력 verdict가 명시한 범위만).

| # | 한 줄 | 문서 |
|---|---|---|
| H1 | null 좌표(값 부재, 미해소 아님)의 연쇄 드롭 재료를 보유할 큐 kind 지정 — §2.4-③은 orphan_anchor·orphan_attach만; docs/국면2_명세공백_인계.md:60-61에 등재된 공백을 확정; §2.5 규약 3의 블록 필드 ≠ 구조 필드 문면 유지 | 문서 2 §2.4-③·§2.7-③ |
| H4·H15 | provenance 항목의 doc_id 접두 부여 주체·형식 확정 — 부록_용어(접두 포함) vs 문서 2 §2.2·문서 6 §6.4-5·[정정] 42(raw locator) vs 문서 4 §4.8-2 ① 회수 산식(합집합) 자기모순 해소 | 부록_용어 provenance 행 · 문서 2 §2.2 · 문서 4 §4.8-1/2 · 문서 6 §6.3-5·[정정] 42 · 문서 5 §5.5 산식 2 |
| H7 | §2.4-③ 「해소된 노드에 저장」의 「해소된 노드」를 attach_to_field 대상으로 정밀화(개정대장 단절-21 「인입 규칙대로 저장」과 정합) · pending_attrs 형식에 contextual 플래그 명시 | 문서 2 §2.4-③ · 문서 4 §4.4-4·§4.7-5 |
| H14 | 같은 관계의 규칙 둘(up 비재귀·down 재귀)이 프론티어에서 상호작용하는 방식(가드가 규칙 단위인지 관계 단위인지) 명문화 | 문서 5 §5.1-4·§5.1-5 |
| H16 | 표 8행(빌드 시간 직접 관측)과 규율 5(build 경로에 심지 않고 별도 호출로만)의 양립 방식 — 빌드가 남긴 측정치의 저장 자리와 별도 호출의 판독 지점 | 문서 5 §5.5 표 8행·산식 규율 5 |
| H26 | 문서 1 F1 괄호 예시(「현행 Property」·「Unit은 극성 결합 유지」)·F4·부록_용어 「canonical 스코프」(:66)를 B26(Property·Unit) 문면으로; 미러쌍.json에 F1↔§4.5 쌍 등재 | 문서 1 F1·F4 · 부록_용어 · docs/회귀스위트/미러쌍.json |
| H27 | 2B_작업가이드.md §7(:271-272) 「generate --resume」「review --instruct」 처방이 존재하지 않는 경로 — 코드에 재등록 경로가 생길 때까지 삭제 또는 「막힘」 명기; 문서 6 §6.5 규격 4의 물리 배선 보고 요구 | docs/가이드/2B_작업가이드.md §7 · 문서 6 §6.5 |
