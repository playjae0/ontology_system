# B86 구현 요청문 — 「사내 모양」 잔여: 레포 밖 상태 루트의 화면 경로 · 킷 관문의 옛 자리 · 낡은 자리 문자열 · 선택 의존 문면 · 등록 표본 시트 관문 · 사내 모양 회귀 루트 · 칸 0.1 · 0.3 · 1.x · 2.1 · 2.8 · 3.1

> **수신**: 구현 세션 · **발신**: 허브 (2026-09-23 · 사용자 확정 2026-09-23) · **B85 뒤 · B87(산문 엑셀 계층) 앞 · 큐 처리 화면은 B88**.
> **왜 이 회차가 있나 — 회차 꼬임의 정리**: B85는 hub_47 판(좌표 층 이름 해방)으로 구현·마감됐다(`9f72a17` · 허브 판정 D-166 확정). 그 사이 허브가 같은 부류를 전수 조사해 B85를 「사내 모양 결함 묶음」으로 넓힌 판(hub_49·50)을 냈는데, 그 판의 ③(좌표 층 한 자리·골격 파일 부재)과 ④(층 이름 8곳)는 **B85가 이미 했다**. 이 요청문은 **넓힌 판에서 남은 것만** 담는다 — 내용은 사용자가 이미 확정한 그대로다.
> **출처 — 사내 실측**: ⓐ 산문 `register generate`가 `pathlib … is not in the subpath of …` ValueError로 죽고 `adapter.py`가 놓이기 전에 끊겼다(상태 루트가 가이드대로 코드 폴더 **밖** `../onto_state`) ⓑ 새 코드 폴더에서 `openpyxl` 없음이 원인 문면 없이 죽고 WMF 그림 경고가 날것으로 흘렀다 ⓒ 시트 수십 장 산문 엑셀을 등록하면 검수·추출 리허설·킷 관문이 시트 전부를 돈다(B83 누락).
> **공통 원인**: 회귀의 바닥은 상태 루트가 **코드 폴더 안**(`state_mock/`)이다. 사내는 밖이다. B85가 둘째 차이(층 이름)를 회귀에 넣었고, 이 회차가 첫째 차이(상태 루트 위치)와 등록 경로를 넣는다(⑥).
> **넓힌 판(hub_50 B85) 번호와의 대응**(구현 세션 대조 2026-09-23 그대로 — 이 요청문이 그 판정을 받는다):
>
> | hub_50 B85 | State at `9f72a17` | This round |
> |---|---|---|
> | ① `paths.show` | 남음 | **①** |
> | ② 킷 `--closed-list` · 주입 전 기본값 | 남음 | **②** |
> | ③ `coord_layer()` · 골격 파일 부재 거부 | **됐다**(B85 ① — 캐시 키 `(상태 루트, 층 목록)` · D-166 ② 확정) | — |
> | ④ 층 이름 박힌 자리 | **됐다**(B85 ② — 남은 `"process"`는 `generate.py:28` 하나 · 회귀가 센다 · 킷 `--coord-layer`) | — |
> | ⑤ `data/ingest_log` | 남음 | **③** |
> | ⑥ 선택 의존 부재 문면 | 남음 | **④** |
> | ⑧ 등록 표본 시트 관문 | 남음 | **⑤** |
> | ⑦ 레포 밖 상태 루트 회귀 | 남음(`test_g6_coord_layer`는 레포 사본 안 — 모자라다) | **⑥** |
>
> 착수 순서는 구현 세션이 제안한 ①②⑤⑥⑧⑦과 같다(= 이 요청문 ①→⑥). hub_50은 **반입하지 않는다**.
> **원칙**: 판정·저장 형식·그래프 동작 변경 0. 화면 경로 표기는 한 함수. 파서는 core를 import하지 않는다(자리는 인자·주입으로만). 표 밖에서 같은 부류를 찾으면 **같은 회차에서 같이** 고치고 보고에 적는다.

## 전제 대조표 (허브 실행 확인 · 2026-09-23 · origin `9f72a17`)

| # | Claim | Command | Expect |
|---|---|---|---|
| 1 | 화면 경로를 코드 폴더 기준으로 계산 | `grep -rn 'relative_to(ROOT)' --include=*.py cli core kit doctor.py run.py \| wc -l` | 21(18줄 보호 없음 → 레포 밖 루트에서 예외 · 3줄 각자 try) |
| 2 | 보호한 3줄 + 1은 각자 짠 같은 로직 | `grep -n 'relative_to(ROOT)' cli/export.py cli/golden.py cli/ingest.py` · `grep -n 'for base in (paths.registry(), ROOT):' cli/register/draft.py` | 50 · 105 · 198 · 183 |
| 3 | 표시 함수는 이미 있다 | `grep -n '^def rel_to_home' core/paths.py` | 133 |
| 4 | 파서의 「주입 전 기본값」이 B78 이전 레포 자리 | `grep -n 'SNAPSHOT = ROOT' parser/tagger.py` · `grep -n 'KEEP_DIR = ROOT' parser/struct_map.py` | 23 · 28 |
| 5 | 킷 관문은 닫힌 목록 파일·보존 자리를 받지 않는다 | `grep -n 'res = parser_pipeline.parse(mod, doc_id, doc,' kit/run_adapter.py` · `grep -n '"--layers", str(paths.layers())' cli/register/gate.py` | 438 · 43(층·좌표 층 이름만 건넨다) |
| 6 | 낡은 자리 `data/ingest_log` | `grep -n "ROOT / 'data' / 'ingest_log'" cli/show.py` · `grep -n 'data/ingest_log' cli/show.py core/build/ledger.py` | 384 · 258·382 · 4 |
| 7 | 선택 의존 부재가 날것의 ImportError | `grep -n 'from openpyxl import load_workbook' parser/reader.py` · `grep -n '^from openpyxl' kit/run_adapter.py` | 77(함수 안) · 35(**모듈 최상단**) |
| 8 | openpyxl 경고를 잡지 않는다 | `grep -c 'catch_warnings' parser/reader.py` | 0 |
| 9 | 파서는 core를 import하지 않는다 | `grep -rn '^from core\|^import core' parser \| wc -l` | 0 |
| 10 | 등록 경로에 시트 역할이 없다 | `grep -rn 'sheet_roles' cli/register kit \| wc -l` · `grep -n 'out.append(pipeline.parse(' cli/register/view.py` · `grep -n '^def _sheet_gate' cli/ingest.py` | 0 · 501(검수 리허설 — 시트 전부) · 395(인입 관문 — 등록은 부르지 않는다) |

---

## ① 화면 경로 표기는 한 함수 — 레포 밖 루트에서 죽지 않는다 (칸 0.3 · 1.x · 0.1) — **먼저**

- `core/paths.py`에 **`show(p)`** 하나: 상태 루트 아래면 루트 기준 상대(`registry/review/rfq/…`) · 레포 아래면 레포 기준 상대 · 둘 다 아니면 절대. 예외를 내지 않는다. (`rel_to_home`은 **기록**용이라 레포 기준 갈래가 없다 — 표시와 기록을 섞지 않는다.)
- 21줄 전부(B85 뒤에도 21 — 전제 1) `paths.show()`로. 각자 짠 셋(`export` 출력 표기 · `golden._rel` · `ingest` 선택 근거 줄)도 지운다 — 같은 기능 한 자리(B77 ④). `draft._rel`은 **상태에 싣는 기록**이라 그대로 두되 기준 순서가 `paths.show`와 같은지 확인만.
- `core/state/registry.py:67`(내장 스키마 경로 기록)은 `ONTO_FIXTURES`가 레포 밖이면 같은 예외 — 절대 경로로 떨어지게.

**완료판정 ①**: 어서션(성질) — `grep -rn 'relative_to(ROOT)' cli core kit doctor.py run.py` == 0(회귀가 잰다) · `paths.show`는 세 갈래 각각에서 예외 0 · 화면 경로가 상태 루트 아래면 상대 표기.

---

## ② 킷 관문이 상태를 레포 옛 자리에서 찾는다 — 파서 주입 전 기본값 제거 (칸 1.5 · 2.8)

- 킷(`kit/run_adapter.py`)은 subprocess라(B85가 좌표 층 **이름**은 `--coord-layer`로 건네게 했다 — 그러나 닫힌 목록 **파일**과 보존 자리는 아직 안 건넨다) `paths.bind_parser()`가 안 걸린다 → 관문의 파서 전 구간 단계가 **`<코드>/data/skeleton_closed_list.json`을 읽고**(사내엔 없다 → 닫힌 목록이 빈 채로 좌표 대조) **`<코드>/extract/struct_maps/`에 쓴다**(코드 폴더에 쓰기 — 공용 서버에서 코드 폴더가 읽기 전용이면 죽는다).
- 등록 CLI(`cli/register/gate.py`)가 `--layers`처럼 **`--closed-list <data/skeleton_closed_list.json>`**을 건넨다(D-160 ① 결 — 킷은 자리를 짓지 않고 받는다). 킷은 구조 지도 보존을 **임시 폴더**로(`struct_map.use_dir` — 관문 산출은 관문이 치운다).
- 파서의 `SNAPSHOT`·`KEEP_DIR` 기본값을 **없앤다**(None) — 주입 없이 쓰이면 조용히 레포를 보지 말고 문면 있는 실패. 킷 단독 실행(레포에서 사람이)에서 `--closed-list`가 없으면 「골격 닫힌 목록 없음 — 좌표 대조 생략」 한 줄.

**완료판정 ②**: 어서션(성질) — 관문 실행 뒤 코드 폴더에 새 파일·폴더 0(`extract/` 없음) · 관문 파서 전 구간이 받은 닫힌 목록 == 상태 루트 스냅샷 · 파서 패키지에 `ROOT / "data"`·`ROOT / "extract"` 0.

---

## ③ 낡은 자리 문자열 — `data/ingest_log` (칸 3.1 · 0.3)

- `cli/show.py:382~384`: 대장 없음 거부 문면이 `data/ingest_log/{doc}.json`을 말하고 **대장 수를 레포 `data/ingest_log`에서 센다**(늘 0 — B78에 `work/ingest_log/`로 옮겼다). `paths.work("ingest_log")`로 · 문면은 `paths.show`로. `show.py:258`·`core/build/ledger.py:4` 문서 문자열도 새 자리로.
- `docs/회귀스위트/점검_경로.py`가 이것을 못 잡은 이유: `'data' / 'ingest_log'`처럼 **토큰이 갈린 형태**를 안 본다. 옮긴 이름 34종에 대해 `ROOT / '<옛 단>' / '<이름>'` 형태도 잡게 보강.

**완료판정 ③**: 어서션(성질) — 대장 없음 문면의 경로 == 대장의 실제 자리 · 점검_경로에 `ROOT / 'data' / 'ingest_log'` 표본을 넣으면 붉다(변이 시험).

---

## ④ 선택 의존 부재 · 읽기 경고를 문면으로 (칸 2.1 · 0.3)

- `parser/reader.py`의 포맷별 지연 import(openpyxl · python-pptx · PyMuPDF)를 한 자리 `_need(module, pip_name, ext)`로 — 없으면 파서가 정의한 `MissingDependency`(ImportError 하위)를 「`.xlsx`를 읽으려면 `openpyxl`이 필요하다 — `pip install openpyxl`」로. CLI 진입점이 잡아 **상태 거부**로 낸다(`[결함]`·traceback 아님). `ingest-dir`는 그 문서만 실패하고 끝 요약에 한 번 모아서.
- `kit/run_adapter.py:35` 모듈 최상단 `from openpyxl.utils import …`를 함수 안으로(또는 같은 문면으로 감싸기) — 지금은 openpyxl이 없으면 관문이 import 단계에서 죽고 등록 화면엔 「관문 실패」만 남는다.
- openpyxl 읽기 경고(`WMF image format is not supported …` 등)를 `warnings.catch_warnings`로 잡아 **보고에 남기고 화면엔 한 줄**: `그림 n개를 읽지 못해 건너뜀(WMF k)` — 그 그림이 이미지 요약에서 빠진다는 사실이 사람이 알 정보다. 날것의 경고 문면은 로그 파일로.

**완료판정 ④**: 어서션(성질) — openpyxl을 가린 환경(`sys.modules` 차단 스텁)에서 xlsx 인입이 상태 거부이고 문면에 `pip install openpyxl` · 킷 import 단계 예외 0 · WMF 그림이 든 표본에서 화면에 건너뜀 한 줄 · stderr에 openpyxl 경고 0.

---

## ⑤ 등록 표본에도 시트 관문 — B83 누락 (칸 1.2 · 1.6 · 3.1)

- **사내 실측(2026-09-23)**: 시트 수십 장짜리 산문 엑셀을 `register generate`로 등록하면 검수 리허설(`cli/register/view.py:501`)과 킷 관문(파서 전 구간 단계)이 **시트 전부**를 돈다. 추출 리허설을 켜면(`--extract`) 가격·일정 시트까지 추출 LLM을 타고 **그 시점에 표본이 `data/`에 인입된다**. B83이 관문을 `ingest-file`·`parse run`에만 붙였다.
- 원칙: 등록은 「이 종류를 어떻게 읽나」라 시트를 정하는 자리가 아니다(그대로) — 다만 **표본도 문서 하나**이므로 인입과 **같은 관문**을 표본마다 건다. 관문 함수는 하나(`cli/ingest._sheet_gate` — 호출자 셋: `ingest-file` · `parse run` · `register generate`/`review`). 답은 같은 자리(`registry/sheet_roles/<doc_id>.json` — 리허설은 운영 doc_id를 쓴다 · B55 ⑤)에 남고, **같은 파일을 나중에 인입하면 다시 묻지 않는다.**
- `register generate … --sheets "<문법>"`도 받는다(표본이 하나일 때만 — 여럿이면 표본마다 관문). 비대화형이면 인입과 같이 상태 거부.
- 킷 관문에는 역할을 **플래그로** 건넨다(`--sheet-roles <기록 파일>` — `--layers`·`--coord-layer`·②의 `--closed-list`와 같은 결). 킷은 그 역할로 `pipeline.parse(sheet_roles=…)`.

**완료판정 ⑤**: 어서션(성질) — 시트 7장 표본(RFQ01)으로 등록하면 관문이 한 번 뜨고 기록이 생긴다 · 검수 리허설·추출 리허설·킷 관문 조각에 `skip` 시트 프레임 0 · 같은 파일 `ingest-file`은 관문 0 · 비tty 등록은 상태 거부(문면에 시트 이름 전부) · 시트 하나짜리 표본은 관문 없음(지금과 같다).

---

## ⑥ 「사내 모양」 회귀 루트 — 두 차이를 회귀에 넣는다 (칸 0.1 · 0.3)

- **B85가 한 것과의 차이**: B85 ③(`test_g6_coord_layer`)은 **레포 사본 안 `state_mock`**에서 층 이름만 바꿔 `doctor --quick`까지 돌렸다(D-166 ⑦⑧ — 확정). 사내와 다른 두 번째 차이(**상태 루트가 코드 밖**)와 **등록 경로**는 아직 회귀가 밟지 않는다 — 이것이 이 항목의 몫이다.
- 회귀에 **사내 모양 루트** 하나: 레포 **밖** 임시 폴더를 상태 루트로(`paths.reset()` 훅) · mock seed를 `layers/equipment/`로 복사(config `layer` 키만 바꿈 · 품질층 같이) · `USE_MOCK=1` 그대로(`USE_MOCK=1`은 `ONTO_HOME`을 무시한다 — D-150 · 시험 훅 `paths.reset()` + 자리 주입으로 레포 밖을 가리킨다. 훅이 없으면 이 회차에서 시험 전용 훅 하나를 연다 — 운영 경로는 그대로).
- 그 루트에서 **전 구간**: `init --fresh` → `bootstrap` → `register generate`(산문 · 표 각 1) → `status` → `confirm` → `skeleton-status/confirm` → `ingest-file` 표본(CP01 · TOC01 · RFQ01) → `query` 12문항 → `export html` → `golden init` → 뷰어 `/api/graph`·`/api/doc` → `doctor --quick`(D-166 ⑧). **예외 0 · 코드 폴더에 새 파일 0 · canonical·엣지(rel, src canonical, dst canonical)·큐 kind별 수가 기본 루트와 같다.**
- 이것이 이번 회차의 **잠금**이다 — ①~⑤는 개별 수정이고, 이 시험이 없으면 다음 회차의 새 화면 줄이 같은 병을 다시 만든다.

**완료판정 ⑥**: 위 한 줄이 그대로 어서션(성질) · 기본 루트와 사내 모양 루트 대조 수치를 보고에.

---

## 순서 · 마감

순서 **① → ② → ③ → ④ → ⑤ → ⑥**(①이 사내를 지금 막고 있다 · ⑥ 사내 모양 루트는 ⑤의 등록 경로까지 포함해 마지막에 잠근다). 회귀 1,517 위에서 순증만 · 클린 2회 동일 · 네 벌 diff 0 · 화면 diff는 경로 표기(상대화) · 건너뜀 한 줄 · 등록 시트 관문 · 검사 5종(점검_경로 보강 포함) · §7 상한 · 코드 지도 재생성 · 자산 해시 갱신(킷 변경).

**가이드·구조도는 이번 회차가 같이 고친다**(사용자 확정 2026-09-23 — 등록·인입 구조가 바뀌는 회차라 화면 문면을 아는 쪽이 같은 커밋에서): `docs/가이드/2B_작업가이드.md` §0(새 코드 폴더 = 새 파이썬 환경이면 선택 의존 다시 — `pip install openpyxl python-pptx pymupdf` · 상태 폴더는 코드 밖이 기본)·§4(등록에도 시트 관문 · `--sheets`)·§7(증상 — 선택 의존 부재 문면 · WMF 건너뜀 줄) · `걸어가기_설비문서.md` §2(등록 때 시트 관문) · `상태_폴더_가이드.md` §5 · 구조도 `01_등록_흐름`(시트 관문 자리) · `02_파싱과_추출`(킷 `--closed-list`·`--sheet-roles`) · `06_손잡이_대장`(0.3 `paths.show` · 2.1 선택 의존) · `00_칸_대장` Code 열. 정제본(문서 6 §6.7 킷 손잡이 · 문서 7 §7.1 화면 경로 표기·§7.5 사내 모양 회귀 루트·§7.6-B-6 선택 의존 문면)은 보고를 받아 허브가 고친다.

**마감 보고**: 전제 대조표 10행 실행 결과 · ①의 grep 결과 전문 · ⑤ 등록 관문 화면 전문 · ⑥ 기본 루트와 사내 모양 루트 대조 수치 · 선택 의존 가림 시험의 거부 문면 전문 · 고친 가이드·구조도 목록 · 표 밖에서 찾아 같이 고친 자리 · 어서션 증감 · 회귀 수 · 가결정.
