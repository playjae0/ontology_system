# B51-2 요청문 — B51 마감 후속 (문면 2 · 경로 1 · 가이드 재작성)

> **수신**: 구현 세션 · **발신**: 허브 (2026-09-08) · **전제**: `handover_b51_2.zip` 배치 (c739258 위)
> B51 보고의 「명세 개정 필요 2건」 판정 결과다. **코드는 한 줄**이고 나머지는 문서 배치다.

## 판정 결과 (개정대장 「B51 회차 마감」)

| Item | Verdict |
|---|---|
| 1. 리허설 doc_id | 구현이 맞다 — 운영 doc_id. 허브 요청문 오류. §6.6에 명시 |
| 2. 검수가 진실을 쓴다 | 보고보다 크다 — `ingest()`가 `register_doc()`도 부르므로 **등록부도 쓴다**. 그래도 **코드 0줄**, 문면으로 닫는다(사용자 판정: 케이스를 늘리지 않는다) |
| 3. (허브 대조) `구조_추출.json` | 「명세 디렉터리에서 뺐다」와 달리 `docs/spec/`에 쓰고 추적됐다 — 아래 ① |

## ① `추출_구조.py` 출력 경로 — 한 줄

`docs/회귀스위트/추출_구조.py:240` `f"{SPEC}/구조_추출.json"` → `f"{OUT}/구조_추출.json"`. 그리고 `git mv docs/spec/구조_추출.json docs/구조도/`. `docs/spec/`에는 명세 11종만 남는다.

**완료판정**: `ls docs/spec` = 11종 · `python docs/회귀스위트/추출_구조.py` 뒤 `git status`가 `docs/spec/` 변화 0.

## ② 문서 배치 (zip 그대로)

| File | What |
|---|---|
| `docs/spec/6_파서와구축모드.md` | §6.6 3구획 문단에 2문장(B51-2) — 미러쌍 봉인 무변화(§6.6 문단은 봉인 대상 아님) |
| `docs/spec/개정대장.md` | 「B51 회차 마감」 + [개정] B51-2 + 가이드 재작성 기록. 누적 [개정] 94 |
| `docs/가이드/2B_작업가이드.md` | **전면 재작성** — 상황→절 표 · §0~§8 같은 모양 · 이력 제거 · §7 증상표 |
| `docs/구조도/00_전체_지도.md` | 갱신 대장에 B51 행 |
| `docs/구조도/01_등록_흐름.md` | review에 `_extract_rehearsal` 노드·표 행 3′ |
| `docs/구조도/02_파싱과_추출.md` | `parse run` 라벨 — doc_id 파일명 파생 |

**완료판정**: 검사 4종 통과(문면 611 · 문서간 0 · 미러 4쌍 0 · 자산 13) · 회귀 802/802 유지.

## 보고 양식
①의 `git status` 화면 · 검사 4종 한 줄 · 명세 개정 필요 목록(있으면 — 고치지 말고).
