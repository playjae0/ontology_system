# 06 · 손잡이 대장 — 조절할 수 있는 것은 전부 여기 있다

> **무엇인가**: 사람이 돌릴 수 있는 것의 **전체 목록**이다. 증상이 없어도 「이 시스템에서 무엇을 어디서 바꿀 수 있나」를 이 한 장으로 본다. 고장이 났을 때 증상으로 찾는 것은 `05_조절_지도` · 가이드 §7이고, 여기는 **재고 목록**이다.
> **칸 번호**는 `00_칸_대장.md`. 지시문 9개 행은 회귀가 잠근다(파일 집합 == 대장) — 나머지 행은 사람이 유지한다: 손잡이를 더하거나 옮기면 **그 회차에 이 표를 고친다**(보고 양식의 「낡은 절 목록」 대상).
> **종류가 곧 「누가 바꾸나」다** — **지시문·config·자산**은 사내 운영자가 파일을 고쳐 바꾼다(코드 무관). **코드 상수**는 사내에서 손대지 않는다 — 값이 명세·가결정에 매여 있어 바꾸려면 회차(허브 요청문)로 간다. **실행 설정**은 환경마다 다르다.
> **최종 갱신**: 2026-09-18 (B78 마감 — 상태 루트 `ONTO_HOME` · 파트 폴더 · 관문 표 자리 넷 · 1362/1362)

## 읽는 법

| Column | Meaning |
|---|---|
| Cell | 어느 칸의 판단을 바꾸나 |
| Kind | 지시문 · config · 자산 · 상수 · 설정 · 사람 입력 |
| Where | 실물 파일 경로 또는 `파일::이름` |
| Changes | 무엇이 달라지나 — 한 줄 |
| Effective | 언제부터 — 다음 등록 · 다음 인입 · 즉시 · 회차 |
| Check | 바꾼 뒤 무엇으로 확인하나 |

---

## 파트 1 · 등록 (읽는 방법을 만든다)

| Cell | Kind | Where | Changes | Effective | Check |
|---|---|---|---|---|---|
| 1.1 | 사람 입력 | `generate … --hint "…"` | 이 doc_type만의 관찰 힌트 — 생성 프롬프트 `[확정 사항]`에 그대로 한 항목 | 그 등록 1회 | `ONTO_DUMP_PROMPT=1` → `review/<dt>/prompt_rendered.md` |
| 1.1 | 사람 입력 | `generate … --interview` | 생성 전 문답을 연다 (한 라운드 질문 하나 · 「진행」으로 끝) | 그 등록 1회 | `input_package.json` `hint.interview[].decisions` |
| 1.1 | 사람 입력 | `generate … --use-basic` / `--no-basic` · 화면의 `[table/prose]` 답 | 산문 포맷의 고정 어댑터(기본) ↔ LLM 생성. 격자 포맷의 형태 판정이 안 서면 이 답이 판정이다(B65 ⑤) | 그 등록 1회 | 화면 첫 줄 · `state.json::form.by = human` |
| 1.2 | 상수 | `parser/reader.py::OBSERVE_ROWS` (20) | 표본에서 LLM에 보이는 앞 N행 | 회차 | — |
| 1.2 | 상수 | `parser/profile.py::SAMPLE_VALUES`(4) · `SAMPLE_CHARS`(30) · `FULL_LIST_MAX`(10, D-105) · `SPARSE_EMPTY_RATIO`(0.7) | 열 프로파일의 대표값 수·길이 · 전목록 상한 · 「거의 비었다」 판정 | 회차 | `input_package.json` `system.reader_head[].열_프로파일` |
| 1.3 | 지시문 | `prompts/1.3_interview.md` | 문답이 무엇을 어떻게 묻나 · 요약 규칙 | 다음 문답부터 | `interview_log.json` |
| 1.3 | 상수 | `cli/interview.py::INTERVIEW_STOP` | 문답을 끝내는 말(「진행」 등) | 회차 | — |
| 1.3 | 사람 입력 | `review <dt> --instruct "<topic>: <결정>"` | 확정 사항 갱신(topic 있으면 교체·없으면 추가, LLM 0) + 재생성 1회 | 즉시 | `decisions[]` |
| 1.4 | 지시문 | `prompts/1.4_generate.md` (`version: 1.7` — B76 ④: 규약 10 단서 `ditto_mark`는 있을 때만 · 규약 11 셀은 헬퍼로만 · B72: 모든 산출 키는 fields에 role · meta 딕셔너리 금지) | 어댑터·스키마 생성 규칙 전부 — role 어휘·UNMAPPABLE·출력 형식·재생성 지시 자리 | 다음 생성부터 | `prompt_rendered.md` · 판은 하나 (옛 판 `git show 9f58ec9:kit/…`) |
| 1.4 | 자산 | 어댑터 `expects.columns` — 값 **세 꼴**: 열문자 `"D"` · 헤더 라벨 `"Center"`(권장 — 시스템이 열문자로 확정) · 리스트 `["Center","G"]`(합치기 — 첫 비지 않은 값) | 어느 열이 어느 필드인가 — 세는 일은 시스템(C38 · B64) | 다음 관문부터(관문 입구 확정) | 화면 `헤더 문자열은 시스템이 채운다` 줄 · G26 · `test_p3` ⑤ C38 잠금(저장본 == 표본 재계산값) |
| 1.4 | 자산 | `kit/어댑터_스켈레톤.py` | LLM이 채우는 어댑터 뼈대 — 빈칸의 모양 | 다음 생성부터 | 생성된 `adapter.py` |
| 1.4 | 자산 | `kit/참조어댑터/{cp,ipqc,toc_report}.{py,json}` | few-shot — 「이렇게 생긴 것을 내라」 | 다음 생성부터 | `{{참조_어댑터}}` 치환 |
| 1.4 | 자산 | `kit/표적출력_정의.md` | 계약 ①(3층 구조) 주입문 | 다음 생성부터 | — |
| 1.4 | config | `layers/<층>/config.json::categories`(정의문) · `relations` · `relation_patterns` | 생성 프롬프트의 층 어휘·관계 패턴표(`{{카테고리_정의문}}`·`{{관계_패턴_표}}`) | 다음 등록부터 | `prompt_rendered.md` |
| 1.4 | 자산 | `schemas/blocks.json` | 공용 블록 — 모든 스키마가 상속하는 필드 | 다음 등록부터 | `{{공용_블록_목록}}` |
| 1.5 | — | `cli/register/gate.py::stamp_system_fields` (관문 입구 — `header_labels` · `adapter_version` · `columns` 열문자) | **손잡이가 아니다** — 켜고 끄는 자리가 없는 것이 C38의 요점. 시스템 필드가 늘면 여기와 `test_p3` ⑤ 표에 행을 더한다 | — | `test_p3` ⑤ · 변이(끄면 붉어짐) |
| 1.5 | 산출·정본 | `review/<dt>/columns.json` — **열 판정 대장**(B67 ② · 손잡이가 아니라 산출이지만 대장 밖 자리를 두지 않는다) | 열마다 role·필드·출처(`by`)·상태 — 재생성·`--resume`의 입력. 사람이 바꾸는 길은 `review --instruct "D열은 attribute"`(열문자·라벨을 집는 지시) — 파일을 손으로 고치지 않는다(관문이 다시 뽑는다) | 다음 관문 | 관문 화면 「열 판정 대장 — N열 (k건 미해결)」 · 뷰 갈린 열 |
| 1.5 | 규칙 | 전시물(`kit/참조어댑터/`)과 표본(`tests/fixtures/raw/`)은 짝이다(B77 ②) — 전시물 쌍 셋(cp·ipqc·toc_report)이 제 표본으로 관문 전 구간을 실행으로 지나는 상시 어서션 | 전시물에만 열을 더하면 관문이 붉는다 · 표본을 고치면 같은 헤더 계열 전부(cp 넷) | 즉시 | 회귀 |
| 1.5 | 상수 | `kit/gate_tables.py::SHAPE_SCHEMA` · `SHAPE_ADAPTER`(B76 ① · B78-2c부터 **닫힌 표는 `gate_tables.py`, ④정합 검사는 `gate_checks.py`, 판정 줄·화면은 `gate_screen.py`, 실행기 ①②③⑤는 `run_adapter.py`**) | LLM 산출의 키마다 허용 형(닫힌 표 — 관문 G4F가 잰다). 새 키가 필요하면 표에 형을 적는다 — 표에 없는 키는 `[모양]` 보고 줄 | 다음 생성부터 | G4F 문면 「키 · 받은 형 · 허용 형」 |
| 1.5 | 플래그 | `kit/run_adapter.py --ledger <columns.json>`(B76 ②) · `--layers <자리>`(B79 ① — 등록 CLI가 상태 루트의 층 자리를 건넨다 · 단독 실행은 레포 `layers/`) | 대장 커버리지 G4G의 입력 — 등록 흐름은 자동으로 넘기고, 킷 단독 실행은 생략 가능 | 즉시 | G4G 문면 |
| 1.5 | 상수 | `cli/register/gate.py::AUTO_FIX` · `WITH_EVIDENCE` · `GATE_SELF` | 관문 실패 처분표 — 어느 라벨이 자동 재생성 / 문답+원문 / 관문 결함 | 회차 | `[FAIL]` 줄 뒤 「→ 재생성 지시 (자동)」 |
| 1.5 | 상수 | `kit/run_adapter.py` 판정 항목 G01~G54 (+G1B · G4C~G4E — B65 · G39 산출 키 ⊆ 스키마 fields — B72) | 관문이 무엇을 검사하나. FAIL 줄에는 관련 열의 프로파일이 붙는다(B64 ②). **어휘 대조 3종의 정본은 손잡이다** — G4C는 `config.categories`, G4D는 `config.relations`, G4E는 `config.relation_patterns`(3.4·3.6 행): 새 카테고리·관계가 필요하면 관문이 아니라 config를 고친다 | 회차 | `state.json::harness_out` |
| 1.6 | 자산 | `kit/검수뷰_데이터스키마.json` · `kit/render_review.py` | 뷰 3구획의 데이터 계약·HTML | 다음 review부터 | `review/<dt>/view.html` |
| 1.6 | 상수 | `cli/register/view.py::REHEARSAL_ROWS`(200) · `cli/register/__init__.py::EXCERPT`(3) | 리허설 파싱 행 수 · 정상 조각 발췌 수 | 회차 (`--rows all`로 1회 우회) | 뷰 |
| 1.6 | 사람 입력 | `review <dt> --llm-coord` · `--extract` | 뷰에 좌표 보조·추출 리허설을 켠다(LLM 비용 동의) | 그 review 1회 | 뷰 |
| 1.7 | 자산 | `parser/adapters/<dt>.py` · `schemas/<dt>.json` | 확정된 어댑터·스키마 — **손으로 고치지 않는다**: `generate --revise`(1.8) | — | 등록부 |

## 파트 2 · 파싱 (문서를 메타데이터로)

| Cell | Kind | Where | Changes | Effective | Check |
|---|---|---|---|---|---|
| 2.1 | 상수 | `parser/reader.py::ENCODINGS` · `DELIMS` · `SUPPORTED` · `PROSE_EXT` · `GRID_EXT` | CSV 인코딩 후보·구분자 후보 · 받는 확장자 · 산문/격자 갈래 | 회차 (사내 인코딩 추가는 D-기록 후 허용 — 문서 6 §7.4-5) | 로그 「CSV … 인코딩 · 구분자」 |
| 2.2 | 자산 | 어댑터 `ADAPTER.expects` — `sheet` · `header_row` · `columns` · prose 지문/분할 상수 | 양식 표류 감지의 지문 · 열 배치 (`header_labels`·`adapter_version`은 시스템이 쓴다 — 손대지 마라) | 그 doc_type 다음 인입부터 | `adapter_mismatch` 큐 detail |
| 2.3 | 계약 | `parser/normalizer.py::resolve_ditto(records, fields=None, marks=…)`(B76 ④) | `marks=None`·빈 집합은 「상동 없음」 — 치환 0으로 돈다. 공용 코어는 호출자의 `None`에 죽지 않는다 | 즉시 | 스켈레톤 최소 어댑터 회귀 |
| 2.3 | 상수 | `parser/form.py::THRESHOLDS` · `AUTO_MIN_FOR`(2) · `AUTO_MAX_AGAINST`(0) · `MIN_VALUES_FOR_UNIQUE`(4) | table/prose 판정 신호 5개의 문턱 · 자동 판정 조건(찬성≥2·반대0) | 회차 (C37) | 등록 화면 「형태 판정」(자동이어도 찍힌다) · 안 서면 사람에게 묻는다 · `summary.form` |
| 2.4 | 지시문 | `prompts/2.4_struct_map.md` | 산문에서 무엇을 제목으로 보나 | 다음 인입부터 | `<상태>/work/struct_maps/<doc_id>.json` |
| 2.4 | 상수 | `core/llm/struct_map_pass.py::MAP_LINE_WIDTHS`(80·40·20) | 구조 지도에 보내는 줄 폭 감축 순서(B41) | 회차 | 로그 |
| 2.5 | 상수 | `parser/struct_map.py::CHUNK_MIN, CHUNK_MAX`(5·40) | 분할 레벨 선택 구간 — avg가 이 안에서 중앙에 가장 가까운 레벨([정정] 46) | 회차 | `hierarchy_unresolved` payload `case` · 화면 「분할 — <프레임> … 짧음 n · 긺 m」(B68) |
| 2.5 | 상수 | `parser/adapters/basic_prose_xlsx.py::SIG_NUM · SIG_MERGE · SIG_BOLD`(「번호」·「가로병합」·「굵게+들여쓰기」) · `MAP_SOURCE`(`adapter:basic_prose_xlsx`) · `expects.heading_pattern` | 고정 산문 xlsx 어댑터의 헤딩 신호 이름과 번호 패턴 — 문면이 화면·뷰의 「분할 기준」에 그대로 나간다(B68). 판정 순서(번호 → 가로병합 → 굵게+들여쓰기)는 코드 | 회차 | generate/status 분할 줄 · 뷰 레벨 선택 표 「분할 기준」 열 · `level_report()[].분할_기준` |
| 2.6 | 자산 | `parser/adapters/basic_ppt.py` · `basic_pdf.py` · `basic_prose_xlsx.py` | 산문 고정 어댑터의 분할 신호 상수 | 다음 인입부터 | 청크 수·크기 |
| 2.8 | 지시문 | `prompts/2.8_coord_tag.md` · `prompts/2.8_image_summary.md` | 좌표를 닫힌 목록에서 고르는 규칙 · 이미지 요약의 문면·길이 | 다음 인입부터 | 청크 `anchor` · 이미지 요약 텍스트 |
| 2.8 | 설정 | `--coord-llm off\|<종수>` — 정본 `cli/parse.py::COORD_CAP`(100) | 인입·`parse run`의 좌표 태깅 LLM 상한 — **단위는 목록 밖 표기 종수**(행 아님 · B69). 초과분은 묻지 않고 목록 밖 그대로 `orphan_anchor` · `off`는 정확 일치만 | 즉시 | 예고 줄 「좌표 태깅 — … → LLM 최대 n회 (상한 N)」 · 초과 줄 · `report["coord_tag"]` |
| 2.8 | 자산 | `layers/<층>/skeleton.json` seed — `ALIASES` · `AXIS_LABELS` | 좌표 표기 변형 · 극성 축 라벨 | `bootstrap` 후 | `run.py bootstrap` |
| 2.9 | — | `parser/validator.py` | 계약 검사 — 손잡이 없음(계약은 문서 2) | — | `parse_failure` 큐 |

## 파트 3 · 구축 (지식으로)

| Cell | Kind | Where | Changes | Effective | Check |
|---|---|---|---|---|---|
| 3.1 | 설정 | `ingest-file --step` (B72 ④) | 7단계(선택·파싱·좌표·판정 예고·판정·부착/엣지·큐/요약)마다 멈춰 설명과 수를 보고 `[c/q]` — `q`면 그래프 쓰기 0. 비대화형·`ingest-dir`는 무시 | 즉시 | 단계 화면 · 요약 줄 |
| 3.1 | 설정 | `ingest-file --step-every N` (B75 ③) | 판정 단계 안에서 값 N개마다 `[판정] 값 i/N · 호출 k · 누적 토큰 t · 남은 값 m → [c/q]` — `q`면 그 문서 `held` · 그래프·사전·큐 되돌림 · 체크포인트는 남는다. `--step`과 독립 | 즉시 | 진행 줄(보폭마다 새 줄 · 로그에도) · 실패해도 `이 문서까지 — LLM 호출 k · 토큰 t · 멈춘 단계` |
| 3.1 | 설정 | `ingest-file --progress-every N` · 설정 키 `PROGRESS_EVERY`(기본 25 · B81 ④) | 판정 진행 줄 보폭(값 N개마다 — `total//10`을 대체) · 진행 줄에 대장 집계(사전·NEW·불확실·큐) 동반. 멈추지 않는다(멈춤은 `--step-every`) | 즉시 | 진행 줄 |
| 3.1 | 화면 | 값 줄 — `ledger.ON_ROW` 콜백(B81 ① · `cli/ingest_screen.py`) · `-v` | 값마다 한 줄 `기호 이름 verdict 경로/후보 → 큐` — 기본은 판단이 갈린 값(LLM 경로 ∪ new·uncertain·orphan·lowres·gate_reject) · `-v`면 전부. 화면은 대장의 투영(새 계산 0) | 즉시 | 값 줄 · `show report`와 같은 행 |
| 3.1 | — | `core/build/gate.py` · `core/build/ingest.py` | 중복·id·필드 검사 — 손잡이 없음 | — | 큐 |
| 3.2 | 자산 | `schemas/<dt>.json` `fields` · `edges` · `unmappable` | 표 레코드에서 무엇이 노드·속성·엣지가 되나 (LLM 0) | 그 doc_type 다음 인입 | `show extract` |
| 3.3 | 지시문 | `prompts/3.3_extract.md` + config `prompts.extract`(덧붙임) | 산문 청크에서 무엇을 뽑나 · 빈 산출 규칙 | 재추출(`cli.extract --force`)부터 | `extract/<doc_id>.json` |
| 3.3 | config | `layers/<층>/config.json::extract_patterns` (mock) · `prompts.extract_enabled` | mock 문형 규칙 · 층별 추출 on/off | 다음 인입 | — |
| 3.4 | 지시문 | `prompts/3.4_judge.md` + config `prompts.judge`(층 어휘 덧붙임) | 「같은 것인가」 판정 기준 | 다음 인입부터 | `gate_rejects.json` · 사전 |
| 3.4 | 상수 | `core/matcher.py::CANDIDATE_TOP_N`(기본 12 · B73 ①) · `scope_filter`(B75 ②) | 판정 LLM에 보내는 후보 상한 — 사전 히트는 상한 무관 · 스코프 카테고리는 **`_narrow` 전에** 같은 부모 아래로 자른다(상한 이하여도 · 0개면 후보 0 → NEW · LLM 0) · 그 안에서 유사도 상위 N(방법은 `CANDIDATE_NARROW`). 노드가 늘어도 판정 입력이 늘지 않는다(CP01 첫 인입 호출 34 → 18 · 후보 평균 4.2 → 1.1) | 다음 인입부터 | 요약 줄 `판정 — 호출 k · 사전 h · 스코프로 끝 s(판정 없이 끝난 수) · 좁힘 — 임베딩 e · 겹침 o · 후보 평균 n.n(상한 12)` |
| 3.1 | 상수 | `core/build/retry.py::ATTEMPT_MAX`(기본 5 · B73 ②) | orphan 재시도 상한 — 넘으면 「사람 판정 대기」. 재시도는 후보 집합 지문이 바뀐 때만 돈다 | 다음 인입부터 | `platform queue` 항목의 `N회 재시도 · 첫 발생` |
| 0.3 | 설정 | `ONTO_TRACEBACK=1`(B76 ③) | 등록·인입의 미포착 예외를 한 줄 `[결함] 파일:줄`이 아니라 전체 traceback으로도 화면에(개발용). 전문은 항상 `defects.log` | 즉시 | `show log defects` |
| 0.3 | 연산 | `run.py ops confirm <층> <node_id|canonical> --actor`(B73 ④) | auto 노드를 사람이 확정 — status confirmed + 큐 종결(`resolution`). `merge`·`obsolete`가 나머지 두 답 | 즉시 | `platform queue auto_node` 계수 감소 · ops_log |
| 0.3 | 연산 | `run.py ops alias <층> <node_id|canonical> <표기> --actor`(B74 ⑤) | 사람이 표기를 노드에 잇는다 — 원 표기 + 조회 키 등재(provenance `op:alias:<actor>`). 거부: 툼스톤 · 같은 부모 아래 다른 live 노드의 표기(→ `ops merge`) · 행위자 없음 | 다음 인입부터 exact | `show node` 별칭 · ops_log |
| 3.5 | 장부 | `<상태>/work/ingest_log/<doc_id>.json`(B74 ② · ④단 — B78) · `run.py show report <doc_id> [--json]` | 행별 결과표 — 값마다 경로(사전/스코프/임베딩/겹침→판정)·판정·후보 수·토큰. 정합성 눈 검수의 단위 | 재인입이 덮는다 | 머리 집계 |
| 3.5 | 화면 | `run.py viewer` / `export html`(B74 ③) | 엣지 라벨·화살촉·호버(rel·출처·status) · 노드 툴팁(status·made_by·별칭·값·큐) · 집계 패널(status·생성 경로·rel·큐) · 문서 필터. 쓰기 없음 · CDN 0 | 즉시 | — |
| 3.4 | config | `config.json::canonical_scope`(`bind_categories`) · `category_pair_map` · `match_threshold` | 좌표에 묶이는 카테고리(노칭의 커터≠탭용접의 커터) · 카테고리 쌍 · 매칭 문턱 | `init --fresh` 후 전량 (스코프) · 다음 인입 (문턱) | 노드 수 |
| 3.4 | 자산 | 사전 (`<상태>/data/dictionary.json` — I축 `run.py ops`로만) | 표면형 → canonical alias (반복 오판정의 영구 처방) | 즉시 | `run.py ops` |
| 3.6 | config | `config.json::relation_patterns` | 커밋 게이트가 허용하는 (출발, 관계, 도착) 삼항 | 다음 인입부터 | `gate_rejects.json` 먼저 |
| 3.7 | config | `config.json::mirrors` · `polarity` | 극성 대칭 관계 · 축 | `init --fresh` 후 | 그래프 엣지 |

## 파트 4 · 질의 (지식을 꺼내 쓴다)

| Cell | Kind | Where | Changes | Effective | Check |
|---|---|---|---|---|---|
| 4.0 | 계약 | `query --json`의 `trace`(B82 ③ — `intent`·`linking`·`hops`·`collection`·`facts(used)`·`answer`·`miss`) | 계측이다 — `graph.neighbors(…, trace=)`가 홉마다 적고 `expand`·`collect_chunks`가 넘긴다 · 판단 0 · 새 순회 0 · 기존 키 불변. 화면(뷰어)과 골든셋이 같은 것을 본다 | 즉시 | `--json` · 뷰어 오버레이 |
| 4.1 | 지시문 | `prompts/4.1_link.md` | 질문의 표기를 노드에 붙이는 LLM 폴백 규칙 (사전 스캔 → LLM → 임베딩 3단) | 즉시 | `run.py query` trace |
| 4.1 | 자산 | 사전 alias | 표기를 못 찾을 때의 1순위 처방 | 즉시 | trace 「링킹」 |
| 4.2 | config | `config.json::query_intents` · `query_traverse` · `cross_layer_traverse`(quality) | 질문 유형 → 경로(`INTENT_PATH`) · 홉·방향 규칙 · 층 간 이동 | 즉시 | trace 「확장」 |
| 4.3 | 상수 | `core/query/query.py::COLLECT_LIMIT`(8) | 근거 상한(문서 5 규약 6) | 회차 | trace 「수집」 |
| 4.3 | config | `config.json::fact_templates` | 그래프 사실을 문장으로 만드는 틀 | 즉시 | 답의 사실 문장 |
| 4.4 | 지시문 | `prompts/4.4_answer.md` | 두 채널 구분 · 근거 없으면 「모른다」 | 즉시 | 답 |
| 5.1 | 명령 | `run.py viewer [--port N] [--no-browser]`(B52 → B82 세미 플랫폼) — `cli/viewer/` 패키지 | 읽기 전용 서버(127.0.0.1 · GET 8 라우트 — `/api/graph`·`/api/query`·`/api/doc/<id>`·`/raw/<경로>`·`/api/funnel`·`/api/health`) · 벤더링 WebGL(`static/vendor/` sigma 3.0.1·graphology 0.25.4 — 고정 버전·LICENSE·해시 README · 외부 URL 0) · 색 축 6(기본 category) · 엣지 rel 토글(걸침은 언제나 · 곡선+별색) · 필터·검색 · 노드→provenance→원본 · 질의 오버레이 · 연결 현황 | 즉시 | 브라우저 · 콘솔 오류 0 · 데이터 해시 불변 |
| 5.1 | 규칙 | `cli/viewer.html` | `export html` **스냅샷 반출 전용**(D-163 ⑤) — 뷰어가 쓰지 않는다 | — | `export/` |

## 파트 밖 · 골격 · 게이트웨이 · 실행

| Cell | Kind | Where | Changes | Effective | Check |
|---|---|---|---|---|---|
| 0.1 | 자산 | `layers/<층>/skeleton.json` seed | 골격 노드·part_of·precedes — LLM 초안·사람 확정(I14) | `bootstrap` 후 | `run.py bootstrap` · 골격 확정 화면 |
| 0.2 | 상수 | `cli/scan.py` — 지문 대상 = reader가 `sheets`를 냈는가(`reader.GRID_EXT` — xlsx·xlsm·csv·tsv) · **포맷 상수 없음**(B66 ①) · 어댑터 소재지 = 등록부(`USE_MOCK=1`이면 + 내장 `tests/fixtures/adapters/` — B70 ①) · 결손은 상태 거부(B70 ②) | 어느 문서가 헤더 지문 대조를 받나 · 어느 어댑터가 대조에 드나. 포맷 이름으로 거르는 상수를 다시 두지 않는다 — 같은 실수가 세 번 났다(B62 ①-a · `_header_actual` · `FINGERPRINTABLE`) | 회차 | 어서션 「봉투 `format`을 읽는 동작 줄 0」 · `run.py scan <문서>` · 미선택 네 갈래 화면(B66 ②) |
| 0.4 | 설정 | `llm.local.json` / `~/.onto/llm.json` / env `LLM_GATEWAY_URL` `CHAT_MODEL` `EMBED_MODEL` `LLM_API_KEY` (`ONTO_CONFIG`로 경로 지정) | 게이트웨이·모델 — 전 지점 공통 | 즉시 | `run.py platform` ① 설정 줄 · `doctor.py` |
| 0.4 | 설정 | `CHAT_TEMPERATURE`(B80 ① — 없으면 안 싣는다 · 숫자면 싣는다 · 인자 > 설정) · `EMBED_BACKEND` `gateway|local` · `EMBED_GATEWAY_URL`(B80 ③) — 조립은 `gateway._payload()` 하나 · 갈래는 `embeddings.embed()` 하나 | 새 모델이 temperature를 거부(400) · 게이트웨이가 임베딩을 안 내줄 때 로컬 모델 폴더(sentence-transformers · 지연 import · 프로세스당 1회 로드 · CPU) · 오류 문면에 `POST <url>` | 즉시 | `llm-check` ①(temperature · embed 갈래) · ⑥(backend · 차원 · device · 로드 s · 인코딩 ms) |
| 0.4 | 자리 | **상태 루트 `ONTO_HOME`**(B78 1b — 기본 `./state` · `USE_MOCK=1`이면 `./state_mock`이고 `ONTO_HOME`을 무시한다) · 폴더를 만드는 자리는 `core/paths.py::ensure` 하나 · 단별 자리 함수 `paths.docs()/layers()/registry()/data()/work()/export()/golden()` · 등록부 경로는 `registry/` 기준 상대 · 설정 넷째 자리 `$ONTO_HOME/llm.json` · **`USE_MOCK=0`인데 `ONTO_HOME`이 없으면 모드 줄·doctor 첫 줄에 「(ONTO_HOME 미설정 — 기본 루트 …)」 한 조각**(B79 ④ · `paths.home_note()`) | 코드와 상태를 다른 뿌리에 — 코드 교체가 상태를 건드리지 않는다 | 즉시(프로세스당 한 번 정한다) | `doctor` 첫 줄 `상태 폴더 <경로> (…) · 모드 …` · `grep -rn 'mkdir(' core cli parser kit` 1곳 |
| 0.1 | 자리 | **층 자산 `$ONTO_HOME/layers/<층>/{config,skeleton}.json`**(B79 ① — ②등록 단) · 레포 `layers/`는 mock 기본 seed(`init --fresh`가 mock 루트에만 복사) · `USE_MOCK=0`에서 없으면 상태 거부 「층 자산 먼저」 · `platform migrate --assets --from <옛 코드 폴더>`(있는데 다르면 덮지 않고 멈춘다) · 킷은 `--layers <자리>`로 받는다 | 사내 골격은 사람이 승인한 것 — 코드 교체로 되돌아가지 않는다 | 즉시 | `doctor` 첫 줄 `층 k(상태 루트)` · 상태 거부 화면 |
| 0.3 | 자리 | **원본 `$ONTO_HOME/raw/`**(B79 ② → 이름 B80 ② — ⓪원본 · 사람이 넣는다 · `paths.raw()` · 옛 이름은 `paths.legacy_raw()`) · 인자 없는 `ingest-dir`가 전체를 재귀로 · `doc_registry.source_path`는 상태 루트 아래면 상대, 밖이면 절대 | 재구축 입력이 상태와 같이 산다 · 폴더를 옮겨도 기록이 안 낡는다 · 옛 이름에 문서가 있으면 상태 거부 「mv docs raw」(0건으로 끝내지 않는다) | 즉시 | `show doc` 원본 열 · `[투입] 넣을 문서가 없다` · `[투입] 원본 자리가 raw/로 바뀌었다` |
| 0.3 | 화면 | `cli/_screen.py` — 색(B81 ③) · `--no-color` · `NO_COLOR` · 로그 갈래(B81 ② · `core/state/log.py`) · `ONTO_LOG_LEVEL`(콘솔) · `ONTO_LOG_FILE` | 색은 특이점에만(표 `KINDS` — 키는 verdict 이름 · new 노랑 · uncertain/lowres 빨강 · orphan/gate_reject/fail 빨강 굵게 · match 초록 · 요약 반전) · tty·NO_COLOR·--no-color 셋을 보고 켠다 · 파이프에 ESC 0. 콘솔 WARNING · 파일 INFO `work/logs/<명령>_<날짜>.log` 언제나 · `-v` = 콘솔 INFO + 값 줄 전량 | 즉시 | 끝 요약 「로그 <경로>」 · `strip_ansi()` 뒤 문면 |
| 0.3 | 검사 | `tests/names_scan.py`(미정의 이름 0 · AST) · 9지점 실호출 본문 스모크(전송만 스텁) · `docs/회귀스위트/자산/자산_해시.json`(레포 정본 자산 — `python tests/asset_hashes.py --write`로 구현 세션이 갱신) | B79 ③④ — mock 회귀가 실호출 본문을 안 돌아 `import json` 누락이 사내에서 터졌다 · 레포 정본 자산의 사내 변경은 doctor ⚠ | 회차 | 회귀 · doctor `[ 주의 ] 레포 정본 자산이 레포 판과 다르다` |
| 0.3 | 명령 | `run.py platform migrate [--from <옛 루트>] [--to <새 루트>] [--dry-run]`(B78 1b) | 옛 배치(레포 루트의 `data/`·`review/`·`parsed/`…)를 5단 자리로 **복사** — 등록부 경로 재작성 · 락 제외 · `work/migrate.log`. 옛 배치가 있고 상태 루트가 비면 다른 명령은 상태 거부 「이관 먼저」 | 즉시 | 이관 화면 · `platform doctypes` 역방향 0 |
| 0.3 | 도구 | `tools/assert_audit.py` → `docs/회귀스위트/자산/어서션_감사.json`(B77 ①) | 어서션 전량 분류(성질·문면:수·문면:화면·픽스처·중복) — 라벨이 아니라 판정식을 읽는다. 회차마다 순증을 이 표로 본다 | 즉시 | 표의 분류별 건수 |
| 0.3 | 계약 | 상태 거부 문면 ④근거(B77 ③) — 스캐너가 `# [상태]` 자리마다 경로 문자열 ≥1을 잰다 | 「무엇을 보고 판정했나」 | 즉시 | `tests/exits_scan.py` 근거 없음 0 |
| 0.4 | 설정 | `CANDIDATE_NARROW=auto|embed|overlap`(설정 파일·env · B75 ①) · `ingest-file --narrow …`(플래그가 설정을 이긴다) | 후보 좁히기 방법 — `auto`는 `EMBED_MODEL` 있으면 임베딩·없으면 겹침(인입이 서지 않는다) · 비교는 `show report --diff a.json b.json` | 다음 인입부터 | 요약 줄 `좁힘 — 임베딩 e · 겹침 o` · 머리 「임베딩 미설정 — 겹침으로 좁힌다」 |
| 0.4 | 설정 | `USE_MOCK=1`(기본) | mock ↔ 실호출. 운영 명령은 mock이면 실행 전에 멈춘다(B48 관문) | 즉시 | 화면 첫 줄 `mode_line` |
| 0.4 | 설정 | `ONTO_DUMP_PROMPT=1` | 조립된 생성 지시문을 `prompt_rendered.md`로 떨군다(관측용) | 즉시 | 파일 |
| 0.4 | 상수 | `core/llm/gateway.py::POINTS`(닫힌 9종) · `CALL_TAGS`(`interview`→`generate`) | 지점 이름 · 호출 태그→지점 되돌림 — **회귀가 대장과 대조한다** | 회차 | `test_2a_gateway` 잠금 |
| 0.4 | 상수 | `core/llm/gateway.py::ERR_BODY_MAX` · `core/build/retry.py` | 오류 본문 보존 상한 · 재시도 정책 | 회차 | 로그 |

---

## 이 표가 말하지 않는 것

- **회귀 어서션·골든셋·계기판**은 손잡이가 아니라 측정 장치다 — 가이드 §5.4 · 04 「확인 루틴」.
- **`review/<dt>/`의 산출물**(`adapter.py`·`schema.json`·`input_package.json`·`state.json`·`interview_log.json`)은 손잡이가 아니라 생성 결과다. 고치는 길은 `--instruct`(1.3)와 `generate --revise`(1.8)뿐 — 파일을 손으로 고치면 다음 관문이 덮거나(`header_labels`·`adapter_version`) 승인 이력이 끊긴다.
- 상수를 회차 없이 사내에서 바꾸면 명세와 코드가 갈린다 — 문서 7 §7.4-5 「공용 코드의 사내 수정은 버전 기록과 외부 통보 없이 하지 않는다」.
