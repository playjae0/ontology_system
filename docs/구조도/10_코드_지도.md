# 코드 지도 — 파일 하나에 자리 하나

> **자동 생성**(`docs/회귀스위트/추출_구조.py`). 손으로 고치지 않는다 — 코드가 바뀌면 다시 돌린다.
> 회귀가 이 파일을 **다시 만들어 레포의 것과 대조한다**(diff 0) — 낡으면 붉다.
> 칸 번호는 각 파일 **첫 줄 머리말**에서 읽는다 — `00_칸_대장.md`와 같은 번호다.

## ⓐ 폴더 = 파트

### `(레포 루트)` — 칸 0 · 파일 3 · 901행 · def 35

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `doctor.py` | 0.3 | 사내 이식 점검기 — 가져온 것이 온전한가 · 다음에 무엇을 해야 하는가 (국면 2 진입 도구) | 619 | 14 |
| `router.py` | 0.1 | 층 폴더 자동 발견 — 등록 배선이 없다 (CH6 6.1) | 19 | 1 |
| `run.py` | 0.3 | 파이프라인 진입점 — CLI+파일 (구현문서 §0) | 263 | 20 |

### `cli/` — 칸 0·1·2·3·4 · 파일 17 · 5,582행 · def 194

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 1.1~4.4 | 사람이 치는 진입점 모음(CLI). 각 모듈이 한 칸의 화면을 맡는다 | 1 | 0 |
| `_gate.py` | 0.3 | mock 관문 — 사람이 치는 운영 명령은 mock에서 실행 전에 멈춘다 (문서 7 §7.6-B-1 · B48) | 115 | 5 |
| `export.py` | 0.3 | 내보내기 — 시각화·외부 도구용 파생물 (명세 §11 · 카드 P5) | 476 | 15 |
| `extract.py` | 3.3 | 추출 단계 진입점 — 플랫폼 계약(문서 7 §7.1) | 95 | 2 |
| `golden.py` | 0.3 | 골든셋 — 문항 틀과 채점기 (문서 5 §5.5-2·3·4 · B54) | 339 | 15 |
| `ingest.py` | 3.1 | 일괄 투입 — 파일 하나 또는 경로 하나로 선택 → 파싱 → 인입을 잇는다 (문서 6 §6.4 · B46) | 759 | 25 |
| `interview.py` | 1.3 | 생성 전 문답 — 이해 요약과 질문, 종료는 사람 (문서 6 §6.5 · B21·B32·B43) | 378 | 9 |
| `llmcheck.py` | 0.4 | 게이트웨이 연결 확인 — 붙었는가를 한 줄로 묻는 명령 (문서 7 §7.6-B) | 73 | 1 |
| `ops.py` | 0.3 | I축 도구 CLI — n5의 사람 진입점 (CLI+파일, 구현문서 §0) | 147 | 3 |
| `parse.py` | 2.1~2.9 | 파서 CLI — 운영 파싱 + 구축 모드 3단 배선 (파서_명세 §6 · 증분0 §3 P1) | 301 | 11 |
| `platform.py` | 0.3 | 4′ 플랫폼 연동 — 국면 1의 플랫폼 쪽 창구 (구현문서 §7 단계 4 · 명세 §16.2 개조 1·2) | 748 | 29 |
| `prompt.py` | 1.4 | 생성 지시문 조립 — 템플릿·주입·스트립·크기 (문서 6 §6.7 킷 #1 · B29~B41) | 370 | 13 |
| `query.py` | 4.4 | 질의 단일 진입점 라우터 (명세 §8-R1) | 286 | 7 |
| `scan.py` | 0.2 | n9 지문 스캔 — doc_type 미지정 정형 문서의 후보 제안 (파서_명세 §5 · 카드 C15) | 292 | 10 |
| `show.py` | 0.3 | 산출물 열람 — 시각화 없이 텍스트로 본다 (명세 §11 · 카드 P5) | 728 | 29 |
| `skeleton.py` | 0.1 | 골격 seed 확정 — 생성은 밖에, 확정만 시스템 안에 (문서 3 §3.7 규약 3 · B25) | 337 | 12 |
| `viewer.py` | 0.3 | 검증 뷰어 — 그래프 그림 위에서 질의가 도는지 보는 창구 (문서 7 시각화 3형태 · B52) | 137 | 8 |

### `cli/register/` — 칸 1 · 파일 9 · 3,534행 · def 107

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 1.1~1.8 | n6 구축 모드 등록 파이프라인 — doc_type 등록의 3단 (파서_명세 §6·§7 · 틀 §2) | 104 | 3 |
| `__main__.py` | 1.1~1.8·**진입점** | `python -m cli.register <명령>` (이름 불변) | 134 | 3 |
| `confirm.py` | 1.7 | ③ 확정: 승인 1회 → 등록부 등재 → 정본 승격 (`register confirm`) | 115 | 2 |
| `draft.py` | 1.4 | 어댑터·스키마 초안: 생성 스키마 · 실호출 갈래 · 고정 어댑터 제안 | 608 | 21 |
| `gate.py` | 1.5 | 기계 관문: 하네스 실행 · 실패 분류(AUTO_FIX·문답·GATE_SELF) · 화면 | 662 | 20 |
| `generate.py` | 1.1·1.2 | ① 생성 흐름: 입력 패키지 → 초안 → 관문 (`register generate`) | 633 | 11 |
| `interview.py` | 1.3 | 문답 로그와 배치: 라운드 전문·결정 이력을 `review/<dt>/`에 남긴다 | 246 | 15 |
| `ledger.py` | 1.5·1.6 | 열 판정 대장과 role 표: 어느 열이 무엇이 됐나를 파일로 남긴다 | 428 | 17 |
| `view.py` | 1.6 | 검수 뷰: 리허설 · 뷰 데이터 산출 · `register review`·`status` 화면 | 604 | 15 |

### `core/` — 칸 0·3·4 · 파일 5 · 1,109행 · def 69

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 0.1~4.3 | core: 처리 코드. 전 층 공용이며 층 어휘 0(구현문서 §0, 카드 B1) | 6 | 0 |
| `dictionary.py` | 3.4 | 동의어 사전 — 접근의 단일 관문 (문서 7 §7.1 core 접근 경계 3종) | 126 | 10 |
| `graph.py` | 3.7 | GraphStore — 그래프 저장의 유일 경계 (틀 §4B-A8, 카드 B6, CH6 6.1 규약 6) | 242 | 17 |
| `matcher.py` | 3.4 | 개체 판정 — "그것이 기존의 무엇인가" (CH3A 3.3) | 428 | 16 |
| `paths.py` | 0.3 | 상태의 자리를 아는 유일한 모듈 (B78 1a · 칸 0.4) | 307 | 26 |

### `core/build/` — 칸 3 · 파일 12 · 3,311행 · def 114

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 3.1~3.7 | 파트 3 구축: 계약 JSON을 지식으로 (칸 3.1~3.7 · 문서 4) | 7 | 0 |
| `build.py` | 3.2·3.5 | 1c′·1d′ 구축 — 계약 JSON을 그래프로 (계약 v2 정합) | 535 | 18 |
| `entry.py` | 3.1~3.7·**진입점** | 문서 하나를 인입하고, 빌드 말미에 전역 패스를 돈다 | 449 | 14 |
| `extract.py` | 3.3 | n3 추출 단계 — 파싱과 구축 사이의 독립 단계 (CH3A 3.1·3.11, 틀 Q1) | 366 | 13 |
| `gate.py` | 3.1·3.6 | n4 커밋 게이트 — 문서 유래 엣지가 그래프에 쓰이기 전의 관문 (CH3A 3.4) | 143 | 5 |
| `ingest.py` | 3.1 | 인입 — 계약 JSON을 받아 근거 축을 확정한다 (n2 · n1) | 318 | 12 |
| `ledger.py` | 3.4 | 판정 대장 — 문서마다, 값마다, 어떻게 해소했는가 (B74 ② · 문서 7 §7.8) | 117 | 7 |
| `loop.py` | 3.2·3.5 | 행 루프: role 핸들러와 그 공용 재료 (문서 4 §4.3·§4.5) | 431 | 18 |
| `naming.py` | 3.4 | 이름 규칙 — 극성 결합과 canonical 스코프 (CH3B 3.5, 틀 §4B-A11-8·9) | 94 | 4 |
| `prose.py` | 3.3·곁 | 비정형(prose) 구축: 추출 후보를 그래프에 착지시킨다 (문서 4 §4.4) | 200 | 3 |
| `retry.py` | 3.5 | orphan 재시도 — 큐에 남은 미부착·미앵커 항목을 다음 빌드 뒤에 다시 시도한다 (문서 4 §4.7-5) | 336 | 9 |
| `table.py` | 3.2 | 정형(table) 구축: 스키마가 관계를 정한다 · LLM 0 (문서 4 §4.3) | 315 | 11 |

### `core/llm/` — 칸 0·2·3 · 파일 7 · 1,245행 · def 49

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 0.4·게이트웨이 | LLM 설정·호출·재시도·mock이 수렴하는 자리 (문서 7 §7.6-B) | 6 | 0 |
| `check.py` | 0.4 | 연결 확인(`run.py llm-check`)이 단계를 끊어 보는 자리 (문서 7 §7.6-B) | 289 | 9 |
| `embeddings.py` | 0.4 | 임베딩 — `embed(text) -> vector` (문서 7 §7.6-B-1 게이트웨이 2파일) | 123 | 5 |
| `gateway.py` | 0.4·게이트웨이 | LLM 설정 접근이 이 파일 하나로 수렴한다 (문서 7 §7.6-B-1) | 561 | 26 |
| `narrow.py` | 3.4·곁 | 후보 좁히기 선택(auto·embed·overlap) (B75 ① · 문서 4 §4.2) | 46 | 2 |
| `points.py` | 2.8·④⑨ | 지점별 얇은 배선: 이미지 요약 · 좌표 태깅 (문서 6 §6.1·§6.4) | 95 | 4 |
| `struct_map_pass.py` | 2.4 | 구조 지도 패스(지점 ⑦): 산문의 제목 표시를 모델이 고른다 (문서 6 §6.3) | 125 | 3 |

### `core/query/` — 칸 4 · 파일 3 · 588행 · def 28

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 4.1~4.3 | 파트 4 질의: 지식을 꺼내 쓴다 (칸 4.1~4.3 · 문서 5) | 6 | 0 |
| `bm25.py` | 4.3 | BM-25 상시 대조군 (문서 5 §5.5-3) | 124 | 8 |
| `query.py` | 4.1~4.3 | 질의 4단 — 범용 읽기 파이프라인 (CH5 5.1·5.2, 명세 §5.6.1~5.6.4) | 458 | 20 |

### `core/state/` — 칸 0·1·3 · 파일 12 · 2,704행 · def 111

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 0.1~0.3 | 상태·운영·기반: 저장·등록부·초기화·골격·I축 연산·이관·로그 (칸 0.1~0.3 · 문서 7 §7.8) | 6 | 0 |
| `bootstrap.py` | 0.1 | n10 — 부트스트랩 seed·config (G2, 기존 1b 흡수). seed 형식 v3.2(D-42) | 227 | 8 |
| `fixtures.py` | 0.3 | 테스트 픽스처의 소재 단일 지점 — mock 격리의 관문 | 48 | 1 |
| `ids.py` | 3.7 | 두 축 id — CH6 6.1 규약 2가 산식의 소유처다 (틀 A4·A7, 카드 N1~N4·C7) | 115 | 9 |
| `init.py` | 0.3 | 클린 상태를 만드는 단일 정의 — `run.py init [--fresh]` (문서 7 §7.6-4) | 131 | 4 |
| `log.py` | 0.3 | 로깅 — 표준 `logging`, 레벨 INFO (문서 7 §7.8 「로그 규격」) | 131 | 7 |
| `migrate.py` | 0.3 | 옛 배치 → 5단 배치 이관 (B78 1b · 문서 7 §7.8) | 276 | 10 |
| `ops.py` | 0.3 | n5 — I축 인스턴스 변경 도구 4연산 (틀 §4B-A2, 카드 L5~L9·G6) | 732 | 23 |
| `registry.py` | 1.7 | doc_type 등록부 — "이 문서 유형이 등록됐는가"의 단일 조회처 (카드 M2 · D-8) | 298 | 14 |
| `skeleton.py` | 0.1 | 골격 심기 — seed의 중첩·순서를 관계로 파생한다 (문서 7 §7.1 「골격 심기 모듈의 자리」) | 377 | 17 |
| `status.py` | 0.3 | 노드 생존 판정과 툼스톤 체인 — 어느 층·어느 연산에도 속하지 않는 잎 모듈 (문서 4 §4.7) | 61 | 2 |
| `store.py` | 3.7 | 공유 자원 파일 입출력 — 층 그래프 파일을 제외한 data/ 전부 | 302 | 16 |

### `kit/` — 칸 1 · 파일 7 · 1,652행 · def 43

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 1.4~1.6 | 어댑터 생성 킷: 스켈레톤·참조 어댑터·실행 관문·검수 뷰 렌더러 | 1 | 0 |
| `gate_checks.py` | 1.5 | ④ 스키마 정합: 형 검사(G4F) · 대장 커버리지(G4G) · 어휘 · role 드라이런 | 305 | 10 |
| `gate_screen.py` | 1.5 | 관문의 화면: 판정 줄 규격(`LINE_RE`)·`show()`·실행 신원 한 줄 | 67 | 2 |
| `gate_tables.py` | 1.5 | 관문의 닫힌 표: 허용 role·금지 import·자기완결 연산·형 표 | 162 | 3 |
| `render_review.py` | 1.6 | 검수 뷰 렌더러 (킷 구성물 ⑤) — 뷰 데이터 JSON → 정적 HTML (파서_명세 §7 · 카드 M11) | 436 | 16 |
| `run_adapter.py` | 1.5 | 실행 하네스 — LLM이 산출한 adapter 코드와 매칭 스키마를 실제로 돌려 검증한다 | 536 | 11 |
| `어댑터_스켈레톤.py` | 1.4 | 어댑터 스켈레톤 양식 (킷 구성물 ②) — 빈칸을 채우는 방식으로 생성한다 | 145 | 1 |

### `kit/참조어댑터/` — 칸 1 · 파일 3 · 337행 · def 5

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `cp.py` | 1.4 | 참조 cp 어댑터 — S11(지문 스캔)의 재료 | 119 | 1 |
| `ipqc.py` | 1.4 | 참조 어댑터(전시물) ipqc: 정형 검사 성적서의 본보기 | 92 | 1 |
| `toc_report.py` | 1.4 | 참조 어댑터(전시물) toc_report: 목차형 산문의 본보기 | 126 | 3 |

### `parser/` — 칸 1·2 · 파일 11 · 2,773행 · def 96

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 2.1~2.9 | 파서 공용 코어 (n7) — 별도 패키지다 (D-9) | 17 | 0 |
| `form.py` | 2.3 | 형태 판정 — 이 문서를 table로 읽을 것인가 prose로 읽을 것인가 (문서 1 C37 · 문서 6 §6.4) | 138 | 3 |
| `normalizer.py` | 2.7 | normalizer — 자기완결 보정 4기능 (파서_명세 §3 · CH2 2.2 계약 ③ · D-12 확정) | 170 | 7 |
| `pipeline.py` | 2.5·2.9 | 파서 진입점 — 코어 6종을 순서대로 엮는다 (파서_명세 §3 · D-2 확정 배열) | 310 | 12 |
| `preflight.py` | 2.2 | preflight — 실행 전 정합 검사 (파서_명세 §5 규약 6 · 카드 C15) | 270 | 10 |
| `profile.py` | 1.2 | 열 프로파일 — 전 행을 스캔한 결정적 통계 (문서 6 B39) | 163 | 8 |
| `reader.py` | 2.1 | 파서 공용 코어 — reader (포맷별 원시 추출) | 629 | 19 |
| `render.py` | 2.8 | 쪽 전체 렌더 — ④ 이미지 요약에 화면 전체를 함께 주기 위한 것 (B53) | 115 | 5 |
| `struct_map.py` | 2.4·2.5 | struct-map — 구조 지도 패스 (틀 v2.8 §4-Q2 · D-58 · R18 · 파서_명세 v0.6 §2·§3) | 509 | 19 |
| `tagger.py` | 2.8 | tagger — 정규 조각 → 계약 JSON (파서_명세 §3 · CH2 2.2) | 329 | 11 |
| `validator.py` | 2.9 | validator — 계약 self-check (파서_명세 §8 · CH2 2.2 · 카드 C14) | 123 | 2 |

### `parser/adapters/` — 칸 2 · 파일 4 · 592행 · def 21

| 파일 | 칸 | 무엇을 맡나 | 행 | def |
|---|---|---|---:|---:|
| `__init__.py` | 2.6 | 코어 기본 어댑터 — LLM 생성 대상이 아니다 (파서_명세 §5 규약 5) | 6 | 0 |
| `basic_pdf.py` | 2.6 | 기본 어댑터 (PDF) — 페이지가 청크다 (B53 · 문서 6 §6.4-5) | 98 | 2 |
| `basic_ppt.py` | 2.6 | 기본 어댑터 (PPT) — 임계 조건화 (B+C 단계형 · 08-19 판정 · 카드 C13 v18) | 267 | 11 |
| `basic_prose_xlsx.py` | 2.6 | 기본 어댑터 (xlsx/csv 산문) — 줄 목록 + 계층 신호가 전부다 (B58 ③ · 문서 6 §6.4-5) | 221 | 8 |

## ⓑ 진입점

**`python run.py <서브커맨드>`** — 위임처는 각 `cmd_*`가 함수 안에서 하는 import에서 읽는다.

| 서브커맨드 | run.py | 위임처 |
|---|---|---|
| `all` | `cmd_all` | (제자리) |
| `bootstrap` | `cmd_bootstrap` | (제자리) |
| `build` | `cmd_ingest` | `core.build.entry.finalize` · `cli.ingest.build_screen` |
| `export` | `cmd_export` | `cli.export.main` |
| `gauges` | `cmd_gauges` | `cli.platform.cmd_gauges` |
| `golden` | `cmd_golden` | `cli.golden.main` |
| `ingest` | `cmd_ingest` | `core.build.entry.finalize` · `cli.ingest.build_screen` |
| `ingest-dir` | `cmd_ingest_batch` | `cli.ingest.main` |
| `ingest-file` | `cmd_ingest_batch` | `cli.ingest.main` |
| `init` | `cmd_init` | `core.state.init.init` |
| `llm-check` | `cmd_llm_check` | `cli.llmcheck.main` |
| `ops` | `cmd_ops` | `cli.ops.main` |
| `parse` | `cmd_parse` | `cli.parse.main` |
| `platform` | `cmd_platform` | `cli.platform.main` |
| `query` | `cmd_query` | `cli.query.main` |
| `register` | `cmd_register` | `cli.register.__main__.main` |
| `scan` | `cmd_scan` | `cli.scan.main` |
| `show` | `cmd_show` | `cli.show.main` |
| `skeleton-confirm` | `cmd_skeleton_confirm` | `cli.skeleton.main` |
| `skeleton-status` | `cmd_skeleton_status` | `cli.skeleton.cmd_status` |
| `viewer` | `cmd_viewer` | `cli.viewer.main` |

**`python -m …`** — `__main__` 블록을 가진 자리.

| 명령 | 파일 |
|---|---|
| `python -m cli.export` | `cli/export.py` |
| `python -m cli.extract` | `cli/extract.py` |
| `python -m cli.golden` | `cli/golden.py` |
| `python -m cli.ingest` | `cli/ingest.py` |
| `python -m cli.llmcheck` | `cli/llmcheck.py` |
| `python -m cli.ops` | `cli/ops.py` |
| `python -m cli.parse` | `cli/parse.py` |
| `python -m cli.platform` | `cli/platform.py` |
| `python -m cli.query` | `cli/query.py` |
| `python -m cli.register` | `cli/register/__main__.py` |
| `python -m cli.scan` | `cli/scan.py` |
| `python -m cli.show` | `cli/show.py` |
| `python -m cli.skeleton` | `cli/skeleton.py` |
| `python -m cli.viewer` | `cli/viewer.py` |

## ⓒ 접근 경계 — `core/` 최상위

경계 밖에서 직접 열지 않는다(문서 7 §7.1). **부르는 모듈 수**는 import 문을 세어 잰다 (운영 모듈 + 회귀).

| 파일 | 칸 | 무엇을 소유하나 | 부르는 모듈 |
|---|---|---|---:|
| `core/dictionary.py` | 3.4 | 동의어 사전 — 접근의 단일 관문 (문서 7 §7.1 core 접근 경계 3종) | 11 |
| `core/graph.py` | 3.7 | GraphStore — 그래프 저장의 유일 경계 (틀 §4B-A8, 카드 B6, CH6 6.1 규약 6) | 6 |
| `core/matcher.py` | 3.4 | 개체 판정 — "그것이 기존의 무엇인가" (CH3A 3.3) | 17 |
| `core/paths.py` | 0.3 | 상태의 자리를 아는 유일한 모듈 (B78 1a · 칸 0.4) | 36 |

## ⓓ 상태의 자리 — `core/paths.py`

| 함수 | 단 | 실경로 | 무엇 |
|---|---|---|---|
| `paths.home()` | — | `<상태>` | 상태 루트 — 프로세스당 한 번 정한다. |
| `paths.legacy_raw()` | — | `<상태>/docs` | 원본 자리의 옛 이름 — `$ONTO_HOME/docs/` (B79 ② → B80 ②). |
| `paths.raw()` | — | `<상태>/raw` | 원본 문서 — `$ONTO_HOME/raw/` (B79 ② · 이름은 B80 ②). 사람이 넣고 시스템은 읽기만. |
| `paths.blocks()` | ①자산 | `<레포>/schemas/blocks.json` | 공용 블록 — 레포 자산이다(등록 산출이 아니다 · git이 추적한다). |
| `paths.config_file()` | ①자산 | `<레포>/state/llm.json` | 설정 파일의 상태 루트 자리 — `core/llm/gateway.py`가 찾는 넷째 자리다(B78 1b). |
| `paths.fixture_schemas()` | ①자산 | `<레포>/tests/fixtures/schemas` | 내장(mock) 스키마의 자리 — `tests/fixtures/schemas/`. |
| `paths.seed_layers()` | ①자산 | `<레포>/layers` | 레포의 기본 seed 자리 — 복사의 출발점이지 읽는 자리가 아니다. |
| `paths.adapters()` | ②등록 | `<상태>/registry/adapters` | 확정 어댑터의 정본 자리(문서 6 §6.4·§6.5). |
| `paths.layers()` | ②등록 | `<상태>/layers` | 층 자산 — `$ONTO_HOME/layers/<층>/{config,skeleton}.json` (B79 ①). |
| `paths.registry()` | ②등록 | `<상태>/registry` | 등록 단 — 사람 승인 1회의 산출. 재생성되지 않고 백업 1순위다. |
| `paths.review()` | ②등록 | `<상태>/registry/review` | `registry/review/<doc_type>/` — 등록 작업·승인 기록. |
| `paths.schemas()` | ②등록 | `<상태>/registry/schemas` | 등록된 매칭 스키마. 내장 mock 스키마는 여기 오지 않는다(자리로 가른다). |
| `paths.data()` | ③진실 | `<상태>/data` | 진실 단 — 그래프·사전·청크·큐·대장. 누적이고 백업 대상이다(§7.7). |
| `paths.extract()` | ④작업·장부 | `<상태>/work/extract` | 추출 체크포인트(파일 존재 = 추출 완료) · 구조 지도 보존. |
| `paths.parsed()` | ④작업·장부 | `<상태>/work/parsed` | 계약 JSON — 파서/에이전트 경계(파일 존재 = 파싱 완료). |
| `paths.work()` | ④작업·장부 | `<상태>/work` | 작업 단 — 재생성 가능한 단계 산출·장부·로그. |
| `paths.export()` | ⑤파생 | `<상태>/export` | 파생물 — 되돌려 읽지 않는다(문서 1). |
| `paths.golden()` | ⑤파생 | `<상태>/golden` | 골든셋 — 사내 실문서를 인용하므로 레포가 추적하지 않는다(D-123 ①). |

## ⓔ 크기 — §7 상한 (파일 800행 · 함수 120행)

- 운영 모듈 **93** · 합 **24,328행** · def **872** · class 20
- **파일 상한 위반 0** · **함수 상한 위반 0**

큰 파일 열 (행 순).

| 파일 | 행 | 최장 함수 | 행 |
|---|---:|---|---:|
| `cli/ingest.py` | 759 | `ingest_file` | 97 |
| `cli/platform.py` | 748 | `gauges` | 79 |
| `core/state/ops.py` | 732 | `transfer` | 82 |
| `cli/show.py` | 728 | `cmd_node` | 76 |
| `cli/register/gate.py` | 662 | `machine_gate` | 119 |
| `cli/register/generate.py` | 633 | `_cmd_generate_package` | 102 |
| `parser/reader.py` | 629 | `_walk` | 70 |
| `doctor.py` | 619 | `check_env` | 104 |
| `cli/register/draft.py` | 608 | `_draft_live` | 69 |
| `cli/register/view.py` | 604 | `cmd_review` | 104 |
