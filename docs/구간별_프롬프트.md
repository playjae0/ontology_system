# 구간별 착수 프롬프트 (Claude Code — VS Code · Ubuntu) — 2026-08-13 개정판

> 사용법: 위에서부터 순서대로 하나씩 붙여넣는다. **정지점은 4개다(G0 / G1+G2 / G3★ / G4)** — 각 정지점이 끝나면 Claude Code가 멈추고 보고하며, 당신은 PROGRESS.md를 프로젝트 세션(claude.ai RAG)에 미러링한 뒤 다음 것을 붙여넣는다.
> **현재 위치: G0·G1+G2·G3★는 완료됐고, 남은 정지점은 G4 하나다 — 별도 승인 대기 · 착수 금지.** 그 사이 정지점이 아닌 두 세션(M2 반입 0~4단계 · M2 후속 G4 전 정리)이 더 지나갔다. G5·G6은 G4 확인 후 연속 진행한다.
> **지나간 정지점의 프롬프트는 지우지 않고 이력으로 남긴다.** 절 머리의 완료 스탬프와 「실제 결과」가 현행이고, 코드블록 안의 수치·지시는 **그때 그렇게 지시했다는 기록**이다 — 고치지 않았으니 지금 값으로 읽지 마라.
> 프롬프트가 짧은 이유: 규범·정본 관계·진행 규칙·읽기 주의는 전부 `CLAUDE.md`에 있고 매 세션 자동 로드된다. 같은 사실을 프롬프트에 다시 쓰면 이중 원천이 된다.
> **공통 규칙**: 문서로 결정되지 않는 지점은 추측하지 말고 BLOCKERS.md에 **판단 재료 3요소(근거 원문·선택지 2~3·귀결)**와 함께 기록하고 멈춘다 / 세부 판단은 DECISIONS.md에 가결정으로 기록하고 전진한다 / 구간 종료 시 PROGRESS.md 갱신 + 커밋 1회 / **PROGRESS.md에 결정 번호를 적지 않는다(08-10 신설 규칙)** — 미러는 보고이고 장부는 DECISIONS.md 하나다. 번호가 필요하면 **등재한 뒤 인용**한다(미러·커밋에만 적었다가 D-39~D-41 채번 충돌이 났다).

---

## 정지점 1 — G0 (A0: 레포 상태 회수 + 계수 3종) 【✅ 완료 2026-08-07 · 08-07 판 · 반입 대조 판정 반영】

> **실제 결과** — 계수 ① graph.json 직접 접근 **0지점** · 계수 ② N#### 하드코딩 **0지점 → ULID 발효 확정**(`id_seq.json` 미생성) · 계수 ③ raw mock `verify_roundtrip` **PASS 50 / FAIL 0**(단 **당시 prefix 10 기준** — 진짜 판정은 G1 선행의 prefix 11이었고, 08-13 현재는 **prefix 12**다).
> 레포에 `core/`·`data/`가 없어 **소스 코드 0바이트** — 기존 단위 1a~5는 "전부 미완료"가 답이었고, 개수(改修)가 아니라 **기존 단위 전량 신규 작성**으로 확정됐다(D-32).
> `DECISIONS.md` **31건 생성**(D-1~D-31, 소유 분담 4계) · `BLOCKERS.md` 생성(멈춤 0 · 판정 필요 2건).

```
CLAUDE.md와 docs/MANIFEST.md를 먼저 읽어라. 특히 CLAUDE.md의 "증분 0 v1.4 정정 5항"은
docs/증분0_구현.md v1.3 본문보다 우선한다(13회차 판정).

0단계 — 문서 정정 반영 (레포 사본을 고치고 커밋 1회 "13회차 판정 반영"):
 a. docs/증분0_구현.md에 정정 5항을 반영한다 — §7 헤더(초기값 31건·소유 분담 4계) /
    §3 P2(킷 6종 = 5종 + 실행 하네스 run_adapter.py, D-27) / §4.1 머리("마이그레이션"→"신규 작성") /
    §4.1-4(CP01 11행은 raw prefix 11과 동일) / §5-2(fixture = 실제 LLM 산출물의 스냅샷, D-26).
    본문을 지우지 말고 해당 문장만 교체하고, 머리의 version:을 v1.4로 올려라.
 b. 배치 확정분을 적용한다 — verify_roundtrip.py·xlsx_common.py → tests/ ·
    reader.py → parser/ · run_adapter.py → kit/ · 구 out/(LLM 산출 adapter·schema·log) → mock/fixtures/.
    run_adapter.py의 sys.path 하드코딩(/home/claude)은 제거한다. 이동은 git mv.

[A0 — 상태 회수]
1. 기존 단위 1a~5의 완료 여부를 실물 기준으로 확정한다. 이 레포에 core/·data/가 없으면
   "전부 미완료"가 곧 답이다 — 증분 0 §2.2 상태 표를 그렇게 채워 PROGRESS.md에 기록한다.
   애매한 항목은 임의 판정하지 말고 "판정 필요 — 사유"로 모은다.

[A0 — 계수 3종]
2. graph.json 직접 접근 계수: core/graph.py 밖에서 graph.json을 여는 지점을 전수 검색해 기록.
   0지점이면 G1 st는 "교체"가 아니라 "처음부터 GraphStore로 작성"이 된다.
3. N#### 하드코딩 계수: 의미 축 id의 N#### 형식을 전제한 지점을 전수 검색해 기록.
   ※ 사용자 판정 완료 — 계수 0을 확인하면 ULID를 발효한다. 처음부터 ULID로 짜고 id_seq.json은 만들지 않는다.
4. raw mock 반입 확인: python -m venv .venv 후 openpyxl·python-pptx를 설치하고
   tests/verify_roundtrip.py를 실행해 결과를 기록한다.
   ※ CP01.xlsx는 아직 C11(coord_mismatch 행)이 없다. 이 시점의 PASS는 prefix 10 기준이며,
     C11 삽입은 G1 선행 작업이다 — 그때 재실행한 것이 진짜 판정이다. 두 결과를 구분해 적어라.
5. parser/reader.py 등 반입 자산은 "n7 선행 자산 반입됨(미검증)"으로 PROGRESS에 적는다.
   이것은 core/ 코드가 아니므로 계수 2·3에 포함하지 않는다.

[초기 장부]
6. DECISIONS.md를 만들고 가결정 31건을 그대로 옮겨 적는다 —
   증분0 §7(D-1~D-14) + raw_mock_대장 §6(D-15~D-21) + 판정대조_ipqc_1차 §8(D-22~D-25)
   + 실행검증_1차 §6(D-26~D-31).

A0가 끝나면 멈추고 보고하라. git 기준선 커밋을 남길 것.
```

**당신이 볼 것**: 상태 표에 "G0 확인" 없음 · 계수 3종 숫자 · verify_roundtrip 결과(prefix 10 기준) · DECISIONS 31건 · 정정 5항 반영 커밋 · 기준선 커밋.

---

## 정지점 2 — G1 + G2 (저장 계층 · 주소 체계 · 부트스트랩) 【✅ 완료 2026-08-07】

> **실제 결과** — `tests/test_g1_g2.py` **31항목 전건 PASS**(이후 M2 반영으로 42 → **46항목**으로 늘었고 현행도 FAIL 0).
> st: GraphStore가 graph.json의 유일 경계 · **직접 접근 0지점을 grep으로 실증** · orjson(표준 json 폴백) · **계기판 7 = 0.005MB · 계기판 8 = 0.0s**.
> `verify_roundtrip` **prefix 11 PASS 50 / FAIL 0**(D-18의 진짜 판정 — 단 현행은 A11-9 ② 실발화 행 삽입으로 **prefix 12**다).
> **멱등성 2종 통과**(2회 인입 동일 · 조각 순서 셔플 3회 동일) · S6 occ 충돌 · S5 doc_hash 차단(`duplicate_doc_hold` 큐 1건) · registry builtin 층 1개 · **의미 축 id 전부 ULID**.
> 1b 승계 판정은 당시 Process 8노드·part_of 7·precedes 6이었다 — **M2 seed v3.2 반입으로 46 / 45 / 22 / 16 / 8 체계로 교체됐다.**

```
G0 결과를 전제로 진행한다. 증분 0 §3의 단위 정의(3요소·완료판정)를 그대로 따른다.

G1: st 저장 계층(GraphStore가 graph.json의 유일 경계 · orjson+표준 json 폴백 ·
    계기판 7 graph 저장 크기 · 8 build 소요 출력 — 틀 §4B-A8, 카드 B6)
    → n1 근거 축 id 전환(의미 축은 ULID 발효 적용) → n2 doc_hash 중복 차단
G2: n10 부트스트랩 seed·config (기존 1b 흡수)

선행 작업 2가지 — n1이 이것을 전제한다:
 ① mock/parsed/ 신규 작성. 구현문서 §6의 mock 전문을 기준으로, 증분 0 §4.1의 4항
    (source_locator 개명 · adapter_version 추가 · provenance 표기 · CP01 11 record)을
    적용한 상태로 CP01·PFMEA01·PPT01을 새로 만든다. 고칠 원본이 없으므로 마이그레이션이 아니다.
 ② mock/raw/CP01.xlsx에 C11(공정구분 "스태킹" + 공정명 "노칭" — coord_mismatch)을
    11번째 record 자리에 삽입하고 확대분을 C12~C29로 민다. 이후 verify_roundtrip을 재실행해
    prefix 11 일치를 확인한다. 이것이 D-18의 진짜 판정이다.
 ③ 증분 0 §4.2의 신규 mock 중 이 구간에 필요한 것(⑤ PPT03, ⑥ CP01B)을 작성한다.

완료판정: st(graph.py 밖 직접 접근 0 — grep으로 실증 + 계기판 7·8 출력)
+ S6(occ 충돌) + 멱등성 2종(2회 인입 동일 · 조각 순서 셔플 동일) + S5(doc_hash 차단)
+ 기존 1b 판정 승계(Process 8노드·part_of 7·precedes 6) + registry에 builtin 층 1개만 존재
+ verify_roundtrip prefix 11 PASS.

끝나면 멈추고 보고하라. 각 판정의 실제 수치를 함께 제시할 것.
```

**당신이 볼 것**: 직접 접근 0이 grep 실증인지 · **멱등성 2종**(뒤 전부의 전제) · prefix 11 PASS · duplicate_doc_hold 큐 1건 · 골격 status가 `seed`인지 · 계기판 7·8 숫자 · 생성된 id가 ULID인지.

---

## 정지점 3 — G3 (계약 v2 정합 · 추출 분리 · 게이트) ★가장 중요 【✅ 완료 2026-08-08】

> **실제 결과** — `tests/test_g3.py` **40항목 전건 PASS**(이후 M2 반영으로 48 → **50항목**, FAIL 0).
> **C11 coord_mismatch 1건 실발생** — raw → 파서 → 인입 사슬이 처음으로 완주한 지점이다.
> 게이트 4분기 K1~K4가 각각 다르게 동작(K1 direction_unverifiable · K2 커밋 · K3 direction_conflict · K4 후보 0) · **경로 ②(정형 edges) 무비용 통과** — PFMEA causes **13** · affects **9** · occurs_in **8** · controlled_by **11** 전건 커밋.
> `extract/{doc_id}.json`이 CH3A 3.11 스키마 그대로(표면형만 · confidence/span 없음) · 구축 2회 재실행 시 추출 미재호출 · `gate_rejects.json`은 큐가 아니라 로그.
> **자체 검출·수정 4건** — 파일 대소문자 통일 / alias는 정확 일치만(포함 규칙은 canonical에만) / 극성을 문자열 휴리스틱이 아닌 **정체성 규칙**으로 / 부트스트랩 멱등성(골격 재삽입으로 60→72 늘던 것 차단). 장부 등재는 08-10 소급(그 지연이 D-39~D-41 채번 충돌의 원인이었다).
> `layers/quality/config.json`을 이 구간에서 만들었으나 `registration: "registered"`로 두어 **builtin 층은 process 1개 유지** — G4 3′의 층 등록 절차 검증은 그대로 남았다.

```
G3 구간을 진행한다. 증분 0 §3의 단위 정의를 그대로 따른다.

1c′·1d′ 인입 개수 (계약 v2 정합 — 변경점 닫힌 목록 5항)
n3 추출 단계 분리 + 체크포인트 (CH3A 3.11이 계약 정본)
n4 커밋 게이트 + direction_unverifiable

필요 mock: §4.2의 ③ QPPT01, ④ PPT02. §5의 USE_MOCK 확장(추출 힌트·문형 규칙)도 이 구간이다.
config 증분: 각 층 config에 relation_patterns 신설(D-6), quality config의 prompts.extract 훅 활성.
인입 루프에는 D-30의 방어를 넣는다 — 알 수 없는 role은 KeyError로 죽지 말고 invalid_role 결함 로그.

완료판정: 기존 1c·1d 판정 전 항목의 계약 v2 재통과 + coord_mismatch 1건(CP01 C11에서 발생)
+ S2(추출) + S13(attach 청크 밖 해소) + 구축 2회 재실행 시 추출 미재호출
+ S3(게이트: K1→direction_unverifiable, K2→커밋, K3→direction_conflict, K4→후보 0)
+ 회귀: PFMEA의 causes·affects·occurs_in·controlled_by가 게이트 도입 후에도 전부 커밋.

설계 결정이 처음으로 코드가 되는 지점이다. 계약과 조금이라도 어긋나는 판단이 필요하면
가결정으로 넘기지 말고 BLOCKERS.md에 올리고 멈춰라.
```

**당신이 볼 것**: extract/{doc_id}.json이 3.11 스키마 그대로인지(표면형만·confidence/span 없음) · 게이트 4분기가 K1~K4에서 각각 다르게 동작하는지 · gate_rejects.json이 큐가 아니라 로그인지 · **경로 ②(정형 edges)가 무비용 통과하는지** · **C11이 coord_mismatch로 잡히는지**(파서→인입 사슬의 실증).
**참고**: D-1(electrode_type) 최종 확정은 G3 착수 전 권장(카드 C10). 미확정이면 가결정대로 진행.

---

## 정지점 아닌 두 세션 — M2 반입 · M2 후속 정리 (기록 · 프롬프트 없음)

> G3와 G4 사이에 정지점이 아닌 두 세션이 지나갔다. G4 프롬프트는 이 둘의 결과 위에 선다.

**① M2 반입 0~4단계 — 2026-08-12 완료 (PR #5).**
seed **v3.2 반입**(`layers/process/skeleton.json` 신설 — config의 `skeleton`은 선언만) · **loader(n10) 전면 개정**(코드가 아는 것은 구문 마커 4종뿐, 축값은 seed 키에서 읽는다 — `cathode`/`anode` 문자열이 `core/`에 없다) · **n10 기대값 5종 전건 일치**(노드 **46** / part_of **45** / precedes **22** / polarity≠none **16** / mirrors 쌍 **8**) · **1c′ M2 반영**(polarity 닫힌 4값 · A11-9 ①/② 배선 · `strip_polarity()` 폐지로 canonical 극성 파싱 지점 0) · **mock raw 실링 재매핑**(7파일 23행 — 프리 실링 / 사이드 실링). DECISIONS 31 → **48건**.

**② M2 후속 — G4 전 정리 — 2026-08-13 완료.**
대원칙 "core를 늘리지 않는다" 아래 새 큐·새 분기 **0건**. **D-52** 게이트 경로 ① 종결(seed는 후보가 아니라 선언 — 죽은 `PATH_SEED` 제거 · `Process precedes Process` 패턴 제거) · **D-53** seed 키 개명(`PROCESS_TREE`→**`TREE`** · `POLARITY_LABELS`→**`AXIS_LABELS`**) · **D-54** `stale_skeleton` 큐 **미신설**(큐 20종은 닫힌 목록) · **D-55** A11-9 ② **실발화**(CP01 record 12 삽입 — core 0줄, 데이터 1행 · **prefix 11 → 12**).
회귀 현행: `test_g1_g2` **46** · `test_g3` **50** · `verify_roundtrip` **50** — FAIL 0. `run.py all` 2회 = 동일 그래프(process **66노드 105엣지**).
미해소로 남긴 것: **BLOCKERS 판정필요-5**(F3 하향 연쇄 미구현 · A11-9 ①의 전제가 Unit에서 불성립) — **G4 0단계가 그 구현이다.**

---

## 정지점 4 — G4 (회귀와 층 등록 절차 검증) 【마지막 정지점 · 08-13 판 — M2 후속 반영】

```
G4 구간을 진행한다. 증분 0 §3의 단위 정의를 따르되, 증분 0은 13회차(08-07)에 동결되어
M2/A11 반영분이 없다 — CLAUDE.md의 「증분 0 미반영분」이 증분 0보다 우선한다.

착수 전 docs/CH3B_인입규칙.md가 v2.2(틀 v2.7 동기화)인지 확인하고, v2.1이면 멈추고 요청하라
— 0번 ②의 판정 문면은 그 판에만 있다. 틀 v2.7·카드 v14·raw_mock 대장 v2.2·seed 문서 v3.3은
이미 반입되어 있다.

0단계 — 선행 정리 (본편 전에 끝내고 커밋 1회 "판정필요-5 종결 + 장부 정리"):
 ① F3 하향 연쇄를 구현한다. mirrors 짝 키(base_canonical)에서 부모의 축값을 걷어낸다 —
    걷어내는 것은 **부모가 mirror 쌍일 때만**이다(카드 F3 "같은 부모 또는 부모가 mirror 쌍").
    지금은 `탭용접::cathode::용접 강도`와 `탭용접::anode::용접 강도`가 둘 다 있어도 키가 달라
    영영 페어링되지 않고 편측으로 큐에 오른다. 실측 오검출 2건이 0이 되어야 한다.
 ② A11-9 ①의 적용 범위를 스코프 카테고리로 한정한다(틀 v2.7 · CH3B v2.2 3.5 규약 3 · 카드 F1).
    표면형 극성 결합 생략은 주소가 canonical에 들어가는 카테고리(현행 Property)에만 적용하고,
    Unit은 F1 극성 결합을 유지한다. `초음파 융착기`(polarity=none)와 `초음파 융착기`(cathode)가
    같은 canonical로 공존하는 상태가 해소되어야 한다.
    ※ canonical_scope.bind_categories는 건드리지 마라 — Unit 스코프 확대(선택지 가)는 채택되지 않았다.
 ③ BLOCKERS.md 판정필요-5를 종결로 등재한다. 판정은 이미 났고(틀 v2.7) 이번이 그 구현이다.
    ①·②는 core 분기를 늘리는 작업이므로 결과를 DECISIONS.md에 등재한 뒤 인용하라
    (미러·커밋에만 적지 않는다 — 08-10 규칙).
 ④ DECISIONS.md D-15·D-18 행의 CP01 prefix 문면을 11 → 12로 정리한다(D-55 지시 잔여).
    원천 raw_mock_대장은 v2.2에서 이미 갱신됐다 — 남은 것은 레포 장부뿐이다.
 0단계 끝에서 회귀 3종(test_g1_g2 · test_g3 · verify_roundtrip)과 기대값 5종
 (46 / 45 / 22 / 16 / 8), run.py all 2회 동일 그래프를 재확인하라.
 mirror_asymmetry는 1건(`노칭::버 높이`)이 정상이고, ①의 오검출 2건은 사라져야 한다.

본편 — 2′ 질의 회귀 · 3′ 품질층 · 3.5′ 재인입 회귀

2′에는 M2가 얹혔다 — **순서 파생 traverse**(D-44 착지 · 틀 §4B-A11-5 · CH5 5.1 규약 8).
자기 선언 > 부모 파생 + 동일 polarity 인스턴스 하강(없으면 개념 노드 = 공유 스텝 합류) >
"순서 정보 없음"(추측 금지)이며, **파생 답에는 해상도를 표기한다**(fact_template —
"(탭용접 기준) 다음 공정은 …"). 파생·표기는 전부 config 값이다 — core에 넣으면 J3이 깨진다.
precedes는 기본 확장에서 제외하고(5.1 규약 4), flow 특례는 개념 레벨 대표 흐름 체인을 공급한다.

3′의 지위에 주의: 품질층은 내장이 아니라 신규 층 등록 절차의 첫 검증 대상이다(J10).
R1 도구 없이 수동 config 작성 경로로 수행하고, 그 자체가 절차 검증이다.
수동 경로로 수행했음과 그때 필요했던 입력 항목을 PROGRESS.md에 명기하라 — 세션 4(R1)의 재료가 된다.
등록 후에도 registry의 builtin 층은 공정층 1개여야 한다.

J3의 기준점은 **"품질층 등록 직전 커밋"**이다. G3에서 quality config를 미리 만들었으므로
그때 core/가 변경됐는지 git log로 소급 확인하라(layers/quality/config.json이 처음 들어온
커밋을 찾아 그 커밋의 core/ 변경 유무를 본다). 소급분과 이번 구간분을 나누어 적어라.
0단계 ①·②는 판정 이전의 core 변경이므로 기준점 앞에 둔다 — 섞으면 J3이 무의미해진다.

완료판정: 0단계 4항 전건 + 기존 단위 2·3·3.5 판정 전 항목(2홉 도달·causes 사슬·orphan·
Q1~8 회귀 무오염·재인입 중복 0) + 순서 파생 3분기와 해상도 표기가 실제 답변에 나옴
+ git diff core/ 가 비어 있음(J3)을 실제 명령으로 확인해 기록
+ 회귀 3종·기대값 5종·run.py all 2회 동일 그래프 유지.

끝나면 멈추고 보고하라. 보고 순서는 0단계 4항의 실제 수치(오검출 2 → 0 · canonical 중복 해소 ·
장부 채번 목록) → 본편 3종 판정 → J3 결과(기준점 커밋 해시 · 소급 확인 결과 · git diff 출력)
→ 회귀 표다. 마지막 정지점이므로 국면 1 잔여(G5·G6·P트랙)도 한 줄로 적어라.
```

**당신이 볼 것**: **`git diff core/` 출력이 실제로 비었는지** — 이 프로젝트에서 가장 중요한 구조 검증이다. 안 비었으면 어느 결정점이 config로 표현되지 않았는지가 3층 대비 관찰 데이터다(성과이니 기록만 하고 실패로 취급하지 말 것) · **0단계 core 변경이 J3 기준점 앞에서 끝났는지**(섞였으면 판정 무효) · mirror_asymmetry가 오검출 없이 1건으로 내려왔는지 · 순서 파생이 코드 분기가 아니라 traverse 스펙으로 도는지 · 해상도 표기가 답변에 실제로 찍히는지 · 품질층 등록 후에도 builtin 층이 1개인지.
**참고**: 판정필요-5의 판정은 이미 났다(틀 v2.7). G4 0번은 판정이 아니라 **구현 캐치업**이다 — 여기서 새 선택지를 발의하지 마라.

---

## 연속 구간 — G5 + G6 (정지점 아님 — G4 확인 후 한 번에)

```
G5·G6을 연속으로 진행한다. 구간 사이에 멈추지 않는다 — BLOCKERS 사유가 나오면 규칙대로 멈춘다.

G5: n5 I축 도구 4연산 (기존 단위 5 흡수 — 계기판은 4′로 이관됨)
G6: 4′ 플랫폼 연동 (계기판 8종 — CH5 5.5 v3.1) · n9 지문 스캔

필요 mock: §4.3 I축 조작 시나리오(I-1~I-6), mock/raw/CP04_unlabeled.xlsx.

I1(개명)에는 골격 canonical 변경의 정본 경로가 걸린다 — 2경로다(D-54 종결 근거).
 · 개발 국면: 깨끗한 재빌드. seed를 고치고 그래프를 다시 세운다.
 · 운영 국면: I1 개명. **id는 불변이고 옛 canonical은 alias로 강등**한다(P4).
 이 둘 말고 "옛 골격 노드를 지우는" 경로는 없다 — stale_skeleton 큐를 신설하지 않은 이유가 이것이다.

큐 kind는 **20종 닫힌 목록**이다(D-54). 열람에 20종이 다 뜨는지 확인하되,
모자란 사유가 보여도 신설하지 마라 — 새 사유는 defects.log와 loader 출력으로 표면화한다.

완료판정: S4(I축 4연산 + 순환 거부, I-1~I-6 전부) + 기존 단위 5 판정(계기판 제외)
+ 기존 단위 4 판정 + 큐 kind 20종이 큐 열람에 뜸 + 계기판 8종 출력 + S11(지문 스캔).
```

**당신이 볼 것**: 파급 미리보기가 실행 **전에** 나오는지 · I-5 순환이 쓰기 시점에 거부되는지 · I1 개명이 id를 바꾸지 않고 옛 canonical을 alias로 남기는지 · n9가 유일 일치에도 자동 라우팅하지 않는지 · 계기판이 8종 전부인지.
**병행**: 세션 4(M3 — CH4 4.7 · R1 도구 · A9/A11 동기화)가 명세 트랙에서 G5·G6과 나란히 돈다. G4 3′에서 기록한 「수동 config 경로의 입력 항목」이 R1의 재료이므로, G5 착수 시 M3 산출물이 들어와 있는지 확인하고 어긋나면 멈춘다.

---

## 병렬 트랙 — P트랙 (파서 코어 · 킷 · 등록 파이프라인)

> **G3 완료로 이미 열려 있다 — 08-13 현재 미착수.** P3(n6)은 P1(n7) 다음. S1·S10·S12는 P3 완료 후(G3는 이미 끝났다). **S14는 n7 완료판정, S15는 n6·n8 소관.**

```
P트랙을 진행한다. 증분 0 §3의 P1·P2·P3 정의를 따르고, 파서 내부 규격은 docs/파서_명세.md
§3·§5~§9가 정본이다.

착수 직전 준비 1건: 레포의 docs/증분0_구현.md 사본을 프로젝트(claude.ai RAG)의 현행판으로
교체하라. 레포 사본은 13회차(08-07) 동결본이라 M2/A11 반영분이 없다 — 교체 전에는
CLAUDE.md의 「증분 0 미반영분」이 우선한다.

단 아래 가결정이 문서보다 우선한다(DECISIONS 참조):
 - 킷은 6종이다 — 기존 5종 + 실행 하네스 run_adapter.py (D-27).
 - ADAPTER.expects에 header_labels(원본 헤더 문자열의 순서 배열)를 필수 키로 넣는다.
   columns는 출력 필드명 매핑이고, 양식 표류 대조(preflight)는 header_labels로 한다 (D-29).
   이 분리가 없으면 프롬프트가 필드명 변환을 지시한 순간 preflight가 무력화된다.
 - UNMAPPABLE로 판정한 열은 스키마 fields에 넣지 않고 어댑터 출력에서도 제외한다 (D-30).
 - electrode_type은 **구조 필드**다 — record에서만 읽고 노드에는 쓰지 않는다. 노드 쪽은
   polarity 닫힌 4값(cathode/anode/none/unbound)이다 (D-43).
 - **구조 필드는 role 6지선다의 대상이 아니다** (D-46). 6지선다에 태우지 마라.
 - fixture는 mock/fixtures/의 LLM 실산출물을 쓴다. 손으로 쓴 fixture는 리허설을 무효화한다 (D-26).

P1: n7 파서 공용 코어 6종 + 생성 하네스 (parser/ 별도 패키지, openpyxl·python-pptx 허용 — D-9)
    반입된 parser/reader.py를 출발점으로 삼되 계약 대조 후 채택 여부를 판단하라.
    완료판정에 S14 포함 — mock/raw/의 CP01·PFMEA01을 파싱한 records의 prefix
    (CP01 12 · PFMEA01 13)가 mock/parsed/*.json과 일치해야 한다(D-18).
    ※ CP01은 11이 아니라 12다 — A11-9 ② 실발화 행 삽입분(D-55)이 prefix 안에 들어간다.
    실행 시점 값(parsed_at·source_path·parser_version·adapter_version)과 process_no는 비교 제외.
P2: n8 어댑터 생성 킷 6종. kit/에 템플릿 v0.1·v0.2가 이미 있다 —
    D-29·D-30·D-46을 반영한 **v0.3으로 개정**하며 작성한다. v0.2를 다시 만들지 마라.
    실행 하네스는 4단(로드·preflight·extract·스키마 정합)이며 실행검증_1차 §2가 규격이다.
    하네스에 use_blocks 로더를 붙여 common_core·process_coord를 조립한 뒤 드라이런한다(§4.4 한계 해소).
P3: n6 구축 모드 등록 파이프라인 (생성 → 실행 하네스 → 검수 → 확정)
    S15 포함: 신규 정형 doc_type ipqc(IPQC01 16열 = 유효 11 + meta 3 + UNMAPPABLE 2)를
    등록 전 과정으로 통과시킨다. role 6지선다와 UNMAPPABLE 판정이 여기서만 검증된다(D-20).

세 단위 각각이 끝날 때마다 중간 보고하고, P3까지 끝나면 멈추고 종합 보고하라.
```

**당신이 볼 것**: **S14가 손으로 만든 JSON이 아니라 raw 실물 파싱으로 통과**하는지 · CP01 prefix가 12로 맞는지 · header_labels가 실제로 표류를 잡는지(CP02_drift) · 검수 뷰가 코드가 아니라 결과 뷰인지(3구획·3층) · S15에서 UNMAPPABLE 2열이 스키마에서 빠지는지 · 구조 필드가 6지선다에 섞이지 않는지 · 어댑터가 순수 함수인지.

---

## 전체 완료 판정 (증분 0 §8)

시나리오 **15종(S1~S15)** + 기존 단위 판정 전부 + `git diff core/` 빈 것(J3) + `run.py all` 2회 동일 그래프.

- **J3의 기준점은 「품질층 등록 직전 커밋」이다** — G4에서 확정한다. G3에서 `layers/quality/config.json`을 미리 만들었으므로 그 커밋의 core/ 변경 유무를 소급 확인하고, 소급분과 G4 구간분을 나누어 적는다. G4 0단계의 core 변경(F3 하향 연쇄 · A11-9 ① 범위 한정)은 판정 이전 작업이므로 기준점 **앞**에 둔다.
- **`run.py all` 2회 동일 그래프의 현행 수치는 process 66노드 105엣지다**(08-13 기준). 이 수치가 바뀌었다면 골격·인입 중 무엇이 움직였는지 먼저 밝히고 나서 판정한다.
- 회귀 3종의 현행 기준선: `test_g1_g2` 46 · `test_g3` 50 · `verify_roundtrip` 50 — FAIL 0. n10 기대값 5종: 46 / 45 / 22 / 16 / 8.

이 넷이 다 서면 국면 1 종료다. 다음은 국면 2(사내 이식) — 첫 작업은 코딩이 아니라 role 배정 실험(CH2 2.6).