# 이연 대장 — 미룬 것이 어디에 있나

> **왜 이 파일이 있나.** 이연 항목이 개정대장·BLOCKERS·감사 결과·대화에 흩어져 있어서, 세션이 바뀌거나 컨텍스트가 압축되면 **요약본에 기대게 된다.** 이 파일 하나만 읽으면 미룬 것이 전부 보이게 한다.
> **갱신 규칙**: 회차를 닫을 때 여기부터 본다. 닫힌 항목은 지우지 말고 「닫힘」 줄을 단다 — 왜 미뤘고 언제 풀렸는지가 판단 이력이다.
> **최종 갱신**: 2026-09-15 (**B67·B68 닫힘** 1173/1173 · 1-h 좌표 태깅 실측 → **B69 발주** · 다음은 사내 이식 B66~B69)

---

## 1. 감사 2차 — 아직 안 닫힌 것

감사 2차: 22절 · 검출 254건(high 27 · med 110 · low 117). high만 반박 검증했다(확정 26 · 부분 1 · 반박 0). **med/low 227건은 검증조차 안 됐다.**

| Bucket | Count | Where | State |
|---|---|---|---|
| A군 — 코드만 | 8 | B55 요청문 | **완료** (`b24a365`, 942/942) |
| B군 — 명세 판정 먼저 | 5 | B56 판정문 → §AL | **완료** (`9a11924`, 973/973) |
| C군 — 영향 재확인 먼저 | 아래 | 감사2차_결과.md | **미착수** |
| med / low | 227 | 감사2차_결과.md | **미검증 원자료** |

### C군 — 미착수 (중복 정리 후 실질 10자리)

| ID | What | Step |
|---|---|---|
| H2 · H22 · H25 | 파서 측 실패(`parse_failure`·`adapter_mismatch`)가 공유 큐에 착지하지 않는다 — 화면 출력만 | Parse |
| H3 | `hierarchy_unresolved` 큐 kind가 코드 어디서도 적재되지 않는다 | Parse |
| H5 | 비정형(prose)이 B14(좌표 없는 노드 미생성)를 구현하지 않는다 | Build |
| H6 | orphan 재시도가 `dropped_edges`를 좌표 층 config로 게이트 — 걸침 문서 엣지가 거부된 뒤 큐가 self-heal로 사라진다 | Build |
| H7 | 재시도가 `pending_attrs`를 좌표 노드에 저장 — 관리항목 규격이 세부공정 Process에 붙는다 | Build |
| H8 | `category_pair_map` 조회 순서 고정 — 비정형 Unit이 공정에 `part_of`로 못 붙는다 | Build |
| H9 · H10 | 사전 히트 경로가 `is_live`를 안 거쳐 폐기(I4) 노드가 1.0 MATCH로 부활 | Build |
| H11 | I축 연산 대상 판정이 `is_live`를 써서 폐기 노드의 개명·재폐기가 거부되고 KeyError | Ops |
| H12 | 상시 kind 종결 경로 없음 — `confirmed` 생산자 0, `resolve_item` 호출부 0 | Ops |
| H27 | 재등록이 **generate 진입**(`register.py:655`)에서 「이름 중복」으로 막힘 | **Register — B58 ①로 닫는다** |

**H27만 지금 단계에 물린다.** 나머지는 파싱·구축·운영 단계다.

### 스텝별 분포 — 언제 물리나

| Step | Findings |
|---|---|
| Register | H19 · H20(닫힘) · H21 · H23 · **H27** |
| Parse | H2/H22/H25 · H3 · H17 · H18 |
| Extract | H13 |
| Build | H1 · H4/H15 · H26 · H5 · H6 · H7 · H8 · H9/H10 |
| Query | H14 |
| Ops · gauges | H11 · H12 · H16 · H24 |

---

## 1-a. 실측이 찾은 것 — B59

| # | Found | Fix |
|---|---|---|
| 1 | 관문 FAIL 뒤 `confirm`·`review`가 막다른 길 — 이유도 다음 명령도 없다 | B59 ① |
| 2 | 「검수」가 기계 관문과 사람 검수 둘 다를 가리킨다 | B59 ① |
| 3 | B58 ②가 관문을 넓혔는데 `AUTO_FIX`는 8줄 그대로 — 새 실패가 전부 문답으로 | B59 ② |
| 4 | prose 표본인데 LLM 생성으로 들어가 FAIL — `--use-basic`을 화면이 안 권했다 | B59 ③ |
| 5 | 사내는 복사가 안 된다 — FAIL 줄을 읽어서 전달할 태그가 없었다 | B59 ① |

**B59로 닫혔다** (1051/1051). 처분 셋째 갈래 `GATE_SELF`는 구현 세션이 세웠다(D-135 ③).

**실측 둘째 → B60**:

| # | Found | Fix |
|---|---|---|
| 6 | **B59 회귀** — 태그 없는 옛 판정 줄을 못 읽고 「생성을 다시 돌려라」. 어댑터가 디스크에 있는데 | B60 ① — 관문은 저장값을 안 믿고 다시 돈다 |
| 7 | `--resume`이 문답 전문을 다시 받아 이상한 결론 — 라운드가 쌓이면 모델이 「결국 뭐로 정해졌나」를 대화에서 재구성한다 | B60 ② — **확정 요약(`decisions`)이 생성의 입력**. 대화는 이력, 판단은 정본 |

**B60 닫힘** (1065/1065 · D-136·D-137 채택).

**B61 발주** — 같은 모양이 남은 자리를 구조로 짚었다. 사람이 상태를 이유로 거부당하는 자리는 가이드 단계마다 하나씩:

| Step | Site | State |
|---|---|---|
| §2 골격 | `skeleton confirm` 「seed를 고쳐라」 — 어느 줄·어느 규칙 불명 | **B61 ②** |
| §4 등록 | 관문 FAIL | B59·B60 닫힘 |
| §5 인입 | 문서 단위 실패 — 화면에 행·이유 불명 · `scan` 불일치 다음 줄이 동사 | **B61 ③** |
| §6 질의 | 상태 거부 없음 | — |

**규칙**(B61 ①): 상태 거부는 원인 + 완성된 다음 명령 + 지금 잰 것. 어서션으로 잠가 다음 자리도 잡는다.


**실측 셋째 — B61 발주 뒤에 나온 것 (다음 회차 후보)**:

| # | Found | Fix |
|---|---|---|
| 8 | 관문 FAIL → 자동 재생성 1회 → 여전히 FAIL 뒤 화면이 「더 돌릴까? [y/N]」 한 줄. 이미 한 번 고쳤다는 것, y가 문답을 여는지, N이 FAIL로 끝나 confirm이 막힌다는 것을 말하지 않는다. **기본값 N이 막다른 길** — B59가 닫은 모양이 한 겹 안쪽(`_ask_more`)에 남아 있었다 | **B62 ④로 발주** (2026-09-14). `grep -n '더 돌릴까' cli/register.py` |
| 9 | B60 이전 코드로 만든 `input_package.json`은 `rounds`만 있고 `decisions`가 없다 → `--resume`이 「확정 사항 없음」으로 조용히 빈 초안을 낸다 | **B62 ②로 해소** (2026-09-14) — 로드 경로에서 경고 한 줄 + 다음 줄 둘, 죽지 않고 진행한다. `test_p3` 「②ⓓ decisions 없는 옛 패키지」 |
| 10 | 가이드·화면이 `python -m cli.register`와 `python run.py register`를 섞어 쓴다 — 태그를 읽어 옮기는 사람에게 두 명령으로 보인다 | **B61 ④** |

**교훈**: 1040/1040이 초록인 동안 이 다섯이 막혀 있었다. 어서션이 못 보는 자리는 **사람이 명령을 치는 자리**다.

## 1-c. 실측이 찾은 것 — 2026-09-14 (CSV 표본 · 헤더 대조)

| # | Found | Where it went |
|---|---|---|
| 1 | preflight `header_labels()`가 xlsx가 아니면 `[]` → **CSV table 어댑터는 관문을 통과할 수 없었다** | B62 ①-a |
| 2 | 시트 0 고정(preflight·`_label_columns`·스켈레톤·참조어댑터) — `expects.sheet` 선언 자리 없음 | B62 ①-b |
| 3 | `header_labels`를 LLM이 받아 적고 시스템이 글자로 대조 | B62 ①-c |
| 4 | 생성 user 메시지 = 패키지 원문 통째(라운드 전문 포함) · 문답은 전문 2회 전송 — B60 ②가 반만 구현 | B62 ② |
| 5 | `adapter_version` 아무도 안 올림 — `adapter_rev2.py` 안이 `1.0` | B62 ③ |
| 6 | 화면 열 이름 「Cent」(원본 「Center」) — 출처 미확인. ① 뒤에도 남으면 리더(`_delimiter`·인코딩) | B62 ① 사내 확인 항목 |
| 7 | 코드 안에 사는 지시문(coord_tag 리터럴) · 코드가 덧붙이는 것(어휘 발췌·config prompts.judge·정의문) · 출력 스키마 5개 — 05 지도에 없다. 번호 체계가 셋(①~⑨ · 01~11 · P/Q/R) | **B63 닫힘** (2026-09-14, PR #44) — `00_칸_대장.md` · `prompts/<ID>_` 9개 · point==대장 어서션 |
| 8 | 어댑터 자산이 네 폴더(kit · prompts · review · parser/adapters)에 흩어짐 | 결정(2026-09-14): 파트 4 · 칸 28 · 코드 폴더는 안 옮긴다 · 자산만(B63 ①). 확정 어댑터 묶음 `adapters/<dt>/`는 별도 회차 |
| 9 | 옛 패키지(B60 이전) → `decisions` 없음 → `--resume`이 빈 `[확정 사항]`으로 조용히 감 | **B63 ③** |

| 10 | 상태 거부 문면 계약의 문서 1 조항화 | 지금은 문서 7 §7.4 + 회귀(`exits_scan`)가 지킨다. 문서 1에 조항을 세우려면 미러쌍 봉인이 한 쌍 는다 — 다음 미러 봉인 회차에 같이 |

## 1-d. 실측이 찾은 것 — 2026-09-15 (CSV · `expects.columns`)

| # | Found | Where it went |
|---|---|---|
| 1 | G26 `empty_columns`에 헤더 라벨 「Center」가 그대로 — LLM이 열문자 자리에 라벨을 썼고 문면은 「header_row 의심」 → 문답 LLM이 사람에게 뜻을 되물음 | B64 ①② |
| 2 | 같은 이름의 열 둘 + 「둘 다면 첫째」 규칙을 `columns`가 표현 못 함 · `_label_columns` dict가 둘째 열을 삼킴 | B64 ①③ |
| 3 | 관문 화면에 열별 「몇 값이 읽혔나」가 없다 — 패키지에는 있다 | B64 ② |

| 4 | `--resume` 전송이 사내에서 약 200,000B — `interview_log.json` 없음. 옛 코드(전문 인라인)인지 본체 부피(골격 닫힌 목록 전량·reader_head)인지 미확인. 감축 순서 셋(§6.5 B41)에 **골격 목록 범위**가 없다 | 사내 확인: `system` 키별 바이트(가이드 §7). 숫자 뒤 **B65 후보** — 골격 목록을 「표본 셀 값과 겹치는 노드만」(시스템이 결정적으로 필터) 또는 canonical만 |

## 1-e. 실측이 찾은 것 — 2026-09-15 (PFMEA)

| # | Found | Where it went |
|---|---|---|
| 1 | **닫힘 B65 ①** — G31 `AttributeError: normalizer.col_to_letter` — 규약 10이 열문자 변환을 normalizer 몫이라 하며 이름을 안 줌(실물은 비공개 `_col`). 실행 단계에서야 예외로 잡혀 문답행 | **B65 ①** — 로드 단 G1B 정적 대조 + 규약 10에서 열문자 변환 삭제(템플릿 v1.5) |
| 2 | **닫힘 B65 ②** — 스키마의 category·relation·삼항이 층 닫힌 목록 밖이어도 등록은 통과하고 인입 때 커밋 게이트가 거른다 | **B65 ②** — G4C·G4D·G4E |
| 3 | PFMEA 원인 열 — 짧은 원인은 entity(Failure)+`causes`로 됨. 문장형 셀(「금형 이물 고착에 따라 punch 동작 이상」)은 entity도 content도 못 받는다 — table content에 추출을 켜는 스키마 속성 `extract: true`(문서 2 §2.4 ④ 개정) | **사용자 확정 대기** — 확정되면 B66 |
| 4 | 골격 확장 — 문서에서 골격급 공정이 나와도 자동 승격 없음(설계 의도). 「골격 후보 제안」(orphan_anchor 같은 표기 N건 → seed 줄 초안) | 사용자 결정: 당분간 사람 수정. 후보 제안은 나중에 |
| 5 | 문답 한 라운드 질문 수 — 명세 「하나」(B60) vs 코드·지시문 ≤3 + 진행 줄 | 허브 제안: ≤3 + 진행 줄로 명세를 코드에 맞춤. **사용자 확정 대기** |
| 6 | 사내 코드가 B60~B64 이전 — `interview_log.json` 없음 · G52 adapter_mismatch · 열 요약 없음 | 이식: `origin/main`(B64까지 PR #47) |

## 1-f. 실측이 찾은 것 — 2026-09-15 (CSV 인입 스캔)

| # | Found | Where it went |
|---|---|---|
| 1 | **닫힘 B66 ①②**(`dcbfa64` · 1144/1144) — 확정된 CSV 어댑터가 `ingest-file --dry-run`(doc-type 없이)에서 「대조할 정형 어댑터가 없다」 — `cli/scan.py::FINGERPRINTABLE = ("xlsx",)`가 CSV를 지문 대상에서 뺐고 `select`가 `not_fingerprintable`를 안 읽음. B62 ①-a와 같은 병의 셋째 자리(B62 대조표에 없었다 — 허브 누락). 명세(문서 6 §6.4 「포맷을 보지 않는다」·C37)는 이미 맞다. 허브 실측: `--doc-type` 지정이면 CSV가 generate→confirm→ingest 성공 record 4 — 막힌 자리는 이 한 줄뿐 | **B66 ①②** |
| 2 | **닫힘 B66 ①ⓑⓒ·⑤** — `CP01.xlsx`↔`CP01.csv` 등가(records 30 · 노드 86 · 엣지 133 · 스모크 11문항 동일) — 회귀가 CSV 표본으로 스캔을 한 번도 안 돌렸다 — 포맷 분기 잔재를 어서션이 못 잡는 구조. build 노드·질의도 CSV 출처로 돌린 적 없고 xlsx와 내용이 같은 CSV 표본이 없다 | **B66 ①ⓑⓒ**(xlsx·csv 동일 판정) · **B66 ⑤**(전 구간 등가 — parsed·그래프·골든 답). tsv는 사내에 없어 제외(사용자 결정) |
| 3 | **닫힘 B66 ③** — 확정 화면 「다음」이 `parse run`·`build`(가이드 흐름과 다름) | B66 ③ — `ingest-file --dry-run` → `ingest-file` 두 줄 |
| 4 | **닫힘 B66 ④** — 「mock 소재지」 — 내장 doc_type(cp·pfmea·ipqc)의 어댑터가 `tests/fixtures/adapters/`에 살고 등록부 뒤에 뒤진다. 화면이 어느 실물을 골랐는지 안 말한다 | B66 ④ 선택 근거에 `← 어댑터 경로` · 소재지 폐지는 별도 판단 |
| 5 | **닫힘** — 06 손잡이 대장에 0.2(스캔) 행이 없었다 — 대장 밖 손잡이 | 허브가 0.2 행 신설(2026-09-15) |

## 1-g. 실측이 찾은 것 — 2026-09-15 (`--resume` · G31 재현)

| # | Found | Where it went |
|---|---|---|
| 1 | **닫힘 B67 ①**(`b4c3fdb` · 1158/1158) — G31→G52로 끝난 등록을 `--resume`하면 같은 G31 — resume이 `draft(doc_type)`를 지시·이력 없이 부르고 온도 0이라 같은 코드가 나온다. 이어하기 = 재현 | **B67 ①** — 초안 있으면 관문 먼저, FAIL 문면+이력을 지시로 |
| 2 | **닫힘 B67 ②③** — 생성이 열마다 내린 판단(role·필드↔열·unmappable)이 코드 안에만 산다 — 코드를 버리면 판단도, 살리면 오류도 | **B67 ②** — 열 판정 대장 `columns.json`(시스템이 적는다 · C38) · 템플릿 열 단위 렌더 |
| 3 | G31 자체는 B65 ① G1B가 로드에서 잡는다 — 사내 코드는 B65 이전 | 이식: `origin/main`(#49) |
| 4 | **닫힘 B68**(`6aa4324` · 1173/1173) — 산문 xlsx 고정 어댑터 등록 성공 — 청크는 잘 잘렸으나 「무엇을 기준으로 잘랐나」가 뷰에 없고 generate 화면에 분할 정보 0. 헤딩 신호 판정 결과(어느 신호였나)를 `_rows_of`가 버린다 | **B68 ①②** — 분할_기준·지도_출처 기록(LLM 0) · generate/status 한 줄/프레임 · 뷰 열 하나 |

## 1-h. 실측이 찾은 것 — 2026-09-15 (인입 좌표 태깅)

| # | Found | Where it went |
|---|---|---|
| 1 | `parse run`에서 `LLM 사용량 coord_tag` 로그 30줄+ — 「무한」으로 읽고 껐다. 실은 미스 **행마다** 1회(같은 표기도 반복) · 인입 경로에 예고·진행·상한 없음. B22가 등록 리허설에서만 막았다 | **B69 ①②③** — 표기당 1회 · 예고/진행 줄 · `--coord-llm` 상한(기본 100종) |
| 2 | 사내 이식본이 main(#49 = B65)이라 B66(CSV 스캔)이 없다 — 「지문 일치 0건」이 그대로 | 이식: B66~B69를 main으로(PR) 뒤. 그 전엔 `--doc-type` |
| 3 | 없는 명령(`run.py doctor`)을 다음 줄로 준 상태 거부 — 스캐너가 접두만 봄(D-147 ④) | **B69 ④** — 명령 실재 어서션 |
| 4 | 좌표 태깅(2.8, 파서)과 동일성 판정(3.4, 구축)이 같은 문제(표기→노드)를 두 자리에서 푼다 — 사전 alias가 태깅에 안 쓰인다(P1: 파서는 core 미import) | 별도 판단 — 숫자(B69 ② 예고 줄의 목록 밖 종수) 뒤 |

## 1-b. B58 잔여 — 실측 뒤로 미룬 것

| # | What | Why deferred |
|---|---|---|
| ⑦ | `prompts/extract.md` 빈 산출 명문화 | 문면 한 줄. 실측이 안 막는다 |
| ⑧ | `payload_kind` 화면 문면 | 문면 |
| ⑨ | 미러 `kind: item` · `doctor.snap` 경로 | 검사 도구 정비 |
| ⑩ | 관문 소요 계측 | 진단 — 실측 중 체감으로 판단 |

**⑥까지 하고 멈췄다** (`c44912e`). 사용자 판단: 어서션이 늘고 새 기능이 붙는 동안 실문서는 0건이었다. 도구를 만들고 한 번도 안 써 봤다. **구현 세션은 대기 — 실측에서 막히는 것이 다음 요청문이 된다.**

## 2. 사내 실측 대기 — [사내 확인]

| # | What | Blocks |
|---|---|---|
| 1 | **실호출 doc_type 1종 등록 완주** — 국면 2A 완료 조건 ① | 국면 2A 종결 · P7 판정 재료 전반 |
| 2 | **골든셋 실물 문항 120건 제작**(`golden init` 틀은 섰다 — 문서 5 §5.5-4) | 계기판 1 분모 교체 · BM-25 대조 · 랭킹 고도화 트리거 |
| 3 | 실문서 기반 med/low 검증 우선순위 판단 | 감사 잔여 227건 |

---

## 3. 명세에 못박아야 하는데 아직 안 한 것

| # | What | Note |
|---|---|---|
| 1 | **국면 2A 완료 조건 4개를 문서 7 §7.6에 못박기** | 사용자 미확정 — 조건 자체는 대화에서 합의됐으나 문면이 없다 |
| 2 | `docs/국면2_명세공백_인계.md` 잔여 공백 | B56-2가 좌표 null 건을 닫았다. 나머지 대조 필요 |
| 4 | **허브 포장 규율 2건**(§AN) — `DECISIONS.md` 통째 배포 금지 · 미러 봉인은 회차 맨 마지막 | 이번에 둘 다 사고가 났다. 규율은 §AN에 있고 여기 색인만 둔다 |
| 5 | **근거 동일성의 정의**(D-145 ②) — 근거는 id 문자열이 아니라 가리키는 자리(doc_id · 시트 · 행/shape)로 같다고 본다. `chunk_id`는 로케이터 해시, 노드 id는 ULID라 같은 문서를 두 번 돌려도 다르다 | B66 ⑤가 어서션으로 쓴 정의. 문서 5(읽기절차)에 한 줄 — 2026-09-15 사용자 승인(hub_14) → **다음 미러 봉인 회차에 문서 5 반영**. D-146 ②④(대장 ← 관문 · 사람 판단 보존)는 문서 6 §6.5에 반영됨(§BD) |
| 3 | `BLOCKERS.md` 67건 | 국면 1~2A 누적. 상당수가 이미 풀렸을 것 — 일괄 대조가 필요하다 |

---

| C38 ↔ 문서 6 §6.5 스탬프 문단 미러 봉인 | **닫힘** 2026-09-15 — 7쌍 |

| 하네스 태그 1:1 규칙(한 태그·한 라벨·원인은 상세)을 문서 7 §7.6에 명문화 | 세 번 깨질 뻔함(G22·G26·G4C). 다음 미러 봉인 회차에 |

## 4. 코드 후속 — 다음 회차에 묶기로 한 작은 것들

| # | What | From |
|---|---|---|
| 1 | `TYPE_PATH` 닫힌 값 축소(Q6·Q7 제거) | 개정대장 B54-2 |
| 2 | 생성 템플릿 v1.2 때 `{{사용자 자유 텍스트 힌트가…}}` → `{{힌트}}` | B55 ①-후속-2 검토 중 발견. 산 자리이나 **이름이 문장이라 설명으로 오독된다**(실제로 허브가 오독했다) |
| 3 | 생성 템플릿을 `payload_kind`로 **조건부 렌더** | B58 이연 — v1.0은 3차 블라인드 판정 통과 문면이고 분기 문면은 이미 있다. **트리거: prose 등록 3건에서 role 오적용이 실제로 관찰될 때** |
| 4 | attribute 숫자 눈금(「5~10개 선」) | B58 이연 — 사내 표본 0건. **트리거: 표본 3~5건 관찰** |
| 5 | `CHUNK_MIN/MAX = 5, 40` 조정 | B58 이연 — 분포를 보고 (P7) |
| 6 | 형태 판정의 LLM 보조 제안 | B58 이연 — **트리거: 애매 구간 비율이 인입 기록으로 실측될 때.** 자리는 라우터가 아니라 사람 화면의 보조 |
| 7 | `.docx` reader 지원 | `SUPPORTED`에 없음. 별건 |
| 8 | **생성 프롬프트의 규약 10 문면 강화** | `tests/fixtures/adapters/ipqc` 실산출 스냅샷이 `_expand_merged`·`_col_to_idx`·`_idx_to_col`를 재구현하고 `normalizer` 호출 0건이다 — 문면이 실호출에서 안 먹혔다는 증거. **트리거: 사내 실측에서 같은 재구현이 또 나올 때**(Rule of Three). 스냅샷은 고치지 않는다(D-26) |
| 9 | **기계 관문 소요 시간 계측·단축** | B58 ②로 관문이 `pipeline.parse` 전 구간을 돌면서 체감이 길어졌다. **먼저 재고, 렌더가 범인이면 끈다** — 관문은 LLM 주입 0이라 렌더 산출을 쓸 데가 없다(만들고 버린다). 화면에 관문 소요를 찍는 것이 시작이다 |

---

| 9 | **`input()`이 EOF 없는 stdin에서 영원히 기다린다** | `doctor.py`를 닫히지 않는 파이프로 돌리면 `review`의 `[Y/n]`에서 멈춘다. `except EOFError`만으로는 안 잡힌다 — `sys.stdin.isatty()` 검사 또는 타임아웃. B51 코드, 실측 B62 ②. 트리거: 사내 자동 실행에서 같은 멈춤이 나올 때 |

## 5. 국면 밖으로 미룬 것 (P7 — 측정 없는 구조 선반영 금지)

트리거가 오기 전에는 손대지 않는다. 정본은 각 명세 절이고 여기는 색인이다.

| What | Trigger | Spec |
|---|---|---|
| 임베딩 3단 링킹 | 사전+LLM 2단의 미스율 실측 | 문서 5 §5.1-1 · §5.4 |
| 벡터DB 사다리 (numpy → faiss → 전용) | 청크 10만 / 수십만+ / 백만+ | 문서 5 §5.4 |
| 저장 전환 R10 | 층별 200MB 또는 build 30초 | 문서 7 · 계기판 7·8 |
| 분화 축 일반화(`identity_axis` config) | 극성 외 두 번째 분화 축 관찰(Rule of Three) | 문서 3 §3.9-⑻ · 문서 4 §4.5 |
| I축 파급 정리 자동화(삽입에 딸린 이동·재부착 한 트랜잭션) | I축 4연산 운용 실측 | 문서 4 §4.9 |
| 걸침 문서 병합 규칙 마감 | — (이게 마감돼야 `canonical_scope` 대상을 또 바꿀 수 있다) | 문서 1 F13 |
| `is_cc` 등 구조체 내부 플래그의 노드 승격 | 감사 질의 수요 확인 | 문서 1 F16 |
| 플랫폼 본체 | 별도 레포 · 뷰어(B52)는 시스템 안 검증 창구로 따로 섰다 | `docs/안건/플랫폼*` |

---

## 6. 닫힌 것 (기록 보존)

| What | Closed by |
|---|---|
| 분할 레벨을 등록 때 상수로 박는 규정 | [정정] 46 — 인입마다 규칙이 계산한다 |
| `doctor.snap()` 바이트 대조가 B6 예외인지 (D-126 ③) | [정정] 47 — 위반 아님. 경로는 GraphStore에서 받는다 |
| 골든셋 `--k`의 뜻 | 이미 닫혀 있었다 — 문서 5 §5.5-4 (B54-2 ③) |
| prose 어댑터를 doc_type마다 생성하는 경로 | [개정] B58-2 — 포맷별 고정본 |
| table/prose 갈래를 사람이 선언하던 것 | [개정] B58-3 — 결정적 형태 판정 |
| 리허설 인입 규칙 — 「id만 계산, 적재 안 함」 갈래 | 철회. 문면으로 닫음(문서 6 §6.6) — 코드 0줄 |
| 좌표 null 연쇄 드롭의 처분 공백 | [개정] B56-2 |
| provenance 접두 — 부록↔문서 4 충돌 | [정정] 43 |
| 계기판 7·8 자리 — 표↔규율 모순 | [정정] 44 |
| F1 Unit 스코프 · D-57 반전 미표기 | [정정] 45 |
