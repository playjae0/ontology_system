---
doc: 파서_명세
version: v0.5.1 (2026-07-31 — 감사 4 정정 1건: §2 표본 관찰 범위 오기["정형: head 10~30줄" → 정형 ~10줄 / 비정형 10~30줄 — 카드 M5·틀 Q2 정합]. v0.5 = 세션 N ⑺ 확정: §7 전면 개정(3구획·3층 표시·재생성 루프 상한 없음·JSON/HTML 분리·층 초안 구획 이관). v0.4 = ⑵ 확정(§6), v0.3 = ⑴ 확정(§5))
status: §1~§7·§9(경계)는 확정. 잔여는 §9 생성 킷의 실물 작성뿐[보완필요⑧ — 규격은 전부 확정됨]
supersedes: 파서_인수인계.md (v1.10 시점 문서 — 계약 필드·청크 경계 서술이 낡음. 본 문서가 대체하되, mock 예시·동기 부여 절은 참조 가치 유지)
relation: 계약 3종의 정본은 CH2(에이전트 측)다. 이 문서는 파서 "내부"의 정본이며 CH2 2.1~2.3을 상속 전제로 참조한다.
---

# 파서 명세

> 파서는 사내 구현이다. 이 문서는 파서를 사내에서 구현하는 사람(그리고 어댑터를 생성하는 LLM)이 보는 정본으로, **경계(무엇을 지켜야 하나)는 CH2에서 상속**하고 **내부(어떻게 만드나)는 여기서 정한다.**

---

## §1 지위와 경계 [확정 — CH2 승계]

```
사내 문서(Excel/PPT/Word/PDF)
   ▼  ── 파서 (사내, 이 문서의 대상) ──
파서 출력 JSON  ◄── 유일한 계약 지점 (계약 3종 — CH2 2.2)
   ▼  온톨로지 에이전트 (외부 설계 — core 파이프라인)
```

1. 파서에 대한 요구는 "예쁜 파싱"이 아니라 **계약 준수**다: ①메타데이터 양식 3층 구조 ②공정좌표 태깅 의무 ③레코드 자기완결성(상동 해소·병합 셀 전개·복수값 분리·nested 전개). 정본은 CH2 2.2.
2. **파서가 내는 id는 `doc_id`와 `source_locator`뿐이다**(틀 A7-1). 정본 chunk_id·record_id는 에이전트가 계산한다. ~~조각 공통의 `chunk_id`~~는 `source_locator`로 개명되었다 — **인수인계 문서(v1.10 시점)의 mock 예시에 있는 `chunk_id` 필드는 전부 `source_locator`로 읽어야 한다.**
3. 파서가 하지 않는 것: 개체 추출·판정(에이전트), 관계 결정(스키마 edges), 카테고리 발명(사람), 그래프 id 부여(에이전트).
4. 좌표 오태깅은 시스템이 anchor 미스 → `orphan_anchor` 큐로 받는다 — 파서 LLM은 "완벽"보다 **"일관"**이 중요하다.

## §2 파서의 두 모드 대응 [확정 — 틀 §2·Q2 승계]

| 모드 | 파서에서 일어나는 일 |
|---|---|
| **구축 모드** (doc_type 미등록) | LLM이 표본을 관찰(**정형: head 약 10줄 / 비정형: head 10~30줄 기본, 목차형은 계층 패턴 2~3회 반복까지 확장** — 카드 M5)하고 **어댑터 코드를 생성**한다 — 정형: 어느 행/열을 어떻게 읽는가 / 비정형: **청크 분할 규칙** + 이미지 placeholder. 분할이 자명한 계열(PPT 등)은 생성 없이 **코어 기본 어댑터**를 붙인다(§5 규약 5). 사람이 결과 뷰로 검수(§7) 후 등록 |
| **운영 모드** (등록됨) | 등록된 어댑터의 **결정적 실행** (실행 전 preflight 정합 검사 — §5 규약 6). 문서마다 LLM이 구조를 다시 판단하지 않는다. 운영 중 LLM은 두 지점뿐: 이미지 요약(코어 소유 — §5 규약 3), 좌표 태깅(닫힌 목록 선택) |

- **청크 경계는 "파서 자유"가 아니다** — 인수인계 문서의 해당 서술은 낡았다. 경계 판단은 구축 모드에서 1회(LLM 생성 → 검수 → 고정), 운영 중에는 고정된 규칙의 실행이다(틀 Q2).
- 판단이 안 서면 LLM은 추측으로 규칙을 만들지 않고 **"판정 불가"로 검수 화면에 올린다.** 폴백: 문서 계층 ≠ 그래프 계층 — 평면으로 읽고 좌표만 태깅해도 그래프는 성립한다(골격이 계층을 이미 보유).

## §3 내부 구조 — 3단 + 어댑터 [가결정 — 인수인계 §6 승계·개정, preflight는 세션 N 신설]

```
[reader]     원본 → 원시 추출   : 포맷별 (xlsx/pptx/docx/pdf) — 공용, 어댑터 아님
[preflight]  실행 전 정합 검사   : 문서 지문 ↔ ADAPTER.expects 결정적 대조 — 공용 (§5 규약 6)
[adapter]    원시 → 정규 record/chunk : doc_type별 — LLM 생성 대상 또는 코어 기본 어댑터 (§5)
[normalizer] 자기완결 보정      : 상동 해소·병합 전개·복수값/nested 분리 — 공용
[tagger]     정규 → 계약 JSON   : 공정좌표 태깅(골격 닫힌 목록 조회) + 봉투 + context + 이미지 placeholder의 요약 완성 — 공용
[validator]  계약 self-check    : 3층 구조·좌표 존재·자기완결 검사 — 공용 (§8)
```

1. **포맷 의존은 reader에만, doc_type 의존은 adapter에만 격리한다.** 새 doc_type = 어댑터 하나 추가(에이전트의 config-only와 같은 결). reader·preflight·normalizer·tagger·validator는 공용 코어로 사람이 1회 작성·유지한다 — **LLM이 생성하는 것은 adapter뿐이고, 그마저 분할 자명 계열은 코어 기본 어댑터로 대체된다(§5 규약 5).** 공용 코어까지 생성 대상으로 넓히면 검수가 "코드 리뷰"가 되어 틀 §2의 검수 수준(결과 뷰 승인 1클릭)이 깨진다.
2. 골격 닫힌 목록(canonical+alias)은 파서·에이전트 **공유 자산**이다 — 파일 경로·버전 규약은 사내 구현 시 확정([사내]).
3. 원본 바이너리는 저장하지 않는다(source_path 참조만). 전 청크 보존, 청크 텍스트만 저장.
4. **reader는 두 모드에서 같은 코드다** — 운영 모드의 원시 추출과 구축 모드(등록 세션)의 표본 관찰 재료 공급이 동일 코어의 재사용이다(§6). 등록 세션 전용 reader는 없다.

## §4 LLM 사용 경계 — 시점별 [확정 — 틀 §3 승계]

| 시점 | LLM이 하는 것 | LLM이 하지 않는 것 |
|---|---|---|
| 구축(등록 1회) | 표본 관찰 → **어댑터 코드·분할 규칙 생성** (관찰 체크리스트: 들여쓰기·열 위치·번호 패턴·서식·병합·빈 셀) | 검수 없이 등록(사람 승인 필수), 공용 코어 수정 |
| 운영(매 문서) | 이미지 요약(코어가 호출 — 어댑터 아님, §5 규약 3), 좌표 태깅(닫힌 목록에서 선택 또는 null) | 구조 재판단, 개체 추출, 관계·카테고리·골격 발명, doc_type 판별(투입 분류는 사람+결정적 지문 스캔 — §5) |

---

## §5 어댑터 인터페이스 [확정 — 세션 N ⑴, 2026-07-30]

> 어댑터가 "코드"라면 무엇을 받고 무엇을 내는 어떤 모양의 코드인가 — LLM에게 생성시키려면 이 계약이 먼저 있어야 한다. 초안의 논의 지점 3건 + 논의 중 발의 3건(기본 어댑터·preflight·투입 분류)을 판정해 확정했다.

### 구조 — 어댑터는 doc_type당 1모듈, 선언 1개 + 함수 1개

```python
# adapters/{doc_type}.py
ADAPTER = {
  "doc_type": "cp",
  "adapter_version": "1.0",   # 개정 시 수동 증가 — 봉투에 1회 기록(A7-3)
  "payload_kind": "table",    # "table" | "prose" — 닫힌 2값 (C9)
  "expects": {                # 관찰 상수 = 이 어댑터가 기대하는 양식의 지문
    "header_row": 3,
    "columns": {"공정명": "B", "관리항목": "D", "규격": "F"},
    # prose형이면: 분할 신호 상수 (헤딩 레벨·들여쓰기 값·번호 패턴 등)
  },
}

def extract(raw) -> list[dict]:
    """reader의 원시 추출물 → 정규 조각 리스트. 조각마다 source_locator 포함.
    table:   [{"source_locator": "Sheet1!R12", 필드명: 값, ...}]
    prose:   [{"source_locator": "슬라이드 5", "text": ..., "section": ..., "meta": ...}]
    이미지:  [{"source_locator": "슬라이드 5", "image_ref": "img_003"}]  ← placeholder (규약 3)"""
```

### 규약

1. **함수는 extract 하나다. locate를 두지 않는다** — 위치는 추출 순회 중에만 자명하고, "조각→위치" 역산은 같은 내용이 문서에 2회 출현하는 순간 모호해진다(A4가 occ를 도입한 바로 그 지점). **전처리 훅도 두지 않는다** — 시트 선택·헤더 정리는 extract 내부 사유이며, 인터페이스에 올리면 LLM 생성·검수 표면적만 는다. [⑴-A]
2. **어댑터는 순수 함수다**: 파일 밖 상태·네트워크·LLM 호출 금지. 같은 입력 → 같은 출력이어야 운영이 결정적이고 리허설(§9 규약 1)이 성립한다.
3. **이미지는 placeholder 조각으로 낸다**: `{source_locator, image_ref, (section)}`까지가 어댑터의 일(이미지의 위치·소속은 어댑터만 안다). **요약 LLM 호출은 공용 코어(tagger 단) 소유** — 코어가 text를 요약문으로 채우고 `meta.image_summary=true`로 완성한다(C8 예외). 어댑터 안에 LLM 호출이 있으면 순수성(규약 2)이 깨지고 검수가 "코드가 맞나"에서 "호출이 잘 되나"로 번진다. I6의 운영 LLM 닫힌 목록과 배선 일치. [⑴-B]
4. **관찰 상수는 `ADAPTER.expects`에 데이터로 분리한다.** 용도 셋: ①검수 뷰가 코드 전문 대신 이것을 보여준다(§7) ②preflight의 대조 기준(규약 6) ③LLM 생성 시 채울 빈칸(§9 스켈레톤).
5. **기본 어댑터**: 분할이 자명한 계열(PPT = 슬라이드 1장이 청크 1개, 틀 Q2 "관찰 거의 불요")은 **코어가 제공하는 고정 기본 어댑터를 등록 시 그대로 붙인다** — LLM 생성은 관찰이 실제로 필요한 양식(목차형 엑셀·비표준 Word/PDF)에만 쓴다. 기본 어댑터를 써도 **등록 절차(층 지정·검수·승인 1회)는 생략하지 않는다**(M9). [⑴-B′]
6. **preflight — 실행 전 정합 검사 (코어 소유, reader와 adapter 사이)**: 운영 인입에서 extract 실행 전에 문서의 실물 지문(정형: 헤더 행 내용)을 `ADAPTER.expects`와 **결정적으로 대조**한다(LLM 아님 — M2의 정신). 불일치면 실행하지 않고 **문서 중단 + `adapter_mismatch` 큐**(차이 내역·adapter_version 제시). 이후 판단은 사람 둘 중 하나: 어댑터 개정(구축 모드 재진입 → adapter_version 증가) 또는 신규 doc_type 등록. 등록 여부 조회(M2)를 대체하지 않는다 — 조회가 모드를 가르고, preflight는 운영 모드 안의 **양식 표류**를 잡는다. [⑴-C′]
7. **실패 계약 — 문서 단위 단일**: 계약 위반 행(자기완결 불성립)·어댑터 예외·validator 위반이 **하나라도 있으면 그 문서는 통째로 인입하지 않는다** + `parse_failure` 큐(사유·해당 행 제시). 수리 후 doc_id 재인입(H1)이 유일한 수리 경로다 — 커밋 단위 = 재인입 단위 = 문서로 정렬된다. **계층 미확정(비정형)은 실패가 아니다** — 평면 폴백으로 인입되고 표시+큐만 남는다(틀 Q2·M8의 성능저하 경로, 그대로 유지). 조각 단위 완화는 중단 빈도가 실측으로 정당화될 때만(P7). [⑴-C]

### 투입 분류 — doc_type 지정 (인입 진입 소유 · 파서와의 접점 기록) [⑴-C″]

- **지정은 사람이 기본**: 플랫폼에서 doc_type별 투입함 또는 투입 시 선택 UI — 투입 경로가 곧 지정이다.
- **미지정 정형 문서는 지문 스캔으로 후보 제안**: 문서 헤더를 등록 전체 어댑터의 `expects`와 일괄 대조(preflight와 같은 연산의 재사용, 결정적) → 후보 제시 → **사람 1클릭 확정**. 비정형은 구조 신호의 변별력이 없어 지정 필수.
- **유일 일치 자동 라우팅은 하지 않는다** — 오배정률이 측정된 뒤 승격 검토(P7, 동종 쌍 방향의 조건부 개방과 같은 패턴). 지정 실수의 안전망은 preflight(규약 6)다.

### 경계와 오해

- **extract는 내용을 해석하지 않는다** — 내용 파악은 파서 다음의 독립된 추출 단계(M7) 소관이다. 어댑터가 층 어휘를 알게 되는 순간 "새 층 = config만"(Q3)이 깨진다.
- **"이미지 처리 로직"은 두 조각이다** — 감지·위치 표기(어댑터) / LLM 요약 호출·완성(코어). 어댑터가 갖는 것은 앞쪽 절반뿐이다.
- **문서 단위 실패는 "조용한 중단"이 아니다** — 큐는 이름 붙은 작업 목록이자 화면이며(P-3), 중단 사유와 해당 행이 함께 제시된다(G5).
- 확정 이력: 초안의 `locate` 함수·전처리 훅 후보는 폐지되었다(⑴-A). 초안에 없던 기본 어댑터·preflight·투입 분류는 논의 중 사용자 발의로 신설되었다(⑴-B′·C′·C″).

**원천**: 틀 §2·§3·§4-Q2·Q3·§4B-A7 / 카드 C7·C8·C9·**C11~C15(이 절이 원천)** / 세션 N 판정 ⑴-A·B·B′·C·C′·C″ (2026-07-30)

## §6 등록 세션 입력 패키지 [확정 — 세션 N ⑵, 2026-07-30]

> 구축 모드(doc_type 등록)가 시작될 때 사람이 무엇을 들고 오고 시스템이 무엇을 차려주는가. **이 절은 doc_type 등록의 입력이다 — 층 등록(CH4 4.7·R1)의 입력이 아니다.** 두 등록의 구분은 "경계와 오해" 참조.

### 구조 — 사람 입력 4 + 시스템 공급 5

| 입력 (사람) | 필수 | 내용 |
|---|---|---|
| 표본 문서 | 필수 | **2부 이상 권장, 1부 허용** — 1부면 검수 뷰에 "표본 1부 · 변형 미관찰" 경고를 명시 표시. 1부의 위험(그 문서의 특이점이 규칙이 됨)은 preflight(§5 규약 6)가 사후 안전망 [⑵-①] |
| doc_type 이름 | 필수 | 신규 등록명. 기존과 중복이면 거부 — 운영 모드로 가야 함 |
| 층 지정 | 필수 | **존재하는 층만 지정 가능.** 신규 층이 필요하면 층 등록(CH4 4.7)이 **먼저 완결**되어야 한다(규약 2) |
| 관찰 힌트 | 선택 | 자유 텍스트("이 파일은 3행이 헤더") — 구조화하지 않는다(규약 3) |

| 시스템 공급 (자동) | 내용 |
|---|---|
| **reader 원시 추출** | 표본을 reader로 돌려 head N줄(M5 규칙)을 LLM 관찰 재료로 제공 — **운영 파서와 동일한 공용 코어 reader의 재사용**(등록 세션 전용 reader는 없다). LLM은 문서 바이너리를 직접 읽지 못하므로 이 공급이 관찰의 전제다 [⑵-②] |
| 골격 닫힌 목록 | 좌표 태깅·anchor 대상 (canonical+alias) |
| 층 어휘 | 지정된 층의 카테고리 정의문·관계 목록 — 매칭 스키마 초안 생성 재료. **층 등록의 산출물을 doc_type 등록이 소비하는 관계**다 |
| 공용 블록 | 좌표 블록 등 기존 스키마 공용부 — 재발명 방지 |
| 어댑터 스켈레톤 | §5의 ADAPTER 선언(expects 빈칸) + extract 뼈대 — LLM은 빈칸을 채운다(⑻킷 구성물 2) [⑴ 귀결] |

### 규약

1. **표본 부수 — 2부 이상 권장, 1부 허용 + 경고 표시.** 부수를 강제하지 않는 근거: 표본 확보가 안 되는 양식의 등록이 막히는 비용이 1부 위험보다 크고, 1부 위험은 preflight가 사후에 받친다. [⑵-①]
2. **층 선행 완결 (순서 강제).** doc_type은 층 지정이 필수라 **층 없이 존재 불가**하고, 층은 doc_type 없이 존재 가능하다(seed·config만으로 성립) — 의존이 비대칭이므로 "원자적 동시 커밋"이 아니라 **층 등록 → doc_type 등록의 순서**를 강제한다. 반쪽 상태가 없다: 층만 확정되고 doc_type 등록이 실패해도 층은 그 자체로 유효한 산출물이다. [⑵-③]
3. **힌트는 자유 텍스트를 유지한다.** 구조화는 관찰된 필요가 없다(B5 Rule of Three) — 어떤 힌트 유형이 반복되는지 쌓이면 그때 항목화한다. **관찰 범위 규칙("몇 줄까지 볼 것")은 힌트가 아니라 시스템 소유다**(M5: 정형 head ~10줄 / 비정형 10~30줄 / 목차형 패턴 2~3회) — ⑻킷의 프롬프트 템플릿에 고정되며, 힌트의 삽입 위치도 템플릿에 고정된다. [⑵-②]
4. **분할 자명 계열 분기**: 표본이 PPT 등 분할 자명 계열이면 LLM 생성 없이 **기본 어댑터 적용을 제안**한다(§5 규약 5). 등록 절차(층 지정·검수·승인 1회)는 동일하게 수행한다.

### 부트스트랩 [⑵-③′ — 사용자 확정]

- **기본 내장 층은 공정정보층 1개다.** 구현 시 seed·config 파일로 내장하며, 층 등록 세션 기능을 거치지 않는다 — 층 0개 상태에서 층 생성부터 하는 것을 시스템 기능으로 만들면 시작부터 꼬인다.
- **품질지식층은 내장하지 않는다** — **신규 층 등록 절차의 첫 검증 대상**으로 등록한다. 층 추가 성공 판정(J3: `git diff core/`가 비어 있음)의 실전 시험이며, 설계가 이미 완료되어 있어(명세 §15) "정답을 아는 시험"이 된다. 이것은 로드맵상의 계획이지 내장 목록이 아니다.
- 층 등록 기능(R1·⑷)은 신규 층 전용이다. 품질층 첫 등록 시점에 R1 도구(LLM 초안 지원)가 미완성이면 **수동 config 작성 경로로도 절차 검증은 성립**한다 — 검증 대상은 도구가 아니라 config-only 구조다.

### 경계와 오해

- **이 절은 노드·엣지를 정의하지 않는다.** 카테고리·관계(노드·엣지 체계)의 정의는 층 등록(CH4 4.7·R1)의 일이고, §6은 **이미 있는 체계에 새 양식을 연결하는** 입력이다. "이 층에서 원하는 정보는 무엇인가 → 어떤 노드·엣지가 필요한가"의 대화는 ⑷ 소관.
- **§6은 신규 층 전용이 아니다** — 기본 층에도 새 양식(개정 CP, 새 PFMEA 템플릿)은 계속 생기므로 doc_type 등록은 상시 기능이다.
- 초안의 "층 지정: 신규 층 → 층 등록 세션이 함께 열림"은 **순서 강제(규약 2)로 대체**되었다 — 함께 열리더라도 확정은 층이 먼저다.

**원천**: 틀 §2·§4-Q2(M5) / 카드 M5·M9·**M10·J10(이 절이 원천)**·B5·J1~J3 / §5(스켈레톤·preflight·기본 어댑터) / 세션 N 판정 ⑵-①·②·③·③′ (2026-07-30)

## §7 검수 뷰 규격 [확정 — 세션 N ⑺ = CH2 묶음 L의 U6, 2026-07-30]

> 틀 §2가 정한 것(이 절의 재론 불가 전제): 사람은 코드가 아니라 **결과 뷰**를 보고, 통과 = 승인 1회(클릭 수준·분 단위), 기록은 명시적, "무수정 = 자동 통과" 금지. 이 절은 그 뷰에 무엇이 어떻게 뜨는가의 확정판이다.

### 구조 — 검수 화면 3구획

| 구획 | 내용 |
|---|---|
| 1 · 파싱 결과 | 표본을 생성된 어댑터로 실제 파싱한 결과 — 표시 규칙은 규약 1(3층 표시) |
| 2 · 배정표 | 필드 → role 매핑(매칭 스키마 초안). 필드별로 role·근거(LLM의 한 줄 설명) 병기 |
| 3 · 어댑터 요약 | `ADAPTER.expects`(헤더 행·열 매핑·분할 신호)와 adapter_version. 코드 전문은 접힘(펼쳐볼 수는 있게) |

**구판의 구획 4(층 신설 시 층 초안)는 층 등록 세션의 검수 뷰로 이관되었다** — ⑵-③(층 선행 완결)의 귀결로 doc_type 등록 화면에 층 초안이 뜰 일이 없다. 층 검수 뷰는 ⑷/R1 소관. [⑺-⓪]

### 규약

1. **3층 표시 — 전 포맷 공통.** ①**요약 통계**(조각 수·필드별 채움율·실패/경고 건수) ②**이상 신호는 전량 필수 표시** — 실패 조각, "판정 불가" 질문, 경고(표본 1부 등). 발췌로 숨겨지면 안 되는 것들 ③**정상 조각은 발췌 기본 + 전량 접힘** — table은 head N행의 레코드 표(원본 셀↔필드 대응), prose는 계층 트리 구조 전체(청크 내용은 발췌) + 청크 경계 표시. **payload_kind별로 렌더만 다르고 3층 구조는 같다** — 이 화면의 목적은 "모든 행이 맞는가"가 아니라 **"규칙이 맞는가"의 확인**이다. [⑺-①]
2. **재생성 루프 — 숫자 상한 없음.** 루프 = 사람의 수정 지시("공정명은 B열이 아니라 C열") → LLM 어댑터 재생성 → 표본 재파싱 → 재검수. 몇 번이고 돌 수 있고 상한을 두지 않는다(A8 — 근거 없는 수치 금지). **매회 수정 지시가 승인 기록에 이력으로 남고, 중단은 사람 판단이다.** "판정 불가"는 두 경로로 온다: LLM이 스스로 올리는 경우(M5)와 사람이 수렴 실패로 중단하는 경우. 화면에는 강제 없는 안내만 둘 수 있다("재생성 n회째 — 표본·힌트 변경을 고려"). [⑺-②]
3. **물리 형태 — 데이터와 표현의 분리.** 표본 파싱 실행이 **검수 뷰 데이터 JSON**(3구획의 내용물)을 먼저 내고, 렌더러(⑻킷 구성물 5)가 그 JSON을 **정적 HTML**로 변환한다. 국면 1은 브라우저로 여는 정적 HTML + CLI/파일 승인으로 충분하다(틀 §6 — 화면 구현은 나중). 플랫폼 화면은 나중에 이 JSON을 직접 읽는다 — **렌더러만 교체되고 데이터 계약은 불변**(P-2의 실행 형태). [⑺-③]
4. **승인 기록** (초안 확정 승계): `{doc_type, adapter_version, 승인자, 시점, 수정 지시 이력}` — "무수정 = 자동 통과" 금지의 실행 형태.
5. **"판정 불가" 항목은 질문 형태로 표시**한다(예: "들여쓰기 2와 4가 혼재 — 어느 쪽이 하위입니까?"). 규약 1의 이상 신호로서 전량 표시 대상이다.

### 경계와 오해

- **이 화면은 어댑터 성능 평가가 아니다** — 등록 승인의 근거 확인이다. 정확도 측정은 골든셋·계기판(CH5 5.5) 소관.
- **재생성 루프는 운영 파싱과 무관하다** — 구축 모드 안에서 표본에 대해서만 돈다. 운영 중 파싱 문제는 큐로 온다(`parse_failure`·`adapter_mismatch` — §5 규약 6·7).
- **CH2 묶음 L의 U6은 이 절로 닫혔다.** 잔여 U4(판단 기준 3개)는 세션 1 소관으로 남는다.

**원천**: 틀 §2·§6 / 카드 M4·M5·M9·**M11(이 절이 원천)**·A8·P-2 / 세션 N 판정 ⑺-⓪·①·②·③ (2026-07-30)

## §8 검증 내장과 열린 질문 [확정 승계 + 사내 항목]

1. **validator를 파서 끝단에 내장한다**: 출력 JSON을 계약(3층 구조·좌표 존재·자기완결·payload_kind 정합)에 대해 self-check. 위반은 문서 단위 실패다(§5 규약 7). 에이전트 측 인입 검증(unknown_field/optional·doc_hash)과 짝 — 파서가 미리 잡으면 큐가 덜 쌓인다. 진입 측 짝은 preflight(§5 규약 6)다.
2. **열린 질문 [사내 — 국면 2에서 확정]**: doc_type 실물 목록(CP·PFMEA·PPT보고·RFQ·이슈보고…) / 골격 파일 포맷·주입 경로 / context 축 실태(모델·사이트·라인) / 극성 표기 실태(electrode_type 명시 여부 — R9와 연동) / 이미지 요약 LLM의 사내 게이트웨이 수준 / 기존 자산(PPT 파이프라인·Excel schema 캐싱)의 reader 흡수.

---

## §9 전달물 정의 — 무엇을 외부가 짜서 주고, 사내는 무엇만 하는가 [확정 — 2026-07-29 사용자 결정 · 생성 킷 상세는 보완필요⑧]

> "파서는 사내 구현"(명세 §1)의 정밀화다. **반출 불가 제약이 막는 것은 "실물 문서를 보는 것"이지 "코드를 짜는 것"이 아니다.** 따라서 실물을 보지 않고 만들 수 있는 전부는 외부에서 완성해 전달하고, 사내에 남기는 것은 실물이 필요한 최소 — 어댑터 생성 실행과 검수 — 뿐이다.

**경계표 [확정]**

| 구분 | 사내 | 외부(전달물) |
|---|---|---|
| 실행 | 파서 전체 실행 · 어댑터 생성 세션(사내 LLM) · 검수 승인 | — |
| 코드 | **어댑터만** (사내 LLM이 생성 — 분할 자명 계열은 코어 기본 어댑터라 생성조차 불요, §5 규약 5) | **공용 코어**(reader·preflight·normalizer·tagger·validator + 기본 어댑터) + **생성 하네스**(구축 모드 3단: 생성→검수→확정의 배선, CLI+파일) |
| 자료 | 실물 문서 표본 | **어댑터 생성 킷** (아래) |

**어댑터 생성 킷 [보완필요⑧ — 구성·규격 확정, 실물 작성이 남음]**

| # | 구성물 | 내용 |
|---|---|---|
| 1 | 생성 프롬프트 템플릿 | 표본 관찰 지시 + 관찰 체크리스트(§2) + 관찰 범위 규칙(M5)·힌트 삽입 위치(⑵-②) + 출력 규격. **⑴⑵ 확정(07-30)으로 작성 가능** — §5의 인터페이스가 프롬프트가 생성할 코드의 모양을 고정한다 |
| 2 | 어댑터 스켈레톤 양식 | §5의 ADAPTER 선언(expects 빈칸) + extract 뼈대. LLM은 빈칸을 채우는 방식으로 생성 — 자유 작문보다 검수가 쉽다. **⑴ 확정으로 작성 가능** |
| 3 | **표적 출력 정의 주입** | **메타데이터 양식(계약 ①, 3층 구조)은 전 층 하나로 고정(틀 Q3)이므로 신규 doc_type이라도 항상 프롬프트에 주입할 수 있다.** 표본이 빈약해도 출력 계약이 어댑터의 형태를 고정한다 — "이 양식으로 나오게 짜라"가 지시의 뼈대다 |
| 4 | 참조 어댑터 예시 | 기존 mock 스키마·mock 출력 3종(CP·PFMEA·PPT — 구현문서 §2·§6, 파서_인수인계 §5)을 few-shot 재료로 동봉. 창작 표본 기준으로 외부에서 리허설 완료 상태로 전달 |
| 5 | 검수 뷰 렌더러 | §7의 3구획 화면(검수 뷰 데이터 JSON → 정적 HTML)을 자동 생성하는 코드 — 사람이 코드를 읽지 않게 하는 장치이므로 킷에 포함되어야 한다. **⑺ 확정(07-30)으로 작성 가능** |

**규약**

1. **외부 구현 범위에 파서 공용 코어와 생성 킷이 포함된다** — 국면 1에서 창작 표본(mock)으로 전 과정(표본 투입→어댑터 생성→검수 뷰→등록→파싱 실행)을 리허설하고, 리허설 통과 상태로 사내 반입한다.
2. **사내 작업의 정의가 바뀐다**: "파서 구현(최대 조각)"이 아니라 **"파서 반입 + doc_type별 어댑터 생성 세션 실행 + 검수"**다. 코딩이 아니라 실행·승인이다.
3. 기존 문서(구현문서·명세·인수인계)에 축적된 문서 양식 지식 — 3층 구조, blocks, 계약 예시, role 배정 실례 — 이 킷의 재료다. **양식을 정확히 몰라도 메타데이터 스키마가 "무엇을 뽑아야 하는가"를 정의하므로 LLM 지시가 성립한다**(사용자 확정 논거). 추가로 필요한 정보가 드러나면 그것이 [보완필요⑧] 논의의 의제다.
4. **[사내 확인] 2건 신규**: ①사내 LLM 게이트웨이의 모델·컨텍스트 제약(프롬프트 킷 설계 입력) ②외부 코드의 사내 반입 절차·보안 검토.
5. reader가 사내 문서의 포맷 특이성(비표준 템플릿·보호 문서)과 안 맞으면 사내에서 reader를 수정할 수 있다 — 단 이것은 어댑터와 달리 공용 코드 수정이므로 버전 기록과 외부 통보를 계약으로 한다.

### 경계와 오해

- **이 절은 틀 §1의 "파서는 사내 구현"을 뒤집지 않는다.** 실행과 어댑터 생성은 여전히 사내다 — 바뀐 것은 작성 주체의 배분이며, 에이전트가 의존하는 것은 변함없이 출력 계약(CH2 2.2)뿐이다.
- **"거의 다 됨"의 정확한 뜻**: 설계·계약·재료는 갖춰졌다. **⑴⑵⑺ 확정(07-30)으로 킷 구성물 1·2·5 전부 작성 가능해졌다** — 남은 것은 실물 작성(구현 작업)이며, 그 시점·순서는 [보완필요⑤]/증분 0이 정한다.