# 정제본 개정 대장

> **이 문서가 답하는 질문**: *"정제본의 이 문장은 어디서 왔고, 원천과 왜 다른가."*
> 정제본은 현행 정본이고 원천(`원천_국면1/` · 레포 태그 `phase1-complete` = `aac08a5`)은 동결된 과거 기록이다. **두 벌을 동기화하지 않는다** — 대신 정제본에 가해진 모든 변경을 이 대장이 근거와 함께 보유한다. 원천은 딱 한 용도로만 연다: 정제본을 못 믿겠을 때 대조.

## 변경의 종류 — 두 가지뿐이고 방향이 반대다

| 태그 | 뜻 | 원천과의 거리 | 근거로 무엇을 다는가 |
|---|---|---|---|
| **[복원]** | 원천에 있었는데 정제본이 놓친 것. 원천 문면을 그대로 이식 | **줄어든다** | 원천 문서·절 좌표 |
| **[개정]** | 새 판정으로 규칙이 바뀐 것 | **늘어난다** — 정상이다 | 결정 장부 번호(D-##) 또는 판정 근거 |
| **[정정]** | 정제가 만든 오류의 수정(신구판 병존·오기·혼입) | 줄어든다 | 무엇이 틀렸는지 + 대체 문장 |

**언제든 이렇게 말할 수 있어야 한다**: *"정제본이 원천과 다른 부분은 전부 [개정]이고, 각각 장부 번호가 있다."* 그 성질이 깨지면 이 대장이 고장난 것이다.

## 개정의 조건 — 회귀 스위트 통과

정제본 개정은 아래 셋을 통과해야 확정된다. **사람이 "괜찮아 보인다"고 판단하는 자리를 두지 않는다.**

| 시험 | 자산 | 잡는 것 | 통과 기준 |
|---|---|---|---|
| 조항 대조 | 원천 추출 902건 + 배경 구명세 797건 | 원천 규범이 개정으로 사라졌는가 | 소실 0 |
| 판정 재현 | 78문항 시험지 + 정답(장부 82건) | 개정이 다른 규칙의 해석을 깨뜨렸는가 | 직전 회차 대비 하락 0 |
| 문면 기계 점검 | 스크립트 | 부정형 문면 이탈 · 부록 미등재 용어 · 역참조 깨짐 · 같은 규칙 두 문서 중복 | 위반 0 |

**시험지는 고정한다** — 매번 새 질문을 만들면 매번 다른 것을 놓친다. 고정해야 회귀가 잡힌다.

## 개정 이력 요약

| 회차 | 일자 | [복원] | [개정] | [정정] | 계기 |
|---|---|---|---|---|---|
| 1 | 2026-08-21 | 28 | — | 2 | P6 검증 V1·V4 (§A·§B) |
| 2 | 2026-08-21 | 122 | — | — | 기저 승계 감사 (§C) |
| 3 | 2026-08-21 | 20 | — | 1 | 교차문서 정합·허브 직접 (§D·§E) |
| 4 | 2026-08-21 | — | — | 1 | 회귀 스위트 첫 실행이 검출 (§F) |
| 5 | 2026-08-24 | 25 | — | 1 | 잔여 원천 4종 재검 — 오분류 정정 · 스위트가 부정형 이탈 1건 검출 (§G) |
| | **누적** | **195** | **0** | **5** | |

**아직 [개정]이 0건이다** — 정제본은 지금까지 원천으로부터 멀어진 적이 없고, 전부 원천 쪽으로 되돌아간 변경이다. 첫 [개정]은 국면 2A에서 새 판정이 나올 때 생긴다.

---

## A. P6 검증 복원 — V1 판정 재현 발 14건 (2026-08-21 · [복원])

장부 82건에서 출제한 78문항을 정제본만 읽은 응시자가 풀고 장부 정답과 대조한 결과, 정제본이 소유해야 할 규범인데 누락·약화된 것.

| # | 정제본 위치 | 원천 | 복원 내용 |
|---|---|---|---|
| 1 | 문서 3 (층 등록 relations) | D-6 | relation_patterns에 **관계 정의문 병기** — 정의문 없는 목록은 등록 판정을 막는다 |
| 2 | 문서 7 §7.1 | D-10 | USE_MOCK 대체 3종: 추출=힌트 파일 우선+문형 규칙 폴백 / 등록 생성=fixture / 이미지 요약=고정 문자열 |
| 3 | 문서 3 §3.9 | D-39 | **alias는 정확 일치만. 포함 규칙은 canonical에만** — alias에 포함을 걸면 "cathode 노칭 프레스"가 무극성 노드에 흡수된다 |
| 4 | 문서 4 §4.7 | D-59 | enqueue 쌍 키 중복 제거(전 kind) · **문서 단위 일괄 회수 기각**(상시 작업목록 kind까지 지움 — 실측 61→11) |
| 5 | 문서 4 I축 | D-62 | 병합 생존자 **3단 규칙**(사람 override → status 등급 seed>registered>auto → 앞선 id) · seed끼리 병합 거부 |
| 6 | 문서 4 I축 | D-66 | **툼스톤은 연산 대상이 아니다** · canonical 조회 전부 is_live(6곳) · 행위자 없는 연산 거부 |
| 7 | 문서 5·7 | D-67 | 플랫폼 노출에 **툼스톤 계수**(층별 merged_into·obsolete 건수) |
| 8 | 문서 5 계기판 | D-68 | recall류=mock 스모크 12문항 분모 · 측정 무오염(측정 중 link_miss 적재 차단) · 별도 호출 전용 |
| 9 | 문서 2 §2.7 | D-71 | 리스트 값은 role 불문 missing_field+필드만 드롭 · 미등록 doc_type=문서 단위 미인입 · invalid_category=노드 미생성 |
| 10 | 문서 4 병합 | D-72 | attr 병합은 인입 규칙 재사용 · **자기참조 엣지는 이설하지 않고 결함 로그만** |
| 11 | 문서 4 걸침 | D-73 | target_layer 소비 · 층당 빌더 캐시 · 끝점 미해소=unresolved_endpoint(gate_rejects 로그, 큐 아님) |
| 12 | 문서 2 공용 블록 | D-80 | 같은 이름이면 **스키마 선언이 블록을 이긴다** |
| 13 | 문서 6 §6.5 | D-81 | 등록부 두 출처(내장/등록) — **같은 이름이면 등록분이 이긴다** |
| 14 | 문서 6 검수 뷰 | D-82 | UNMAPPABLE 복원 산식 = header_labels − columns 차집합, 스키마 명시가 이긴다 |

## B. P6 검증 복원 — V4 역질문 발 16건 + 정정 2건 (2026-08-21)

정제본이 답하지 못하는 질문 90건을 만들어 원천에서 답을 수색한 결과.

**[복원]**: USE_MOCK 판정·임베딩 mock 기저 규칙(구명세 §8) · provenance 항목 형식(source_locator 기반) · unbound와 none의 구분 정의 · 파서 실패의 전달 계약(공유 큐 직접 착지) · 걸침 필드의 저장 층·스코프·회수 · cross fact_template 소유(생성층) · 완료판정의 "기존 단위 판정 전부 유효" · 구축 모드 진입 경로(사람이 여는 워크플로우) · 층/doc_type 등록 순서 강제 · data/ 지위와 approval.json · schema_version–adapter_version 짝의 의미 · 구조 가변 prose에서 어댑터의 몫 · prose의 preflight 지문 · USE_MOCK 대체 전표 · 킷 6종과 봉인 정답표의 구분 · 어댑터 배치 경로(`adapters/{doc_type}.py`).

**[정정] 2건**:

| # | 무엇이 틀렸나 | 대체 |
|---|---|---|
| 1 | 문서 4가 게이트 관할의 **구판 문면**("①②④는 정의상 패턴 안")을, 문서 1 I5가 신판을 각각 승계해 정제본 안에서 모순이 표면화됐다 | D-52 신판으로 통일 — 게이트 관할은 경로 ③ 한정, 골격 소유 관계는 relation_patterns에 넣지 않는다 |
| 2 | 문서 1의 카드 L5가 병합 생존자 **구판**("작은 id")을 승계했다 | D-62 3단 규칙으로 정정 |

## B-2. 사용자 제기 4논점 발 복원 3건 (2026-08-21 · [복원]·[정정])

레포 실물 감사(클론 후 실행 검증)에서 검출.

| # | 정제본 위치 | 종류 | 내용 |
|---|---|---|---|
| 1 | 문서 7 **§7.6-B 신설** | [복원] | **실 연결 규격 — USE_MOCK=0의 대칭 요구**: 게이트웨이 2파일(`core/llm.py`·`core/embeddings.py`)·환경변수·LLM 지점의 mock/실호출 분기·주석을 이음매로 삼지 않음·mock 산출물의 그래프 유입 금지·프롬프트 템플릿 파일 정본·의존 매니페스트 |
| 2 | 문서 7 §7.8 | [복원] | **시각화 3형태**(html·Cypher·Neo4j, GraphStore 경유·필터 렌더) — 원천 CH6 §6.4 규약 2가 요구했으나 정제본이 파생물 지위만 승계하고 요구 자체를 잃었다 |
| 3 | 문서 7 §7.6 | [정정] | **완료판정의 범위 진술** — "완료 = mock 위 메커니즘"이며 실 연결은 별도 확인. 시나리오 통과만 세면 명세가 요구한 파일이 통째로 없어도 판정이 통과한다(실측된 실패 유형) |

> **이 3건의 검출 경로가 시사적이다.** 사람이 "LLM 연결이 없는 것 아니냐"고 물어서 찾았다 — 검출이 직관에 걸려 있었다는 뜻이다. §C의 승계 감사는 그 직관을 기계적 전수 대조로 대체한 것이고, 그 결과 같은 유형이 122건 더 나왔다. **§B-2의 §7.6-B조차 불완전했다** — LLM 지점을 7종이라 썼으나 답변 생성이 빠져 8종이었고, §C에서 정정됐다.

## C. 승계 감사 복원 122건 (2026-08-21 · [복원])

> 배경 구명세 3종(`ontology_system_명세.md`·`role_어휘_정의서.md`·`ontology_system_구현용문서.md`)에서 규범 797건을 추출해 정제본과 전수 대조한 결과다. 승계됨 599 · 부분승계 161 · 미승계 37 → 갭 198건을 3분류(승계필요 127·의도적폐기 68·국면2안건 3) → 복원 작업자가 필요성 시험으로 재차 거른 뒤 **122건 적용·9건 기각**.
> 전건 [복원] — 원천에 있던 문면의 이식이므로 원천과 정제본의 거리를 **줄인다**. 창작·확장은 없다.

| # | 정제본 위치 | 조치 | 등급 | 원천 | 복원 내용 (원문 발췌) |
|---|---|---|---|---|---|
| 1 | 2.4 ① anchor | 삽입 | 높음 | ontology_system_명세.md | anchor 미스 → 후보검색+판정 → 실패 시 큐 kind=orphan_anchor(레코드 전체를 보류하지 않고 해당 엣지만 생략). |
| 2 | 2.4 ③ attribute | 삽입 | 높음 | ontology_system_구현용문서.md | effect orphan(R9 셀 부풀음)·process orphan(R13)은 severity 부착 대상 자체가 미해소 → 저장 보류. |
| 3 | 2.5 규약 2 (속성 일람) | 수정 | 높음 | role_어휘_정의서.md | \| `attr_name` \| attribute \| 저장 키 이름 (생략 시 필드명) \| |
| 4 | 2.5 규약 2 (속성 일람 아래 새 문단) | 삽입 | 높음 | role_어휘_정의서.md | **에이전트 처리**: `attach_to_field`가 가리키는 (entity 또는 anchor-해소) 노드의 필드로 저장(저장 키는 `attr_name`, 생략 시 필드명). |
| 5 | 2.5 PFMEA 실례 edges + 읽는 법 ④ | 삽입 | 높음 | ontology_system_명세.md | `Failure ─controlled_by→ Property`: 출처는 control_item 열. 행에 있을 때만 생기는 보조 연결. Property 부재 시 자동 생성 규칙(§15.7 규칙 A)이 받음. |
| 6 | 2.5 PFMEA 실례 edges | 삽입 | 높음 | ontology_system_명세.md | edges: cause─causes→failure_mode / failure_mode─affects→effect.category / failure_mode·cause─controlled_by→control_item / failure_mode─occurs_in→process_ref. |
| 7 | 2.5 PFMEA 실례 edges | 삽입 | 높음 | role_어휘_정의서.md | {"from": "failure_mode", "relation": "controlled_by", "to": "control_item_for_fm",    "optional": true}, {"from": "cause",        "relation": "controlled_by", "to": "control_item_f |
| 8 | 2.5 PFMEA 실례 edges | 삽입 | 높음 | ontology_system_명세.md | {"from": "failure_mode", "relation": "controlled_by", "to": "control_item_for_fm",    "optional": true}, |
| 9 | 2.6 변경 등급 L1~L4 (표 아래 불릿) | 삽입 | 높음 | ontology_system_명세.md | \| 카테고리 추가 \| 큼 — §6.1 봉쇄 훼손 \| **제안만, L3/L4 판정**(정의서 §8.1) \| |
| 10 | 2.7 핸들러 루프 (코드 블록 직후 첫 불릿) | 삽입 | 높음 | ontology_system_명세.md | 핸들러 공통 시그니처 `handle(value, spec, ctx) → resolved_id\|value\|None`. ctx = {graph들(layer별), dic, queue, record, schema}. |
| 11 | 2.4 ① anchor (Tier1 한정 문장 말미) | 삽입 | 보통 | role_어휘_정의서.md | **Tier1 한정 (v1.8)**: 조회 결과가 auto 노드(발견형)뿐이면 anchor로 쓰지 않고 orphan_anchor 큐에 후보 id를 실어 사람 판단으로. anchor 대상은 사람이 보증한 것(seed provenance 또는 승격 confirmed)뿐. |
| 12 | 2.5 PFMEA 실례 fields | 삽입 | 보통 | ontology_system_명세.md | "control_item_for_cause": {"role": "entity",    "category": "Property", "target_layer": "process", "optional": true}, |
| 13 | 2.5 PFMEA 실례 edges | 삽입 | 보통 | ontology_system_명세.md | {"from": "cause",        "relation": "controlled_by", "to": "control_item_for_cause", "optional": true} |
| 14 | 2.5 PFMEA 실례 edges | 삽입 | 보통 | role_어휘_정의서.md | "edges": [   {"from": "cause",        "relation": "causes",    "to": "failure_mode"},   {"from": "failure_mode", "relation": "occurs_in", "to": "@process_ref"},   {"from": "failure |
| 15 | 2.5 (PFMEA 실례 읽는 법 뒤, 경계 앞 — 번호 없는 소제목 블록) | 삽입 | 보통 | role_어휘_정의서.md | \| 설비/지그 \| entity(Unit) \| |
| 16 | 2.5 CP 계열 배정 예 표 | 표행추가 | 보통 | role_어휘_정의서.md | \| 규격/공차 \| attribute → 관리항목 (attr_name: spec) \| |
| 17 | 2.7 핸들러 3단 구조 불릿 | 삽입 | 보통 | ontology_system_명세.md | 관찰 대상 갭: 빈 셀 부착(부착 대상 부재), 상동/병합 셀, 복수값 셀("A, B"), entity 칸의 혼입 산문. 단 상동/병합/복수값의 1차 방어선은 파서의 자기완결 레코드 계약(§12). |
| 18 | 2.5 PFMEA 실례 fields | 삽입 | 낮음 | role_어휘_정의서.md | "control_item_for_fm":    {"role": "entity",    "category": "Property", "target_layer": "process", "optional": true}, "control_item_for_cause": {"role": "entity",    "category": "P |
| 19 | 3.1 | 삽입 | 높음 | ontology_system_명세.md | 구현용 문서 내용: 파일 트리, JSON 계약 실예시, config 스키마, mock 데이터 전문, 구현 순서(국면 1의 5단계), 단계별 완료 판정 기준. |
| 20 | 3.5 | 삽입 | 높음 | ontology_system_명세.md | **RPN 인자 이연(v1.3)**: 행 단위 RPN 인자(발생도/검출도 등)의 정주소는 노드가 아니라 causes 엣지 속성(엣지가 모드×원인 행의 실체)이나, 현 실물 스키마엔 severity만 존재하고 영향 단위 저장으로 충분. RPN 기반 질의(심각도 N 이상 등)는 **PoC 밖 — 저장만, 이연**. |
| 21 | 3.5 | 삽입 | 높음 | ontology_system_명세.md | "category_pair_map": {}, |
| 22 | 3.9-⑵ | 삽입 | 높음 | ontology_system_구현용문서.md | process config.skeleton = {"type":"tree", "category":"Process", "data":{"조립":["노칭","스태킹","cathode 탭용접","anode 탭용접","패키징","전해액주입","실링"]}, "relations":{"child":"part_of","sibling":"p |
| 23 | 3.9-⑻ | 삽입 | 높음 | ontology_system_명세.md | "polarity": {"bind_categories": ["Unit", "Property"], "values": ["cathode", "anode"]}, |
| 24 | 3.4 | 수정 | 보통 | role_어휘_정의서.md | \| 측정방법/주기/샘플 \| attribute → 관리항목 \| |
| 25 | 3.4 | 삽입 | 보통 | ontology_system_명세.md | 설비·기구부·지그·금형 = entity(발견 대상). 골격에 넣지 않는다. |
| 26 | 3.5 | 삽입 | 보통 | ontology_system_명세.md | **FailureEffect의 얇은 Tier1 목록** = 셀 관점 고장영향 분류(단락, 화재, 방전기능상실, 충전기능상실 등 — 실물 목록은 사내 확정). 안정적·공정무관·유한하므로 골격 자격 충족. |
| 27 | 3.5 | 수정 | 보통 | ontology_system_명세.md | FailureEffect 표준 목록 실물 / effect.detail 성격(attribute vs content) / Failure–Failure prose 방향 케이스 / PFMEA에 설비 직접 지목 열 존재 여부(있으면 §8-3 L1 추가). |
| 28 | 3.5 | 삽입 | 보통 | ontology_system_명세.md | **Unit과의 직접 연결도 미도입** — controlled_by→Property→(has_property 역방향)→Unit 경로로 충분. PFMEA 실물에 설비 열이 확인되면 L1급 추가. |
| 29 | 3.9-⑴ | 삽입 | 보통 | ontology_system_구현용문서.md | **core/skeleton.py (범용)**: config.skeleton을 해석. `type=tree`면 data(중첩 dict)를 부모-자식 part_of + 형제 나열순 precedes로 심음. |
| 30 | 4.3 (규약 6 신설) | 삽입 | 높음 | ontology_system_명세.md | **matcher.py**: 입력 mention+후보들(canonical, aliases, 부착 위치, category). USE_MOCK=문자열 정규화 포함 규칙(공백 제거 후 동일/포함), 실물=판정 프롬프트(정의문·비대칭 기준은 층 config에서 주입). |
| 31 | 4.4-3 · 4.4-5 | 삽입 | 높음 | ontology_system_명세.md | attach_to 해소=entity 판정 파이프라인 재사용(Tier1 한정 미적용 — auto 설비 부착 가능), 성공 시 관계=config category_pair_map으로 결정 후 엣지 생성, 실패 시 규칙B 폴백+orphan_attach 큐(dedup=(노드id,정규화 표면형)), null이면 폴백만. |
| 32 | 4.4 (「좌표(anchor) 미해소의 연쇄」 소제목 블록 신설 — 걸침 배선 문단 직전) | 삽입 | 높음 | ontology_system_명세.md | **앵커 미스 시**(process_ref가 골격에 없음) occurs_in은 생성되지 않고 orphan_anchor 큐가 그 Failure를 보유(orphan 재시도 배치의 대상) — "반드시"는 정상 경로 한정, 미스는 큐로 표면화. |
| 33 | 4.4 (같은 「좌표(anchor) 미해소의 연쇄」 블록) | 삽입 | 높음 | ontology_system_명세.md | 이때 규칙 B의 폴백 대상(@process_ref)도 미스이므로 Property 부착도 함께 드롭됨(연쇄). |
| 34 | 4.4 (같은 「좌표(anchor) 미해소의 연쇄」 블록 — spec-s7-11-26·27과 합침) | 삽입 | 높음 | ontology_system_구현용문서.md | \| R13 \| **레이저노칭** \| 슬리팅 버 \| 빔 출력 편차 \| 단락 \| 빔 출력 \| **process_ref anchor 미스**(골격에 없는 공정): occurs_in 드롭 + 규칙B 폴백(@process_ref) 미스로 Property 부착도 드롭 → orphan_anchor 큐(§8-1 연쇄 검증) \| |
| 35 | 4.5-4 | 삽입 | 높음 | ontology_system_명세.md | **mirrors 엣지**: 자동 규칙(§5.3) 산출물, provenance=auto:mirror_rule. |
| 36 | 4.5-5 | 삽입 | 높음 | role_어휘_정의서.md | 표면형은 alias로 등재되어 질의 링킹은 그대로. |
| 37 | 4.7-3 · 4.7-4 | 삽입 | 높음 | ontology_system_명세.md | **수정 도구 세트가 본체** (상세는 §13 항목): 노드 병합/분리, alias 이관, canonical 개명, 엣지 삭제, 큐 우선순위 정렬(참조 많은 auto 노드 우선 등), 근거 소멸 처리(§5.5-3). |
| 38 | 4.7-4 (미리보기 대상 목록) · 4.9 (이동 정의) | 수정 | 높음 | ontology_system_명세.md | 대상: Tier2의 **병합**(양쪽 엣지 전부 이동)·**개명**(Property canonical이 `{세부공정}::{인자}`이므로 **공정 개명은 하위 인자 canonical 연쇄 변경**)·**이관**(스코프 변경 연쇄), Tier1의 골격 개정(§9.1). |
| 39 | 4.10 (「프롬프트 자산의 위치」 문단 뒤) | 삽입 | 높음 | ontology_system_명세.md | 추출(prose 전용): 정의문 3종 + 부착 후보 목록(해당 청크 process_ref의 세부공정 및 그 하위 part_of 설비 canonical — 수십 개 수준; 좌표 null이면 세부공정 목록만)을 프롬프트에 삽입. |
| 40 | 4.3 (②후보 검색 문단) | 삽입 | 보통 | ontology_system_명세.md | (a) 판정용 노드 임베딩 [이연 아님] — canonical+정의문 기반, 수백~수천 개, 매 로드 시 on-the-fly 재생성 → P4의 "비저장"은 이것을 말함. |
| 41 | 4.3 (규약 6 신설 — spec-A-95와 한 문장으로 합침) | 삽입 | 보통 | ontology_system_명세.md | 입력 = mention + 후보들(canonical, aliases, 부착 위치). 출력 `{"type":"match\|new\|uncertain","matched_id","confidence"}`. |
| 42 | 4.4 (경로 ② 표행 + 게이트 문단) | 수정 | 보통 | ontology_system_명세.md | {"from": "설비",    "relation": "part_of",      "to": "@process_ref"}, |
| 43 | 4.4-3 | 삽입 | 보통 | ontology_system_명세.md | attach 해소는 anchor 경로가 아니므로 Tier1 한정 미적용(산문 발견 auto 설비에도 부착 가능). |
| 44 | 4.4-8 (a) | 수정 | 보통 | ontology_system_명세.md | **v1.17에서 확장 전파 규칙이 정의되어 이 경로는 명세상 성립하므로, 이 검증은 "열린 질문"이 아니라 정의대로 구현됐는지 확인하는 테스트다** — 통과 시 (a) 갈아끼움 보류 해제. 미통과(구현 결함)면 수정 전까지 갈아끼움 보류(정밀화가 공정 질의 회귀를 만들지 않도록). |
| 45 | 4.7-4 (전 연산 공통 뒤 불릿 신설) | 수정 | 보통 | ontology_system_명세.md | 목적은 위험 제거가 아니라 **가시화** — 사람이 승인 전에 규모를 보고 판단한다(자동 커밋+큐, 명시적 실패와 같은 사상). 파급 1건 이하(엣지 삭제 등)는 미리보기 불요. |
| 46 | 4.7-4 I3 분리 행 | 수정 | 보통 | ontology_system_명세.md | 행 극성이 both/무표기이면 Unit/Property도 극성 무관 노드로 생성 — 나중에 … spec_conflict가 극성 분리 신호가 되어 수정 도구 node split으로 사람이 가름. |
| 47 | 4.7-4 I3 분리 행 (spec-A-080과 한 문장으로 합침) | 수정 | 보통 | ontology_system_명세.md | both/무표기 Unit·Property는 극성 무관 노드(추후 spec_conflict가 극성 분리 신호). |
| 48 | 4.7-4 I3 분리 행 (spec-A-080·A-78과 한 문장으로 합침) | 수정 | 보통 | ontology_system_명세.md | 공정별 세부 구분이 정말 필요한 경우는 spec_conflict/사후 관찰로 표면화되어 node split. |
| 49 | 4.9-③ | 삽입 | 보통 | ontology_system_명세.md | 2. **플랫폼이 적용까지**(단위 4·5와 함께): 승인하면 플랫폼이 파일 갱신·재빌드. 원천은 여전히 파일이라 P2 유지, git으로 복원. |
| 50 | 4.9 (「삽입과 이동은 분리한다」 소제목 블록 신설) | 삽입 | 보통 | ontology_system_명세.md | \| 골격 엣지 재배선(중간 삽입: A→B를 A→C, C→B로) \| 작음 — 엣지 2~3개, **하위 노드 canonical 불변** \| 제안 허용 \| |
| 51 | 4.9 (같은 「삽입과 이동은 분리한다」 블록) | 삽입 | 보통 | ontology_system_명세.md | **삽입과 이동은 분리한다**: 중간 삽입 자체는 하위 노드를 건드리지 않는다(파급 작음). "이 인자는 사실 새 공정 소속"이라는 **이동**은 canonical 연쇄를 일으키는 별개 작업으로, 건별 사람 판단(§9 미리보기 대상). |
| 52 | 4.10 (규약 8 신설) | 삽입 | 보통 | ontology_system_명세.md | "각 개체가 붙는 대상이 본문에 서술되어 있으면 attach_to로 그 이름을 표기(후보 목록 우선, 목록에 없으면 본문의 이름 그대로 — 발견 보존), 서술이 없으면 null. 관계의 종류는 표기하지 않는다." |
| 53 | 5.1-4 | 삽입 | 높음 | ontology_system_명세.md | `neighbors(ids, traverse_spec)` — traverse_spec은 관계별 {direction: "out\|in\|both", recursive: bool} dict를 **인자로** 받음(내용은 config가 소유 = B, 명세 §5.6.2). |
| 54 | 5.2-1 | 삽입 | 높음 | ontology_system_명세.md | 노이즈는 **채널 분리(그래프 사실은 답변 LLM이 질문 관련성으로 필터 §8-R4·§5.6.4) + 청크 tier2 잘림(§5.6.3)**으로 통제 (…) 파라미터 조정은 로그 관찰로(P7). |
| 55 | 5.2-1 | 삽입 | 높음 | ontology_system_구현용문서.md | **Q1~8 회귀**: cross-layer on 상태에서 1~8이 단계2 baseline과 동일 응답(브리지로 딸려온 Failure 청크·노드에 오염 안 됨) — §8-6 채널분리(그래프 사실 관련성 필터 + 청크 tier2 잘림)의 실증 지점 |
| 56 | 5.1-5 | 삽입 | 보통 | ontology_system_명세.md | 확장 범위는 홉 수 상한이 아니다: 범위는 관계별 규칙(재귀 여부·방향)의 조합으로 정해진다. `part_of` 하향은 재귀이므로 트리 깊이에 따라 3홉 이상도 정상 |
| 57 | 5.2-1 | 삽입 | 보통 | ontology_system_명세.md | "fact_templates": {     "part_of": "{src}는 {dst}의 하위 요소이다",     "precedes": "{src} 다음 공정은 {dst}이다",     "has_property": "{src}의 관리인자: {dst}",     "mirrors": "{src}는 {dst}와(과) 극성 대칭 |
| 58 | 5.2-3 | 수정 | 보통 | ontology_system_명세.md | ⑴근거 있음 → 근거 기반 답 + 출처 표기 … ⑵근거 없음/부족 → "사내 문서에서 근거를 찾지 못했다"를 먼저 밝히고 [일반지식 — 사내 검증 필요] 표시 … + 등록 개체 안내 … ⑶링킹 미스는 로그로 축적 |
| 59 | 5.2-3 | 수정 | 보통 | ontology_system_구현용문서.md | 답변 3단 규칙: 근거 있음→출처 / 없음→"사내 근거 없음"+[일반지식—사내 검증 필요]+등록 개체 안내 / 미스 로그). |
| 60 | 5.2-5 | 삽입 | 보통 | ontology_system_명세.md | 프롬프트가 두 채널을 구분해 사용하게 함: 그래프 사실=시스템이 보증하는 구조 정보, 문서 근거=서술 정보. 구조·값 질문은 전자가, 서술 질문은 후자가, 복합은 함께 답을 구성. |
| 61 | 5.3 | 수정 | 보통 | ontology_system_명세.md | Q5 \| 역방향/필터형 … \| 역방향 엣지+필드 \| ✅ 양방향 확장 + LLM 필터 (규모 커지면 코드 필터) |
| 62 | 5.4 | 수정 | 보통 | ontology_system_명세.md | **도입은 보류율·판정 정확도 측정 후**(P7) — 효과가 "수정 큐로 가는 건수 감소"로 정확히 측정됨. |
| 63 | 5.4 | 삽입 | 보통 | ontology_system_명세.md | **질의 이연 목록 (저장은 하되 질의 대상 승격은 명세 확정 후 별도 논의)** |
| 64 | 5.4 | 삽입 | 보통 | ontology_system_명세.md | RPN 인자(발생도/검출도 등, §15.5) — "심각도 N 이상" 류. |
| 65 | 5.3 | 삽입 | 낮음 | ontology_system_명세.md | 질문이 요구하는 정보의 소재지는 네 곳뿐(엣지/노드 필드/청크/그래프 밖) — 이 기준의 전수 열거: |
| 66 | 5.4 | 표행추가 | 낮음 | ontology_system_명세.md | "fact_templates": {     "causes": "{src}는 {dst}의 원인이 될 수 있다",     "affects": "{src}는 {dst}(으)로 이어질 수 있다",     "occurs_in": "{src}는 {dst} 공정에서 발생한다",     "controlled_by": "{src}는 {d |
| 67 | 7.1 저장 계층 — GraphStore 경계 | 수정 | 높음 | ontology_system_명세.md | 파일 락·원자적 쓰기(tmp+rename)는 단위 4에서 구현하되, 계약은 지금 확정 — 이는 성능(P7 이연)과 별개인 정합성 요구다. |
| 68 | 7.1 core 접근 경계 (신설 소제목 블록) / 7.8 dictionary.json 행 | 표행추가 | 높음 | ontology_system_명세.md | │   ├── dictionary.py     전 층 공유 사전: register/lookup(→후보 id 목록) |
| 69 | 7.1 core 접근 경계 | 표행추가 | 높음 | ontology_system_명세.md | │   ├── matcher.py        개체 판정: match(surface, candidates, category) |
| 70 | 7.1 core 접근 경계 (graph.py 행, spec-A-14와 한 행) | 표행추가 | 높음 | ontology_system_명세.md | `add_edge(src, rel, dst, status, provenance)`(중복 무시. **status="deleted_by_user" 툼스톤인 (src,rel,dst)-by-id는 건너뜀** — 재인입 부활 방지, 명세 §5.5-3. enforcement는 단계5) |
| 71 | 7.1 코드 배치 트리 (run.py) / 7.6-4 | 수정 | 높음 | ontology_system_명세.md | ├── run.py                파이프라인 단일 진입점: init[--fresh]/build/query/test/status/all |
| 72 | 7.6-4 / 7.1 run.py (spec-A-38과 한 번에) | 수정 | 높음 | ontology_system_구현용문서.md | **디렉토리 초기화**: 첫 실행 시 data/ 하위(process/·quality/·dictionary.json·id_seq.json·chunks.json·review_queue.json)를 빈 상태로 생성하는 `init` 동작. |
| 73 | 7.2 말미 「저장 레코드 스키마 — graph.json·chunks.json의 형태」(번호 없는 소 | 삽입 | 높음 | ontology_system_명세.md | "N0002": { "id": "N0002", "canonical": "노칭", "category": "Process", "layer": "process",                "status": "confirmed", "attrs": {"process_no": null},                "aliases |
| 74 | 7.2 저장 레코드 스키마 | 삽입 | 높음 | ontology_system_명세.md | "N0031": { "id": "N0031", "canonical": "cathode 노칭 프레스::노칭 정밀도", "category": "Property", "layer": "process",                "attrs": { "spec": [ {"context": {"model": "M1"}, "value |
| 75 | 7.2 저장 레코드 스키마 | 삽입 | 높음 | ontology_system_명세.md | "edges": [ { "src": "N0002", "rel": "precedes", "dst": "N0003",                "status": "confirmed", "provenance": ["seed"] },              { "src": "N0021", "rel": "mirrors", "ds |
| 76 | 7.5 말미 「실호출로만 검증되는 항목의 표시」(번호 없는 블록, 신설) | 삽입 | 높음 | ontology_system_구현용문서.md | \| P8 \| 노칭 \| "notching press" 영문 표기 서술 \| 표기 변형 판정 (USE_MOCK 규칙 한계 시 `# 실물 검증 항목` 주석) \| |
| 77 | 7.6 「질의 스모크의 채점 규격」(번호 없는 블록, 신설) | 삽입 | 높음 | ontology_system_구현용문서.md | 각 문항에 expected_path(graph_fact \| chunk \| both \| general_knowledge) 명기. |
| 78 | 7.6 「기존 단위의 완료판정 — 위 2번이 가리키는 대상」 표 (신설 블록) — 정형 인입 행 | 표행추가 | 높음 | ontology_system_구현용문서.md | seed 8노드(극성 탭용접 포함). CP 인입 후 Unit·Property·has_property 엣지 존재, C4가 spec_conflict 큐(같은 context 그룹), **C7은 충돌 없이 병렬 항목**(context 그룹 상이), **C8·C9 극성 결합 canonical 2노드 + mirrors 엣지 + mi |
| 79 | 7.1 USE_MOCK 대체 표 / 7.6-B-2 | 수정 | 높음 | ontology_system_구현용문서.md | **llm.py**: 추출=키워드 규칙(config categories의 예시 표면형 + mock 데이터의 개체명 매칭), 판정=문자열 정규화 규칙(공백 제거 후 동일/포함 → match 0.95), 답변=두 채널을 정형 텍스트로 나열(문장 생성 없음). |
| 80 | 7.1 외부 의존 불릿 | 수정 | 높음 | ontology_system_구현용문서.md | **패키지**: PoC 코어는 **외부 패키지 0** 목표(USE_MOCK=1 경로). 실물 경로용 선택 의존은 지연 import로 격리 — `sentence-transformers`(임베딩), LLM 게이트웨이 호출용 `requests`(또는 표준 urllib). requirements.txt에 선택 의존 명시하되 US |
| 81 | 7.1 core 접근 경계 (spec-A-15와 한 번에) | 표행추가 | 보통 | ontology_system_명세.md | │   ├── dictionary.py            동의어 사전: register / lookup |
| 82 | 7.1 core 접근 경계 | 표행추가 | 보통 | ontology_system_명세.md | │   ├── graph.py          add_node/add_edge/neighbors(ids, traverse_spec)/save/load, id 발급(전역 N####) |
| 83 | 7.2 저장 레코드 스키마 | 삽입 | 보통 | ontology_system_명세.md | { "chunks": { "PPT01-C003": { "doc_id": "PPT01", "text": "...", "section": "슬라이드 5",               "meta": {}, "linked": false } },   "describes": [ {"chunk_id": "PPT01-C001", "nod |
| 84 | 7.6 기존 단위의 완료판정 표 — 정형 인입 행 (impl-B-74와 한 행) | 표행추가 | 보통 | ontology_system_구현용문서.md | \| 1c \| core/ingest.py(핸들러)+matcher(MOCK)+build + cp.json \| CP01 인입 후 Unit·Property·has_property, C4 spec_conflict·C7 병렬·C8/C9 극성+mirrors+mirror_asymmetry 큐 \| |
| 85 | 7.6 기존 단위의 완료판정 표 — 정형 인입 행 + 부착 두 규칙 행 | 표행추가 | 보통 | ontology_system_구현용문서.md | 규칙A(auto Property)·규칙B(공정 부착 → C2로 보강) 동작. effect_category↔severity 정렬로 spec_conflict는 C4에서만 발생. |
| 86 | 7.6 기존 단위의 완료판정 표 — 부착 두 규칙 행 | 표행추가 | 보통 | ontology_system_구현용문서.md | \| R1 \| 노칭 \| 전극 치수 불량 \| 금형 마모 \| 방전기능상실 \| 금형 클리어런스 \| 기본 경로 + 규칙A(Property auto 생성) + 규칙B(공정 부착) \| |
| 87 | 7.6 기존 단위의 완료판정 표 — 비정형 인입 행 | 표행추가 | 보통 | ontology_system_구현용문서.md | PPT 인입 후 P5 linked=false 보존, P6 auto+큐. USE_MOCK=1 전 과정 무에러 |
| 88 | 7.6 기존 단위의 완료판정 표 — 비정형 인입 행 (impl-B-75와 한 행) | 표행추가 | 보통 | ontology_system_구현용문서.md | \| 1d \| content 경로 + PPT01 \| P5 linked=false 보존, P6 auto+큐, describes 연결 \| |
| 89 | 7.6 기존 단위의 완료판정 표 — 코어(저장) 행 | 표행추가 | 보통 | ontology_system_구현용문서.md | \| 1a \| core/graph.py·dictionary.py·id_seq + init \| 노드 2개 add→저장→로드 시 id 전역 유일·복원 동일 \| |
| 90 | 7.6 기존 단위의 완료판정 표 — 골격 심기 행 | 표행추가 | 보통 | ontology_system_구현용문서.md | \| 1b \| core/skeleton.py + process config.skeleton \| seed 심기 후 Process 8노드(조립+세부7, 극성 탭용접 2), part_of 7·precedes 6 엣지 \| |
| 91 | 7.6 기존 단위의 완료판정 표 — 질의 행 | 표행추가 | 보통 | ontology_system_구현용문서.md | \| 2 \| 질의 4단 + 이원 근거 채널 \| queries.json 1~8·11·12가 expected_path대로 응답. flow 질의(5번)가 골격 전체 공급. 링킹 미스 로그 기록 확인. |
| 92 | 7.6 기존 단위의 완료판정 표 — 질의 행 (impl-B-76과 한 행) | 표행추가 | 보통 | ontology_system_구현용문서.md | \| 2 \| core/query.py + router + cli/query + queries.json \| Q1~8·11·12 expected_path 일치, flow(5) 골격 공급, 미스 로그, **2홉 도달(전파 규칙 준수)** \| |
| 93 | 7.6 기존 단위의 완료판정 표 — cross 질의 행 | 표행추가 | 보통 | ontology_system_구현용문서.md | cross 질의 응답: 9번=occurs_in 역방향으로 노칭의 Failure들, 10번=affects 역방향 **직접 결과**(수집 노드 간 causes 엣지는 그래프 사실로 문장화 — 2홉 연쇄 전체는 스모크 요구 아님). |
| 94 | 7.6 기존 단위의 완료판정 표 — orphan 재시도 행 | 표행추가 | 보통 | ontology_system_명세.md | ②orphan_attach 재시도의 (a) 수행(provenance 회수 확인). |
| 95 | 7.1 (불릿 신설) | 삽입 | 보통 | ontology_system_구현용문서.md | **패키지 구조**: core/·layers/·cli/에 `__init__.py`. import는 절대경로(`from core.graph import ...`), 실행은 프로젝트 루트에서 `python -m cli.build ...`. |
| 96 | 7.8 말미 「로그 규격」(번호 없는 블록, 신설) | 삽입 | 보통 | ontology_system_구현용문서.md | **로깅**: logging 표준 모듈, 레벨 INFO. MOCK 경고·큐 적재·명시적 실패(§3.6)는 명확히 로그. |
| 97 | 7.4 순서 원칙 (4 → 5, ⑤ 추가) | 수정 | 보통 | ontology_system_명세.md | ### 16.1 파이프라인 우선 (플랫폼 직행 배제) - 근거: 진실(JSON 그래프)이 먼저 서야 플랫폼이 보여줄 것이 생김 / 핵심 검증(인입·질의 4단·Rule of Three)은 CLI+mock이 최속 / P1 덕에 선(先)파이프라인이 재작업을 만들지 않음. |
| 98 | 7.4 완료판정 원칙 | 수정 | 보통 | ontology_system_구현용문서.md | 각 단계 종료 시 사람 검수(그래프 JSON·큐 눈검사) 후 진행 — 병목은 코드 생성이 아니라 검수 시간. |
| 99 | 7.4 진행 규칙 (장부) | 수정 | 보통 | ontology_system_구현용문서.md | 3. 명시적 실패가 정당한 곳(§3.6: config 표현 밖)은 raise로 드러내고 BLOCKERS.md에도 기록. |
| 100 | 7.5-6 시나리오 표 S13 행 + 표 아래 근거 1줄 | 수정 | 보통 | ontology_system_명세.md | ③attach_to 자유 이름이 해소 전 엣지를 만들지 않음. |
| 101 | 7.8 시각화 3형태 | 수정 | 보통 | ontology_system_명세.md | 개조 4포인트: ①데이터 계약 교체(신규 JSON, 2층+cross-layer 표시) |
| 102 | 7.1 코드 배치 트리 | 삽입 | 보통 | ontology_system_명세.md | ├── mock/                 §6 mock 데이터 전문 |
| 103 | 7.5 말미 「mock 자산의 자리 — 경로 규약」(번호 없는 블록, 신설) | 삽입 | 보통 | ontology_system_명세.md | │   └── queries.json |
| 104 | 7.5 「mock 자산의 자리 — 경로 규약」 | 삽입 | 보통 | ontology_system_구현용문서.md | ### 6.1 mock/parsed/PFMEA01.json — records 12행 (각 행 주석 = 검증 메커니즘) |
| 105 | 7.2 저장 레코드 스키마 | 삽입 | 낮음 | ontology_system_구현용문서.md | 각 파일 초기값: graph.json={"nodes":{},"edges":[]}, dictionary.json={}, id_seq.json={"next":1}, chunks.json={"chunks":{},"describes":[]}, review_queue.json=[]. |
| 106 | 7.1 (불릿 신설) | 삽입 | 낮음 | ontology_system_구현용문서.md | **Python 3.10+**. 표준 라이브러리 우선(json, hashlib, dataclasses, pathlib, argparse, logging). |
| 107 | 7.5 「mock 자산의 자리 — 경로 규약」(impl-B-21과 한 번에) | 삽입 | 낮음 | ontology_system_구현용문서.md | ### 6.2 mock/parsed/CP01.json — records 6행 |
| 108 | 7.5 「mock 자산의 자리 — 경로 규약」(impl-B-21과 한 번에) | 삽입 | 낮음 | ontology_system_구현용문서.md | ### 6.3 mock/parsed/PPT01.json — prose 청크 8개 |
| 109 | 정제본0 §0.6 (미결 등록부 R5 행 — 트리거 칸 확장) | 수정 | 높음 | ontology_system_명세.md | 자재(전극·분리막·전해액 등): 카테고리 미도입 — … PoC에서 자재 열은 content/meta로 배정하고 등장 빈도를 관찰 항목으로 기록 → 3번째 층 우선순위 판단 재료. |
| 110 | 정제본부록 §2 (UNMAPPABLE 행 — 뜻 칸 보강 + 예시 교체) | 수정 | 높음 | ontology_system_명세.md | 자재(전극·분리막·전해액 등): 카테고리 미도입 — … PoC에서 자재 열은 content/meta로 배정하고 등장 빈도를 관찰 항목으로 기록 → 3번째 층 우선순위 판단 재료. |
| 111 | 정제본6 §6.7-2 (사내 작업의 정의 — 항목 안 문단 추가) | 삽입 | 높음 | ontology_system_명세.md | [검증] 실물 2~3종(PFMEA, Control Plan 우선) 전 열에 role 배정 실험 — 사내 첫 작업은 코딩이 아니라 이 실험. … 막히는 필드는 L3/L4 판정. |
| 112 | 정제본1 · A그룹 확장 조항 (A20 신설) | 표행추가 | 보통 | ontology_system_명세.md | 줄 수를 맞추기 위한 압축·생략(에러 처리, 로깅, provenance 기록, 명시적 변수명의 희생)은 금지 — 명료함이 짧음보다 우선. |
| 113 | 정제본1 · A그룹 확장 조항 (A20 신설 — spec-A-047과 한 건의 삽입) | 표행추가 | 보통 | ontology_system_명세.md | **명료함 > 짧음** — 에러 처리, 로깅, provenance 기록, 명시적 변수명을 줄 수를 위해 희생하지 않는다. |
| 114 | 정제본0 §0.2 (P3 경계 — 기존 "경계 하나" 뒤) | 삽입 | 보통 | ontology_system_명세.md | 줄 수를 맞추기 위한 압축·생략(에러 처리, 로깅, provenance 기록, 명시적 변수명의 희생)은 금지 — 명료함이 짧음보다 우선. |
| 115 | 정제본0 §0.2 (P3 경계 — spec-A-047과 한 건의 삽입) | 삽입 | 보통 | ontology_system_명세.md | **명료함 > 짧음** — 에러 처리, 로깅, provenance 기록, 명시적 변수명을 줄 수를 위해 희생하지 않는다. |
| 116 | 정제본0 §0.6 (미결 등록부 — 번호 없는 "—" 행 추가) | 표행추가 | 보통 | ontology_system_명세.md | 발생도/검출도가 실물에 등장하거나 RPN 질의가 요구되면 엣지 속성 도입 결정(P7). |
| 117 | 정제본1 · P그룹 확장 조항 (P-4 신설) | 표행추가 | 보통 | ontology_system_명세.md | 플랫폼 핵심 기능의 원형: 스키마 등록 워크플로우(§6.7) + 수정 도구 세트(§9) + 계기판(§10) + Q7류 집계 대시보드. |
| 118 | 정제본6 §6.7-2 (spec-A-201·role-136과 한 건의 삽입) | 삽입 | 보통 | ontology_system_명세.md | 1. **role 배정 실험** (첫 작업, 코딩 아님 — 설계 가정 검증 + 스키마 등록 재료) |
| 119 | 정제본6 §6.7-2 (spec-A-201·spec-12-16-25와 한 건의 삽입) | 삽입 | 보통 | role_어휘_정의서.md | 실물 문서 2~3종(PFMEA, Control Plan 우선)의 열 이름 전체에 role 배정을 시도한다. |
| 120 | 정제본6 §6.7-4 ([사내 확인] 목록 — 항목 1개 추가, spec-A-201의 §6.7-4 몫 | 수정 | 보통 | ontology_system_명세.md | \| 사내 \| role 배정 실험(§6.7) / doc_type 스키마 / 블록 / 핸들러 폴백 갭(§7-6) / 자재 빈도(§5.4) / FailureEffect 목록·effect.detail·Failure prose 방향(§15.8) / control_item 극성 명시 여부(§5.2) \| 사내 진입 후 \| |
| 121 | 정제본1 · I그룹 I4 (근거 병기) | 수정 | 낮음 | ontology_system_명세.md | **제어 흐름은 코드 소유**: 구축 파이프라인의 에이전트화(LLM이 다음 행동을 결정) 금지 — 실행마다 순서가 달라지면 재현·디버깅·감사(provenance)가 무너지고 P3가 깨진다. |
| 122 | 정제본부록 §1 (attribute 행 — 뜻 칸에 명명 근거 + 금지 1줄) | 수정 | 낮음 | role_어휘_정의서.md | Neo4j 용어는 property이나, 본 시스템의 카테고리 Property(관리인자)와의 충돌을 피해 attribute를 채택. |

**기각 9건** (필요성 시험 불통과 — 이미 다른 절이 소유하거나 명세가 소유하지 않는 범주):

- `impl-B-93` — 필요성 시험 불통과. severity 낮음이고 항목 자신이 impact를 "낮음 — 7.5-1과 7.6-B-4가 같은 위험을 규범으로 막는다"로 인정한다. 요구의 실질(런타임에서 mock 산출을 실측치로 오독하지 않게 경고를 남길 것)은 이번에 7.8에 신설한 「로그 규격」의 필수 로그 3종 중 **MOCK 경고**가 이미 담는다 — 임베딩 한 지점만을 위한 고정 문구("M
- `spec-s7-11-82` — 필요성 시험 불통과 — 규범이 이미 §4.6 말미에 있다: "구조체 내부 플래그(is_cc 등)는 **저장 전용** — 질의 노드 승격은 감사 질의 수요 확인 후(P7)." '저장 전용'과 '질의 노드 승격'이 곧 '구조체 안에 묻힌 값은 검색되지 않는다'는 전제를 문면으로 말하고 있고, 정제본1 F16도 같은 규범을 '저장 전용이며 … 질의 가능한 노드로 승격하지 않는다'
- `role-092` — 필요성 시험 불통과 + 같은 규범을 다른 절이 이미 소유. severity 낮음이고 감사 스스로 impact를 "실례 부족"으로만 적었다. 규칙 본문("table의 청크 입도: 한 record에 content 필드가 여럿이면 **필드별 별도 청크**(id `{record_id}-{필드명}`)")이 §2.4 ④에 전문 그대로 살아 있어, detection_control 한 줄
- `spec-A-187` — 필요성 시험 불통과 + 근거가 이미 문서에 있음. severity 낮음이고 감사 스스로 "M2·§2.5가 간접 방어하므로 위험은 낮다"고 적었다. 감사가 사라졌다고 한 반증 근거(런타임 연결이 조회 1회)는 §2.5 첫 문단에 문면 그대로 있다 — "등록 시점 1회 작성, 런타임에는 doc_type 문자열로 조회만." 여기에 정제본1 M2(모드 판별은 결정적 — 등록 여부 
- `spec-A-166` — 필요성 시험 불통과(severity 낮음). §5.4 이연 표 하이브리드 행이 이미 "물리 사다리: numpy → faiss → 전용 벡터DB"로 순서를 고정하고 있어 "초기값 = numpy 전수계산"은 그 순서가 이미 함의한다. 남는 것은 전환 규모 수치(청크 10만 / 수십만+ / 백만+, ~400MB·질의당 수십 ms)뿐인데, 이는 명세가 소유하지 않기로 한 규모 감각
- `spec-A-052` — severity 낮음 + 같은 규범을 다른 절이 이미 의무형으로 갖고 있다. §3.3-2("core는 가정 없는 순회기다 — 관계·카테고리·골격 축의 개수·이름·유무를 가정하지 않고 config에 있는 만큼만 처리한다")와 문서 1 B10이 의무 조항이므로, 무가정 순회를 §3.2-4(미래 층 코드 선작성 금지)를 근거로 특수화하면 §3.3-2를 정면으로 어긴다 — 충돌 해
- `spec-12-16-29` — 필요성 시험 불통과(담당 파일 몫). 원천에서 이 문장은 CH7 7.1 규약 3 '국면 2 실행 배열 ⑤'의 일정 항목이다. 정제본0 §0.5는 말미에 '현재 위치·일정 좌표는 이 문서가 갖지 않는다 — 운영 기록(장부·진행 문서)의 몫이다'로 소유를 명시적으로 밀어냈고, README 서술 규격도 '국면·일정·진행 상태는 이 묶음에 쓰지 않는다'로 못박았다 — 국면 2 행에
- `spec-s7-11-68` — 필요성 시험 불통과 + 소유 문서가 담당 밖. '반영의 3단계 진화' 중 ①②는 이미 정제본4 §4.9에 살아 있다('③의 적용은 플랫폼이 대신해도 된다 — 승인 후 파일 갱신과 재빌드를 플랫폼이 수행해도…' 문단). 3단계만 정제본0 §0.6에 떼어 놓으면 한 사실이 두 문서로 갈려 §0.7·A16(같은 사실은 한 문서에만 살고 나머지는 참조한다)을 어긴다. 시험 자체도 
- `spec-12-16-15` — 절반만 반영(role 배정 실험 → 정제본6 §6.7-4에 적용). 나머지 절반인 'Failure prose 방향 케이스'는 감사가 못 찾았을 뿐 이미 정제본3 §3.5에 근거 1줄과 함께 등재돼 있다 — '[사내 확인] 잔여: effect_detail의 성격(attribute vs content) · **Failure–Failure prose 방향 케이스**(이 사내에서 

## D. 교차문서 정합 20건 (2026-08-21 · [복원])

> §C의 복원이 다른 문서에도 참조·용어 등재·소유 보강을 요구한 잔여분. 큰 절을 새로 쓰지 않고 참조 정합·표행·한 줄 병기로 처리했다.

| # | 정제본 위치 | 내용 |
|---|---|---|
| 1 | 문서 README 「재구성 절차 (요약 — 상세는 문서 7)」 4번 | 4. 구현 단위 10종(문서 7)을 순서 원칙 5에 따라 만들고, 단위마다 배정 시나리오로 판정한다. |
| 2 | 문서 부록_용어 §1 기본 구조 (그래프와 층) — 기존 provenance 행에 병기 | **문서 유래가 아닌 항목은 문서 표기를 쓰지 않는다** — 코드가 규칙으로 만든 것은 `auto:{규칙명}`(예: `auto:mirror_rule`), 골격 seed 유래는 `seed`로 적는다(문서 7 §7.2). 비워 두면 provenance가 0이 된 auto 엣지가 매 빌드 evidence_lost 큐에 적재된다   [원천 칸도  |
| 3 | 문서 부록_용어 §1 기본 구조 (그래프와 층) — provenance 행 다음에 표행 2개 추가 | \| **attrs / aliases (노드 레코드 필드)** *(문서 7 등재)* \| 노드 레코드가 값·표면형을 담는 두 필드 — `attrs`는 attr_name별 값 항목 리스트를 담는 컨테이너, `aliases`는 `{surface, provenance}` 항목의 배열. 문자열 배열·스칼라로 줄이지 않는다 — 줄이면 alias별 pr |
| 4 | 문서 부록_용어 §2 문서와 인입 (쓰기 경로) — 「매칭 스키마」 행 다음 (현 59행) | \| **attr_name** *(문서 2 등재)* \| 매칭 스키마의 attribute 필드 spec 속성 — 값이 저장될 **저장 키 이름**이며, 생략 시 필드명이 키가 된다 \| CP의 규격/공차 → `attr_name: spec` \| 문서 2 §2.5 \| |
| 5 | 문서 11 G. 수정 모델 — G6 행 (기존 문면 보존, — 병기 추가) | — 여기의 **이관은 I축 연산이 아니라 문서 4 §4.9의 골격 소속 변경**이다(스코프 소속 변경·canonical 연쇄·건별 사람 판단이며, 미리보기 대상 지목은 §4.7-4) — I축은 개명·병합·분리·폐기 **4연산으로 닫혀 있어**(L5), 이 병기가 없으면 검사기가 존재하지 않는 5번째 I축 연산을 전제한다 |
| 6 | 문서 11 G. 수정 모델 — G6 행 (기존 문면 보존, — 병기 추가) | — **미리보기의 목적은 위험 제거가 아니라 가시화이고**(사람이 실행 전에 규모를 보고 판단한다) **파급 1건 이하는 불요다**(문서 4 §4.7-4) — 목적이 적혀 있지 않으면 검사기만 읽는 구현자가 미리보기를 차단형 승인 게이트로 만들어 G1(승인 게이트 폐기)과 충돌시킨다 |
| 7 | 문서 11 G그룹 확장 조항 — G9 행 (기존 문면 보존, — 병기 추가) | — **정밀화가 공정 질의 회귀를 만들지 않도록** — 이 검증은 열린 질문이 아니라 정의대로 구현됐는지 확인하는 테스트이며, 통과하면 보류가 해제되고 미통과면 수정 전까지 보류한다(문서 4 §4.4-8(a)) — 사유가 적혀 있지 않으면 다음 사람이 "2홉이 되는데 왜 보류하나"로 조항을 지우고 갈아끼움을 켠다 |
| 8 | 문서 2_계약 §2.4 role 어휘 — ① anchor (처리 서술, "조용히 버린 사실이 큐  | **생략된 엣지의 출발 노드는 orphan_anchor 큐 항목이 payload로 보유한다**(예: occurs_in이 생략된 행의 Failure) — 큐 항목이 재시도 배치의 손잡이이므로, 보유하지 않으면 골격이 나중에 자라도 그 노드는 영영 공정에 붙지 않는다. 따라서 **「Failure는 반드시 공정에 붙는다」는 서술은 정상 경로 한 |
| 9 | 문서 2_계약 §2.5 매칭 스키마 — CP 계열 배정 예 표 (규격/공차 행 아래 표행 추가) | \| 측정방법/주기/샘플 \| attribute → 관리항목 \| |
| 10 | 문서 3_구조 §3.5 품질지식층 — 발생도·검출도(RPN 인자) 불릿 말미 | **실물 PFMEA에 이 열이 등장하면 severity를 본떠 attribute로 배정하지 않는다 — 검수 배정표에서 UNMAPPABLE로 올려 사람 판정(L3/L4 — 문서 2 §2.5 규약 6)에 넘긴다.** 근거: 정주소인 엣지 속성 저장이 현 모델에 없어 role 5종 어디에도 매핑되지 않으며, attribute로 배정하면 정주소  |
| 11 | 문서 3_구조 §3.7 신규 층 등록 — 경계 문단 ("구축 모드로 사람에게 넘어간다." 뒤 ·  | **운영 중 문서에서 "새 카테고리가 필요하다"가 감지됐을 때도 같은 문이다**: 감지는 제안까지만이고(문서 2 §2.6 — L3/L4 판정), 그 제안은 **문서 4 §4.9의 structural_proposal 큐를 타지 않는다** — 그 큐는 Tier1 골격 구조 개정 제안 전용이고 승인이 곧 seed·config 파일 diff 반영 |
| 12 | 문서 4_쓰기절차 §4.3 (판정 파이프라인 3단 — ③판정 문장 뒤에 이어 붙임) | **이 파이프라인의 단일 진입점은 `core/matcher.py`의 `match(surface, candidates, category)`다**(소재 정본: 문서 7 §7.1) — 재사용 세 지점(4.4-3 attach_to 해소·4.7-5 orphan 재시도·I2 병합 후보)이 가리키는 것이 이 함수다. 소재가 지정되지 않으면 지점마다 별 |
| 13 | 문서 4_쓰기절차 §4.8-3 (툼스톤 — 기존 3항 말미에 이어 붙임) | **부활 방지의 강제 지점은 쓰기 API다 — GraphStore의 `add_edge`가 `status="deleted_by_user"` 툼스톤인 (src,rel,dst)-by-id를 건너뛴다**(소재 정본: 문서 7 §7.1). 검사를 재인입 경로에만 두면 다른 문서가 같은 엣지를 주장할 때 사람의 삭제 판단이 조용히 뒤집힌다. |
| 14 | 문서 4_쓰기절차 §4.5-2 (극성 결합 3조건 — 조건 나열 직후, '하나라도 빠지면…' 앞에 병 | **①의 카테고리 목록과 ②의 cathode/anode는 코드 상수가 아니라 층 config `polarity.bind_categories`·`values`와의 대조다**(규범 소유: 문서 3 §3.9-⑻ — 코드는 카테고리 이름도 극성 값도 갖지 않는다). |
| 15 | 문서 4_쓰기절차 §4.9 (①②가 이미 있는 반영 문단 말미 — 「삽입과 이동은 분리한다」 앞) | **파급 정리까지 자동(삽입에 딸린 이동·canonical 갱신·재부착을 한 트랜잭션으로)은 이연이다 — 트리거: I축 4연산 운용 실측(수정 도구 성숙). 지금 구현하는 것은 P7 위반이며, 그때까지 이동은 아래 「삽입과 이동은 분리한다」대로 건별 사람 판단이다.** 이연으로 등재하지 않으면 도구가 성숙한 뒤 트리거 없이 감으로 트랜잭션 |
| 16 | 문서 5_읽기절차 §5.3 (질문 유형 대응표 — 표 아래 'PoC 질의 범위' 문장 뒤) | **질의 스모크의 채점 필드 `expected_path`(닫힌 4값 — 문서 7 §7.6)는 이 「답의 소재」 열의 축약이다**: 청크(Q1) → `chunk` · 엣지·노드 필드(Q2~Q5) → `graph_fact` · 두 채널이 함께 근거가 되는 문항 → `both` · 그래프 밖(Q8) → `general_knowledge`. 대응 |
| 17 | 문서 7_구현규격과검증 §7.1 코드 배치 (불릿 목록 — 「패키지화」 뒤, 「core 접근 경계」 앞에  | - **골격 심기 모듈의 자리는 `core/skeleton.py`다** — 층 config의 `skeleton` 키(type·relations)를 읽어 seed의 중첩→child 관계, 자식 배열 순서→sibling 관계를 파생하는 코드가 여기 산다(파생 규칙의 규범 소유는 문서 3 §3.9-⑴/⑵이며, core는 관계 이름을 가정하지 않 |
| 18 | 문서 7_구현규격과검증 §7.8 산출물 표 (data/ 표의 graph.json·review_queue.j | [표행 1] \| `{층}/graph.json` \| 그래프 본체 — 노드·엣지 \| 골격 트리·노드 상세·엣지 열람 명령 · **엣지 삭제 도구**(사람의 삭제를 `deleted_by_user` 툼스톤으로 기록 — 문서 4 §4.7-4) \| [표행 2] \| `review_queue.json` \| 수정 큐 (닫힌 20종) \| kind별 큐 조회 |

**기각 5건**: 이미 다른 절이 소유하거나(부록 attr_name 중복·문서 2 §2.6 등급 판정·문서 0 §0.6 RPN 미결 등재) 담당 파일 밖(문서 4 §4.7-1 — 허브가 직접 처리함).

## E. 허브 직접 처리 3건 (2026-08-21)

| # | 정제본 위치 | 종류 | 내용 |
|---|---|---|---|
| 1 | 문서 4 §4.7-1 | [복원] | 문서 유래가 아닌 provenance 표기 — `auto:{규칙명}`·`seed`(§C 교차 잔여) |
| 2 | README 재구성 절차 5 | [정정] | 완료판정이 3조건으로 적혀 있어 문서 7 §7.6의 4조건과 어긋났다 → 4조건 + 범위 진술 병기 |
| 3 | README 개정 규칙 | [개정 규칙 신설] | ①모든 개정은 대장에 태그·근거와 함께 등재 ②개정은 회귀 스위트 통과로 확정 ③**정본 전환 시 승계 대조표 의무** — 이 규칙의 부재가 실 연결 규격 유실의 직접 원인이었다 |

---

## 다음 회차에 남는 것

- **회귀 스위트 실물 구축** — 조항 대조(원천 902 + 배경 797)·판정 재현(78문항 고정 시험지)·문면 기계 점검 스크립트. 지금은 규칙만 서 있고 자산이 1회용 상태다.
- **국면 2A의 첫 [개정]** — 실 연결에서 새 판정이 나오면 이 대장의 [개정] 칸이 처음 채워진다. 그때 원천과 정제본의 거리가 처음으로 벌어지며, 그것이 정상 경로다.


## F. 회귀 스위트 첫 실행 검출 1건 (2026-08-21 · [정정])

문면 기계 점검(`회귀스위트/점검_문면.py`)을 만들어 처음 돌리자마자 검출됐다.

| # | 정제본 위치 | 종류 | 무엇이 틀렸나 | 조치 |
|---|---|---|---|---|
| 1 | 문서 1 「사용법」 절 | [정정] | 절 전체가 **원천 카드의 운영 문단 그대로**였다 — "챕터를 쓸 때"·"이 카드의 문안이 현재의 진실이다"·"공백 현황(v11 기준)"·"명세 반영(v1.19)만 대기" 등 회차 이력과 원천 체계 어휘가 통째로 잔존. README 서술 규격("국면·일정·진행 상태는 이 묶음에 쓰지 않는다") 위반 | 「사용법 — 이 문서는 검사기다」로 교체: 본문 문서 작성 시의 조항 선행 확인 · 위계 · 부정형 grep · **그룹 간 교차 대조 지도(8행 표)**만 남기고 회차 이력 전량 제거. 개정 이력은 이 대장의 몫임을 명시 |

> **검출 경로가 요점이다.** 이 잔재는 P6 검증 4종·기저 승계 감사 797건 대조를 전부 통과했다 — 그 시험들은 "규범이 승계됐는가"를 물었지 "정제본이 자기 체계 안에서 말하는가"를 묻지 않았기 때문이다. 문면 규칙 8줄짜리 스크립트가 그것을 첫 실행에서 잡았다. **기계 점검이 값싸고 다른 것을 잡는다**는 증거다.

## G. 잔여 원천 4종 복원 25건 (2026-08-24 · [복원])

> 「실측 기록」으로 분류해 정제 대조에서 제외했던 4종(`실행검증_1차`·`판정대조_ipqc_1차`·`skeleton_seed`·`구간별_프롬프트`)을 재검한 결과 — **그 분류가 오분류였다.** 넷 다 규범이 섞인 혼합 문서였고(skeleton_seed는 사실상 규범 문서), 미승계 16건이 나와 교차 반영 포함 24건을 복원했다.

| # | 정제본 위치 | 원천 | 복원 내용 |
|---|---|---|---|
| 1 | 정제본2_계약.md §2.4 ④ content — 서술 | 판정대조_ipqc_1차.md · §7 개정 지시 3 (근거 §3-2) | **판별 기준은 "자유 서술"만이 아니다 — 필드 단위 규칙 처리가 불가능하고, 값이 아니라 문장으로 읽히며, 청크로 보존해 노드에 연결할 것이면 content다. 짧아도 조치·설명·대응 계열이면 content다**(길이는 기준이 아니다). 근거: 길이를 기준으로 삼으면 짧은 정형 문구(부적합 조치·대응계획 계열 |
| 2 | 정제본2_계약.md §2.4 ⑥ edges 선언 (스키마 레벨) | 판정대조_ipqc_1차.md · §4 판정 및 §5.3 · §7 개정 지 | **`mirrors`는 edges에 선언하지 않는다** — 극성 대칭은 필드 사이의 관계가 아니라 build 저장 직전에 자동 규칙(config.mirrors)이 polarity 필드 비교로 산출하는 관계다(자동 규칙 경로는 문서 4). 근거: 선언을 허용하면 근거 1쌍만 관찰한 표본의 특이점이 그 doc_typ |
| 3 | 정제본1_금지와불변_초안.md I5 (카드 승계 원문에 병기 — 원천 칸에 "· (구) 판정대 | 판정대조_ipqc_1차.md · §4 판정 및 §5.3 · §7 개정 지 | — **mirrors는 매칭 스키마 edges에도 선언하지 않는다**: 극성 대칭은 build 저장 직전 자동 규칙(config.mirrors)의 산출이라 스키마 선언 대상이 아니며, 이 병기가 없으면 relation_patterns 한정 금지가 edges 허용으로 읽혀 근거 1쌍짜리 mirrors 엣지가 선언된 |
| 4 | 정제본2_계약.md §2.2 계약 ① — payload 고정 키 4종 항목 | 실행검증_1차.md · §3.2 채택된 분할 규칙 | **`section`의 값은 헤딩 경로를 `" > "`(공백·꺾쇠·공백)로 이어 만든다** — 예: `"1. 개요 > 1.1 노칭"`. 이음 문자는 어댑터 재량이 아니라 계약이다: section은 chunk_id 산식의 성분이므로(문서 7) 구현마다 이음이 갈리면 같은 문서가 다른 chunk_id를 낳아 재인입  |
| 5 | 정제본1_금지와불변_초안.md N3 (카드 승계 원문에 병기 — 원천 칸에 "· (구) 실행검 | 실행검증_1차.md · §3.2 채택된 분할 규칙 | — **성분 `section`의 문자열 구성도 계약이 정한다: 헤딩 경로를 `" > "`로 잇는다**(문서 2 §2.2). 이음 문자를 구현이 각자 정하면 같은 문서가 다른 chunk_id를 낳아 멱등성(H8)이 깨진다 |
| 6 | 정제본3_구조.md §3.4 공정정보층 — 카테고리 3종 · Unit 정의문 | 판정대조_ipqc_1차.md · §5.1 및 §8 가결정 D-25 | **[사내 확인] 검사·계측 장비의 포섭 여부는 유보다** — 정의문의 "공정을 **수행하는**"이라는 한정이 검사성적서·계측기록 계열 문서가 지목하는 검사·계측 장비를 배제하는지가 확정되지 않았다. 그 계열 문서를 넣으려면 정의문 개정이 선행되어야 하며, 현행 정의문은 그대로 두고 해당 장비는 Unit으로 받되 |
| 7 | 정제본3_구조.md §3.9 ⑹ 좌표 태깅 닫힌 목록 = 골격 전 노드 | skeleton_seed.md · 문법 요약 7항 (auto alias  | **극성 인스턴스의 auto alias는 loader가 두 형태로 생성한다 — `"{극성영문} {이름}"`·`"{극성한글} {이름}"`**(인스턴스당 2건). 문서·관행의 극성 수식 표기(예: `"양극 탭용접"`)는 이 산식이 커버하므로 **사람이 seed ALIASES에 적지 않는다.** 근거: 형태가 명세에  |
| 8 | 정제본1_금지와불변_초안.md D5 (카드 승계 원문에 병기 — 원천 칸에 "· (구) ske | skeleton_seed.md · 문법 요약 7항 (auto alias  | — auto alias 생성 산식은 인스턴스당 두 형태(`"{극성영문} {이름}"`·`"{극성한글} {이름}"`)이며, 사람은 극성 수식 표기를 seed ALIASES에 적지 않는다(형태가 명세에 없으면 파서의 좌표 태깅과 에이전트의 해소가 서로 다른 표기를 기대한다 — 문서 3 §3.9 ⑹) |
| 9 | 정제본2_계약.md §2.1 규약 4 (공유 스냅샷 — 소유처 참조만 추가) | skeleton_seed.md · 문법 요약 7항 (auto alias  | (auto alias 포함 — seed에 없는 파생 표기까지. **auto alias의 생성 산식은 문서 3 §3.9 ⑹**) |
| 10 | 정제본3_구조.md §3.9 ⑹ 좌표 태깅 닫힌 목록 = 골격 전 노드 | skeleton_seed.md · 문법 요약 7항 (ALIASES 블록  | **seed의 ALIASES 블록에 사람이 적는 것은 사람이 보증한 초기 표기뿐이다** — 키로 짧은 이름을 허용하되, 그 키가 골격 안에서 모호하면 loader는 조용히 하나를 고르거나 버리지 않고 **접두 표기를 요구하며 명시적으로 실패한다**(3.3 명시적 실패). 근거: 위 등재 필터는 등재 시점의 필터라 |
| 11 | 정제본1_금지와불변_초안.md F7 (확장 조항에 병기 — 원천 칸에 "· (구) skelet | skeleton_seed.md · 문법 요약 7항 (ALIASES 블록  | — 사람이 seed ALIASES에 적은 키도 모호하면 조용히 통과시키지 않는다: loader가 접두 표기를 요구하며 명시적으로 실패한다(등재 필터는 등재 시점의 필터라 입력 검증을 대신하지 못하고, 버려진 표기를 작성자가 모른다) |
| 12 | 정제본1_금지와불변_초안.md J3 (카드 승계 원문에 병기 — 원천 칸에 "· (구) 구간별 | 구간별_프롬프트.md · G4 본편 J3 문단 · 전체 완료 판정 302 | — 판정이 깨진 회차에 그 사실을 숨기거나 판정 기준점을 옮겨 통과로 맞추지 않는다: 깨진 그대로 보고하며, 그 보고는 실패가 아니라 관찰 기록으로 다룬다(기준점 이동 자체는 J30의 단독 커밋이 구조로 막았고 남은 구멍은 보고 측이다) |
| 13 | 정제본7_구현규격과검증.md §7.5 mock 체계와 시나리오 15종 — 「mock 자산의 자 | 실행검증_1차.md · §6 신규 가결정 D-28 (근거 §1-③) | **창작 표본의 최소 규모**: 등록 리허설에 쓰는 창작 표본은 **정형 24행 이상 · 비정형 25행 이상 · PPT 8슬라이드 이상**으로 만든다 — 관찰 창(정형 head ~10줄 / 비정형 10~30줄 — 문서 6 §6.5)이 시험되려면 문서가 head보다 커야 하기 때문이다. 하한이 없으면 head =  |
| 14 | 정제본0_기반과원칙.md §0.6 미결 등록부 — 표 마지막에 「—」 행 1개 추가 | 판정대조_ipqc_1차.md · §5.1 및 §8 가결정 D-25 | \| — \| **Unit 정의문의 포섭 범위 — 검사·계측 장비.** 현행 정의문("공정을 **수행하는** 물리 장비" — 문서 3)이 검사설비·계측기를 포섭하는지가 미정이다. 검사성적서·계측기록 계열 문서를 넣으려면 **Unit 정의문 개정이 선행되어야 한다** — 그때까지는 Unit으로 두되 이 유보를 기록한다 |
| 15 | 정제본6_파서와구축모드.md §6.7 어댑터 생성 킷 6종 — 킷 1(생성 프롬프트 템플릿)  | 판정대조_ipqc_1차.md · §4 판정 및 §5.3 · §7 개정 지 | ·**`mirrors`는 매칭 스키마 edges에 선언하지 않는다** — build 저장 직전의 자동 규칙(config.mirrors) 소관이므로, 골격 소유 관계의 edges 선언 허용(문서 4 §4.4)은 여기까지 미치지 않는다: 금지 문면이 없으면 근거 1쌍만 관찰한 판정자가 mirrors 엣지를 선언해도  |
| 16 | 정제본6_파서와구축모드.md §6.7 어댑터 생성 킷 6종 — 킷 1(생성 프롬프트 템플릿)  | 판정대조_ipqc_1차.md · §7 개정 지시 7 | ·**표본이 1부면 보수적으로 선언한다 — 변형을 관찰하지 못했으므로 근거가 1건뿐인 관계는 edges에 선언하지 말고 "판정 불가"로 올린다** — 1부 경고 표시(6.5)는 사후 표시라, 사전 억제가 없으면 근거 1건짜리 엣지를 걸러내는 부담이 전부 검수자에게 남는다(preflight는 양식 표류를 잡지 근거 |
| 17 | 정제본6_파서와구축모드.md §6.5 doc_type 등록 입력 패키지 — 시스템 공급 표 「 | 판정대조_ipqc_1차.md · §7 개정 지시 6 | **여기에 존재하는 층의 목록도 함께 제시한다** — 걸침 필드의 `target_layer`(문서 2 §2.5 규약 2) 판정의 전제다: 지정 층의 어휘만 주면 다른 층을 가리켜야 하는 필드에서 어떤 층이 존재하는지 제시받지 못해 판정이 막히고(실측), 걸침 필드가 무조건 스키마 헤더 layer로 폴백된다 |
| 18 | 정제본6_파서와구축모드.md §6.7 어댑터 생성 킷 6종 — 킷 6(실행 하네스) 행 말미에 | 실행검증_1차.md · §2 실행 하네스 | **호출 규격 — 진입점 `run_adapter`, 인자 순서 `<adapter.py> <schema.json> <문서…>`**: 진입점 이름과 인자 순서를 명세가 정하지 않으면 외부 구현자가 임의 진입점을 만들어, 사내가 반입받은 하네스를 무엇으로 부르는지가 문서로 답해지지 않는다(mock 자산의 자리를 경로  |
| 19 | 정제본7_구현규격과검증.md §7.2 두 축 id — 산식 불릿 목록에 「record_id의  | 실행검증_1차.md · §3.2 채택된 분할 규칙 | - **`section`은 헤딩 경로를 `" > "`(공백·꺾쇠·공백)로 이어 구성한다** — 표기도 산식의 일부다: `section`은 chunk_id의 해시 입력 성분인데 이음 문자가 어댑터·분할기 구현마다 갈리면 같은 문서가 다른 chunk_id를 낳아 재인입 멱등성이 깨진다. |
| 20 | 정제본7_구현규격과검증.md §7.5 — 「S13이 부정 경로를 함께 지는 이유」 다음, 「m | 구간별_프롬프트.md · P트랙 P1 (S14 판정 산식) | **S14의 대조 산식 — 비교 제외 필드**: 역산 정합의 대조에서 **실행 시점 값(`parsed_at`·`source_path`·`parser_version`·`adapter_version`)과 `process_no`는 비교에서 제외한다.** 앞의 넷은 매 실행·환경·개정마다 달라지므로 "파서 산출 = 수기 |
| 21 | 정제본7_구현규격과검증.md §7.5 — 같은 「S14의 대조 산식」 문단의 마지막 문장 (번 | 판정대조_ipqc_1차.md · §6.2 및 §8 가결정 D-24 (pr | `process_no`를 제외하는 사유는 따로다 — parsed 계약의 검증 대상이 아니므로, 자산 쪽에서 번호 체계가 재정렬되어도 역산 정합이 파서 결함이 아닌 사유로 실패해서는 안 된다. |
| 22 | 정제본7_구현규격과검증.md §7.4 「진행 규칙 (장부)」 — 기록 형식 다음, 「발번처는  | 구간별_프롬프트.md · 공통 규칙 7행 | **BLOCKERS 항목에는 3요소와 함께 판단자가 고를 선택지 2~3을 동봉한다** — 3요소는 상태 서술이라 그것만 올리면 판단자가 결정하려고 선택지를 처음부터 구성해야 하고, 멈춤 1건마다 왕복이 한 번씩 더 든다. |
| 23 | 정제본7_구현규격과검증.md §7.6 구현 국면의 완료판정 — 조건 3(config-only  | 구간별_프롬프트.md · G4 본편 J3 문단 · 전체 완료 판정 302 | **판정이 깨진 것으로 나오면 그대로 보고한다 — 숨기거나 기준점을 옮겨 맞추지 않는다.** 판정이 커밋 1회로 잠겨 있어 경계를 옮기는 것 말고는 결과를 바꿀 방법이 없으므로, 이 금지가 없으면 층 추가가 코어를 건드렸다는 실측이 통과 보고 뒤로 사라진다. |
| 24 | 정제본부록_용어.md §1 기본 구조 — 「alias (표면형)」 다음, 「동의어 사전」 앞에 | skeleton_seed.md · 문법 요약 7항 (auto alias  | \| **auto alias** \| 사람이 seed에 적지 않아도 시스템이 골격 인스턴스에서 파생해 사전에 싣는 표면형. **극성 수식 표기의 생성 산식은 `"{극성영문} {이름}"`·`"{극성한글} {이름}"` 두 형태다** — 그래서 인스턴스의 극성 수식 표기는 seed ALIASES에 적지 않는다. 산식이 문 |
| 25 | 정제본부록_용어.md §2 문서와 인입 — 「content (서술)」 행의 뜻 칸에 판별 기준 | 판정대조_ipqc_1차.md · §7 개정 지시 3 (근거 §3-2) | **판별 기준: 필드 단위 규칙 처리가 불가능하고, 값이 아니라 문장으로 읽히며, 청크로 보존해 노드에 연결할 것.** **짧아도 조치·설명·대응 계열이면 content다** — "자유 서술"만으로 판별하면 짧은 정형 문구가 attribute로 배정되어, 예시 칸의 대응계획 열과 같은 성격의 열이 문서마다 다른  |

**주요 항목**: `section`의 헤딩 경로 이음 문자 `" > "`(chunk_id 산식 성분 — 이음이 갈리면 멱등성이 깨진다) · auto alias 생성 산식 2형태 · seed ALIASES 모호 키의 명시적 실패 · content 판별 기준 보강("짧아도 조치·설명·대응 계열이면 content") · **mirrors는 매칭 스키마 edges에 선언하지 않는다** · 창작 표본 최소 규모(정형 24행·비정형 25행·PPT 8슬라이드) · S14 대조의 비교 제외 필드 · Unit 정의문의 검사·계측 장비 유보([사내 확인] 등재) · 킷 하네스 호출 규격.

**건너뜀 16건**: 담당 밖이거나 mock 자산 층의 배정 세부(정제본이 소유하지 않는 범주).

