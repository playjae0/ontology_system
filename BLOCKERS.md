# BLOCKERS.md — 멈춤 사유 대장

> **기록 규칙**(CLAUDE.md 진행 규칙): 카드·계약과 **충돌**하는 지점만 여기 온다. 그 외 세부는 `DECISIONS.md`에 가결정으로 기록하고 전진한다.
> 항목에는 **판단 재료 3요소**를 첨부한다 — ①근거 원문(어느 문서 어느 절) ②선택지 2~3개 ③각 선택지의 귀결.

## 현재 상태: **멈춤 사유 0건** · **판정 필요 0건** — 1~6 전건 해소

A0(G0) 수행 중 카드·계약과 충돌하는 지점은 발생하지 않았다. 반입 대조 9건의 상충은 13회차에서 전건 판정되어 `CLAUDE.md` 「증분 0 v1.4 정정 5항」과 각 문서에 반영을 마쳤으므로 재발의하지 않는다(MANIFEST 「넣은 뒤 확인」 지시).

---

## 판정 필요 (멈춤은 아니나 사용자 확인 대기)

`PROGRESS.md` §7과 같은 내용이다. 진행을 막지 않으므로 BLOCKERS 등급은 아니지만, 답에 따라 뒤 구간의 분량이 달라진다.

### ~~판정필요-1~~ · 이전 레포의 1a~1c 구현물 소재 — **해소 (사용자 판정 08-07)**

**판정: 선택지 (다) — 전량 신규 작성.** 이전 레포 회수 여부와 무관하게 기존 단위 1a~5를 전량 신규 작성한다. 회수분이 있더라도 **참조용 한정**이며 코드로 반입하지 않는다.

**근거**: 구 구현물은 계약 v1 산물(chunk_id 발급 · N#### 의미 축 id · 커밋 게이트 없음)이라 v2 이식 비용이 신규 작성 비용을 넘고, 낡은 전제가 코드로 유입될 위험이 있다.

**귀결**: `DECISIONS.md` **D-32**로 기록(가결정이 아니라 **확정**). A0가 확정한 "전부 미완료"가 그대로 실질이 된다. `git diff core/` 완료판정(J3)의 기준선이 "빈 core/"이므로 G4 판정의 의미가 명확해진다. G1~G3의 실질 분량은 "개수(改修)"가 아니라 신규 작성이다.

### ~~판정필요-2~~ · `mock/raw/CP01.xlsx` C11 삽입 시점 — **해소 (G1에서 실행, 08-07)**

**결과: 선택지 (가) — G1 착수 첫 작업으로 삽입.** 프롬프트 문면 그대로 처리했다.

11번째 record 자리(14행)에 coord_mismatch 행을 **삽입**하고 확대분을 C12~C29로 밀었다. 덧붙임이 아니라 삽입이어야 prefix 11이 성립한다. 28 → **29 record**, C1~C10 불변.

**귀결**: `verify_roundtrip` **prefix 11 PASS**(50항목 전량, FAIL 0). 이것이 D-18의 진짜 판정이며 A0 시점의 prefix 10 결과를 대체한다.

**파생 미결 — D-33 후보**: C11의 설비·관리항목을 문서가 지정하지 않는다(`raw_mock_대장` v2.1 §2.1은 메커니즘만 명세하고 나머지 열은 비워둔다). 레코드가 자기완결하려면 값이 있어야 해 **`노칭 프레스 / 노칭 피치 / 50±0.2mm`**로 정했다 — 설비는 기존 노드를 재사용해 새 Unit이 생기지 않고, 관리항목은 신규이나 어서션이 거는 "적층 정렬도"·"금형 클리어런스"를 피했다. **DECISIONS 등재 여부는 사용자 확인 대기.**

### ~~판정필요-3~~ · ① seed 경로가 커밋 게이트를 우회한다 — **해소 (명세 트랙 판정 08-13)**

**판정: (나) — 우회가 아니라 설계다.** seed는 후보가 아니라 **선언**이다(틀 §4B-A3 경로 ①). 게이트는 추출 후보(경로 ③)를 검증하는 장치이므로 골격 엣지가 지나지 않는 것이 정상이고, 층의 관계 선언은 config `relations` 배열이 이미 한다 — `relation_patterns`는 **별개의 추출용 게이트 패턴**이다.

**귀결**: `DECISIONS.md` **D-52**. gate.py 독스트링을 정정하고 죽은 `PATH_SEED` 상수·분기를 제거했다. config에 주석 1줄을 넣고 회귀 4항으로 잠갔다(패턴표에 골격 관계 부재 / 경로 ②③④ 어느 쪽도 커밋 불가 / loader가 gate를 부르지 않음 / 골격 엣지 status=seed·거부 로그 0). **반입 시 표에 `Process precedes Process`가 이미 있어** 경로 ②가 골격 흐름을 쓸 수 있었고, 소비처 0곳임을 확인하고 제거했다.

<details><summary>원 발의 (판단 재료 3요소)</summary>


**①근거 원문.** `CH3A 3.4` / `core/gate.py` 독스트링: *"엣지가 생기는 경로는 넷뿐이고 **넷 모두 같은 관문**을 지난다: ① seed(사람·골격) ② edges 선언 ③ 추출 후보 ④ 자동 규칙"*, *"①②④는 정의상 패턴 안이라 무비용 통과"*. 그런데 `core/bootstrap.py`는 골격 엣지를 `g.add_edge()`로 **직접** 쓴다 — ①은 관문을 지나지 않는다. 게이트에 `PATH_SEED` 상수만 있고 호출부가 없다.

동시에 `layers/process/config.json`의 `relation_patterns`에 **골격 자신의 관계가 선언되어 있지 않다**: `Process -part_of-> Process`도 `Process -mirrors-> Process`도 없다(선언된 part_of는 `Unit→Process` 하나뿐). 즉 ①을 지금 게이트에 태우면 **골격 45 + mirrors 16 엣지가 전부 `invalid_pattern`으로 거부**된다. 두 결함이 서로를 가려 왔다 — 우회하고 있어서 선언 누락이 드러나지 않았다.

**②선택지.**
- (가) **패턴 선언을 보강하고 ①을 게이트에 태운다.** config에 `Process part_of Process`(정의문: 공정이 상위 공정에 속한다) · `Process mirrors Process`(symmetric) 추가 + `bootstrap`이 `gate.commit_edge(..., PATH_SEED)`를 부른다.
- (나) **①을 게이트 밖으로 명문화한다.** "seed는 사람이 파일로 선언한 것이라 관문의 대상이 아니다"로 CH3A 3.4·gate.py 독스트링을 고치고, 패턴표는 **문서 유래 엣지의 표**로 용도를 좁힌다.
- (다) 현행 유지(코드와 문서가 어긋난 채로).

**③각 귀결.**
- (가): 4경로가 실제로 한 관문을 지난다 — I7("거부가 잦으면 고칠 것은 코드가 아니라 config")이 골격에도 성립한다. 대신 패턴표가 넓어져 **경로 ③(추출)도 `Process part_of Process`를 커밋할 수 있게 된다** — 문서에서 골격을 자라게 하는 경로가 열리는 셈이라, 그것을 막으려면 경로별 패턴 구분이 새로 필요하다.
- (나): 선언이 좁게 유지되고 골격은 seed 파일이라는 단일 원천에만 종속된다(J12와 정합). 대신 "넷 모두 같은 관문"이라는 서술이 셋으로 줄어든다.
- (다): 다음 사람이 gate.py 독스트링을 믿고 ①을 태웠다가 골격 전량 거부를 만난다.

**당시 상태**: (다)로 전진했다 — 판정 없이 패턴표를 넓히는 것은 게이트의 의미를 조용히 바꾸는 일이라 하지 않았다.
</details>

### ~~판정필요-4~~ · seed 형식 v3.2의 트리 블록 키가 `PROCESS_TREE`다 — **해소 (명세 트랙 판정 08-13)**

**판정: (가) — 개명한다.** `PROCESS_TREE` → `TREE`, `POLARITY_LABELS` → `AXIS_LABELS`. B1은 최상위 불변식이고, 상수 격리는 위반을 유지한 채 숨기는 것이다. seed 실물 1개·의존 코드 1곳인 지금이 가장 싸다.

**귀결**: `DECISIONS.md` **D-53**. seed 실물·loader 상수·seed 문서의 JSON 블록을 함께 고쳤고 개명 후 기대값 5종(46/45/22/16/8) 불변을 확인했다. 노드 필드 `polarity`·config `polarity` 블록은 A11-8의 시스템 어휘라 **그대로 둔다**. 판정문 §1-12·§4·§8·대장의 형식 서술 갱신은 **명세 트랙 몫**이다.

<details><summary>원 발의 (판단 재료 3요소)</summary>


**①근거 원문.** 카드 `B1`: *"core에 층 어휘 0"*. M2 §1-12·§4: seed v3.2 = 단일 파일 `skeleton.json`(**PROCESS_TREE** + ALIASES + POLARITY_LABELS). 판정 A는 *"docs/skeleton_seed_v3.2.md의 JSON 블록 **그대로**"* 반입을 지시했으므로 실물은 `PROCESS_TREE`다. loader는 그 키를 읽어야 하고, 따라서 **`core/bootstrap.py`에 특정 층의 이름이 상수로 들어간다**(`KEY_TREE = "PROCESS_TREE"`).

M2 §11-2는 도메인 특수 잔재를 "축의 이름·값" 하나로 수렴시켰는데, **이것은 그 목록에 없는 네 번째 잔재**다. 게다가 축과 달리 config·seed의 *데이터*가 아니라 **형식의 키 이름**이라 config화로 격리되지도 않는다 — 품질층이 `tree-v3.2`를 쓰게 되면 그 seed도 `PROCESS_TREE`라는 키를 써야 한다.

**②선택지.**
- (가) **seed 형식에서 `PROCESS_TREE` → `TREE`로 개명**(v3.3). 층 이름이 형식에서 사라진다.
- (나) config `skeleton` 블록이 키 이름을 값으로 선언한다(예: `"tree_key": "PROCESS_TREE"`). 코드에서는 사라지지만 형식에는 남는다.
- (다) 현행 유지 + B1에 예외 1건을 명시(`electrode_type`과 같은 등급).

**③각 귀결.**
- (가): 가장 깨끗하다. 비용은 seed 실물 1줄 + loader 상수 1줄 + 판정문·seed 문서의 형식 서술. **확정본을 개정하는 것이라 명세 트랙의 승인이 필요하다.**
- (나): 코드는 깨끗해지나 판정 A가 준 config 블록에 키가 하나 늘고, "형식의 키 이름을 층이 정한다"는 층별 형식 분기의 입구가 된다.
- (다): 비용 0이나 B1 예외가 하나 늘고, 그 예외는 `electrode_type`과 달리 **일반화 경로(identity_axis)가 없다.**

**당시 상태**: (다)로 전진했다 — 실물이 확정본이라 임의 개명이 불가하다고 보고 상수 1곳에 격리했다.
</details>

### ~~판정필요-5~~ · A11-9 ①의 부착 경로가 mirror·정체성 규칙과 어긋난다 — **해소 (판정 = 틀 v2.7·카드 v14 / 구현 = G4 0단계, 08-13)**

**판정: 결함 A는 (가)의 앞 절반 · 결함 B는 (나).** 명세 트랙이 틀 v2.7·카드 v14·CH3B v2.2로 이미 내렸고 G4 0단계는 그 **구현 캐치업**이다. 새 선택지를 발의하지 않았다.

**귀결**: `DECISIONS.md` **D-56**(F3 하향 연쇄 — 짝 키를 부모·이름부·polarity 세 요소로, 부모가 mirror 쌍이면 동일시) · **D-57**(A11-9 ① 생략을 스코프 카테고리 한정 · Unit은 F1 유지 · `canonical_scope`는 불변).
**실측**: canonical 중복 **0건**(`초음파 융착기` / `anode 초음파 융착기`로 갈림) · 짝 키 정규화 성립(`탭용접::cathode` → `탭용접`) · **오검출 0건**. 회귀는 개수가 아니라 **구조**로 잠갔다 — 큐에 오른 항목마다 반대 극성 노드를 직접 찾아 정말로 짝이 없는지 확인한다.
**남은 관찰 1건**: 큐가 3건에서 1건으로 내려오리라던 정지점 4의 예측은 성립하지 않았다 — 상세는 `PROGRESS.md` G4 0단계 보고. 요지는 **오검출은 사라졌고 남은 3건이 전부 참**이며, 페어링 규칙 자체는 짝을 만들어 주면 즉시 성립함을 실증했다(합성 쌍 투입 시 큐 1건).

<details><summary>원 발의 (판단 재료 3요소)</summary>


**①근거 원문.** 틀 `§4B-A11-9 ①`: *"부착 대상 골격 노드의 polarity가 none이 아니면 표면형 극성 결합을 생략한다 — **극성이 이미 주소(스코프 접두)에 있으므로**"*. 카드 `F3`: *"같은 부모(**또는 부모가 mirror 쌍**) + 자기 이름부 동일 + polarity 반대 → mirrors 자동 페어링(**하향 연쇄**)"*. 카드 `D-40`(레포): *"극성이 다르면 감점이 아니라 **후보에서 제외**"*.

**결함 A — 하향 연쇄 미구현 → mirror_asymmetry 오검출.**
`탭용접::cathode::용접 강도`의 짝은 `탭용접::anode::용접 강도`인데, 짝 찾기 키(`base_canonical`)에 **부모 주소가 그대로 들어간다.** 두 노드가 모두 있어도 키가 달라 **절대 페어링되지 않고**, A11-9 ①로 부착된 모든 노드가 편측으로 큐에 오른다. F3의 "부모가 mirror 쌍이면 하향 연쇄" 절이 코드에 없다. 실측 오검출 2건(`초음파 융착기` · `탭용접::cathode::용접 강도`).

**결함 B — Unit에는 주소가 없다 → canonical 중복 노드.**
A11-9 ①의 전제("극성이 이미 주소에 있다")는 **스코프가 붙는 카테고리에서만 참**이다. `canonical_scope.bind_categories = ["Property"]`이므로 **Unit의 canonical에는 주소가 들어가지 않는다.** 결과: `초음파 융착기`(polarity=none, C5 유래)와 `초음파 융착기`(polarity=cathode, C12 유래)가 **같은 canonical·같은 category·같은 층에 공존**한다 — D-40이 후보에서 제외하고, A11-9 ①이 이름을 안 바꾸기 때문이다. 이름으로 구별되지 않는 두 노드는 노드 유일성(P4)의 취지에 어긋난다.

**②선택지.**
- (가) **하향 연쇄 구현 + Unit 스코프 확대.** 짝 키에서 부모의 축값을 걷어내고(부모가 mirror 쌍일 때만), `canonical_scope.bind_categories`에 Unit을 추가한다. core 분기 2곳 증가.
- (나) **A11-9 ①의 적용 범위를 스코프 카테고리로 좁힌다.** 주소가 이름에 실제로 들어가는 카테고리(Property)에만 결합 생략을 적용하고, Unit은 종전대로 표면형 결합(`cathode 초음파 융착기`). core 분기 1곳, 결함 B만 해소.
- (다) 현행 유지 + 오검출을 알려진 것으로 두고 검수 뷰에서 거른다.

**③각 귀결.**
- (가): 규칙이 문면대로 돈다. 대신 Unit canonical이 전부 스코프를 갖게 되어 **기존 Unit 노드의 이름이 바뀐다**(`노칭 프레스` → `노칭::노칭 프레스`) — 개명은 I1 경로이고 G3 기대값 다수가 움직인다.
- (나): 결함 B가 뿌리에서 사라지고 Unit은 극성이 이름에 남아 D-40과 정합한다. 대신 A11-9 ①의 문면("polarity ≠ none이면 생략")에 카테고리 단서가 붙는다 — 틀 개정이 필요하다.
- (다): 큐에 상시 오검출이 섞인다. mirror_asymmetry는 "문서 편측 갱신"을 잡는 장치인데 그 신호가 희석된다.

**당시 상태**: (다)로 전진했다 — 셋 다 core 변경이나 틀 개정을 요구했다.
</details>

### ~~stale_skeleton 큐 신설~~ — **미신설로 종결 (명세 트랙 판정 08-13)**

3단계가 제안한 신규 큐 kind는 **만들지 않는다**(`DECISIONS.md` **D-54**). 큐 20종은 닫힌 목록이고, 골격 잔존은 운영 상시 유입이 아니라 **일회성 마이그레이션 산물**이다 — 개발 국면의 정본 경로는 **깨끗한 재빌드**, 운영 국면의 정본 경로는 **I1 개명**(id 불변 · 옛 canonical은 alias 강등)이므로 잔존은 구조적으로 발생하지 않는다. loader의 ⚠ 경고 + `defects.log`는 **개발 안전망**으로 유지한다.

---

### ~~판정필요-6~~ · 같은 대상이 좌표 **해상도**에 따라 두 canonical로 갈린다 — **해소 (사용자 판정 08-19)**

**판정: (가) — 하강 부착.** 좌표가 개념이고 record의 축값이 확정이면 **동일 극성 인스턴스가 존재할 때** 그리로 하강해 부착한다. 하강 후에는 A11-9 ①이 적용된다. **인스턴스가 없으면 하강하지 않고 F1 결합 유지**(임의 인스턴스 생성 금지 — 골격은 Tier1).

**귀결**: 틀 v2.10 §4B-A11-9 ⓪ · 카드 v18 F1 · CH3B v2.4 3.5 규약 3-⓪ 신설(21회차 반입). 구현은 `Builder.descend_anchor` 1곳이고 A11-5 순서 파생의 하강을 그대로 재사용한다. `DECISIONS.md` **D-75**. 종결 어서션 실측: `탭용접::cathode 용접 가압력` ↔ `탭용접::cathode::용접 가압력` 1쌍이 깨끗한 재빌드 후 **단일 노드**. G3 기대값 1항 이동(C8·C9의 극성 쌍 canonical이 F1 결합형 → 스코프형).

<details><summary>원 발의 (판단 재료 3요소)</summary>

**멈춤이 아니다.** G6.5는 이 항목 없이 완결되며, 판정은 뒤 구간(P트랙 인입 실측 또는 명세 트랙)에서 받아도 된다. 다만 **답에 따라 I축 병합 작업량과 mock 기대값이 달라진다.**

**①근거 원문.**
- 카드 **F1**: 극성 결합 3조건 — 표면형에 축값이 없고 record가 축값을 확정하면 표면형에 결합한다.
- 틀 **§4B-A11-9 ①**(카드 F1 v14 · CH3B v2.2 3.5 규약 3): *"부착 노드의 polarity가 확정이면 표면형 극성 결합을 생략하고 polarity를 상속한다"* — 생략의 근거는 **"극성이 이미 주소에 있다"**이고, 적용 범위는 스코프 카테고리(Property)로 한정된다(판정필요-5 ② 종결).
- 카드 **P4**(노드 유일성): 같은 실물이 두 노드로 서면 안 된다.

**실물.** `D1`(스키마 `target_layer` 소비) 수리로 PFMEA의 `control_item_*`이 품질층 복제본이 아니라 **공정층**에 해소되면서, 같은 대상이 두 이름을 갖게 됐다 — 수리 전에는 층이 갈려 있어 보이지 않던 충돌이다.

| 출처 | `process_ref` | `electrode_type` | 적용 규칙 | 결과 canonical |
|---|---|---|---|---|
| `PFMEA01-R7` | `탭용접` (**개념**) | `cathode` | 주소에 극성 없음 → **F1 결합** | `탭용접::cathode 용접 가압력` |
| `PPT01-C004` | `cathode 탭용접` (**인스턴스**) | `both` | 주소에 극성 있음 → **A11-9 ① 생략** | `탭용접::cathode::용접 가압력` |
| `CP01-C5` | `탭용접` (**개념**) | `both` | 축값 미확정 → 결합 없음 | `탭용접::용접 가압력` |

앞의 둘은 **같은 실물**(cathode 탭용접의 용접 가압력)이고 세 번째는 저해상도 부착이라 별개다. 규칙은 각각 문면대로 돌았는데 이름이 갈렸다 — **결함은 코드가 아니라 두 규칙의 접점에 있다.**

**②선택지.**
- **(가) A11-9 ①을 하강 규칙으로 확장한다.** 좌표가 개념이고 record의 축값이 확정이면 **그 축 인스턴스로 하강**해 부착한다(`탭용접` + cathode → `탭용접::cathode`). 그러면 두 경로가 같은 canonical에 착지한다.
- **(나) 현행 유지 + I축 병합으로 사후 처리.** 규칙은 그대로 두고, 갈린 쌍은 `uncertain_match`/검수 뷰를 통해 사람이 I2 병합한다.
- **(다) 문서 측 규약으로 막는다.** "record가 축값을 확정하면 `process_ref`도 인스턴스로 적는다"를 파서·어댑터 계약에 넣고, 어기면 `coord_mismatch` 계열로 표면화한다.

**③각 귀결.**
- **(가)**: 이름이 하나로 모여 P4가 지켜지고 걸침 병합이 완전해진다. 대신 **틀 개정**이며(A11-9 ①의 문면이 "생략"에서 "하강 후 생략"으로 바뀐다), 개념 좌표에 붙던 기존 노드 일부의 canonical이 움직여 G3 기대값이 재산정된다. core 분기 1곳 증가.
- **(나)**: 코드·틀 무변경. 대신 **같은 실물의 노드 2개가 상시 존재**하고 질의가 둘 다 링킹한다 — 병합 대기 항목이 문서 수에 비례해 늘고, 사람 손이 계속 든다.
- **(다)**: 규칙 접점을 아예 없앤다(개념+축값 조합이 입력에서 사라진다). 대신 **기존 mock·사내 문서가 그 형태를 실제로 쓰고 있어**(PFMEA01-R7이 그 실례) 재작성 부담이 크고, D-26(fixture 불변)과 부딪힌다.

**현 상태**: **(나)로 전진했다** — G6.5의 수리 원칙(§0-1 새 개념·새 규범 0)이 (가)·(다)를 금지한다. 실측 잔여는 공정층 `탭용접::cathode 용접 가압력` ↔ `탭용접::cathode::용접 가압력` 1쌍이다.
</details>
