# -*- coding: utf-8 -*-
"""칸 5.1~5.4 — **검증 뷰어**: 시스템 안의 화면 하나 (B82 · 문서 7 시각화 3형태).

    python run.py viewer [--port N] [--no-browser]

**플랫폼이 아니지만 플랫폼의 일부다.** 사내 서비스 창구는 별도 레포이고, 이것은
**이 시스템 안에서 「그래프와 답이 서로 맞는가」를 눈으로 보는 창구**다. 규율 넷:

- **화면은 엔진을 갖지 않는다**(PF11) — 질의·판정·인입은 시스템 함수를 부르고
  화면은 결과와 trace를 그린다. 골든셋 채점의 정본도 하나다.
- **쓰기 0** — 쓰기 라우트가 없다(`do_POST`를 두지 않으면 501로 거절된다).
  파생물에서 그래프를 고치는 경로는 없다(문서 1 P5)가 화면에도 적용된다.
- **외부 URL 0** — 렌더러(sigma.js·graphology)는 `static/vendor/`에 **고정 버전으로
  벤더링**한다. CDN을 부르지 않는다: 사내망에서 화면이 비어 뜨면 검증 도구가 아니다.
- **127.0.0.1에만 바인드** — 검증 도구이지 서비스가 아니다.

파일 셋: `server.py`(라우트·기동) · `data.py`(API 묶음) · `static/`(화면).
"""
